---
phase: 09-mapping-table-tenant-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db.ts
  - lib/db-mapping-tenant.ts
  - lib/db.mapping-tenant-migration.integration.test.ts
  - lib/repositories/import-mapping.repo.ts
  - lib/repositories/import-mapping.repo.unit.test.ts
  - lib/services/import-mapping.service.ts
  - lib/services/import-mapping.service.unit.test.ts
  - app/api/import-mapping/route.ts
  - app/api/import-mapping/[id]/route.ts
  - app/api/import-mapping/route.test.ts
  - test/repo-db.ts
autonomous: true
requirements: [TENANT-01]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A user in company A cannot read or mutate another company's timeline import mappings (cross-company 403)."
    - "Every timeline_import_mappings row belongs to a company; listings and unique names are scoped to that company."
    - "Existing timeline mapping data remains usable after backfill — no orphaned NULL company_id rows and no collapse of all tenants into one company."
    - "PUT/DELETE /api/import-mapping/[id] for a foreign existing row returns 403 { error: 'Forbidden' } (D-03)."
    - "GET /api/import-mapping returns only the session company's rows; empty list is 200 (D-03)."
    - "POST /api/import-mapping stamps company_id from the session actor, not from the request body (D-03)."
    - "An actor with null company_id cannot list, create, or mutate timeline mappings (403) (D-03)."
  artifacts:
    - path: "lib/db-mapping-tenant.ts"
      provides: "Idempotent per-table mapping tenancy migrate (nullable column, backfill, NOT NULL+FK, composite unique) — timeline spec in this plan"
      exports: ["migrateMappingTableTenancy"]
    - path: "lib/services/import-mapping.service.ts"
      provides: "Timeline list/create/update/delete with requireCompanyId + assertCompanyRow (D-04)"
    - path: "app/api/import-mapping/[id]/route.ts"
      provides: "Thin withAuth PUT/DELETE calling the service"
    - path: "lib/services/import-mapping.service.unit.test.ts"
      provides: "ForbiddenError vs NotFoundError ordering for timeline by-id (D-05)"
  key_links:
    - from: "app/api/import-mapping/[id]/route.ts"
      to: "lib/services/import-mapping.service.ts"
      via: "updateTimelineMapping / deleteTimelineMapping(params.id, actor)"
      pattern: "from '@/lib/services/import-mapping.service'"
    - from: "lib/services/import-mapping.service.ts"
      to: "lib/repositories/import-mapping.repo.ts"
      via: "getTimelineMappingById then mutate with companyId"
      pattern: "getTimelineMappingById"
    - from: "lib/db.ts"
      to: "lib/db-mapping-tenant.ts"
      via: "getDb migrate pass calls migrateMappingTableTenancy(pool) (D-01)"
      pattern: "migrateMappingTableTenancy"
  prohibitions:
    - collapsing every legacy mapping row onto one company when more than one company exists
    - answering a foreign existing timeline mapping id with NotFound
    - enforcing timeline template names unique across all companies
    - writing a tenant id taken from the JSON body
    - wrapping these company-global mapping routes in a project-scoped access helper
    - introducing a new company-access HTTP wrapper
    - moving schema init out of getDb
    - changing useImportMapping or ImportMappingDialog
---

<objective>
Close TENANT-01 for `timeline_import_mappings` as the production-quality tracer: schema (D-01, D-02) + repository `companyId` (D-04) + service tenant assert (D-03, D-04) + thin `/api/import-mapping` routes still on `withAuth` (D-04) + Vitest 4 403/list/create tests (D-05).

Purpose: Prove the architecture on one table before expanding to bug mappings and Jira tables. Locked CONTEXT decisions: D-01 migration order; D-02 single- vs multi-company backfill; D-03 403 on cross-company by-id and session-company lists; D-04 service assert + repo filter + keep withAuth; D-05 Vitest 4.

Output: Timeline mappings isolated by company; reusable `migrateMappingTableTenancy` for plans 09-02 and 09-03.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-mapping-table-tenant-isolation/09-CONTEXT.md
@.planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md
@.planning/phases/09-mapping-table-tenant-isolation/09-PATTERNS.md
@.planning/phases/09-mapping-table-tenant-isolation/09-VALIDATION.md

Flagged assumption (TENANT-01 edge probe unclassified): missing timeline id → NotFoundError / 404; foreign existing id → ForbiddenError / 403 (research A3). `is_admin` does not bypass mapping list scoping (research A2). Do not add a UI visual suite.

Do not implement deferred items: CPMO/PM/Viewer on these routes, DATA-01, ops/admin/config thinning, import dialog redesign.

<interfaces>
AccessActor (lib/http/with-auth.ts and lib/services/access.ts): { company_id: number | null; is_admin: number | boolean }.
withAuth already wraps import-mapping routes; catch tail maps ForbiddenError via serviceErrorResponse to { error: 'Forbidden' } status 403 (lib/api-errors.ts).
ForbiddenError / NotFoundError / ConflictError live in lib/services/errors.ts (no HTTP status field).
Analog assert ordering: programs.service.ts assertProgramAccess — missing row NotFoundError, wrong company ForbiddenError. Mapping rows have a single company_id; do not copy the admin-bypass branch from assertProgramAccess (D-03 session-company only).
Analog list SQL: listPortfolioMembers(companyId) WHERE company_id = ?.
Analog thin service: holidays.service.ts (repo alias imports, ConflictError on duplicate).
Current timeline repo (global): listTimelineMappings(), createTimelineMapping(name, mappingsJson), updateTimelineMapping(id, name, mappingsJson), deleteTimelineMapping(id).
Current [id] route: withAuth DELETE/PUT calling repo with params.id only.
createTimelineMappingSchema: name + mappings_json union string|record; frozen 400 { error: 'Missing fields' }. Extra JSON keys must remain ignored (do not mark the schema strict).
migratePostgresSchema is a string-array of idempotent SQL in lib/db.ts; procedural backfill belongs in a dedicated function (mirror backfillWeightedCompletion), not a single array string.
test/repo-db.ts DDL currently has no mapping tables — add timeline_import_mappings with company_id for integration tests.
hasTestDb / describe.skipIf(!hasTestDb) from test/db.ts and programs.repo.test.ts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="09-01-01">
  <name>End-to-end timeline mapping tenant deny — one path</name>
  <reversibility rating="one-way">Adding company_id, backfilling rows, then NOT NULL+FK+UNIQUE(company_id, name) on timeline_import_mappings is a data-format door; undoing it needs another migration. CONTEXT already accepted D-01/D-02 — do not re-ask.</reversibility>
  <files>lib/db-mapping-tenant.ts, lib/db.ts, lib/repositories/import-mapping.repo.ts, lib/services/import-mapping.service.ts, lib/services/import-mapping.service.unit.test.ts, app/api/import-mapping/[id]/route.ts, app/api/import-mapping/route.test.ts</files>
  <read_first>
  - .planning/phases/09-mapping-table-tenant-isolation/09-CONTEXT.md (D-01..D-05)
  - .planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md (backfill algorithm, Pattern 2 assert, Pitfall 1)
  - .planning/phases/09-mapping-table-tenant-isolation/09-PATTERNS.md (migrate helper, getTimelineMappingById, holidays/programs analogs)
  - lib/db.ts (migratePostgresSchema end ~500, getDb ~596, backfillWeightedCompletion ~506)
  - lib/repositories/import-mapping.repo.ts (timeline functions; leave bug functions' signatures unchanged)
  - app/api/import-mapping/[id]/route.ts
  - app/api/import-mapping/route.test.ts (401 fixtures — preserve)
  - lib/services/programs.service.ts (assertProgramAccess ordering only)
  - lib/services/holidays.service.unit.test.ts (vi.hoisted repo mocks)
  - lib/api-errors.ts (ForbiddenError → 403 body)
  </read_first>
  <behavior>
  - updateTimelineMapping / deleteTimelineMapping with actor.company_id 9 against a row whose company_id is 5 throws ForbiddenError and must not throw NotFoundError; repo mutate is not called (D-03, D-04).
  - Same methods with a missing row throw NotFoundError (D-03 / research A3).
  - Actor with company_id null throws ForbiddenError on by-id mutate (D-03).
  - PUT and DELETE /api/import-mapping/[id] with a session whose company_id is 9, when the service throws ForbiddenError, return status 403 and body { error: 'Forbidden' } (D-03, D-05).
  - Existing 401 no-session tests still pass (D-04 keep withAuth).
  </behavior>
  <action>
  TDD (D-05): write the failing service unit tests and the failing PUT/DELETE 403 route tests first; they must fail before production code exists. Then implement until green. Do not add a UI suite.

  Create lib/db-mapping-tenant.ts exporting migrateMappingTableTenancy(pool). This plan registers only the timeline_import_mappings spec. Per D-01 for that table: ADD COLUMN company_id INTEGER REFERENCES companies(id) (nullable); backfill every row; ALTER COLUMN SET NOT NULL; DROP INDEX IF EXISTS any name-only unique; CREATE UNIQUE INDEX on (company_id, name); CREATE INDEX on (company_id). Per D-02: COUNT companies; if one company, UPDATE NULL rows to that company's id; if more than one, INSERT copies via CROSS JOIN of still-NULL rows against companies then DELETE remaining NULL rows — never assign every leftover row to a single hardcoded company. If company count is 0 and NULL rows remain, skip SET NOT NULL this boot (retry next getDb, same spirit as backfillWeightedCompletion). Empty table: constraints still apply. Idempotent: safe to run twice (settings flag or no-op when zero NULL rows and column already NOT NULL). Do not use Prisma or an external migrate CLI (DATA-01 deferred).

  Wire getDb() in lib/db.ts: after migratePostgresSchema(pool), await migrateMappingTableTenancy(pool). Do not remove existing init/migrate.

  Repo (D-04): add getTimelineMappingById(id) as SELECT by primary key with no company predicate (service assert needs the row). Change updateTimelineMapping and deleteTimelineMapping to take companyId first and include AND company_id = ? on write. Leave list/create signatures for task 09-01-02. Leave all bug_* functions untouched. Replace the file header that currently describes the tables as global.

  Service (D-04): new lib/services/import-mapping.service.ts. requireCompanyId(actor) throws ForbiddenError when company_id is null. assertCompanyRow: missing row → NotFoundError; actor.company_id null or row.company_id mismatch → ForbiddenError. update/delete: getTimelineMappingById, assertCompanyRow, then repo with actor.company_id. Do not import next/server. Do not add admin list bypass (D-03).

  Route (D-04): [id]/route.ts stops importing the repo; PUT/DELETE call the service with params.id and actor. Keep withAuth. Do not add a project-scoped wrapper or a new company wrapper.

  Route tests: keep vi.mock of getSessionFromRequest; switch DELETE/PUT mocks from the repo module to the service module; add foreign-session 403 cases matching export/ppt/[id]/route.test.ts shape (D-05). Preserve all 401 assertions.

  Commit the tracer only after the automated verify is green.
  </action>
  <verify>
    <automated>npx vitest run lib/services/import-mapping.service.unit.test.ts app/api/import-mapping/route.test.ts -x</automated>
  </verify>
  <done>PUT/DELETE of a foreign existing timeline mapping is 403 Forbidden; missing id is NotFoundError at the service; withAuth 401 cases still pass; timeline table has company_id NOT NULL after migrateMappingTableTenancy (D-01..D-05).</done>
  <acceptance_criteria>
  - lib/db-mapping-tenant.ts exports migrateMappingTableTenancy and runs D-01/D-02 for timeline_import_mappings only
  - getDb() calls migrateMappingTableTenancy after migratePostgresSchema
  - getTimelineMappingById exists; update/delete SQL includes company_id bind
  - import-mapping.service.ts throws ForbiddenError (not NotFoundError) for cross-company by-id
  - [id]/route.ts imports from import-mapping.service, not import-mapping.repo
  - Route PUT/DELETE 403 test expects { error: 'Forbidden' }
  - npx vitest run lib/services/import-mapping.service.unit.test.ts app/api/import-mapping/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-01-02">
  <name>Scope timeline list and stamp create from session</name>
  <files>lib/repositories/import-mapping.repo.ts, lib/repositories/import-mapping.repo.unit.test.ts, lib/services/import-mapping.service.ts, lib/services/import-mapping.service.unit.test.ts, app/api/import-mapping/route.ts, app/api/import-mapping/route.test.ts</files>
  <read_first>
  - lib/services/import-mapping.service.ts (from 09-01-01)
  - app/api/import-mapping/route.ts
  - app/api/import-mapping/schema.ts (do not mark strict; frozen 400 body)
  - lib/repositories/import-mapping.repo.unit.test.ts (it.each create arity — split timeline vs bug)
  - lib/services/holidays.service.ts (ConflictError pre-check analog)
  </read_first>
  <behavior>
  - listTimelineMappings(actor) with company_id 5 calls repo with 5; with company_id null throws ForbiddenError and does not call repo (D-03, D-04).
  - createTimelineMapping(actor, name, json) inserts with actor.company_id; a body field named company_id is never passed to the repo (D-03).
  - Duplicate name for the same company throws ConflictError (maps to 409 via existing serviceErrorResponse) (D-01 unique).
  - GET /api/import-mapping with owner session returns the service list; GET still 401 without session (D-04, D-05).
  - POST still 201 for owner and 400 { error: 'Missing fields' } on schema failure.
  </behavior>
  <action>
  TDD first (D-05): extend service.unit.test.ts and route.test.ts with the behaviors above; then implement.

  Repo (D-04): listTimelineMappings(companyId) and createTimelineMapping(companyId, name, mappingsJson) with WHERE/INSERT company_id. Split the existing it.each in import-mapping.repo.unit.test.ts so the timeline create case uses the new arity; keep the bug create case on the old two-argument signature (09-02 owns bug).

  Service: listTimelineMappings(actor) via requireCompanyId. createTimelineMapping stamps company from actor only (D-03) — do not read a tenant id off the parsed body. Map unique-violation or a same-company name pre-check to ConflictError (holidays analog). requireCompanyId on create (D-03).

  Route GET/POST: pass actor into the service; stop importing the repo. Keep Zod safeParse and 201/400 shapes. Keep withAuth (D-04).

  Update GET/POST route tests to mock the service module instead of the repo (research Pitfall 5). Preserve 401 tests. GET owner test should still expect the same JSON list shape.
  </action>
  <verify>
    <automated>npx vitest run lib/services/import-mapping.service.unit.test.ts lib/repositories/import-mapping.repo.unit.test.ts app/api/import-mapping/route.test.ts -x</automated>
  </verify>
  <done>List is session-company only; create stamps session company; in-company duplicate name is ConflictError; 401 and 400 shapes unchanged (D-03, D-04, D-05).</done>
  <acceptance_criteria>
  - listTimelineMappings repo SQL contains WHERE company_id = ? with the bind as companyId
  - create INSERT includes company_id from the service actor
  - GET/POST route.ts imports import-mapping.service, not import-mapping.repo
  - Service unit covers null-company ForbiddenError on list and create
  - npx vitest run lib/services/import-mapping.service.unit.test.ts lib/repositories/import-mapping.repo.unit.test.ts app/api/import-mapping/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-01-03">
  <name>Prove two-company timeline backfill and unique-per-company</name>
  <precondition>TEST_DATABASE_URL may be unset — suites must use describe.skipIf(!hasTestDb) so the file still loads; when the URL is set, assertions must run (not skip).</precondition>
  <files>lib/db.mapping-tenant-migration.integration.test.ts, test/repo-db.ts, lib/repositories/import-mapping.repo.test.ts, lib/db-mapping-tenant.ts</files>
  <read_first>
  - lib/db-mapping-tenant.ts (09-01-01)
  - test/repo-db.ts (DDL string, seedCompany, setupRepoTables)
  - test/db.ts (hasTestDb)
  - lib/repositories/programs.repo.test.ts (skipIf + two-company fixture)
  - .planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md (D-02 algorithm, Pitfall 1)
  </read_first>
  <behavior>
  - After migrateMappingTableTenancy on a fixture with two companies and one pre-column global timeline row: zero rows with NULL company_id; not all surviving rows share a single company_id; each company can list a copy of the template name (D-02).
  - createTimelineMapping(companyA, 'Standard', json) then createTimelineMapping(companyB, 'Standard', json) both succeed (D-01 unique per company).
  - Second createTimelineMapping(companyA, 'Standard', json) rejects (D-01).
  </behavior>
  <action>
  TDD (D-05): add lib/db.mapping-tenant-migration.integration.test.ts and lib/repositories/import-mapping.repo.test.ts with describe.skipIf(!hasTestDb). Tests describe the D-02 two-company fixture and D-01 same-name-across-companies cases; implement any helper gaps until green.

  Extend test/repo-db.ts DDL with timeline_import_mappings matching production columns plus company_id referencing companies(id). Do not need bug/jql/sync tables in this plan.

  Integration migrate test: create the table without company_id (or with nullable column and NULL values), insert one global named row, seed two companies via seedCompany, run migrateMappingTableTenancy against the test pool, then assert D-02: no NULL company_id; row counts are not all on one company; each company has a usable copy. Never implement backfill as "set every NULL to the first company id" when COUNT(companies) is greater than 1 (D-02).

  Repo integration: mock getDb to testDb(); prove same template name allowed across companies and duplicate name within one company rejected.

  If migrateMappingTableTenancy is not importable without pulling the full getDb singleton, export it from lib/db-mapping-tenant.ts only (already required by 09-01-01) and call that function with testPool() — do not boot getDb() inside this spec.
  </action>
  <verify>
    <automated>npx vitest run lib/db.mapping-tenant-migration.integration.test.ts lib/repositories/import-mapping.repo.test.ts lib/repositories/import-mapping.repo.unit.test.ts -x</automated>
  </verify>
  <done>Two-company backfill leaves no NULL company_id and does not collapse tenants; unique names are per company (D-01, D-02, D-05). Suites skip cleanly when TEST_DATABASE_URL is absent.</done>
  <acceptance_criteria>
  - test/repo-db.ts DDL includes timeline_import_mappings with company_id
  - Integration spec uses describe.skipIf(!hasTestDb)
  - Backfill assertions cover no NULL company_id and copies for both companies when two companies exist
  - Same name across companies succeeds; duplicate within one company fails
  - npx vitest run lib/db.mapping-tenant-migration.integration.test.ts lib/repositories/import-mapping.repo.test.ts lib/repositories/import-mapping.repo.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser cookie session → /api/import-mapping | Authenticated actor; company_id comes only from session |
| Service → repository | companyId must be the asserted session company, never a client-supplied id |
| migrateMappingTableTenancy → PostgreSQL | Backfill SQL can duplicate or assign tenant ownership of existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | GET/PUT/DELETE import-mapping by id | high | mitigate | Service assertCompanyRow → ForbiddenError; route maps to 403 (D-03, D-04) |
| T-09-02 | Information Disclosure | GET /api/import-mapping list | high | mitigate | listTimelineMappings WHERE company_id = ?; no admin bypass (D-03, D-04) |
| T-09-03 | Tampering | POST body tenant field | high | mitigate | Stamp company_id from actor; schema does not accept a client tenant column (D-03) |
| T-09-05 | Tampering | Multi-company backfill | high | mitigate | Duplicate-per-company CROSS JOIN when COUNT(companies) > 1 (D-02) |
| T-09-06 | Information Disclosure | Missing vs foreign id | medium | mitigate | NotFoundError only when no row; ForbiddenError when row exists for another company (D-03) |
| T-09-07 | Elevation of Privilege | ACCESS_ENFORCEMENT=shadow | medium | accept | Existing Phase 6 operator opt-in; this phase does not change shadow semantics |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 09-01-01: vitest service unit + import-mapping route tests green; PUT/DELETE 403 body is { error: 'Forbidden' }
- 09-01-02: list/create scoped; route no longer imports the repo
- 09-01-03: skipIf integration file exists; when TEST_DATABASE_URL set, D-02 assertions pass
- Full wave: npx vitest run lib/services/import-mapping.service.unit.test.ts lib/repositories/import-mapping.repo.unit.test.ts app/api/import-mapping/route.test.ts -x
</verification>

<success_criteria>
Timeline import mappings are company-scoped end-to-end: foreign by-id access is 403, lists are session-company only, create stamps the session company, backfill does not orphan rows or collapse tenants, and unique names are per company (TENANT-01 tracer, D-01..D-05).
</success_criteria>

<output>
Create `.planning/phases/09-mapping-table-tenant-isolation/09-01-SUMMARY.md` when done
</output>
