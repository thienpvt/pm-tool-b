---
phase: 09-mapping-table-tenant-isolation
plan: 02
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/db-mapping-tenant.ts
  - lib/db.mapping-tenant-migration.integration.test.ts
  - lib/repositories/import-mapping.repo.ts
  - lib/repositories/import-mapping.repo.unit.test.ts
  - lib/repositories/import-mapping.repo.test.ts
  - lib/services/import-mapping.service.ts
  - lib/services/import-mapping.service.unit.test.ts
  - app/api/bug-import-mapping/route.ts
  - app/api/bug-import-mapping/[id]/route.ts
  - app/api/bug-import-mapping/route.test.ts
  - test/repo-db.ts
autonomous: true
requirements: [TENANT-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A user in company A cannot read or mutate another company's bug import mappings (cross-company 403)."
    - "Every bug_import_mappings row belongs to a company; listings and unique names are scoped to that company."
    - "Existing bug mapping data remains usable after backfill — no orphaned NULL company_id rows and no collapse of all tenants into one company."
    - "DELETE /api/bug-import-mapping/[id] for a foreign existing row returns 403 { error: 'Forbidden' } (D-03, D-05)."
    - "GET /api/bug-import-mapping returns only the session company's rows; empty list is 200 (D-03)."
    - "POST cap eviction deletes the oldest row of the session company only — never another company's templates (D-04)."
  artifacts:
    - path: "lib/services/import-mapping.service.ts"
      provides: "Bug mapping list/create/delete + company-scoped cap eviction (D-04)"
    - path: "app/api/bug-import-mapping/route.ts"
      provides: "Thin withAuth GET/POST calling the service"
    - path: "app/api/bug-import-mapping/[id]/route.ts"
      provides: "Thin withAuth DELETE calling the service"
  key_links:
    - from: "app/api/bug-import-mapping/route.ts"
      to: "lib/services/import-mapping.service.ts"
      via: "listBugMappings / createBugMapping(actor, ...)"
      pattern: "from '@/lib/services/import-mapping.service'"
    - from: "lib/services/import-mapping.service.ts"
      to: "lib/repositories/import-mapping.repo.ts"
      via: "bugMappingIds(companyId) then delete/create with companyId"
      pattern: "bugMappingIds"
  prohibitions:
    - collapsing every legacy bug mapping row onto one company when more than one company exists
    - answering a foreign existing bug mapping id with NotFound
    - enforcing bug template names unique across all companies
    - writing a tenant id taken from the JSON body
    - running cap eviction DELETE without a company_id predicate
    - wrapping bug-import-mapping routes in a project-scoped access helper
    - thinning unrelated ops/admin/config routes
---

<objective>
Expand TENANT-01 to `bug_import_mappings` using the 09-01 helper and import-mapping service: D-01/D-02 schema for the bug table, D-04 repo `companyId` + service assert, D-03 403 on cross-company DELETE, session-company lists, session-stamped create, and company-scoped 5-template eviction (D-05 tests).

Purpose: Same architecture as the timeline tracer; bug cap/evict currently lives in the route and is globally ordered — it must move to the service and filter by company (research Pitfall 3 analog).

Output: Bug import templates isolated per company; timeline behavior from 09-01 unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-mapping-table-tenant-isolation/09-CONTEXT.md
@.planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md
@.planning/phases/09-mapping-table-tenant-isolation/09-PATTERNS.md
@.planning/phases/09-mapping-table-tenant-isolation/09-01-PLAN.md

Flagged assumption (unclassified TENANT-01 probe): missing bug mapping id → 404; foreign existing id → 403. Bug routes have DELETE by id only (no PUT). MAX_TEMPLATES remains 5.

Do not touch jira_jql_presets / jira_sync_mappings (09-03). Do not change bug-import-mapping/schema.ts frozen 400 { error: 'Missing fields' }. Do not mark the schema strict.

<interfaces>
09-01 exports migrateMappingTableTenancy(pool) from lib/db-mapping-tenant.ts; getDb already calls it. Add a bug_import_mappings spec to the same registry (copy columns: name, mappings_json, created_at as present on the table). Unique key (company_id, name) per D-01.
09-01 import-mapping.service.ts already has requireCompanyId and assertCompanyRow — reuse those private helpers in the same file for bug functions; do not create a new HTTP wrapper (D-04).
Current bug repo: listBugMappings(), bugMappingIds(), createBugMapping(name, json), deleteBugMapping(id) — all global.
Current POST route: calls bugMappingIds(), if length >= 5 deletes oldest.id via deleteBugMapping, then createBugMapping — move this into the service with company scope.
AccessActor, ForbiddenError, withAuth, serviceErrorResponse: same as 09-01.
Route tests today mock @/lib/repositories/import-mapping.repo — retarget to the service module after thinning (Pitfall 5).
</interfaces>
</context>

<feature>
  <name>Company-scoped bug import mappings</name>
  <files>lib/services/import-mapping.service.ts, lib/services/import-mapping.service.unit.test.ts, lib/repositories/import-mapping.repo.ts, app/api/bug-import-mapping/route.ts, app/api/bug-import-mapping/[id]/route.ts, app/api/bug-import-mapping/route.test.ts</files>
  <behavior>
  Cases:
  - Foreign actor DELETE existing bug row → ForbiddenError / HTTP 403 { error: 'Forbidden' } (D-03)
  - Missing id → NotFoundError / 404 (research A3)
  - List returns only actor.company_id rows; empty array is success (D-03)
  - Create stamps session company; extra body tenant field is ignored (D-03)
  - Same template name allowed in two companies; duplicate name in one company → ConflictError (D-01)
  - When the company already has 5 templates, create deletes that company's oldest id only (D-04)
  - Null company_id actor → ForbiddenError on list/create/delete (D-03)
  </behavior>
  <implementation>
  Register bug table in migrateMappingTableTenancy (D-01, D-02). Repo functions take companyId with WHERE company_id = ?. Service owns assert, stamp, ConflictError, and cap/evict. Routes stay on withAuth and call the service (D-04).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="false" id="09-02-01">
  <name>Migrate bug_import_mappings with D-01/D-02 helper</name>
  <reversibility rating="one-way">NOT NULL company_id plus UNIQUE(company_id, name) on bug_import_mappings cannot be undone without another data migration. CONTEXT already accepted D-01/D-02.</reversibility>
  <precondition>lib/db-mapping-tenant.ts exports migrateMappingTableTenancy from plan 09-01 and getDb already invokes it.</precondition>
  <files>lib/db-mapping-tenant.ts, lib/db.mapping-tenant-migration.integration.test.ts, test/repo-db.ts</files>
  <read_first>
  - lib/db-mapping-tenant.ts (timeline spec — copy the same step order)
  - lib/db.mapping-tenant-migration.integration.test.ts
  - test/repo-db.ts
  - .planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md (D-01 order, D-02 branches)
  </read_first>
  <action>
  Add bug_import_mappings to migrateMappingTableTenancy using the same D-01 sequence and D-02 single- vs multi-company backfill as timeline (name + mappings_json copy columns; UNIQUE(company_id, name)). Do not edit lib/db.ts (the call site already exists). Do not SET NOT NULL on jira tables. Do not backfill by assigning every NULL row to one company when multiple companies exist (D-02).

  Extend test/repo-db.ts DDL with bug_import_mappings + company_id. Extend the mapping-tenant integration spec with a two-company bug-table fixture asserting no NULL company_id and a copy per company (D-02, D-05). Keep describe.skipIf(!hasTestDb).
  </action>
  <verify>
    <automated>npx vitest run lib/db.mapping-tenant-migration.integration.test.ts -x</automated>
  </verify>
  <done>bug_import_mappings has non-null company_id and per-company unique names after migrateMappingTableTenancy; two-company backfill does not collapse tenants (D-01, D-02).</done>
  <acceptance_criteria>
  - migrateMappingTableTenancy registry includes bug_import_mappings
  - Integration spec asserts bug table: no NULL company_id after backfill with two companies
  - test/repo-db.ts DDL includes bug_import_mappings
  - npx vitest run lib/db.mapping-tenant-migration.integration.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-02-02">
  <name>Service and repo: bug mappings company-scoped</name>
  <files>lib/repositories/import-mapping.repo.ts, lib/repositories/import-mapping.repo.unit.test.ts, lib/repositories/import-mapping.repo.test.ts, lib/services/import-mapping.service.ts, lib/services/import-mapping.service.unit.test.ts</files>
  <read_first>
  - lib/services/import-mapping.service.ts (timeline helpers from 09-01)
  - lib/repositories/import-mapping.repo.ts (bug functions)
  - app/api/bug-import-mapping/route.ts (MAX_TEMPLATES = 5 eviction — move to service)
  - lib/services/holidays.service.unit.test.ts
  </read_first>
  <behavior>
  - listBugMappings / createBugMapping / deleteBugMapping follow the same requireCompanyId + assertCompanyRow rules as timeline (D-03, D-04).
  - bugMappingIds(companyId) SQL includes WHERE company_id = ? ordered newest-first.
  - createBugMapping when the company already has 5 ids deletes only that company's oldest id then inserts with that company_id (D-04).
  - deleteBugMapping does not call repo delete when assert fails.
  - Same name two companies allowed; duplicate within company ConflictError (D-01).
  </behavior>
  <action>
  RED then GREEN (D-05). Add getBugMappingById(id) PK fetch without company predicate. Change listBugMappings, bugMappingIds, createBugMapping, deleteBugMapping to take companyId and filter SQL (D-04). Update repo.unit.test.ts bug create arity (timeline case already updated in 09-01).

  Service: listBugMappings(actor), createBugMapping(actor, name, json) with cap 5 using bugMappingIds(companyId) and deleteBugMapping(companyId, oldestId), deleteBugMapping(id, actor) via getBugMappingById + assertCompanyRow. Stamp company from actor only (D-03). Reuse requireCompanyId / assertCompanyRow. Do not import next/server.

  Extend import-mapping.repo.test.ts same-name-across-companies for bug rows when hasTestDb.
  </action>
  <verify>
    <automated>npx vitest run lib/services/import-mapping.service.unit.test.ts lib/repositories/import-mapping.repo.unit.test.ts lib/repositories/import-mapping.repo.test.ts -x</automated>
  </verify>
  <done>Bug repo SQL is company-scoped; service asserts 403-not-404 on foreign id; eviction is per company; unique names are per company (D-01, D-03, D-04, D-05).</done>
  <acceptance_criteria>
  - bugMappingIds and listBugMappings bind companyId in WHERE company_id = ?
  - Service unit: foreign delete throws ForbiddenError; repo delete not called
  - Service unit: create with 5 existing company ids deletes the last id of that company only
  - npx vitest run lib/services/import-mapping.service.unit.test.ts lib/repositories/import-mapping.repo.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-02-03">
  <name>Thin bug-import-mapping routes and 403 tests</name>
  <files>app/api/bug-import-mapping/route.ts, app/api/bug-import-mapping/[id]/route.ts, app/api/bug-import-mapping/route.test.ts</files>
  <read_first>
  - app/api/bug-import-mapping/route.ts
  - app/api/bug-import-mapping/[id]/route.ts
  - app/api/bug-import-mapping/route.test.ts
  - app/api/import-mapping/route.test.ts (09-01 service-mock 403 pattern)
  - app/api/bug-import-mapping/schema.ts
  </read_first>
  <behavior>
  - DELETE /api/bug-import-mapping/[id] foreign session when service throws ForbiddenError → 403 { error: 'Forbidden' } (D-03, D-05).
  - GET/POST/DELETE still 401 with no session; POST still 201 for owner; GET still returns list JSON (D-04).
  - POST no longer calls bugMappingIds in the route — eviction is in the service.
  </behavior>
  <action>
  RED: add the 403 DELETE case and any GET list/create service-mock updates first (D-05). GREEN: GET/POST/DELETE handlers import import-mapping.service, pass actor, keep withAuth and Zod 400/201 shapes (D-04). Remove repo imports and in-route MAX_TEMPLATES loop. Preserve 401 tests. Mock the service module, not the repo (Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run app/api/bug-import-mapping/route.test.ts app/api/import-mapping/route.test.ts -x</automated>
  </verify>
  <done>Bug mapping HTTP API is company-scoped with 403 on foreign DELETE; 401 tests still pass; cap logic is not in the route (D-03, D-04, D-05).</done>
  <acceptance_criteria>
  - bug-import-mapping route.ts and [id]/route.ts import from import-mapping.service, not import-mapping.repo
  - Route test includes 403 { error: 'Forbidden' } for foreign DELETE
  - 401 no-session tests still pass
  - npx vitest run app/api/bug-import-mapping/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → /api/bug-import-mapping | company_id only from AccessActor |
| Service cap/evict → DELETE bug_import_mappings | Unscoped DELETE would destroy another tenant's templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | DELETE /api/bug-import-mapping/[id] | high | mitigate | assertCompanyRow → ForbiddenError → 403 (D-03, D-04) |
| T-09-02 | Information Disclosure | GET list | high | mitigate | WHERE company_id = ? (D-03, D-04) |
| T-09-03 | Tampering | POST body tenant field | high | mitigate | Stamp from actor (D-03) |
| T-09-04 | Destruction | POST cap eviction | high | mitigate | bugMappingIds and delete scoped by company_id (D-04) |
| T-09-05 | Tampering | Bug table backfill | high | mitigate | Same D-02 helper as timeline |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
- 09-02-01: integration migrate spec still green (skipIf when no TEST_DATABASE_URL)
- 09-02-02: service + repo unit tests include Forbidden + company-scoped eviction
- 09-02-03: bug-import-mapping route tests include 403 and 401
- Regression: npx vitest run app/api/import-mapping/route.test.ts lib/services/import-mapping.service.unit.test.ts -x
</verification>

<success_criteria>
Bug import mappings match the timeline tracer contract: company-owned rows, session-scoped lists, 403 on foreign DELETE, per-company unique names and 5-template eviction, backfill without tenant collapse (TENANT-01, D-01..D-05).
</success_criteria>

<output>
Create `.planning/phases/09-mapping-table-tenant-isolation/09-02-SUMMARY.md` when done
</output>
