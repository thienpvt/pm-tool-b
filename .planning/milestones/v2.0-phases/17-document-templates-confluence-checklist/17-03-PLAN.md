---
phase: 17-document-templates-confluence-checklist
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - lib/documents/compliance.ts
  - lib/documents/compliance.unit.test.ts
  - lib/services/errors.ts
  - lib/api-errors.ts
  - lib/api-errors.test.ts
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
  - lib/services/document-compliance.service.ts
  - lib/services/document-compliance.service.unit.test.ts
  - app/api/dashboards/document-compliance/route.ts
  - app/api/dashboards/document-compliance/route.test.ts
autonomous: true
requirements: [DOC-02, DOC-05, DOC-06]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "createProject success calls generateProjectChecklist for the new project's stage so ALL and matching-stage active catalog items appear; it does not remove rows (D-04, DOC-02)."
    - "When PATCH stage differs from current.stage and any mandatory checklist item whose catalog.stage equals the current stage is not approved and not not_applicable, updateProject throws MandatoryIncompleteError unless acknowledge_incomplete_mandatory is true (D-09, DOC-06)."
    - "serviceErrorResponse maps MandatoryIncompleteError to HTTP 409 { code: 'mandatory_incomplete', items } and does not use the generic ConflictError { error } shape (D-09)."
    - "After ack, stage still changes, generateProjectChecklist runs for the new stage, prior-stage checklist rows remain, and auditLog records stage_change_ack (D-04, D-09, D-14, DOC-02)."
    - "acknowledge_incomplete_mandatory is peeled off before applyProjectGovernance and updateProjectRepo so it never hits PROJECT_COLUMNS (D-09)."
    - "CPMO GET /api/dashboards/document-compliance returns per-project compliant | not_compliant | not_applicable using D-08/D-10 rollup, with filters stage, status, rag, program; unknown keys 400; PM/Viewer/null-admin 403 (D-10, D-12, DOC-06)."
    - "Optional catalog items do not fail project compliance; mandatory + not_applicable is not a failure; mandatory + none/drafting/pending_approval is not_compliant (D-08, DOC-05)."
  artifacts:
    - path: "lib/services/errors.ts"
      provides: "MandatoryIncompleteError with code and items (D-09)"
      exports: ["MandatoryIncompleteError"]
    - path: "lib/api-errors.ts"
      provides: "409 mapper before ConflictError branch (D-09)"
    - path: "lib/documents/compliance.ts"
      provides: "projectComplianceStatus pure rollup (D-08, D-10)"
      exports: ["projectComplianceStatus"]
    - path: "lib/services/projects.service.ts"
      provides: "createProject generate hook; updateProject stage guard + generate (D-04, D-09)"
    - path: "lib/services/document-compliance.service.ts"
      provides: "getDocumentCompliance (D-10, D-12)"
      exports: ["getDocumentCompliance"]
    - path: "app/api/dashboards/document-compliance/route.ts"
      provides: "GET withCpmo (D-01, D-10)"
  key_links:
    - from: "lib/services/projects.service.ts"
      to: "lib/services/document-checklist-generate.ts"
      via: "generateProjectChecklist after createProjectRepo and after successful stage update (D-04)"
      pattern: "generateProjectChecklist"
    - from: "lib/services/projects.service.ts"
      to: "lib/repositories/project-document-checklist.repo.ts"
      via: "listChecklistByProject for current-stage mandatory guard before updateProjectRepo (D-09)"
      pattern: "listChecklistByProject"
    - from: "lib/api-errors.ts"
      to: "lib/services/errors.ts"
      via: "MandatoryIncompleteError instanceof branch returns 409 { code, items } (D-09)"
      pattern: "MandatoryIncompleteError"
    - from: "app/api/dashboards/document-compliance/route.ts"
      to: "lib/services/document-compliance.service.ts"
      via: "GET getDocumentCompliance(actor, query) (D-10)"
      pattern: "getDocumentCompliance"
  prohibitions:
    - editing app/api/projects/[id]/documents or the diary service/repo (D-01)
    - checking mandatory items for the destination stage instead of current.stage (D-09)
    - removing prior-stage checklist rows on generate (D-04)
    - passing acknowledge_incomplete_mandatory into updateProjectRepo
    - adding npm packages
    - CASL or leftover ops/admin re-gating (D-14)
    - UI-SPEC (D-13)
    - reimplementing dashboard filter vocabulary beyond stage, status, rag, program (D-10)
    - calling getPeriodTracking
    - editing lib/repositories/project-document-checklist.repo.ts (17-02 owns updateChecklistItemRepo; this plan only reads listChecklistByProject)
---

<objective>
Wire checklist generation into createProject and stage PATCH (DOC-02, D-04), return the DOC-06 409 warning via MandatoryIncompleteError (D-09), and expose CPMO GET /api/dashboards/document-compliance (D-10, DOC-06) with D-08 rollup (DOC-05). D-12: compliance withCpmo plus assertCompanyWrite. D-13: server tests are the gate. D-14: auditLog on stage-change ack.

Purpose: Close the governance loop: catalog from 17-01 actually appears on projects, and CPMO is warned before leaving a stage with incomplete mandatory items.

Output: Hooks + structured 409 + compliance listing. Template/checklist PATCH remain 17-02 (parallel; this plan must not edit those files).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md
@.planning/phases/17-document-templates-confluence-checklist/17-RESEARCH.md
@.planning/phases/17-document-templates-confluence-checklist/17-PATTERNS.md
@.planning/phases/17-document-templates-confluence-checklist/17-VALIDATION.md
@.planning/phases/17-document-templates-confluence-checklist/17-01-PLAN.md

Discretion locked by planner: generateProjectChecklist is called after createProjectRepo / updateProjectRepo (not inside the repo). Stage guard uses catalog.stage === current.stage only (not ALL). Compliance route is /api/dashboards/document-compliance. Isolation none — execute is sequential even though 17-02 and 17-03 share Wave 2; this plan is the only writer of projects.service.ts hooks. Wave 2 with 17-02 — do not modify template or checklist PATCH files, and do not edit project-document-checklist.repo.ts.

Do not implement deferred items: pixel UI, diary migration, Phase 18 full audit.

Prior-plan artifact: generateProjectChecklist, listChecklistByProject (joined catalog.name/stage/mandatory), listProjects from projects.repo, applyDashboardFilters / parseDashboardFilters in lib/dashboards/filters.ts, withCpmo, assertCompanyWrite, projectUpdateSchema passthrough on app/api/projects/[id]/route.ts.

<interfaces>
export class MandatoryIncompleteError extends Error {
  readonly code = 'mandatory_incomplete';
  readonly items: Array<{ checklist_id: number; catalog_id: number; name: string; status: string }>;
  constructor(items: MandatoryIncompleteError['items']); name = 'MandatoryIncompleteError'.
}

serviceErrorResponse: handle MandatoryIncompleteError BEFORE ConflictError: NextResponse.json({ code: e.code, items: e.items }, { status: 409 }). Do not include a generic error string as the only body. Add lib/api-errors.test.ts case expecting that exact JSON.

projectComplianceStatus(mandatoryItems: Array<{ status: string }>): 'compliant' | 'not_compliant' | 'not_applicable'
- length 0 → compliant (no mandatory work)
- every status not_applicable → not_applicable
- some status not in {approved, not_applicable} → not_compliant
- otherwise (all approved or mix approved + not_applicable with at least one approved) → compliant
Callers pass only mandatory catalog items. Optional rows must not be included (D-08).

createProject: after const row = await createProjectRepo(...), await generateProjectChecklist(Number(row.id), { companyId: actor.company_id, stage: (row.stage as string | null) ?? null }); then return { ...row, warnings }. Mock generate in unit tests; assert it is called with the new id.

updateProject:
1. await assertProjectWriteAccess as today
2. load current
3. const ack = fields.acknowledge_incomplete_mandatory === true
4. const clone = { ...fields }; delete clone.acknowledge_incomplete_mandatory (and do not pass that key into applyProjectGovernance)
5. existing project_code / governance logic on clone
6. If governed.stage is a string and String(governed.stage) !== String(current.stage ?? ''):
   a. rows = await listChecklistByProject(Number(projectId))
   b. incomplete = rows where catalog mandatory is true AND catalog.stage === current.stage AND status is not approved and not not_applicable
   c. if incomplete.length && !ack throw new MandatoryIncompleteError(mapped items)
   d. remember stageChanged = true
7. updateProjectRepo(projectId, governed)
8. if stageChanged: await generateProjectChecklist(Number(projectId), { companyId: actor.company_id ?? Number(current.company_id), stage: String(governed.stage) })
9. if stageChanged && ack: auditLog entity_type project action stage_change_ack before { stage: current.stage } after { stage: governed.stage }
10. return { ...row, warnings }

Guard must NOT use destination-stage catalog rows (pitfall: L2→L3 must fire on incomplete L2 mandatory, not on empty L3). ALL-stage catalog items do not participate in this guard (planner lock matching RESEARCH A4).

getDocumentCompliance(actor, query: Record<string, string>): assertCompanyWrite first; parseComplianceFilters from query allowing ONLY keys stage, status, rag, program; unknown key ValidationError(key); coerce program to number when the string is all digits; listProjects(actor.company_id) from projects.repo; applyDashboardFilters(projects as FilterableProjectRow[], filters); for each filtered project listChecklistByProject; status = projectComplianceStatus(mandatory items); return { filters, projects: [{ project_id, project_code, name, stage, status, rag, compliance }] }.

GET /api/dashboards/document-compliance = withCpmo; parse req.nextUrl.searchParams into a plain object; NextResponse.json(await getDocumentCompliance(actor, query)). Analog app/api/dashboards/portfolio/route.ts. Route tests analog portfolio/route.test.ts: 401, pm 403, viewer 403, null-company admin 403, cpmo 200.

Extend lib/services/projects.service.unit.test.ts with vi.mock of generateProjectChecklist and listChecklistByProject. Existing create/update tests must still pass — generate mockResolvedValue({ inserted: 0 }) in beforeEach.

app/api/projects/[id]/route.ts already passthrough; do not require a schema change unless tests show the ack key is dropped. Do not edit PROJECT_COLUMNS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="17-03-01">
  <name>MandatoryIncompleteError mapper and compliance rollup</name>
  <precondition>lib/api-errors.ts exports serviceErrorResponse; lib/services/errors.ts exports ConflictError and SubmitValidationError</precondition>
  <reversibility rating="costly">409 { code, items } becomes the client contract for stage-change warnings; swapping back to ConflictError { error } would break CPMO warning UI (D-09).</reversibility>
  <files>lib/services/errors.ts, lib/api-errors.ts, lib/api-errors.test.ts, lib/documents/compliance.ts, lib/documents/compliance.unit.test.ts</files>
  <read_first>
  - lib/services/errors.ts SubmitValidationError sibling shape
  - lib/api-errors.ts serviceErrorResponse ConflictError branch
  - lib/api-errors.test.ts serviceErrorResponse describe
  - .planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md D-08 D-09 D-10
  </read_first>
  <behavior>
  - serviceErrorResponse(new MandatoryIncompleteError(items)) returns status 409 and body { code: 'mandatory_incomplete', items } (D-09, DOC-06).
  - ConflictError still returns 409 { error: message } so duplicate-code conflicts stay unchanged (D-09).
  - projectComplianceStatus: empty → compliant; all not_applicable → not_applicable; any none/drafting/pending_approval → not_compliant; all approved → compliant; mix approved + not_applicable → compliant; optional items are not passed in (D-08, D-10, DOC-05).
  </behavior>
  <action>
  TDD. RED: failing api-errors.test.ts 409 code/items case; failing compliance.unit.test.ts for the rollup matrix including empty mandatory set and optional-not-included (tested by omitting optional rows from the input array). Commit `test(17-03): red mandatory incomplete 409 and compliance rollup`.
  GREEN: add MandatoryIncompleteError; map it in serviceErrorResponse before ConflictError; implement projectComplianceStatus. Commit `feat(17-03): structured mandatory-incomplete 409 and compliance helper`.
  Do not hook projects.service in this task (17-03-02). Do not add the compliance HTTP route yet (17-03-03).
  </action>
  <verify>
    <automated>npx vitest run lib/api-errors.test.ts lib/documents/compliance.unit.test.ts</automated>
  </verify>
  <done>409 bodies for incomplete mandatory items include code and items; compliance rollup matches D-08/D-10 (DOC-05, DOC-06, D-08, D-09).</done>
  <acceptance_criteria>
  - MandatoryIncompleteError maps to 409 { code: 'mandatory_incomplete', items }
  - ConflictError mapping is unchanged
  - projectComplianceStatus covers compliant, not_compliant, not_applicable
  - npx vitest run lib/api-errors.test.ts lib/documents/compliance.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="17-03-02">
  <name>createProject generate and stage-change 409 plus ack</name>
  <files>lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts</files>
  <read_first>
  - lib/services/projects.service.ts createProject and updateProject
  - lib/services/document-checklist-generate.ts from 17-01
  - lib/repositories/project-document-checklist.repo.ts listChecklistByProject
  - .planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md D-04 D-09 D-14
  </read_first>
  <behavior>
  - createProject calls generateProjectChecklist after createProjectRepo with the new id, companyId, and stage (D-04, DOC-02).
  - updateProject L2→L3 with an incomplete mandatory L2 checklist row throws MandatoryIncompleteError and does not call updateProjectRepo or generateProjectChecklist (D-09, DOC-06).
  - the same change with only incomplete L3 (destination) rows and complete L2 does not throw (guard uses current stage) (D-09).
  - acknowledge_incomplete_mandatory true allows the stage write, then generateProjectChecklist for the new stage, and auditLog stage_change_ack; generate is insert-only so prior L2 rows remain in the mocked list (D-04, D-09, D-14, DOC-02).
  - acknowledge_incomplete_mandatory is not passed to updateProjectRepo (D-09).
  </behavior>
  <action>
  TDD. RED: failing projects.service.unit.test.ts cases for createProject generate call, L2 incomplete 409 without repo write, destination-stage-only rows do not block, ack proceeds + generate + auditLog, ack key absent from updateProjectRepo fields. Mock generateProjectChecklist and listChecklistByProject. Keep existing create/update tests green via default generate mock. Commit `test(17-03): red project generate and stage guard`.
  GREEN: implement hooks per interfaces. Call generate after successful repo write. Peel the ack key before applyProjectGovernance. Commit `feat(17-03): generate checklist on create and stage change`.
  Do not edit app/api/projects/[id]/documents (D-01). Do not remove checklist rows.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts</automated>
  </verify>
  <done>New projects get checklist rows; stage changes warn with MandatoryIncompleteError unless acked, then generate the new stage without dropping history (D-04, D-09, DOC-02, DOC-06).</done>
  <acceptance_criteria>
  - createProject invokes generateProjectChecklist after insert
  - incomplete current-stage mandatory blocks without ack
  - destination-stage incompleteness does not block
  - ack writes stage, generates, audits
  - npx vitest run lib/services/projects.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="17-03-03">
  <name>CPMO document-compliance GET with dashboard filter keys</name>
  <files>lib/services/document-compliance.service.ts, lib/services/document-compliance.service.unit.test.ts, app/api/dashboards/document-compliance/route.ts, app/api/dashboards/document-compliance/route.test.ts</files>
  <read_first>
  - lib/dashboards/filters.ts applyDashboardFilters parseDashboardFilters DASHBOARD_FILTER_KEYS
  - lib/services/spec-dashboards.service.ts getPortfolioDashboard assertCompanyWrite-first
  - app/api/dashboards/portfolio/route.ts
  - app/api/dashboards/portfolio/route.test.ts
  </read_first>
  <behavior>
  - getDocumentCompliance throws ForbiddenError without listing when assertCompanyWrite fails (D-12).
  - unknown filter key throws ValidationError (D-10, DOC-06).
  - stage/status/rag/program filters shrink the project list via applyDashboardFilters (D-10).
  - each remaining project has compliance from projectComplianceStatus on mandatory checklist items only (D-08, D-10, DOC-05, DOC-06).
  - HTTP GET as CPMO 200; PM/Viewer/null-admin 403; missing session 401 (D-12).
  </behavior>
  <action>
  TDD. RED: failing document-compliance.service.unit.test.ts for assertCompanyWrite-first, unknown key, filter shrink, rollup using mocked listChecklistByProject; failing document-compliance/route.test.ts auth matrix. Commit `test(17-03): red document-compliance GET`.
  GREEN: implement getDocumentCompliance and GET withCpmo per interfaces. Reuse applyDashboardFilters; do not invent new filter key names. Commit `feat(17-03): CPMO document-compliance dashboard`.
  Do not add UI pages (D-13). Do not import the diary service (D-01).
  </action>
  <verify>
    <automated>npx vitest run lib/services/document-compliance.service.unit.test.ts lib/documents/compliance.unit.test.ts app/api/dashboards/document-compliance/route.test.ts</automated>
  </verify>
  <done>CPMO can GET portfolio document compliance with stage/status/rag/program filters; other roles cannot (D-10, D-12, DOC-06).</done>
  <acceptance_criteria>
  - GET path is /api/dashboards/document-compliance
  - filters limited to stage, status, rag, program
  - unknown keys ValidationError
  - CPMO 200; PM/Viewer 403
  - npx vitest run lib/services/document-compliance.service.unit.test.ts lib/documents/compliance.unit.test.ts app/api/dashboards/document-compliance/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PATCH /api/projects/[id] stage | Untrusted actor; ack flag must not skip tenancy; 409 must not leak other companies' items |
| Client → GET /api/dashboards/document-compliance | Untrusted actor; must not leak another company's compliance |
| Service → listChecklistByProject | Project ids from tenant-scoped listProjects only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-11 | Elevation of Privilege | GET document-compliance | high | mitigate | withCpmo plus assertCompanyWrite; listProjects(actor.company_id); PM/Viewer/null-admin 403 (D-10, D-12, ASVS V4) |
| T-17-12 | Tampering | stage PATCH without ack | medium | mitigate | MandatoryIncompleteError before updateProjectRepo unless acknowledge_incomplete_mandatory true (D-09, ASVS V5) |
| T-17-13 | Information Disclosure | MandatoryIncompleteError items | medium | mitigate | Items loaded only for the authorized project via listChecklistByProject after assertProjectWriteAccess (D-06, D-09, ASVS V4) |
| T-17-14 | Tampering | compliance query filters | medium | mitigate | Allowlist stage/status/rag/program; unknown key ValidationError (D-10, ASVS V5) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-17-15 | Elevation of Privilege | ack bypasses mandatory gate | low | accept | Ack is the documented CPMO warning override (D-09); still requires project write access |
</threat_model>

<verification>
- 17-03-01: api-errors 409 shape + compliance rollup green
- 17-03-02: create generate + stage guard/ack green
- 17-03-03: compliance GET + filters + auth matrix green
- Wave: npx vitest run lib/api-errors.test.ts lib/documents/compliance.unit.test.ts lib/services/projects.service.unit.test.ts lib/services/document-compliance.service.unit.test.ts app/api/dashboards/document-compliance/route.test.ts
</verification>

<success_criteria>
Creating a project or changing stage generates checklist rows without dropping history; incomplete current-stage mandatory items 409 unless acked; CPMO reads filtered portfolio compliance (DOC-02, DOC-05, DOC-06, D-04, D-08, D-09, D-10).
</success_criteria>

<output>
Create `.planning/phases/17-document-templates-confluence-checklist/17-03-SUMMARY.md` when done
</output>
