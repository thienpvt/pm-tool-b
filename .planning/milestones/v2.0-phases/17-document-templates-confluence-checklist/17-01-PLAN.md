---
phase: 17-document-templates-confluence-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-documents.ts
  - lib/db-documents.ddl.unit.test.ts
  - lib/db.ts
  - lib/repositories/document-catalog.repo.ts
  - lib/repositories/project-document-checklist.repo.ts
  - lib/services/document-catalog.service.ts
  - lib/services/document-catalog.service.unit.test.ts
  - lib/services/document-checklist-generate.ts
  - lib/services/document-checklist-generate.unit.test.ts
  - app/api/document-catalog/route.ts
  - app/api/document-catalog/route.test.ts
  - app/api/document-catalog/[id]/route.ts
  - app/api/document-catalog/[id]/route.test.ts
autonomous: true
requirements: [DOC-01, DOC-02]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO POST /api/document-catalog creates a company-scoped row with name, purpose, stage L0-L5 or ALL, mandatory, and active; GET lists only the actor company (D-02, D-12, DOC-01)."
    - "PATCH catalog sets active=false to soft-retire; catalog/template/checklist repos expose insert, update, and select only (D-02, D-11)."
    - "apply_to_in_flight true inserts checklist rows for existing Active projects whose stage matches catalog.stage or ALL and that lack that catalog_id; false (default) does not backfill (D-03, DOC-01)."
    - "generateProjectChecklist inserts active catalog items whose stage is ALL or equals the project stage, skipping existing (project_id, catalog_id); it never removes prior-stage rows (D-04, DOC-02)."
    - "migrateDocuments runs from getDb immediately after migrateDashboards and before backfillWeightedCompletion (D-11)."
    - "Catalog writes use withCpmo plus assertCompanyWrite; PM GET catalog is 200 with company match; Viewer GET is 403; null-company admin POST is 403 (D-12)."
    - "New catalog/checklist modules do not import the legacy project diary service or repo; app/api/projects/[id]/documents is not edited (D-01)."
  artifacts:
    - path: "lib/db-documents.ts"
      provides: "DOCUMENTS_DDL_FLAG documents_ddl_v1, three CREATE TABLE fragments, migrateDocuments (D-01, D-11)"
      exports: ["migrateDocuments", "DOCUMENTS_DDL", "DOCUMENTS_DDL_FLAG"]
    - path: "lib/repositories/document-catalog.repo.ts"
      provides: "Company-scoped catalog insert/list/update (D-02)"
      exports: ["listDocumentCatalog", "insertDocumentCatalog", "updateDocumentCatalog", "getDocumentCatalog", "listActiveCatalogForStage"]
    - path: "lib/repositories/project-document-checklist.repo.ts"
      provides: "Idempotent insert + list joined to catalog (D-04, D-06)"
      exports: ["insertChecklistRowIfMissing", "listChecklistCatalogIds", "listChecklistByProject"]
    - path: "lib/services/document-catalog.service.ts"
      provides: "CPMO create/update/list plus apply_to_in_flight backfill (D-03, D-12, D-14)"
      exports: ["createDocumentCatalogItem", "updateDocumentCatalogItem", "listDocumentCatalog", "getDocumentCatalogItem"]
    - path: "lib/services/document-checklist-generate.ts"
      provides: "generateProjectChecklist idempotent insert (D-04)"
      exports: ["generateProjectChecklist"]
    - path: "app/api/document-catalog/route.ts"
      provides: "GET withAuth, POST withCpmo (D-12)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-documents.ts"
      via: "getDb awaits migrateDocuments(pool) immediately after migrateDashboards(pool) (D-11)"
      pattern: "migrateDocuments"
    - from: "app/api/document-catalog/route.ts"
      to: "lib/services/document-catalog.service.ts"
      via: "POST createDocumentCatalogItem; GET listDocumentCatalog (D-12)"
      pattern: "createDocumentCatalogItem"
    - from: "lib/services/document-checklist-generate.ts"
      to: "lib/repositories/project-document-checklist.repo.ts"
      via: "insertChecklistRowIfMissing ON CONFLICT DO NOTHING (D-04)"
      pattern: "insertChecklistRowIfMissing"
    - from: "lib/services/document-catalog.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog on catalog create and update (D-14)"
      pattern: "auditLog"
  prohibitions:
    - editing app/api/projects/[id]/documents or lib/services/documents.service or lib/repositories/documents.repo (D-01)
    - storing spec checklist rows in the legacy documents table (D-01)
    - Prisma migrate CLI (D-11)
    - physical row-removal SQL helpers on catalog, template, or checklist tables (D-11)
    - adding npm packages
    - CASL or leftover ops/admin re-gating (D-14)
    - UI-SPEC or treating a thin page as a gate (D-13)
    - createProject / updateProject stage hooks (17-03)
    - template routes and checklist PATCH (17-02)
    - MandatoryIncompleteError (17-03)
---

<objective>
Prove the Phase 17 parallel document spine. D-01: new tables document_catalog, document_templates, project_document_checklist and /api/document-catalog — not the legacy project diary. D-02: company-scoped catalog with L0–L5 or ALL. D-03: explicit apply_to_in_flight backfill. D-04: generateProjectChecklist inserts missing (project_id, catalog_id) only. D-11: settings-flag DDL after migrateDashboards. D-12: withCpmo writes; PM GET. D-13: server tests are the gate. D-14: auditLog on catalog create/update. DOC-01, DOC-02 helper.

Purpose: Catch an architectural dead-end on the first commit — parallel catalog + generate helper, not the diary upsert — before templates, checklist PATCH, and project hooks.

Output: Production-quality tracer (DDL + catalog POST/GET + generate helper) plus expansions for in-flight apply, soft-retire, and [id] routes. Template versioning is 17-02. createProject/stage 409 is 17-03.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md
@.planning/phases/17-document-templates-confluence-checklist/17-RESEARCH.md
@.planning/phases/17-document-templates-confluence-checklist/17-PATTERNS.md
@.planning/phases/17-document-templates-confluence-checklist/17-VALIDATION.md

Discretion locked by planner: templates attach via catalog_id FK. Template library column is template_url TEXT (HTTPS pointer). generateProjectChecklist is a dedicated module called after repo writes in 17-03. Catalog GET uses withAuth so PMs with company_id can list; Viewer is 403. Isolation none.

Do not implement deferred items: full audit coverage (Phase 18), pixel catalog UI, migrating diary rows into the checklist.

Prior-phase artifact: migrateDashboards in getDb, withCpmo, withAuth, assertCompanyWrite, listProjects(companyId) from lib/repositories/projects.repo.ts, auditLog, settingsFlagExists pattern in lib/db-dashboards.ts.

<interfaces>
DOCUMENTS_DDL_FLAG = documents_ddl_v1. Copy settingsFlagExists / writeSettingsFlag / try-catch retry from lib/db-dashboards.ts migrateDashboards.

DOCUMENTS_DDL three CREATE TABLE IF NOT EXISTS fragments:

document_catalog: id SERIAL PRIMARY KEY; company_id INTEGER NOT NULL REFERENCES companies(id); name TEXT NOT NULL; purpose TEXT NOT NULL DEFAULT ''; stage TEXT NOT NULL CHECK (stage IN ('L0','L1','L2','L3','L4','L5','ALL')); mandatory BOOLEAN NOT NULL DEFAULT FALSE; active BOOLEAN NOT NULL DEFAULT TRUE; created_at / updated_at TIMESTAMPTZ NOT NULL DEFAULT now().

document_templates: id SERIAL PRIMARY KEY; catalog_id INTEGER NOT NULL REFERENCES document_catalog(id); company_id INTEGER NOT NULL REFERENCES companies(id); name TEXT NOT NULL; document_type TEXT NOT NULL; version INTEGER NOT NULL; effective_date DATE NOT NULL; guidance TEXT NOT NULL DEFAULT ''; template_url TEXT; retired_at TIMESTAMPTZ; created_at TIMESTAMPTZ NOT NULL DEFAULT now(); UNIQUE (catalog_id, version). No binary column types. 17-01 creates the table; 17-02 owns the service.

project_document_checklist: id SERIAL PRIMARY KEY; project_id INTEGER NOT NULL REFERENCES projects(id); catalog_id INTEGER NOT NULL REFERENCES document_catalog(id); status TEXT NOT NULL DEFAULT 'none' CHECK (status IN ('none','drafting','pending_approval','approved','not_applicable')); confluence_url TEXT; approved_at DATE; approved_by TEXT; na_reason TEXT; notes TEXT; created_at / updated_at TIMESTAMPTZ NOT NULL DEFAULT now(); UNIQUE (project_id, catalog_id).

getDb: after await migrateDashboards(pool) dynamically import migrateDocuments and await migrateDocuments(pool) before backfillWeightedCompletion.

Repos use getDb() with ? placeholders like programs.repo.ts. Always WHERE company_id = ? on catalog writes.

listActiveCatalogForStage(companyId, projectStage): active=true AND (stage = 'ALL' OR stage = projectStage). If projectStage is null, only ALL rows.

insertChecklistRowIfMissing(projectId, catalogId): INSERT status 'none' ON CONFLICT (project_id, catalog_id) DO NOTHING RETURNING id.

listChecklistByProject(projectId): SELECT checklist columns PLUS catalog.name, catalog.stage, catalog.mandatory, catalog.active JOIN document_catalog. Order by catalog.name. 17-03 stage guard and compliance consume this join.

generateProjectChecklist(projectId, opts: { companyId: number; stage: string | null }): load listActiveCatalogForStage; skip ids in listChecklistCatalogIds; insertChecklistRowIfMissing per remaining; return { inserted: number }. Do not filter on project status here (D-04 generate on create/stage, not only Active). Do not remove rows.

createDocumentCatalogItem(actor, body): assertCompanyWrite; validate name trim nonempty; stage in L0-L5|ALL; stamp actor.company_id; insert; if apply_to_in_flight === true then applyCatalogToInFlightProjects; auditLog entity_type document_catalog action create; return row.

applyCatalogToInFlightProjects(companyId, catalogId, catalogStage): listProjects(companyId) from projects.repo (not projects.service); keep rows whose status equals Active (case-insensitive); keep stage match (catalog ALL or row.stage === catalogStage); insertChecklistRowIfMissing each. Default apply_to_in_flight false means skip this.

updateDocumentCatalogItem: assertCompanyWrite; load row; 404 if missing; 403 if company_id mismatch; UPDATE name/purpose/stage/mandatory/active/updated_at; if apply_to_in_flight === true run the same backfill; auditLog action update. Never issue a row-removal statement.

listDocumentCatalog(actor): require actor.company_id; Viewer-only (hasRole viewer and not cpmo and not pm) throws ForbiddenError; return repo list for that company. PM and CPMO allowed (D-12).

GET /api/document-catalog = withAuth calling listDocumentCatalog. POST = withCpmo + zod schema { name, purpose optional, stage, mandatory optional, active optional, apply_to_in_flight optional } .strict(); 201 on create. Analog app/api/weekly-periods/route.ts for POST; GET analog uses withAuth from lib/http/with-auth.ts not withCpmo.

GET/PATCH /api/document-catalog/[id]: GET withAuth getDocumentCatalogItem; PATCH withCpmo updateDocumentCatalogItem. Route tests analog app/api/dashboards/portfolio/route.test.ts sessions: cpmo company_id 5; pm; viewer; { is_admin: 1, company_id: null, roles: [] }. POST pm/viewer/null-admin 403; GET pm 200; GET viewer 403.

Isolation none. TEST_DATABASE_URL for any skipIf(!hasTestDb) must end in _test. Prefer mocked repos in service tests; ddl test is hermetic string asserts like lib/db-dashboards.ddl.unit.test.ts.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="17-01-01">
  <name>End-to-end CPMO catalog create plus generate helper</name>
  <precondition>lib/db-dashboards.ts exports migrateDashboards; getDb already awaits migrateDashboards; lib/services/access.ts exports assertCompanyWrite; lib/http/with-role.ts exports withCpmo</precondition>
  <reversibility rating="costly">Additive document_catalog / document_templates / project_document_checklist and GET/POST /api/document-catalog become a published parallel surface; folding this into the legacy diary later would force a data split (D-01, D-11).</reversibility>
  <files>lib/db-documents.ts, lib/db-documents.ddl.unit.test.ts, lib/db.ts, lib/repositories/document-catalog.repo.ts, lib/repositories/project-document-checklist.repo.ts, lib/services/document-catalog.service.ts, lib/services/document-catalog.service.unit.test.ts, lib/services/document-checklist-generate.ts, lib/services/document-checklist-generate.unit.test.ts, app/api/document-catalog/route.ts, app/api/document-catalog/route.test.ts</files>
  <read_first>
  - .planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md (D-01, D-02, D-04, D-11, D-12)
  - .planning/phases/17-document-templates-confluence-checklist/17-PATTERNS.md (settings-flag DDL, programs.repo insert, insertShell analog)
  - lib/db-dashboards.ts (settingsFlagExists, writeSettingsFlag, try/catch)
  - lib/db.ts getDb migrateDashboards then backfillWeightedCompletion
  - lib/repositories/programs.repo.ts insert/list company scope
  - lib/http/with-role.ts withCpmo
  - lib/http/with-auth.ts withAuth
  - lib/services/access.ts assertCompanyWrite
  - app/api/weekly-periods/route.ts
  - app/api/dashboards/portfolio/route.test.ts session matrix
  </read_first>
  <behavior>
  - migrateDocuments is exported; DOCUMENTS_DDL_FLAG is documents_ddl_v1; joined DDL creates document_catalog, document_templates with template_url TEXT, and project_document_checklist with UNIQUE (project_id, catalog_id); getDb awaits migrateDocuments after migrateDashboards and before backfillWeightedCompletion (D-01, D-11).
  - createDocumentCatalogItem with a CPMO actor stamps company_id, persists name/purpose/stage/mandatory/active, and calls auditLog create; it throws ForbiddenError without inserting when assertCompanyWrite fails (D-02, D-12, D-14, DOC-01).
  - generateProjectChecklist for stage L2 inserts active L2 and ALL catalog ids missing on the project and does not insert ids already present; a second call inserted === 0 (D-04, DOC-02).
  - HTTP POST /api/document-catalog as CPMO returns 201; GET as CPMO and as PM with company_id 5 returns 200; PM POST returns 403; missing session returns 401; null-company admin POST returns 403 (D-12).
  - document-catalog.service and document-checklist-generate source do not import lib/services/documents.service or lib/repositories/documents.repo (D-01).
  </behavior>
  <action>
  TDD first (D-13). Isolation is none — keep this plan sequential-safe on the main tree.
  RED: add failing lib/db-documents.ddl.unit.test.ts cases for the flag, three CREATE TABLE names, catalog stage CHECK including ALL, checklist UNIQUE (project_id, catalog_id), template_url TEXT, and getDb wire order after migrateDashboards; failing document-catalog.service.unit.test.ts for create stamps company_id and assertCompanyWrite-before-insert plus auditLog; failing document-checklist-generate.unit.test.ts for L2+ALL insert and skip-existing; failing document-catalog/route.test.ts auth matrix. Tests must fail before production modules exist. Commit `test(17-01): red catalog POST and generate helper`.
  GREEN: implement until green per interfaces. Wire lib/db.ts only as specified. Do not add an ORM migrate CLI. Do not add row-removal helpers. Do not hook createProject yet (17-03). Do not add template HTTP routes (17-02). Mock assertCompanyWrite, catalog repo, checklist repo, listProjects, auditLog in service tests. Route tests mock the catalog service like portfolio/route.test.ts mocks getPortfolioDashboard.
  Commit `feat(17-01): document catalog API and checklist generate helper`.

  Tracer apply_to_in_flight may remain unimplemented (default false path only); task 17-01-02 completes backfill. Tracer GET/POST still call list/create so the catalog seam exists (DOC-01).
  Do not wrap checklist routes with a policy engine. Do not add pages or UI-SPEC (D-13). Do not edit the legacy project diary routes or services (D-01).
  </action>
  <verify>
    <automated>npx vitest run lib/db-documents.ddl.unit.test.ts lib/services/document-catalog.service.unit.test.ts lib/services/document-checklist-generate.unit.test.ts app/api/document-catalog/route.test.ts</automated>
  </verify>
  <done>CPMO can POST and GET /api/document-catalog for their company; generateProjectChecklist inserts missing stage-matching rows only; migrateDocuments runs after migrateDashboards; Viewer cannot GET the catalog; diary modules stay untouched (D-01, D-02, D-04, D-11, D-12, DOC-01, DOC-02).</done>
  <acceptance_criteria>
  - lib/db-documents.ts exports migrateDocuments and DOCUMENTS_DDL_FLAG documents_ddl_v1
  - getDb awaits migrateDocuments after migrateDashboards and before backfillWeightedCompletion
  - createDocumentCatalogItem calls assertCompanyWrite before insert
  - generateProjectChecklist second call inserts 0 duplicates
  - GET handler returns 200 for CPMO and PM; POST returns 201 for CPMO
  - document-catalog.service.unit.test.ts asserts the service module source does not import the diary service (D-01)
  - npx vitest run lib/db-documents.ddl.unit.test.ts lib/services/document-catalog.service.unit.test.ts lib/services/document-checklist-generate.unit.test.ts app/api/document-catalog/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="17-01-02">
  <name>In-flight apply and catalog soft-retire</name>
  <files>lib/services/document-catalog.service.ts, lib/services/document-catalog.service.unit.test.ts, lib/repositories/document-catalog.repo.ts</files>
  <read_first>
  - lib/services/document-catalog.service.ts from 17-01-01
  - .planning/phases/17-document-templates-confluence-checklist/17-CONTEXT.md D-03 D-02
  - lib/repositories/projects.repo.ts listProjects
  </read_first>
  <behavior>
  - create with apply_to_in_flight true inserts checklist rows for Active projects whose stage matches catalog.stage or ALL and that lack that catalog_id (D-03, DOC-01).
  - create with apply_to_in_flight omitted or false does not call insertChecklistRowIfMissing for existing projects (D-03).
  - updateDocumentCatalogItem setting active false persists active=false and does not remove the catalog row; apply_to_in_flight true on update still only inserts missing rows and never removes checklist rows (D-02, D-03, D-11).
  - Paused or non-Active projects are skipped by in-flight apply (D-03).
  </behavior>
  <action>
  TDD. RED: failing service unit tests for apply true vs false vs omitted, Active-only backfill, stage/ALL match, skip existing catalog_id, and active=false update without row-removal. Commit `test(17-01): red in-flight catalog apply`.
  GREEN: implement applyCatalogToInFlightProjects using listProjects from lib/repositories/projects.repo.ts (not projects.service to avoid a cycle with 17-03). updateDocumentCatalogItem per interfaces; auditLog update (D-14). Commit `feat(17-01): apply catalog to in-flight projects`.

  Do not generate on createProject here. Do not treat apply_to_in_flight as implicit when only mandatory changes without the flag (D-03).
  </action>
  <verify>
    <automated>npx vitest run lib/services/document-catalog.service.unit.test.ts</automated>
  </verify>
  <done>Explicit apply_to_in_flight backfills Active matching projects; default false does not; soft-retire is active=false (D-02, D-03, DOC-01).</done>
  <acceptance_criteria>
  - apply_to_in_flight true inserts missing checklist rows for Active matching projects
  - default false does not backfill
  - active false update leaves the catalog row in place
  - npx vitest run lib/services/document-catalog.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="17-01-03">
  <name>Catalog by-id GET and PATCH plus company isolation</name>
  <files>app/api/document-catalog/[id]/route.ts, app/api/document-catalog/[id]/route.test.ts, lib/services/document-catalog.service.ts, lib/services/document-catalog.service.unit.test.ts</files>
  <read_first>
  - app/api/document-catalog/route.ts from 17-01-01
  - lib/services/programs.service.ts get/update company mismatch 403
  - app/api/dashboards/portfolio/route.test.ts
  </read_first>
  <behavior>
  - getDocumentCatalogItem returns the row for the actor company and throws NotFoundError when missing or ForbiddenError on company mismatch (D-02, D-12).
  - PATCH /api/document-catalog/[id] as CPMO updates fields and returns 200; PM PATCH is 403; Viewer GET is 403; PM GET is 200 (D-12, DOC-01).
  - Cross-company GET/PATCH never returns another company's catalog row (D-02).
  </behavior>
  <action>
  TDD. RED: failing [id]/route.test.ts for GET pm 200, viewer 403, cpmo PATCH 200, pm PATCH 403, 401; failing service tests for company mismatch. Commit `test(17-01): red catalog by-id routes`.
  GREEN: add [id]/route.ts GET withAuth and PATCH withCpmo per interfaces. No DELETE handler on this route (D-11). Commit `feat(17-01): catalog item GET and PATCH`.
  Do not add template or checklist HTTP routes.
  </action>
  <verify>
    <automated>npx vitest run lib/services/document-catalog.service.unit.test.ts app/api/document-catalog/route.test.ts app/api/document-catalog/[id]/route.test.ts</automated>
  </verify>
  <done>CPMO can PATCH a catalog item in their company; PMs can GET it; other companies cannot read it; there is no catalog DELETE handler (D-02, D-11, D-12, DOC-01).</done>
  <acceptance_criteria>
  - GET [id] PM 200 and Viewer 403
  - PATCH [id] CPMO 200 and PM 403
  - company mismatch ForbiddenError
  - route file has no DELETE export
  - npx vitest run lib/services/document-catalog.service.unit.test.ts app/api/document-catalog/route.test.ts app/api/document-catalog/[id]/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST/PATCH /api/document-catalog | Untrusted actor; must not write another company's catalog |
| Client → GET /api/document-catalog | Untrusted actor; Viewer and cross-company must not list catalog |
| getDb → migrateDocuments | Boot-time schema; must not drop diary or dashboard tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | POST /api/document-catalog | high | mitigate | withCpmo plus assertCompanyWrite; stamp actor.company_id; PM/Viewer 403; null-company admin 403 (D-12, ASVS V4) |
| T-17-02 | Information Disclosure | GET listDocumentCatalog | high | mitigate | Filter WHERE company_id = actor.company_id; Viewer-only ForbiddenError (D-02, D-12, ASVS V4) |
| T-17-03 | Tampering | catalog stage/name body | medium | mitigate | zod .strict() plus stage CHECK L0-L5/ALL; ValidationError on empty name (D-02, ASVS V5) |
| T-17-04 | Tampering | migrateDocuments DDL | high | mitigate | Settings-flag idempotent CREATE IF NOT EXISTS; do not emit DROP TABLE; wire after migrateDashboards only (D-11) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-17-05 | Denial of Service | in-flight apply over all Active projects | low | accept | Same listProjects set as existing project index; ASVS L1 list size is acceptable |
</threat_model>

<verification>
- 17-01-01: ddl unit + catalog create + generate skip-existing + POST/GET auth matrix green
- 17-01-02: apply_to_in_flight true/false and soft-retire green
- 17-01-03: [id] GET/PATCH company isolation green
- Wave: npx vitest run lib/db-documents.ddl.unit.test.ts lib/services/document-catalog.service.unit.test.ts lib/services/document-checklist-generate.unit.test.ts app/api/document-catalog
</verification>

<success_criteria>
CPMO maintains a company-scoped document catalog with explicit in-flight apply; generateProjectChecklist is idempotent and does not remove rows; diary routes stay untouched (DOC-01, DOC-02, D-01, D-02, D-03, D-04, D-11, D-12).
</success_criteria>

<output>
Create `.planning/phases/17-document-templates-confluence-checklist/17-01-SUMMARY.md` when done
</output>
