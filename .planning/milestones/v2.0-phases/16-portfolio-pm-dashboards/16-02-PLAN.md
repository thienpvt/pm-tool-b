---
phase: 16-portfolio-pm-dashboards
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - lib/repositories/dashboard-filter-state.repo.ts
  - lib/repositories/dashboard-filter-state.repo.test.ts
  - lib/dashboards/filter-schema.ts
  - lib/services/spec-dashboards.service.ts
  - lib/services/spec-dashboards.service.unit.test.ts
  - app/api/dashboards/portfolio/filters/route.ts
  - app/api/dashboards/portfolio/filters/route.test.ts
  - app/api/dashboards/portfolio/export/route.ts
  - app/api/dashboards/portfolio/export/route.test.ts
  - app/api/dashboards/portfolio/export/schema.ts
  - lib/export/dashboard-portfolio.ts
  - lib/export/dashboard-portfolio.unit.test.ts
  - next.config.ts
autonomous: true
requirements: [PDSH-05, PDSH-06]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO PUT /api/dashboards/portfolio/filters replaces dashboard_filter_state for (actor.user_id, surface portfolio); a later GET /api/dashboards/portfolio applies that blob to tiles, charts, list, and drill-downs (D-07, PDSH-05)."
    - "POST filters with action clear or defaults upserts filters_json to {}; unknown filter keys on PUT are ValidationError 400 (D-06, D-07, PDSH-06)."
    - "CPMO POST /api/dashboards/portfolio/export with format xlsx returns an exceljs spreadsheet buffer; format pdf returns a jspdf PDF buffer; both include filtered KPI numbers, project list, and drill-down row ids (D-08, PDSH-06)."
    - "Optional body.filters overrides stored filters for that export only; omitted body.filters uses the stored blob (D-07, D-08)."
    - "Successful export calls auditLog action dashboard_export with format and applied filters; Viewer/PM are 403; null-company admin is 403 (D-08, D-12)."
    - "Filter writes are upsert-in-place; no physical DELETE of dashboard_filter_state rows (D-15)."
  artifacts:
    - path: "lib/repositories/dashboard-filter-state.repo.ts"
      provides: "upsertDashboardFilters ON CONFLICT (user_id, surface) DO UPDATE filters_json and updated_at (D-07, D-15)"
      exports: ["getDashboardFilters", "upsertDashboardFilters"]
    - path: "lib/dashboards/filter-schema.ts"
      provides: "dashboardFiltersSchema and filterActionSchema for PUT/POST (D-06)"
      exports: ["dashboardFiltersSchema", "filterActionSchema"]
    - path: "lib/export/dashboard-portfolio.ts"
      provides: "generatePortfolioDashboardXlsx and generatePortfolioDashboardPdf (D-08)"
      exports: ["generatePortfolioDashboardXlsx", "generatePortfolioDashboardPdf"]
    - path: "lib/services/spec-dashboards.service.ts"
      provides: "savePortfolioDashboardFilters, clearPortfolioDashboardFilters, exportPortfolioDashboard (D-07, D-08)"
      exports: ["getPortfolioDashboard", "savePortfolioDashboardFilters", "clearPortfolioDashboardFilters", "exportPortfolioDashboard"]
    - path: "app/api/dashboards/portfolio/filters/route.ts"
      provides: "GET/PUT/POST withCpmo filter persist (D-07, D-12)"
    - path: "app/api/dashboards/portfolio/export/route.ts"
      provides: "POST withCpmo xlsx|pdf (D-08, D-12)"
  key_links:
    - from: "app/api/dashboards/portfolio/filters/route.ts"
      to: "lib/services/spec-dashboards.service.ts"
      via: "PUT savePortfolioDashboardFilters; POST clearPortfolioDashboardFilters; GET getDashboardFilters via service (D-07)"
      pattern: "savePortfolioDashboardFilters"
    - from: "app/api/dashboards/portfolio/export/route.ts"
      to: "lib/services/spec-dashboards.service.ts"
      via: "POST exportPortfolioDashboard then binary NextResponse (D-08)"
      pattern: "exportPortfolioDashboard"
    - from: "lib/services/spec-dashboards.service.ts"
      to: "lib/export/dashboard-portfolio.ts"
      via: "generate buffer then auditLog action dashboard_export (D-08)"
      pattern: "auditLog"
    - from: "lib/repositories/dashboard-filter-state.repo.ts"
      to: "dashboard_filter_state"
      via: "INSERT ON CONFLICT (user_id, surface) DO UPDATE (D-07, D-15)"
      pattern: "upsertDashboardFilters"
  prohibitions:
    - editing getPortfolioSummary or GET /api/portfolio (D-01)
    - npm install or new PDF packages; docx/pptx are not PDF substitutes (D-08)
    - physical DELETE of filter-state or governed rows (D-15)
    - importing v1 weekly Excel or activity-weighted report helpers (D-14)
    - CASL or leftover ops/admin re-gating (D-12)
    - UI-SPEC (D-13)
    - PM dashboard routes (16-03)
    - calling getPeriodTracking (D-10)
---

<objective>
Deliver PDSH-05 remainder and PDSH-06. D-06: unknown filter keys 400 on PUT. D-07: persist/replace/clear/defaults for (user_id, surface portfolio); GET inherits stored blob. D-08: POST export xlsx via exceljs and pdf via jspdf with auditLog dashboard_export; no npm install. D-12: withCpmo plus assertCompanyWrite on filters and export. D-13: server tests are the gate. D-14: do not import v1 weekly Excel helpers. D-15: upsert-in-place, no physical DELETE.

Purpose: Session filters survive GET/drill-down and the user can restore empty defaults and download the filtered KPI pack without new npm packages.

Output: Filter routes plus export generator. PM surface persist is 16-03. Server tests are the gate (D-13).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md
@.planning/phases/16-portfolio-pm-dashboards/16-RESEARCH.md
@.planning/phases/16-portfolio-pm-dashboards/16-PATTERNS.md
@.planning/phases/16-portfolio-pm-dashboards/16-01-PLAN.md

Uses getPortfolioDashboard, parseDashboardFilters, dashboard_filter_state, and migrateDashboards from 16-01.

Discretion locked by planner: PUT replaces the whole blob (not merge). POST action clear and defaults both write {}. Export POST body { format: xlsx|pdf, filters?: object }; filters override is one-shot and is not written to dashboard_filter_state. xlsx sheets: KPIs, Projects, Overdue Milestones, High RAID, Technology Council. PDF is server-side jspdf text/tables (KPI block, project table, drill-down id lists) — not html-to-image. Copy exceljs styling helpers locally into dashboard-portfolio.ts; do not import lib/export/consolidated-weekly.ts. Add jspdf to next.config.ts serverExternalPackages beside exceljs (config only; no npm install).

<interfaces>
upsertDashboardFilters(userId, surface, filtersJson): INSERT INTO dashboard_filter_state (user_id, surface, filters_json) VALUES (?, ?, ?::jsonb) ON CONFLICT (user_id, surface) DO UPDATE SET filters_json = excluded.filters_json, updated_at = now(). JSON.stringify the object. Analog weekly-periods.repo upsertCompanyWeeklyConfig. skipIf(!hasTestDb) tests: replace same (user_id, surface); portfolio vs pm rows independent; no DELETE SQL in the repo module.

dashboardFiltersSchema: zod object of optional portfolio_year (number), program (number), unit (string), pm_user_id (number), stage (string), status (string), rag (string), type (string), weekly_report_enabled (boolean); .strict() so unknown keys fail 400 at the route. filterActionSchema: { action: 'clear' | 'defaults' }.

savePortfolioDashboardFilters(actor, body): assertCompanyWrite; parseDashboardFilters(body) then upsertDashboardFilters(actor.user_id, 'portfolio', parsed). clearPortfolioDashboardFilters(actor): assertCompanyWrite; upsertDashboardFilters(actor.user_id, 'portfolio', {}).

exportPortfolioDashboard(actor, body): assertCompanyWrite; if body.filters is present parseDashboardFilters(body.filters) as the applied set without upserting; else load stored portfolio blob. Recompute the same payload as getPortfolioDashboard using that applied set (listProjects + Phase 12 lists + KPI helpers). Generate Buffer via generatePortfolioDashboardXlsx or generatePortfolioDashboardPdf. Then auditLog { actor_id: actor.user_id, company_id: actor.company_id, entity_type: 'dashboard', entity_id: 'portfolio', action: 'dashboard_export', before: null, after: { format, filters: applied } }. Return { buffer, contentType, filename }. Filename portfolio-dashboard.xlsx or portfolio-dashboard.pdf. Content types: xlsx application/vnd.openxmlformats-officedocument.spreadsheetml.sheet; pdf application/pdf. Generate the Buffer before auditLog so a generator throw leaves no audit row.

GET/PUT/POST /api/dashboards/portfolio/filters = withCpmo. GET returns { filters, updated_at } from getDashboardFilters(actor.user_id, 'portfolio') after assertCompanyWrite in a thin getPortfolioDashboardFilters helper. PUT uses dashboardFiltersSchema. POST uses filterActionSchema and only clear|defaults.

POST /api/dashboards/portfolio/export = withCpmo with portfolioExportSchema { format: 'xlsx' | 'pdf', filters: dashboardFiltersSchema.optional() }. Return NextResponse(buffer) with Content-Type and Content-Disposition attachment. Analog app/api/weekly-periods/[periodId]/export/route.ts. Route tests: 401; 403 pm; 403 viewer; 400 invalid format; 200 binary with Content-Disposition.

Isolation none. TEST_DATABASE_URL _test suffix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="16-02-01">
  <name>Persist, clear, and restore default filters</name>
  <precondition>16-01 shipped getPortfolioDashboard, parseDashboardFilters, getDashboardFilters, migrateDashboards, and GET /api/dashboards/portfolio</precondition>
  <files>lib/repositories/dashboard-filter-state.repo.ts, lib/repositories/dashboard-filter-state.repo.test.ts, lib/dashboards/filter-schema.ts, lib/services/spec-dashboards.service.ts, lib/services/spec-dashboards.service.unit.test.ts, app/api/dashboards/portfolio/filters/route.ts, app/api/dashboards/portfolio/filters/route.test.ts</files>
  <read_first>
  - lib/repositories/dashboard-filter-state.repo.ts from 16-01
  - lib/repositories/weekly-periods.repo.ts upsertCompanyWeeklyConfig
  - app/api/weekly-periods/config/route.ts
  - .planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md D-06 D-07 D-15
  </read_first>
  <behavior>
  - upsertDashboardFilters replaces filters_json for the same (user_id, surface); a second surface for the same user stays independent (D-07).
  - savePortfolioDashboardFilters then getPortfolioDashboard applies the new blob to list membership (D-07, PDSH-05).
  - clearPortfolioDashboardFilters writes {}; defaults is the same empty object (D-07, PDSH-06).
  - PUT unknown key returns 400; PUT/POST as PM or viewer returns 403; missing session 401 (D-06, D-12).
  </behavior>
  <action>
  TDD. RED: failing skipIf(!hasTestDb) upsert replace and two-surface independence tests; failing service tests for save-then-GET apply and clear-to-empty; failing filters route.test.ts for 401/403/400/200. Commit `test(16-02): red portfolio filter persist`.
  GREEN: implement upsertDashboardFilters, save/clear service functions, and withCpmo filters route per interfaces. PUT replaces the whole blob. POST clear and defaults both call clearPortfolioDashboardFilters. Do not add DELETE FROM dashboard_filter_state (D-15). Commit `feat(16-02): persist portfolio dashboard filters`.

  Actor.user_id from the session is the only user_id written — never take user_id from the body (IDOR). Do not add PM filter routes here (16-03). Do not export yet.
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/dashboard-filter-state.repo.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/filters/route.test.ts</automated>
  </verify>
  <done>CPMO can persist AND filters per user+portfolio surface, clear them to {}, restore defaults to {}, and see GET inherit the stored blob (D-07, PDSH-05, PDSH-06).</done>
  <acceptance_criteria>
  - upsert uses ON CONFLICT (user_id, surface) DO UPDATE
  - POST action clear and defaults both store {}
  - PUT unknown keys 400
  - subsequent GET /api/dashboards/portfolio uses the stored blob
  - npx vitest run lib/repositories/dashboard-filter-state.repo.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/filters/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="16-02-02">
  <name>Export filtered dashboard as xlsx and pdf</name>
  <files>lib/export/dashboard-portfolio.ts, lib/export/dashboard-portfolio.unit.test.ts, lib/services/spec-dashboards.service.ts, lib/services/spec-dashboards.service.unit.test.ts, app/api/dashboards/portfolio/export/route.ts, app/api/dashboards/portfolio/export/schema.ts, app/api/dashboards/portfolio/export/route.test.ts, next.config.ts</files>
  <read_first>
  - lib/export/consolidated-weekly.ts navHeader sectionTitle labelValueRow thinBorder (copy locally, do not import the module)
  - app/api/weekly-periods/[periodId]/export/route.ts
  - lib/services/weekly-tracking.service.ts exportConsolidatedWeekly auditLog tail
  - next.config.ts serverExternalPackages
  - .planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md D-08
  </read_first>
  <behavior>
  - generatePortfolioDashboardXlsx returns a non-empty Buffer whose workbook has sheets named KPIs, Projects, Overdue Milestones, High RAID, Technology Council and includes active_count plus filtered project ids (D-08, PDSH-06).
  - generatePortfolioDashboardPdf returns a Buffer whose first bytes are %PDF and includes KPI numbers plus project ids (D-08, PDSH-06).
  - exportPortfolioDashboard calls auditLog with action dashboard_export after a successful Buffer; body.filters overrides storage for that call only (D-07, D-08).
  - POST export as CPMO returns 200 with Content-Disposition; PM/viewer 403; invalid format 400 (D-12).
  </behavior>
  <action>
  TDD. RED: failing dashboard-portfolio.unit.test.ts for xlsx sheet names and pdf magic bytes; failing service test for auditLog dashboard_export and one-shot filters override; failing export route.test.ts auth/format matrix. Commit `test(16-02): red portfolio dashboard export`.
  GREEN: implement generators with existing exceljs and jspdf only. Copy styling helpers into dashboard-portfolio.ts — do not import consolidated-weekly.ts (D-14). Use jspdf jsPDF text layout; do not use html-to-image. Add jspdf to serverExternalPackages in next.config.ts next to exceljs. Do not run npm install. Commit `feat(16-02): portfolio dashboard xlsx and pdf export`.

  Generate Buffer before auditLog. Do not write body.filters into dashboard_filter_state. Do not substitute docx or pptx for pdf (D-08).
  </action>
  <verify>
    <automated>npx vitest run lib/export/dashboard-portfolio.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/export/route.test.ts</automated>
  </verify>
  <done>CPMO can export the filtered portfolio dashboard as xlsx and pdf with KPI numbers, project list, drill-down ids, and an audit row (D-08, PDSH-06).</done>
  <acceptance_criteria>
  - xlsx and pdf generators use exceljs and jspdf already in package.json
  - auditLog action is dashboard_export
  - optional body.filters is one-shot and not persisted
  - next.config.ts serverExternalPackages includes jspdf
  - npx vitest run lib/export/dashboard-portfolio.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/export/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PUT /api/dashboards/portfolio/filters | Untrusted filter JSON; must not write another user's row |
| Client → POST /api/dashboards/portfolio/export | Untrusted format/filters; binary download is company-scoped |
| Service → auditLog | Export must leave an actor/time trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Elevation of Privilege | filters/export routes | high | mitigate | withCpmo plus assertCompanyWrite; PM/Viewer 403 (D-12, ASVS V4) |
| T-16-07 | Tampering | upsertDashboardFilters user_id | high | mitigate | Always actor.user_id from session; never body.user_id (ASVS V4) |
| T-16-08 | Information Disclosure | exportPortfolioDashboard | high | mitigate | Same company listProjects + stored/override filters as GET; no cross-company rows (D-08, ASVS V4) |
| T-16-09 | Repudiation | export audit | medium | mitigate | auditLog action dashboard_export after successful Buffer (D-08, ASVS L1) |
| T-16-10 | Tampering | filter JSON keys | medium | mitigate | zod .strict() plus parseDashboardFilters unknown-key ValidationError (D-06, ASVS V5) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; reuse exceljs and jspdf |
| T-16-11 | Information Disclosure | one-shot export filters | low | accept | Override is opt-in CPMO body; still company-scoped |
</threat_model>

<verification>
- 16-02-01: upsert + filters route green
- 16-02-02: xlsx/pdf generators + export route + auditLog green
- Wave: npx vitest run lib/repositories/dashboard-filter-state.repo.test.ts lib/export/dashboard-portfolio.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/filters/route.test.ts app/api/dashboards/portfolio/export/route.test.ts
</verification>

<success_criteria>
CPMO persists, clears, and restores dashboard filters and exports the filtered dashboard as xlsx and pdf with auditLog dashboard_export (PDSH-05, PDSH-06, D-07, D-08, D-12, D-15).
</success_criteria>

<output>
Create `.planning/phases/16-portfolio-pm-dashboards/16-02-SUMMARY.md` when done
</output>
