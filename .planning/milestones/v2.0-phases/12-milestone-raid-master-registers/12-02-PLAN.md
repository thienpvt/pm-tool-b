---
phase: 12-milestone-raid-master-registers
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/repositories/risks.repo.ts
  - lib/repositories/issues.repo.ts
  - lib/repositories/risks.repo.test.ts
  - lib/repositories/issues.repo.test.ts
  - lib/services/risks.service.ts
  - lib/services/issues.service.ts
  - lib/services/risks.service.unit.test.ts
  - lib/services/issues.service.unit.test.ts
  - app/api/projects/[id]/risks/route.ts
  - app/api/projects/[id]/issues/route.ts
  - app/api/projects/[id]/risks/route.test.ts
  - app/api/projects/[id]/issues/route.test.ts
  - lib/repositories/ALLOWLIST-DIFF.md
  - lib/http/role-matrix.test.ts
  - app/api/projects/[id]/risks/route.access.test.ts
autonomous: true
requirements: [RAID-01]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PM/CPMO create and update risks and issues; Viewer cannot mutate (D-02, D-09, RAID-01)."
    - "code is unique per project case-insensitively; duplicate throws ConflictError which already maps to HTTP 409 (D-05, D-09, RAID-01)."
    - "Omitted code auto-generates R-nnn for risks and I-nnn for issues, zero-padded to 3 digits (D-05)."
    - "HTTP DELETE on risks and issues deactivates (status deactivated, deactivated_at set); the row remains; response is 200 { ok: true } (D-05, D-13, RAID-01)."
    - "deactivate calls auditLog action deactivate (D-10)."
    - "technology_council is an allowlisted issues column defaulting false (D-08)."
  artifacts:
    - path: "lib/repositories/risks.repo.ts"
      provides: "code on RISK_COLUMNS; findRiskByCode; deactivateRisk; auto R-nnn (D-05)"
    - path: "lib/repositories/issues.repo.ts"
      provides: "code and technology_council on ISSUE_COLUMNS; findIssueByCode; deactivateIssue; auto I-nnn (D-05, D-08)"
    - path: "lib/services/risks.service.ts"
      provides: "ConflictError on duplicate code; deactivateRisk; no deleteRisk export (D-09, D-13)"
    - path: "lib/services/issues.service.ts"
      provides: "ConflictError on duplicate code; deactivateIssue; no deleteIssue export (D-09, D-13)"
    - path: "app/api/projects/[id]/risks/route.ts"
      provides: "DELETE calls deactivateRisk, returns { ok: true } (D-13)"
    - path: "app/api/projects/[id]/issues/route.ts"
      provides: "DELETE calls deactivateIssue, returns { ok: true } (D-13)"
  key_links:
    - from: "lib/services/risks.service.ts"
      to: "lib/repositories/risks.repo.ts"
      via: "findRiskByCode before create/update; ConflictError on hit (D-09)"
      pattern: "findRiskByCode"
    - from: "app/api/projects/[id]/risks/route.ts"
      to: "lib/services/risks.service.ts"
      via: "DELETE → deactivateRisk (D-13)"
      pattern: "deactivateRisk"
    - from: "lib/repositories/risks.repo.ts"
      to: "risks_project_code_lower_unique"
      via: "Index created in 12-01 is the race-safe uniqueness source of truth (D-05)"
      pattern: "LOWER(code)"
  prohibitions:
    - a second write-gate wrapper (D-02)
    - physical row-removal SQL on risks or issues (D-05)
    - dashboard pages (D-11)
    - Phase 13 snapshot tables (D-12)
    - putting helpers in portfolio.service (D-15)
    - adding npm packages
    - renaming risk_id / issue_id away; keep them populated from code for legacy readers
---

<objective>
Deliver RAID-01 (D-05, D-08, D-09, D-13): unique per-project codes, auto R-nnn / I-nnn, deactivate instead of row removal, Viewer 403, technology_council allowlisted on issues.

Purpose: Master-register identity and retire path for risks and issues, using the columns 12-01 already migrated.

Output: Existing DELETE clients keep working; uniqueness is index-backed; deactivate is audited.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-milestone-raid-master-registers/12-CONTEXT.md
@.planning/phases/12-milestone-raid-master-registers/12-RESEARCH.md
@.planning/phases/12-milestone-raid-master-registers/12-PATTERNS.md
@.planning/phases/12-milestone-raid-master-registers/12-VALIDATION.md
@.planning/phases/12-milestone-raid-master-registers/12-01-PLAN.md

<interfaces>
findRiskByCode(projectId, code, excludeId?) / findIssueByCode: SELECT id WHERE project_id = ? AND LOWER(code) = LOWER(?) AND id != COALESCE(excludeId, -1) LIMIT 1. Analog: findProjectByCompanyCode and holidays findHolidayByDate.

deactivateRisk / deactivateIssue: UPDATE status = 'deactivated', deactivated_at = now() WHERE id = ? AND project_id = ? RETURNING *. Empty → service NotFoundError. Status string is deactivated not closed (planner lock).

createRisk auto-code: if body.code is a non-empty trim, use it; else generate R- plus the next integer among existing codes matching /^R-\\d+$/ (fallback COUNT(*)+1), padStart(3,'0'). Loop until findRiskByCode is empty. Also set risk_id to body.risk_id || code so legacy display ids stay populated. Mirror for issues with I- and issue_id.

createRisk / updateRisk service: assertProjectWriteAccess (D-02, D-09). If a non-empty code is present and findRiskByCode hits, throw new ConflictError('Risk code already exists') — same class holidays.service uses (maps to 409 in lib/api-errors.ts). On create, pass body through to repo after the pre-check; repo applies auto-code when omitted. If the driver raises SQLSTATE 23505, rethrow ConflictError with the same message.

getRisk / getIssue: scoped SELECT for deactivate audit prior status.

RISK_COLUMNS add code. ISSUE_COLUMNS add code and technology_council. Document both in ALLOWLIST-DIFF.md as migrate-added. deactivated_at is not allowlisted (service sets it).

HTTP DELETE keeps searchParams rowId. Swap service import from deleteRisk/deleteIssue to deactivateRisk/deactivateIssue; still return { ok: true } (D-13).

Update every vi.mock of risks.repo / issues.repo in this plan's files: export deactivateRisk/deactivateIssue, findRiskByCode/findIssueByCode, getRisk/getIssue, and keep unused prior names only if something still imports them. Stop exporting deleteRisk / deleteIssue from the services.

Do not implement due-date history append (12-03) or RAID-05 ordering (12-03). Do not change assertProjectWriteAccess.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="12-02-01">
  <name>Unique risk codes and deactivate</name>
  <files>lib/repositories/risks.repo.ts, lib/repositories/risks.repo.test.ts, lib/services/risks.service.ts, lib/services/risks.service.unit.test.ts, lib/repositories/ALLOWLIST-DIFF.md</files>
  <read_first>
  - lib/repositories/risks.repo.ts RISK_COLUMNS createRisk row-removal
  - lib/services/risks.service.ts
  - lib/services/holidays.service.ts ConflictError pre-check
  - lib/services/errors.ts ConflictError
  - lib/services/risks.service.unit.test.ts deleteRisk describe
  </read_first>
  <behavior>
  - createRisk with no code persists code R-001 form (zero-padded) and write-gates first (D-05, D-02, RAID-01).
  - createRisk with a code whose LOWER matches an existing row throws ConflictError and does not call createRiskRepo (D-09).
  - updateRisk changing code to a sibling row's code throws ConflictError (D-09).
  - deactivateRisk sets status deactivated, calls auditLog action deactivate, throws NotFoundError on empty RETURNING (D-05, D-10).
  - Viewer / write-denied actor never reaches the repo (D-02).
  - risks.service no longer exports deleteRisk (D-13, RAID-01).
  </behavior>
  <action>
  TDD: failing tests for auto R-nnn padding, duplicate ConflictError on create and update, deactivate audit + NotFound, write-gate. Then implement findRiskByCode, deactivateRisk, getRisk, RISK_COLUMNS code, auto-code on create, service ConflictError pre-check, auditLog on deactivate. Replace deleteRisk service/repo tests. Keep risk_id populated from code. Document code in ALLOWLIST-DIFF.md risks table.

  Do not add a new auth wrapper (D-02). Do not append due-date history here (12-03). Do not build UI (D-11).
  </action>
  <verify>
    <automated>npx vitest run lib/services/risks.service.unit.test.ts lib/repositories/risks.repo.test.ts -x</automated>
  </verify>
  <done>Risks have unique per-project codes, auto R-nnn, and deactivate-in-place with audit (D-05, D-09, D-10, RAID-01).</done>
  <acceptance_criteria>
  - RISK_COLUMNS includes code
  - Duplicate code throws ConflictError
  - Auto code matches R- then 3 digits
  - Service exports deactivateRisk not deleteRisk
  - npx vitest run lib/services/risks.service.unit.test.ts lib/repositories/risks.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="12-02-02">
  <name>Unique issue codes, deactivate, technology_council</name>
  <files>lib/repositories/issues.repo.ts, lib/repositories/issues.repo.test.ts, lib/services/issues.service.ts, lib/services/issues.service.unit.test.ts, lib/repositories/ALLOWLIST-DIFF.md</files>
  <read_first>
  - lib/repositories/issues.repo.ts ISSUE_COLUMNS
  - lib/services/issues.service.ts
  - lib/services/risks.service.ts after 12-02-01 (mirror)
  </read_first>
  <behavior>
  - createIssue with no code persists I-nnn zero-padded (D-05, RAID-01).
  - Duplicate LOWER(code) in the same project throws ConflictError (D-09).
  - deactivateIssue sets status deactivated and auditLogs (D-05, D-10).
  - ISSUE_COLUMNS includes code and technology_council; updateIssue can persist technology_council true (D-08).
  - issues.service no longer exports deleteIssue.
  - Viewer cannot mutate (D-02).
  </behavior>
  <action>
  TDD then implement the risks pattern for issues: findIssueByCode, deactivateIssue, getIssue, auto I-nnn, ConflictError('Issue code already exists'), allowlist technology_council, document ALLOWLIST-DIFF.md issues table. Extend the issues.service owner fixture to a full AccessActor with user_id.

  Mirror risks. Do not implement listTechnologyCouncilIssues yet (12-03). Do not change Jira or dashboard UI (D-04, D-11).
  </action>
  <verify>
    <automated>npx vitest run lib/services/issues.service.unit.test.ts lib/repositories/issues.repo.test.ts -x</automated>
  </verify>
  <done>Issues have unique I-nnn codes, deactivate-in-place, and an allowlisted technology_council flag (D-05, D-08, D-09, RAID-01).</done>
  <acceptance_criteria>
  - ISSUE_COLUMNS includes code and technology_council
  - Duplicate code throws ConflictError
  - Auto code matches I- then 3 digits
  - Service exports deactivateIssue not deleteIssue
  - npx vitest run lib/services/issues.service.unit.test.ts lib/repositories/issues.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="12-02-03">
  <name>Map RAID HTTP DELETE to deactivate</name>
  <files>app/api/projects/[id]/risks/route.ts, app/api/projects/[id]/issues/route.ts, app/api/projects/[id]/risks/route.test.ts, app/api/projects/[id]/issues/route.test.ts, lib/http/role-matrix.test.ts, app/api/projects/[id]/risks/route.access.test.ts</files>
  <read_first>
  - app/api/projects/[id]/risks/route.ts DELETE
  - app/api/projects/[id]/issues/route.ts DELETE
  - app/api/projects/[id]/risks/route.test.ts DELETE case
  </read_first>
  <behavior>
  - DELETE /api/projects/[id]/risks?rowId= returns 200 { ok: true } and calls deactivateRisk (D-13).
  - DELETE /api/projects/[id]/issues?rowId= returns 200 { ok: true } and calls deactivateIssue (D-13).
  - Existing Viewer POST 403 proof on risks still passes (D-02).
  </behavior>
  <action>
  TDD: update the existing DELETE route tests to mock deactivateRisk / deactivateIssue on the repo and expect { ok: true }. Then swap route imports. Add the new repo exports to vi.mock objects in role-matrix.test.ts and risks/route.access.test.ts so POST still loads. Keep rowId query param. Do not return 405 (D-13).
  </action>
  <verify>
    <automated>npx vitest run app/api/projects/[id]/risks/route.test.ts app/api/projects/[id]/issues/route.test.ts app/api/projects/[id]/risks/route.access.test.ts lib/http/role-matrix.test.ts -x</automated>
  </verify>
  <done>Clients keep calling DELETE; rows deactivate instead of disappearing (D-13, RAID-01).</done>
  <acceptance_criteria>
  - risks and issues DELETE handlers import deactivate functions
  - Tests expect { ok: true }
  - npx vitest run app/api/projects/[id]/risks/route.test.ts app/api/projects/[id]/issues/route.test.ts app/api/projects/[id]/risks/route.access.test.ts lib/http/role-matrix.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST/PUT/DELETE /api/projects/[id]/risks and /issues | Untrusted code and rowId; Viewer must not mutate |
| Service → unique code | Duplicate code within a project is a conflict, not a silent overwrite |
| Service → deactivate UPDATE | Scoped WHERE prevents cross-project retire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | create/update/deactivate RAID | high | mitigate | assertProjectWriteAccess on every mutate (D-02, D-09) |
| T-12-02 | Elevation of Privilege | deactivateRisk / deactivateIssue | high | mitigate | WHERE id AND project_id; empty RETURNING → NotFoundError |
| T-12-03 | Tampering | RAID code | high | mitigate | Partial unique index from 12-01 plus findByCode ConflictError; map SQLSTATE 23505 (D-05, D-09) |
| T-12-06 | Tampering | updateIssue / updateRisk fields | medium | mitigate | buildUpdate allowlist; add code and technology_council only; omit deactivated_at from allowlist |
| T-12-07 | Tampering | risks.repo / issues.repo retire path | high | mitigate | Deactivate UPDATE only; stop exporting row-removal functions (D-05) |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
| T-12-08 | Tampering | HTTP DELETE still accepted | low | accept | D-13 maps DELETE to deactivate with 200 { ok: true } |
</threat_model>

<verification>
- 12-02-01: risks unique code + deactivate tests green
- 12-02-02: issues unique code + technology_council tests green
- 12-02-03: DELETE route tests still 200 { ok: true }
- Wave: npx vitest run lib/services/risks.service.unit.test.ts lib/services/issues.service.unit.test.ts app/api/projects/[id]/risks/route.test.ts app/api/projects/[id]/issues/route.test.ts -x
</verification>

<success_criteria>
PM/CPMO mutate risks and issues with unique per-project codes; Viewer cannot; DELETE deactivates in place (RAID-01, D-05, D-09, D-13).
</success_criteria>

<output>
Create `.planning/phases/12-milestone-raid-master-registers/12-02-SUMMARY.md` when done
</output>
