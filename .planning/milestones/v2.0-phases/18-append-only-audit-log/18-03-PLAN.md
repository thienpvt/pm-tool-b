---
phase: 18-append-only-audit-log
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
  - lib/services/milestones.service.ts
  - lib/services/milestones.service.unit.test.ts
  - lib/services/project-document-checklist.service.ts
  - lib/services/project-document-checklist.service.unit.test.ts
autonomous: true
requirements: [AUDIT-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "createProject after a successful insert calls auditLog entity_type project action create with before null (D-02, D-03, AUDIT-01)."
    - "updateProject for general fields (name, status, rag, and other governed fields) calls auditLog action update with full before/after snapshots; existing code_change and stage_change_ack audits remain (D-02, D-03)."
    - "deleteProject loads the prior row then calls auditLog action delete with after null (D-02, D-03)."
    - "createMilestone and updateMilestone call auditLog entity_type milestone actions create and update; cancel remains (D-02, D-03)."
    - "patchChecklistItem audits when status, confluence_url, approved_at, approved_by, na_reason, or notes change (D-02, D-03, AUDIT-01)."
    - "No leftover ops/admin routes are re-gated or newly audited (D-09)."
  artifacts:
    - path: "lib/services/projects.service.ts"
      provides: "create/update/delete auditLog gap fill (D-02)"
    - path: "lib/services/milestones.service.ts"
      provides: "create/update auditLog gap fill (D-02)"
    - path: "lib/services/project-document-checklist.service.ts"
      provides: "remaining PATCH field audit (D-02)"
  key_links:
    - from: "lib/services/projects.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog on create, general update, delete (D-02)"
      pattern: "auditLog"
    - from: "lib/services/milestones.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog on create and update (D-02)"
      pattern: "auditLog"
    - from: "lib/services/project-document-checklist.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog on remaining PATCH fields (D-02)"
      pattern: "auditLog"
  prohibitions:
    - editing leftover ops/admin/config services or routes (D-09)
    - CASL (D-09)
    - UI-SPEC (D-08)
    - adding npm packages
    - changing GET /api/audit (18-01)
    - changing risk/issue services (18-02)
---

<objective>
Close remaining D-02 gaps: project master create/update/delete, milestone create/update, and document-checklist PATCH fields beyond status/url. Payload D-03. AUDIT-01 project, milestone, and checklist halves.

Purpose: Those mutators either skip audit entirely or only fire on specialized actions.

Output: Tested auditLog calls in projects.service.ts, milestones.service.ts, and project-document-checklist.service.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-append-only-audit-log/18-CONTEXT.md
@.planning/phases/18-append-only-audit-log/18-RESEARCH.md
@.planning/phases/18-append-only-audit-log/18-PATTERNS.md
@.planning/phases/18-append-only-audit-log/18-VALIDATION.md

Discretion locked by planner: single update action with full snapshots for general project field changes (matches users.service); keep specialized code_change and stage_change_ack. Isolation none.

<interfaces>
auditLog from lib/services/audit.service.ts. Call after successful repo write.

Project auditSnapshot(row): if !row return null; else { id, name, project_code, status, rag, stage, company_id, customer_id, portfolio_year }.

createProject: after createProjectRepo and generateProjectChecklist, auditLog entity_type 'project', entity_id String(row.id), action 'create', before null, after auditSnapshot(row). Do not audit when ForbiddenError/ValidationError/ConflictError fire before insert.

updateProject: current is already loaded. After updateProjectRepo returns row, auditLog action 'update' with before auditSnapshot(current) and after auditSnapshot(row) when those snapshots differ. Keep existing code_change (before the write) and stage_change_ack (after generate). Existing unit tests use toHaveBeenCalledWith objectContaining — additional update calls must not break those tests.

deleteProject: load getProjectRepo first; then deleteProjectRepo; then auditLog action 'delete', entity_type 'project', entity_id String(projectId), before auditSnapshot(prior), after null (D-03). If getProjectRepo is empty, still delete as today (do not newly 404 unless already required).

Milestone auditSnapshot(row): { id, name, status, start_date, end_date, plan_end } or null.

createMilestone: after createMilestoneRepo, auditLog entity_type 'milestone', action 'create', before null, after snapshot.

updateMilestone: load getMilestoneRepo before updateMilestoneRepo; after successful update, auditLog action 'update' with before/after snapshots. Keep cancelMilestone audit action cancel.

Checklist: replace the current status-or-url-only guard. Build before/after objects { status, confluence_url, approved_at, approved_by, na_reason, notes } from existing vs updated. If any of those six fields differ, call auditLog entity_type 'document_checklist', entity_id String(updated.id). Action stays 'status_change' when status or confluence_url differs; otherwise action 'update'. Extend project-document-checklist.service.unit.test.ts: existing status_change test stays; add PATCH that changes only approved_at/approved_by, only na_reason, and only notes — each must call auditLog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="18-03-01">
  <name>Audit createProject, general updateProject, and deleteProject</name>
  <files>lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts</files>
  <read_first>
  - lib/services/projects.service.ts createProject updateProject deleteProject
  - lib/services/projects.service.unit.test.ts createProject stamp test, code_change test, stage_change_ack test, deleteProject describe
  - lib/services/users.service.ts create/update auditLog
  - .planning/phases/18-append-only-audit-log/18-CONTEXT.md D-02 D-03
  </read_first>
  <behavior>
  - Successful createProject calls auditLog entity_type project action create with before null after the write and generateProjectChecklist (D-02, D-03, AUDIT-01).
  - updateProject that persists name or status calls auditLog action update with snapshots; code_change and stage_change_ack tests still pass (D-02).
  - deleteProject calls auditLog action delete with after null and still calls deleteProjectRepo (D-02, D-03).
  - Failed create (PM ForbiddenError) does not call auditLog.
  </behavior>
  <action>
  TDD (D-08). Isolation none. RED: failing assertions on the existing CPMO create success test plus new cases for general update (name/status/rag) and deleteProject audit. Do not weaken code_change or stage_change_ack expectations. Commit `test(18-03): red project create update delete audit`.
  GREEN: add auditSnapshot and the three auditLog sites per interfaces. Commit `feat(18-03): audit project master mutations`.
  Do not edit RAID services. Do not add UI.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts</automated>
  </verify>
  <done>Project create, general update, and delete append audit rows; specialized code_change and stage_change_ack remain (D-02, D-03, AUDIT-01).</done>
  <acceptance_criteria>
  - createProject success expects auditLog action create
  - general updateProject expects action update
  - deleteProject expects action delete with after null
  - existing code_change and stage_change_ack assertions still pass
  - npx vitest run lib/services/projects.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="18-03-02">
  <name>Audit createMilestone and updateMilestone</name>
  <files>lib/services/milestones.service.ts, lib/services/milestones.service.unit.test.ts</files>
  <read_first>
  - lib/services/milestones.service.ts
  - lib/services/milestones.service.unit.test.ts create/update/cancel tests
  </read_first>
  <behavior>
  - createMilestone success calls auditLog entity_type milestone action create with before null (D-02, D-03, AUDIT-01).
  - updateMilestone success calls auditLog action update with prior vs updated snapshots (D-02).
  - cancelMilestone still emits action cancel; write-denied paths still do not call auditLog.
  </behavior>
  <action>
  TDD. RED: failing tests on the existing createMilestone success path plus a new updateMilestone audit case. Keep cancel tests. Commit `test(18-03): red milestone create and update audit`.
  GREEN: load prior on update via getMilestoneRepo; auditLog after successful create/update per interfaces. Commit `feat(18-03): audit milestone create and update`.
  Do not audit linkEpic/unlinkEpic (not D-02 entity mutators).
  </action>
  <verify>
    <automated>npx vitest run lib/services/milestones.service.unit.test.ts</automated>
  </verify>
  <done>Milestone create and update append audit rows; cancel is unchanged (D-02, D-03, AUDIT-01).</done>
  <acceptance_criteria>
  - createMilestone expects auditLog action create
  - updateMilestone expects auditLog action update
  - cancelMilestone still expects action cancel
  - npx vitest run lib/services/milestones.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="18-03-03">
  <name>Audit remaining document-checklist PATCH fields</name>
  <files>lib/services/project-document-checklist.service.ts, lib/services/project-document-checklist.service.unit.test.ts</files>
  <read_first>
  - lib/services/project-document-checklist.service.ts patchChecklistItem
  - lib/services/project-document-checklist.service.unit.test.ts existingItem and status_change test
  - .planning/phases/18-append-only-audit-log/18-VALIDATION.md D-02 checklist row
  </read_first>
  <behavior>
  - PATCH that changes only approved_at and approved_by (status already approved) calls auditLog entity_type document_checklist (D-02, D-03, AUDIT-01).
  - PATCH that changes only na_reason for status not_applicable calls auditLog.
  - PATCH that changes only notes calls auditLog.
  - Existing status_change audit when status or confluence_url changes still fires with action status_change.
  - PATCH that changes none of those six fields does not call auditLog.
  </behavior>
  <action>
  TDD. RED: failing tests for approved fields, na_reason, and notes PATCHes using existingItem fixtures (set status approved or not_applicable on the existing mock as needed so assertChecklistPatchRules still passes). Commit `test(18-03): red checklist PATCH audit coverage`.
  GREEN: broaden the change detection and payload per interfaces. Commit `feat(18-03): audit remaining checklist PATCH fields`.
  Do not edit leftover ops/admin (D-09). Do not add pages (D-08). After green, run the already-covered entity unit tests as regression: users, pm-assignments, fiscal-budget, weekly-reports.
  </action>
  <verify>
    <automated>npx vitest run lib/services/project-document-checklist.service.unit.test.ts lib/services/projects.service.unit.test.ts lib/services/milestones.service.unit.test.ts lib/services/users.service.unit.test.ts lib/services/pm-assignments.service.unit.test.ts lib/services/fiscal-budget.service.unit.test.ts lib/services/weekly-reports.service.unit.test.ts</automated>
  </verify>
  <done>Checklist PATCH of compliance fields appends audit; project and milestone gap fills stay green; already-covered D-02 entities still audit (D-02, D-03, D-08, D-09, AUDIT-01).</done>
  <acceptance_criteria>
  - approved_at/approved_by-only PATCH expects auditLog
  - na_reason-only PATCH expects auditLog
  - notes-only PATCH expects auditLog
  - status_change test still expects action status_change
  - npx vitest run of the listed service unit files exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project/milestone/checklist mutators → auditLog | After successful write; tenant access already asserted by existing write gates |
| App → leftover ops/admin | Must not newly wrap or audit those routes (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-09 | Repudiation | createProject / updateProject / deleteProject | medium | mitigate | auditLog after successful write with snapshots (D-02, D-03) |
| T-18-10 | Repudiation | createMilestone / updateMilestone / patchChecklistItem | medium | mitigate | auditLog after successful write (D-02, D-03) |
| T-18-11 | Elevation of Privilege | leftover ops/admin | high | mitigate | Do not add role wrappers or audit calls on leftover ops/admin/config (D-09) |
| T-18-12 | Tampering | auditLog from these services | high | mitigate | Existing INSERT-only auditLog; no new mutate helpers (D-01, D-04) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 18-03-01: project create/update/delete audit green
- 18-03-02: milestone create/update audit green
- 18-03-03: remaining checklist PATCH audit plus D-02 regression files green
- Wave: npx vitest run lib/services/projects.service.unit.test.ts lib/services/milestones.service.unit.test.ts lib/services/project-document-checklist.service.unit.test.ts
</verification>

<success_criteria>
Project master, milestone, and document-checklist mutators listed in D-02 append actor/time/entity/before-after via existing auditLog; leftover ops/admin is untouched (AUDIT-01, D-02, D-03, D-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/18-append-only-audit-log/18-03-SUMMARY.md` when done
</output>
