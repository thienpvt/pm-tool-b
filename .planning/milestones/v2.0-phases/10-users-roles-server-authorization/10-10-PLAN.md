---
phase: 10-users-roles-server-authorization
plan: 10
type: execute
wave: 4
depends_on: [10-04, 10-09]
files_modified:
  - lib/http/role-matrix.test.ts
  - app/api/portfolio/route.ts
  - app/api/portfolio/roadmap/route.ts
  - app/api/portfolio/quota/route.ts
  - app/api/portfolio/program-allocations/[id]/route.ts
  - app/api/portfolio/members/route.ts
  - app/api/portfolio/members/[id]/route.ts
  - app/api/portfolio/budgets/route.ts
  - app/api/portfolio/budgets/[id]/route.ts
  - app/api/portfolio/budgets/[id]/categories/route.ts
  - app/api/portfolio/budgets/[id]/categories/[catId]/route.ts
  - app/api/portfolio/budgets/[id]/allocations/route.ts
  - app/api/portfolio/budgets/[id]/allocations/[allocId]/route.ts
  - app/api/programs/route.ts
  - app/api/portfolio/milestones/route.ts
  - app/api/portfolio/report/route.ts
  - app/api/portfolio/roadmap/epics/route.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A single role-matrix file proves Viewer GET allow and Viewer POST deny on representative routes (D-19, AUTH-05)."
    - "Leftover portfolio and programs actors (GET /api/portfolio, GET /api/portfolio/roadmap, GET /api/programs, GET /api/portfolio/milestones, GET /api/portfolio/report, GET /api/portfolio/roadmap/epics, plus quota/members/budgets) copy roles via toAccessActor (D-03, D-24)."
  artifacts:
    - path: "lib/http/role-matrix.test.ts"
      provides: "Viewer/PM/CPMO matrix across >=1 GET and >=1 POST (D-19)"
    - path: "app/api/portfolio/quota/route.ts"
      provides: "toAccessActor(user) instead of a two-field actor (D-03)"
    - path: "app/api/portfolio/route.ts"
      provides: "GET uses toAccessActor(user) so 10-09 company lists see roles (D-03)"
    - path: "app/api/programs/route.ts"
      provides: "GET+POST peel toAccessActor(user) (D-03, D-24)"
    - path: "app/api/portfolio/milestones/route.ts"
      provides: "GET uses toAccessActor(user) (D-03, D-24)"
    - path: "app/api/portfolio/report/route.ts"
      provides: "GET uses toAccessActor(user); POST peel remains 10-08 (D-03, D-24)"
    - path: "app/api/portfolio/roadmap/epics/route.ts"
      provides: "GET assertProjectAccess uses toAccessActor(user) (D-03, D-24)"
  key_links:
    - from: "hand-rolled portfolio actor helpers"
      to: "toAccessActor"
      via: "SessionUser fields including roles (D-03)"
      pattern: "toAccessActor"
    - from: "lib/http/role-matrix.test.ts"
      to: "app/api/projects/[id]/risks POST"
      via: "Viewer 403 proof (D-15, D-19)"
      pattern: "role-matrix"
  prohibitions:
    - wrapping platform /api/admin/companies in CPMO (D-21, D-23)
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** (D-23 AUTH-05 carve-out)
    - adding npm packages
---

<objective>
Finish AUTH-05 actor peel and the D-19 role matrix (D-03, D-15, D-19, D-24): replace leftover two-field actors on GET /api/portfolio, GET /api/portfolio/roadmap, GET+POST /api/programs, GET /api/portfolio/milestones, GET /api/portfolio/report, GET /api/portfolio/roadmap/epics, and portfolio quota/members/budgets/allocations routes with toAccessActor; ship lib/http/role-matrix.test.ts covering GET allow + POST deny for Viewer. Runs after 10-04 (risks POST write assert) and 10-09 (assertCompanyWrite needs actor.roles). File count is 17; keep these peels here rather than splitting a task or adding a 12th plan.

Purpose: Services already compose tenant+role. Routes that build actors by hand must carry roles or write asserts never see them.

Output: role-matrix.test.ts green; portfolio GET/roadmap plus remaining two-field peels (programs GET+POST, milestones GET, report GET, roadmap/epics GET) and the existing actorOf sites use toAccessActor. D-23 leftover ops/admin stay session+tenant.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-04-PLAN.md
@.planning/phases/10-users-roles-server-authorization/10-09-PLAN.md

AUTH-05 inventory: this plan owns leftover actors on GET /api/portfolio, GET /api/portfolio/roadmap, GET+POST /api/programs, GET /api/portfolio/milestones, GET /api/portfolio/report, GET /api/portfolio/roadmap/epics, and the ten portfolio files listed in files_modified, plus role-matrix.test.ts. Wave 4: 10-04 must have switched risks mutators to assertProjectWriteAccess (PM-unassigned POST 403); 10-09 must have exported assertCompanyWrite (portfolio writes need actor.roles). 10-08 already peels POST /api/portfolio/report; this plan peels GET only on that file. Carve-out D-23: app/api/operations/** and platform /api/admin/companies stay session+tenant. Do not convert those platform routes.

<interfaces>
role-matrix.test.ts: table of { method, import route, session fixture } for GET /api/projects/:id (viewer 200 when in-company) and POST /api/projects/:id/risks (viewer 403). Follow route.access.test.ts mocks. At least one PM unassigned POST 403 and one CPMO in-company POST 200 (may mock repo create).

Hand-rolled actors in app/api/portfolio/route.ts, app/api/portfolio/roadmap/route.ts, app/api/programs/route.ts (GET+POST), app/api/portfolio/milestones/route.ts, GET app/api/portfolio/report/route.ts, app/api/portfolio/roadmap/epics/route.ts, and the listed quota/members/budgets files: replace { company_id, is_admin } (and actorOf helpers) with toAccessActor(user) from access.ts so roles are present for 10-09 assertCompanyWrite. If a file already uses withAuth, no actorOf remains. On portfolio/report/route.ts, peel GET only; leave the 10-08 POST isCpmo gate in place.

Keep ACCESS_ENFORCEMENT shadow behavior unchanged (accept existing Phase 6 opt-in).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-10-01">
  <name>Ship Viewer/PM/CPMO role-matrix test</name>
  <files>lib/http/role-matrix.test.ts</files>
  <read_first>
  - app/api/projects/[id]/route.access.test.ts
  - app/api/projects/[id]/risks/route.access.test.ts
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-15, D-19)
  </read_first>
  <behavior>
  - role-matrix.test.ts: Viewer GET in-company project 200 (D-19, AUTH-04).
  - Viewer POST risks 403 (D-15, D-19, AUTH-05).
  - PM unassigned POST risks 403 (D-14, D-19).
  - CPMO in-company POST risks is not 403 when tenant allows (D-13, D-19).
  </behavior>
  <action>
  TDD (D-19): write lib/http/role-matrix.test.ts using the same getSessionFromRequest mock as route.access tests. Cover the four cases above. Keep existing route.access tests; this file is the matrix, not a replacement.
  </action>
  <verify>
    <automated>npx vitest run lib/http/role-matrix.test.ts app/api/projects/[id]/route.access.test.ts app/api/projects/[id]/risks/route.access.test.ts -x</automated>
  </verify>
  <done>A single role-matrix file proves Viewer/PM/CPMO on representative GET+POST (D-03, D-15, D-19, AUTH-05).</done>
  <acceptance_criteria>
  - lib/http/role-matrix.test.ts exists and includes Viewer POST 403
  - npx vitest run lib/http/role-matrix.test.ts app/api/projects/[id]/route.access.test.ts app/api/projects/[id]/risks/route.access.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-10-02">
  <name>toAccessActor on portfolio GET, programs, milestones, quota, allocations, and members</name>
  <files>app/api/portfolio/route.ts, app/api/portfolio/roadmap/route.ts, app/api/portfolio/quota/route.ts, app/api/portfolio/program-allocations/[id]/route.ts, app/api/portfolio/members/route.ts, app/api/portfolio/members/[id]/route.ts, app/api/portfolio/budgets/route.ts, app/api/programs/route.ts, app/api/portfolio/milestones/route.ts</files>
  <read_first>
  - app/api/portfolio/route.ts
  - app/api/portfolio/roadmap/route.ts
  - app/api/portfolio/quota/route.ts (actorOf analog)
  - app/api/programs/route.ts (GET+POST two-field actor)
  - app/api/portfolio/milestones/route.ts
  - lib/services/access.ts toAccessActor
  </read_first>
  <behavior>
  - Each listed route builds ctx.actor / service actor via toAccessActor(user) including roles (D-03, D-24).
  - GET+POST /api/programs and GET /api/portfolio/milestones peel toAccessActor(user), not a two-field actor (D-24).
  </behavior>
  <action>
  Replace inline actor objects that only copy company_id and the leftover flag with toAccessActor(user) on GET /api/portfolio, GET /api/portfolio/roadmap, GET+POST /api/programs, GET /api/portfolio/milestones, quota, program-allocations, members, and budgets/route.ts (D-24). Leave platform admin routes unchanged (D-21, D-23).
  </action>
  <verify>
    <automated>npx vitest run lib/http/role-matrix.test.ts -x</automated>
  </verify>
  <done>GET portfolio/roadmap/programs/milestones, quota, program-allocations, and members actors carry roles (D-03, D-24, AUTH-05).</done>
  <acceptance_criteria>
  - Listed files call toAccessActor
  - GET+POST /api/programs and GET /api/portfolio/milestones call toAccessActor (D-24)
  - npx vitest run lib/http/role-matrix.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-10-03">
  <name>toAccessActor on portfolio budget routes, report GET, and epics</name>
  <files>app/api/portfolio/budgets/[id]/route.ts, app/api/portfolio/budgets/[id]/categories/route.ts, app/api/portfolio/budgets/[id]/categories/[catId]/route.ts, app/api/portfolio/budgets/[id]/allocations/route.ts, app/api/portfolio/budgets/[id]/allocations/[allocId]/route.ts, app/api/portfolio/report/route.ts, app/api/portfolio/roadmap/epics/route.ts</files>
  <read_first>
  - app/api/portfolio/budgets/route.ts
  - app/api/portfolio/report/route.ts (GET two-field actor; POST is 10-08)
  - app/api/portfolio/roadmap/epics/route.ts
  - lib/services/access.ts toAccessActor
  </read_first>
  <behavior>
  - Each listed budget/category/allocation route builds the actor via toAccessActor(user) including roles (D-03).
  - GET /api/portfolio/report and GET /api/portfolio/roadmap/epics pass toAccessActor(user) including roles (D-03, D-24).
  </behavior>
  <action>
  Same replacement as 10-10-02 on the six budget-tree files. On GET /api/portfolio/report, replace the two-field actor passed to getPortfolioReport with toAccessActor(user); do not redo the 10-08 POST isCpmo gate. On GET /api/portfolio/roadmap/epics, pass toAccessActor(user) into assertProjectAccess. Leave app/api/operations/** and /api/admin/companies unchanged (D-23).
  </action>
  <verify>
    <automated>npx vitest run lib/http/role-matrix.test.ts app/api/projects/[id]/route.access.test.ts -x</automated>
  </verify>
  <done>Portfolio budget-tree leftover actors plus GET portfolio/report and GET roadmap/epics carry roles (D-03, D-24, AUTH-05).</done>
  <acceptance_criteria>
  - Listed files call toAccessActor
  - GET /api/portfolio/report and GET /api/portfolio/roadmap/epics call toAccessActor (D-24)
  - npx vitest run lib/http/role-matrix.test.ts app/api/projects/[id]/route.access.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-rolled actor without roles → service write assert | Missing roles would skip Viewer deny |
| Platform admin routes | Must stay break-glass, not CPMO (D-21, D-23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Elevation of Privilege | actorOf without roles | high | mitigate | toAccessActor on leftover portfolio, programs, milestones, report GET, and epics routes (D-03, D-24) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-10-01: role-matrix + project access tests (needs 10-04 assertProjectWriteAccess)
- 10-10-02 / 10-10-03: toAccessActor on GET portfolio/roadmap/programs/milestones/report/epics and leftover quota/members/budgets files (needs 10-09 assertCompanyWrite)
</verification>

<success_criteria>
role-matrix proves Viewer POST 403; leftover portfolio, programs, milestones, report GET, and epics actors include roles (AUTH-05, D-03, D-19, D-24). D-23 leftover ops/admin are not gated here.
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-10-SUMMARY.md` when done
</output>
