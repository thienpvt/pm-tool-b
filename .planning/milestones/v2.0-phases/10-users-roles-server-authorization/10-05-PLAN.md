---
phase: 10-users-roles-server-authorization
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/services/users.service.ts
  - lib/services/users.service.unit.test.ts
  - lib/services/audit.service.ts
  - lib/services/audit.service.unit.test.ts
  - lib/repositories/users.repo.ts
  - lib/repositories/users.repo.test.ts
  - lib/http/with-role.ts
  - app/api/admin/users/route.ts
  - app/api/admin/users/schema.ts
  - app/api/admin/users/route.test.ts
autonomous: true
requirements: [USER-01, USER-02, USER-03, USER-04, USER-05, USER-06]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Admin can list, search, and filter users by status, role, and unit (D-20 unit is company_id; USER-01)."
    - "Admin can create or update a user with unique username and unique email; a locked account's username/email cannot be reused on another account (D-06, D-12, USER-02)."
    - "Admin can assign one or more roles to a user; effective permissions are the union of assigned roles; empty roles are invalid (D-01, D-04, USER-03)."
    - "Admin can set account status to Active, Inactive, or Locked; only Active users can log in (D-05, USER-04)."
    - "Admin can lock or unlock an account; the system records who changed the record and when (D-08, USER-05)."
    - "A user who has generated business data cannot be physically deleted; history still shows their display name (D-07, USER-06)."
    - "CPMO company user admin is split from platform /api/admin/companies (D-21)."
  artifacts:
    - path: "lib/services/users.service.ts"
      provides: "CPMO-scoped list/filter/create/update/lock/deactivate + uniqueness + audit (USER-01..06)"
    - path: "lib/repositories/users.repo.ts"
      provides: "SQL for users + user_roles; no DELETE FROM users (D-07)"
    - path: "lib/services/audit.service.ts"
      provides: "append-only auditLog INSERT (D-08)"
      exports: ["auditLog"]
    - path: "lib/http/with-role.ts"
      provides: "withAuth + require cpmo + session company (D-17, D-21)"
    - path: "app/api/admin/users/route.ts"
      provides: "Thin CPMO user admin; DELETE maps to deactivate (D-07, D-21)"
    - path: "app/api/admin/users/route.test.ts"
      provides: "CPMO GET/POST 200/201; non-cpmo 403; unauthenticated 401 (D-19, D-21)"
  key_links:
    - from: "app/api/admin/users/route.ts"
      to: "lib/services/users.service.ts"
      via: "withRole cpmo then listUsers/createUser/updateUser/deactivateUser (D-21)"
      pattern: "from '@/lib/services/users.service'"
    - from: "lib/services/users.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog after successful create, update, lock, unlock, and deactivate (D-08)"
      pattern: "auditLog"
    - from: "app/api/admin/companies/route.ts"
      to: "requireAdmin break-glass"
      via: "unchanged platform ops (D-21)"
      pattern: "requireAdmin"
  prohibitions:
    - physical DELETE FROM users (D-07)
    - listing all tenants' users for a company CPMO (D-21)
    - routing CPMO user CRUD through listAdminUsers(null, true)
    - moving schema out of getDb (D-22)
    - adding npm packages
    - a new UI-SPEC
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Deliver USER-01..USER-06 on a CPMO company-scoped users service (D-04, D-06, D-07, D-08, D-12, D-20, D-21): list/search/filter by status, role, and company; unique username/email including locked rows; multi-role union with min one role; Active/Inactive/Locked; lock/unlock with auditLog on create, update, lock, unlock, and deactivate; deactivate instead of physical delete. /admin Users UI and Sidebar are 10-11. Platform companies routes stay on the break-glass flag (D-21, D-23).

Purpose: Admins can manage users without a global user table leak, and USER-05 starts incremental audit.

Output: users.service + users.repo + audit.service + thin /api/admin/users + route.test.ts.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-RESEARCH.md
@.planning/phases/10-users-roles-server-authorization/10-PATTERNS.md
@.planning/phases/10-users-roles-server-authorization/10-VALIDATION.md
@.planning/phases/10-users-roles-server-authorization/10-01-PLAN.md

Schema and role backfill already exist from 10-01 (user_roles, status, email, audit_logs, unique email index). Do not add Prisma (D-22).

Flagged A1 locked D-20. A4 locked D-21. Discretion: extend existing /admin page Users tab rather than a new route.

<interfaces>
users.service.ts analog holidays.service.ts: assertCpmoCompany(actor) throws ForbiddenError unless hasRole cpmo and actor.company_id is not null. All methods take actor and scope SQL to that company_id. Do not accept a target company_id from the body that differs from actor.company_id.

listUsers(actor, filters: { q?: string; status?: UserStatus; role?: AppRole }): JOIN user_roles; filter status, role, company_id (unit, D-20); search q against username, display_name, email ILIKE. Exclude nothing for locked rows (they must remain listed so credentials stay reserved, D-06). Soft-deleted (deleted_at set) still list with status inactive so history/display_name remain (USER-06) unless filter status says otherwise — default list includes inactive/locked.

createUser(actor, input): Zod already parsed. Hash password via existing hashPassword. roles.min(1) (D-04). Unique username / unique lower(email) → ConflictError 409 (D-12). Insert users + user_roles. Do not set break-glass from cpmo (D-03). auditLog action create.

updateUser(actor, id, input): load row in company or NotFoundError; foreign company ForbiddenError (not 404 if you can detect existence — if id exists other company ForbiddenError; if missing NotFoundError). Update display_name, email, status, roles (replace set, min 1). Unique conflicts ConflictError. Locked email/username stay on the same row (D-06) — do not free them. auditLog update with before/after JSON.

lockUser/unlockUser: status locked/active; set/clear locked_at, locked_by = actor.user_id; DELETE FROM sessions WHERE user_id = ? on lock (D-10). auditLog lock/unlock (D-08).

deactivateUser: SET status inactive, deleted_at = now(); never db.run DELETE FROM users (D-07). Self-deactivate ForbiddenError or ValidationError. auditLog deactivate. Route DELETE calls deactivateUser.

auditLog({ actor_id, company_id, entity_type, entity_id, action, before, after }): INSERT only. No UPDATE/DELETE helpers.

users.repo.ts: column allowlist for updates (mirror PROJECT_COLUMNS). Map PG unique_violation 23505 to throw a repo error the service turns into ConflictError, or pre-check findUserByUsernameInCompany / findUserByEmailLower.

with-role.ts: withAuth then if !hasRole(ctx.actor, required) return 403 Forbidden. Export withCpmo(handler, opts) for users routes.

app/api/admin/users/schema.ts: createUserSchema username, password min 8, display_name optional, email z.email(), roles z.array(z.enum(['cpmo','pm','viewer'])).min(1), status enum default active. Do not take company_id from client. updateUserSchema id, optional email/display_name/roles/status/password. Remove is_admin from the CPMO schema (D-03). Frozen 400: keep a single error string — use { error: 'Username and password required' } on create parse fail and { error: 'id required' } on update parse fail to match current bodies unless tests force a better message; prefer keeping those literals.

Route: GET query q, status, role. POST 201. PUT. DELETE ?id= deactivate. Stop importing admin.repo user functions. Leave deleteAdminUser unused (or make it throw if called).

Platform /api/admin/companies, demo-requests, jira-config, rag-config: unchanged requireAdmin on the break-glass field (D-21, D-23). Leftover app/api/operations/** stays session+tenant (D-23).

Sidebar and /admin Users tab are 10-11 (D-18).

Password min 8 on create (research Zod). Existing users keep hashes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-05-01">
  <name>CPMO user list/create/update with unique credentials and roles</name>
  <files>lib/repositories/users.repo.ts, lib/repositories/users.repo.test.ts, lib/services/users.service.ts, lib/services/users.service.unit.test.ts, lib/http/with-role.ts, app/api/admin/users/schema.ts</files>
  <read_first>
  - lib/services/holidays.service.ts (assert + ConflictError)
  - lib/repositories/admin.repo.ts (listAdminUsers / createAdminUser — do not keep global list)
  - app/api/admin/users/schema.ts
  - lib/http/with-auth.ts
  - lib/services/access.ts (hasRole, toAccessActor)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-01, D-04, D-12, D-20, D-21)
  </read_first>
  <behavior>
  - listUsers for CPMO company 5 never returns company 9 rows (D-21, USER-01).
  - listUsers filters by status, role, and q against username/email/display_name (USER-01, D-20).
  - createUser with duplicate username or duplicate lower(email) throws ConflictError (D-12, USER-02).
  - createUser with empty roles throws ValidationError (D-04, USER-03).
  - createUser stamps company_id from actor not body (D-21).
  - Non-cpmo actor throws ForbiddenError on list/create (D-21).
  </behavior>
  <action>
  TDD (D-19): write users.service.unit.test.ts and users.repo tests first. Then implement repo, service, withCpmo, schema. Stop using listAdminUsers(null, true) as the CPMO list path. Lock/deactivate is 10-05-03. Hash passwords with existing hashPassword (D-09). auditLog wiring is 10-05-02.
  </action>
  <verify>
    <automated>npx vitest run lib/services/users.service.unit.test.ts lib/repositories/users.repo.test.ts -x</automated>
  </verify>
  <done>CPMO can list/search/filter users in their company and create/update with unique username/email and at least one role (USER-01, USER-02, USER-03, D-04, D-12, D-20, D-21).</done>
  <acceptance_criteria>
  - users.service.ts scopes by actor.company_id
  - createUserSchema requires roles.min(1) and email
  - npx vitest run lib/services/users.service.unit.test.ts lib/repositories/users.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-05-02">
  <name>Thin users route plus auditLog on create and update</name>
  <files>lib/services/audit.service.ts, lib/services/audit.service.unit.test.ts, lib/services/users.service.ts, lib/services/users.service.unit.test.ts, app/api/admin/users/route.ts, app/api/admin/users/route.test.ts</files>
  <read_first>
  - lib/services/users.service.ts from 10-05-01
  - app/api/admin/users/route.ts
  - app/api/auth/login/route.test.ts (session mock pattern)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-08, D-21)
  </read_first>
  <behavior>
  - createUser inserts an audit_logs row with action create after a successful insert (D-08, USER-05).
  - updateUser inserts an audit_logs row with action update and before/after JSON (D-08, USER-05).
  - auditLog only INSERTs (D-08).
  - GET/POST /api/admin/users: CPMO 200/201; session without cpmo 403; no session 401 (D-19, D-21).
  - Route imports users.service rather than admin.repo user CRUD (D-21).
  </behavior>
  <action>
  TDD (D-19): write audit.service.unit.test.ts and app/api/admin/users/route.test.ts first (GET/POST CPMO vs non-cpmo vs no session). Implement auditLog INSERT. Call auditLog from createUser and updateUser (not only lock — lock is 10-05-03). Thin route: withCpmo then listUsers/createUser/updateUser. Leave deleteAdminUser unused by this route.
  </action>
  <verify>
    <automated>npx vitest run lib/services/audit.service.unit.test.ts lib/services/users.service.unit.test.ts app/api/admin/users/route.test.ts -x</automated>
  </verify>
  <done>Create and update persist actor and time in audit_logs; /api/admin/users is CPMO-scoped with route tests (D-08, D-19, D-21, USER-05).</done>
  <acceptance_criteria>
  - audit.service.ts exports auditLog as INSERT only
  - createUser and updateUser call auditLog
  - app/api/admin/users/route.test.ts covers CPMO success and non-cpmo 403
  - npx vitest run lib/services/audit.service.unit.test.ts lib/services/users.service.unit.test.ts app/api/admin/users/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-05-03">
  <name>Lock, unlock, and deactivate users</name>
  <files>lib/services/users.service.ts, lib/services/users.service.unit.test.ts, lib/services/audit.service.ts, app/api/admin/users/route.ts, app/api/admin/users/route.test.ts</files>
  <read_first>
  - lib/services/users.service.ts from 10-05-02
  - lib/repositories/admin.repo.ts deleteAdminUser (leave unused)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-05, D-06, D-07, D-08)
  </read_first>
  <behavior>
  - lockUser sets status locked, locked_at, locked_by, deletes that user's sessions, and inserts an audit_logs row (D-05, D-08, D-10, USER-05).
  - unlockUser sets status active and clears lock columns with an audit row (USER-05).
  - deactivateUser sets inactive + deleted_at and does not run DELETE FROM users (D-07, USER-06).
  - Creating a second user with a locked user's username or email throws ConflictError (D-06, USER-02).
  - Route DELETE calls deactivateUser not deleteAdminUser (D-07).
  </behavior>
  <action>
  TDD (D-19): users.service lock/deactivate/unique-locked cases plus route DELETE tests first. Implement lockUser, unlockUser, deactivateUser. Change route DELETE to deactivateUser. Leave deleteAdminUser unused by routes. After lock, sessions for that user_id are gone so AUTH-06 holds. Record actor.user_id and timestamps via auditLog (D-08).
  </action>
  <verify>
    <automated>npx vitest run lib/services/audit.service.unit.test.ts lib/services/users.service.unit.test.ts lib/repositories/users.repo.test.ts app/api/admin/users/route.test.ts -x</automated>
  </verify>
  <done>Lock/unlock/deactivate record actor and time in audit_logs; locked credentials stay reserved; no physical user delete (USER-04, USER-05, USER-06, D-06, D-07, D-08).</done>
  <acceptance_criteria>
  - users route DELETE does not call deleteAdminUser
  - Locked username/email unique test exists
  - lockUser and unlockUser call auditLog
  - npx vitest run lib/services/audit.service.unit.test.ts lib/services/users.service.unit.test.ts lib/repositories/users.repo.test.ts app/api/admin/users/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CPMO browser → /api/admin/users | Must not read/write other companies' users |
| Client body company_id / roles | Must not elevate to platform operator or other tenant |
| DELETE /api/admin/users | Must not physically remove a user row |
| audit_logs | Append-only; no edit API this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-17 | Elevation of Privilege | /api/admin/users | high | mitigate | withCpmo + actor.company_id scope; no global listAdminUsers (D-21) |
| T-10-18 | Tampering | create/update unique | high | mitigate | PG unique username + lower(email); ConflictError (D-06, D-12) |
| T-10-19 | Repudiation | lock/unlock/update | high | mitigate | auditLog actor/time/before/after (D-08, USER-05) |
| T-10-20 | Tampering | DELETE users | high | mitigate | deactivateUser only; no DELETE FROM users (D-07) |
| T-10-21 | Elevation of Privilege | roles / break-glass | high | mitigate | Do not set break-glass from cpmo; companies routes stay requireAdmin (D-03, D-21) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-05-01: users service/repo tests
- 10-05-02: auditLog on create/update + app/api/admin/users/route.test.ts
- 10-05-03: lock/deactivate tests + route DELETE
- UI is 10-11
</verification>

<success_criteria>
CPMO manages users in their company (list/search/filter, unique credentials, roles union, statuses, lock with audit on create/update/lock, no physical delete); platform companies API remains break-glass-only (USER-01..06, D-21, D-23).
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-05-SUMMARY.md` when done
</output>
