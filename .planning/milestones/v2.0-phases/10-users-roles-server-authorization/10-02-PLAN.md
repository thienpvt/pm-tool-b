---
phase: 10-users-roles-server-authorization
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/auth.ts
  - lib/auth.session.unit.test.ts
  - app/api/auth/session/extend/route.ts
  - app/api/auth/session/extend/route.test.ts
  - app/api/auth/logout/route.ts
  - app/api/auth/logout/route.test.ts
  - app/api/auth/me/route.ts
  - app/api/auth/me/route.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-06, USER-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An Active user can keep a session that expires per policy and can be extended without losing in-progress draft input (D-11, AUTH-02)."
    - "User can log out and end the session (D-09, AUTH-03)."
    - "Inactive or Locked users cannot keep a session after status changes mid-session (D-10, AUTH-06)."
    - "GET /api/auth/me returns roles and status from SessionUser (D-01, D-05, USER-03)."
  artifacts:
    - path: "lib/auth.ts"
      provides: "extendSession(sessionId) UPDATEs expires_at in place; getSessionUser already nulls non-active (D-10, D-11)"
      exports: ["extendSession"]
    - path: "app/api/auth/session/extend/route.ts"
      provides: "POST refreshes expiry; same pm_session cookie value (D-11, AUTH-02)"
    - path: "app/api/auth/logout/route.ts"
      provides: "Existing deleteSession + cookie clear (AUTH-03)"
    - path: "app/api/auth/me/route.ts"
      provides: "JSON includes roles and status from SessionUser (D-01, D-05, USER-03)"
  key_links:
    - from: "app/api/auth/session/extend/route.ts"
      to: "lib/auth.ts"
      via: "extendSession(cookie value) then leave cookie unchanged"
      pattern: "extendSession"
    - from: "lib/auth.ts getSessionUser"
      to: "lib/auth.ts deleteSession"
      via: "non-active status deletes the session row (D-10)"
      pattern: "deleteSession"
  prohibitions:
    - rotating or rewriting the pm_session cookie on extend (D-11)
    - a second draft token or JWT
    - replacing scrypt sessions (D-09)
    - adding npm packages
---

<objective>
Close AUTH-02 and AUTH-03 on the existing scrypt + DB session stack (D-09, D-11): extend a valid session by updating expires_at in place so client draft state is untouched; prove logout still deletes the session; prove a lock/inactive mid-session cannot keep using the cookie (D-10, AUTH-06 remainder). Expose roles and status on GET /api/auth/me (D-01, D-05, USER-03) — peeled from the 10-01 tracer.

Purpose: Session policy is a user-visible login promise. Extending without a new cookie is the AUTH-02 draft-preservation rule.

Output: POST /api/auth/session/extend, extendSession helper, logout + mid-session tests, /api/auth/me with roles and status.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-RESEARCH.md
@.planning/phases/10-users-roles-server-authorization/10-PATTERNS.md
@.planning/phases/10-users-roles-server-authorization/10-01-PLAN.md

Depends on 10-01: SessionUser.status, getSessionUser non-active → null, login status gate. Do not re-open schema.

Flagged assumption A5: dedicated POST /api/auth/session/extend (D-11), not auto-extend on GET /api/auth/me.

<interfaces>
createSession already inserts expires_at = now + 7 days. extendSession(sessionId): UPDATE sessions SET expires_at = ? WHERE id = ? AND expires_at > now-equivalent. Return true if a row was updated; false if missing or already expired.

POST /api/auth/session/extend: read cookie pm_session; if missing 401; if extendSession false 401 { error: 'Unauthorized' }; if true JSON { ok: true } without cookies.set of a new id. Cookie value must be identical to the request cookie.

Logout route already deletes session and sets cookie maxAge 0. Add tests only if none exist; do not change the JSON { ok: true } contract.

getSessionUser (10-01) already deletes session when status is not active. This plan adds a focused unit test: mock db.get returning a locked/inactive user, expect deleteSession SQL and null return.

GET /api/auth/me: add roles and status from SessionUser. Unauthenticated stays 401. Keep id, username, display_name, company_id, company_name, onboarding_completed, and the break-glass field. Dedicated extend stays on POST /api/auth/session/extend (D-11, A5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-02-01">
  <name>Extend session in place without rotating cookie</name>
  <files>lib/auth.ts, lib/auth.session.unit.test.ts, app/api/auth/session/extend/route.ts, app/api/auth/session/extend/route.test.ts</files>
  <read_first>
  - lib/auth.ts (createSession, SESSION_COOKIE_NAME, getSessionFromRequest)
  - app/api/auth/logout/route.ts (cookie clear analog — extend must not copy maxAge 0)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-11)
  - lib/repositories/auth.repo.unit.test.ts (vi.hoisted getDb mock)
  </read_first>
  <behavior>
  - extendSession(id) with an unexpired row UPDATEs expires_at to now+7 days and returns true (D-11).
  - extendSession(id) with a missing or already-expired row returns false and does not insert a new session id (D-11).
  - POST /api/auth/session/extend with a valid cookie returns 200 { ok: true } and does not Set-Cookie a different session id (AUTH-02, D-11).
  - POST /api/auth/session/extend with no cookie or a failed extend returns 401 (D-11).
  </behavior>
  <action>
  TDD (D-19): write lib/auth.session.unit.test.ts and extend route tests first. Then add extendSession next to createSession using the same 7-day duration constant (extract a named constant if both sites currently inline the millisecond math). Route: analog of logout but keep the cookie; call extendSession; 401 when false. Do not mint a new session id. Do not add npm packages.
  </action>
  <verify>
    <automated>npx vitest run lib/auth.session.unit.test.ts app/api/auth/session/extend/route.test.ts -x</automated>
  </verify>
  <done>A valid session can be extended for another 7 days without rotating pm_session, so in-progress draft input in the browser is not dropped (D-11, AUTH-02).</done>
  <acceptance_criteria>
  - lib/auth.ts exports extendSession
  - UPDATE targets the existing sessions.id
  - Extend route does not assign a new cookie value on success
  - npx vitest run lib/auth.session.unit.test.ts app/api/auth/session/extend/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-02-02">
  <name>Invalidate mid-session and prove logout</name>
  <files>lib/auth.ts, lib/auth.session.unit.test.ts, app/api/auth/logout/route.ts, app/api/auth/logout/route.test.ts</files>
  <read_first>
  - lib/auth.ts getSessionUser after 10-01
  - lib/auth.session.unit.test.ts from 10-02-01
  - app/api/auth/logout/route.ts
  - app/api/auth/session/extend/route.test.ts (cookie + 401 patterns)
  </read_first>
  <behavior>
  - getSessionUser for a row whose status is locked deletes that session id and returns null (D-10, AUTH-06).
  - getSessionUser for a row whose status is inactive deletes that session id and returns null (D-10).
  - getSessionUser for an active unexpired session still returns SessionUser including roles (D-01, AUTH-01).
  - POST /api/auth/logout with a pm_session cookie calls deleteSession with that id, returns { ok: true }, and sets the cookie to empty with maxAge 0 (AUTH-03, D-09).
  - POST /api/auth/logout with no cookie still returns { ok: true } and clears the cookie (AUTH-03).
  </behavior>
  <action>
  TDD (D-19): extend lib/auth.session.unit.test.ts with the three getSessionUser cases (mock getDb). 10-01 already implemented the production branch — if tests were missing, add them and keep the deleteSession-then-null behavior. The sessions row must be removed so a captured id cannot be replayed. Add logout route tests mocking deleteSession. Keep the success JSON and SESSION_COOKIE_NAME. Keep a single logout path.
  </action>
  <verify>
    <automated>npx vitest run lib/auth.session.unit.test.ts app/api/auth/logout/route.test.ts app/api/auth/session/extend/route.test.ts -x</automated>
  </verify>
  <done>Locking or deactivating a user mid-session makes getSessionUser return null and deletes the sessions row; logout ends the server session and clears the browser cookie (D-10, AUTH-03, AUTH-06, D-09).</done>
  <acceptance_criteria>
  - Locked and inactive cases call deleteSession and return null
  - Active case still returns the user
  - deleteSession is invoked when a logout cookie is present
  - Response clears pm_session
  - npx vitest run lib/auth.session.unit.test.ts app/api/auth/logout/route.test.ts app/api/auth/session/extend/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-02-03">
  <name>Expose roles and status on GET /api/auth/me</name>
  <files>app/api/auth/me/route.ts, app/api/auth/me/route.test.ts</files>
  <read_first>
  - app/api/auth/me/route.ts
  - lib/auth.ts SessionUser after 10-01
  - app/api/auth/login/route.test.ts (mock getSessionFromRequest pattern)
  </read_first>
  <behavior>
  - GET /api/auth/me with an active session returns JSON including roles (array) and status (D-01, D-05, USER-03).
  - GET /api/auth/me without a session stays 401 (existing contract).
  - Response still includes id, username, display_name, company_id, company_name, onboarding_completed, and the break-glass field for Sidebar compat (D-03, D-18 later).
  </behavior>
  <action>
  TDD (D-19): add app/api/auth/me/route.test.ts mocking getSessionFromRequest. Then add roles and status to the JSON body from SessionUser. Session extend stays on POST /api/auth/session/extend (D-11). Keep the break-glass field visible until 10-11 Sidebar reads roles.
  </action>
  <verify>
    <automated>npx vitest run app/api/auth/me/route.test.ts app/api/auth/login/route.test.ts -x</automated>
  </verify>
  <done>Authenticated /api/auth/me includes roles and status so later UI can gate on roles without a second source of truth (D-01, D-05, USER-03).</done>
  <acceptance_criteria>
  - me JSON includes roles and status from SessionUser
  - Unauthenticated GET remains 401
  - npx vitest run app/api/auth/me/route.test.ts app/api/auth/login/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser cookie → extend/logout | Caller may present a stolen, expired, or cleared cookie |
| getSessionUser → sessions table | Status change must revoke server-side state, not only UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Spoofing | extendSession | high | mitigate | UPDATE only when expires_at still in the future; expired id returns 401 (D-11) |
| T-10-07 | Spoofing | getSessionUser after lock | high | mitigate | Delete sessions row when status is not active (D-10) |
| T-10-08 | Tampering | Session cookie on extend | medium | mitigate | Do not issue a new session id; attacker cannot swap cookies via extend (D-11) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |

## Trust notes

T-10-02 from 10-01 is completed here for the mid-session half of AUTH-06.
</threat_model>

<verification>
- 10-02-01: extend unit + route tests
- 10-02-02: locked/inactive getSessionUser tests + logout tests
- 10-02-03: /api/auth/me includes roles and status
</verification>

<success_criteria>
A valid session can be extended without rotating the cookie; logout ends the session; Inactive/Locked cannot keep a session; /api/auth/me exposes roles and status (AUTH-02, AUTH-03, AUTH-06, USER-03).
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-02-SUMMARY.md` when done
</output>
