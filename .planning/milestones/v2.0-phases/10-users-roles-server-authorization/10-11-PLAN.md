---
phase: 10-users-roles-server-authorization
plan: 11
type: execute
wave: 3
depends_on: [10-05]
files_modified:
  - app/admin/page.tsx
  - components/layout/Sidebar.tsx
autonomous: true
requirements: [USER-01, USER-03, USER-04, USER-05, AUTH-05]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO can manage company users from /admin Users (list/search/filter/create/update/roles/status/lock) (D-18, USER-01)."
    - "Sidebar shows Admin Panel when roles includes cpmo or the break-glass field is set (D-18, D-21)."
    - "Platform Companies/Demo tabs stay visible only when the break-glass field is set (D-21)."
    - "Hiding a UI control is not treated as access control — server 403 from 10-05 remains the gate (D-15, AUTH-05)."
  artifacts:
    - path: "app/admin/page.tsx"
      provides: "Users tab: list/search/filter/create/update/roles/status (D-18, USER-01)"
    - path: "components/layout/Sidebar.tsx"
      provides: "Admin Panel link for cpmo or break-glass (D-18, D-21)"
  key_links:
    - from: "components/layout/Sidebar.tsx"
      to: "app/api/auth/me"
      via: "Me.roles from GET /api/auth/me (D-01, D-18)"
      pattern: "roles"
    - from: "app/admin/page.tsx"
      to: "app/api/admin/users/route.ts"
      via: "GET/POST/PUT/DELETE company-scoped users (D-21)"
      pattern: "/api/admin/users"
  prohibitions:
    - a new UI-SPEC
    - treating UI hiding as the AUTH-05 gate (D-15)
    - listing all tenants' users for a company CPMO (D-21)
    - adding npm packages
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Deliver enough /admin Users UI and Sidebar nav that a CPMO can manage company users (D-18, D-21): list/search/filter, create/edit with email and roles, lock/unlock/deactivate. Platform companies tabs stay on the break-glass flag.

Purpose: USER-01..05 screens. Server 403 from 10-05 is the authorization gate (D-15).

Output: Updated /admin Users tab + Sidebar CPMO link. human_verify_mode is end-of-phase — human-check, not a blocking checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-05-PLAN.md
@.planning/phases/10-users-roles-server-authorization/10-02-PLAN.md

10-05 shipped /api/admin/users. 10-02 shipped GET /api/auth/me with roles and status.

Flagged A4 locked D-21. Discretion: extend existing /admin page Users tab rather than a new route. No UI-SPEC (D-18).

<interfaces>
Sidebar Me type: add roles?: AppRole[]. Show Admin Panel if me.is_admin OR roles includes cpmo (D-18, D-21).

admin/page.tsx Users tab: table with search input, status filter, role filter; create/edit form with email, roles checkboxes (min 1), status select including lock/unlock; hide Companies/Demo tabs unless me.is_admin (D-21). Viewer never reaches this page without CPMO — still hide mutate buttons if roles are viewer-only (D-15, D-18). Server 403 remains the gate.

human_verify_mode is end-of-phase — include human-check, not a blocking checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-11-01">
  <name>Sidebar Admin Panel for CPMO and break-glass</name>
  <files>components/layout/Sidebar.tsx</files>
  <read_first>
  - components/layout/Sidebar.tsx Me type and Admin Panel link (~46, ~213)
  - app/api/auth/me/route.ts after 10-02 (roles on JSON)
  </read_first>
  <behavior>
  - Sidebar shows Admin Panel when roles includes cpmo or the break-glass field is set (D-18, D-21).
  - Me type includes roles from /api/auth/me (D-01, USER-03).
  </behavior>
  <action>
  Extend Me with roles?: AppRole[]. Show the Admin Panel link when roles includes cpmo or the existing break-glass field is set. Keep the link target /admin. Do not add a UI-SPEC.
  </action>
  <verify>
    <automated>npx vitest run app/api/auth/me/route.test.ts -x</automated>
    <human-check>As a company CPMO with roles: ['cpmo'] and the break-glass field unset, confirm Sidebar shows Admin Panel. Automated gate stays GET /api/auth/me (new Sidebar test file would exceed this plan's cheap-gate budget).</human-check>
  </verify>
  <done>A company CPMO sees Admin Panel without needing the break-glass flag (D-18, D-21).</done>
  <acceptance_criteria>
  - Sidebar Admin link allows cpmo without requiring break-glass
  - Human-check: Admin Panel visible for roles ['cpmo'] without break-glass (D-18, D-21)
  - npx vitest run app/api/auth/me/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-11-02">
  <name>Admin Users tab for CPMO company management</name>
  <files>app/admin/page.tsx</files>
  <read_first>
  - app/admin/page.tsx (User type, tabs, fetch /api/admin/users)
  - app/api/admin/users/schema.ts after 10-05
  </read_first>
  <behavior>
  - Users tab lists users and can filter by status and role and search text (USER-01, D-20).
  - Create/edit collects email, one or more roles, and status Active/Inactive/Locked (USER-02, USER-03, USER-04).
  - Companies/Demo platform tabs stay visible only when the break-glass field is set (D-21).
  - Viewer-only session does not see mutate controls on the Users tab (D-15, D-18); server 403 remains if they POST.
  </behavior>
  <action>
  Extend /admin Users tab against the new JSON (roles[], email, status). Wire search/filter query params to GET /api/admin/users. Replace is_admin checkboxes with role multi-select. Lock/unlock and deactivate (DELETE) buttons call the same APIs. Hide Companies/Demo unless the break-glass field is set (D-21). Server 403 from 10-05 remains the AUTH-05 gate (D-15).
  </action>
  <verify>
    <automated>npx vitest run lib/services/users.service.unit.test.ts app/api/admin/users/route.test.ts -x</automated>
    <human-check>As a CPMO, open /admin, use Users to search/filter, create a user with email and roles, lock them, and confirm they cannot log in. As a platform operator (break-glass), confirm Companies tab still works. As a Viewer, confirm mutate controls are hidden and a direct POST /api/admin/users is 403.</human-check>
  </verify>
  <done>CPMO can manage company users in /admin; platform company ops remain separate; Viewer cannot manage users (USER-01..05, D-18, D-21, AUTH-05 screens).</done>
  <acceptance_criteria>
  - Users tab has search, status filter, role filter, email, roles, status
  - Platform tabs gated on break-glass only
  - npx vitest run lib/services/users.service.unit.test.ts app/api/admin/users/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser /admin UI → /api/admin/users | UI hiding is not authorization; API 403 is |
| Sidebar Admin link | CPMO vs platform break-glass must not be conflated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-25 | Elevation of Privilege | /admin Users UI | medium | mitigate | Hide mutate controls for viewer-only; server 403 from 10-05 is the gate (D-15, D-18) |
| T-10-21 | Elevation of Privilege | Sidebar vs companies tab | high | mitigate | Companies/Demo tabs stay break-glass only; CPMO gets Users (D-21) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-11-01: me route still green; human-check Sidebar Admin Panel for roles ['cpmo'] without break-glass
- 10-11-02: users service + admin users route tests; human-check at phase end (workflow.human_verify_mode=end-of-phase)
</verification>

<success_criteria>
CPMO can manage users in /admin; platform companies stay break-glass; Viewer cannot manage users via UI and is 403 on the API (USER-01, USER-03, USER-04, USER-05, AUTH-05, D-18, D-21).
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-11-SUMMARY.md` when done
</output>
