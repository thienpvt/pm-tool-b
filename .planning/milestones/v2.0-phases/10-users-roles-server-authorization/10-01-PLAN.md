---
phase: 10-users-roles-server-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-roles.ts
  - lib/db-roles.backfill.unit.test.ts
  - lib/db.ts
  - lib/auth.ts
  - lib/http/with-auth.ts
  - lib/http/with-auth.test.ts
  - lib/http/with-program-access.test.ts
  - lib/services/access.ts
  - lib/services/access.unit.test.ts
  - lib/services/risks.service.ts
  - lib/services/risks.service.unit.test.ts
  - lib/repositories/auth.repo.ts
  - app/api/auth/login/route.ts
  - app/api/auth/login/route.test.ts
  - app/api/projects/[id]/risks/route.access.test.ts
  - test/repo-db.ts
autonomous: true
requirements: [AUTH-01, AUTH-04, AUTH-05, AUTH-06, USER-03, USER-04]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An Active user can log in with username and password (D-09, AUTH-01)."
    - "Inactive or Locked users are rejected and cannot obtain a session (D-05, D-10, AUTH-06)."
    - "SessionUser.roles is the authorization truth and withAuth peels roles onto AccessActor; with-auth and with-program-access tests expect toAccessActor fields including roles (D-01, D-03, D-24, USER-03)."
    - "Viewer can read but cannot mutate; hiding a UI control is not treated as access control — POST createRisk is 403 for a viewer-only actor (D-15, AUTH-04, AUTH-05)."
    - "Role checks compose with tenant checks; the tracer does not remove company isolation (D-16)."
  artifacts:
    - path: "lib/db-roles.ts"
      provides: "Idempotent users/roles/audit DDL + role backfill in getDb migrate loop (D-02, D-22)"
      exports: ["migrateUsersRolesAndAudit"]
    - path: "lib/auth.ts"
      provides: "SessionUser with roles, status, email; status gate on getSessionUser (D-05, D-10)"
    - path: "lib/http/with-auth.test.ts"
      provides: "withAuth actor includes roles and other toAccessActor fields (D-03, D-24)"
    - path: "lib/http/with-program-access.test.ts"
      provides: "withProgramAccess actor includes roles via toAccessActor (D-03, D-24)"
    - path: "lib/services/access.ts"
      provides: "AppRole, UserStatus, AccessActor.roles, hasRole, assertCanMutate, toAccessActor (D-01, D-15, D-17)"
    - path: "app/api/auth/login/route.ts"
      provides: "Status gate before createSession; same 401 body for bad creds and non-active (D-10, AUTH-06)"
    - path: "app/api/projects/[id]/risks/route.access.test.ts"
      provides: "Viewer POST 403 proof for one mutating API (D-15, D-19)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-roles.ts"
      via: "getDb after migrateMappingTableTenancy calls migrateUsersRolesAndAudit(pool) (D-22)"
      pattern: "migrateUsersRolesAndAudit"
    - from: "lib/http/with-auth.ts"
      to: "lib/services/access.ts"
      via: "toAccessActor(user) builds ctx.actor including roles (D-03, D-17)"
      pattern: "toAccessActor"
    - from: "lib/services/risks.service.ts"
      to: "lib/services/access.ts"
      via: "createRisk calls assertCanMutate after assertProjectAccess (D-15, D-16)"
      pattern: "assertCanMutate"
  prohibitions:
    - replacing scrypt or DB sessions with another auth library (D-09)
    - treating CPMO as a global superuser on this tracer
    - physically deleting users
    - moving schema init out of getDb (D-22)
    - adding npm packages
    - UI-only Viewer gating without the risks POST 403 test (D-15)
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Prove the Phase 10 spine end-to-end (D-09, D-17, D-19, D-22, D-24): schema for status + `user_roles` + audit tables in the getDb migrate loop; login rejects Inactive/Locked with the existing 401 body (AUTH-06); SessionUser.roles reaches AccessActor (USER-03); withAuth/withProgramAccess tests expect toAccessActor fields including roles; one Viewer mutate path (createRisk) returns 403 (AUTH-04, AUTH-05, D-15).

Purpose: Catch an architectural dead-end on the first commit — roles in session, status gate, compose tenant then role — before expanding CPMO/PM assignment, the rest of the mutators, and user admin.

Output: Production-quality tracer, not a prototype. Later plans fill remaining call sites without changing this shape.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-RESEARCH.md
@.planning/phases/10-users-roles-server-authorization/10-PATTERNS.md
@.planning/phases/10-users-roles-server-authorization/10-VALIDATION.md

Flagged assumptions (EDGE_ABSENT / research log): A3 role strings stored lowercase (`cpmo`,`pm`,`viewer`) — locked here. A1 unit column absent — D-20 (not this plan). A5 dedicated extend endpoint — D-11 (plan 10-02). A4 platform vs CPMO split — D-21 (plan 10-05). A2 interim PM match — D-14 (plan 10-03). Null-company users during role backfill: use the user's `company_id` when set; when null, skip inserting a `user_roles` row until a company is assigned (do not invent a hardcoded company 1).

Do not implement deferred items: Phase 11 assignment windows, Phase 18 full AUDIT-01, DATA-01, CASL, next-auth.

<interfaces>
AccessActor lives in both lib/services/access.ts and lib/http/with-auth.ts today as { company_id, is_admin }. Canonical type after this plan is lib/services/access.ts; with-auth imports it (or re-exports) and must not keep a drifting duplicate.

Target AccessActor: { company_id: number | null; is_admin: number | boolean; roles: AppRole[]; status: UserStatus; user_id: number; username: string; display_name: string; email: string }.

AppRole = 'cpmo' | 'pm' | 'viewer'. UserStatus = 'active' | 'inactive' | 'locked'.

toAccessActor(user: SessionUser): AccessActor copies those fields. withAuth uses toAccessActor(user) instead of an inline two-field object.

hasRole(actor, role) is actor.roles.includes(role). Union: a user with ['cpmo','viewer'] has CPMO write.

assertCanMutate(actor): throw ForbiddenError when the actor has viewer and has neither cpmo nor pm (viewer-only). Throw-on-deny, never boolean.

SessionUser adds roles, status, email. getSessionUser SELECT joins user_roles via ARRAY_AGG; after fetch, if status is not active, deleteSession(sessionId) and return null (D-10). Tracer login still uses findUserByUsername + createSession.

AuthUserRow adds status and email. Login: if no row, or status is not active, or password fails — one 401 JSON { error: 'Invalid username or password' } (no enumeration). Active + good password still 200 + pm_session cookie (AUTH-01).

migrateUsersRolesAndAudit(pool) analog of migrateMappingTableTenancy: settings flags, idempotent, called from getDb after migrateMappingTableTenancy. No Prisma.

Existing access.unit.test.ts admin fixture currently expects a global bypass — do not invert that in this tracer (10-03 owns D-13). Tracer only adds hasRole / assertCanMutate tests and keeps current tenant cases green by giving fixtures roles: ['pm'] (owner) so they are not viewer-only.

risks.service unit tests mock the access module — extend the mock to export assertCanMutate (vi.fn resolved) plus existing assertProjectAccess so createRisk still runs. Route access test is the real Viewer 403 proof (do not mock assertCanMutate there).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="10-01-01">
  <name>End-to-end Inactive login reject + Viewer createRisk 403</name>
  <reversibility rating="one-way">Checkpoint waived: D-22 and D-02 are already locked in CONTEXT.md, so this one-way schema migrate does not insert a checkpoint:decision. users.status, user_roles, audit_logs, and unique email indexes are on-disk format.</reversibility>
  <files>lib/db-roles.ts, lib/db.ts, lib/auth.ts, lib/http/with-auth.ts, lib/http/with-auth.test.ts, lib/http/with-program-access.test.ts, lib/services/access.ts, lib/services/access.unit.test.ts, lib/services/risks.service.ts, lib/services/risks.service.unit.test.ts, lib/repositories/auth.repo.ts, app/api/auth/login/route.ts, app/api/auth/login/route.test.ts, app/api/projects/[id]/risks/route.access.test.ts, test/repo-db.ts</files>
  <read_first>
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-01, D-03, D-05, D-09, D-10, D-15, D-16, D-17, D-19, D-22)
  - .planning/phases/10-users-roles-server-authorization/10-RESEARCH.md (Patterns 1-2, login status gate, Pitfall 2/3)
  - .planning/phases/10-users-roles-server-authorization/10-PATTERNS.md (SessionUser, withAuth actor peel, risks.service, db-mapping-tenant analog)
  - lib/auth.ts (getSessionUser, createSession, deleteSession)
  - lib/http/with-auth.ts (AccessActor duplicate, line ~98 actor construction)
  - lib/services/access.ts (assertProjectAccess — do not remove the existing early-return in this task)
  - lib/services/risks.service.ts
  - app/api/auth/login/route.ts
  - app/api/projects/[id]/route.access.test.ts (session mock + 403 shape)
  - lib/db.ts getDb (~596) and migrateMappingTableTenancy import
  - lib/db-mapping-tenant.ts (settings flag + idempotent migrate)
  - test/repo-db.ts users DDL
  </read_first>
  <behavior>
  - POST /api/auth/login with a user whose status is inactive returns 401 { error: 'Invalid username or password' } and does not call createSession (D-05, D-10, AUTH-06).
  - Same 401 body and no session for status locked (D-10, AUTH-06).
  - POST /api/auth/login with an active user and a matching password still returns 200 and sets cookie pm_session (AUTH-01, D-09).
  - assertCanMutate on a viewer-only actor throws ForbiddenError (D-15).
  - assertCanMutate on an actor whose roles include pm or cpmo does not throw (D-01 union).
  - POST /api/projects/:id/risks with a session whose roles are ['viewer'] returns 403 { error: 'Forbidden' } when the project is in-company (D-15, D-16, AUTH-05).
  - Existing owner GET 403 cross-company cases on projects/[id] stay green (D-16, D-19).
  - withAuth ctx.actor includes roles and the other toAccessActor fields, not a two-field actor (D-03, D-24).
  - withProgramAccess passes that same actor (including roles) into assertProgramAccess (D-24).
  </behavior>
  <action>
  TDD first (D-19): write failing login status tests, failing assertCanMutate unit tests, and a failing Viewer POST risks 403 route.access test. They must fail before production code exists. Then implement until green.

  Create lib/db-roles.ts exporting migrateUsersRolesAndAudit(pool: Pool). Mirror lib/db-mapping-tenant.ts settings flags. DDL (idempotent ALTER/CREATE): users.email TEXT; users.status TEXT NOT NULL DEFAULT 'active'; users.locked_at TIMESTAMPTZ; users.locked_by INTEGER REFERENCES users(id); users.deleted_at TIMESTAMPTZ; CHECK-equivalent enforcement via a settings-flagged constraint or a documented CHECK if PG allows IF NOT EXISTS pattern used in-repo; user_roles (user_id FK users ON DELETE CASCADE, role TEXT CHECK IN ('cpmo','pm','viewer'), company_id INTEGER REFERENCES companies(id), PRIMARY KEY (user_id, role)); unique index users_email_lower_unique ON users (LOWER(email)) WHERE email IS NOT NULL AND email &lt;&gt; ''; audit_logs (id BIGSERIAL PK, company_id FK, actor_id FK users, entity_type TEXT, entity_id TEXT, action TEXT, before JSONB, after JSONB, created_at TIMESTAMPTZ DEFAULT now()). Role backfill (flag roles_backfill_v1, D-02): INSERT INTO user_roles for users who have no rows yet — is_admin = 1 maps to role cpmo, else role pm, using users.company_id when not null; skip users with null company_id. Do not flip the break-glass flag from CPMO (D-03). Do not use Prisma or an external migrate CLI (D-22).

  Wire getDb() in lib/db.ts: after migrateMappingTableTenancy(pool), await migrateUsersRolesAndAudit(pool). Keep initPostgresSchema and migratePostgresSchema. Do not move schema out of getDb.

  Extend test/repo-db.ts users DDL with email, status default active, locked_at, locked_by, deleted_at, plus CREATE user_roles and audit_logs matching production enough for later repo tests.

  AuthUserRow and findUserByUsername: include status and email (SELECT * already returns new columns once migrated).

  Login route (D-10): after findUserByUsername, reject with the existing 401 body when the user is missing, status is not active, or verifyPassword fails. Do not createSession on reject. Do not change the 400 missing-fields body.

  SessionUser (D-01, D-05): add roles: AppRole[], status: UserStatus, email: string. getSessionUser SELECT adds u.email, u.status, and aggregated roles (empty array if none). After fetch, if status is not active, deleteSession(sessionId) and return null (D-10). Keep 7-day createSession (D-11 duration; extend endpoint is 10-02). Export AppRole and UserStatus from access.ts (or auth.ts once) and import in both.

  Canonical AccessActor in lib/services/access.ts with the interface fields listed in context. Add hasRole, toAccessActor, assertCanMutate (D-15, D-17). with-auth.ts stops defining its own AccessActor shape; import from access.ts; build ctx.actor via toAccessActor(user) (D-03).

  Update lib/http/with-auth.test.ts and lib/http/with-program-access.test.ts session fixtures with toAccessActor fields (roles, status, email, display_name, username, user_id). Replace two-field actor equality with expects that include roles (and the other toAccessActor fields) on ctx.actor and on the actor passed to assertProgramAccess (D-24). Do not edit with-program-access.ts in this plan.

  risks.service createRisk (D-15, D-16): after assertProjectAccess, await assertCanMutate(actor). Do not add assertPmWriteAccess in this plan (10-03). listRisks stays tenant-only. Update risks.service.unit.test.ts mock to include assertCanMutate as a resolved vi.fn so existing create tests still pass.

  access.unit.test.ts: give owner/admin/nullCompany fixtures roles (owner and nullCompany: ['pm']; keep admin fixture as-is plus roles: ['cpmo'] without changing the global-bypass expectation — 10-03 inverts that). Add assertCanMutate cases for viewer-only vs union.

  Login tests: new app/api/auth/login/route.test.ts mocking findUserByUsername, verifyPassword, createSession. Cover active success, inactive 401, locked 401, missing fields 400.

  Risks route access test: follow app/api/projects/[id]/route.access.test.ts — mock getSessionFromRequest, run real assertProjectAccess + assertCanMutate. Viewer-only in-company session POST must be 403 { error: 'Forbidden' }.

  Commit the tracer only after the automated verify is green.
  </action>
  <verify>
    <automated>npx vitest run lib/services/access.unit.test.ts lib/services/risks.service.unit.test.ts app/api/auth/login/route.test.ts app/api/projects/[id]/risks/route.access.test.ts lib/http/with-auth.test.ts lib/http/with-program-access.test.ts -x</automated>
  </verify>
  <done>Inactive/Locked login is 401 without a session cookie; Active login still sets pm_session; Viewer-only POST createRisk is 403; SessionUser.roles reach ctx.actor via toAccessActor; with-auth and with-program-access tests expect roles; migrateUsersRolesAndAudit is called from getDb (D-01, D-03, D-05, D-09, D-10, D-15, D-16, D-17, D-19, D-22, D-24, AUTH-01, AUTH-06).</done>
  <acceptance_criteria>
  - lib/db-roles.ts exports migrateUsersRolesAndAudit
  - getDb() awaits migrateUsersRolesAndAudit after migrateMappingTableTenancy
  - SessionUser includes roles and status; getSessionUser returns null for non-active
  - Login 401 body for inactive/locked equals Invalid username or password
  - withAuth actor includes roles from toAccessActor
  - with-auth.test.ts and with-program-access.test.ts expect actor.roles (and other toAccessActor fields), not a two-field actor (D-24)
  - createRisk calls assertCanMutate
  - Viewer POST risks route.access test expects status 403 and body error Forbidden
  - npx vitest run lib/services/access.unit.test.ts lib/services/risks.service.unit.test.ts app/api/auth/login/route.test.ts app/api/projects/[id]/risks/route.access.test.ts lib/http/with-auth.test.ts lib/http/with-program-access.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-01-02">
  <name>Backfill existing users to CPMO or PM roles</name>
  <files>lib/db-roles.ts, lib/db-roles.backfill.unit.test.ts</files>
  <read_first>
  - lib/db-roles.ts (from 10-01-01)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-02, D-03)
  - lib/db-mapping-tenant.ts (settings flag insert)
  </read_first>
  <behavior>
  - Running migrateUsersRolesAndAudit twice does not duplicate user_roles primary keys (D-02, D-22).
  - A user with the break-glass flag equal to 1 and a non-null company_id receives role cpmo and no viewer row (D-02).
  - A user with the break-glass flag equal to 0 and a non-null company_id receives role pm (D-02).
  - Backfill does not clear or set the break-glass flag from the new role (D-03).
  </behavior>
  <action>
  TDD (D-19): unit-test the backfill SQL path with a mocked Pool (query/rows) or a small exported helper if that keeps the test hermetic. Then implement any gap in migrateUsersRolesAndAudit.

  Flag key roles_backfill_v1. INSERT ... WHERE NOT EXISTS matching (user_id, role). Map break-glass 1 to cpmo else pm. Skip null company_id users. Do not UPDATE users to derive the break-glass flag from roles (D-03). Second run is a no-op via settings row or NOT EXISTS.
  </action>
  <verify>
    <automated>npx vitest run lib/db-roles.backfill.unit.test.ts -x</automated>
  </verify>
  <done>Existing admins backfill to cpmo and everyone else with a company to pm; rerun is idempotent; break-glass flag is unchanged (D-02, D-03, D-22).</done>
  <acceptance_criteria>
  - settings key roles_backfill_v1 is written after a successful backfill
  - INSERT uses cpmo when break-glass is 1 else pm
  - Backfill unit test covers idempotent second run
  - npx vitest run lib/db-roles.backfill.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/auth/login | Untrusted credentials; must not enumerate users or skip status |
| Cookie pm_session → getSessionUser | Stolen or stale cookie; status may have changed since login |
| withAuth actor peel → services | Client must not supply roles; only session-derived AccessActor |
| Viewer client → POST /api/projects/:id/risks | Hidden UI is not a control; service must 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | createRisk / assertCanMutate | high | mitigate | Viewer-only throws ForbiddenError; route.access test POST 403 (D-15, AUTH-05) |
| T-10-02 | Spoofing | Login + getSessionUser | high | mitigate | Non-active status cannot createSession; getSessionUser deletes session and returns null (D-10, AUTH-06) |
| T-10-03 | Information Disclosure | Login error body | medium | mitigate | Same 401 message for missing user, bad password, inactive, locked (D-10) |
| T-10-04 | Elevation of Privilege | AccessActor.roles | high | mitigate | toAccessActor copies SessionUser only; no body field for roles (D-03) |
| T-10-05 | Tampering | migrateUsersRolesAndAudit | medium | mitigate | Idempotent settings flags; skip null-company users rather than assign company 1 (D-02, D-22) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 10-01-01: login status tests + Viewer risks 403 + access/risks unit green
- 10-01-02: backfill unit green and idempotent
- GET /api/auth/me roles/status is 10-02 (peeled from this tracer)
- Wave: npx vitest run lib/services/access.unit.test.ts lib/services/risks.service.unit.test.ts app/api/auth/login/route.test.ts app/api/projects/[id]/risks/route.access.test.ts lib/http/with-auth.test.ts lib/http/with-program-access.test.ts lib/db-roles.backfill.unit.test.ts -x
</verification>

<success_criteria>
Inactive/Locked users cannot obtain a session; Active login still works; SessionUser.roles are on the actor; Viewer-only createRisk is 403 on the server (AUTH-01, AUTH-06, AUTH-04/05 tracer, USER-03, USER-04).
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-01-SUMMARY.md` when done
</output>
