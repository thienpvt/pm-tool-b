---
phase: 10-users-roles-server-authorization
plan: 08
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - lib/services/project-report.service.ts
  - lib/services/project-report.service.unit.test.ts
  - app/api/projects/[id]/report/route.ts
  - app/api/projects/[id]/report/route.access.test.ts
  - app/api/projects/[id]/project-report/route.ts
  - app/api/projects/[id]/project-report/generate-email/route.ts
  - lib/services/portfolio-report.service.ts
  - lib/services/portfolio-report.service.unit.test.ts
  - app/api/portfolio/report/route.ts
  - app/api/portfolio/report/route.access.test.ts
  - app/api/portfolio/report/generate-email/route.ts
  - app/api/portfolio/report/send-email/route.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Viewer cannot POST project or portfolio AI report/email routes; those POSTs 403 (D-15, AUTH-05)."
    - "PM cannot POST project AI routes on an unassigned project (D-14)."
    - "Portfolio AI POSTs are CPMO-only in the session company (D-13)."
    - "Role checks compose with tenant checks (D-16)."
  artifacts:
    - path: "lib/services/project-report.service.ts"
      provides: "Writes call assertProjectWriteAccess (D-15)"
    - path: "lib/services/portfolio-report.service.ts"
      provides: "Writes require isCpmo after company tenant (D-13)"
    - path: "app/api/projects/[id]/report/route.ts"
      provides: "POST uses toAccessActor then assertProjectWriteAccess (AUTH-05)"
    - path: "app/api/portfolio/report/route.ts"
      provides: "POST uses toAccessActor then isCpmo (D-13, AUTH-05)"
  key_links:
    - from: "app/api/projects/[id]/report/route.ts POST"
      to: "lib/services/access.ts"
      via: "toAccessActor then assertProjectWriteAccess (D-16)"
      pattern: "assertProjectWriteAccess"
    - from: "app/api/portfolio/report/send-email/route.ts POST"
      to: "lib/services/access.ts"
      via: "toAccessActor then isCpmo (D-13)"
      pattern: "isCpmo"
  prohibitions:
    - skipping tenant assert because a role check exists (D-16)
    - UI-only Viewer gating without POST 403 (D-15)
    - adding npm packages
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Gate AI report POSTs and report-service writes (D-13, D-14, D-15, AUTH-05): project-report.service plus POST app/api/projects/[id]/report, project-report, generate-email; portfolio-report.service plus POST app/api/portfolio/report, generate-email, send-email.

Purpose: These routes spend Anthropic credentials or send email. Session-only is not AUTH-05. Hiding a Generate button is not access control.

Output: Report services and the six POST routes deny Viewer / unassigned PM / non-CPMO portfolio writes. D-23 leftover ops/admin stay session+tenant.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-04-PLAN.md

AUTH-05 inventory: this plan owns the six AI POST routes listed above plus project-report and portfolio-report service writes. GET report data stays session+tenant (Viewer may read). Carve-out D-23: app/api/operations/** and platform /api/admin/companies stay session+tenant.

<interfaces>
project-report.service writes (anything that persists or is called only from POST generate paths): assertProjectWriteAccess. Reads: assertProjectAccess.

Project AI POST handlers that currently call getSessionFromRequest then run inline Anthropic: after session, toAccessActor(user), then assertProjectWriteAccess(projectId, actor) before resolveAnthropicCredentials / createMessage. Map ForbiddenError to 403 via serviceErrorResponse. Keep existing 401/400/503 bodies. Add app/api/projects/[id]/report/route.access.test.ts: Viewer POST 403 and createMessage is not called (D-15, D-19).

portfolio-report.service: GET/list stays company tenant (Viewer may read). Writes and any method used only by POST generate/send: after tenant, isCpmo(actor) or ForbiddenError (D-13). assertCompanyWrite is 10-09; this plan uses isCpmo from 10-03 so Wave 3 can run in parallel. Stop passing actor leftover flag into listCompanyPrograms, listPortfolioReportProjects, and sibling list helpers — pass actor.company_id only (D-13). 10-09 deletes leftover-flag all-rows SQL in portfolio.repo.ts / programs.repo.ts.

Portfolio AI POST handlers (report, generate-email, send-email): after session, toAccessActor(user); if !isCpmo(actor) return 403 { error: 'Forbidden' } before Anthropic/email. Keep company_id from the session; do not take company_id from the body. Replace inline { company_id, is_admin } actor objects with toAccessActor (D-03). Add app/api/portfolio/report/route.access.test.ts: Viewer POST 403 and createMessage is not called (D-15, D-19).

Do not add CPMO/PM/Viewer asserts on app/api/operations/** (D-23).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-08-01">
  <name>Gate project-report service and project AI POSTs</name>
  <files>lib/services/project-report.service.ts, lib/services/project-report.service.unit.test.ts, app/api/projects/[id]/report/route.ts, app/api/projects/[id]/report/route.access.test.ts, app/api/projects/[id]/project-report/route.ts, app/api/projects/[id]/project-report/generate-email/route.ts</files>
  <read_first>
  - lib/services/project-report.service.ts
  - lib/services/project-report.service.unit.test.ts
  - app/api/projects/[id]/report/route.ts
  - app/api/projects/[id]/project-report/route.ts
  - app/api/projects/[id]/project-report/generate-email/route.ts
  - lib/services/access.ts (toAccessActor, assertProjectWriteAccess)
  - app/api/projects/[id]/route.access.test.ts (session mock pattern)
  </read_first>
  <behavior>
  - project-report.service mutators call assertProjectWriteAccess (D-15, D-16).
  - POST /api/projects/:id/report with a viewer-only session returns 403 (AUTH-05).
  - POST /api/projects/:id/project-report and generate-email likewise 403 for viewer-only (AUTH-05).
  - GET report paths stay session+tenant (Viewer may read).
  </behavior>
  <action>
  TDD (D-19): update project-report.service.unit.test.ts mocks first. Then switch service writes to assertProjectWriteAccess. On the three POST handlers, peel toAccessActor and await assertProjectWriteAccess before Anthropic/email work. Map ForbiddenError to 403. Keep existing 401 when no session. Add app/api/projects/[id]/report/route.access.test.ts: viewer-only session POST returns 403 { error: 'Forbidden' }; mock createMessage and resolveAnthropicCredentials so the 403 happens without calling createMessage (D-15, D-19). Follow getSessionFromRequest mocks in route.access.test.ts and include roles on the session.
  </action>
  <verify>
    <automated>npx vitest run lib/services/project-report.service.unit.test.ts app/api/projects/[id]/report/route.access.test.ts -x</automated>
  </verify>
  <done>Project report writes and the three project AI POSTs deny Viewer and unassigned PM (AUTH-05, D-14, D-15).</done>
  <acceptance_criteria>
  - project-report mutators call assertProjectWriteAccess
  - Three project POST handlers call toAccessActor then assertProjectWriteAccess
  - app/api/projects/[id]/report/route.access.test.ts includes Viewer POST 403
  - npx vitest run lib/services/project-report.service.unit.test.ts app/api/projects/[id]/report/route.access.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-08-02">
  <name>Gate portfolio-report service and portfolio AI POSTs</name>
  <files>lib/services/portfolio-report.service.ts, lib/services/portfolio-report.service.unit.test.ts, app/api/portfolio/report/route.ts, app/api/portfolio/report/route.access.test.ts, app/api/portfolio/report/generate-email/route.ts, app/api/portfolio/report/send-email/route.ts</files>
  <read_first>
  - lib/services/portfolio-report.service.ts
  - lib/services/portfolio-report.service.unit.test.ts
  - app/api/portfolio/report/route.ts (POST spends Anthropic)
  - app/api/portfolio/report/generate-email/route.ts
  - app/api/portfolio/report/send-email/route.ts
  </read_first>
  <behavior>
  - portfolio-report.service writes require isCpmo after company tenant (D-13, D-16).
  - POST /api/portfolio/report, generate-email, and send-email return 403 for viewer-only and for pm without cpmo (D-13, AUTH-05).
  - GET /api/portfolio/report stays readable for in-company Viewer (AUTH-04 read).
  </behavior>
  <action>
  TDD (D-19): update portfolio-report.service.unit.test.ts so write paths fail until isCpmo is checked. Then switch leftover { company_id, is_admin } actors to toAccessActor (D-03). Stop passing the leftover flag into listCompanyPrograms, listPortfolioReportProjects, and sibling list helpers — pass actor.company_id only (D-13). On the three POST handlers, after session, toAccessActor then ForbiddenError unless isCpmo, before Anthropic/email. Session company only (D-13). Add app/api/portfolio/report/route.access.test.ts: viewer-only session POST returns 403 { error: 'Forbidden' }; mock createMessage so it is not called (D-15, D-19).
  </action>
  <verify>
    <automated>npx vitest run lib/services/portfolio-report.service.unit.test.ts app/api/portfolio/report/route.access.test.ts -x</automated>
  </verify>
  <done>Portfolio AI POSTs are CPMO-only in the session company; Viewer cannot burn the Anthropic key (D-13, AUTH-05).</done>
  <acceptance_criteria>
  - Three portfolio POST handlers call toAccessActor then isCpmo
  - Inline leftover-flag actor objects on those files are replaced with toAccessActor
  - app/api/portfolio/report/route.access.test.ts includes Viewer POST 403
  - npx vitest run lib/services/portfolio-report.service.unit.test.ts app/api/portfolio/report/route.access.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewer HTTP client → AI POST that spends Anthropic or sends email | Must 403 even if UI hid Generate |
| PM HTTP client → project AI POST on unassigned project | D-14 after tenant |
| Non-CPMO → portfolio AI POST | Company-scoped CPMO only (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-24 | Elevation of Privilege | Project/portfolio AI POSTs | high | mitigate | assertProjectWriteAccess on project POSTs; isCpmo on portfolio POSTs (D-13, D-15, AUTH-05) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-08-01: project-report.service.unit.test.ts + app/api/projects/[id]/report/route.access.test.ts Viewer POST 403
- 10-08-02: portfolio-report.service.unit.test.ts + app/api/portfolio/report/route.access.test.ts Viewer POST 403
</verification>

<success_criteria>
Viewer cannot POST the six AI report/email routes; unassigned PM cannot POST project AI; portfolio AI POSTs are CPMO-only (AUTH-05). D-23 leftover ops/admin are not gated here.
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-08-SUMMARY.md` when done
</output>
