---
phase: 10-users-roles-server-authorization
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - lib/services/risks.service.ts
  - lib/services/risks.service.unit.test.ts
  - lib/services/issues.service.ts
  - lib/services/issues.service.unit.test.ts
  - lib/services/milestones.service.ts
  - lib/services/milestones.service.unit.test.ts
  - lib/services/activities.service.ts
  - lib/services/activities.service.unit.test.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Viewer cannot mutate risks, issues, milestones (including linkEpic/unlinkEpic), or activities (including importActivities); service throws ForbiddenError (D-15, AUTH-05)."
    - "PM cannot mutate those resources on an unassigned project; CPMO can mutate in-company (D-13, D-14)."
    - "Role checks compose with tenant checks — assertProjectAccess still runs inside assertProjectWriteAccess (D-16)."
  artifacts:
    - path: "lib/services/risks.service.ts"
      provides: "Mutators call assertProjectWriteAccess (D-15, D-16)"
    - path: "lib/services/milestones.service.ts"
      provides: "linkEpic and unlinkEpic call assertProjectWriteAccess (AUTH-05)"
    - path: "lib/services/activities.service.ts"
      provides: "importActivities and other mutators call assertProjectWriteAccess (AUTH-05)"
  key_links:
    - from: "lib/services/risks.service.ts mutators"
      to: "lib/services/access.ts"
      via: "assertProjectWriteAccess after 10-03 (D-16)"
      pattern: "assertProjectWriteAccess"
    - from: "lib/services/milestones.service.ts linkEpic"
      to: "lib/services/access.ts"
      via: "assertProjectWriteAccess (AUTH-05)"
      pattern: "linkEpic"
  prohibitions:
    - skipping tenant assert because a role check exists (D-16)
    - UI-only Viewer gating without service 403 (D-15)
    - reintroducing global CPMO bypass (D-13)
    - adding npm packages
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Gate nested project mutators slice A (D-15, D-16, AUTH-05): risks, issues, milestones including linkEpic/unlinkEpic, and activities including importActivities all call assertProjectWriteAccess exported by 10-03.

Purpose: 10-01 proved one Viewer 403; 10-03 proved CPMO/PM on projects. This plan is the RAID/timeline write surface so hiding a button is not access control.

Output: Four services + matching unit tests green. Remaining AUTH-05 product mutators are 10-06, 10-07, 10-08, 10-09; actor peel is 10-10. D-23 leftover ops/admin stay session+tenant.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-RESEARCH.md
@.planning/phases/10-users-roles-server-authorization/10-PATTERNS.md
@.planning/phases/10-users-roles-server-authorization/10-03-PLAN.md

10-03 exported assertProjectWriteAccess. 10-01 already calls assertCanMutate on createRisk — replace that pair with assertProjectWriteAccess on all risk mutators (list stays assertProjectAccess).

## AUTH-05 inventory (product vs carve-out)

Gated this phase, by plan:

- 10-03: projects.service list/create/get/update/delete
- 10-04 (this plan): risks, issues, milestones (create/update/delete + linkEpic + unlinkEpic), activities (create/update/delete + importActivities)
- 10-06: meetings, team, bugs, escalations
- 10-07: holidays, documents, budget, budget-items
- 10-08: project-report.service writes; POST app/api/projects/[id]/report, app/api/projects/[id]/project-report, app/api/projects/[id]/project-report/generate-email; portfolio-report.service writes; POST app/api/portfolio/report, generate-email, send-email
- 10-09: programs, portfolio.service, import-mapping, jira-mapping (assertCompanyWrite)
- 10-10: leftover actorOf on portfolio quota/members/budgets/allocations + lib/http/role-matrix.test.ts

AUTH-05 carve-out (D-23) — session+tenant only this phase; no CPMO/PM/Viewer write asserts:

- app/api/operations/**
- platform /api/admin/companies (demo/Jira/RAG config stay on the break-glass flag per D-21)

<interfaces>
Project mutators (create/update/delete/replace/import/link, not list/get): switch the first line from assertProjectAccess to assertProjectWriteAccess. Read methods keep assertProjectAccess.

risks.service: remove the extra assertCanMutate line once write assert composes it.

milestones.service: linkEpic and unlinkEpic are writes (they currently call assertProjectAccess only). listEpics stays read.

activities.service: importActivities is a write. Other create/update/delete likewise.

Unit tests that vi.mock access.ts: add assertProjectWriteAccess to the mock (vi.fn resolved) so existing ForbiddenError cases that mocked assertProjectAccess still compile. Prefer aliasing: mock assertProjectWriteAccess the same way assertProjectAccess was mocked on mutator describes.

Keep withProjectAccess wrappers on routes unchanged (D-17). This plan does not edit access.ts (assertProjectWriteAccess already exists).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-04-01">
  <name>Gate risks and issues mutators</name>
  <files>lib/services/risks.service.ts, lib/services/risks.service.unit.test.ts, lib/services/issues.service.ts, lib/services/issues.service.unit.test.ts</files>
  <read_first>
  - lib/services/access.ts (assertProjectWriteAccess from 10-03)
  - lib/services/risks.service.ts (createRisk already has assertCanMutate)
  - lib/services/issues.service.ts
  - matching *.service.unit.test.ts mocks of access
  </read_first>
  <behavior>
  - create/update/delete on risks and issues call assertProjectWriteAccess (D-15, D-16).
  - list/get still call assertProjectAccess only (read).
  - Existing unit tests that expected assertProjectAccess on mutators now expect assertProjectWriteAccess (D-19).
  </behavior>
  <action>
  TDD (D-19): update risks and issues unit-test mocks first so mutator tests fail on missing export/call, then switch mutators to assertProjectWriteAccess. risks.service: drop the extra assertCanMutate line once write assert composes it. Keep route wrappers unchanged (D-17).
  </action>
  <verify>
    <automated>npx vitest run lib/services/risks.service.unit.test.ts lib/services/issues.service.unit.test.ts -x</automated>
  </verify>
  <done>Risk and issue writes go through assertProjectWriteAccess so Viewer and unassigned PM are denied in the service (D-14, D-15, D-16, AUTH-05).</done>
  <acceptance_criteria>
  - Mutator functions in risks.service.ts and issues.service.ts call assertProjectWriteAccess
  - List/get still call assertProjectAccess
  - npx vitest run lib/services/risks.service.unit.test.ts lib/services/issues.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-04-02">
  <name>Gate milestone writes including linkEpic and unlinkEpic</name>
  <files>lib/services/milestones.service.ts, lib/services/milestones.service.unit.test.ts</files>
  <read_first>
  - lib/services/milestones.service.ts (createMilestone, updateMilestone, deleteMilestone, linkEpic, unlinkEpic, listEpics)
  - lib/services/milestones.service.unit.test.ts
  </read_first>
  <behavior>
  - createMilestone, updateMilestone, deleteMilestone, linkEpic, and unlinkEpic call assertProjectWriteAccess (D-15, AUTH-05).
  - listMilestones and listEpics still call assertProjectAccess only.
  </behavior>
  <action>
  TDD (D-19): add or update unit tests so linkEpic and unlinkEpic fail until they call assertProjectWriteAccess, then switch those five write functions. listEpics stays read. Keep the existing already-linked ignore in linkEpic.
  </action>
  <verify>
    <automated>npx vitest run lib/services/milestones.service.unit.test.ts -x</automated>
  </verify>
  <done>Milestone CRUD plus epic link/unlink deny Viewer and unassigned PM on the server (AUTH-05, D-15, D-16).</done>
  <acceptance_criteria>
  - linkEpic and unlinkEpic call assertProjectWriteAccess
  - listEpics still calls assertProjectAccess
  - npx vitest run lib/services/milestones.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-04-03">
  <name>Gate activity writes including importActivities</name>
  <files>lib/services/activities.service.ts, lib/services/activities.service.unit.test.ts</files>
  <read_first>
  - lib/services/activities.service.ts (create/update/delete and importActivities)
  - lib/services/activities.service.unit.test.ts
  </read_first>
  <behavior>
  - Activity create/update/delete and importActivities call assertProjectWriteAccess (D-15, AUTH-05).
  - list/get stay assertProjectAccess only.
  </behavior>
  <action>
  TDD (D-19): update activities.service.unit.test.ts so importActivities and other mutators expect assertProjectWriteAccess, then switch those functions. Keep Phase 9 tenant stamping on imported rows.
  </action>
  <verify>
    <automated>npx vitest run lib/services/activities.service.unit.test.ts -x</automated>
  </verify>
  <done>Activity writes including importActivities deny Viewer and unassigned PM on the server (AUTH-05, D-15, D-16).</done>
  <acceptance_criteria>
  - importActivities calls assertProjectWriteAccess
  - Other activity mutators call assertProjectWriteAccess
  - npx vitest run lib/services/activities.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewer HTTP client → POST/PATCH/DELETE nested RAID/timeline | Must 403 even if UI hid the button |
| PM HTTP client → nested resource on unassigned project | D-14 after tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-13 | Elevation of Privilege | risks/issues/milestones/activities mutators | high | mitigate | assertProjectWriteAccess on create/update/delete plus linkEpic, unlinkEpic, importActivities (D-15, AUTH-05) |
| T-10-16 | Information Disclosure | Viewer GET | low | accept | Viewer may read in-company resources; AUTH-04 read-only is intended |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-04-01: risks + issues unit tests
- 10-04-02: milestones including linkEpic/unlinkEpic
- 10-04-03: activities including importActivities
</verification>

<success_criteria>
Viewer mutate is 403 on risks, issues, milestones (including epic link/unlink), and activities (including import); PM unassigned writes 403; tenant compose holds (AUTH-04, AUTH-05). D-23 leftover ops/admin are not gated here.
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-04-SUMMARY.md` when done
</output>
