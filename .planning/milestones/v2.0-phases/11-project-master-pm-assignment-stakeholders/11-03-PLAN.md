---
phase: 11-project-master-pm-assignment-stakeholders
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - lib/repositories/pm-assignments.repo.ts
  - lib/services/pm-assignments.service.ts
  - lib/services/pm-assignments.service.unit.test.ts
  - app/api/projects/[id]/pm-assignments/route.ts
  - app/api/projects/[id]/pm-assignments/schema.ts
  - app/api/projects/[id]/pm-assignments/route.test.ts
  - lib/services/access.ts
  - lib/services/access.unit.test.ts
  - lib/repositories/projects.repo.ts
  - lib/repositories/projects.repo.unit.test.ts
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
  - lib/db-project-master.ts
  - lib/db-project-master.backfill.unit.test.ts
autonomous: true
requirements: [PMAS-01, PMAS-02, PMAS-03, PMAS-04]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO can assign exactly one active primary PM or leave none; history is kept by setting effective_to (D-11, D-12, D-15, PMAS-01, PMAS-03)."
    - "Collaborators only while a primary is active; a user cannot hold both roles on overlapping windows (D-12, PMAS-02)."
    - "Assigned PM cannot create or end assignment windows (D-15)."
    - "assertPmWriteAccess keeps its function name; lookup is an active primary or collaborator window for actor.user_id; CPMO still short-circuits (D-13, PMAS-04)."
    - "PM-only assertProjectAccess and listProjects use the same window query, not email or display name, after backfill (D-13, D-14, PMAS-04)."
    - "Backfill inserts at most one open primary per project that matched Phase 10 email-then-name rules (D-14)."
  artifacts:
    - path: "lib/repositories/pm-assignments.repo.ts"
      provides: "hasActivePmAssignment shared by access and list (D-13, PMAS-04)"
      exports: ["hasActivePmAssignment"]
    - path: "lib/services/pm-assignments.service.ts"
      provides: "CPMO nested CRUD with D-12 invariants and auditLog (D-11, D-12, D-15, D-19)"
    - path: "app/api/projects/[id]/pm-assignments/route.ts"
      provides: "Nested GET/POST/PATCH under withProjectAccess (D-15)"
    - path: "lib/services/access.ts"
      provides: "assertPmWriteAccess and PM-only assertProjectAccess call hasActivePmAssignment (D-13)"
    - path: "lib/db-project-master.ts"
      provides: "pm_assignment_backfill_v1 D-14 backfill (D-14, D-19)"
  key_links:
    - from: "lib/services/access.ts"
      to: "lib/repositories/pm-assignments.repo.ts"
      via: "assertPmWriteAccess and PM-only assertProjectAccess call hasActivePmAssignment (D-13)"
      pattern: "hasActivePmAssignment"
    - from: "lib/repositories/projects.repo.ts"
      to: "project_pm_assignments"
      via: "listProjects EXISTS window for opts.pmUserId (D-13, PMAS-04)"
      pattern: "pmUserId"
    - from: "lib/services/projects.service.ts"
      to: "listProjectsRepo"
      via: "PM-only list passes { pmUserId: actor.user_id } (D-13)"
      pattern: "pmUserId"
  prohibitions:
    - physical DELETE of an assignment window that existed (D-11)
    - PM self-assign (D-15)
    - keeping email or display-name match as an access fallback after backfill (D-13)
    - renaming assertPmWriteAccess
    - adding npm packages
    - a UI-SPEC (D-20)
    - Phase 13 snapshot tables (D-09)
---

<objective>
Replace Phase 10 interim PM lookup with assignment windows (D-11 through D-15, PMAS-01 through PMAS-04): nested /api/projects/[id]/pm-assignments for CPMO, D-12 invariants, D-14 backfill, and — in this same plan — rewire assertPmWriteAccess, the PM-only branch of assertProjectAccess, and the listProjects filter to hasActivePmAssignment / pmUserId.

Purpose: One access source so a PM who can PATCH also appears in list and nested GET, and a PM whose window ended loses all three.

Output: assignment API plus access rewire plus backfill. Function name assertPmWriteAccess unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-RESEARCH.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-01-PLAN.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-02-PLAN.md

11-01 created project_pm_assignments. 11-02 must complete first because this plan edits projects.service.ts (listProjects) after governance wiring. Do not implement stakeholders (11-04).

<interfaces>
hasActivePmAssignment(projectId: number, userId: number): Promise&lt;boolean&gt; — SELECT 1 FROM project_pm_assignments WHERE project_id and user_id match AND effective_from is on or before CURRENT_DATE AND (effective_to IS NULL OR effective_to is after CURRENT_DATE) LIMIT 1. Role may be primary or collaborator (D-13).

assertPmWriteAccess(projectId, actor): keep export name. If isCpmo(actor) return. Else if not await hasActivePmAssignment(Number(projectId), actor.user_id) throw ForbiddenError. Stop importing the interim identity helper from projects.repo. Delete the email/name matcher function from access.ts (D-13).

assertProjectAccess PM-only branch (isPmOnly after tenant match): same hasActivePmAssignment check instead of identity columns (D-13). Viewer-only and CPMO branches unchanged. Tenant-then-role order unchanged (Phase 10 D-16).

listProjects(companyId, opts?: { pmUserId?: number }): when pmUserId is set, AND EXISTS the same window predicate on project_pm_assignments. Remove pmEmail/pmName/username opts (D-13). projects.service PM-only branch passes { pmUserId: actor.user_id }.

Nested routes: withProjectAccess like milestones. GET listPmAssignments (assertProjectAccess in service). POST createPmAssignment. PATCH endPmAssignment (body includes assignment id and optional effective_to; default today). Service mutations: assertProjectAccess then assertCompanyWrite / isCpmo plus non-null company_id (D-15). Schema z.object({}).passthrough().

D-12 before insert (transaction):
- role primary: if another active primary exists, UPDATE that row effective_to to today then insert the new primary (PMAS-03 change).
- role collaborator: if no active primary, ValidationError. If this user_id has an overlapping window in the other role, ValidationError (PMAS-02).
- Zero primaries allowed: ending the last primary also soft-ends remaining active collaborators in the same transaction so D-12 stays true.
- Never DELETE FROM project_pm_assignments (D-11).

After primary change, UPDATE projects.pm_name and pm_email from the active primary user's display_name and email (denormalized display only, not an access source) (D-14).

auditLog entity_type pm_assignment, actions create and end, snapshots { id, project_id, user_id, role, effective_from, effective_to } (D-19).

Backfill flag pm_assignment_backfill_v1 inside migrateProjectMaster after DDL (D-14, D-19): for each project with non-empty pm_email or pm_name, if a same-company user matches Phase 10 rules (email-first when pm_email non-empty; else trim+lower pm_name vs display_name then username), INSERT one primary window effective_from CURRENT_DATE effective_to NULL. WHERE NOT EXISTS assignment for that project_id. Skip if no user match. Second boot is a no-op via the settings flag.

Access unit tests: mock hasActivePmAssignment from pm-assignments.repo, not the old identity helper. Assigned PM with true is allowed; false throws ForbiddenError; CPMO does not call the window helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="11-03-01">
  <name>CPMO assignment windows with D-12 invariants</name>
  <files>lib/repositories/pm-assignments.repo.ts, lib/services/pm-assignments.service.ts, lib/services/pm-assignments.service.unit.test.ts, app/api/projects/[id]/pm-assignments/route.ts, app/api/projects/[id]/pm-assignments/schema.ts, app/api/projects/[id]/pm-assignments/route.test.ts</files>
  <read_first>
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md (D-11, D-12, D-15)
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md (milestones route, users.service CPMO gate, hasActivePmAssignment SQL)
  - app/api/projects/[id]/milestones/route.ts
  - lib/services/milestones.service.ts
  - lib/services/users.service.ts assertCpmoCompany and auditLog
  - lib/repositories/users.repo.ts findUserById
  </read_first>
  <behavior>
  - createPmAssignment as CPMO with role primary succeeds and auditLogs create (D-11, D-15, PMAS-01).
  - createPmAssignment as PM throws ForbiddenError (D-15).
  - createPmAssignment collaborator with no active primary throws ValidationError (D-12, PMAS-02).
  - createPmAssignment collaborator for a user who is already active primary throws ValidationError (D-12, PMAS-02).
  - createPmAssignment as CPMO with role primary while another primary window is active soft-ends the previous primary (sets effective_to) and leaves exactly one active primary (D-12, PMAS-01, PMAS-03).
  - endPmAssignment of the last active primary also sets effective_to on remaining active collaborators in the same transaction (D-12, PMAS-01, PMAS-02).
  - endPmAssignment sets effective_to and does not call a SQL DELETE (D-11, PMAS-03).
  - listPmAssignments returns ended windows as well as open ones (PMAS-03).
  - POST /api/projects/:id/pm-assignments as viewer or PM is 403; as CPMO is 201 (D-15, D-20).
  </behavior>
  <action>
  TDD (D-20): write failing pm-assignments.service.unit.test.ts and route.test.ts for the behavior list before production files exist. Put the D-12 primary-swap and last-primary collaborator cascade cases in pm-assignments.service.unit.test.ts so the existing vitest command below runs them with the rest of the list. Then implement repo, service, nested route.

  Use withProjectAccess for tenancy; enforce CPMO in the service (D-15). Validate user_id via findUserById and same company_id as the project/actor. Implement D-12 in the service with repo helpers for active primary, overlapping windows, insert, and soft-end. After primary insert or end, denormalize projects.pm_name and pm_email from the remaining active primary or clear them when none.

  Do not add a DELETE handler. PATCH is the end path. Keep files in this task limited to assignment API files listed.
  </action>
  <verify>
    <automated>npx vitest run lib/services/pm-assignments.service.unit.test.ts app/api/projects/[id]/pm-assignments/route.test.ts -x</automated>
  </verify>
  <done>CPMO can create and soft-end assignment windows with D-12 invariants; PM cannot mutate assignments; history remains (D-11, D-12, D-15, PMAS-01, PMAS-02, PMAS-03).</done>
  <acceptance_criteria>
  - PM create test expects ForbiddenError
  - Collaborator without primary expects ValidationError
  - Second primary while one is active: prior window has effective_to and exactly one active primary remains (D-12, PMAS-01)
  - Ending the last primary also sets effective_to on remaining active collaborators in the same transaction (D-12, PMAS-01, PMAS-02)
  - End path updates effective_to
  - npx vitest run lib/services/pm-assignments.service.unit.test.ts app/api/projects/[id]/pm-assignments/route.test.ts -x exits 0 (includes the primary-swap and last-primary collaborator cascade cases)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="11-03-02">
  <name>Rewire all three PM lookup sites and backfill</name>
  <files>lib/services/access.ts, lib/services/access.unit.test.ts, lib/repositories/projects.repo.ts, lib/repositories/projects.repo.unit.test.ts, lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts, lib/db-project-master.ts, lib/db-project-master.backfill.unit.test.ts</files>
  <read_first>
  - lib/services/access.ts assertProjectAccess PM-only branch and assertPmWriteAccess
  - lib/services/access.unit.test.ts current identity mocks
  - lib/repositories/projects.repo.ts listProjects
  - lib/repositories/projects.repo.unit.test.ts D-14 predicate test
  - lib/services/projects.service.ts listProjects
  - lib/db-project-master.ts from 11-01
  - lib/db-roles.ts backfillUserRoles analog
  </read_first>
  <behavior>
  - assertPmWriteAccess still exported; CPMO returns without a window query; PM with hasActivePmAssignment true is allowed; false throws ForbiddenError (D-13, PMAS-04).
  - PM-only assertProjectAccess after tenant match uses the same window helper and throws ForbiddenError when it is false (D-13).
  - listProjects for PM-only calls listProjectsRepo(companyId, { pmUserId: actor.user_id }) (D-13).
  - listProjects SQL for pmUserId includes EXISTS on project_pm_assignments and the date window predicate, not pm_email (D-13, PMAS-04).
  - Backfill inserts one open primary when pm_email matches a company user and skips projects that already have an assignment row; second run writes no extra rows (D-14).
  </behavior>
  <action>
  TDD first: invert access.unit.test.ts to mock hasActivePmAssignment. Change the listProjects service test that currently passes pmEmail/pmName/username to expect pmUserId. Change projects.repo.unit.test.ts PM filter test to pass { pmUserId: 2 } and expect EXISTS plus user_id bind, not LOWER(pm_email). Add db-project-master.backfill.unit.test.ts for idempotent D-14 insert. These must fail on the old lookup. Then implement all three call sites in this same task so they cannot drift (D-13, PMAS-04).

  Keep the export name assertPmWriteAccess. Replace only the lookup. Do not keep an email/name fallback after backfill (D-13).

  Add backfill inside migrateProjectMaster after DDL using flag pm_assignment_backfill_v1, NOT EXISTS per project_id, email-first then name match copied from Phase 10 D-14 rules (D-14). Leave pm_name/pm_email columns in place as display.

  Do not edit stakeholder files. Do not rename assertPmWriteAccess. Do not add package installs.
  </action>
  <verify>
    <automated>npx vitest run lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts lib/repositories/projects.repo.unit.test.ts lib/db-project-master.backfill.unit.test.ts -x</automated>
  </verify>
  <done>Write access, PM-only read, and PM list all follow assignment windows; backfill is idempotent; assertPmWriteAccess name unchanged (D-13, D-14, PMAS-04).</done>
  <acceptance_criteria>
  - access.unit.test.ts mocks hasActivePmAssignment
  - listProjects PM test expects pmUserId
  - repo unit test SQL includes project_pm_assignments
  - npx vitest run lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts lib/repositories/projects.repo.unit.test.ts lib/db-project-master.backfill.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/projects/[id]/pm-assignments | Only CPMO may mutate; tenancy via withProjectAccess |
| Session actor → assertPmWriteAccess | Window table is the only PM write source after backfill |
| listProjects PM filter | Must match the same window predicate as write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Elevation of Privilege | assertPmWriteAccess / listProjects / assertProjectAccess | high | mitigate | Single hasActivePmAssignment helper on all three sites in this plan (D-13, PMAS-04) |
| T-11-06 | Elevation of Privilege | createPmAssignment | high | mitigate | assertCompanyWrite after project access; PM POST 403 (D-15) |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 11-03-01: assignment service + route tests green
- 11-03-02: access, listProjects, backfill tests green
- Wave: npx vitest run lib/services/pm-assignments.service.unit.test.ts app/api/projects/[id]/pm-assignments/route.test.ts lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts lib/repositories/projects.repo.unit.test.ts lib/db-project-master.backfill.unit.test.ts -x
</verification>

<success_criteria>
CPMO manages assignment history; PM write/read/list follow the active window; email/name is not an access source (PMAS-01 through PMAS-04).
</success_criteria>

<output>
Create `.planning/phases/11-project-master-pm-assignment-stakeholders/11-03-SUMMARY.md` when done
</output>
