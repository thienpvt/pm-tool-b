---
phase: 11-project-master-pm-assignment-stakeholders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-project-master.ts
  - lib/db-project-master.ddl.unit.test.ts
  - lib/db.ts
  - test/repo-db.ts
  - lib/repositories/projects.repo.ts
  - lib/repositories/ALLOWLIST-DIFF.md
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-07]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO can create a project with portfolio year, name, unique project code, and program (D-01, D-03, D-04, PROJ-01)."
    - "Unique project code is per company and case-insensitive (D-01)."
    - "Program is the existing customers row via customer_id; there is no second program table (D-04)."
    - "Schema lives in the getDb migrate loop via migrateProjectMaster; no Prisma (D-19)."
    - "projects.progress_pct exists as live master progress; this plan does not add Phase 13 snapshot persistence (D-09, PROJ-07)."
    - "Linked child rows stay on project_id; create does not drop/recreate an existing project to set a code (D-02, PROJ-02)."
  artifacts:
    - path: "lib/db-project-master.ts"
      provides: "DDL for project identity columns, assignment and stakeholder tables, partial unique index (D-01, D-11, D-16, D-19)"
      exports: ["migrateProjectMaster"]
    - path: "lib/db.ts"
      provides: "getDb awaits migrateProjectMaster after migrateUsersRolesAndAudit (D-19)"
    - path: "lib/repositories/projects.repo.ts"
      provides: "PROJECT_COLUMNS includes project_code and progress_pct; createProject INSERT writes identity columns (D-01, D-04, D-09)"
    - path: "lib/services/projects.service.ts"
      provides: "CPMO create requires project_code, portfolio_year, customer_id; ConflictError on duplicate code (D-01, D-03, PROJ-01)"
    - path: "test/repo-db.ts"
      provides: "Test DDL mirrors Phase 11 columns and both history tables (D-19)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-project-master.ts"
      via: "getDb after migrateUsersRolesAndAudit calls migrateProjectMaster(pool) (D-19)"
      pattern: "migrateProjectMaster"
    - from: "lib/services/projects.service.ts"
      to: "lib/repositories/projects.repo.ts"
      via: "createProject validates then createProjectRepo with session company_id (D-03, D-04)"
      pattern: "createProjectRepo"
    - from: "lib/db-project-master.ts"
      to: "projects_company_code_lower_unique"
      via: "partial unique index on (company_id, LOWER(project_code)) (D-01)"
      pattern: "projects_company_code_lower_unique"
  prohibitions:
    - Prisma or an external migrate CLI (D-19)
    - a second programs table (D-04)
    - Phase 13 weekly period or snapshot persistence tables (D-09)
    - recreating a project row to change code (D-02)
    - adding npm packages
    - a UI-SPEC (D-20)
    - rewriting assertPmWriteAccess in this plan (D-13 is 11-03)
---

<objective>
Prove the Phase 11 spine (D-01, D-03, D-04, D-09, D-19, PROJ-01): `migrateProjectMaster` in the getDb loop adds `project_code`, `portfolio_year`, governance columns, `progress_pct`, weekly-report flag columns, and both history tables; CPMO POST create requires code, year, and program; duplicate code in the same company is ConflictError.

Purpose: Catch an architectural dead-end on the first commit — identity columns, unique index, and create validation — before governance defaults, assignment windows, and stakeholders.

Output: Production-quality tracer. Later plans fill PATCH governance, window lookup, and nested APIs without changing this migrate helper location.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-RESEARCH.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-VALIDATION.md

Flagged research assumptions locked here: A1 classification and governance are TEXT columns named classification and governance. A4 effective_from/effective_to on history tables are DATE. Discretion: column project_code not code.

Do not implement deferred items: Phase 13 period shells, Phase 12 RAID/milestone masters, Phase 16 dashboards, Phase 18 full audit coverage.

<interfaces>
migrateProjectMaster(pool: Pool): Promise&lt;void&gt; — analog of migrateUsersRolesAndAudit. Settings flags: project_master_ddl_v1 for DDL; do not write pm_assignment_backfill_v1 in this plan (11-03 owns D-14 backfill). Wrap in try/catch like db-roles so missing settings table does not crash first boot.

getDb in lib/db.ts (~596): after migrateUsersRolesAndAudit(pool), dynamic-import migrateProjectMaster and await it. Keep initPostgresSchema, migratePostgresSchema, migrateMappingTableTenancy, backfillWeightedCompletion.

Partial unique index name: projects_company_code_lower_unique ON projects (company_id, LOWER(project_code)) WHERE project_code IS NOT NULL AND TRIM(project_code) is not empty (D-01).

New projects columns (ADD COLUMN IF NOT EXISTS): project_code TEXT; portfolio_year INTEGER; stage TEXT; status_reason TEXT; rag TEXT; progress_pct INTEGER DEFAULT 0; weekly_report_enabled BOOLEAN DEFAULT FALSE; weekly_report_start_period TEXT; plan_end TEXT; adjusted_end TEXT; actual_end TEXT; classification TEXT; governance TEXT. Keep current_phase unchanged (D-05 is 11-02 for writers; column already exists).

Tables (CREATE TABLE IF NOT EXISTS):
- project_pm_assignments: id SERIAL PK; project_id INTEGER NOT NULL REFERENCES projects(id); user_id INTEGER NOT NULL REFERENCES users(id); role TEXT NOT NULL CHECK (role IN ('primary','collaborator')); effective_from DATE NOT NULL; effective_to DATE; created_at TIMESTAMPTZ DEFAULT now() (D-11).
- project_stakeholders: id SERIAL PK; project_id INTEGER NOT NULL REFERENCES projects(id); stakeholder_role TEXT NOT NULL CHECK (stakeholder_role IN ('sponsor','psc_chair','psc_member','project_director','key_stakeholder')); user_id INTEGER REFERENCES users(id); external_name TEXT; external_email TEXT; effective_from DATE NOT NULL; effective_to DATE; created_at TIMESTAMPTZ DEFAULT now() (D-16).

createProject(actor, body) stays CPMO-only and stamps actor.company_id. Require non-empty trimmed project_code, integer portfolio_year, and customer_id. Resolve program with getProgram from programs.repo; throw NotFoundError if missing; throw ForbiddenError if program.company_id is not actor.company_id (D-04). Duplicate: findProjectByCompanyCode(companyId, code) case-insensitive; throw ConflictError('Project code already exists') (D-01). Then createProjectRepo. Return the created project row at the top level (keep data.id for the existing new-project page).

findProjectByCompanyCode(companyId, code) in projects.repo: SELECT id FROM projects WHERE company_id = ? AND LOWER(project_code) = LOWER(?) LIMIT 1.

createProject INSERT must include the new identity/governance columns (at least project_code, portfolio_year, customer_id, stage, progress_pct, weekly_report_enabled) plus existing fields. customer_id stays absent from PROJECT_COLUMNS (tenancy). project_code IS on PROJECT_COLUMNS so later CPMO PATCH can UPDATE in place (D-02, 11-02).

Extend PROJECT_COLUMNS with: project_code, portfolio_year, stage, status_reason, rag, progress_pct, weekly_report_enabled, weekly_report_start_period, plan_end, adjusted_end, actual_end, classification, governance. Do not add company_id or customer_id.

ConflictError already maps to 409 in lib/api-errors.ts. ValidationError maps to 400. Do not catch UnknownColumnError in the service.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="11-01-01">
  <name>End-to-end CPMO create with unique project_code</name>
  <reversibility rating="costly">DDL and unique index are additive ALTERs in getDb; undo is another migrate, not a data rewrite of child FKs.</reversibility>
  <files>lib/db-project-master.ts, lib/db-project-master.ddl.unit.test.ts, lib/db.ts, test/repo-db.ts, lib/repositories/projects.repo.ts, lib/repositories/ALLOWLIST-DIFF.md, lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts</files>
  <read_first>
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md (D-01, D-03, D-04, D-09, D-11, D-16, D-19)
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md (db-roles analog, PROJECT_COLUMNS, createProject INSERT)
  - lib/db-roles.ts (settings flags, unique index, migrateUsersRolesAndAudit)
  - lib/db.ts getDb migrate loop after migrateUsersRolesAndAudit
  - lib/repositories/projects.repo.ts PROJECT_COLUMNS and createProject
  - lib/services/projects.service.ts createProject
  - lib/services/projects.service.unit.test.ts createProject describe
  - test/repo-db.ts projects CREATE TABLE
  - lib/repositories/programs.repo.ts getProgram
  - lib/repositories/ALLOWLIST-DIFF.md projects table
  </read_first>
  <behavior>
  - CPMO createProject with name, project_code, portfolio_year, and in-company customer_id calls createProjectRepo with actor.company_id (D-03, D-04, PROJ-01).
  - Missing project_code, missing portfolio_year, or missing customer_id throws ValidationError (PROJ-01, D-04).
  - PM or viewer createProject still throws ForbiddenError (D-03).
  - migrateProjectMaster is a function exported from lib/db-project-master.ts and getDb awaits it after migrateUsersRolesAndAudit (D-19).
  - DDL unit test asserts the unique index identifier projects_company_code_lower_unique and LOWER(project_code) (D-01).
  - DDL unit test asserts CREATE TABLE for project_pm_assignments and project_stakeholders only as the new tables (D-11, D-16, D-09).
  </behavior>
  <action>
  TDD first (D-20): add failing createProject tests for required project_code, portfolio_year, and customer_id, plus a failing ddl unit test that migrateProjectMaster exists and the unique index SQL uses company_id plus LOWER(project_code). Tests must fail before production code exists. Then implement until green.

  Create lib/db-project-master.ts exporting migrateProjectMaster(pool). Copy the two-helper settings-flag pattern from lib/db-roles.ts (settingsFlagExists, writeSettingsFlag). DDL flag key project_master_ddl_v1. Idempotent ADD COLUMN IF NOT EXISTS for the columns listed in interfaces. CREATE TABLE IF NOT EXISTS for project_pm_assignments and project_stakeholders with the CHECKs listed. Create the partial unique index projects_company_code_lower_unique (D-01). Write the DDL flag after DDL. Do not run assignment backfill in this plan (D-14 is 11-03). Do not use Prisma (D-19). Do not add a programs table (D-04). Do not add Phase 13 snapshot persistence tables (D-09).

  Wire getDb() in lib/db.ts: after migrateUsersRolesAndAudit(pool), await migrateProjectMaster(pool) via dynamic import like the mapping and roles helpers.

  Mirror every new projects column and both history tables in test/repo-db.ts projects DDL (Pitfall 5).

  Extend PROJECT_COLUMNS and createProject INSERT as specified in interfaces. Add findProjectByCompanyCode. Keep getProjectPmIdentity untouched (11-03 rewires access).

  Update ALLOWLIST-DIFF.md projects section: document each new allowlisted column as migrate-added; keep customer_id and company_id excluded.

  createProject service: keep isCpmo and null company_id ForbiddenError. Validate required identity fields. Load program via getProgram; reject cross-company programs. Pre-check duplicate code with findProjectByCompanyCode then ConflictError. Then createProjectRepo(actor.company_id, body). Mock getProgram and findProjectByCompanyCode in unit tests.

  lib/db-project-master.ddl.unit.test.ts: export a testable SQL fragment list or inspect string constants used by migrate so the unique index and table names are asserted without a live Pool if that keeps the test hermetic (same style as db-roles.backfill.unit.test.ts). Assert progress_pct is an ALTER on projects. Assert the new table names are project_pm_assignments and project_stakeholders.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts lib/db-project-master.ddl.unit.test.ts -x</automated>
  </verify>
  <done>CPMO can create a project with unique per-company project_code, portfolio_year, and program; migrateProjectMaster runs from getDb; unique index exists; history tables exist empty; progress_pct is on projects (D-01, D-03, D-04, D-09, D-19, PROJ-01, PROJ-07).</done>
  <acceptance_criteria>
  - lib/db-project-master.ts exports migrateProjectMaster
  - getDb awaits migrateProjectMaster after migrateUsersRolesAndAudit
  - Unique index name is projects_company_code_lower_unique
  - PROJECT_COLUMNS includes project_code and progress_pct
  - createProject throws ValidationError when project_code is missing
  - npx vitest run lib/services/projects.service.unit.test.ts lib/db-project-master.ddl.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="11-01-02">
  <name>Reject duplicate codes and foreign programs</name>
  <files>lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts, lib/repositories/projects.repo.ts</files>
  <read_first>
  - lib/services/projects.service.ts createProject from 11-01-01
  - lib/services/import-mapping.service.ts ConflictError pre-check pattern
  - lib/repositories/programs.repo.ts getProgram
  </read_first>
  <behavior>
  - Same company, same project_code ignoring case throws ConflictError and does not call createProjectRepo (D-01, PROJ-01).
  - customer_id whose customers.company_id differs from actor.company_id throws ForbiddenError (D-04).
  - Missing program id throws NotFoundError.
  - Existing CPMO stamp of actor.company_id ignoring body.company_id stays green (Phase 10 D-13).
  </behavior>
  <action>
  TDD: failing tests for duplicate LOWER(project_code) in-company and for a program owned by another company. Then implement findProjectByCompanyCode + getProgram checks if the tracer left any gap.

  Do not treat code change as delete plus insert (D-02). Do not add email/name PM matching here.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts -x</automated>
  </verify>
  <done>Duplicate in-company codes are 409 ConflictError; program must be in the actor company (D-01, D-04, PROJ-01).</done>
  <acceptance_criteria>
  - Duplicate code test expects ConflictError
  - Foreign program test expects ForbiddenError
  - npx vitest run lib/services/projects.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/projects | Untrusted identity fields; CPMO session required |
| Service → projects unique index | Race on duplicate code within a tenant |
| Service → customers row | customer_id must not attach another company's program |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | createProject / unique index | high | mitigate | Case-insensitive per-company unique index plus ConflictError pre-check (D-01) |
| T-11-02 | Elevation of Privilege | createProject customer_id | high | mitigate | getProgram company_id must equal actor.company_id (D-04) |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 11-01-01: create required-field tests + ddl unit green
- 11-01-02: duplicate code and foreign program tests green
- Wave: npx vitest run lib/services/projects.service.unit.test.ts lib/db-project-master.ddl.unit.test.ts -x
</verification>

<success_criteria>
CPMO create requires unique per-company project_code, portfolio_year, and in-company program; schema is in getDb via lib/db-project-master.ts (PROJ-01, D-01, D-04, D-19).
</success_criteria>

<output>
Create `.planning/phases/11-project-master-pm-assignment-stakeholders/11-01-SUMMARY.md` when done
</output>
