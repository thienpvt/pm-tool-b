---
phase: 27-nits-validation-operator-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/weekly/backend/services/nit-01-exports.contract.test.ts
autonomous: true
requirements: [NIT-01]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "listPeriodShells remains a named export of weekly-reports.service and dashboards plus weekly-tracking still import listPeriodShellsRepo (NIT-01, D-01)."
    - "listOpenProjectDependencies remains a named export of project-dependencies.repo and its repo test still imports it (NIT-01, D-01)."
    - "Neither function is deleted; no new npm packages are added (D-01, D-06)."
  artifacts:
    - path: "modules/weekly/backend/services/nit-01-exports.contract.test.ts"
      provides: "NIT-01 consumer contract (D-01)"
  key_links:
    - from: "modules/weekly/backend/services/nit-01-exports.contract.test.ts"
      to: "modules/weekly/backend/services/weekly-reports.service.ts"
      via: "static import listPeriodShells plus source scan of export"
      pattern: "listPeriodShells"
    - from: "modules/weekly/backend/services/nit-01-exports.contract.test.ts"
      to: "modules/dashboards/backend/services/spec-dashboards.service.ts"
      via: "source scan listPeriodShellsRepo"
      pattern: "listPeriodShellsRepo"
    - from: "modules/weekly/backend/services/nit-01-exports.contract.test.ts"
      to: "modules/projects/backend/repositories/project-dependencies.repo.ts"
      via: "static import listOpenProjectDependencies"
      pattern: "listOpenProjectDependencies"
  prohibitions:
    - statement: "Must not delete listPeriodShells or listOpenProjectDependencies (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages or edit eslint/route-wrapper-allowlist.json (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Lock NIT-01: listPeriodShells and listOpenProjectDependencies stay consumed (D-01, D-06).

Purpose: Prevent a later cleanup from dropping the dashboard/service spine that already calls these exports.

Output: modules/weekly/backend/services/nit-01-exports.contract.test.ts collected by the vitest node project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-nits-validation-operator-gate/27-CONTEXT.md
@.planning/phases/27-nits-validation-operator-gate/27-RESEARCH.md
@modules/weekly/backend/services/weekly-reports.service.ts
@modules/dashboards/backend/services/spec-dashboards.service.ts
@modules/weekly/backend/services/weekly-tracking.service.ts
@modules/projects/backend/repositories/project-dependencies.repo.ts
@modules/projects/backend/repositories/project-dependencies.repo.test.ts
@lib/rsc-chrome.gate.test.ts
@vitest.config.ts

Isolation none. Sequential on the main tree. TDD: two commits per task, test(27-01) then feat(27-01). Never a RED-only task. Do not implement NIT-02 audit skip (27-02). Do not write BUDGET-COEXISTENCE.md, 27-HYG-02.md, or VALIDATION.md edits (27-03). Do not create 27-UI-SPEC.md (D-06). Do not install packages (D-06).

<interfaces>
vitest.config.ts node include already covers modules/**/*.test.ts. Run npx vitest run --project node. Do not pass -x (Vitest 4 ignores it).

Root for readFileSync: from modules/weekly/backend/services resolve four segments up (repo root). On Windows, normalize relative paths with replace(/\\/g, '/') like lib/rsc-chrome.gate.test.ts.

Copy the rsc-chrome codeLines helper (trim, drop empty, drop lines starting with // or * ) so commented-out imports do not count as consumption.

D-01 consumer map (do not require spec-dashboards to import the service-level listPeriodShells; it uses the repo helper):

- listPeriodShells defined as export async function listPeriodShells in modules/weekly/backend/services/weekly-reports.service.ts (calls listPeriodShellsRepo after assertCompanyWrite / company match / getWeeklyPeriodByCompany).
- listPeriodShellsRepo imported in modules/dashboards/backend/services/spec-dashboards.service.ts and modules/weekly/backend/services/weekly-tracking.service.ts.
- listOpenProjectDependencies defined as export async function listOpenProjectDependencies in modules/projects/backend/repositories/project-dependencies.repo.ts.
- listOpenProjectDependencies imported in modules/projects/backend/repositories/project-dependencies.repo.test.ts.

Static imports in the contract test:

import { listPeriodShells } from '@/modules/weekly/backend/services/weekly-reports.service';
import { listOpenProjectDependencies } from '@/modules/projects/backend/repositories/project-dependencies.repo';

Do not vi.mock those modules for the typeof checks.
</interfaces>
</context>

<feature>
  <name>NIT-01 export consumption contract</name>
  <files>modules/weekly/backend/services/nit-01-exports.contract.test.ts</files>
  <behavior>
    typeof listPeriodShells and typeof listOpenProjectDependencies are function (NIT-01, D-01).
    Named consumer source files still contain the tokens in the interface map (D-01).
  </behavior>
  <implementation>
    New vitest node contract file using static imports plus readFileSync source scans. Keep both production functions (D-01, D-06).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="27-01-01">
  <name>End-to-end listPeriodShells consumption contract</name>
  <reversibility rating="reversible">Characterization test only; production wiring already exists (D-01).</reversibility>
  <files>modules/weekly/backend/services/nit-01-exports.contract.test.ts</files>
  <read_first>
  - .planning/phases/27-nits-validation-operator-gate/27-CONTEXT.md (D-01)
  - .planning/phases/27-nits-validation-operator-gate/27-RESEARCH.md (NIT-01 consumer map)
  - lib/rsc-chrome.gate.test.ts (codeLines / readFileSync)
  - modules/weekly/backend/services/weekly-reports.service.ts
  - modules/dashboards/backend/services/spec-dashboards.service.ts
  - modules/weekly/backend/services/weekly-tracking.service.ts
  </read_first>
  <behavior>
  - Contract file imports listPeriodShells from @/modules/weekly/backend/services/weekly-reports.service and expect(typeof listPeriodShells).toBe('function') (NIT-01, D-01).
  - codeLines of weekly-reports.service.ts include the substring export async function listPeriodShells (D-01).
  - codeLines of spec-dashboards.service.ts and weekly-tracking.service.ts include listPeriodShellsRepo (D-01).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(27-01) then feat(27-01). Never RED-only. Cite D-01 and D-06.
  RED: Create modules/weekly/backend/services/nit-01-exports.contract.test.ts with a describe for NIT-01 listPeriodShells. Write the three behavior assertions using readFileSync plus codeLines, and the static import plus typeof check. Run npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts. If the file was missing, the runner failing to collect it is red. If assertions fail, production consumers drifted — do not delete the functions; restore the import or export tokens listed in interfaces (D-01). Commit test(27-01): red listPeriodShells consumer contract.
  GREEN: Keep listPeriodShells on weekly-reports.service. Keep listPeriodShellsRepo imports on spec-dashboards.service and weekly-tracking.service. Do not add a barrel re-export. Do not require spec-dashboards to import the service-level listPeriodShells. Do not add npm. Do not edit eslint/route-wrapper-allowlist.json (D-06). Re-run the vitest command until it exits 0. Commit feat(27-01): lock listPeriodShells consumers.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts</automated>
  </verify>
  <done>listPeriodShells is still exported and dashboards/tracking still import listPeriodShellsRepo (NIT-01, D-01).</done>
  <acceptance_criteria>
  - modules/weekly/backend/services/nit-01-exports.contract.test.ts exists under the vitest node glob
  - weekly-reports.service.ts still contains export async function listPeriodShells
  - npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="27-01-02">
  <name>Lock listOpenProjectDependencies consumption</name>
  <files>modules/weekly/backend/services/nit-01-exports.contract.test.ts</files>
  <read_first>
  - modules/weekly/backend/services/nit-01-exports.contract.test.ts from 27-01-01
  - modules/projects/backend/repositories/project-dependencies.repo.ts
  - modules/projects/backend/repositories/project-dependencies.repo.test.ts
  </read_first>
  <behavior>
  - Contract file imports listOpenProjectDependencies from @/modules/projects/backend/repositories/project-dependencies.repo and expect(typeof listOpenProjectDependencies).toBe('function') (NIT-01, D-01).
  - codeLines of project-dependencies.repo.ts include export async function listOpenProjectDependencies (D-01).
  - codeLines of project-dependencies.repo.test.ts include listOpenProjectDependencies (D-01).
  </behavior>
  <action>
  TDD. Two commits test(27-01) then feat(27-01). Never RED-only. Cite D-01 and D-06.
  RED: Expand the same contract file with a second describe for listOpenProjectDependencies covering the three behaviors. Run the same vitest path — it must fail until those assertions exist and pass. Do not treat project-dependencies.service as the consumer; D-01 locks the repo export plus repo tests. Commit test(27-01): red listOpenProjectDependencies consumer contract.
  GREEN: Keep the named export on project-dependencies.repo.ts. Keep the repo test import. Do not delete the function. Do not add dashboard wiring in this plan. Do not add npm (D-06). Re-run vitest until exit 0. Commit feat(27-01): lock listOpenProjectDependencies consumers.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts</automated>
  </verify>
  <done>listOpenProjectDependencies remains exported and imported by its repo test (NIT-01, D-01).</done>
  <acceptance_criteria>
  - the contract test file imports listOpenProjectDependencies
  - project-dependencies.repo.ts still contains export async function listOpenProjectDependencies
  - npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → source tree | Contract reads production files; no untrusted HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | nit-01-exports.contract.test.ts | low | accept | Read-only source scan; cannot change runtime auth |
| T-27-02 | Elevation of Privilege | weekly-reports.service listPeriodShells | medium | mitigate | Do not strip assertCompanyWrite or company_id match on listPeriodShells (D-01 keep wiring) |
| T-27-SC | Tampering | npm installs | high | mitigate | No install tasks this plan (D-06); T-27-SC closed by absence of package-manager commands |

ASVS L1: no new trust boundary; access asserts on listPeriodShells stay as shipped.
</threat_model>

<verification>
- 27-01-01: listPeriodShells contract green
- 27-01-02: listOpenProjectDependencies contract green
- Wave: npx vitest run --project node modules/weekly/backend/services/nit-01-exports.contract.test.ts
- Isolation none. TDD RED then GREEN per task.
</verification>

<success_criteria>
Both NIT-01 exports remain importable functions with documented consumers; neither is removed (NIT-01, D-01, D-06).
</success_criteria>

<output>
Create `.planning/phases/27-nits-validation-operator-gate/27-01-SUMMARY.md` when done
</output>
