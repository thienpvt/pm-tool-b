---
phase: 27-nits-validation-operator-gate
plan: 02
type: tdd
wave: 2
depends_on: [27-01]
files_modified:
  - modules/projects/backend/services/milestones.service.ts
  - modules/projects/backend/services/milestones.service.unit.test.ts
autonomous: true
requirements: [NIT-02]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When auditSnapshot of the prior milestone equals auditSnapshot of the updated row, updateMilestone does not call auditLog (NIT-02, D-02)."
    - "When those snapshots differ, updateMilestone still calls auditLog with action update and before/after snapshots (NIT-02, D-02)."
    - "A no-op still runs updateMilestoneRepo; createMilestone and cancelMilestone still always audit (D-02)."
    - "No new npm packages; milestone PATCH wrappers stay as shipped (D-06)."
  artifacts:
    - path: "modules/projects/backend/services/milestones.service.ts"
      provides: "snapshotsEqual guard on updateMilestone (D-02)"
      exports: ["updateMilestone"]
    - path: "modules/projects/backend/services/milestones.service.unit.test.ts"
      provides: "NIT-02 no-op skip plus real-change audit tests (D-02)"
  key_links:
    - from: "modules/projects/backend/services/milestones.service.ts"
      to: "modules/audit/backend/services/audit.service.ts"
      via: "auditLog only when snapshots differ"
      pattern: "snapshotsEqual"
    - from: "modules/projects/backend/services/milestones.service.ts"
      to: "modules/projects/backend/services/projects.service.ts"
      via: "same JSON.stringify snapshotsEqual algorithm"
      pattern: "JSON.stringify"
  prohibitions:
    - statement: "Must not skip the repository UPDATE on a no-op PATCH (D-02)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not import projects.service into milestones.service (D-02, unit graph)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages or edit eslint/route-wrapper-allowlist.json (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Skip audit_logs insert when a milestone PATCH is a no-op on audited fields, using the snapshotsEqual algorithm from projects.service.ts (NIT-02, D-02, D-06).

Purpose: Cut CPMO audit-viewer noise for identical before/after milestone updates without weakening append-only logging of real changes.

Output: Guard in updateMilestone plus unit tests in milestones.service.unit.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-nits-validation-operator-gate/27-CONTEXT.md
@.planning/phases/27-nits-validation-operator-gate/27-RESEARCH.md
@modules/projects/backend/services/milestones.service.ts
@modules/projects/backend/services/milestones.service.unit.test.ts
@modules/projects/backend/services/projects.service.ts
@modules/projects/backend/repositories/milestones.repo.ts
@modules/audit/backend/services/audit.service.ts

Isolation none. Sequential after 27-01 on the main tree. TDD: two commits per task, test(27-02) then feat(27-02). Never a RED-only task. Do not edit NIT-01 contract tests. Do not write 27-03 docs. Do not rewrite report routes (D-04). Do not add npm (D-06). Do not create 27-UI-SPEC.md (D-06).

<interfaces>
updateMilestone(projectId, actor, milestoneId, body) in milestones.service.ts: assertProjectWriteAccess; getMilestoneRepo; updateMilestoneRepo; throw NotFoundError if updated is missing; currently always auditLog action update with auditSnapshot(prior) / auditSnapshot(updated).

auditSnapshot fields (only these count for D-02 equality): id, name, status, start_date, end_date, plan_end. adjusted_end is outside the snapshot (RESEARCH pitfall 2 — accept).

projects.service.ts snapshotsEqual: JSON.stringify(before) === JSON.stringify(after). Define the same helper locally in milestones.service.ts. Do not import projects.service from milestones.service (would load governance/checklist into the milestone unit graph). Using X per user decision: the snapshotsEqual algorithm from projects.service (research also showed a local copy).

Existing unit mocks (keep): vi.hoisted auditLog, getMilestoneRepo, updateMilestoneRepo, assertProjectWriteAccess. Existing it('updateMilestone calls auditLog action update on success (D-02)') uses name Before vs Renamed — must remain green.

No-op fixture (aligned dates, RESEARCH pitfall 1): id 3, name M, status planned, start_date 2026-01-01, end_date 2026-06-30, plan_end 2026-06-30. getMilestoneRepo and updateMilestoneRepo both resolve that same object. Body { name: 'M' }. owner actor from the existing test file.

updateMilestone still must call updateMilestoneRepo(7, 3, { name: 'M' }) on the no-op path.

Vitest: npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts (no -x).
</interfaces>
</context>

<feature>
  <name>No-op milestone update skips auditLog</name>
  <files>modules/projects/backend/services/milestones.service.ts, modules/projects/backend/services/milestones.service.unit.test.ts</files>
  <behavior>
    Identical auditSnapshot before/after → auditLog not called; repo update still called (NIT-02, D-02).
    Different snapshots → auditLog called with action update (D-02).
  </behavior>
  <implementation>
    Local snapshotsEqual helper matching projects.service.ts; wrap the existing auditLog in updateMilestone with if (!snapshotsEqual(beforeSnap, afterSnap)).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="27-02-01">
  <name>End-to-end no-op milestone update skips auditLog</name>
  <reversibility rating="reversible">Guard is a local if around auditLog; undo is deleting the helper (D-02).</reversibility>
  <files>modules/projects/backend/services/milestones.service.ts, modules/projects/backend/services/milestones.service.unit.test.ts</files>
  <read_first>
  - .planning/phases/27-nits-validation-operator-gate/27-CONTEXT.md (D-02)
  - .planning/phases/27-nits-validation-operator-gate/27-RESEARCH.md (NIT-02 path, Pattern 1, pitfall 1)
  - modules/projects/backend/services/projects.service.ts (snapshotsEqual)
  - modules/projects/backend/services/milestones.service.ts
  - modules/projects/backend/services/milestones.service.unit.test.ts
  </read_first>
  <behavior>
  - it('updateMilestone skips auditLog when before equals after (NIT-02)'): after updateMilestone(7, owner, 3, { name: 'M' }) with identical prior/updated rows from interfaces, expect(auditLog).not.toHaveBeenCalled() (NIT-02, D-02).
  - Same test: expect(updateMilestoneRepo).toHaveBeenCalledWith(7, 3, { name: 'M' }) (D-02).
  - Same test: expect(assertProjectWriteAccess).toHaveBeenCalledWith(7, owner) (D-02).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(27-02) then feat(27-02). Never RED-only. Cite D-02 and D-06.
  RED: Add the NIT-02 it() to milestones.service.unit.test.ts using the aligned-date fixture from interfaces. Run npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts. It must fail because updateMilestone currently always calls auditLog. Commit test(27-02): red no-op milestone audit skip.
  GREEN: In milestones.service.ts add a local snapshotsEqual helper with the same JSON.stringify comparison as projects.service.ts. After a successful repo update, compute beforeSnap = auditSnapshot(prior) and afterSnap = auditSnapshot(updated); call auditLog only when they are not equal. Keep assertProjectWriteAccess, getMilestoneRepo, updateMilestoneRepo, and NotFoundError when updated is missing. Do not skip the repo UPDATE. Do not import projects.service. Do not change createMilestone or cancelMilestone in this task. Do not add npm (D-06). Re-run the unit file until exit 0. Commit feat(27-02): skip auditLog on no-op milestone update.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts</automated>
  </verify>
  <done>No-op updateMilestone does not call auditLog and still writes through the repo (NIT-02, D-02).</done>
  <acceptance_criteria>
  - milestones.service.unit.test.ts contains the NIT-02 skip it()
  - updateMilestone calls auditLog only inside a snapshotsEqual guard
  - npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="27-02-02">
  <name>Keep auditLog on real milestone field changes</name>
  <files>modules/projects/backend/services/milestones.service.ts, modules/projects/backend/services/milestones.service.unit.test.ts</files>
  <read_first>
  - modules/projects/backend/services/milestones.service.ts
  - modules/projects/backend/services/milestones.service.unit.test.ts
  </read_first>
  <behavior>
  - Existing it('updateMilestone calls auditLog action update on success (D-02)') still expects auditLog with before name Before and after name Renamed (NIT-02, D-02).
  - createMilestone still calls auditLog action create; cancelMilestone still calls auditLog action cancel (D-02).
  </behavior>
  <action>
  TDD. Two commits test(27-02) then feat(27-02). Never RED-only. Cite D-02 and D-06.
  RED: Re-run the unit file. If the Before/Renamed it() or create/cancel audit tests fail after 27-02-01, that is red — add or restore assertions so real snapshot diffs still expect auditLog with entity_type milestone and action update (and create/cancel unchanged). Commit test(27-02): red real milestone audit retained.
  GREEN: Keep the snapshotsEqual guard only on updateMilestone. Do not wrap createMilestone or cancelMilestone. Do not compare full row objects — only auditSnapshot fields. Do not add npm or touch route handlers / withProjectAccess (D-06). Re-run the unit file until exit 0. Commit feat(27-02): retain milestone audit on real changes.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts</automated>
  </verify>
  <done>Real milestone field changes still append audit; create and cancel still audit (NIT-02, D-02).</done>
  <acceptance_criteria>
  - the Before/Renamed updateMilestone audit it() still exists and passes
  - createMilestone and cancelMilestone tests still expect auditLog
  - npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PATCH milestone | Untrusted body; still schema + withProjectAccess |
| Service → audit_logs | Append-only insert skipped only when snapshots equal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Repudiation | updateMilestone auditLog | high | mitigate | Skip insert only when JSON.stringify of auditSnapshot is equal; real diffs still auditLog (D-02); unit tests cover both paths |
| T-27-04 | Tampering | snapshotsEqual | medium | mitigate | Compare auditSnapshot field set only (stable key order in the helper object); do not delete audit_logs rows |
| T-27-05 | Elevation of Privilege | updateMilestone | high | mitigate | Keep assertProjectWriteAccess before get/update; do not change withProjectAccess on the route (D-06) |
| T-27-06 | Information Disclosure | auditSnapshot | low | accept | Snapshot still omits adjusted_end as shipped in Phase 18; NIT-02 does not widen the payload |
| T-27-SC | Tampering | npm installs | high | mitigate | No install tasks (D-06) |

ASVS L1: access control stays on assertProjectWriteAccess; NIT-02 only changes when an append happens.
</threat_model>

<verification>
- 27-02-01: no-op skip green
- 27-02-02: real-change audit green
- Wave: npx vitest run --project node modules/projects/backend/services/milestones.service.unit.test.ts
- Isolation none. TDD RED then GREEN per task.
</verification>

<success_criteria>
A no-op milestone PATCH does not append audit_logs; a real audited-field change still does (NIT-02, D-02, D-06).
</success_criteria>

<output>
Create `.planning/phases/27-nits-validation-operator-gate/27-02-SUMMARY.md` when done
</output>
