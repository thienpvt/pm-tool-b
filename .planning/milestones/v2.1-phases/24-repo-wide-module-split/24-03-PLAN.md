---
phase: 24-repo-wide-module-split
plan: 03
type: tdd
wave: 3
depends_on: [24-02]
files_modified:
  - modules/weekly/backend/weekly-module-split.test.ts
  - modules/weekly/backend/services/weekly-reports.service.ts
  - modules/weekly/backend/services/weekly-tracking.service.ts
  - modules/weekly/backend/repositories/weekly-periods.repo.ts
  - modules/weekly/backend/repositories/weekly-reports.repo.ts
  - modules/weekly/backend/repositories/weekly-export.repo.ts
  - modules/weekly/backend/routes/weekly-periods/route.ts
  - modules/weekly/backend/routes/projects/[id]/weekly-reports/handlers.ts
  - modules/weekly/backend/routes/export/weekly-report/[id]/handlers.ts
  - app/api/weekly-periods/route.ts
  - app/api/projects/[id]/weekly-reports/route.ts
  - app/api/export/weekly-report/[id]/route.ts
  - modules/dashboards/backend/services/spec-dashboards.service.ts
  - lib/dashboards/period-resolver.ts
  - lib/dashboards/period-resolver.unit.test.ts
autonomous: true
requirements: [MOD-01, MOD-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "modules/weekly/{backend,ui}/ both exist; weekly UI trees stay put (MOD-01, D-04, D-06)."
    - "/api/weekly-periods/** URLs still resolve via P2 re-exports (MOD-02, D-02)."
    - "app/api/projects/[id]/weekly-reports/** and app/api/export/weekly-report/[id]/route.ts still call withProjectAccess locally (P3, ENF-01). Public URLs are unchanged (D-06)."
    - "Existing weekly page shells at /weekly/* and /projects/[id]/weekly-reports/[reportId] stay thin re-exports (D-02, D-10)."
    - "No new npm dependency is added (D-08)."
  artifacts:
    - path: "modules/weekly/backend/weekly-module-split.test.ts"
      provides: "P2 plus P3 wrapper-stays contract (ENF-01, MOD-02)"
    - path: "modules/weekly/backend/services/weekly-reports.service.ts"
      provides: "Moved weekly reports service (S1)"
    - path: "app/api/projects/[id]/weekly-reports/route.ts"
      provides: "P3 shell with local withProjectAccess (ENF-01)"
  key_links:
    - from: "app/api/weekly-periods/route.ts"
      to: "modules/weekly/backend/routes/weekly-periods/route.ts"
      via: "P2 named re-export"
      pattern: "modules/weekly/backend/routes/weekly-periods/route"
    - from: "app/api/projects/[id]/weekly-reports/route.ts"
      to: "modules/weekly/backend/routes/projects/[id]/weekly-reports/handlers.ts"
      via: "withProjectAccess(handler)"
      pattern: "withProjectAccess"
  prohibitions:
    - statement: "Must not replace project-scoped weekly-reports route files with a pure named re-export (ENF-01, P3)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not move weekly UI or change weekly-reports URL ownership (D-04, D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit vitest.config.ts, eslint/route-wrapper-allowlist.json, or Sidebar.tsx"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Move weekly services, repos, and route bodies into modules/weekly/backend. Keep weekly-periods as P2. Keep project-scoped weekly-reports and export/weekly-report wrappers in app/api (P3, ENF-01, MOD-01, MOD-02, D-01, D-02, D-03, D-04, D-06, D-08, D-09).

Purpose: First wave that exercises P3 wrapper-stays. Projects Wave 6 must not take these URLs.

Output: weekly backend tree; P2 weekly-periods shells; P3 weekly-reports and export weekly-report shells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-repo-wide-module-split/24-RESEARCH.md
@.planning/phases/24-repo-wide-module-split/24-PATTERNS.md
@app/api/weekly-periods/route.ts
@app/api/projects/[id]/weekly-reports/route.ts
@app/api/export/weekly-report/[id]/route.ts
@eslint/rules/require-auth-wrapper.mjs

Isolation none. Do not edit vitest.config.ts, the eslint allowlist, or Sidebar.tsx. Execute RESEARCH Wave 3 file-move map exactly, including colocated schema.ts and *.test.ts (D-09). Also move weekly-reports.submit.integration.test.ts.

<interfaces>
P2: weekly-periods/** including config, tracking, export, export/preview — full handler move plus named re-export of exported HTTP methods.
P3: app/api/projects/[id]/weekly-reports/** and app/api/export/weekly-report/[id]/route.ts — extract handler functions to modules/weekly/backend/routes/.../handlers.ts (or keep helper functions next to a module route file that does not export GET). app/api file MUST contain a local withProjectAccess( call wrapping those handlers. Forbidden: export { GET } from a module on these files (ENF-01).
Move existing route.test.ts with the module files; for P3 tests that imported GET from ./route, import GET from the app/api shell path so wrapper coverage remains (D-09).
</interfaces>
</context>

<feature>
  <name>Weekly backend split with P3 wrapper-stays</name>
  <files>modules/weekly/backend/services/weekly-reports.service.ts, app/api/weekly-periods/route.ts, app/api/projects/[id]/weekly-reports/route.ts</files>
  <behavior>
    weekly-periods app/api files are P2 re-exports (D-02).
    projects/[id]/weekly-reports app/api files contain withProjectAccess( (ENF-01, P3).
  </behavior>
  <implementation>
    git mv services/repos/P2 routes; extract P3 handlers; keep wrappers in app/api.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="24-03-01">
  <name>Contract tests plus weekly-periods P2 tracer</name>
  <files>modules/weekly/backend/weekly-module-split.test.ts, modules/weekly/backend/services/weekly-reports.service.ts, modules/weekly/backend/services/weekly-reports.service.unit.test.ts, modules/weekly/backend/services/weekly-reports.submit.integration.test.ts, modules/weekly/backend/repositories/weekly-periods.repo.ts, modules/weekly/backend/repositories/weekly-periods.repo.test.ts, modules/weekly/backend/routes/weekly-periods/route.ts, modules/weekly/backend/routes/weekly-periods/route.test.ts, modules/weekly/backend/routes/weekly-periods/schema.ts, app/api/weekly-periods/route.ts</files>
  <read_first>
  - .planning/phases/24-repo-wide-module-split/24-RESEARCH.md (Wave 3)
  - .planning/phases/24-repo-wide-module-split/24-PATTERNS.md (P2 and P3)
  - app/api/weekly-periods/route.ts
  - app/api/projects/[id]/weekly-reports/route.ts
  </read_first>
  <behavior>
  - Import of @/modules/weekly/backend/services/weekly-reports.service exposes listWeeklyPeriods or the equivalent existing export (S1, D-01).
  - app/api/weekly-periods/route.ts re-exports GET and POST from the module weekly-periods route (P2, D-02).
  - app/api/projects/[id]/weekly-reports/route.ts source contains withProjectAccess( and does not consist of a sole named GET re-export (P3, ENF-01).
  - Existing weekly page shells still mention modules/weekly/ui (D-04).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(24-03) then feat(24-03). Do not edit vitest.config.ts, eslint allowlist, Sidebar.tsx, or package.json (D-08).
  RED: Add modules/weekly/backend/weekly-module-split.test.ts covering S1 import, P2 weekly-periods re-export, P3 withProjectAccess( present in app/api/projects/[id]/weekly-reports/route.ts and app/api/export/weekly-report/[id]/route.ts, and P1 shells at app/weekly/periods/page.tsx plus app/projects/[id]/weekly-reports/[reportId]/page.tsx. Commit test(24-03): red weekly backend tracer.
  GREEN: git mv weekly-reports.service* (including submit integration test), weekly-periods.repo*, and app/api/weekly-periods/route.ts plus route.test.ts plus schema.ts per RESEARCH. Fix imports (D-03). Write P2 shell at app/api/weekly-periods/route.ts exporting GET and POST. Leave P3 files as they are until 24-03-03 except the contract already asserts the current wrapper identifiers. Commit feat(24-03): weekly-periods P2 tracer.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/weekly-module-split.test.ts modules/weekly/backend/routes/weekly-periods/route.test.ts modules/weekly/backend/services/weekly-reports.service.unit.test.ts</automated>
  </verify>
  <done>weekly-periods GET/POST runs through modules/weekly/backend with a P2 shell; P3 contract is stated (MOD-02, D-02, ENF-01).</done>
  <acceptance_criteria>
  - app/api/weekly-periods/route.ts re-exports GET and POST from the weekly backend
  - weekly-module-split.test.ts asserts withProjectAccess( in the projects weekly-reports app/api file
  - npx vitest run --project node modules/weekly/backend/weekly-module-split.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="24-03-02">
  <name>Move remaining weekly-periods P2 routes, tracking, and repos</name>
  <files>modules/weekly/backend/services/weekly-tracking.service.ts, modules/weekly/backend/repositories/weekly-reports.repo.ts, modules/weekly/backend/repositories/weekly-export.repo.ts, modules/weekly/backend/routes/weekly-periods/config/route.ts, app/api/weekly-periods/config/route.ts, app/api/weekly-periods/[periodId]/tracking/route.ts, app/api/weekly-periods/[periodId]/export/route.ts, app/api/weekly-periods/[periodId]/export/preview/route.ts, lib/dashboards/period-resolver.ts, lib/dashboards/period-resolver.unit.test.ts, modules/dashboards/backend/services/spec-dashboards.service.ts</files>
  <read_first>
  - .planning/phases/24-repo-wide-module-split/24-RESEARCH.md (Wave 3 weekly-periods routes)
  </read_first>
  <behavior>
  - Every remaining app/api/weekly-periods/**/route.ts is a P2 named re-export of the matching modules/weekly/backend/routes/weekly-periods/**/route.ts (D-02).
  - weekly-tracking.service, weekly-reports.repo, and weekly-export.repo live under modules/weekly/backend (D-01, D-09).
  - lib/dashboards/period-resolver.ts and its unit test import WeeklyPeriodRow from @/modules/weekly/backend/repositories/weekly-periods.repo (D-03). Wave 1 left lib/dashboards in place.
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits.
  RED: Extend weekly-module-split.test.ts for each remaining weekly-periods route file in the RESEARCH table (config, tracking, export, export/preview) asserting P2 re-exports. Also assert lib/dashboards/period-resolver.ts and lib/dashboards/period-resolver.unit.test.ts source contain @/modules/weekly/backend/repositories/weekly-periods.repo. Commit test(24-03): red remaining weekly-periods P2 routes.
  GREEN: git mv RESEARCH remaining weekly-periods routes (with schema.ts and tests), weekly-tracking.service*, weekly-reports.repo*, weekly-export.repo*. Write P2 shells exporting the methods each module file exports. Fix @/ imports only (D-03). Update modules/dashboards/backend/services/spec-dashboards.service.ts weekly-reports.service and weekly-periods.repo imports to module paths, and the matching vi.mock in spec-dashboards.service.unit.test.ts. Retarget lib/dashboards/period-resolver.ts and lib/dashboards/period-resolver.unit.test.ts from @/lib/repositories/weekly-periods.repo to @/modules/weekly/backend/repositories/weekly-periods.repo (path-only; do not restyle). Grep remaining @/lib/repositories/weekly-periods.repo and retarget any leftover. Commit feat(24-03): remaining weekly-periods P2 routes.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/weekly-module-split.test.ts lib/dashboards/period-resolver.unit.test.ts</automated>
  </verify>
  <done>All /api/weekly-periods URLs are P2 shells over modules/weekly/backend (MOD-02, D-02, D-06).</done>
  <acceptance_criteria>
  - weekly-tracking.service.ts lives under modules/weekly/backend/services
  - app/api/weekly-periods/config/route.ts is a named re-export
  - npx vitest run --project node modules/weekly/backend/weekly-module-split.test.ts lib/dashboards/period-resolver.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="24-03-03">
  <name>Extract P3 weekly-reports and export weekly-report handlers</name>
  <files>modules/weekly/backend/routes/projects/[id]/weekly-reports/handlers.ts, modules/weekly/backend/routes/projects/[id]/weekly-reports/[reportId]/handlers.ts, modules/weekly/backend/routes/projects/[id]/weekly-reports/[reportId]/submit/handlers.ts, modules/weekly/backend/routes/projects/[id]/weekly-reports/[reportId]/correct/handlers.ts, modules/weekly/backend/routes/export/weekly-report/[id]/handlers.ts, app/api/projects/[id]/weekly-reports/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts, app/api/export/weekly-report/[id]/route.ts, modules/weekly/backend/weekly-module-split.test.ts</files>
  <read_first>
  - .planning/phases/24-repo-wide-module-split/24-PATTERNS.md (P3 analog)
  - app/api/projects/[id]/weekly-reports/route.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/route.ts
  - app/api/export/weekly-report/[id]/route.ts
  - eslint/rules/require-auth-wrapper.mjs
  </read_first>
  <behavior>
  - Each listed app/api route.ts source contains withProjectAccess( wrapping a handler imported from modules/weekly/backend/routes/... (P3, ENF-01, D-02).
  - None of those app/api files is only a named GET/POST re-export from a module (ENF-01).
  - Public paths /api/projects/:id/weekly-reports and /api/export/weekly-report/:id are unchanged (MOD-02, D-06).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits.
  RED: Extend weekly-module-split.test.ts: for each P3 file in RESEARCH Wave 3 (projects weekly-reports list, reportId, submit, correct, and export weekly-report), assert source includes withProjectAccess( and includes modules/weekly/backend/routes. Commit test(24-03): red weekly P3 wrapper-stays.
  GREEN: For each of those routes, move handler bodies (and colocated schema.ts / route.test.ts / route.access.test.ts per RESEARCH) into modules/weekly/backend/routes/... . Keep wrappers in the original app/api/route.ts: import handler functions, export const GET = withProjectAccess(handler) (and POST/PATCH the same way, including existing schema options). Do not rewrite Excel generation in export/weekly-report (D-03). Point P3 tests at the app/api shell for wrapped GET/POST. Run eslint on the touched app/api project-scoped files so ENF-01 still sees local wrapper calls. Commit feat(24-03): weekly P3 wrapper-stays.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/weekly-module-split.test.ts && npx eslint "app/api/projects/[id]/weekly-reports/**/route.ts" "app/api/export/weekly-report/[id]/route.ts"</automated>
    <human-check>
      <test>Open /weekly/periods and a project weekly-report editor URL</test>
      <expected>Same pages at the same URLs (D-10)</expected>
      <why_human>URL smoke is visual</why_human>
    </human-check>
  </verify>
  <done>Project-scoped weekly APIs keep wrappers in app/api; handler bodies live under modules/weekly/backend (ENF-01, D-02, D-06).</done>
  <acceptance_criteria>
  - app/api/projects/[id]/weekly-reports/route.ts contains withProjectAccess(
  - Handler bodies import from modules/weekly/backend
  - npx eslint on those app/api weekly-reports and export weekly-report route files exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/projects/[id]/weekly-reports | Project tenancy; ENF-01 wrapper must stay in app/api |
| Browser → /api/weekly-periods | CPMO period admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04 | Elevation of Privilege | app/api/projects/[id]/weekly-reports/**/route.ts | critical | mitigate | P3: keep withProjectAccess( in the app/api file. Contract test plus eslint require-auth-wrapper. ASVS L1 V4. |
| T-24-05 | Elevation of Privilege | app/api/export/weekly-report/[id]/route.ts | high | mitigate | Same P3 wrapper-stays. |
| T-24-SC | Tampering | npm installs | high | accept | No package-manager install (D-08). |
</threat_model>

<verification>
- 24-03-01: weekly-periods P2 tracer green
- 24-03-02: remaining weekly-periods tests green
- 24-03-03: P3 eslint plus weekly backend tests green
- Wave: npx vitest run modules/weekly
</verification>

<success_criteria>
Weekly backend exists; weekly-periods are P2; weekly-reports under projects and export/weekly-report stay P3 with wrappers in app/api; URLs unchanged (MOD-01, MOD-02, ENF-01, D-06).
</success_criteria>

<output>
Create `.planning/phases/24-repo-wide-module-split/24-03-SUMMARY.md` when done
</output>
