---
phase: 23-document-checklist-audit-viewer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/documents/ui/shared/types.ts
  - modules/documents/ui/shared/documents.fixture.ts
  - modules/documents/ui/catalog/useDocumentCatalog.ts
  - modules/documents/ui/catalog/DocumentCatalogPage.tsx
  - modules/documents/ui/catalog/CatalogList.tsx
  - modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx
  - app/documents/catalog/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/Sidebar.documents-nav.component.test.tsx
autonomous: true
requirements: [DOC-07]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO opens /documents/catalog via a thin app re-export of DocumentCatalogPage and sees title Document catalog (DOC-07, D-01)."
    - "One GET /api/document-catalog loads the list; the page never invents a document endpoint (D-03, D-04)."
    - "Initial GET shows a centered spinner and Loading document catalog… (UI-SPEC loading catalog-page-shell)."
    - "401 shows Session expired — refresh the page and sign in again.; 403 shows You don't have access to this page.; 5xx shows Couldn't load this page. Try again. in a centered main panel with AlertTriangle (UI-SPEC error catalog-page-shell, D-04)."
    - "Zero catalog rows render No catalog items yet and Add the first required document type for your company above. in a table colspan cell (UI-SPEC empty catalog-list)."
    - "A typical list (under 50 items) renders a compact table of Name, Stage, Mandatory, Status without VirtualRows (UI-SPEC populated catalog-list)."
    - "Retired rows (active false) use text-slate-400 line-through; Status badge is Retired (UI-SPEC catalog retired)."
    - "Subtitle uses item vs items from the active count (UI-SPEC zero-one-many catalog-list)."
    - "Catalog list wrapper uses overflow-x-auto; purpose truncates at 200px with title (UI-SPEC overflow catalog-list, long-text catalog-purpose)."
    - "With no row selected, templates zone shows Select a catalog item to manage templates. (UI-SPEC empty templates-no-selection, D-14)."
    - "Sidebar shows Catalog (/documents/catalog), Compliance (/documents/compliance), and Audit log (/audit) after Weekly tracking when roles includes cpmo; pm and viewer never receive those three links (D-02, UI-SPEC populated/error sidebar-nav-document-links)."
    - "NAV still renders from cached /api/auth/me while a catalog page body loads (UI-SPEC loading sidebar-nav-document-links)."
    - "PROJECT_NAV Documents href /documents is unchanged; no PM checklist item is added to Sidebar (D-02, D-06)."
    - "Headings use text-base font-semibold; labels text-xs font-semibold; body text-sm; no extra font-weight utilities on the catalog module page (D-04)."
    - "No new npm dependency is added (D-03)."
  artifacts:
    - path: "modules/documents/ui/shared/types.ts"
      provides: "Client-safe catalog, template, checklist, compliance, audit types (D-03)"
    - path: "modules/documents/ui/shared/documents.fixture.ts"
      provides: "Shared mocked payloads including 150-row compliance and audit lists"
    - path: "app/documents/catalog/page.tsx"
      provides: "Thin use-client re-export (D-01)"
    - path: "modules/documents/ui/catalog/DocumentCatalogPage.tsx"
      provides: "Catalog shell, loading/error, list (DOC-07, D-04)"
    - path: "components/layout/Sidebar.tsx"
      provides: "CPMO Catalog, Compliance, Audit log links (D-02)"
  key_links:
    - from: "app/documents/catalog/page.tsx"
      to: "modules/documents/ui/catalog/DocumentCatalogPage.tsx"
      via: "default re-export"
      pattern: "modules/documents/ui/catalog/DocumentCatalogPage"
    - from: "useDocumentCatalog.ts"
      to: "/api/document-catalog"
      via: "fetch GET"
      pattern: "/api/document-catalog"
    - from: "components/layout/Sidebar.tsx"
      to: "/documents/catalog"
      via: "Catalog Link when roles includes cpmo"
      pattern: "/documents/catalog"
  prohibitions:
    - statement: "Must not add npm packages or change package.json (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add document or audit API routes (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not overwrite app/projects/[id]/documents (D-01, D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not put a PM checklist item on Sidebar (D-02)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit app/projects/[id]/page.tsx (23-03 owns the hub card)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Prove the documents module plus thin App Router re-export plus CPMO Sidebar catalog/compliance/audit links end-to-end, and ship the CPMO catalog list/shell (DOC-07 list, D-01, D-02, D-03, D-04, D-14).

Purpose: Lock the Phase 21/22 module pattern before create/edit/retire, templates, checklist, compliance, and audit expand from it.

Output: Shared types and fixtures for 23-02 through 23-05, catalog GET page plus re-export, CPMO document/audit NAV.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-document-checklist-audit-viewer/23-CONTEXT.md
@.planning/phases/23-document-checklist-audit-viewer/23-RESEARCH.md
@.planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md
@.planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md
@.planning/phases/22-weekly-workflow-surfaces/22-01-PLAN.md
@vitest.config.ts
@components/layout/Sidebar.tsx
@components/layout/Sidebar.weekly-nav.component.test.tsx
@modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
@modules/weekly/ui/periods/useWeeklyPeriods.ts
@modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx
@app/weekly/periods/page.tsx
@app/api/document-catalog/route.ts
@lib/repositories/document-catalog.repo.ts
@lib/repositories/document-templates.repo.ts
@lib/repositories/project-document-checklist.repo.ts
@lib/repositories/audit.repo.ts
@lib/documents/compliance.ts
@lib/documents/checklist-status.ts

Planner-assigned locked IDs for this plan: D-01 module pages plus thin re-export at /documents/catalog; D-02 all three CPMO NAV links; D-03 consume existing APIs only; D-04 catalog list plus 403 in-page; D-14 one-page catalog plus templates zone.

Do not implement catalog POST/PATCH, templates GET/POST, checklist, compliance, or audit pages (23-02 through 23-05). Isolation none: this is the only plan that may edit Sidebar.tsx. Do not edit app/projects/[id]/page.tsx.

<interfaces>
GET /api/document-catalog returns DocumentCatalogRow array: id, company_id, name, purpose, stage (L0-L5 or ALL), mandatory boolean, active boolean, created_at, updated_at. Ordered by name. withAuth; pure viewer 403.

ERROR_COPY (23-UI-SPEC, English — not dashboard analog strings):
unauthorized: Session expired — refresh the page and sign in again.
forbidden: You don't have access to this page.
load_failed: Couldn't load this page. Try again.

Copywriting: page title Document catalog; loading Loading document catalog…; empty heading No catalog items yet; empty body Add the first required document type for your company above.; templates-no-selection Select a catalog item to manage templates.

Page shell: flex flex-col lg:flex-row min-h-screen bg-slate-50, Sidebar, main flex-1 p-4 lg:p-6 lg:p-8 overflow-auto. Spinner w-8 h-8 border-2 border-blue-500 border-t-transparent rounded-full animate-spin. Headings text-base font-semibold. Labels/chips text-xs font-semibold. Body text-sm. Do not add font-medium or font-bold on the catalog module page.

TableHead: h-8 px-2 text-xs font-semibold. TableCell: p-2 text-sm.

Shared types.ts is client-only (do not import lib/services or lib/repositories — those pull pg). Export CatalogRow, TemplateRow, ChecklistStatus, ChecklistItem, ComplianceStatus, ComplianceProject, CompliancePayload, ComplianceFilters, AuditLogRow, AuditFilters now so 23-02..23-05 do not edit this file.

documents.fixture.ts: catalogFixture (two rows: id 1 Charter stage L2 mandatory true active true purpose Signed charter; id 2 SoW stage ALL mandatory false active false), emptyCatalogFixture helper, templatesFixture (one HTTPS template_url for catalog_id 1), emptyTemplatesFixture, checklistFixture (two items, statuses drafting and approved), emptyChecklistFixture, complianceFixture { filters: {}, projects: two rows with compliance compliant and not_compliant }, complianceProjects150 (project_id 1..150, distinct names), auditRowsFixture (two rows with object before/after), auditRows150 (id 1..150).

vitest.config.ts already includes modules/** (Phase 21). Do not edit it. Do not edit package.json.
</interfaces>
</context>

<feature>
  <name>Document catalog list tracer with CPMO document NAV</name>
  <files>modules/documents/ui/catalog/DocumentCatalogPage.tsx, app/documents/catalog/page.tsx, components/layout/Sidebar.tsx</files>
  <behavior>
    Mocked GET /api/document-catalog 200 shows Document catalog and list rows (DOC-07, D-04).
    cpmo Sidebar includes Catalog, Compliance, and Audit log (D-02).
  </behavior>
  <implementation>
    Catalog GET hook plus page; thin app re-export; Sidebar links after Weekly tracking and before NAV_SECONDARY (D-01, D-02).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="23-01-01">
  <name>End-to-end catalog GET path plus shared types</name>
  <reversibility rating="costly">Module root modules/documents/ui plus /documents/catalog become the v2.1 document landing pattern (D-01).</reversibility>
  <files>modules/documents/ui/shared/types.ts, modules/documents/ui/shared/documents.fixture.ts, modules/documents/ui/catalog/useDocumentCatalog.ts, modules/documents/ui/catalog/DocumentCatalogPage.tsx, modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx, app/documents/catalog/page.tsx</files>
  <read_first>
  - .planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md (thin re-export, fetch hook, page shell)
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (catalog zones, Copywriting, spacing, typography)
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx
  - app/weekly/periods/page.tsx
  - app/api/document-catalog/route.ts
  - lib/repositories/document-catalog.repo.ts
  </read_first>
  <behavior>
  - Deferred fetch on DocumentCatalogPage: Loading document catalog… is in the document before JSON arrives (UI-SPEC loading catalog-page-shell).
  - GET 200: heading Document catalog, table shows fixture name Charter and stage L2 (DOC-07, D-04).
  - GET 403: You don't have access to this page. (UI-SPEC error catalog-page-shell, D-04).
  - app/documents/catalog/page.tsx re-exports DocumentCatalogPage (D-01).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Merge RED and GREEN in this task. Do not create a RED-only task. Tests before implementation. Two commits: test(23-01) then feat(23-01).
  RED: Add modules/documents/ui/shared/types.ts as client-only types (do not import lib/services or lib/repositories). Export CatalogRow, TemplateRow, ChecklistStatus as the five API status strings none drafting pending_approval approved not_applicable, ChecklistItem matching checklist GET JSON, ComplianceStatus compliant not_compliant not_applicable, ComplianceFilters with optional stage status rag program, ComplianceProject, CompliancePayload with filters and projects, AuditLogRow matching GET /api/audit row shape, AuditFilters with optional entity_type entity_id from to limit. Add modules/documents/ui/shared/documents.fixture.ts with catalogFixture, emptyCatalogFixture, templatesFixture, emptyTemplatesFixture, checklistFixture, emptyChecklistFixture, complianceFixture, complianceProjects150, auditRowsFixture, auditRows150 as specified in interfaces. Add DocumentCatalogPage.component.test.tsx: mock next/navigation usePathname to /documents/catalog; mock Sidebar as nav data-testid sidebar; stub fetch GET /api/document-catalog with deferred Promise for loading, 200 JSON catalogFixture, and 403. Assert sidebar testid, title Document catalog, loading copy, 403 copy, and Charter. Tests must fail because production modules are missing. Commit test(23-01): red catalog list tracer.
  GREEN: Create useDocumentCatalog: useState data/loading/error; load GET /api/document-catalog only (D-03); 401 unauthorized, 403 forbidden, other !ok load_failed, 200 sets the JSON array; do not assign window.location. Create DocumentCatalogPage: Phase 22 shell; loading spinner plus Loading document catalog…; error panel AlertTriangle plus UI-SPEC strings (403 is this page not this dashboard); after load, h1 Document catalog, subtitle N item or items from active true count. Inline compact Table for the list (extract in task 3). Create app/documents/catalog/page.tsx with use client and export { default } from the module page (D-01). Do not POST or PATCH. Do not add packages. Do not edit app/projects/[id]/documents. Commit feat(23-01): catalog list tracer.
  REFACTOR only if tests stay green.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</automated>
    <human-check>
      <test>Sign in as CPMO and open /documents/catalog</test>
      <expected>Sidebar present; title Document catalog; loading then a catalog table or the empty copy; no full-page crash</expected>
      <why_human>Shell spacing and NAV density are visual</why_human>
    </human-check>
  </verify>
  <done>CPMO catalog GET path renders through the module re-export (DOC-07, D-01, D-03, D-04).</done>
  <acceptance_criteria>
  - app/documents/catalog/page.tsx re-exports DocumentCatalogPage
  - useDocumentCatalog fetch URL is /api/document-catalog
  - npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-01-02">
  <name>Sidebar Catalog, Compliance, and Audit log links</name>
  <reversibility rating="costly">NAV hrefs /documents/catalog, /documents/compliance, and /audit are the published CPMO entry points (D-02).</reversibility>
  <files>components/layout/Sidebar.tsx, components/layout/Sidebar.documents-nav.component.test.tsx</files>
  <read_first>
  - components/layout/Sidebar.tsx (weekly cpmo block then NAV_SECONDARY)
  - components/layout/Sidebar.weekly-nav.component.test.tsx
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (Routes and NAV table)
  - .planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md (CPMO Sidebar gate)
  </read_first>
  <behavior>
  - me.roles includes cpmo: links Catalog to /documents/catalog, Compliance to /documents/compliance, Audit log to /audit are in the document after Weekly tracking (D-02).
  - me.roles includes pm only: none of those three CPMO links is present (UI-SPEC error sidebar-nav-document-links).
  - me.roles is viewer or missing: none of those three CPMO links is present.
  - pathname /documents/catalog applies existing active classes bg-blue-600 text-white on Catalog (UI-SPEC populated sidebar-nav-document-links).
  - There is no Sidebar link whose accessible name is Document checklist (D-02).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add components/layout/Sidebar.documents-nav.component.test.tsx copying stubFetch from Sidebar.weekly-nav.component.test.tsx. Cover cpmo sees Catalog, Compliance, and Audit log with those hrefs; pm-only and viewer see none of the three; pathname /documents/catalog applies bg-blue-600 text-white on Catalog; cpmo still sees Weekly periods. Tests fail until links exist. Commit test(23-01): red sidebar catalog compliance audit nav.
  GREEN: Import ClipboardCheck and ScrollText from lucide-react (FileText is already imported). Inside the existing me?.roles?.includes('cpmo') fragment, after the Weekly tracking Link and before the fragment closes, render Catalog (FileText, href /documents/catalog) then Compliance (ClipboardCheck, href /documents/compliance) then Audit log (ScrollText, href /audit) using the same Link className and onNavClick as Weekly tracking (D-02). Active match: catalog and compliance use pathname equals or startsWith their href; audit uses pathname equals /audit or startsWith /audit/. Do not add a PM checklist Sidebar item (D-02). Do not change PROJECT_NAV Documents href /documents. Do not change Spec dashboard, My dashboard, or weekly hrefs. English labels only. There is no company-level /documents page — do not add a NAV_PRIMARY /documents item; Catalog lives at /documents/catalog so CPMO does not 404 (D-02). Commit feat(23-01): sidebar catalog compliance audit links.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom components/layout/Sidebar.documents-nav.component.test.tsx components/layout/Sidebar.weekly-nav.component.test.tsx components/layout/Sidebar.dashboard-nav.component.test.tsx</automated>
    <human-check>
      <test>As cpmo, confirm Catalog, Compliance, and Audit log sit after Weekly tracking; as pm, confirm they are absent</test>
      <expected>cpmo sees the three links; pm still sees My dashboard without those three</expected>
      <why_human>Role-gated NAV order is visual</why_human>
    </human-check>
  </verify>
  <done>CPMO sees Catalog, Compliance, and Audit log; pm and viewer do not; existing dashboard and weekly NAV tests stay green (D-02).</done>
  <acceptance_criteria>
  - Sidebar.tsx contains href /documents/catalog, /documents/compliance, and /audit
  - Those three links are gated on roles includes cpmo
  - npx vitest run --project jsdom components/layout/Sidebar.documents-nav.component.test.tsx components/layout/Sidebar.weekly-nav.component.test.tsx components/layout/Sidebar.dashboard-nav.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-01-03">
  <name>Catalog list empty, overflow, retired row, templates prompt</name>
  <files>modules/documents/ui/catalog/CatalogList.tsx, modules/documents/ui/catalog/DocumentCatalogPage.tsx, modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</files>
  <read_first>
  - modules/documents/ui/catalog/DocumentCatalogPage.tsx from 23-01-01
  - modules/weekly/ui/periods/WeeklyPeriodList.tsx
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (catalog list table, empty copy, overflow, D-14)
  </read_first>
  <behavior>
  - Zero catalog rows: table headers remain; one body cell colSpan all columns; heading No catalog items yet and the empty body (UI-SPEC empty catalog-list).
  - Two fixture rows: columns Name, Stage, Mandatory (Yes/No Badge), Status Active or Retired (UI-SPEC populated catalog-list).
  - Retired fixture row uses text-slate-400 line-through on the name (UI-SPEC catalog retired).
  - Wrapper overflow-x-auto; purpose truncates with title (UI-SPEC overflow/long-text catalog-list).
  - Subtitle 1 item vs 2 items from active count (UI-SPEC zero-one-many catalog-list).
  - GET 401 uses Session expired — refresh the page and sign in again. (UI-SPEC error catalog-page-shell).
  - No selected row: Select a catalog item to manage templates. is visible (UI-SPEC empty templates-no-selection, D-14).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend DocumentCatalogPage.component.test.tsx for empty array, 401 copy, subtitle pluralization, overflow-x-auto on the list wrapper, retired line-through on SoW, and the templates-no-selection string (D-14). Commit test(23-01): red catalog list empty overflow and 401.
  GREEN: Extract CatalogList with compact shadcn Table matching Phase 21/22 density. Columns Name, Stage, Mandatory, Status. Empty state Copywriting strings in a spanning cell. Retired name line-through. Wire it from DocumentCatalogPage. Below the table, render a muted templates prompt using the UI-SPEC no-selection string (D-14). Keep GET-only hook (D-03, D-04). Do not add create, edit, retire, or template fetch (23-02). Commit feat(23-01): catalog list empty overflow and 401.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</automated>
  </verify>
  <done>Catalog list covers empty, populated, retired, overflow, 401, and the D-14 templates prompt (DOC-07, D-04, D-14).</done>
  <acceptance_criteria>
  - CatalogList.tsx exists and is rendered by DocumentCatalogPage
  - Empty copy matches 23-UI-SPEC catalog-list empty strings
  - npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser module UI → GET /api/document-catalog | Session cookie; catalog list crosses here |
| Sidebar NAV → page URL | Hiding a link is UX only; API remains the authz truth (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | DocumentCatalogPage / Sidebar | high | mitigate | NAV hide is not policy; 401/403 handled in-page; rely on withAuth/withCpmo (D-04, ASVS V4) |
| T-23-02 | Information Disclosure | CatalogList | medium | mitigate | Consume GET /api/document-catalog only; do not fetch other-company URLs (D-03) |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-03) |

ASVS L1: high client→API authz stays on existing wrappers; this plan does not add a second policy engine.
</threat_model>

<verification>
- 23-01-01: catalog GET tracer green
- 23-01-02: Sidebar document/audit links green; dashboard and weekly NAV still green
- 23-01-03: empty/401/overflow/retired/D-14 prompt green
- Wave: npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx components/layout/Sidebar.documents-nav.component.test.tsx
- package.json and package-lock.json unchanged
</verification>

<success_criteria>
A CPMO can open /documents/catalog (module UI plus thin re-export) and see the catalog list from GET /api/document-catalog; Sidebar exposes Catalog, Compliance, and Audit log by cpmo role (DOC-07 list, D-01, D-02, D-03, D-04, D-14).
</success_criteria>

<output>
Create `.planning/phases/23-document-checklist-audit-viewer/23-01-SUMMARY.md` when done
</output>
