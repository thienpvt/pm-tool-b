---
phase: 23-document-checklist-audit-viewer
plan: 02
type: tdd
wave: 2
depends_on: [23-01]
files_modified:
  - modules/documents/ui/catalog/CatalogForm.tsx
  - modules/documents/ui/catalog/TemplatePanel.tsx
  - modules/documents/ui/catalog/useDocumentCatalog.ts
  - modules/documents/ui/catalog/DocumentCatalogPage.tsx
  - modules/documents/ui/catalog/CatalogList.tsx
  - modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx
autonomous: true
requirements: [DOC-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Create catalog card POSTs name, purpose, stage, mandatory, active, and apply_to_in_flight to POST /api/document-catalog; Add catalog item is disabled while POST is in-flight (DOC-07, D-04, D-12, UI-SPEC loading catalog-create-form)."
    - "Successful POST toasts Catalog item added and the new row appears; failed POST toasts Couldn't add catalog item. Check the fields and try again. (UI-SPEC error catalog-create-form)."
    - "Edit pre-fills name, purpose, stage, mandatory from the selected row; apply_to_in_flight checkbox starts unchecked because GET does not persist that flag; Save catalog item PATCHes /api/document-catalog/{id} including apply_to_in_flight when checked (D-12, UI-SPEC partial catalog-edit-form)."
    - "Successful PATCH toasts Catalog item saved (UI-SPEC catalog save success)."
    - "Retire item opens dialog Retire this catalog item? with body Existing checklist rows remain; new projects won't receive this item.; confirm PATCHes { active: false }; success toasts Catalog item retired and the row shows Retired (D-04, UI-SPEC populated catalog-retire-flow)."
    - "Selecting a catalog row (or ?catalogId=) loads GET /api/document-templates?catalog_id={id} into the templates panel (D-14)."
    - "Zero templates for a selected item show No templates for this item and Publish a template URL to give PMs a starting link. (UI-SPEC empty templates-panel)."
    - "Template list shows name, type, version, effective date, and an external HTTPS link with target _blank and rel noopener noreferrer (UI-SPEC populated templates-panel, template-url-link)."
    - "Long template URLs truncate with title (UI-SPEC overflow templates-panel)."
    - "Publish template POSTs catalog_id, name, document_type, effective_date, guidance, template_url to /api/document-templates; button disabled while POST in-flight; success toasts Template published (D-04, UI-SPEC loading template-create-form)."
    - "Invalid template_url shows inline error under Template URL (UI-SPEC error template-create-form)."
    - "Template retire row action PATCHes /api/document-templates/{id} with { retire: true } (DOC-07)."
    - "No file picker exists on the catalog page; templates are URL-only (D-04)."
    - "Primary buttons use bg-blue-600 text-white hover:bg-blue-700; two font weights only (D-04)."
    - "Viewer still sees the 403 page panel from 23-01; mutations are not a second client authz layer (D-04)."
  artifacts:
    - path: "modules/documents/ui/catalog/CatalogForm.tsx"
      provides: "Create and edit catalog fields including apply_to_in_flight (D-04, D-12)"
    - path: "modules/documents/ui/catalog/TemplatePanel.tsx"
      provides: "Selected-row template list and URL-only publish (D-04, D-14)"
    - path: "modules/documents/ui/catalog/useDocumentCatalog.ts"
      provides: "POST/PATCH catalog, GET/POST templates, PATCH retire template (D-03)"
  key_links:
    - from: "useDocumentCatalog.ts"
      to: "/api/document-catalog"
      via: "POST create and PATCH id"
      pattern: "/api/document-catalog"
    - from: "useDocumentCatalog.ts"
      to: "/api/document-templates"
      via: "GET catalog_id, POST publish, PATCH retire"
      pattern: "/api/document-templates"
  prohibitions:
    - statement: "Must not add document API routes or npm packages (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit Sidebar.tsx (23-01 owns NAV)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit modules/documents/ui/shared/types.ts or documents.fixture.ts (23-01 owns)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add a binary upload control on catalog or templates (D-04)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO can create, edit, and soft-retire catalog items and publish URL-only templates from /documents/catalog using existing catalog and template routes (DOC-07, D-03, D-04, D-12, D-14).

Purpose: Complete DOC-07 after the 23-01 list tracer.

Output: CatalogForm, TemplatePanel, hook mutations, tests, Copywriting toasts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-document-checklist-audit-viewer/23-CONTEXT.md
@.planning/phases/23-document-checklist-audit-viewer/23-RESEARCH.md
@.planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md
@.planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md
@.planning/phases/23-document-checklist-audit-viewer/23-01-PLAN.md
@modules/documents/ui/catalog/useDocumentCatalog.ts
@modules/documents/ui/catalog/DocumentCatalogPage.tsx
@modules/documents/ui/catalog/CatalogList.tsx
@modules/weekly/ui/periods/WeeklyConfigForm.tsx
@app/api/document-catalog/route.ts
@app/api/document-catalog/[id]/route.ts
@app/api/document-templates/route.ts
@app/api/document-templates/[id]/route.ts
@components/ui/dialog.tsx

Planner-assigned locked IDs: D-03 consume existing APIs only; D-04 create/update/soft-retire plus URL-only templates; D-12 apply_to_in_flight checkbox posted because the existing schema has the field; D-14 one page with template panel for the selected row.

Isolation none: only this Wave 2 plan may edit catalog hook/page/list/form/panel files. Do not touch Sidebar.tsx, shared types/fixtures, checklist, compliance, audit, or the project hub.

<interfaces>
POST /api/document-catalog body (strict): name string min 1, purpose optional, stage L0|L1|L2|L3|L4|L5|ALL, mandatory optional boolean, active optional boolean, apply_to_in_flight optional boolean. 201 row. withCpmo.

PATCH /api/document-catalog/{id} same fields optional plus active false for retire. 200 row. withCpmo.

GET /api/document-templates?catalog_id={id} returns effective template rows (withAuth). POST body: catalog_id number, name, document_type, effective_date, guidance optional, template_url. Server parseHttpsUrl on template_url. 201. PATCH /api/document-templates/{id} body { retire: true } only — not { active: false }.

There is no Checkbox shadcn primitive in components/ui. Use native input type checkbox with a Label, matching modules/weekly/ui/tracking/TrackingGrid.tsx. Do not run shadcn add.

Copywriting: Add catalog item, Save catalog item, Retire item, Publish template; toasts Catalog item added, Catalog item saved, Catalog item retired, Template published, Couldn't add catalog item. Check the fields and try again., Couldn't save catalog item — try again.; retire dialog title Retire this catalog item?; body Existing checklist rows remain; new projects won't receive this item.

Primary Button size sm className bg-blue-600 text-white hover:bg-blue-700. Destructive retire confirm uses red-600.
</interfaces>
</context>

<feature>
  <name>Catalog create edit retire and URL-only templates</name>
  <files>modules/documents/ui/catalog/CatalogForm.tsx, modules/documents/ui/catalog/TemplatePanel.tsx, modules/documents/ui/catalog/useDocumentCatalog.ts</files>
  <behavior>
    POST catalog refreshes the list; PATCH retire sets active false; selected row loads templates; POST template_url publishes (DOC-07, D-04, D-12, D-14).
  </behavior>
  <implementation>
    Extend useDocumentCatalog with create, update, retire, loadTemplates, publishTemplate, retireTemplate; Card forms on DocumentCatalogPage (D-03, D-04, D-14).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="23-02-01">
  <name>Create catalog item with apply_to_in_flight</name>
  <files>modules/documents/ui/catalog/CatalogForm.tsx, modules/documents/ui/catalog/useDocumentCatalog.ts, modules/documents/ui/catalog/DocumentCatalogPage.tsx, modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</files>
  <read_first>
  - modules/documents/ui/catalog/useDocumentCatalog.ts
  - modules/documents/ui/catalog/DocumentCatalogPage.tsx
  - app/api/document-catalog/route.ts
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (create form fields)
  </read_first>
  <behavior>
  - Add catalog item POSTs JSON to /api/document-catalog including apply_to_in_flight boolean (D-12).
  - Button disabled while POST in-flight (UI-SPEC loading catalog-create-form).
  - 201 toasts Catalog item added and GET reloads so the new name appears (DOC-07).
  - Failed POST toasts Couldn't add catalog item. Check the fields and try again. (UI-SPEC error catalog-create-form).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend DocumentCatalogPage.component.test.tsx: fill Name, submit Add catalog item, assert fetch POST /api/document-catalog JSON includes name and apply_to_in_flight; mock 201 then list reload; assert Catalog item added via sonner; mock failed POST and assert the create-error toast. Assert Add catalog item is disabled during an unresolved POST. Commit test(23-02): red catalog create.
  GREEN: Extend useDocumentCatalog with createCatalog POST to /api/document-catalog (D-03). Body includes name, purpose, stage, mandatory, and apply_to_in_flight (D-12 — the existing schema has the field, so always send the boolean). Add CatalogForm fields: Input name, Textarea purpose rows 2, Select stage L0-L5 and ALL, native checkbox Mandatory, native checkbox Apply to in-flight projects, primary Add catalog item. Place the create Card above CatalogList (D-14 zone order). Disable the button while POST in-flight. Do not PATCH in this task. Commit feat(23-02): catalog create with apply_to_in_flight.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</automated>
  </verify>
  <done>CPMO can POST a catalog item including apply_to_in_flight and see the list refresh (DOC-07, D-04, D-12).</done>
  <acceptance_criteria>
  - POST /api/document-catalog is called with apply_to_in_flight in the JSON body
  - Create success toast matches Catalog item added
  - npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-02-02">
  <name>Edit catalog item and retire with confirmation</name>
  <files>modules/documents/ui/catalog/CatalogForm.tsx, modules/documents/ui/catalog/CatalogList.tsx, modules/documents/ui/catalog/useDocumentCatalog.ts, modules/documents/ui/catalog/DocumentCatalogPage.tsx, modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</files>
  <read_first>
  - modules/documents/ui/catalog/CatalogList.tsx
  - app/api/document-catalog/[id]/route.ts
  - components/ui/dialog.tsx
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (edit, retire dialog)
  </read_first>
  <behavior>
  - Edit catalog item opens a form pre-filled with name, purpose, stage, mandatory from the row (UI-SPEC partial catalog-edit-form).
  - Save catalog item PATCHes /api/document-catalog/{id}; success toasts Catalog item saved.
  - Retire opens the confirmation dialog; confirming PATCHes { active: false }; success toasts Catalog item retired (D-04, UI-SPEC populated catalog-retire-flow).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend tests: click Edit catalog item on Charter, assert pre-filled Charter; submit Save catalog item and assert PATCH /api/document-catalog/1; toast Catalog item saved. Click Retire, assert dialog title Retire this catalog item? and body Existing checklist rows remain; new projects won't receive this item.; confirm Retire item; assert PATCH body active false; toast Catalog item retired. Commit test(23-02): red catalog edit and retire.
  GREEN: Add updateCatalog and retireCatalog on the hook (PATCH existing route, D-03). Edit uses the same CatalogForm fields; apply_to_in_flight starts unchecked on edit (GET has no such column) and is sent on PATCH when the user checks it (D-12). Retire uses shadcn Dialog already in the repo; confirm sends { active: false } only for the retire action. Do not DELETE. Re-activate is allowed via Edit PATCH active true if the executor keeps an Active checkbox on edit. Commit feat(23-02): catalog edit and retire.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</automated>
  </verify>
  <done>Edit PATCH and retire confirmation work; list reflects Retired (DOC-07, D-04).</done>
  <acceptance_criteria>
  - Retire confirmation copy matches 23-UI-SPEC
  - PATCH retire body uses active false, not a DELETE
  - npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-02-03">
  <name>Templates panel for selected catalog row</name>
  <files>modules/documents/ui/catalog/TemplatePanel.tsx, modules/documents/ui/catalog/useDocumentCatalog.ts, modules/documents/ui/catalog/DocumentCatalogPage.tsx, modules/documents/ui/catalog/CatalogList.tsx, modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</files>
  <read_first>
  - app/api/document-templates/route.ts
  - app/api/document-templates/[id]/route.ts
  - modules/documents/ui/shared/documents.fixture.ts
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (templates panel)
  </read_first>
  <behavior>
  - Clicking a catalog row (or ?catalogId=) GETs /api/document-templates?catalog_id={id} (D-14).
  - Empty templates list shows No templates for this item and Publish a template URL to give PMs a starting link. (UI-SPEC empty templates-panel).
  - Publish template POSTs template_url HTTPS; disabled while in-flight; toast Template published (D-04).
  - Invalid template_url shows inline error under Template URL (UI-SPEC error template-create-form).
  - HTTPS template_url renders as an external link with target _blank and rel noopener noreferrer (UI-SPEC template-url-link).
  - Retire template PATCHes { retire: true } (Pitfall 5).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend tests: click Charter row (or set searchParams catalogId=1); assert GET /api/document-templates?catalog_id=1; empty templatesFixture replacement shows No templates for this item; populated templatesFixture shows template name and an anchor to the HTTPS URL with rel containing noopener. Submit Publish template with a non-https URL and assert inline error under Template URL. Successful POST asserts toast Template published. Optional retire control asserts PATCH body retire true. Mock next/navigation useSearchParams if using ?catalogId=. Commit test(23-02): red templates panel.
  GREEN: Implement TemplatePanel plus hook loadTemplates, publishTemplate, retireTemplate. GET query catalog_id. POST body catalog_id from selection, name, document_type, effective_date, guidance, template_url. Client-check template_url starts with https:// before POST; on 400 with field template_url show the error message under that Input. No file input. Native date Input for effective_date. Primary Publish template bg-blue-600. Replace the 23-01 muted-only prompt: still show the no-selection string when selectedId is null; show TemplatePanel when a row is selected (D-14). CatalogList row click sets selectedId. Do not copy VirtualRows. Commit feat(23-02): catalog templates panel.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx</automated>
  </verify>
  <done>Selected catalog row loads URL-only templates; publish and retire use existing template routes (DOC-07, D-04, D-14).</done>
  <acceptance_criteria>
  - GET /api/document-templates is called with catalog_id
  - Template retire uses retire true, not active false
  - npx vitest run --project jsdom modules/documents/ui/catalog/DocumentCatalogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser forms → POST/PATCH catalog and templates | CPMO session; mutation JSON crosses here |
| Template URL field → stored HTTPS string | Untrusted URL string; rendered as an anchor href |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03 | Tampering | CatalogForm POST/PATCH | high | mitigate | Send JSON matching existing zod schemas; rely on withCpmo; 403 stays in-page (D-03, D-04, ASVS V4) |
| T-23-04 | Tampering | Template URL | medium | mitigate | URL-only HTTPS field; server parseHttpsUrl remains authoritative; no binary fields in the form (D-04) |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-03) |

ASVS L1: mutations stay on existing withCpmo routes.
</threat_model>

<verification>
- 23-02-01: create POST plus apply_to_in_flight green
- 23-02-02: edit and retire green
- 23-02-03: templates panel green
- package.json unchanged
</verification>

<success_criteria>
CPMO can manage the document catalog and URL-only templates in the UI on one page (DOC-07, D-04, D-12, D-14).
</success_criteria>

<output>
Create `.planning/phases/23-document-checklist-audit-viewer/23-02-SUMMARY.md` when done
</output>
