---
phase: 23-document-checklist-audit-viewer
plan: 03
type: tdd
wave: 2
depends_on: [23-01]
files_modified:
  - modules/documents/ui/checklist/useProjectChecklist.ts
  - modules/documents/ui/checklist/ProjectChecklistPage.tsx
  - modules/documents/ui/checklist/ChecklistItemRow.tsx
  - modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx
  - app/projects/[id]/document-checklist/page.tsx
  - app/projects/[id]/page.tsx
  - app/projects/[id]/page.checklist-card.test.ts
autonomous: true
requirements: [DOC-08]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PM opens /projects/{id}/document-checklist via a thin app re-export of ProjectChecklistPage and sees title Document checklist (DOC-08, D-01)."
    - "GET /api/projects/{id}/document-checklist populates the table; the page never invents a checklist endpoint (D-03, D-05)."
    - "Initial GET shows spinner and Loading checklist… (UI-SPEC loading checklist-page-shell)."
    - "401/403/5xx use the same Copywriting error strings as catalog (UI-SPEC error checklist-page-shell). 404 maps to load_failed copy."
    - "Zero items show No checklist items yet and Checklist rows appear when the project stage matches catalog items. (UI-SPEC empty checklist-table)."
    - "Rows show catalog_name, Mandatory Badge when catalog_mandatory, catalog_stage, status Badge, Confluence link or em dash (UI-SPEC populated checklist-table)."
    - "Status Select labels are None, Drafting, Pending approval, Approved, Not applicable; PATCH values are none, drafting, pending_approval, approved, not_applicable (D-11)."
    - "Approved shows date plus approver fields; not_applicable shows N/A reason; other statuses hide those fields (D-05, UI-SPEC partial checklist-item-editor)."
    - "Empty Confluence URL is allowed for none and drafting (UI-SPEC partial checklist-confluence-url)."
    - "PATCH 400 { error, field } renders that field inline in text-red-600 text-xs plus toast Fix the highlighted field and try again. (D-13, UI-SPEC error checklist-item-editor). Do not read a fields array."
    - "Save checklist item is disabled while PATCH is in-flight; success toasts Checklist item saved (UI-SPEC loading checklist-item-save)."
    - "Valid HTTPS URL shows Open in Confluence below the input (UI-SPEC Open in Confluence)."
    - "Table wrapper overflow-x-auto; subtitle uses item vs items (UI-SPEC overflow/zero-one-many checklist-table)."
    - "N/A reason in the collapsed row truncates until Edit expands it (UI-SPEC long-text checklist-na-reason)."
    - "No file picker on the checklist page (D-05)."
    - "Project hub card label Document checklist, body Complete Confluence evidence for this stage., href /projects/{id}/document-checklist; existing Documents card to /projects/{id}/documents remains (D-06, D-15, UI-SPEC populated/error project-hub-checklist-card)."
    - "Sidebar is not used as the PM entry; checklist is project-scoped from the hub (D-02)."
  artifacts:
    - path: "app/projects/[id]/document-checklist/page.tsx"
      provides: "Thin use-client re-export (D-01)"
    - path: "modules/documents/ui/checklist/ProjectChecklistPage.tsx"
      provides: "Checklist shell, loading/error, table (DOC-08, D-05)"
    - path: "modules/documents/ui/checklist/ChecklistItemRow.tsx"
      provides: "Inline editor, conditional fields, singular field errors (D-11, D-13)"
    - path: "app/projects/[id]/page.tsx"
      provides: "Hub Document checklist card (D-15)"
  key_links:
    - from: "app/projects/[id]/document-checklist/page.tsx"
      to: "modules/documents/ui/checklist/ProjectChecklistPage.tsx"
      via: "default re-export"
      pattern: "modules/documents/ui/checklist/ProjectChecklistPage"
    - from: "useProjectChecklist.ts"
      to: "/api/projects/"
      via: "GET checklist and PATCH itemId"
      pattern: "/document-checklist"
    - from: "app/projects/[id]/page.tsx"
      to: "/projects/{id}/document-checklist"
      via: "QUICK_LINKS href /document-checklist"
      pattern: "/document-checklist"
  prohibitions:
    - statement: "Must not overwrite app/projects/[id]/documents (D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add checklist API routes or npm packages (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit Sidebar.tsx (23-01 owns NAV; no PM checklist NAV per D-02)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not invent extra checklist statuses (D-11)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not parse 400 fields as an array (D-13)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
A PM can complete a project's Confluence checklist in the UI at /projects/{id}/document-checklist, reached from a project-hub card, using existing GET/PATCH routes (DOC-08, D-01, D-03, D-05, D-06, D-11, D-13, D-15).

Purpose: Close DOC-08 without touching the v1 documents dump.

Output: Checklist page, row editor, hook, thin re-export, hub card, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-document-checklist-audit-viewer/23-CONTEXT.md
@.planning/phases/23-document-checklist-audit-viewer/23-RESEARCH.md
@.planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md
@.planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md
@.planning/phases/23-document-checklist-audit-viewer/23-01-PLAN.md
@modules/weekly/ui/report/WeeklyReportEditorPage.tsx
@modules/weekly/ui/report/useWeeklyReportEditor.ts
@modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx
@app/weekly/reports/[projectId]/[reportId]/page.tsx
@app/api/projects/[id]/document-checklist/route.ts
@app/api/projects/[id]/document-checklist/[itemId]/route.ts
@lib/documents/checklist-status.ts
@lib/api-errors.ts
@app/projects/[id]/page.tsx
@modules/documents/ui/shared/types.ts
@modules/documents/ui/shared/documents.fixture.ts

Planner-assigned locked IDs: D-01 re-export at /projects/[id]/document-checklist; D-03 existing APIs; D-05 editor plus HTTPS plus Approved/NA rules; D-06 do not overwrite v1 documents; D-11 API enum as-is; D-13 400 { error, field } singular (honor D-13 over D-05 fields wording); D-15 hub card copy.

Isolation none: this is the only plan that may edit app/projects/[id]/page.tsx. Do not edit Sidebar.tsx. Do not edit catalog files.

<interfaces>
GET /api/projects/{id}/document-checklist returns ChecklistWithCatalogRow array: id, project_id, catalog_id, status, confluence_url, approved_at, approved_by, na_reason, notes, catalog_name, catalog_stage, catalog_mandatory, catalog_active. withProjectAccess.

PATCH /api/projects/{id}/document-checklist/{itemId} body optional: status enum, confluence_url, approved_at, approved_by number or string, na_reason, notes. withProjectAccess then assertProjectWriteAccess. 400 ValidationError body { error, field? } from lib/api-errors.ts — singular field, never fields array (D-13). Server assertChecklistPatchRules: approved requires approved_at, approved_by, HTTPS confluence_url; not_applicable requires na_reason; pending_approval requires HTTPS confluence_url; none/drafting allow empty URL.

CHECKLIST_STATUSES wire values: none, drafting, pending_approval, approved, not_applicable.

Copywriting: title Document checklist; loading Loading checklist…; empty heading/body as UI-SPEC; Save checklist item; Open in Confluence; toast Checklist item saved; validation toast Fix the highlighted field and try again.

Project header: optional GET /api/projects/{id} for name; link to /projects/{id}. Pass projectId to Sidebar.

Hub QUICK_LINKS analog: { href: '/document-checklist', icon: ClipboardCheck, label: 'Document checklist', desc: 'Complete Confluence evidence for this stage.' } so existing Link template `/projects/${id}${href}` resolves correctly (D-15). Keep the Documents entry href /documents.
</interfaces>
</context>

<feature>
  <name>PM Confluence checklist editor and hub card</name>
  <files>modules/documents/ui/checklist/ProjectChecklistPage.tsx, app/projects/[id]/document-checklist/page.tsx, app/projects/[id]/page.tsx</files>
  <behavior>
    GET 200 renders checklist rows; PATCH 400 highlights the named field (DOC-08, D-05, D-13).
    Hub card Document checklist links to /projects/{id}/document-checklist (D-15).
  </behavior>
  <implementation>
    GET/PATCH hook plus row editor; thin re-export; QUICK_LINKS entry (D-01, D-05, D-15).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="23-03-01">
  <name>End-to-end checklist GET shell and re-export</name>
  <files>modules/documents/ui/checklist/useProjectChecklist.ts, modules/documents/ui/checklist/ProjectChecklistPage.tsx, modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx, app/projects/[id]/document-checklist/page.tsx</files>
  <read_first>
  - modules/weekly/ui/report/WeeklyReportEditorPage.tsx
  - modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx
  - app/api/projects/[id]/document-checklist/route.ts
  - modules/documents/ui/shared/documents.fixture.ts
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (checklist zones)
  </read_first>
  <behavior>
  - Deferred GET shows Loading checklist… and sidebar with data-project-id 42 (UI-SPEC loading checklist-page-shell).
  - GET 200 heading Document checklist; fixture catalog_name visible (DOC-08, D-05).
  - GET 403 shows You don't have access to this page. (D-05).
  - app/projects/[id]/document-checklist/page.tsx re-exports ProjectChecklistPage (D-01).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add ProjectChecklistPage.component.test.tsx mocking next/navigation useParams id 42 and usePathname /projects/42/document-checklist; mock Sidebar as nav data-testid sidebar data-project-id from props. Stub GET /api/projects/42/document-checklist with deferred Promise, 200 checklistFixture, 403. Assert loading copy, title Document checklist, 403 copy. Tests fail until modules exist. Commit test(23-03): red checklist GET tracer.
  GREEN: useProjectChecklist(projectId) loads GET /api/projects/{id}/document-checklist only (D-03). 401/403/load_failed tristate; do not redirect. ProjectChecklistPage uses Phase 22 shell with Sidebar projectId={projectId}. Title Document checklist. Optional GET /api/projects/{id} for the name Link to /projects/{id} — if that fetch fails, still show the checklist. Create thin re-export app/projects/[id]/document-checklist/page.tsx (D-01). Do not PATCH yet. Do not edit app/projects/[id]/documents. Commit feat(23-03): checklist GET shell.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx</automated>
  </verify>
  <done>Checklist GET path renders through the module re-export (DOC-08, D-01, D-03, D-05).</done>
  <acceptance_criteria>
  - app/projects/[id]/document-checklist/page.tsx re-exports ProjectChecklistPage
  - GET URL is /api/projects/42/document-checklist in the test
  - npx vitest run --project jsdom modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-03-02">
  <name>Checklist item PATCH editor with singular field errors</name>
  <files>modules/documents/ui/checklist/ChecklistItemRow.tsx, modules/documents/ui/checklist/useProjectChecklist.ts, modules/documents/ui/checklist/ProjectChecklistPage.tsx, modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx</files>
  <read_first>
  - lib/documents/checklist-status.ts
  - lib/api-errors.ts (ValidationError body field singular)
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (row editor, status labels)
  - modules/weekly/ui/report/WeeklyReportForm.tsx
  </read_first>
  <behavior>
  - Status Select options use API values; visible labels match UI-SPEC (D-11).
  - Status approved shows approved_at date and approved_by; not_applicable shows na_reason; other statuses hide those controls (D-05).
  - PATCH 400 JSON { error, field: 'confluence_url' } renders that error under the Confluence Input plus toast Fix the highlighted field and try again. (D-13).
  - Save checklist item disabled while PATCH in-flight; 200 toasts Checklist item saved and reloads (UI-SPEC loading checklist-item-save).
  - Empty list copy matches UI-SPEC empty checklist-table.
  - Zero file inputs in the document (D-05).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend ProjectChecklistPage.component.test.tsx: empty array shows No checklist items yet; populated row shows status badge and Edit checklist item; expand editor; set status approved and assert approved_at and approved_by inputs appear; set not_applicable and assert na_reason; mock PATCH 400 { error: 'confluence_url must use https://', field: 'confluence_url' } and assert that message under the URL field and the validation toast. Mock PATCH 200 and assert Checklist item saved. Assert no input with type file. Commit test(23-03): red checklist PATCH editor.
  GREEN: Implement ChecklistItemRow plus patchItem on the hook. PATCH /api/projects/{id}/document-checklist/{itemId} (D-03). On 400 parse { error, field } singular (D-13) — do not read fields. Map field to the matching control. Status Select values are the five API strings (D-11). When sending approved, include approved_at, approved_by, and confluence_url; when not_applicable include na_reason; pending_approval include confluence_url. HTTPS Open in Confluence link when the URL starts with https://. No file picker. Primary Save checklist item bg-blue-600. Table overflow-x-auto. Commit feat(23-03): checklist PATCH editor.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx</automated>
  </verify>
  <done>PM can PATCH checklist items with API statuses and inline singular-field errors (DOC-08, D-05, D-11, D-13).</done>
  <acceptance_criteria>
  - PATCH 400 uses body.field not body.fields
  - Status option values are the five API enums
  - npx vitest run --project jsdom modules/documents/ui/checklist/ProjectChecklistPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-03-03">
  <name>Project hub Document checklist card</name>
  <files>app/projects/[id]/page.tsx, app/projects/[id]/page.checklist-card.test.ts</files>
  <read_first>
  - app/projects/[id]/page.tsx (QUICK_LINKS around lines 74-82 and render around 490-506)
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (project hub)
  </read_first>
  <behavior>
  - QUICK_LINKS includes label Document checklist, desc Complete Confluence evidence for this stage., href /document-checklist (D-15).
  - QUICK_LINKS still includes label Documents and href /documents (D-06).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits. Isolation none: this is the only Phase 23 plan that may edit app/projects/[id]/page.tsx.
  RED: Add app/projects/[id]/page.checklist-card.test.ts in the vitest node project. Read page.tsx via fs from process.cwd() and assert it contains label: 'Document checklist', desc: 'Complete Confluence evidence for this stage.', href: '/document-checklist', and still contains label: 'Documents' and href: '/documents'. Test fails until the card exists. Commit test(23-03): red hub checklist card.
  GREEN: Import ClipboardCheck from lucide-react. Append one QUICK_LINKS entry after Documents: href /document-checklist, icon ClipboardCheck, label Document checklist, desc Complete Confluence evidence for this stage. (D-15). Do not remove or change the Documents card (D-06). Do not restyle the quick-link grid. Do not add a global Sidebar checklist link. Commit feat(23-03): project hub document checklist card.
  </action>
  <verify>
    <automated>npx vitest run --project node app/projects/[id]/page.checklist-card.test.ts</automated>
  </verify>
  <done>Project hub exposes Document checklist beside the v1 Documents card (D-06, D-15).</done>
  <acceptance_criteria>
  - QUICK_LINKS has /document-checklist and still has /documents
  - npx vitest run --project node app/projects/[id]/page.checklist-card.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser editor → PATCH checklist item | Project write session; Confluence URL and status cross here |
| Hub card → checklist URL | UX only; withProjectAccess remains authz |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05 | Elevation of Privilege | PATCH checklist | high | mitigate | Rely on withProjectAccess plus assertProjectWriteAccess; 403 in-page; NAV is not policy (D-03, D-05, ASVS V4) |
| T-23-06 | Tampering | confluence_url field | medium | mitigate | Text input only; server parseHttpsUrl and rejectBinaryFields remain authoritative; no file picker (D-05) |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-03) |

ASVS L1: write path stays on existing project-access wrappers.
</threat_model>

<verification>
- 23-03-01: GET shell plus re-export green
- 23-03-02: PATCH editor plus D-13 field errors green
- 23-03-03: hub card green; Documents card still present
- Do not modify app/projects/[id]/documents
</verification>

<success_criteria>
A PM can complete a project's Confluence checklist in the UI (HTTPS link; Approved or Not applicable) and reach it from the project hub (DOC-08, D-05, D-06, D-11, D-13, D-15).
</success_criteria>

<output>
Create `.planning/phases/23-document-checklist-audit-viewer/23-03-SUMMARY.md` when done
</output>
