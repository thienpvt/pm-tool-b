---
phase: 23-document-checklist-audit-viewer
plan: 05
type: tdd
wave: 2
depends_on: [23-01]
files_modified:
  - modules/audit/ui/useAuditLog.ts
  - modules/audit/ui/AuditLogPage.tsx
  - modules/audit/ui/AuditFiltersBar.tsx
  - modules/audit/ui/AuditTable.tsx
  - modules/audit/ui/AuditLogPage.component.test.tsx
  - app/audit/page.tsx
autonomous: true
requirements: [AUDIT-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO opens /audit via a thin app re-export of AuditLogPage and sees title Audit log (AUDIT-02, D-01)."
    - "GET /api/audit with entity_type, entity_id, from, to, limit drives the table; no audit write endpoint is called (D-03, D-08)."
    - "Initial GET shows spinner and Loading audit log… (UI-SPEC loading audit-page-shell)."
    - "401/403/5xx use catalog Copywriting error strings (UI-SPEC error audit-page-shell, D-08)."
    - "Zero rows show No audit entries found and Broaden the date range or clear entity filters. (UI-SPEC empty audit-table)."
    - "Rows show locale-formatted created_at, actor_id, entity_type plus entity_id, action in text-xs (UI-SPEC populated audit-table)."
    - "Filter bar forwards any subset of entity_type, entity_id, from, to, limit; default limit 50; Select 50/100/200 (D-08, UI-SPEC partial audit-filter-bar)."
    - "Apply filters disabled while GET in-flight; helper Showing up to {limit} most recent entries. (UI-SPEC loading audit-filter-apply)."
    - "Expand toggle aria-label Show audit details reveals Before and After labels text-xs font-semibold and pretty-printed JSON in pre elements via JSON.stringify value, null, 2 as text children (D-08, D-10, UI-SPEC populated audit-row-expand, error audit-json-render)."
    - "Missing snapshots stringify as null; JSON panels max-h-60 overflow-auto whitespace-pre-wrap; large JSON scrolls inside the pre without page blowout (UI-SPEC overflow audit-table, audit-json-panel, long-text audit-json-panel)."
    - "Collapse hides the panels (UI-SPEC populated audit-row-expand)."
    - "When rows.length is greater than 100 and no row is expanded, use VirtualRows at ROW_HEIGHT 40; expanding a row renders detail for that row (D-09, UI-SPEC populated virtual-rows-audit)."
    - "There is no PATCH or DELETE control on the audit page (D-08)."
    - "Viewer/PM 403 in-page; no redirect (D-08)."
    - "No new npm virtualizer or JSON tree widget (D-09, D-10)."
  artifacts:
    - path: "app/audit/page.tsx"
      provides: "Thin use-client re-export (D-01)"
    - path: "modules/audit/ui/AuditLogPage.tsx"
      provides: "Audit shell, filters, table (AUDIT-02, D-08)"
    - path: "modules/audit/ui/AuditTable.tsx"
      provides: "Expandable before/after pre text plus VirtualRows gate (D-08, D-09, D-10)"
    - path: "modules/audit/ui/useAuditLog.ts"
      provides: "GET /api/audit with filters (D-03, D-08)"
  key_links:
    - from: "app/audit/page.tsx"
      to: "modules/audit/ui/AuditLogPage.tsx"
      via: "default re-export"
      pattern: "modules/audit/ui/AuditLogPage"
    - from: "useAuditLog.ts"
      to: "/api/audit"
      via: "GET entity_type entity_id from to limit"
      pattern: "/api/audit"
    - from: "AuditTable.tsx"
      to: "modules/weekly/ui/shared/VirtualRows.tsx"
      via: "import VirtualRows when length greater than 100"
      pattern: "VirtualRows"
  prohibitions:
    - statement: "Must not add PATCH or DELETE UI for /api/audit (D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add a JSON tree widget or npm virtualizer (D-09, D-10)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not modify VirtualRows.tsx"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit Sidebar.tsx or app/projects/[id]/page.tsx"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO can view the company-scoped audit log with filters and before/after snapshots as pretty-printed text, windowed above 100 collapsed rows (AUDIT-02, D-01, D-03, D-08, D-09, D-10).

Purpose: Close AUDIT-02 without audit write UI.

Output: Audit page, hook GET, filter bar, expandable table, thin re-export, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-document-checklist-audit-viewer/23-CONTEXT.md
@.planning/phases/23-document-checklist-audit-viewer/23-RESEARCH.md
@.planning/phases/23-document-checklist-audit-viewer/23-PATTERNS.md
@.planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md
@.planning/phases/23-document-checklist-audit-viewer/23-01-PLAN.md
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
@modules/weekly/ui/periods/useWeeklyPeriods.ts
@modules/weekly/ui/shared/VirtualRows.tsx
@app/api/audit/route.ts
@lib/services/audit.service.ts
@lib/repositories/audit.repo.ts
@modules/documents/ui/shared/types.ts
@modules/documents/ui/shared/documents.fixture.ts

Planner-assigned locked IDs: D-01 re-export /audit; D-03 existing GET only; D-08 filters plus expand JSON as pre text, no write UI, 403 in-page; D-09 VirtualRows when length greater than 100; D-10 pretty-printed text not a tree widget.

Isolation none: do not edit Sidebar.tsx, hub page, documents catalog/checklist/compliance files, or VirtualRows.tsx. Import AuditLogRow and audit fixtures from modules/documents/ui/shared (23-01). Import VirtualRows from weekly.

<interfaces>
GET /api/audit query: entity_type, entity_id, from, to, limit. withCpmo. Returns AuditLogRow array: id, company_id, actor_id, entity_type, entity_id string, action, before, after, created_at. Default limit 50, clamp 1-200 in audit.service.ts.

Copywriting: title Audit log; subtitle Append-only company audit trail; loading Loading audit log…; empty heading/body as UI-SPEC; Apply filters.

Expand: button aria-label Show audit details. Two-column grid on md: Before and After. Render JSON.stringify(value, null, 2) as the text children of a pre element with classes text-xs font-mono whitespace-pre-wrap overflow-auto max-h-60 bg-slate-50 p-2 rounded border. Missing snapshot: still JSON.stringify(null, null, 2) producing the null literal (UI-SPEC). Do not set HTML from the snapshot string.

No Collapsible shadcn primitive is installed. Use a button plus conditional panel (budget-page expand analog). Do not run shadcn add.

VirtualRows when rows.length greater than 100 and expandedId is null; ROW_HEIGHT 40; height 480. When a row is expanded, render that row's detail below it (or disable windowing while expanded).
</interfaces>
</context>

<feature>
  <name>CPMO audit log viewer with JSON expand and VirtualRows</name>
  <files>modules/audit/ui/AuditLogPage.tsx, app/audit/page.tsx</files>
  <behavior>
    GET 200 lists actor/time/entity/action; expand shows pretty JSON text in pre elements; 150 collapsed rows window (AUDIT-02, D-08, D-09, D-10).
  </behavior>
  <implementation>
    GET hook plus filters plus table; stringify into pre text; VirtualRows when length greater than 100 (D-03, D-08, D-09, D-10).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="23-05-01">
  <name>Audit GET shell, filters, and re-export</name>
  <precondition>modules/weekly/ui/shared/VirtualRows.tsx exists and exports ROW_HEIGHT (Phase 22 PERF-01).</precondition>
  <files>modules/audit/ui/useAuditLog.ts, modules/audit/ui/AuditFiltersBar.tsx, modules/audit/ui/AuditLogPage.tsx, modules/audit/ui/AuditLogPage.component.test.tsx, app/audit/page.tsx</files>
  <read_first>
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - app/api/audit/route.ts
  - lib/services/audit.service.ts
  - modules/documents/ui/shared/documents.fixture.ts
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (audit zones)
  </read_first>
  <behavior>
  - Deferred GET shows Loading audit log… (UI-SPEC loading audit-page-shell).
  - GET 200 heading Audit log and subtitle Append-only company audit trail (AUDIT-02, D-08).
  - GET 403 shows You don't have access to this page. (D-08).
  - Apply filters GETs /api/audit with entity_type, from, to, limit when set; default limit 50 (D-08).
  - Re-export at app/audit/page.tsx (D-01).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add AuditLogPage.component.test.tsx mocking usePathname /audit and Sidebar. Stub GET /api/audit with deferred, 200 auditRowsFixture, 403. Assert loading copy, title Audit log, 403 copy. Click Apply filters with entity_type document_catalog and limit 100; assert fetch URL includes those query keys. Commit test(23-05): red audit GET shell.
  GREEN: useAuditLog load GET /api/audit with URLSearchParams for provided filters only (D-03). Default limit 50. 401/403/load_failed; do not redirect. AuditLogPage Phase 22 shell. Title Audit log. AuditFiltersBar: Input entity_type, Input entity_id, date from, date to, Select limit 50/100/200, primary Apply filters bg-blue-600 disabled while in-flight. Helper Showing up to {limit} most recent entries. Thin re-export (D-01). GET only — no PATCH or DELETE fetch. Commit feat(23-05): audit GET shell and filters.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx</automated>
  </verify>
  <done>Audit GET path with filters renders through the module re-export (AUDIT-02, D-01, D-08).</done>
  <acceptance_criteria>
  - app/audit/page.tsx re-exports AuditLogPage
  - Fetch URL starts with /api/audit
  - npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-05-02">
  <name>Expand row pretty-printed JSON in pre text</name>
  <files>modules/audit/ui/AuditTable.tsx, modules/audit/ui/AuditLogPage.tsx, modules/audit/ui/AuditLogPage.component.test.tsx</files>
  <read_first>
  - app/projects/[id]/budget/page.tsx (expand toggle analog)
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (audit table expand)
  - modules/documents/ui/shared/documents.fixture.ts (auditRowsFixture)
  </read_first>
  <behavior>
  - Table columns Time, Actor, Entity, Action, Details (UI-SPEC populated audit-table).
  - Empty array shows No audit entries found and Broaden the date range or clear entity filters. (UI-SPEC empty audit-table).
  - Clicking Show audit details reveals Before and After; each pre element's textContent equals JSON.stringify of that snapshot with two-space indent (D-08, D-10).
  - Second click hides the panels (UI-SPEC populated audit-row-expand).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend tests: 200 fixture rows show actor_id and action. Empty array shows empty copy. Click the details button; assert two pre elements whose textContent equals JSON.stringify(row.before, null, 2) and JSON.stringify(row.after, null, 2). Click again and assert those pre elements are gone. Commit test(23-05): red audit expand JSON.
  GREEN: AuditTable compact Table overflow-x-auto. Expand via button aria-label Show audit details (chevron). Store expanded id in state. Detail panel: Before and After labels text-xs font-semibold; each snapshot is JSON.stringify(value, null, 2) passed as children of a pre element (D-10). Do not assign HTML strings onto the DOM. No PATCH or DELETE buttons (D-08). Format created_at with locale. Entity cell `{entity_type} #{entity_id}`. Commit feat(23-05): audit expand JSON pre text.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx</automated>
  </verify>
  <done>Expanding a row shows before/after as pre text from JSON.stringify (AUDIT-02, D-08, D-10).</done>
  <acceptance_criteria>
  - Expand test asserts pre textContent equals JSON.stringify with two-space indent
  - No audit mutation fetch in the hook
  - npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="23-05-03">
  <name>VirtualRows for audit lists above 100</name>
  <files>modules/audit/ui/AuditTable.tsx, modules/audit/ui/AuditLogPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/shared/VirtualRows.tsx
  - modules/documents/ui/shared/documents.fixture.ts (auditRows150)
  - .planning/phases/23-document-checklist-audit-viewer/23-UI-SPEC.md (virtual-rows-audit)
  </read_first>
  <behavior>
  - 150 collapsed rows: nodes with data-testid audit-row number at most 30 (D-09, UI-SPEC populated virtual-rows-audit).
  - Expanding a visible row still shows the two pre JSON panels (D-08).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Stub GET returning auditRows150. Assert getAllByTestId('audit-row').length is less than or equal to 30 and auditRows150.length is 150. Expand the first mounted details button and assert pre text is present. Commit test(23-05): red audit VirtualRows.
  GREEN: When rows.length greater than 100 and no row is expanded, render the body through VirtualRows height 480 rowHeight ROW_HEIGHT import from @/modules/weekly/ui/shared/VirtualRows; each renderRow root data-testid audit-row. When a row is expanded, keep that row's detail visible (render detail under the row or skip windowing while expanded per UI-SPEC). Do not copy VirtualRows. Do not edit VirtualRows.tsx. Do not add npm (D-09). Commit feat(23-05): audit VirtualRows gate.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx modules/weekly/ui/shared/VirtualRows.component.test.tsx</automated>
  </verify>
  <done>Audit lists over 100 collapsed rows use in-repo VirtualRows (AUDIT-02, D-09).</done>
  <acceptance_criteria>
  - AuditTable imports VirtualRows from the weekly shared module
  - 150-row test asserts at most 30 audit-row nodes
  - npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/audit | CPMO session; filter strings and JSON snapshots cross here |
| Snapshot display → DOM | before/after JSON must not execute as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08 | Tampering | AuditTable JSON panel | high | mitigate | Render JSON.stringify output as pre text children only (D-08, D-10, ASVS V5) |
| T-23-09 | Information Disclosure | AuditLogPage | high | mitigate | Consume GET /api/audit only; company scope stays in listAuditLogs; 403 in-page for non-CPMO (D-03, D-08, ASVS V4) |
| T-23-10 | Tampering | VirtualRows | low | accept | Window math is client UX only |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install (D-03, D-09) |

ASVS L1: XSS on snapshots is the primary client boundary; text rendering closes it.
</threat_model>

<verification>
- 23-05-01: GET plus filters green
- 23-05-02: expand JSON pre text green
- 23-05-03: VirtualRows green
- package.json unchanged; VirtualRows.tsx unmodified
</verification>

<success_criteria>
CPMO can view the company-scoped audit log with filters and before/after snapshots (AUDIT-02, D-08, D-09, D-10).
</success_criteria>

<output>
Create `.planning/phases/23-document-checklist-audit-viewer/23-05-SUMMARY.md` when done
</output>
