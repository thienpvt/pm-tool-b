---
phase: 25-kysely-repositories
plan: 09
type: tdd
wave: 9
depends_on: [25-08]
files_modified:
  - modules/weekly/backend/repositories/weekly-periods.repo.ts
  - modules/weekly/backend/repositories/weekly-periods.repo.test.ts
  - modules/weekly/backend/repositories/weekly-export.repo.ts
  - modules/weekly/backend/repositories/weekly-export.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 50000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "weekly-periods config/list/createPeriodWithShells use getKysely and still call runInTransaction (ENF-02, D-05, D-06, W8)."
    - "Inner period+shell writes join the 25-02 ALS so a thrown createPeriodWithShells rolls back (Pitfall 2, D-06)."
    - "weekly-export insertWeeklyExportLog uses getKysely (D-05)."
    - "weekly-reports.service.ts still calls runInTransaction — this plan does not edit services (D-06)."
  artifacts:
    - path: "modules/weekly/backend/repositories/weekly-periods.repo.ts"
      provides: "Kysely periods plus tx-backed createPeriodWithShells"
    - path: "modules/weekly/backend/repositories/weekly-export.repo.ts"
      provides: "Kysely export log"
  key_links:
    - from: "createPeriodWithShells"
      to: "runInTransaction"
      via: "withPgTransaction then getKysely ALS"
      pattern: "runInTransaction"
    - from: "weekly-periods.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not rewrite weekly-reports.service.ts to db.transaction().execute (D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not convert weekly-reports.repo.ts in this plan (25-10 owns that file)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert weekly-periods and weekly-export to getKysely using the 25-02 transaction bridge (ENF-02, D-05, D-06, W8).

Purpose: Period creation already uses runInTransaction; Kysely must join that BEGIN.

Output: converted periods + export repos; rollback coverage; services untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-RESEARCH.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/weekly/backend/repositories/weekly-periods.repo.ts
@modules/weekly/backend/repositories/weekly-periods.repo.test.ts
@modules/weekly/backend/repositories/weekly-export.repo.ts
@modules/weekly/backend/repositories/weekly-export.repo.test.ts
@lib/db.ts
@lib/db-tx.ts

Planner-assigned locked IDs: D-05 convert these repos; D-06 keep runInTransaction; D-02 getKysely ALS; D-07 same test pool.

Conversion contract: getKysely; keep withPgTransaction wrapping runInTransaction; TDD two commits test(25-09) then feat(25-09). Isolation none. Do not edit weekly-reports.repo.ts.

<interfaces>
getCompanyWeeklyConfig / upsertCompanyWeeklyConfig / listWeeklyPeriods: Kysely on company_weekly_config and weekly_periods.

listObligatedProjectIds: today optional PoolClient. After conversion, always getKysely() (ALS supplies the tx connection). If the function still accepts client for call-site compatibility, ignore it and use getKysely so D-06 service signatures that pass nothing stay valid. Prefer deleting the unused client param if the only caller is this file.

createPeriodWithShells: keep withPgTransaction. Inside, insert weekly_periods via getKysely (stringify config_snapshot). Call insertShell from weekly-reports.repo — 25-10 converts insertShell. Until then insertShell still takes a queryable client: pass the PoolClient argument as today so the dual path works, OR if insertShell is only called here, keep passing client for 25-10 to switch to getKysely. Do not change weekly-reports.service.ts.

weekly-export: insertWeeklyExportLog insertInto weekly_export_logs returning id.
</interfaces>
</context>

<feature>
  <name>Weekly periods and export on Kysely</name>
  <files>modules/weekly/backend/repositories/weekly-periods.repo.ts, modules/weekly/backend/repositories/weekly-export.repo.ts</files>
  <behavior>
    Existing weekly-periods and weekly-export tests pass via testKysely plus runInTransactionOnPool mock (D-05).
    Thrown createPeriodWithShells leaves no weekly_periods row (D-06).
  </behavior>
  <implementation>
    Use getKysely inside existing runInTransaction (25-PATTERNS weekly analog).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-09-01">
  <name>Convert weekly-periods.repo.ts</name>
  <files>modules/weekly/backend/repositories/weekly-periods.repo.ts, modules/weekly/backend/repositories/weekly-periods.repo.test.ts</files>
  <read_first>
  - modules/weekly/backend/repositories/weekly-periods.repo.ts
  - modules/weekly/backend/repositories/weekly-periods.repo.test.ts
  - lib/db-tx.ts
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (weekly analog)
  </read_first>
  <behavior>
  - Existing weekly-periods.repo.test.ts cases pass (D-05).
  - createPeriodWithShells that throws after insert leaves zero periods for that iso_week (D-06).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely to testKysely; keep runInTransaction mocked to runInTransactionOnPool(testPool()). Add rollback case if missing. Commit test(25-09): red weekly periods kysely.
  GREEN: Convert config/list/obligated/createPeriodWithShells to getKysely. Keep withPgTransaction. Do not edit weekly-reports.service.ts (D-06). Commit feat(25-09): weekly periods kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/repositories/weekly-periods.repo.test.ts</automated>
  </verify>
  <done>weekly-periods.repo.ts uses getKysely inside runInTransaction (W8, D-05, D-06).</done>
  <acceptance_criteria>
  - weekly-periods.repo.ts imports getKysely
  - withPgTransaction still calls runInTransaction
  - npx vitest run --project node modules/weekly/backend/repositories/weekly-periods.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-09-02">
  <name>Convert weekly-export.repo.ts</name>
  <files>modules/weekly/backend/repositories/weekly-export.repo.ts, modules/weekly/backend/repositories/weekly-export.repo.test.ts</files>
  <read_first>
  - modules/weekly/backend/repositories/weekly-export.repo.ts
  - modules/weekly/backend/repositories/weekly-export.repo.test.ts
  </read_first>
  <behavior>
  - Existing weekly-export.repo.test.ts cases pass via testKysely (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; add called assertion. Commit test(25-09): red weekly export kysely.
  GREEN: Convert insertWeeklyExportLog. Commit feat(25-09): weekly export kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/repositories/weekly-export.repo.test.ts modules/weekly/backend/repositories/weekly-periods.repo.test.ts</automated>
  </verify>
  <done>weekly-export.repo.ts uses getKysely (W8, D-05).</done>
  <acceptance_criteria>
  - weekly-export.repo.ts imports getKysely
  - npx vitest run --project node modules/weekly/backend/repositories/weekly-export.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createPeriodWithShells → weekly_periods + weekly_reports | Partial commit if Kysely leaves the transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-14 | Tampering | createPeriodWithShells | high | mitigate | getKysely inside runInTransaction ALS; rollback test (Pitfall 2, D-06) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withCpmo/withProjectAccess on weekly routes unchanged (D-06).
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/weekly/backend/repositories/weekly-periods.repo.test.ts modules/weekly/backend/repositories/weekly-export.repo.test.ts
</verification>

<success_criteria>
Weekly periods and export repos use getKysely; period creation still goes through runInTransaction (ENF-02, D-05, D-06, W8).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-09-SUMMARY.md` when done
</output>
