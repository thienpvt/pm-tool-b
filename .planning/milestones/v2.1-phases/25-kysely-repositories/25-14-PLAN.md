---
phase: 25-kysely-repositories
plan: 14
type: tdd
wave: 14
depends_on: [25-13]
files_modified:
  - modules/projects/backend/repositories/issues.repo.ts
  - modules/projects/backend/repositories/issues.repo.test.ts
  - modules/projects/backend/repositories/meetings.repo.ts
  - modules/projects/backend/repositories/meetings.repo.test.ts
  - modules/projects/backend/repositories/escalations.repo.ts
  - modules/projects/backend/repositories/escalations.repo.test.ts
  - modules/projects/backend/repositories/team.repo.ts
  - modules/projects/backend/repositories/team.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "updateIssue, updateMeeting, updateEscalation, and updateTeamMember call pickAllowed with their COLUMNS consts (ENF-02, D-04, W9b)."
    - "UnknownColumnError tests on these four files stay green (D-04)."
    - "ISSUE_COLUMNS / MEETING_COLUMNS / ESCALATION_COLUMNS / TEAM_COLUMNS still exclude project_id and id (D-04)."
    - "lib/http/with-auth.test.ts and project route access tests still map UnknownColumnError to 400 without code changes (D-06)."
  artifacts:
    - path: "modules/projects/backend/repositories/issues.repo.ts"
      provides: "Kysely issues plus pickAllowed"
    - path: "modules/projects/backend/repositories/team.repo.ts"
      provides: "Kysely team_members plus pickAllowed"
  key_links:
    - from: "updateIssue"
      to: "pickAllowed"
      via: "ISSUE_COLUMNS"
      pattern: "pickAllowed"
  prohibitions:
    - statement: "Must not edit withAuth or routes (D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not delete the SET-fragment helper yet (25-15 W10)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert issues, meetings, escalations, and team write repos to getKysely with pickAllowed (ENF-02, D-04, D-05, W9b). Isolated from cleanup.

Purpose: Finish the seven allowlist writers. HTTP 400 mapping stays on withAuth.

Output: four converted repos; mass-assignment tests green; 400 chain re-run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@lib/repositories/_kysely-helpers.ts
@modules/projects/backend/repositories/issues.repo.ts
@modules/projects/backend/repositories/meetings.repo.ts
@modules/projects/backend/repositories/escalations.repo.ts
@modules/projects/backend/repositories/team.repo.ts
@lib/http/with-auth.test.ts
@app/api/projects/[id]/route.access.test.ts

Planner-assigned locked IDs: D-04 pickAllowed; D-05 convert these four; D-06 re-run but do not edit wrappers; D-07 testKysely.

W9b isolated. TDD two commits test(25-14) then feat(25-14). Isolation none.

<interfaces>
Same write pattern as 25-13: pickAllowed(COLUMNS, fields); updateTable.set; where id and project_id; returningAll where the old SQL used RETURNING *.

issues.repo.ts RAID CASE/ORDER via sql tagged template like risks.

Do not add createEscalation (no API today).

Task 3 GREEN also runs with-auth and route.access tests without modifying those files (D-06).
</interfaces>
</context>

<feature>
  <name>W9b issues/meetings/escalations/team pickAllowed</name>
  <files>modules/projects/backend/repositories/issues.repo.ts, modules/projects/backend/repositories/team.repo.ts</files>
  <behavior>
    Unknown extra keys on updateIssue / updateTeamMember throw UnknownColumnError (D-04).
    withAuth still returns 400 for UnknownColumnError (D-06).
  </behavior>
  <implementation>
    pickAllowed then Kysely updates; re-run wrapper tests without editing them (D-06).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-14-01">
  <name>Convert issues.repo.ts with pickAllowed</name>
  <files>modules/projects/backend/repositories/issues.repo.ts, modules/projects/backend/repositories/issues.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/issues.repo.ts
  - modules/projects/backend/repositories/issues.repo.test.ts
  </read_first>
  <behavior>
  - Existing UnknownColumnError tests pass (D-04).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; retarget @/test imports. Commit test(25-14): red issues pickAllowed kysely.
  GREEN: Convert all exports. updateIssue uses pickAllowed(ISSUE_COLUMNS, fields). Commit feat(25-14): issues repo pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/issues.repo.test.ts</automated>
  </verify>
  <done>issues.repo.ts is on getKysely with pickAllowed (W9b, D-04, D-05).</done>
  <acceptance_criteria>
  - issues.repo.ts imports pickAllowed
  - npx vitest run --project node modules/projects/backend/repositories/issues.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-14-02">
  <name>Convert meetings.repo.ts with pickAllowed</name>
  <files>modules/projects/backend/repositories/meetings.repo.ts, modules/projects/backend/repositories/meetings.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/meetings.repo.ts
  - modules/projects/backend/repositories/meetings.repo.test.ts
  </read_first>
  <behavior>
  - Existing UnknownColumnError tests pass (D-04).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely. Commit test(25-14): red meetings pickAllowed kysely.
  GREEN: Convert list/create/update/delete. updateMeeting uses pickAllowed(MEETING_COLUMNS, fields). Commit feat(25-14): meetings repo pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/meetings.repo.test.ts</automated>
  </verify>
  <done>meetings.repo.ts is on getKysely with pickAllowed (W9b, D-04, D-05).</done>
  <acceptance_criteria>
  - meetings.repo.ts imports pickAllowed
  - npx vitest run --project node modules/projects/backend/repositories/meetings.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-14-03">
  <name>Convert escalations and team repos; re-run 400 chain</name>
  <files>modules/projects/backend/repositories/escalations.repo.ts, modules/projects/backend/repositories/escalations.repo.test.ts, modules/projects/backend/repositories/team.repo.ts, modules/projects/backend/repositories/team.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/escalations.repo.ts
  - modules/projects/backend/repositories/team.repo.ts
  - lib/http/with-auth.test.ts
  - lib/api-errors.ts
  </read_first>
  <behavior>
  - Existing escalations and team UnknownColumnError tests pass (D-04).
  - with-auth.test.ts and app/api/projects/[id]/route.access.test.ts still pass without edits (D-06).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely on both test files. Commit test(25-14): red escalations and team pickAllowed kysely.
  GREEN: Convert both files. updateEscalation uses pickAllowed(ESCALATION_COLUMNS); updateTeamMember uses pickAllowed(TEAM_COLUMNS). Do not add create/delete escalation. Do not edit with-auth.ts or route files. After GREEN tests, run the 400-chain files listed in verify. Commit feat(25-14): escalations and team pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/escalations.repo.test.ts modules/projects/backend/repositories/team.repo.test.ts lib/http/with-auth.test.ts app/api/projects/[id]/route.access.test.ts</automated>
  </verify>
  <done>Remaining W9b writers use pickAllowed; HTTP 400 mapping still works (D-04, D-06).</done>
  <acceptance_criteria>
  - escalations.repo.ts and team.repo.ts import pickAllowed
  - npx vitest run --project node modules/projects/backend/repositories/escalations.repo.test.ts modules/projects/backend/repositories/team.repo.test.ts lib/http/with-auth.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATCH body → UPDATE SET | Extra keys must not write tenant or parent ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-20 | Elevation of Privilege | updateIssue / updateTeamMember | high | mitigate | pickAllowed + existing UnknownColumnError tests (D-04) |
| T-25-21 | Elevation of Privilege | withAuth catch | high | accept | Mapping already in withAuth; this plan re-runs tests only (D-06) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: mass-assignment stays repo throw plus wrapper 400.
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/projects/backend/repositories/issues.repo.test.ts modules/projects/backend/repositories/meetings.repo.test.ts modules/projects/backend/repositories/escalations.repo.test.ts modules/projects/backend/repositories/team.repo.test.ts lib/http/with-auth.test.ts
</verification>

<success_criteria>
All seven allowlist write repos (this plan plus 25-13) use pickAllowed; UnknownColumnError still becomes HTTP 400 (ENF-02, D-04, W9b).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-14-SUMMARY.md` when done
</output>
