---
phase: 25-kysely-repositories
plan: 02
type: tdd
wave: 2
depends_on: [25-01]
files_modified:
  - lib/repositories/_kysely-helpers.ts
  - lib/repositories/_kysely-helpers.test.ts
  - lib/db-tx.ts
  - lib/db/kysely.ts
  - lib/db-tx.kysely.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "pickAllowed throws UnknownColumnError for keys outside the allowlist, names every unknown key, rejects an empty field set, and returns only allowlisted keys (ENF-02, D-04)."
    - "Write paths that accept a caller field map will use pickAllowed plus a Pick of Updateable columns matching today's COLUMNS consts — not a wholesale Updateable of the table (D-04)."
    - "runInTransactionOnPool after BEGIN stores a Kysely Database bound to that PoolClient; getKysely() inside the callback uses it so weekly waves join the same BEGIN (D-02, D-06)."
    - "A thrown callback still ROLLBACK; Kysely writes inside the callback are not visible after the throw (Pitfall 2)."
  artifacts:
    - path: "lib/repositories/_kysely-helpers.ts"
      provides: "pickAllowed runtime allowlist (D-04)"
      exports: ["pickAllowed"]
    - path: "lib/db-tx.ts"
      provides: "txKysely ALS write in runInTransactionOnPool"
      exports: ["txKyselyTarget"]
  key_links:
    - from: "pickAllowed"
      to: "UnknownColumnError"
      via: "throw on unknown keys"
      pattern: "UnknownColumnError"
    - from: "getKysely"
      to: "txKyselyTarget"
      via: "ALS first"
      pattern: "txKyselyTarget"
  prohibitions:
    - statement: "Must not delete buildUpdate yet (D-05 — W10 after all writers convert)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not rewrite weekly-reports.service.ts to Kysely native transactions (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Ship pickAllowed (runtime mass-assignment bridge) and the transactional Kysely ALS so weekly repos can join existing runInTransaction callbacks (ENF-02, D-04, D-06).

Purpose: Compile-time types do not replace runtime allowlists (D-04). Services already call runInTransaction; Kysely must see the same PoolClient.

Output: _kysely-helpers.ts, ALS write in runInTransactionOnPool, rollback proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-RESEARCH.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@.planning/phases/25-kysely-repositories/25-01-PLAN.md
@lib/repositories/_helpers.ts
@lib/repositories/_helpers.test.ts
@lib/db-tx.ts
@lib/db/kysely.ts
@lib/db.ts
@test/db.ts
@test/repo-db.ts

Planner-assigned locked IDs: D-04 pickAllowed plus UnknownColumnError; D-02 factory already checks txKyselyTarget; D-06 keep runInTransaction call sites; D-07 single test pool.

Do not convert weekly or projects repos. Do not remove the SET-fragment helper in _helpers.ts. Isolation none. TDD: two commits per task, test(25-02) then feat(25-02).

<interfaces>
pickAllowed(allowlist, fields): Object.keys(fields) not in allowlist throws UnknownColumnError(unknown). Allowlisted keys present in fields, emitted in allowlist order. Zero remaining columns throws UnknownColumnError([]). Falsy values (empty string) are kept. Import UnknownColumnError from _helpers.ts; do not duplicate the class.

Compile-time: callers use Pick of Updateable Database[table] keyed by typeof COLUMNS[number]. pickAllowed itself stays a runtime filter on Record string unknown.

tx bridge: after BEGIN inside runInTransactionOnPool, create an ephemeral Kysely Database whose PostgresDialect pool adapter connect() returns the active PoolClient with a no-op release (the outer finally still client.release()). Run fn inside txKyselyStore.run(txKysely, () => fn(client)). Nested with existing txStore.run so PostgresClient and Kysely join the same client. Do not call db.transaction().execute in services (D-06).

Rollback test: skipIf(!hasTestDb); setupRepoTables; runInTransactionOnPool(testPool(), async () => { await getKysely().insertInto('audit_logs').values({...}).execute(); throw new Error('boom'); }); expect list count unchanged.

getKysely already prefers txKyselyTarget() from 25-01; this plan must populate the store.
</interfaces>
</context>

<feature>
  <name>pickAllowed and transactional Kysely ALS</name>
  <files>lib/repositories/_kysely-helpers.ts, lib/db-tx.ts</files>
  <behavior>
    pickAllowed({company_id: 9}) against allowlist name/status throws UnknownColumnError (D-04).
    Thrown runInTransaction callback rolls back a Kysely insert (D-06).
  </behavior>
  <implementation>
    Mirror _helpers.test.ts cases onto pickAllowed; bind ephemeral Kysely to PoolClient in runInTransactionOnPool (25-PATTERNS Pattern 3–4).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-02-01">
  <name>pickAllowed mirrors UnknownColumnError semantics</name>
  <files>lib/repositories/_kysely-helpers.ts, lib/repositories/_kysely-helpers.test.ts</files>
  <read_first>
  - lib/repositories/_helpers.ts
  - lib/repositories/_helpers.test.ts
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (Pattern 3)
  </read_first>
  <behavior>
  - pickAllowed(['name','status','notes'], {name: 'x'}) returns {name: 'x'} (D-04).
  - Keys notes then name emit name then notes (allowlist order).
  - company_id throws UnknownColumnError; mixed known+unknown throws; empty object throws; columns array names every unknown key; notes: '' is kept (D-04).
  </behavior>
  <action>
  TDD. Two commits: test(25-02) then feat(25-02). Never RED-only.
  RED: Add _kysely-helpers.test.ts copying every case from _helpers.test.ts onto pickAllowed (same allowlist, same UnknownColumnError assertions, return shape is the picked object instead of sql/values). Tests fail because the module is missing. Commit test(25-02): red pickAllowed allowlist.
  GREEN: Implement pickAllowed in _kysely-helpers.ts per 25-PATTERNS Pattern 3. Reuse UnknownColumnError from _helpers.ts. Do not change _helpers.ts. Do not convert any *.repo.ts. Honor D-07. Commit feat(25-02): pickAllowed runtime allowlist.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/repositories/_kysely-helpers.test.ts lib/repositories/_helpers.test.ts</automated>
  </verify>
  <done>pickAllowed preserves UnknownColumnError semantics for later write repos (ENF-02, D-04).</done>
  <acceptance_criteria>
  - lib/repositories/_kysely-helpers.ts exports pickAllowed
  - npx vitest run --project node lib/repositories/_kysely-helpers.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-02-02">
  <name>Transactional Kysely joins runInTransactionOnPool</name>
  <files>lib/db-tx.ts, lib/db/kysely.ts, lib/db-tx.kysely.test.ts</files>
  <read_first>
  - lib/db-tx.ts
  - lib/db/kysely.ts
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (Pattern 4)
  - .planning/phases/25-kysely-repositories/25-RESEARCH.md (Pitfall 2)
  </read_first>
  <behavior>
  - Inside runInTransactionOnPool, getKysely() returns the ALS instance, not the process singleton (D-02).
  - Thrown callback: audit_logs row inserted via getKysely is absent after ROLLBACK (Pitfall 2).
  - Existing PostgresClient txQueryTarget behavior is unchanged (D-06).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Add lib/db-tx.kysely.test.ts skipIf(!hasTestDb). setupRepoTables; seed a company and actor. runInTransactionOnPool(testPool(), async () => { insert via (await getKysely()).insertInto('audit_logs')...; throw new Error('rollback-kysely'); }). Expect reject and SELECT count of that company audit_logs === 0. Tests fail until ALS is populated. Commit test(25-02): red kysely transaction rollback.
  GREEN: After BEGIN, construct ephemeral Kysely Database with PostgresDialect pool adapter that hands back the active PoolClient and no-op release. Enter txKyselyStore.run around fn, nested with existing txStore.run so both clients see the same connection (D-02, D-06). Do not replace service runInTransaction call sites. Do not convert weekly repos. Honor D-07 (same testPool). Commit feat(25-02): kysely ALS transaction bridge.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/db-tx.kysely.test.ts lib/repositories/_kysely-helpers.test.ts</automated>
  </verify>
  <done>Kysely queries inside runInTransactionOnPool share BEGIN/ROLLBACK with PostgresClient (D-06).</done>
  <acceptance_criteria>
  - lib/db-tx.ts populates txKyselyTarget inside runInTransactionOnPool
  - npx vitest run --project node lib/db-tx.kysely.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller field map → UPDATE SET | Untrusted JSON keys must not become column names |
| runInTransaction → connection | Mixed DbClient and Kysely must not split the transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Elevation of Privilege | pickAllowed | high | mitigate | UnknownColumnError on extra keys; no wholesale Updateable (D-04, ASVS V5) |
| T-25-04 | Tampering | lib/db-tx.ts | high | mitigate | ALS Kysely uses the BEGIN client so weekly writes cannot commit on a second connection |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withAuth still maps UnknownColumnError to 400; pickAllowed is the repo-layer control.
</threat_model>

<verification>
- 25-02-01: pickAllowed tests green; _helpers.test.ts still green
- 25-02-02: rollback test green
- Wave: npx vitest run --project node lib/repositories/_kysely-helpers.test.ts lib/db-tx.kysely.test.ts
</verification>

<success_criteria>
Runtime allowlists throw UnknownColumnError; Kysely inside runInTransactionOnPool rolls back with the callback (ENF-02, D-04, D-06).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-02-SUMMARY.md` when done
</output>
