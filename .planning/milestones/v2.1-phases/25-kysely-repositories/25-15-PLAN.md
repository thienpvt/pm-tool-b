---
phase: 25-kysely-repositories
plan: 15
type: tdd
wave: 15
depends_on: [25-14]
files_modified:
  - lib/repositories/_helpers.ts
  - lib/repositories/_helpers.test.ts
  - lib/repositories/kysely-migration.gate.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No production *.repo.ts under modules/ or lib/repositories/ imports getDb except files that are not repositories (ENF-02, D-05, W10)."
    - "The SET-fragment helper is gone from _helpers.ts once zero repository callers remain; UnknownColumnError class remains (D-04, D-05)."
    - "package.json still pins kysely 0.29.5 and kysely-codegen 0.20.0 (D-09)."
    - "lib/auth.ts session SQL and migrate/seed SQL stay on DbClient (D-05)."
  artifacts:
    - path: "lib/repositories/_helpers.ts"
      provides: "UnknownColumnError only (D-04)"
    - path: "lib/repositories/kysely-migration.gate.test.ts"
      provides: "Repo migration grep gates (D-05)"
  key_links:
    - from: "pickAllowed"
      to: "UnknownColumnError"
      via: "still imported from _helpers.ts"
      pattern: "UnknownColumnError"
  prohibitions:
    - statement: "Must not convert lib/auth.ts session SQL (D-05)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not remove UnknownColumnError (D-04)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Remove the unused SET-fragment helper and prove every production repository uses getKysely (ENF-02, D-05, D-09, W10).

Purpose: Dual-path SQL in repos is the Pitfall 4 failure mode; cleanup only after W9b writers moved.

Output: gate test, _helpers.ts without the SET helper, UnknownColumnError kept.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@lib/repositories/_helpers.ts
@lib/repositories/_helpers.test.ts
@lib/repositories/_kysely-helpers.ts
@lib/auth.ts
@package.json

Planner-assigned locked IDs: D-05 delete SET helper only when writers are done; D-04 keep UnknownColumnError; D-09 pins; D-07 no second pool.

TDD two commits test(25-15) then feat(25-15). Isolation none.

<interfaces>
Gate test lib/repositories/kysely-migration.gate.test.ts reads the filesystem:

1. Glob modules/**/backend/repositories/*.repo.ts and lib/repositories/*.repo.ts (exclude _helpers and _kysely-helpers). Each file source must include getKysely and must not import a getDb binding from '@/lib/db'. Scanning comments: ignore lines whose trimmed form starts with // or *.

2. Zero production *.repo.ts files import the SET SQL builder still exported from _helpers.ts at plan time (the function _helpers.test.ts currently tests besides UnknownColumnError).

3. package.json dependencies.kysely === '0.29.5'.

4. lib/auth.ts still contains session SQL using getDb (leave it). Assert lib/auth.ts still imports getDb so D-05 is not violated by over-conversion.

After GREEN: _helpers.ts exports UnknownColumnError only. Move former SET-helper unit cases out — they already live on pickAllowed in _kysely-helpers.test.ts. _helpers.test.ts covers UnknownColumnError message and columns only.

Do not edit migrate runner or seedAuthData.
</interfaces>
</context>

<feature>
  <name>Repository Kysely cleanup gates</name>
  <files>lib/repositories/kysely-migration.gate.test.ts, lib/repositories/_helpers.ts</files>
  <behavior>
    Every production *.repo.ts source includes getKysely (D-05).
    _helpers.ts exports UnknownColumnError and does not export a SET-fragment builder (D-05).
  </behavior>
  <implementation>
    Add gate tests RED; remove unused helper GREEN after confirming zero callers (D-05).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-15-01">
  <name>Gate test: all production repos use getKysely</name>
  <files>lib/repositories/kysely-migration.gate.test.ts</files>
  <read_first>
  - lib/repositories/_helpers.ts
  - package.json
  - lib/auth.ts
  </read_first>
  <behavior>
  - Gate fails if any production *.repo.ts omits getKysely (D-05).
  - Gate fails if kysely version is not 0.29.5 (D-09).
  - lib/auth.ts is still required to import getDb (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Write kysely-migration.gate.test.ts with the four assertions in interfaces. If W9b completed, RED may already pass for getKysely — then add the SET-helper caller count === 0 assertion which fails until GREEN removes the helper and its imports. If callers are already zero, RED is the version pin plus auth.ts assertion plus getKysely glob. Commit test(25-15): red kysely repo migration gate.
  GREEN: Do not convert more repos in this task; only make the gate file complete and passing against current tree. If the SET-helper still has callers, stop and report — W9b is incomplete. Commit feat(25-15): kysely repo migration gate.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/repositories/kysely-migration.gate.test.ts</automated>
  </verify>
  <done>Gate test proves production repos use getKysely and pin 0.29.5 (D-05, D-09).</done>
  <acceptance_criteria>
  - lib/repositories/kysely-migration.gate.test.ts exists
  - npx vitest run --project node lib/repositories/kysely-migration.gate.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-15-02">
  <name>Remove unused SET-fragment helper; keep UnknownColumnError</name>
  <files>lib/repositories/_helpers.ts, lib/repositories/_helpers.test.ts, lib/repositories/kysely-migration.gate.test.ts</files>
  <read_first>
  - lib/repositories/_helpers.ts
  - lib/repositories/_helpers.test.ts
  - lib/repositories/_kysely-helpers.test.ts
  </read_first>
  <behavior>
  - UnknownColumnError still has columns[] and the same messages (D-04).
  - _helpers.ts no longer exports a SET-fragment builder (D-05).
  - pickAllowed tests remain the allowlist proof (D-04).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Change _helpers.test.ts to assert UnknownColumnError only (message for unknown keys and empty set). Add gate assertion that _helpers.ts source does not export the old SET-fragment function name. Tests fail until GREEN deletes it. Commit test(25-15): red helpers UnknownColumnError only.
  GREEN: Delete the SET-fragment function from _helpers.ts. Keep UnknownColumnError. Confirm no remaining imports of that function under modules/ or lib/repositories/*.repo.ts. Do not edit lib/auth.ts. Commit feat(25-15): remove unused SET helper keep UnknownColumnError.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/repositories/_helpers.test.ts lib/repositories/_kysely-helpers.test.ts lib/repositories/kysely-migration.gate.test.ts</automated>
  </verify>
  <done>SET-fragment helper removed; UnknownColumnError and pickAllowed remain (D-04, D-05, W10).</done>
  <acceptance_criteria>
  - _helpers.ts exports UnknownColumnError
  - npx vitest run --project node lib/repositories/_helpers.test.ts lib/repositories/kysely-migration.gate.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo glob gate → filesystem | Ensures no raw DbClient path remains in repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-22 | Tampering | leftover DbClient SQL in a repo | high | mitigate | Gate test requires getKysely in every production *.repo.ts (D-05) |
| T-25-23 | Elevation of Privilege | removing UnknownColumnError | critical | mitigate | Keep the class; pickAllowed still throws it (D-04) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: mass-assignment control remains pickAllowed plus wrapper 400.
</threat_model>

<verification>
- Wave: npx vitest run --project node lib/repositories/kysely-migration.gate.test.ts lib/repositories/_helpers.test.ts lib/repositories/_kysely-helpers.test.ts
- Confirm lib/auth.ts still uses getDb
</verification>

<success_criteria>
All production repositories use getKysely; the unused SET helper is gone; UnknownColumnError remains; kysely stays 0.29.5 (ENF-02, D-04, D-05, D-09, W10).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-15-SUMMARY.md` when done
</output>
