---
phase: 25-kysely-repositories
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/db.ts
  - lib/db/kysely.ts
  - lib/db/database.ts
  - lib/db/kysely.test.ts
  - lib/db-tx.ts
  - test/repo-db.ts
  - modules/audit/backend/repositories/audit.repo.ts
  - modules/audit/backend/repositories/audit.repo.test.ts
autonomous: false
requirements: [ENF-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "kysely is pinned at 0.29.5 in dependencies and kysely-codegen at 0.20.0 in devDependencies after the legitimacy gate (ENF-02, D-01, D-09)."
    - "getPool() awaits getDb() then returns the same pg.Pool singleton; getKysely() builds one Kysely<Database> via PostgresDialect on that pool (D-01, D-02)."
    - "lib/db/database.ts is checked into git and names every table the 40 production repos query, matching migrations/0001-baseline-schema.sql (D-03)."
    - "insertAuditLog and listAuditLogs run through getKysely; audit.repo.test.ts stays green on the existing test pool (D-05, D-08)."
    - "testKysely() wraps testPool() — not a second Pool (D-07)."
    - "getDb() still returns DbClient on the same singleton Pool (D-01, D-08)."
  artifacts:
    - path: "lib/db/kysely.ts"
      provides: "getKysely() lazy singleton (D-02)"
      exports: ["getKysely"]
    - path: "lib/db/database.ts"
      provides: "Database interface (D-03)"
    - path: "lib/db.ts"
      provides: "getPool() after getDb init (D-01)"
      exports: ["getPool"]
    - path: "test/repo-db.ts"
      provides: "testKysely() on testPool (D-07)"
      exports: ["testKysely"]
    - path: "modules/audit/backend/repositories/audit.repo.ts"
      provides: "Kysely audit tracer (D-08)"
  key_links:
    - from: "lib/db/kysely.ts"
      to: "lib/db.ts"
      via: "getPool after getDb"
      pattern: "getPool"
    - from: "modules/audit/backend/repositories/audit.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
    - from: "test/repo-db.ts"
      to: "test/db.ts"
      via: "testPool into PostgresDialect"
      pattern: "testKysely"
  prohibitions:
    - statement: "Must not add Prisma, Drizzle, or a second connection pool (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not rewrite services, routes, UI, ENF-01 wrappers, or lib/auth.ts session SQL (D-05, D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not silence Kysely with type-erasing casts in repos (D-07)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Prove Kysely on the existing pg.Pool end-to-end: legitimacy-gated install, Database types, getPool/getKysely factory, and the audit repository tracer (ENF-02, D-01, D-02, D-03, D-05, D-07, D-08, D-09).

Purpose: Lock the single-pool architecture before sequential module waves convert the remaining 39 repos.

Output: kysely@0.29.5, kysely-codegen@0.20.0, lib/db/{kysely,database}.ts, getPool, testKysely, converted audit.repo.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-RESEARCH.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@lib/db.ts
@lib/db-tx.ts
@test/repo-db.ts
@test/db.ts
@modules/audit/backend/repositories/audit.repo.ts
@modules/audit/backend/repositories/audit.repo.test.ts
@migrations/0001-baseline-schema.sql
@package.json

Planner-assigned locked IDs: D-01 shared Pool + PostgresDialect; D-02 one factory after getDb; D-03 Database types; D-05 convert all repos starting with audit; D-07 no silencing casts and no second test pool; D-08 tracer; D-09 pin 0.29.5 / 0.20.0.

Do not convert other repos. Do not implement pickAllowed (25-02). Do not store a transactional Kysely inside runInTransactionOnPool (25-02). Isolation none. TDD: two commits per task, test(25-01) then feat(25-01). Never a RED-only task.

<interfaces>
getDb() in lib/db.ts creates one Pool, assertMigrated, assigns _pool and _client, seeds. Export:

export async function getPool(): Promise<Pool> {
  await getDb();
  if (!_pool) throw new Error('Database pool is not initialized');
  return _pool;
}

getKysely() in lib/db/kysely.ts: if txKyselyTarget() is set return it; else lazy-singleton Kysely<Database>({ dialect: new PostgresDialect({ pool: await getPool() }) }). Repos must not construct Pool or Kysely.

txKyselyTarget(): this plan adds AsyncLocalStorage<Kysely<Database>> plus getter returning the store (undefined outside a transaction). 25-02 writes the store inside runInTransactionOnPool.

package.json scripts.codegen:db = npx kysely-codegen --out-file lib/db/database.ts

Database: prefer kysely-codegen against a migrated DATABASE_URL; if codegen cannot run, hand-author from migrations/0001-baseline-schema.sql. Include every table the 40 production *.repo.ts files touch. JSONB columns as Json | null. Generated<T> for serial ids. Do not register a global int8 parser.

testKysely(): new Kysely<Database>({ dialect: new PostgresDialect({ pool: testPool() }) }) cached on the module, same pool as testDb().

Audit tracer (25-PATTERNS Pattern 5): insertInto('audit_logs').values({... stringify before/after ...}).execute(); selectFrom with optional filters, orderBy created_at desc, orderBy id desc, limit. Keep exported types. JSONB via stringify, not a global parser.

Vitest: npx vitest run --project node (no -x). Isolation none.
</interfaces>
</context>

<feature>
  <name>Kysely factory plus audit tracer</name>
  <files>lib/db/kysely.ts, lib/db/database.ts, modules/audit/backend/repositories/audit.repo.ts</files>
  <behavior>
    getKysely dialect uses getPool (D-01, D-02).
    insertAuditLog then listAuditLogs returns the row on the test pool (D-08).
  </behavior>
  <implementation>
    Pin kysely@0.29.5 after the legitimacy checkpoint; factory on existing Pool; convert audit.repo.ts only (D-08, D-09).
  </implementation>
</feature>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Confirm kysely@0.29.5 on npmjs.com (D-09)</name>
  <files>package.json</files>
  <action>
  RESEARCH.md Package Legitimacy Audit flags kysely as SUS (too-new, published 2026-08-10) with disposition Flagged. Do not run npm install until the human confirms. kysely-codegen@0.20.0 is OK and waits for the same GREEN install task. D-09 accepts the seam flag for the pinned versions.
  </action>
  <what-built>Nothing installed yet. Phase 25 needs kysely@0.29.5 (runtime) and kysely-codegen@0.20.0 (devDependency) per D-01 and D-09.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/kysely and confirm version 0.29.5, publisher kysely / kysely-org, repository github.com/kysely-org/kysely.
    2. Confirm scripts.postinstall is empty (RESEARCH.md already recorded empty postinstall from npm view).
    3. Confirm https://www.npmjs.com/package/kysely-codegen version 0.20.0 is the intended codegen pin (verdict OK).
    4. Type approved to install exactly those two versions. Type reject to stop the phase.
  </how-to-verify>
  <resume-signal>Type approved to continue 25-01-02, or describe why the pin is refused</resume-signal>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.dependencies && p.dependencies.kysely) { console.error('kysely already in dependencies before checkpoint'); process.exit(1); } console.log('kysely not installed yet');"</automated>
  </verify>
  <done>Human confirmed kysely@0.29.5 (D-09). package.json still has no kysely until the next task GREEN.</done>
</task>

<task type="auto" tdd="true" id="25-01-02">
  <name>Factory: Database types, getPool, getKysely</name>
  <reversibility rating="costly">Kysely Database on the getDb Pool becomes the repository query contract for the rest of Phase 25 (D-01, D-02, D-09).</reversibility>
  <files>package.json, package-lock.json, lib/db.ts, lib/db/kysely.ts, lib/db/database.ts, lib/db/kysely.test.ts, lib/db-tx.ts</files>
  <read_first>
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (factory, getPool, Database)
  - .planning/phases/25-kysely-repositories/25-RESEARCH.md (Pattern 1–2, Package Legitimacy, Pitfall 3)
  - lib/db.ts (singleton _pool / getDb)
  - lib/db-tx.ts
  - migrations/0001-baseline-schema.sql
  - package.json
  </read_first>
  <behavior>
  - package.json dependencies.kysely is exactly 0.29.5; devDependencies kysely-codegen is exactly 0.20.0; scripts.codegen:db runs kysely-codegen --out-file lib/db/database.ts (D-01, D-09).
  - lib/db/kysely.ts source contains getPool( and PostgresDialect and does not construct a Pool (D-01, D-02).
  - lib/db.ts exports getPool (D-01).
  - lib/db/kysely.test.ts: factory source contract plus testKysely-equivalent dialect on testPool can selectFrom('audit_logs') when hasTestDb (D-07, D-08).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Merge RED and GREEN. Two commits: test(25-01) then feat(25-01). Never RED-only.
  RED: Add lib/db/kysely.test.ts. Assert package.json dependencies.kysely === 0.29.5 and devDependencies['kysely-codegen'] === 0.20.0 and scripts['codegen:db'] includes kysely-codegen and lib/db/database.ts. Read lib/db.ts as text and expect export async function getPool. Read lib/db/kysely.ts as text and expect getPool, PostgresDialect, and txKyselyTarget. describe.skipIf(!hasTestDb) case: new Kysely with PostgresDialect({ pool: testPool() }) selectFrom('audit_logs').select('id').limit(1).execute() resolves. Tests fail because files and packages are missing. Commit test(25-01): red kysely factory and Database types.
  GREEN: After checkpoint approval only: npm install kysely@0.29.5 and npm install -D kysely-codegen@0.20.0 (D-09). Add scripts.codegen:db. Export getPool from lib/db.ts per interfaces (D-01). Add txKyselyTarget getter plus its AsyncLocalStorage in lib/db-tx.ts (store stays empty until 25-02). Create lib/db/kysely.ts getKysely per 25-PATTERNS (D-02). Create lib/db/database.ts via codegen:db if a migrated DATABASE_URL is available; otherwise hand-author table interfaces from migrations/0001-baseline-schema.sql covering every table the 40 production repos query (D-03). Check database.ts into git. Do not register a global int8 parser. Do not import Prisma or Drizzle. Honor D-07 on Kysely call sites. Commit feat(25-01): kysely factory on existing pool.
  REFACTOR only if tests stay green.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/db/kysely.test.ts</automated>
  </verify>
  <done>kysely@0.29.5 and getKysely share the getDb Pool; Database types are in git (ENF-02, D-01, D-02, D-03, D-09).</done>
  <acceptance_criteria>
  - package.json dependencies.kysely is 0.29.5
  - lib/db.ts exports getPool
  - lib/db/kysely.ts calls getPool and PostgresDialect
  - npx vitest run --project node lib/db/kysely.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="tracer" tdd="true" id="25-01-03">
  <name>End-to-end audit repo through getKysely</name>
  <files>test/repo-db.ts, modules/audit/backend/repositories/audit.repo.ts, modules/audit/backend/repositories/audit.repo.test.ts</files>
  <read_first>
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (Pattern 5 tracer, test harness)
  - modules/audit/backend/repositories/audit.repo.ts
  - modules/audit/backend/repositories/audit.repo.test.ts
  - test/repo-db.ts
  - lib/db/kysely.ts from 25-01-02
  </read_first>
  <behavior>
  - insertAuditLog then listAuditLogs still returns both rows for the same entity_id (existing D-07 append-only case) (D-08).
  - audit.repo.test.ts mocks @/lib/db/kysely getKysely to testKysely(); existing getDb mock may remain for other imports (D-07).
  - audit.repo.ts imports getKysely from @/lib/db/kysely (D-02, D-05).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits. Never RED-only.
  RED: Export testKysely from test/repo-db.ts (implementation can throw until GREEN). In audit.repo.test.ts add vi.mock of @/lib/db/kysely returning getKysely as a vi.fn that resolves to testKysely(). Add it('loads via getKysely') that imports @/lib/db/kysely after the mock and expects getKysely toHaveBeenCalled after listAuditLogs. Existing append-only test remains. Tests fail until the repo calls getKysely. Commit test(25-01): red audit kysely tracer.
  GREEN: Implement testKysely as a module-cached Kysely Database whose PostgresDialect pool is testPool() (D-07). Convert insertAuditLog and listAuditLogs to getKysely: insertInto audit_logs with stringify on before/after; selectFrom with the same filters, order, and limit as today (D-05, D-08). Keep exported types. Do not change services or routes (D-06). Do not convert other repos. Honor D-07. Commit feat(25-01): audit repo kysely tracer.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/audit/backend/repositories/audit.repo.test.ts lib/db/kysely.test.ts</automated>
  </verify>
  <done>Audit insert and list run through getKysely on the existing test pool (ENF-02, D-08).</done>
  <acceptance_criteria>
  - audit.repo.ts imports getKysely
  - test/repo-db.ts exports testKysely
  - npx vitest run --project node modules/audit/backend/repositories/audit.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → PostgreSQL | SQL identifiers and values cross here; Kysely quotes identifiers |
| npm registry → package.json | kysely install crosses a supply-chain boundary (T-25-SC) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | lib/db/kysely.ts | high | mitigate | PostgresDialect uses getPool only; no second Pool (D-01, D-02) |
| T-25-02 | Information Disclosure | audit.repo.ts | medium | mitigate | listAuditLogs remains company_id scoped (D-05, D-06) |
| T-25-SC | Tampering | npm install kysely | high | mitigate | blocking-human legitimacy checkpoint before install; pin 0.29.5 (D-09) |

ASVS L1: mass-assignment HTTP mapping stays on existing withAuth; this plan does not add write allowlists (audit is append-only).
</threat_model>

<verification>
- 25-01-01: human approved kysely@0.29.5
- 25-01-02: lib/db/kysely.test.ts green; pins present
- 25-01-03: audit.repo.test.ts green on testKysely
- Wave: npx vitest run --project node lib/db/kysely.test.ts modules/audit/backend/repositories/audit.repo.test.ts
</verification>

<success_criteria>
One Pool still backs getDb and getKysely; audit insert/list is typed Kysely; kysely@0.29.5 is installed after human confirmation (ENF-02, D-01, D-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-01-SUMMARY.md` when done
</output>
