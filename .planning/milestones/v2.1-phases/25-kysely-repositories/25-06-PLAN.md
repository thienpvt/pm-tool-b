---
phase: 25-kysely-repositories
plan: 06
type: tdd
wave: 6
depends_on: [25-05]
files_modified:
  - modules/jira/backend/repositories/import-mapping.repo.ts
  - modules/jira/backend/repositories/import-mapping.repo.test.ts
  - modules/operations/backend/repositories/operations.repo.ts
  - modules/operations/backend/repositories/operations.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "import-mapping timeline/bug mapping CRUD uses getKysely and stays company-scoped (ENF-02, D-05, W5)."
    - "operations systems/budget/expenses/incidents CRUD uses getKysely (D-05, W6)."
    - "Existing import-mapping and operations repo tests stay green (D-07)."
    - "Operations routes do not gain withCpmo (D-06)."
  artifacts:
    - path: "modules/jira/backend/repositories/import-mapping.repo.ts"
      provides: "Kysely import mappings (W5)"
    - path: "modules/operations/backend/repositories/operations.repo.ts"
      provides: "Kysely operations (W6)"
  key_links:
    - from: "import-mapping.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not add withCpmo to operations or change Jira search logging (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert jira import-mapping and operations repositories to getKysely (ENF-02, D-05, W5+W6).

Purpose: Small remaining non-projects modules share one wave.

Output: two converted repos; existing tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/jira/backend/repositories/import-mapping.repo.ts
@modules/jira/backend/repositories/import-mapping.repo.test.ts
@modules/operations/backend/repositories/operations.repo.ts
@modules/operations/backend/repositories/operations.repo.test.ts

Planner-assigned locked IDs: D-05 convert these repos; D-06 no withCpmo on operations; D-02 getKysely; D-07 testKysely.

Conversion contract: getKysely; keep signatures; mock testKysely; TDD two commits test(25-06) then feat(25-06). Isolation none.

<interfaces>
import-mapping: timeline_import_mappings, bug_import_mappings, plus any jql helpers already in this file. Preserve UNIQUE (company_id, name) conflict behavior.

operations.repo: operations_systems, operations_budget_items, operations_expenses, operations_incidents. Preserve isAdmin vs companyId list filter on systems.
</interfaces>
</context>

<feature>
  <name>Jira mapping and operations repos on Kysely</name>
  <files>modules/jira/backend/repositories/import-mapping.repo.ts, modules/operations/backend/repositories/operations.repo.ts</files>
  <behavior>
    Existing import-mapping and operations tests pass via testKysely (D-05).
  </behavior>
  <implementation>
    Convert SQL to Kysely; do not edit operations/jira routes (D-06).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-06-01">
  <name>Convert import-mapping.repo.ts</name>
  <files>modules/jira/backend/repositories/import-mapping.repo.ts, modules/jira/backend/repositories/import-mapping.repo.test.ts</files>
  <read_first>
  - modules/jira/backend/repositories/import-mapping.repo.ts
  - modules/jira/backend/repositories/import-mapping.repo.test.ts
  </read_first>
  <behavior>
  - Existing mapping tests pass via testKysely (D-05).
  - getKysely is called (D-02).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; add called assertion. Commit test(25-06): red import-mapping kysely.
  GREEN: Convert all exports. Keep company_id on mappings. Commit feat(25-06): import-mapping kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/jira/backend/repositories/import-mapping.repo.test.ts</automated>
  </verify>
  <done>import-mapping.repo.ts uses getKysely (W5, D-05).</done>
  <acceptance_criteria>
  - import-mapping.repo.ts imports getKysely
  - npx vitest run --project node modules/jira/backend/repositories/import-mapping.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-06-02">
  <name>Convert operations.repo.ts</name>
  <files>modules/operations/backend/repositories/operations.repo.ts, modules/operations/backend/repositories/operations.repo.test.ts</files>
  <read_first>
  - modules/operations/backend/repositories/operations.repo.ts
  - modules/operations/backend/repositories/operations.repo.test.ts
  </read_first>
  <behavior>
  - Existing operations.repo.test.ts cases pass via testKysely (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; add called assertion. Commit test(25-06): red operations kysely.
  GREEN: Convert all system/budget/expense/incident functions. Preserve isAdmin list filter. Do not edit operations routes or add withCpmo (D-06). Commit feat(25-06): operations repo kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/operations/backend/repositories/operations.repo.test.ts</automated>
  </verify>
  <done>operations.repo.ts uses getKysely (W6, D-05).</done>
  <acceptance_criteria>
  - operations.repo.ts imports getKysely
  - npx vitest run --project node modules/operations/backend/repositories/operations.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import mapping → company_id | Tenant isolation on mapping tables |
| Operations list → isAdmin | Platform vs tenant systems |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-10 | Elevation of Privilege | operations.repo.ts | high | mitigate | Preserve isAdmin vs companyId filter (D-06) |
| T-25-11 | Tampering | import-mapping.repo.ts | medium | mitigate | Writes stay company-scoped (D-05) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: D-23 operations wrappers unchanged (D-06).
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/jira/backend/repositories/import-mapping.repo.test.ts modules/operations/backend/repositories/operations.repo.test.ts
</verification>

<success_criteria>
Jira import-mapping and operations repos query through getKysely (ENF-02, D-05, W5+W6).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-06-SUMMARY.md` when done
</output>
