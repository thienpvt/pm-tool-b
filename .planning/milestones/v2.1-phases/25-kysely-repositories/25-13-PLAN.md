---
phase: 25-kysely-repositories
plan: 13
type: tdd
wave: 13
depends_on: [25-12]
files_modified:
  - modules/projects/backend/repositories/projects.repo.ts
  - modules/projects/backend/repositories/projects.repo.test.ts
  - modules/projects/backend/repositories/activities.repo.ts
  - modules/projects/backend/repositories/activities.repo.test.ts
  - modules/projects/backend/repositories/risks.repo.ts
  - modules/projects/backend/repositories/risks.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "updateProject, updateActivity, and updateRisk call pickAllowed with the existing COLUMNS consts then updateTable.set (ENF-02, D-04, W9b)."
    - "PROJECT_COLUMNS still omits company_id, customer_id, and id (D-04)."
    - "UnknownColumnError tests still reject extra keys and leave the row unchanged (D-04)."
    - "Reads and other writes in these three files use getKysely (D-05)."
  artifacts:
    - path: "modules/projects/backend/repositories/projects.repo.ts"
      provides: "Kysely projects plus pickAllowed update"
    - path: "modules/projects/backend/repositories/activities.repo.ts"
      provides: "Kysely activities plus pickAllowed update"
    - path: "modules/projects/backend/repositories/risks.repo.ts"
      provides: "Kysely risks plus pickAllowed update"
  key_links:
    - from: "updateProject"
      to: "pickAllowed"
      via: "PROJECT_COLUMNS"
      pattern: "pickAllowed"
    - from: "pickAllowed"
      to: "UnknownColumnError"
      via: "throw"
      pattern: "UnknownColumnError"
  prohibitions:
    - statement: "Must not pass wholesale Updateable of the table into .set (D-04)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit withAuth or project routes (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert projects, activities, and risks write repos to getKysely with pickAllowed (ENF-02, D-04, D-05, W9b). Isolated from other work.

Purpose: Runtime mass-assignment must survive Kysely (Pitfall 3).

Output: three converted repos; UnknownColumnError tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-RESEARCH.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@lib/repositories/_kysely-helpers.ts
@lib/repositories/_helpers.ts
@modules/projects/backend/repositories/projects.repo.ts
@modules/projects/backend/repositories/projects.repo.test.ts
@modules/projects/backend/repositories/activities.repo.ts
@modules/projects/backend/repositories/activities.repo.test.ts
@modules/projects/backend/repositories/risks.repo.ts
@modules/projects/backend/repositories/risks.repo.test.ts
@lib/http/with-auth.ts

Planner-assigned locked IDs: D-04 pickAllowed plus UnknownColumnError; D-05 convert these repos; D-06 no route rewrite; D-07 testKysely.

W9b isolated: do not convert issues/meetings/escalations/team (25-14). TDD two commits test(25-13) then feat(25-13). Isolation none.

<interfaces>
Write path: const picked = pickAllowed(COLUMNS, fields); await db.updateTable(table).set(picked).where(...). Execute. Narrow type: Pick of Updateable Database[table] keyed by typeof COLUMNS[number]. Keep COLUMNS exports identical (same string members).

projects.repo.test.ts currently imports UnknownColumnError from a relative _helpers path that no longer exists beside the file — retarget to @/lib/repositories/_helpers and @/test/db like audit.repo.test.ts.

RAID CASE/ORDER fragments in risks.repo.ts: kysely sql tagged template, same predicates.

Honor D-07. Do not change withAuth mapping (D-06).
</interfaces>
</context>

<feature>
  <name>W9b projects/activities/risks pickAllowed updates</name>
  <files>modules/projects/backend/repositories/projects.repo.ts, modules/projects/backend/repositories/activities.repo.ts, modules/projects/backend/repositories/risks.repo.ts</files>
  <behavior>
    updateProject({company_id: 99}) throws UnknownColumnError and leaves company_id unchanged (D-04).
    ACTIVITY_COLUMNS and RISK_COLUMNS still exclude project_id (D-04).
  </behavior>
  <implementation>
    pickAllowed then updateTable.set; all other SQL via getKysely (25-PATTERNS projects analog).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-13-01">
  <name>Convert projects.repo.ts with pickAllowed</name>
  <files>modules/projects/backend/repositories/projects.repo.ts, modules/projects/backend/repositories/projects.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/projects.repo.ts
  - modules/projects/backend/repositories/projects.repo.test.ts
  - lib/repositories/_kysely-helpers.ts
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (projects analog)
  </read_first>
  <behavior>
  - updateProject unknown company_id throws UnknownColumnError; row company_id unchanged (D-04).
  - names every unknown column (existing test) (D-04).
  - PROJECT_COLUMNS excludes company_id, customer_id, id (D-04).
  - allowlisted status write still returns the updated row (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely to testKysely; fix test imports to @/test/db, @/test/repo-db, @/lib/repositories/_helpers. Keep all UnknownColumnError cases. Commit test(25-13): red projects pickAllowed kysely.
  GREEN: Convert every SQL function to getKysely. updateProject uses pickAllowed(PROJECT_COLUMNS, fields) then updateTable('projects').set(picked).where id. Do not put id or company_id on the Pick type (D-04). Honor D-07. Do not edit routes. Commit feat(25-13): projects repo pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/projects.repo.test.ts</automated>
  </verify>
  <done>projects.repo.ts is on getKysely with pickAllowed (W9b, D-04, D-05).</done>
  <acceptance_criteria>
  - projects.repo.ts imports pickAllowed and getKysely
  - PROJECT_COLUMNS omits company_id
  - npx vitest run --project node modules/projects/backend/repositories/projects.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-13-02">
  <name>Convert activities.repo.ts with pickAllowed</name>
  <files>modules/projects/backend/repositories/activities.repo.ts, modules/projects/backend/repositories/activities.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/activities.repo.ts
  - modules/projects/backend/repositories/activities.repo.test.ts
  </read_first>
  <behavior>
  - Existing UnknownColumnError and project-scope tests pass (D-04, D-05).
  - updateActivity uses pickAllowed(ACTIVITY_COLUMNS, fields) (D-04).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; retarget @/test imports. Commit test(25-13): red activities pickAllowed kysely.
  GREEN: Convert all exports. updateActivity: pickAllowed then updateTable('activities').set.where id and project_id returningAll. Commit feat(25-13): activities repo pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/activities.repo.test.ts</automated>
  </verify>
  <done>activities.repo.ts is on getKysely with pickAllowed (W9b, D-04, D-05).</done>
  <acceptance_criteria>
  - activities.repo.ts imports pickAllowed
  - npx vitest run --project node modules/projects/backend/repositories/activities.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-13-03">
  <name>Convert risks.repo.ts with pickAllowed</name>
  <files>modules/projects/backend/repositories/risks.repo.ts, modules/projects/backend/repositories/risks.repo.test.ts</files>
  <read_first>
  - modules/projects/backend/repositories/risks.repo.ts
  - modules/projects/backend/repositories/risks.repo.test.ts
  </read_first>
  <behavior>
  - Existing UnknownColumnError tests pass (D-04).
  - RAID ordering predicates stay equivalent via sql tagged templates (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely; keep UnknownColumnError cases. Commit test(25-13): red risks pickAllowed kysely.
  GREEN: Convert all exports. updateRisk uses pickAllowed(RISK_COLUMNS, fields). RAID CASE order via sql tagged template. Commit feat(25-13): risks repo pickAllowed kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/projects/backend/repositories/risks.repo.test.ts modules/projects/backend/repositories/projects.repo.test.ts</automated>
  </verify>
  <done>risks.repo.ts is on getKysely with pickAllowed (W9b, D-04, D-05).</done>
  <acceptance_criteria>
  - risks.repo.ts imports pickAllowed
  - npx vitest run --project node modules/projects/backend/repositories/risks.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATCH body → UPDATE SET | Extra keys (company_id) must 400, not write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-18 | Elevation of Privilege | updateProject | critical | mitigate | pickAllowed(PROJECT_COLUMNS) throws UnknownColumnError; tests leave tenant column unchanged (D-04, ASVS V5) |
| T-25-19 | Elevation of Privilege | updateActivity / updateRisk | high | mitigate | pickAllowed; project_id stays WHERE not SET (D-04) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withAuth still maps UnknownColumnError to 400 (D-06); do not move that mapping into the repo.
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/projects/backend/repositories/projects.repo.test.ts modules/projects/backend/repositories/activities.repo.test.ts modules/projects/backend/repositories/risks.repo.test.ts
</verification>

<success_criteria>
projects, activities, and risks updates go through pickAllowed plus Kysely; mass-assignment tests still fail closed (ENF-02, D-04, W9b).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-13-SUMMARY.md` when done
</output>
