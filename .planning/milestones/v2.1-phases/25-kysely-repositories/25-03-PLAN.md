---
phase: 25-kysely-repositories
plan: 03
type: tdd
wave: 3
depends_on: [25-02]
files_modified:
  - modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts
  - modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts
  - lib/repositories/auth.repo.ts
  - lib/repositories/auth.repo.test.ts
  - lib/repositories/settings.repo.ts
  - lib/repositories/settings.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "dashboard-filter-state get/upsert use getKysely; JSONB upsert still ON CONFLICT (user_id, surface) (ENF-02, D-05, W1)."
    - "auth.repo.ts find/hash/onboarding functions use getKysely; lib/auth.ts session SQL stays on DbClient (D-05)."
    - "settings get/list/set use getKysely; settings has no serial id (D-05)."
    - "Existing dashboard and settings repo tests stay green; new auth.repo.test.ts covers findUserByUsername (D-07)."
  artifacts:
    - path: "modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts"
      provides: "Kysely dashboard filter upsert (W1)"
    - path: "lib/repositories/auth.repo.ts"
      provides: "Kysely auth user reads/writes (D-05)"
    - path: "lib/repositories/settings.repo.ts"
      provides: "Kysely settings kv (D-05)"
  key_links:
    - from: "dashboard-filter-state.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not edit lib/auth.ts session SQL (D-05)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not restyle dashboard UI or change route wrappers (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert dashboards plus lib auth/settings repositories to getKysely (ENF-02, D-05, W1+W2).

Purpose: Cross-cutting lib repos and the remaining dashboard repo share the W0 factory.

Output: three converted repos and tests including new auth.repo.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts
@modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts
@lib/repositories/auth.repo.ts
@lib/repositories/settings.repo.ts
@lib/repositories/settings.repo.test.ts
@lib/auth.ts
@test/repo-db.ts

Planner-assigned locked IDs: D-05 convert these repos not lib/auth.ts; D-02 getKysely; D-06 no route/UI rewrite; D-07 testKysely.

Repo conversion contract (all 25-03 tasks): import getKysely from @/lib/db/kysely; keep exported function signatures; mock getKysely to testKysely in tests using @/test/db and @/test/repo-db; stringify JSONB; Honor D-07; do not add pickAllowed unless the function already accepted a free-form field map (none of these three do). TDD two commits test(25-03) then feat(25-03). Isolation none.

<interfaces>
getDashboardFilters / upsertDashboardFilters: selectFrom dashboard_filter_state; insert onConflict columns user_id, surface doUpdateSet filters_json + updated_at now(). Keep migrateDashboards() in beforeAll.

auth.repo: findUserByUsername selectFrom users where username; userPasswordHash select password_hash; setUserPasswordHash updateTable set password_hash; markOnboardingComplete set onboarding_completed 1. Do not open lib/auth.ts.

settings: selectFrom settings; insert onConflict key doUpdateSet value. No RETURNING id.
</interfaces>
</context>

<feature>
  <name>Dashboards and lib repos on Kysely</name>
  <files>modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts, lib/repositories/auth.repo.ts, lib/repositories/settings.repo.ts</files>
  <behavior>
    Existing dashboard and settings tests pass via testKysely (D-05).
    findUserByUsername hits getKysely (D-05).
  </behavior>
  <implementation>
    Swap DbClient SQL for Kysely builders; leave session SQL in lib/auth.ts (D-05).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-03-01">
  <name>Convert dashboard-filter-state.repo.ts</name>
  <files>modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts, modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts</files>
  <read_first>
  - modules/dashboards/backend/repositories/dashboard-filter-state.repo.ts
  - modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts
  - .planning/phases/25-kysely-repositories/25-PATTERNS.md (JSONB upsert analog)
  </read_first>
  <behavior>
  - Existing get/upsert tests still pass (D-05).
  - getKysely is called after upsertDashboardFilters (D-02).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Add getKysely mock to dashboard-filter-state.repo.test.ts (testKysely). Add it('loads via getKysely') expecting getKysely toHaveBeenCalled after getDashboardFilters. Commit test(25-03): red dashboard filter kysely.
  GREEN: Convert both functions to getKysely per 25-PATTERNS dashboard analog. Keep migrateDashboards setup. Do not edit dashboard UI or routes (D-06). Commit feat(25-03): dashboard filter state kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts</automated>
  </verify>
  <done>Dashboard filter get/upsert are Kysely (W1, D-05).</done>
  <acceptance_criteria>
  - dashboard-filter-state.repo.ts imports getKysely
  - npx vitest run --project node modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-03-02">
  <name>Convert auth.repo.ts</name>
  <files>lib/repositories/auth.repo.ts, lib/repositories/auth.repo.test.ts</files>
  <read_first>
  - lib/repositories/auth.repo.ts
  - lib/auth.ts (confirm session SQL stays here)
  - test/repo-db.ts
  </read_first>
  <behavior>
  - findUserByUsername returns the seeded username (D-05).
  - setUserPasswordHash then userPasswordHash returns the new hash.
  - lib/auth.ts is unmodified (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Create lib/repositories/auth.repo.test.ts skipIf(!hasTestDb), setupRepoTables, insert a users row, mock getKysely to testKysely, cover findUserByUsername and setUserPasswordHash/userPasswordHash, plus getKysely called. Tests fail until conversion. Commit test(25-03): red auth repo kysely.
  GREEN: Convert the four exports to getKysely. Do not edit lib/auth.ts (D-05). Explicit column sets only — no pickAllowed. Commit feat(25-03): auth repo kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/repositories/auth.repo.test.ts</automated>
  </verify>
  <done>auth.repo.ts uses getKysely; session SQL remains in lib/auth.ts (D-05).</done>
  <acceptance_criteria>
  - auth.repo.ts imports getKysely
  - lib/auth.ts is unchanged vs HEAD for this task
  - npx vitest run --project node lib/repositories/auth.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-03-03">
  <name>Convert settings.repo.ts</name>
  <files>lib/repositories/settings.repo.ts, lib/repositories/settings.repo.test.ts</files>
  <read_first>
  - lib/repositories/settings.repo.ts
  - lib/repositories/settings.repo.test.ts
  </read_first>
  <behavior>
  - Existing settings tests pass via testKysely (D-05).
  - getKysely is called after setSetting (D-02).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely in settings.repo.test.ts; add getKysely called assertion. Commit test(25-03): red settings repo kysely.
  GREEN: Convert getSetting, listSettings, setSetting to getKysely. Keep ON CONFLICT key. Commit feat(25-03): settings repo kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/repositories/settings.repo.test.ts lib/repositories/auth.repo.test.ts</automated>
  </verify>
  <done>settings.repo.ts uses getKysely (W2, D-05).</done>
  <acceptance_criteria>
  - settings.repo.ts imports getKysely
  - npx vitest run --project node lib/repositories/settings.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth repo → users table | Password hash writes; still hashed by lib/auth.ts |
| Settings upsert → settings table | Key/value including secret-bearing keys; routes still mask |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Information Disclosure | auth.repo.ts | medium | mitigate | Keep SELECT shapes; do not log password_hash; leave hashing in lib/auth.ts (D-05, D-06) |
| T-25-06 | Tampering | settings.repo.ts | medium | mitigate | Upsert still keyed by explicit key argument, not a free-form column map (D-04) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: auth wrappers unchanged (D-06).
</threat_model>

<verification>
- 25-03-01: dashboard-filter-state.repo.test.ts
- 25-03-02: auth.repo.test.ts
- 25-03-03: settings.repo.test.ts
- Wave: npx vitest run --project node modules/dashboards/backend/repositories/dashboard-filter-state.repo.test.ts lib/repositories/auth.repo.test.ts lib/repositories/settings.repo.test.ts
</verification>

<success_criteria>
Dashboard filters, auth.repo, and settings.repo query through getKysely; lib/auth.ts session SQL untouched (ENF-02, D-05).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-03-SUMMARY.md` when done
</output>
