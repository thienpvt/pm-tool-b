---
phase: 22-weekly-workflow-surfaces
plan: 05
type: tdd
wave: 2
depends_on: [22-01]
files_modified:
  - modules/weekly/ui/report/useWeeklyReportEditor.ts
  - modules/weekly/ui/report/WeeklyReportEditorPage.tsx
  - modules/weekly/ui/report/WeeklyReportForm.tsx
  - modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx
  - app/projects/[id]/weekly-reports/[reportId]/page.tsx
  - app/weekly/reports/[projectId]/[reportId]/page.tsx
autonomous: true
requirements: [WKRP-07]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Required thin re-export at /projects/[id]/weekly-reports/[reportId] loads the module editor so Phase 16 hrefs resolve (WKRP-07, D-02, D-13)."
    - "Optional alias /weekly/reports/[projectId]/[reportId] is a one-file re-export of the same editor (D-01, D-13)."
    - "GET /api/projects/{id}/weekly-reports/{reportId} populates stacked Cards; prev_week_rag is a read-only Badge and is never sent on PATCH (D-05, D-14, UI-SPEC populated pm-editor-form)."
    - "Debounced 300ms PATCH sends allowlisted draft keys; this_week_rag Select values are Green, Amber, Red, Not applicable (D-05)."
    - "409 on PATCH toasts Report is submitted — open a correction to edit. (D-05, UI-SPEC error pm-editor-draft-save)."
    - "Submit report POSTs .../submit; 201 toasts Report submitted; 400 { error, fields } renders inline under those fields plus toast Fix validation errors before submitting. (D-05, D-06, UI-SPEC error pm-editor-submit)."
    - "Open correction POSTs .../correct when status is submitted; toast Correction opened — you can edit the report. then fields become editable (D-05, UI-SPEC populated pm-editor-submit-actions)."
    - "There is no RAID grid or second RAID editor; raid_dependency is a Textarea and submit fields list is the only RAID error UI (D-06)."
    - "404 uses Weekly report not found copy; 401/403 use periods-page error strings; loading uses Loading report… (UI-SPEC empty/error/loading pm-editor-shell)."
    - "Null draft fields still render empty textareas (UI-SPEC partial pm-editor-form)."
    - "Submit report disabled while submit POST in-flight (UI-SPEC loading pm-editor-submit)."
    - "Header links project name from GET /api/projects/{id} to /projects/{id}; period display_name truncates with title (D-14, UI-SPEC long-text pm-editor-header)."
  artifacts:
    - path: "app/projects/[id]/weekly-reports/[reportId]/page.tsx"
      provides: "Required Phase 16 href re-export (D-13)"
    - path: "app/weekly/reports/[projectId]/[reportId]/page.tsx"
      provides: "Optional module URL re-export (D-01)"
    - path: "modules/weekly/ui/report/WeeklyReportEditorPage.tsx"
      provides: "Editor shell, header, actions (WKRP-07, D-14)"
    - path: "modules/weekly/ui/report/WeeklyReportForm.tsx"
      provides: "Single-column stacked sections (D-05, D-06, D-14)"
    - path: "modules/weekly/ui/report/useWeeklyReportEditor.ts"
      provides: "GET/PATCH/submit/correct (D-03, D-05)"
  key_links:
    - from: "app/projects/[id]/weekly-reports/[reportId]/page.tsx"
      to: "modules/weekly/ui/report/WeeklyReportEditorPage.tsx"
      via: "default re-export"
      pattern: "modules/weekly/ui/report/WeeklyReportEditorPage"
    - from: "useWeeklyReportEditor.ts"
      to: "/api/projects/"
      via: "GET/PATCH report and POST submit/correct"
      pattern: "/weekly-reports/"
  prohibitions:
    - statement: "Must not change getPmDashboard href assembly (D-02, D-13)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add a RAID master editor UI (D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add weekly API routes or npm packages (D-03, D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not replace the existing activity weekly-report or project-report pages (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not PATCH prev_week_rag (D-05)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
A PM with write access can draft, submit, and correct a weekly report in the UI at the Phase 16 dashboard href, with an optional module-URL alias (WKRP-07, D-01, D-02, D-03, D-05, D-06, D-13, D-14).

Purpose: Close WKRP-07 without sharing files with 22-02 or 22-03.

Output: Editor page, form, hook, two thin re-exports, component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-weekly-workflow-surfaces/22-CONTEXT.md
@.planning/phases/22-weekly-workflow-surfaces/22-RESEARCH.md
@.planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md
@.planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md
@modules/weekly/ui/shared/types.ts
@modules/weekly/ui/shared/weekly.fixture.ts
@modules/dashboards/ui/pm/PmDashboardPage.tsx
@app/api/projects/[id]/weekly-reports/[reportId]/schema.ts
@app/api/projects/[id]/weekly-reports/[reportId]/route.ts
@app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts
@app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts
@lib/services/spec-dashboards.service.ts
@app/projects/[id]/communication/page.tsx

Planner-assigned locked IDs: D-01 optional /weekly/reports alias; D-02 honor Phase 16 href; D-03 existing APIs; D-05 GET/PATCH/submit/correct/409/prev_week_rag; D-06 no RAID editor; D-13 required projects path re-export; D-14 single-column stacked sections.

Isolation none: do not edit Sidebar.tsx, VirtualRows.tsx, periods, or tracking files. Consume types/fixtures from 22-01. Do not edit lib/services/spec-dashboards.service.ts.

<interfaces>
GET /api/projects/{id}/weekly-reports/{reportId} → shell + prev_week_rag (see route.test.ts shell).
PATCH same path, weeklyReportDraftSchema allowlist. 409 ConflictError when submitted && !correction_open.
POST .../submit rawBody true, 201. 400 { error, fields: string[] } from SubmitValidationError.
POST .../correct schema partial draft, 200.
GET /api/projects/{id} for project.name (existing withProjectAccess; D-03).
useParams: id or projectId, plus reportId — one module page supports both re-exports.
RAG Select: Green, Amber, Red, Not applicable (schema.ts). Never lowercase green.
Copywriting: Weekly report, Loading report…, Weekly report not found, You don't have access to this page., Submit report, Open correction, Report submitted, Fix validation errors before submitting., Report is submitted — open a correction to edit., Correction opened — you can edit the report., Couldn't save draft — try again.
editable = status !== 'submitted' || correction_open.
Sections top to bottom: RAG, Highlights, Completed work, Next week goals, Nearest milestone, RAID dependency, Leadership support (D-14).
Primary Submit report bg-blue-600. No extra font-weight utilities beyond font-semibold.
</interfaces>
</context>

<feature>
  <name>PM weekly report editor with Phase 16 path re-export</name>
  <files>modules/weekly/ui/report/WeeklyReportEditorPage.tsx, app/projects/[id]/weekly-reports/[reportId]/page.tsx, modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</files>
  <behavior>
    Mocked GET shell renders Weekly report and draft fields; PATCH 409 shows the submitted toast; POST submit 201 (WKRP-07, D-05, D-13).
  </behavior>
  <implementation>
    Hook GET/PATCH/submit/correct; single-column form; required projects re-export plus optional weekly alias (D-01, D-13, D-14).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="22-05-01">
  <name>Editor GET shell, loading, and required re-export</name>
  <precondition>modules/weekly/ui/shared/types.ts exports WeeklyReportEditorShell from plan 22-01</precondition>
  <files>modules/weekly/ui/report/useWeeklyReportEditor.ts, modules/weekly/ui/report/WeeklyReportEditorPage.tsx, modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx, app/projects/[id]/weekly-reports/[reportId]/page.tsx, app/weekly/reports/[projectId]/[reportId]/page.tsx</files>
  <read_first>
  - modules/weekly/ui/shared/weekly.fixture.ts reportShellFixture
  - modules/dashboards/ui/pm/PmDashboardPage.tsx
  - app/api/projects/[id]/weekly-reports/[reportId]/route.ts
  - lib/services/spec-dashboards.service.ts (href comment only — do not edit)
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (PM editor zones, Copywriting)
  </read_first>
  <behavior>
  - Deferred GET: Loading report… (UI-SPEC loading pm-editor-shell).
  - 200: title prefix Weekly report, display_name, status Badge, due date, prev_week_rag read-only (D-05, D-14).
  - 404: Weekly report not found heading and body (UI-SPEC empty pm-editor-form).
  - 403: You don't have access to this page. (UI-SPEC error pm-editor-shell).
  - app/projects/[id]/weekly-reports/[reportId]/page.tsx re-exports the module default (D-13).
  - app/weekly/reports/[projectId]/[reportId]/page.tsx re-exports the same default (D-01).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add WeeklyReportEditorPage.component.test.tsx. Mock useParams to { id: '7', reportId: '10' }, usePathname, Sidebar. Stub GET /api/projects/7/weekly-reports/10 with deferred then reportShellFixture, GET /api/projects/7 with { name: 'Alpha' }. Assert loading copy, Weekly report, prev_week_rag not in an editor combobox, 404 copy, 403 copy. Tests fail because modules are missing. Commit test(22-05): red weekly report editor get shell.
  GREEN: useWeeklyReportEditor(projectId, reportId): GET report then GET project name (D-03). 401/403/load_failed plus not_found on 404. WeeklyReportEditorPage reads useParams id or projectId plus reportId so both routes work (D-01, D-13). Shell Phase 21: Sidebar + main. Header display_name, project Link /projects/{id}, status Badge, due_at. Read-only prev_week_rag Badge. Both app re-exports: use client and export { default } from the module editor. Do not edit spec-dashboards.service.ts (D-02, D-13). Do not PATCH yet. Commit feat(22-05): weekly report editor get shell.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</automated>
  </verify>
  <done>Phase 16 path and optional alias re-export the editor; GET shell renders loading/error/header (WKRP-07, D-01, D-05, D-13, D-14).</done>
  <acceptance_criteria>
  - app/projects/[id]/weekly-reports/[reportId]/page.tsx re-exports WeeklyReportEditorPage
  - app/weekly/reports/[projectId]/[reportId]/page.tsx re-exports WeeklyReportEditorPage
  - npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-05-02">
  <name>Debounced draft PATCH and 409 toast</name>
  <files>modules/weekly/ui/report/WeeklyReportForm.tsx, modules/weekly/ui/report/useWeeklyReportEditor.ts, modules/weekly/ui/report/WeeklyReportEditorPage.tsx, modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</files>
  <read_first>
  - app/api/projects/[id]/weekly-reports/[reportId]/schema.ts
  - app/projects/[id]/communication/page.tsx
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (PM editor form, draft save)
  </read_first>
  <behavior>
  - Changing highlights PATCH allowlisted JSON after ~300ms debounce; this_week_rag options are Title Case (D-05).
  - prev_week_rag is absent from PATCH bodies (D-05).
  - Null fields render empty textareas (UI-SPEC partial pm-editor-form).
  - Failed PATCH toasts Couldn't save draft — try again. (UI-SPEC error pm-editor-draft-save).
  - 409 toasts Report is submitted — open a correction to edit. (D-05).
  - Fields disabled when status is submitted and correction_open is false (D-05).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: From a draft fixture, type in highlights and fake timers 300ms; assert PATCH /api/projects/7/weekly-reports/10 body includes highlights and does not include prev_week_rag. Stub 409 and assert toast Report is submitted — open a correction to edit. Stub 500 and assert Couldn't save draft — try again. Submitted shell: textareas disabled. Commit test(22-05): red weekly report draft patch and 409 toast.
  GREEN: WeeklyReportForm single-column Cards (D-14): this_week_rag Select Green/Amber/Red/Not applicable, then highlights, completed_work, next_week_goals, nearest_milestone plus nearest_milestone_id, raid_dependency Textarea, leadership_support Textarea. Labels text-xs font-semibold. Debounce 300ms PATCH of allowlisted keys only (D-05). editable gate as specified. No RAID table, no extra RAID routes (D-06). Textareas min-h 80px resize-y. Commit feat(22-05): weekly report draft patch and 409 toast.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</automated>
  </verify>
  <done>Draft PATCH uses the allowlist and Title Case RAG; 409 shows Copywriting toast; no RAID editor (WKRP-07, D-05, D-06, D-14).</done>
  <acceptance_criteria>
  - PATCH bodies omit prev_week_rag
  - this_week_rag options are Green, Amber, Red, Not applicable
  - WeeklyReportForm.tsx has no RAID grid
  - npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-05-03">
  <name>Submit report, Open correction, and validation fields</name>
  <files>modules/weekly/ui/report/useWeeklyReportEditor.ts, modules/weekly/ui/report/WeeklyReportForm.tsx, modules/weekly/ui/report/WeeklyReportEditorPage.tsx, modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</files>
  <read_first>
  - app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts
  - lib/api-errors.ts SubmitValidationError JSON shape
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Submit, Correct)
  </read_first>
  <behavior>
  - Draft/not_submitted: Submit report visible; POST .../submit with empty body; 201 toast Report submitted (D-05, UI-SPEC populated pm-editor-submit-actions).
  - Submit 400 { error, fields: ['raid_dependency'] } shows that path inline and toast Fix validation errors before submitting. (D-06, UI-SPEC error pm-editor-submit).
  - Submitted: Open correction visible; POST .../correct; toast Correction opened — you can edit the report. then fields enabled (D-05).
  - Submit report disabled while POST in-flight (UI-SPEC loading pm-editor-submit).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Draft fixture shows Submit report; click it, assert POST .../submit; 201 toast Report submitted. 400 fields raid_dependency asserts inline text and validation toast. Submitted fixture shows Open correction, not Submit report; click it, assert POST .../correct; fields become enabled. Hold submit Promise and assert button disabled. Commit test(22-05): red weekly report submit and correct.
  GREEN: submitReport POST .../submit with no JSON body (rawBody route). Parse 400 JSON fields into fieldErrors for form labels (D-06). correctReport POST .../correct JSON {} or partial overlay then reload GET. Sticky or bottom Card actions: Submit report primary bg-blue-600 when draft/not_submitted; Open correction when submitted. Do not invent RAID CRUD. Do not change getPmDashboard. Commit feat(22-05): weekly report submit and correct.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx</automated>
    <human-check>
      <test>From My dashboard, follow Open report into the editor; save a draft; submit if validation allows</test>
      <expected>Phase 16 href opens the editor; stacked sections; prev-week RAG not editable; toasts match Copywriting</expected>
      <why_human>Deep-link landing and stacked layout are visual</why_human>
    </human-check>
  </verify>
  <done>PM can submit and open correction through existing POSTs; validation fields render inline (WKRP-07, D-05, D-06).</done>
  <acceptance_criteria>
  - Submit uses POST .../submit
  - Correct uses POST .../correct
  - 400 fields render inline
  - npx vitest run --project jsdom modules/weekly/ui/report/WeeklyReportEditorPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET/PATCH report | Session cookie; project-scoped shell and draft text cross here |
| Browser → POST submit/correct | Write actions; validation fields return to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-11 | Elevation of Privilege | useWeeklyReportEditor | high | mitigate | withProjectAccess + assertProjectWriteAccess on mutate; 401/403/404 in-page (D-03, D-05, ASVS V4) |
| T-22-12 | Tampering | WeeklyReportForm highlights | medium | mitigate | React text nodes only; no dangerouslySetInnerHTML; PATCH allowlisted keys only (D-05) |
| T-22-13 | Information Disclosure | GET /api/projects/{id} | medium | mitigate | Same project id as the report route; existing withProjectAccess |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install (D-08) |

ASVS L1: IDOR stays on existing wrappers; UI is not a second policy engine.
</threat_model>

<verification>
- Editor GET/PATCH/submit/correct tests green
- Both re-export files exist
- spec-dashboards.service.ts unchanged
- package.json unchanged
</verification>

<success_criteria>
A PM can open the Phase 16 weekly-report href, draft via PATCH, submit, and correct, with RAID errors only as submit fields (WKRP-07, D-05, D-06, D-13, D-14).
</success_criteria>

<output>
Create `.planning/phases/22-weekly-workflow-surfaces/22-05-SUMMARY.md` when done
</output>
