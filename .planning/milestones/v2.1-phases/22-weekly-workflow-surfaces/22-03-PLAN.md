---
phase: 22-weekly-workflow-surfaces
plan: 03
type: tdd
wave: 2
depends_on: [22-01]
files_modified:
  - modules/weekly/ui/tracking/usePeriodTracking.ts
  - modules/weekly/ui/tracking/WeeklyTrackingPage.tsx
  - modules/weekly/ui/tracking/TrackingCountsBar.tsx
  - modules/weekly/ui/tracking/TrackingFiltersBar.tsx
  - modules/weekly/ui/tracking/TrackingGrid.tsx
  - modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx
  - app/weekly/tracking/page.tsx
autonomous: true
requirements: [CPMO-05, PERF-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO opens /weekly/tracking via thin re-export and sees title Weekly tracking (CPMO-05, D-01)."
    - "Period Select uses GET /api/weekly-periods; ?periodId= wins when valid, otherwise latest iso_week; changing Select router.replace /weekly/tracking?periodId= (D-10)."
    - "Invalid periodId falls back to the latest period; period-list fetch failure uses the page-shell error panel (UI-SPEC partial/error tracking-period-selector)."
    - "No periods shows No periods to track and Create a weekly period first, then return here to track submissions. with a link to /weekly/periods (UI-SPEC empty tracking-period-selector)."
    - "GET /api/weekly-periods/{id}/tracking drives six count chips including zeros and the virtualized grid (D-03, D-07, UI-SPEC populated tracking-summary-chips and tracking-grid)."
    - "Zero obligated rows show No projects in this period and This period has no obligated weekly reports, or filters exclude all rows. (UI-SPEC empty tracking-grid)."
    - "Apply filters refetches GET with status, lateness, pm_user_id, stage, rag, technology_council query params; omitted keys mean no constraint (D-07, UI-SPEC populated/partial tracking-filter-bar)."
    - "TrackingGrid wraps VirtualRows from 22-01 at ROW_HEIGHT 40; checkboxes for submitted rows only; header checkbox toggles all filtered eligible rows in memory not only the window; selection order is the project_ids array (PERF-01, D-08, D-12)."
    - "Open report href is /projects/{project_id}/weekly-reports/{report_id} (D-13)."
    - "Initial load shows Loading tracking…; 401/403/5xx use the same Copywriting error strings as periods (UI-SPEC loading/error tracking-page-shell)."
    - "Filter bar flex-wrap gap-2; chips wrap; grid wrapper overflow-x-auto and vertical window scroll (UI-SPEC overflow tracking-filter-bar, tracking-summary-chips, tracking-grid)."
  artifacts:
    - path: "app/weekly/tracking/page.tsx"
      provides: "Thin use-client re-export (D-01)"
    - path: "modules/weekly/ui/tracking/WeeklyTrackingPage.tsx"
      provides: "Tracking shell, period Select, counts, filters, grid (CPMO-05, D-07, D-10)"
    - path: "modules/weekly/ui/tracking/TrackingGrid.tsx"
      provides: "VirtualRows body + selection (PERF-01, D-08, D-12)"
    - path: "modules/weekly/ui/tracking/usePeriodTracking.ts"
      provides: "GET tracking with query filters (D-03, D-07)"
  key_links:
    - from: "app/weekly/tracking/page.tsx"
      to: "modules/weekly/ui/tracking/WeeklyTrackingPage.tsx"
      via: "default re-export"
      pattern: "modules/weekly/ui/tracking/WeeklyTrackingPage"
    - from: "usePeriodTracking.ts"
      to: "/api/weekly-periods/"
      via: "GET tracking?filters"
      pattern: "/tracking"
    - from: "TrackingGrid.tsx"
      to: "modules/weekly/ui/shared/VirtualRows.tsx"
      via: "import VirtualRows"
      pattern: "VirtualRows"
  prohibitions:
    - statement: "Must not modify VirtualRows.tsx (created in 22-01; consume only)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not POST export or call export preview in this plan (D-11, 22-04)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add weekly API routes or npm packages (D-03, D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not replace the existing activity weekly-report or project-report pages (D-01)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO can open /weekly/tracking, pick a period via shareable ?periodId=, filter and view obligated rows in a virtualized grid (CPMO-05 view, PERF-01, D-01, D-03, D-07, D-08, D-10, D-12).

Purpose: Ship the tracking view on the 22-01 VirtualRows primitive. Export pack is 22-04.

Output: Tracking page, hook GET, counts, filters, virtualized grid with checkbox selection order.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-weekly-workflow-surfaces/22-CONTEXT.md
@.planning/phases/22-weekly-workflow-surfaces/22-RESEARCH.md
@.planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md
@.planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md
@modules/weekly/ui/shared/VirtualRows.tsx
@modules/weekly/ui/shared/types.ts
@modules/weekly/ui/shared/weekly.fixture.ts
@app/login/page.tsx
@app/api/weekly-periods/[periodId]/tracking/route.ts
@lib/services/weekly-tracking.service.ts
@modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
@modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx

Planner-assigned locked IDs: D-01 /weekly/tracking re-export; D-03 existing APIs; D-07 GET tracking; D-08 consume VirtualRows; D-10 ?periodId=; D-12 selection order for later export; D-13 Open report uses Phase 16 href shape.

Isolation none: do not edit Sidebar.tsx, VirtualRows.tsx, types.ts, weekly.fixture.ts, or periods files. Import types and fixtures from 22-01. Do not implement ExportToolbar (22-04).

<interfaces>
GET /api/weekly-periods → WeeklyPeriodListItem[] iso_week DESC (latest is index 0).
GET /api/weekly-periods/{periodId}/tracking?status=&lateness=&pm_user_id=&stage=&rag=&technology_council=true
status: not_submitted | draft | submitted | overdue
lateness: on_time | late
technology_council only sent when true.
200: { period, counts, rows } from weekly-tracking.service.ts.
useSearchParams + useRouter like app/login/page.tsx; wrap the page that calls useSearchParams in Suspense (login default export pattern).
Open report: /projects/${project_id}/weekly-reports/${report_id}
Copywriting: Weekly tracking, Loading tracking…, No periods to track, No projects in this period, Apply filters, Open report, error strings same as periods page (this page not this dashboard).
Count chips always six: Obligated, Submitted, Draft, Not submitted, Overdue, Late.
Grid columns: checkbox, Project link /projects/{id}, Code, Stage, PM, Status Badge, Overdue Badge, RAG Badge, Lateness, Tech council, Open report.
Eligible checkbox: status === submitted. Non-submitted checkboxes disabled.
Typography: font-semibold + default body only. Accent links text-blue-600.
</interfaces>
</context>

<feature>
  <name>CPMO tracking grid with period query and VirtualRows</name>
  <files>modules/weekly/ui/tracking/WeeklyTrackingPage.tsx, modules/weekly/ui/tracking/TrackingGrid.tsx, app/weekly/tracking/page.tsx</files>
  <behavior>
    Valid ?periodId= loads that period tracking payload; 150-row data still windows through VirtualRows (CPMO-05, PERF-01, D-10, D-08).
  </behavior>
  <implementation>
    Hook GET tracking; page Select + counts + filters + TrackingGrid wrapping VirtualRows; thin app re-export (D-01, D-07).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="22-03-01">
  <name>Tracking page period query and GET payload</name>
  <precondition>modules/weekly/ui/shared/VirtualRows.tsx exists and exports ROW_HEIGHT from plan 22-01</precondition>
  <files>modules/weekly/ui/tracking/usePeriodTracking.ts, modules/weekly/ui/tracking/WeeklyTrackingPage.tsx, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx, app/weekly/tracking/page.tsx</files>
  <read_first>
  - modules/weekly/ui/shared/weekly.fixture.ts
  - modules/weekly/ui/shared/types.ts
  - app/login/page.tsx (Suspense + useSearchParams)
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - app/api/weekly-periods/[periodId]/tracking/route.ts
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (tracking header, loading/error)
  </read_first>
  <behavior>
  - Missing periodId: after GET periods, selected period is the first iso_week DESC fixture id; GET tracking uses that id (D-10).
  - searchParams periodId matching a period: GET tracking for that id (D-10).
  - Unknown periodId: fall back to latest iso_week (UI-SPEC partial tracking-period-selector).
  - Empty periods array: No periods to track panel and link /weekly/periods (UI-SPEC empty tracking-period-selector).
  - Deferred fetch: Loading tracking… (UI-SPEC loading tracking-page-shell).
  - 403: You don't have access to this page. (UI-SPEC error tracking-page-shell).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add WeeklyTrackingPage.component.test.tsx. Mock next/navigation usePathname /weekly/tracking, useSearchParams (overridable), useRouter replace vi.fn. Mock Sidebar. Stub GET /api/weekly-periods with periodsFixture and GET /api/weekly-periods/2/tracking with trackingPayload. Cover loading copy, title Weekly tracking, 403 copy, empty periods panel, invalid periodId fallback, Select change calling replace with ?periodId=. Tests fail because modules are missing. Commit test(22-03): red tracking period query and get.
  GREEN: usePeriodTracking(periodId, filters) GET /api/weekly-periods/${periodId}/tracking plus optional URLSearchParams (D-03, D-07). Same 401/403/load_failed mapping as periods. WeeklyTrackingPage: Suspense wrapper like login; read periodId from searchParams; GET periods to build Select; default per D-10; on Select change router.replace /weekly/tracking?periodId=. Shell matches Phase 21. Empty-period panel Copywriting + Link /weekly/periods. Thin re-export app/weekly/tracking/page.tsx (D-01). Do not POST export. Do not edit VirtualRows.tsx. Commit feat(22-03): tracking period query and get.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</automated>
  </verify>
  <done>/weekly/tracking loads tracking JSON for the shareable period query with fallback to latest iso_week (CPMO-05, D-01, D-07, D-10).</done>
  <acceptance_criteria>
  - app/weekly/tracking/page.tsx re-exports WeeklyTrackingPage
  - Invalid or missing periodId falls back to latest iso_week
  - npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-03-02">
  <name>Counts chips and Apply filters bar</name>
  <files>modules/weekly/ui/tracking/TrackingCountsBar.tsx, modules/weekly/ui/tracking/TrackingFiltersBar.tsx, modules/weekly/ui/tracking/WeeklyTrackingPage.tsx, modules/weekly/ui/tracking/usePeriodTracking.ts, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</files>
  <read_first>
  - modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
  - modules/dashboards/ui/portfolio/PortfolioKpiTiles.tsx
  - app/api/weekly-periods/[periodId]/tracking/route.ts parseTrackingFilters
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (summary chips, filter bar)
  </read_first>
  <behavior>
  - Six chips Obligated, Submitted, Draft, Not submitted, Overdue, Late bind counts including 0 (UI-SPEC populated/zero-one-many tracking-summary-chips).
  - Apply filters refetches GET with only selected keys; technology_council query is the string true when checked (D-07, UI-SPEC populated/partial tracking-filter-bar).
  - Filter controls wrap flex-wrap; chips wrap (UI-SPEC overflow).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Assert data-testid tracking-counts-bar shows six labels and fixture numbers including zeros. Assert data-testid tracking-filter-bar; choose status overdue, check technology council, click Apply filters; assert fetch URL contains status=overdue and technology_council=true and omits empty keys. Commit test(22-03): red tracking counts and filters.
  GREEN: TrackingCountsBar from counts. TrackingFiltersBar draft state like PortfolioFiltersBar; keys status (All / not_submitted / draft / submitted / overdue), lateness, pm_user_id from distinct rows, stage L0-L5, rag Green/Amber/Red/All, technology_council Checkbox. Apply filters button. Hook load(filters) builds URLSearchParams (D-07). Do not persist filters via PUT. Commit feat(22-03): tracking counts and filters.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</automated>
  </verify>
  <done>Counts always render six numeric chips; Apply filters refetches GET query params (CPMO-05, D-07).</done>
  <acceptance_criteria>
  - tracking-counts-bar and tracking-filter-bar test ids exist
  - Apply filters issues GET with forwarded query keys
  - npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-03-03">
  <name>Virtualized tracking grid and checkbox selection order</name>
  <files>modules/weekly/ui/tracking/TrackingGrid.tsx, modules/weekly/ui/tracking/WeeklyTrackingPage.tsx, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/shared/VirtualRows.tsx
  - modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (tracking grid columns, selection)
  </read_first>
  <behavior>
  - Grid uses VirtualRows; a 150-row tracking fixture still mounts at most 30 data-testid virtual-row nodes (PERF-01, D-08, D-09).
  - Empty rows: No projects in this period copy (UI-SPEC empty tracking-grid).
  - Mixed statuses still render Badge columns (UI-SPEC partial tracking-grid).
  - Submitted row checkbox enabled; draft/not_submitted disabled.
  - Header checkbox selects all filtered submitted rows currently in memory, including rows outside the window (UI-SPEC tracking-export-selection, D-12).
  - Checking row A then row B yields selected project_ids [A, B] in that order (D-12).
  - Open report Link href /projects/{project_id}/weekly-reports/{report_id} (D-13).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Render grid with trackingRows150 from weekly.fixture.ts; assert virtual-row count at most 30. Empty rows copy. Click two submitted checkboxes in order; expose selected ids via a test id or callback spy as [first, second]. Assert Open report href uses the projects weekly-reports path. Commit test(22-03): red tracking grid virtual rows and selection order.
  GREEN: TrackingGrid sticky Table header (h-8 px-2 text-xs) plus VirtualRows body rowHeight ROW_HEIGHT from shared module, container max-h calc 100vh-280px or height 400 in tests, overscan 5. Do not edit VirtualRows.tsx — import it (D-08). Columns per UI-SPEC. Selection state lives on the page or grid: append project_id on check, remove on uncheck, never reorder except by user check sequence (D-12). Header checkbox only eligible submitted rows in the current filtered array. Status/overdue/RAG Badges per UI-SPEC colors. Project name truncates with title. Do not add export POST. Commit feat(22-03): tracking grid virtual rows and selection order.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx modules/weekly/ui/shared/VirtualRows.component.test.tsx</automated>
    <human-check>
      <test>Open /weekly/tracking as CPMO, change period, apply a status filter, scroll the grid</test>
      <expected>URL keeps periodId; chips and rows update; scrolling does not hitch on a large list</expected>
      <why_human>Scroll performance and filter layout wrapping are visual</why_human>
    </human-check>
  </verify>
  <done>Tracking grid virtualizes rows and records checkbox selection order for export (CPMO-05, PERF-01, D-08, D-12, D-13).</done>
  <acceptance_criteria>
  - TrackingGrid.tsx imports VirtualRows from modules/weekly/ui/shared/VirtualRows
  - Open report href uses /projects/ and /weekly-reports/
  - npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET tracking | CPMO session; periodId path and filter query cross here |
| Browser → GET periods | Used only to populate the period Select (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-06 | Elevation of Privilege | WeeklyTrackingPage | high | mitigate | 401/403 in-page; withCpmo on tracking GET (D-03, ASVS V4) |
| T-22-07 | Information Disclosure | periodId query | medium | mitigate | Invalid id falls back locally; server still 404/403 if the period is not in-company |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install (D-08) |

ASVS L1: authz remains on withCpmo.
</threat_model>

<verification>
- Period query, counts/filters, virtualized grid tests green
- VirtualRows.tsx git diff empty in this plan
- package.json unchanged
</verification>

<success_criteria>
CPMO can track a period from /weekly/tracking with shareable periodId, filters, counts, and a virtualized selectable grid (CPMO-05, PERF-01, D-07, D-08, D-10, D-12).
</success_criteria>

<output>
Create `.planning/phases/22-weekly-workflow-surfaces/22-03-SUMMARY.md` when done
</output>
