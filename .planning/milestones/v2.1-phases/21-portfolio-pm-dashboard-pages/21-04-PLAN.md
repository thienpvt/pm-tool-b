---
phase: 21-portfolio-pm-dashboard-pages
plan: 04
type: tdd
wave: 2
depends_on: [21-01]
files_modified:
  - modules/dashboards/ui/pm/usePmDashboard.ts
  - modules/dashboards/ui/pm/PmDashboardPage.tsx
  - modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx
  - modules/dashboards/ui/pm/PmActionQueues.tsx
  - modules/dashboards/ui/pm/PmFiltersBar.tsx
  - app/dashboards/pm/page.tsx
autonomous: true
requirements: [MDSH-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Assigned PM opens /dashboards/pm via thin app re-export and sees title My dashboard (MDSH-06, D-01, D-02, D-10)."
    - "GET /api/dashboards/pm drives three queues weekly, milestones, raid; each action link uses the server href verbatim (D-10, D-12)."
    - "Weekly link copy Open report, milestones View milestone, RAID View RAID (UI-SPEC populated pm-action-queues)."
    - "Empty weekly/milestones/raid queues show the Copywriting empty heading + body with table headers still visible (UI-SPEC empty pm queues)."
    - "Section headers show count badge (N) when N>0 and hide the badge when N=0 (UI-SPEC zero-one-many pm-action-queues)."
    - "Weekly rows may mix draft, not_submitted, and overdue Badge (UI-SPEC partial pm-weekly-queue)."
    - "Each queue table scrolls horizontally inside Card; milestone name and RAID code truncate with title (UI-SPEC overflow/long-text pm-action-queues)."
    - "PM with zero assigned projects still renders shell, filters, and three empty queues (UI-SPEC empty pm-page-shell)."
    - "Same AND filter keys via GET/PUT/POST /api/dashboards/pm/filters; Apply disabled during refetch (D-13, UI-SPEC pm-filter-bar loading/partial/populated/error)."
    - "401/403/5xx use the same Copywriting error strings as portfolio; Viewer 403 (D-12, UI-SPEC error pm-page-shell)."
    - "Hook refetches GET on mount and when the document becomes visible so resolved rows drop after returning from a deep link (D-11)."
    - "CPMO visiting /dashboards/pm still consumes GET /api/dashboards/pm (assignment window is server D-09 from Phase 16) (D-12)."
    - "Initial GET shows Loading dashboard… (UI-SPEC loading pm-page-shell)."
  artifacts:
    - path: "app/dashboards/pm/page.tsx"
      provides: "Thin use-client re-export (D-01, D-02)"
    - path: "modules/dashboards/ui/pm/PmDashboardPage.tsx"
      provides: "My dashboard shell, filters, queues (MDSH-06)"
    - path: "modules/dashboards/ui/pm/usePmDashboard.ts"
      provides: "GET /api/dashboards/pm + pm filter persist + visibility refetch (D-10, D-11, D-13)"
    - path: "modules/dashboards/ui/pm/PmActionQueues.tsx"
      provides: "Three queue tables with Link href={row.href} (D-10)"
  key_links:
    - from: "app/dashboards/pm/page.tsx"
      to: "modules/dashboards/ui/pm/PmDashboardPage.tsx"
      via: "default re-export"
      pattern: "modules/dashboards/ui/pm/PmDashboardPage"
    - from: "usePmDashboard.ts"
      to: "/api/dashboards/pm"
      via: "fetch GET"
      pattern: "/api/dashboards/pm"
    - from: "PmActionQueues.tsx"
      to: "row.href"
      via: "next/link Link"
      pattern: "row.href"
  prohibitions:
    - statement: "Must not invent weekly/milestone/RAID URLs; only server href (D-10)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add mutator pages for completing actions (D-10, D-11)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages (D-17)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not overwrite /dashboard (D-02)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Assigned PMs (and CPMO on their own assignment window) open `/dashboards/pm` with weekly, milestone, and RAID action queues that deep-link via server hrefs (MDSH-06, D-10, D-11, D-12, D-13).

Purpose: Deliver MDSH-06 in parallel with portfolio filters (no file overlap with 21-02).

Output: PM module page, hook, queues, filters, thin re-export, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-CONTEXT.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-RESEARCH.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md
@modules/dashboards/ui/shared/types.ts
@lib/services/spec-dashboards.service.ts
@app/api/dashboards/pm/route.ts
@app/api/dashboards/pm/filters/route.ts
@modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts

Sidebar My dashboard link already shipped in 21-01. Do not edit Sidebar.tsx.

<interfaces>
GET /api/dashboards/pm JSON:
{ filters, projects: PortfolioDashboardListRow[], actions: {
  weekly: { project_id, report_id, period_id, period_display_name, due_at, status, overdue, href }[],
  milestones: { project_id, milestone_id, name, plan_end, adjusted_end, kind: 'upcoming'|'overdue', href }[],
  raid: { project_id, entity_type, id, code, due_date, has_technology_council, href }[]
}}

Weekly href from service: /projects/{project_id}/weekly-reports/{report_id}
Milestone href: /projects/{project_id}/milestones
RAID href: /projects/{project_id}/raid

PM filters: GET/PUT/POST /api/dashboards/pm/filters same schema as portfolio; withAuth + pm|cpmo (D-13).

Copywriting: My dashboard; Open report; View milestone; View RAID;
empty weekly No weekly reports due / All obligated weekly reports for your assigned projects are submitted.
empty milestones No milestone actions / No upcoming or overdue milestones on your assigned projects.
empty RAID No RAID actions / No high-priority RAID items due on your assigned projects.
Loading dashboard…; same 401/403/5xx strings as portfolio.

Join project_id to projects[].name for the Project column. Do not build href client-side.
</interfaces>
</context>

<feature>
  <name>PM dashboard action queues</name>
  <files>modules/dashboards/ui/pm/PmDashboardPage.tsx, modules/dashboards/ui/pm/usePmDashboard.ts, modules/dashboards/ui/pm/PmActionQueues.tsx, app/dashboards/pm/page.tsx</files>
  <behavior>
    GET 200 renders three queues with fixture hrefs as link href attributes (MDSH-06, D-10).
    GET 403 shows You don't have access to this dashboard. (D-12).
  </behavior>
  <implementation>
    Mirror portfolio hook against /api/dashboards/pm; queues render Link href={row.href}; thin app re-export.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="21-04-01">
  <name>End-to-end My dashboard queues with server hrefs</name>
  <precondition>app/api/dashboards/pm/route.ts exists and exports GET (Phase 16); modules/dashboards/ui/shared/types.ts from 21-01 exists</precondition>
  <files>modules/dashboards/ui/pm/usePmDashboard.ts, modules/dashboards/ui/pm/PmDashboardPage.tsx, modules/dashboards/ui/pm/PmActionQueues.tsx, modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx, app/dashboards/pm/page.tsx</files>
  <read_first>
  - modules/dashboards/ui/shared/types.ts
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - lib/services/spec-dashboards.service.ts getPmDashboard return and href strings
  - app/api/dashboards/pm/route.ts
  - 21-UI-SPEC PM page zones + queue columns + Copywriting
  </read_first>
  <behavior>
  - Deferred GET shows Loading dashboard… (UI-SPEC loading pm-page-shell).
  - 200: title My dashboard; weekly row Open report href equals fixture href; milestones View milestone; RAID View RAID (MDSH-06, D-10).
  - Empty actions arrays: three empty Copywriting headings; table headers still in the document (UI-SPEC empty queues).
  - Zero projects: page still shows three queue Cards (UI-SPEC empty pm-page-shell).
  - Weekly section with 2 rows shows badge (2); empty weekly hides the numeric badge (UI-SPEC zero-one-many).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: PmDashboardPage.component.test.tsx covering page and queues (optional extra PmActionQueues.component.test.tsx if it keeps the page test under 200 lines). Mock Sidebar and usePathname /dashboards/pm. Fixture weekly href /projects/10/weekly-reports/99, milestone href /projects/10/milestones, raid href /projects/10/raid. Assert those hrefs on the anchors, not client-built variants. Long milestone name and RAID code get truncate + title. Commit test(21-04): red pm dashboard queues.
  GREEN: usePmDashboard fetch GET /api/dashboards/pm with the same 401/403/load_failed mapping as the portfolio hook (in-page, no window.location). PmActionQueues: three Card size=sm sections Weekly reports, Milestones, RAID. Columns per UI-SPEC. Link text Open report / View milestone / View RAID with class text-blue-600 text-sm hover:underline href={row.href} only. Resolve project name from data.projects. Status/kind Badges; overdue flag on weekly. Count badge (N) when N>0. PmDashboardPage shell matches portfolio (Sidebar + main). app/dashboards/pm/page.tsx use client default re-export (D-01, D-02). Do not create weekly submit or RAID edit pages. Do not edit Sidebar. Commit feat(21-04): pm dashboard queues and re-export.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/pm</automated>
  </verify>
  <done>PM dashboard renders three action queues whose links are the server href strings (MDSH-06, D-10).</done>
  <acceptance_criteria>
  - app/dashboards/pm/page.tsx re-exports PmDashboardPage
  - Queue links use row.href
  - npx vitest run --project jsdom on the two test files exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-04-02">
  <name>PM AND filters and live refetch on focus</name>
  <files>modules/dashboards/ui/pm/PmFiltersBar.tsx, modules/dashboards/ui/pm/usePmDashboard.ts, modules/dashboards/ui/pm/PmDashboardPage.tsx, modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx</files>
  <read_first>
  - app/api/dashboards/pm/filters/route.ts
  - modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
  - 21-CONTEXT D-11 D-13
  </read_first>
  <behavior>
  - Apply filters PUT /api/dashboards/pm/filters then GET /api/dashboards/pm (D-13).
  - Failed persist toast Couldn't save filters — try again. (UI-SPEC error pm-filter-bar).
  - After mount, a visibilitychange to visible calls GET /api/dashboards/pm again (D-11).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: page/hook tests for PUT then GET; POST clear/defaults; toast on PUT 500; fire Event visibilitychange with document.visibilityState visible and expect another GET. Commit test(21-04): red pm filters and visibility refetch.
  GREEN: Reuse the nine AND keys and Apply/Clear/Reset copy. PUT/POST /api/dashboards/pm/filters. Apply disabled while refreshing. Add document visibilitychange listener that calls load when visibilityState is visible (D-11). Do not cache the dashboard across navigations (load on mount is already in the hook). Commit feat(21-04): pm filters and visibility refetch.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx</automated>
  </verify>
  <done>PM filters persist on the pm surface; returning to the tab refetches live GET (D-11, D-13).</done>
  <acceptance_criteria>
  - Filter requests hit /api/dashboards/pm/filters
  - visibilitychange visible triggers load
  - npx vitest run --project jsdom modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-04-03">
  <name>PM 401/403/5xx panels and mixed weekly statuses</name>
  <files>modules/dashboards/ui/pm/PmDashboardPage.tsx, modules/dashboards/ui/pm/PmActionQueues.tsx, modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx</files>
  <read_first>
  - modules/dashboards/ui/pm/PmDashboardPage.tsx
  - 21-UI-SPEC error pm-page-shell and partial weekly rows
  - app/api/dashboards/pm/route.ts hasRole pm|cpmo
  </read_first>
  <behavior>
  - GET 403: You don't have access to this dashboard. (D-12).
  - GET 401: Session expired — refresh the page and sign in again.
  - GET 500: Couldn't load the dashboard. Try again.
  - Weekly fixture with one draft and one not_submitted overdue still renders both rows with status Badge (UI-SPEC partial pm-weekly-queue).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: 401/403/500 page tests; mixed weekly status test. Commit test(21-04): red pm forbidden and mixed weekly rows.
  GREEN: same centered error panel pattern as portfolio. Do not add a client CPMO vs PM branch that would show other people's assignments (D-12). Queue Cards stay overflow-x-auto. Commit feat(21-04): pm forbidden and mixed weekly rows.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/pm</automated>
  </verify>
  <done>Viewer 403 is in-page; mixed weekly statuses render; assignment scope stays on the API (MDSH-06, D-12).</done>
  <acceptance_criteria>
  - 401/403/5xx Copywriting strings asserted
  - Mixed weekly statuses asserted
  - npx vitest run --project jsdom modules/dashboards/ui/pm exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/dashboards/pm | Session; pm or cpmo; assignment window is server-side |
| Queue Link → project weekly/milestone/RAID pages | Destination routes enforce project access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-10 | Elevation of Privilege | PmDashboardPage | high | mitigate | No client assignment filter; GET /api/dashboards/pm is the scope (D-12, Phase 16 API D-09, ASVS V4) |
| T-21-11 | Elevation of Privilege | PmActionQueues Link | high | mitigate | href from JSON only; do not concatenate project ids into new paths (D-10) |
| T-21-12 | Information Disclosure | CPMO on /dashboards/pm | medium | mitigate | UI does not switch to portfolio payload on this route (D-12) |
| T-21-SC | Tampering | npm installs | high | accept | No package install in this plan |

ASVS L1: PM dashboard authz remains withAuth + role + assignment on the existing route.
</threat_model>

<verification>
- 21-04-01: queues + hrefs + re-export
- 21-04-02: pm filters + visibility refetch
- 21-04-03: 401/403/5xx + mixed weekly
- Wave: npx vitest run --project jsdom modules/dashboards/ui/pm
</verification>

<success_criteria>
An assigned PM can open `/dashboards/pm`, see weekly/milestone/RAID queues with server deep links, filter them, and refetch after completing an action on the destination page (MDSH-06).
</success_criteria>

<output>
Create `.planning/phases/21-portfolio-pm-dashboard-pages/21-04-SUMMARY.md` when done
</output>
