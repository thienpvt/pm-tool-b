---
phase: 21-portfolio-pm-dashboard-pages
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - components/layout/Sidebar.tsx
  - components/layout/Sidebar.dashboard-nav.component.test.tsx
  - modules/dashboards/ui/shared/types.ts
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
  - app/dashboards/portfolio/page.tsx
autonomous: true
requirements: [PDSH-07, NIT-04]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO opens /dashboards/portfolio via a thin app re-export of the module page and sees title Spec dashboard (PDSH-07, D-01, D-02)."
    - "One GET /api/dashboards/portfolio loads the page; six KPI tiles bind active_count, on_track_count, watch_act_count, overdue_milestone_project_count, high_open_raid_count, technology_council_count (D-04, D-05)."
    - "KPI tiles always render six cells with numeric zero — never hidden when count is 0 (UI-SPEC empty portfolio-kpi-row)."
    - "Initial GET shows centered spinner + Loading dashboard… matching the existing portfolio loading shell (UI-SPEC loading portfolio-page-shell)."
    - "Six tiles show API numbers; grid is grid-cols-2 sm:grid-cols-3 xl:grid-cols-6 gap-4; tile labels truncate with title (UI-SPEC populated/overflow/zero-one-many portfolio-kpi-row)."
    - "Sidebar Spec dashboard is visible when roles includes cpmo; My dashboard when roles includes pm or cpmo; unauthorized roles never receive those links (D-03, D-09, UI-SPEC populated/error sidebar-nav-links)."
    - "NAV still renders from cached /api/auth/me while the dashboard body loads; new labels are short English Spec dashboard / My dashboard (UI-SPEC loading/long-text/overflow sidebar-nav-links)."
    - "data-testid spec-kpi-row contains none of the NIT-04 fiscal patterns; fiscal metrics stay off spec tiles (NIT-04, D-14, D-15)."
    - "Existing Portfolio / and Project Dashboard /dashboard routes stay as they are (D-02, D-03)."
  artifacts:
    - path: "vitest.config.ts"
      provides: "jsdom and node include globs collect modules/** tests (Wave 0)"
      contains: "modules"
    - path: "app/dashboards/portfolio/page.tsx"
      provides: "Thin use-client re-export of the module page (D-01, D-02)"
    - path: "modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx"
      provides: "Spec dashboard shell, loading copy, six KPI tiles (PDSH-07, D-05)"
    - path: "modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts"
      provides: "GET /api/dashboards/portfolio fetch hook (D-04)"
    - path: "modules/dashboards/ui/shared/types.ts"
      provides: "Payload types re-exported from Phase 16 service/kpi/filters"
    - path: "components/layout/Sidebar.tsx"
      provides: "Role-gated Spec dashboard and My dashboard NAV links (D-03)"
  key_links:
    - from: "app/dashboards/portfolio/page.tsx"
      to: "modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx"
      via: "default re-export"
      pattern: "modules/dashboards/ui/portfolio/PortfolioDashboardPage"
    - from: "usePortfolioSpecDashboard.ts"
      to: "/api/dashboards/portfolio"
      via: "fetch GET"
      pattern: "/api/dashboards/portfolio"
    - from: "components/layout/Sidebar.tsx"
      to: "/dashboards/portfolio"
      via: "Spec dashboard Link when roles includes cpmo"
      pattern: "/dashboards/portfolio"
  prohibitions:
    - statement: "Must not fetch the existing home portfolio collection endpoint from module UI (D-04)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not import the existing home-page dashboard hook (D-04)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages or change package.json (D-17)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not overwrite / or /dashboard (D-02)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO can open `/dashboards/portfolio` and see spec KPI tiles from GET `/api/dashboards/portfolio`, with role-gated Sidebar links and NIT-04 fiscal omission recorded (PDSH-07, NIT-04, D-01..D-05, D-14, D-15, D-16, D-17).

Purpose: Prove the module-UI + thin `app/` re-export + vitest `modules/**` glob end-to-end before filters, charts, export, and the PM page.

Output: Wave 0 vitest glob, tracer Spec dashboard, both NAV links, shared types, NIT-04 comment + KPI-row omission test.
</objective>

## Artifacts this plan produces

- Vitest jsdom/node globs include `modules/**`
- `modules/dashboards/ui/` greenfield tree (portfolio page + hook + types)
- Thin `app/dashboards/portfolio/page.tsx`
- Sidebar Spec dashboard + My dashboard links (D-03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-CONTEXT.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-RESEARCH.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md
@vitest.config.ts
@components/layout/Sidebar.tsx
@lib/dashboards/kpi.ts
@lib/dashboards/filters.ts
@lib/services/spec-dashboards.service.ts
@app/portfolio/report/page.component.test.tsx
@app/page.tsx
@app/api/dashboards/portfolio/route.ts

Planner-assigned locked IDs for this plan: D-01 module pages + thin re-export; D-02 URLs /dashboards/portfolio and /dashboards/pm; D-03 both NAV links; D-04 consume Phase 16 APIs only; D-05 six spec KPI tiles from one GET; D-14/D-15/NIT-04 omit fiscal tiles; D-16 Sidebar + Card; D-17 component tests with mocked fetch.

Do not implement filters, charts, drill-down clicks, export, or the PM page in this plan (21-02, 21-03, 21-04).

<interfaces>
From lib/dashboards/kpi.ts:
export type PortfolioKpis = {
  active_count: number;
  on_track_count: number;
  watch_act_count: number;
  overdue_milestone_project_count: number;
  high_open_raid_count: number;
  technology_council_count: number;
};
export type PortfolioCharts = {
  by_stage: Record<string, number>;
  by_rag: { green: number; amber: number; red: number };
};

From lib/dashboards/filters.ts:
export const DASHBOARD_FILTER_KEYS = ['portfolio_year','program','unit','pm_user_id','stage','status','rag','type','weekly_report_enabled'] as const;
export type DashboardFilters = Partial<Record<DashboardFilterKey, unknown>>;

From lib/services/spec-dashboards.service.ts getPortfolioDashboard return:
{ filters, kpis, charts, list, drilldowns: { overdue_milestones, high_raid, technology_council } }

KPI tile labels (21-UI-SPEC): Active projects, On track, Watch / act, Overdue milestones, High open RAID, Technology council.

Copywriting: page title Spec dashboard; loading Loading dashboard…

vitest.config.ts today: node include '{lib,app,eslint}/**/*.test.ts'; jsdom include '{components,app}/**/*.test.tsx' and '{components,app}/**/*.component.test.tsx'.

Sidebar NAV array lines 22-30; NAV.map at line 141; Admin Panel role gate at lines 220-234. LayoutDashboard and ClipboardList already imported.

app/page.tsx loading shell (lines 88-99): flex flex-col lg:flex-row min-h-screen bg-slate-50, Sidebar, centered spinner border-blue-500, text-slate-400 text-sm.

Card from @/components/ui/card with size="sm".
</interfaces>
</context>

<feature>
  <name>Spec dashboard KPI tracer with module re-export</name>
  <files>vitest.config.ts, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, app/dashboards/portfolio/page.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <behavior>
    After mocked GET 200, page shows Spec dashboard and six KPI labels with fixture numbers (PDSH-07, D-05).
    Before fetch settles, page shows Loading dashboard… (UI-SPEC loading portfolio-page-shell).
    npx vitest run --project jsdom on the new test file collects and executes it (Wave 0 glob).
  </behavior>
  <implementation>
    Extend vitest globs; hook fetches GET /api/dashboards/portfolio; page renders Sidebar + KPI row; app/ re-exports the module default (D-01, D-02).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="21-01-01">
  <name>End-to-end Spec dashboard KPI path through module UI</name>
  <reversibility rating="costly">Module UI plus /dashboards/portfolio URL become the v2.1 landing pattern Phase 22–24 will copy (D-01, D-02).</reversibility>
  <files>vitest.config.ts, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, app/dashboards/portfolio/page.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md (Pattern 1 re-export, Pattern 2 hook, Pattern 5 tests, Wave 0 glob)
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md (KPI tiles, Copywriting, page shell)
  - vitest.config.ts
  - lib/dashboards/kpi.ts
  - app/portfolio/report/page.component.test.tsx
  - app/page.tsx loading block
  - app/api/dashboards/portfolio/route.ts (withCpmo GET — consume only)
  </read_first>
  <behavior>
  - npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx collects this file (Wave 0).
  - Deferred fetch: Loading dashboard… is in the document before JSON arrives (UI-SPEC loading portfolio-page-shell).
  - GET 200: heading Spec dashboard, data-testid spec-kpi-row shows Active projects, On track, Watch / act, Overdue milestones, High open RAID, Technology council and numbers from the fixture (PDSH-07, D-05).
  - Six KPI cells remain visible when every count is 0 (UI-SPEC empty portfolio-kpi-row).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Merge RED and GREEN in this task.
  Wave 0 first: edit vitest.config.ts so the node include is the brace glob with lib, app, eslint, and modules for **/*.test.ts, and the jsdom include uses the brace glob with components, app, and modules for both **/*.test.tsx and **/*.component.test.tsx. Keep setupFiles and the lib/log.test.ts exclude. Do not add packages. Do not pass watch flags to vitest.
  RED: add modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx. Mock next/navigation usePathname to /dashboards/portfolio. Mock @/components/layout/Sidebar as a nav with data-testid sidebar. Stub fetch: when url is /api/dashboards/portfolio (no method or GET), return 200 JSON with filters {}, kpis { active_count: 3, on_track_count: 2, watch_act_count: 1, overdue_milestone_project_count: 0, high_open_raid_count: 0, technology_council_count: 0 }, charts.by_stage keys L0 through L5 at 0, charts.by_rag green/amber/red 0, list [], drilldowns overdue_milestones/high_raid/technology_council []. Use a deferred Promise for the loading case. Assert sidebar testid, title Spec dashboard, spec-kpi-row labels and numbers. Tests must fail because the page module is missing. Commit test(21-01): red spec dashboard kpi tracer.
  GREEN: create usePortfolioSpecDashboard with useState data/loading/error, useCallback load that fetch GET /api/dashboards/portfolio, status 401 sets error unauthorized and data null (in-page only; do not assign window.location), 403 sets forbidden, other !ok sets load_failed, 200 sets data from res.json(). useEffect calls load on mount. Create PortfolioDashboardPage: flex flex-col lg:flex-row min-h-screen bg-slate-50, Sidebar, main flex-1. While loading, centered spinner (w-8 h-8 border-2 border-blue-500 border-t-transparent rounded-full animate-spin) and Loading dashboard…. After load with data, h1 Spec dashboard and a div data-testid spec-kpi-row with class grid grid-cols-2 sm:grid-cols-3 xl:grid-cols-6 gap-4 containing six Card size=sm tiles (min height 72px) bound to the six PortfolioKpis fields and UI-SPEC labels. Tile number text-3xl font-semibold; label text-xs font-semibold text-slate-600 truncate with title. Keep clickable drill-down wiring for 21-03. Create app/dashboards/portfolio/page.tsx with use client and export { default } from the module page (D-01, D-02). Hook and page must fetch /api/dashboards/portfolio only. Do not import the home-page dashboard hook. Do not call computePortfolioKpis or getPortfolioDashboard from the client (D-04). Do not edit app/page.tsx or app/projects/[id]/dashboard/page.tsx. Commit feat(21-01): spec dashboard kpi tracer.
  REFACTOR only if tests stay green.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>CPMO-facing Spec dashboard renders six spec KPI tiles from mocked GET /api/dashboards/portfolio; vitest collects the module test (PDSH-07, D-01, D-02, D-04, D-05).</done>
  <acceptance_criteria>
  - vitest.config.ts jsdom and node includes contain modules
  - app/dashboards/portfolio/page.tsx re-exports PortfolioDashboardPage
  - usePortfolioSpecDashboard fetch URL is /api/dashboards/portfolio
  - npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-01-02">
  <name>Sidebar Spec dashboard and My dashboard links</name>
  <reversibility rating="costly">NAV hrefs /dashboards/portfolio and /dashboards/pm are the published UI entry points (D-02, D-03).</reversibility>
  <files>components/layout/Sidebar.tsx, components/layout/Sidebar.dashboard-nav.component.test.tsx</files>
  <read_first>
  - components/layout/Sidebar.tsx (NAV array, NAV.map, Admin Panel role gate, me from /api/auth/me)
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md (Pattern 4)
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md (Routes and NAV table)
  </read_first>
  <behavior>
  - me.roles includes cpmo: links Spec dashboard -> /dashboards/portfolio and My dashboard -> /dashboards/pm are in the document (D-03).
  - me.roles includes pm only: My dashboard present; Spec dashboard absent.
  - me.roles is viewer or missing: neither spec dashboard link is present (UI-SPEC error sidebar-nav-links).
  - pathname /dashboards/portfolio applies existing active classes bg-blue-600 text-white on Spec dashboard (UI-SPEC populated sidebar-nav-links).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: add components/layout/Sidebar.dashboard-nav.component.test.tsx (jsdom already includes components/**). Mock next/navigation usePathname (overridable per test) and useRouter. Stub fetch: /api/auth/me returns { username, display_name, company_name, is_admin: 0, roles }, /api/projects returns []. Render default Sidebar. Cover the four cases in behavior. Tests fail until links exist. Commit test(21-01): red sidebar spec and my dashboard nav.
  GREEN: split the static NAV array so Portfolio, Portfolio Roadmap, and Portfolio Report stay in the first group and Portfolio Budget through Programs stay in the second group. After mapping the first group, render Spec dashboard (LayoutDashboard) when me?.roles?.includes('cpmo'), then My dashboard (ClipboardList) when roles includes pm or cpmo, then map the second group. Match existing Link className and onNavClick. Insert before the Projects collapsible so visual order is Report, then the two new links, then Budget (D-03, 21-UI-SPEC). Do not change href / or /dashboard. Do not hide Admin Panel. English labels only (Claude discretion, Sidebar neighbors). Commit feat(21-01): sidebar spec and my dashboard links.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom components/layout/Sidebar.dashboard-nav.component.test.tsx</automated>
  </verify>
  <done>CPMO sees Spec dashboard; PM and CPMO see My dashboard; viewer sees neither; existing Portfolio and Project Dashboard entries remain (D-03, D-02).</done>
  <acceptance_criteria>
  - Sidebar.tsx contains href /dashboards/portfolio and /dashboards/pm
  - Spec dashboard is gated on roles includes cpmo
  - My dashboard is gated on roles includes pm or cpmo
  - npx vitest run --project jsdom components/layout/Sidebar.dashboard-nav.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-01-03">
  <name>NIT-04 KPI-row omission and shared payload types</name>
  <files>modules/dashboards/ui/shared/types.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md (NIT-04 section, Implementation Notes item 3)
  - lib/dashboards/kpi.ts
  - lib/services/spec-dashboards.service.ts (PortfolioDashboardListRow and GET return shapes, including getPmDashboard return)
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx from 21-01-01
  </read_first>
  <behavior>
  - data-testid spec-kpi-row textContent matches none of /budget|ROI|benefit|\$|₫|VND/i (NIT-04, D-14, D-15).
  - spec-kpi-row still has exactly six tiles after the omission test (UI-SPEC NIT-04 backstop).
  - modules/dashboards/ui/shared/types.ts re-exports PortfolioKpis, PortfolioCharts, DashboardFilters, PortfolioDashboardListRow and declares PortfolioDashboardPayload and PmDashboardPayload matching Phase 16 JSON.
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: extend PortfolioDashboardPage.component.test.tsx so a 200 render asserts spec-kpi-row (not the whole page) fails the NIT-04 character class in 21-UI-SPEC NIT-04 Test gate. Keep an optional muted footnote outside spec-kpi-row so the page may name the existing fiscal route without failing the row assertion (D-14). Commit test(21-01): red nit-04 kpi row omission.
  GREEN: copy 21-UI-SPEC Implementation Notes item 3 onto PortfolioDashboardPage.tsx as a file comment. Optional single-line footnote outside spec-kpi-row using text-xs text-muted-foreground per that NIT-04 Redirect note. Do not fetch /api/portfolio/budgets. Do not add fields to PortfolioKpis (D-15). Create modules/dashboards/ui/shared/types.ts that re-exports PortfolioKpis and PortfolioCharts from @/lib/dashboards/kpi, DashboardFilters from @/lib/dashboards/filters, PortfolioDashboardListRow from @/lib/services/spec-dashboards.service, and exports PortfolioDashboardPayload and PmDashboardPayload matching getPortfolioDashboard / getPmDashboard JSON (filters, kpis, charts, list, drilldowns; filters, projects, actions.weekly|milestones|raid). Switch the hook to import those types. Commit feat(21-01): nit-04 omission and shared types.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Spec KPI row omits fiscal tiles with NIT-04 recorded; shared types match Phase 16 payloads (NIT-04, D-14, D-15).</done>
  <acceptance_criteria>
  - PortfolioDashboardPage.tsx contains the NIT-04 file comment from UI-SPEC Implementation Notes item 3
  - spec-kpi-row test covers the NIT-04 omission regex
  - types.ts exists and is imported by the hook
  - npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser module UI → GET /api/dashboards/portfolio | Session cookie; CPMO-only payload crosses here |
| Sidebar NAV → page URL | Hiding a link is UX only; API remains the authz truth (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Elevation of Privilege | PortfolioDashboardPage / Sidebar | high | mitigate | NAV hide is not policy; 401/403 handled in-page in 21-02; this plan still fetch-only and relies on withCpmo (D-09, ASVS V4) |
| T-21-02 | Information Disclosure | spec-kpi-row | medium | mitigate | NIT-04: no fiscal tiles and no budgets endpoint from this page (D-14, D-15) |
| T-21-03 | Tampering | usePortfolioSpecDashboard | medium | mitigate | Consume Phase 16 GET only; do not recompute KPIs in the browser (D-04) |
| T-21-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-17) |

ASVS L1: high client→API authz stays on withCpmo; this plan does not add a second policy engine.
</threat_model>

<verification>
- 21-01-01: KPI tracer + glob green
- 21-01-02: Sidebar role links green
- 21-01-03: NIT-04 KPI-row omission + types green
- Wave: npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx components/layout/Sidebar.dashboard-nav.component.test.tsx
- package.json and package-lock.json unchanged
</verification>

<success_criteria>
A CPMO can open `/dashboards/portfolio` (module UI + thin re-export) and see six spec KPI tiles from the Phase 16 GET; Sidebar exposes Spec dashboard and My dashboard by role; fiscal tiles stay off the spec KPI row (PDSH-07, NIT-04, D-01..D-05, D-14, D-15).
</success_criteria>

<output>
Create `.planning/phases/21-portfolio-pm-dashboard-pages/21-01-SUMMARY.md` when done
</output>
