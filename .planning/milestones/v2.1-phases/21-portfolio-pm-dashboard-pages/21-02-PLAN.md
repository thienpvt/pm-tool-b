---
phase: 21-portfolio-pm-dashboard-pages
plan: 02
type: tdd
wave: 2
depends_on: [21-01]
files_modified:
  - modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
  - modules/dashboards/ui/portfolio/PortfolioFiltersBar.component.test.tsx
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
autonomous: true
requirements: [PDSH-07]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Filter chrome binds AND keys portfolio_year, program, unit, pm_user_id, stage, status, rag, type, weekly_report_enabled; Apply PUT /api/dashboards/portfolio/filters then refetches dashboard GET (PDSH-07, D-06)."
    - "Clear filters POSTs { action: 'clear' } and Reset defaults POSTs { action: 'defaults' } then refetch GET (D-06)."
    - "Loaded filters from GET pre-populate controls on mount; omitted keys mean no constraint (UI-SPEC populated/partial portfolio-filter-bar)."
    - "Apply is disabled while a refetch is in-flight; the page does not full-page-block on filter-only refresh (UI-SPEC loading portfolio-filter-bar)."
    - "Failed PUT/POST filters shows Couldn't save filters — try again. and retains prior filter state (UI-SPEC error portfolio-filter-bar)."
    - "Filter controls wrap (flex-wrap); program/PM closed-select options truncate (UI-SPEC overflow/long-text portfolio-filter-bar)."
    - "401 shows Session expired — refresh the page and sign in again.; 403 shows You don't have access to this dashboard.; 5xx shows Couldn't load the dashboard. Try again. in a centered main panel with no uncaught throw (D-09, UI-SPEC error portfolio-page-shell)."
    - "Viewer never gets a second client authz layer; 403 from the API is the policy truth (D-09)."
  artifacts:
    - path: "modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx"
      provides: "AND filter controls + Apply / Clear / Reset (D-06, D-16)"
    - path: "modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts"
      provides: "saveFilters PUT, clear/defaults POST, refreshing vs initial loading"
    - path: "modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx"
      provides: "Filter bar wired; 401/403/5xx in-page panels (D-09)"
  key_links:
    - from: "usePortfolioSpecDashboard.ts"
      to: "/api/dashboards/portfolio/filters"
      via: "PUT apply and POST action clear|defaults"
      pattern: "/api/dashboards/portfolio/filters"
    - from: "PortfolioFiltersBar.tsx"
      to: "usePortfolioSpecDashboard saveFilters"
      via: "Apply filters button"
      pattern: "Apply filters"
  prohibitions:
    - statement: "Must not persist filters in localStorage (D-06)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not redirect 403 like admin; in-page forbidden only (D-09)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages (D-17)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Portfolio Spec dashboard AND filters persist through the Phase 16 filter routes and refetch GET; 401/403/5xx use Copywriting Contract strings in-page (PDSH-07, D-06, D-09, D-16).

Purpose: Expand the 21-01 tracer so CPMO can constrain the dashboard and see forbidden/error states.

Output: PortfolioFiltersBar, hook persist methods, error/loading-refresh behavior, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-CONTEXT.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-RESEARCH.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-01-PLAN.md
@modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
@app/api/dashboards/portfolio/filters/route.ts
@lib/dashboards/filter-schema.ts
@lib/dashboards/filters.ts
@app/programs/page.tsx

<interfaces>
PUT /api/dashboards/portfolio/filters body: dashboardFiltersSchema strict object with optional portfolio_year number, program number, unit string, pm_user_id number, stage string, status string, rag string, type string, weekly_report_enabled boolean.
POST body: { action: 'clear' | 'defaults' } — both call clearPortfolioDashboardFilters on the server; UI still exposes both buttons (21-UI-SPEC).
GET dashboard payload.filters is the stored blob used to pre-populate (same as GET /filters).

Copywriting: Apply filters, Clear filters, Reset defaults, Filter save error toast Couldn't save filters — try again.
401 Session expired — refresh the page and sign in again.
403 You don't have access to this dashboard.
5xx Couldn't load the dashboard. Try again.

Native select h-8 text-sm (21-UI-SPEC allows native select). FieldRow: space-y-1.5, Label text-xs font-semibold text-slate-600 (programs page analog).
Button primary bg-blue-600 hover:bg-blue-700 h-8.
toast from sonner.

unit is a string control that still PUTs (D-06). Server matchesFilter for unit is a no-op; still bind the key.

Hook must distinguish initial loading (full-page spinner from 21-01) from refreshing (Apply disabled, page stays).
</interfaces>
</context>

<feature>
  <name>Portfolio AND filters persist and refetch</name>
  <files>modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx</files>
  <behavior>
    Apply filters → PUT JSON of set keys then GET /api/dashboards/portfolio again (D-06).
    Clear / Reset POST then GET again.
    403 → You don't have access to this dashboard. (D-09).
  </behavior>
  <implementation>
    PortfolioFiltersBar + hook saveFilters/clearFilters; page wires bar and error panel.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="21-02-01">
  <name>AND filter bar Apply PUT then refetch GET</name>
  <precondition>app/api/dashboards/portfolio/filters/route.ts exists and exports PUT (Phase 16)</precondition>
  <files>modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx, modules/dashboards/ui/portfolio/PortfolioFiltersBar.component.test.tsx, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - app/api/dashboards/portfolio/filters/route.ts
  - lib/dashboards/filter-schema.ts
  - .planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md (Filter bar, Copywriting)
  - app/programs/page.tsx FieldRow analog
  </read_first>
  <behavior>
  - Apply filters with stage L2 calls PUT /api/dashboards/portfolio/filters with JSON containing stage L2 then GET /api/dashboards/portfolio (D-06).
  - Controls exist for portfolio_year, program, unit, pm_user_id, stage, status, rag, type, weekly_report_enabled (D-06).
  - Mount with payload.filters.stage L2 pre-selects Stage (UI-SPEC populated portfolio-filter-bar).
  - Apply is disabled while refreshing is true (UI-SPEC loading portfolio-filter-bar).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Isolation none.
  RED: add PortfolioFiltersBar.component.test.tsx rendering the bar with fixture filters and onApply. Extend PortfolioDashboardPage.component.test.tsx: stub PUT /api/dashboards/portfolio/filters 200 then a second GET; fireEvent click Apply filters after setting stage; expect fetch called with method PUT and body containing stage. Tests fail until the bar and saveFilters exist. Commit test(21-02): red portfolio filter apply persist.
  GREEN: extend usePortfolioSpecDashboard: saveFilters(filters) PUT Content-Type application/json then await load(). load() on subsequent calls sets refreshing true without flipping initial loading back to a full-page spinner. Export { data, loading, refreshing, error, load, saveFilters }. Create PortfolioFiltersBar: flex flex-wrap gap-2 items-end inside Card size=sm. Native select/input h-8 for all nine AND keys (D-06). program and pm_user_id options derived from data.list unique customer_id/program_name and pm_user_id/pm_name plus an All empty option. stage always L0-L5. rag green/amber/red. weekly_report_enabled All/Yes/No mapping omit/true/false. unit is a text input (schema string). Apply filters Button bg-blue-600 hover:bg-blue-700 h-8 disabled when disabled or refreshing. Wire the bar on the page under the header. Do not use localStorage. Do not fetch the home portfolio collection. Commit feat(21-02): portfolio filter apply persist.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioFiltersBar.component.test.tsx modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Apply filters persists AND keys via PUT then refetches the dashboard GET (PDSH-07, D-06).</done>
  <acceptance_criteria>
  - PortfolioFiltersBar.tsx renders all nine DASHBOARD_FILTER_KEYS controls
  - saveFilters PUT then load()
  - npx vitest run --project jsdom on the two test files exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-02-02">
  <name>Clear/Reset POST and filter save error toast</name>
  <files>modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioFiltersBar.component.test.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - app/api/dashboards/portfolio/filters/route.ts POST
  - 21-UI-SPEC Copywriting Contract filter strings
  </read_first>
  <behavior>
  - Clear filters POSTs { action: 'clear' } to /api/dashboards/portfolio/filters then GET dashboard (D-06).
  - Reset defaults POSTs { action: 'defaults' } then GET dashboard (D-06).
  - Failed PUT calls toast.error with Couldn't save filters — try again. and does not replace data.filters (UI-SPEC error portfolio-filter-bar).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: mock sonner toast.error. Click Clear filters and Reset defaults; assert POST bodies. Stub PUT 500 and click Apply; expect toast.error and prior stage still selected. Commit test(21-02): red filter clear defaults and save error.
  GREEN: add clearFilters(action: 'clear' | 'defaults') POST JSON then load(). On !res.ok for PUT or POST, toast.error with the Copywriting filter save string and return without clobbering data. Wire Clear filters and Reset defaults buttons (not primary). Wrap controls so they do not clip (flex-wrap). Commit feat(21-02): filter clear defaults and save errors.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioFiltersBar.component.test.tsx modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Clear/Reset POST then refetch; failed persist toasts and keeps prior filters (D-06).</done>
  <acceptance_criteria>
  - POST action clear and defaults covered
  - toast.error on failed persist
  - npx vitest run --project jsdom on the two test files exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-02-03">
  <name>In-page 401/403/5xx panels without a second authz layer</name>
  <files>modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts</files>
  <read_first>
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - 21-UI-SPEC Auth and forbidden + Copywriting error strings
  - 21-CONTEXT D-09
  </read_first>
  <behavior>
  - GET 403: main shows You don't have access to this dashboard. and no KPI row (D-09, UI-SPEC error portfolio-page-shell).
  - GET 401: Session expired — refresh the page and sign in again. (no window.location assign).
  - GET 500: Couldn't load the dashboard. Try again.
  - Hook never throws; error state is a string union (UI-SPEC no uncaught throw).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: page tests for 401, 403, and 500 fetch status asserting the three Copywriting strings and that spec-kpi-row is absent. Commit test(21-02): red portfolio forbidden and error panels.
  GREEN: when error is unauthorized/forbidden/load_failed, render Sidebar + centered main panel with muted icon (lucide AlertTriangle already used in-app) and the matching Copywriting string. Do not redirect to /admin or /login. Do not inspect me.roles to skip the GET (D-09). Keep fetch on mount for every visitor of the URL. Commit feat(21-02): portfolio in-page forbidden and errors.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Non-CPMO 403 and other load failures show in-page Copywriting; API remains the authz truth (D-09).</done>
  <acceptance_criteria>
  - 401/403/5xx strings from 21-UI-SPEC Copywriting Contract are asserted
  - Page still calls GET /api/dashboards/portfolio without a client role short-circuit
  - npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PUT/POST /api/dashboards/portfolio/filters | Session cookie; Zod on the route validates the blob |
| Browser → GET dashboard | 403 is the CPMO gate (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Elevation of Privilege | PortfolioDashboardPage | high | mitigate | No client role skip; 403 panel only (D-09, ASVS V4) |
| T-21-05 | Spoofing | Filter PUT | medium | mitigate | Same-origin cookie; Zod dashboardFiltersSchema on the existing route (ASVS V5); UI sends only DASHBOARD_FILTER_KEYS |
| T-21-06 | Tampering | Filter persist | low | accept | Server parseDashboardFilters rejects unknown keys; UI cannot widen the schema |
| T-21-SC | Tampering | npm installs | high | accept | No package install in this plan |

ASVS L1: high authz on the dashboard GET stays server-side.
</threat_model>

<verification>
- 21-02-01: Apply PUT + refetch
- 21-02-02: Clear/Reset + toast
- 21-02-03: 401/403/5xx panels
- Wave: npx vitest run --project jsdom modules/dashboards/ui/portfolio
</verification>

<success_criteria>
CPMO can apply, clear, and reset AND filters against the Phase 16 filter API; forbidden and error states use in-page Copywriting (PDSH-07, D-06, D-09).
</success_criteria>

<output>
Create `.planning/phases/21-portfolio-pm-dashboard-pages/21-02-SUMMARY.md` when done
</output>
