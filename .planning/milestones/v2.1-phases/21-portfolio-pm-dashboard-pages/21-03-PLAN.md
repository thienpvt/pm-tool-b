---
phase: 21-portfolio-pm-dashboard-pages
plan: 03
type: tdd
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - modules/dashboards/ui/portfolio/PortfolioKpiTiles.tsx
  - modules/dashboards/ui/portfolio/PortfolioKpiTiles.component.test.tsx
  - modules/dashboards/ui/portfolio/PortfolioCharts.tsx
  - modules/dashboards/ui/portfolio/PortfolioCharts.component.test.tsx
  - modules/dashboards/ui/portfolio/PortfolioDrilldownTable.tsx
  - modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx
  - modules/dashboards/ui/shared/downloadBlob.ts
  - modules/dashboards/ui/shared/downloadBlob.test.ts
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
autonomous: true
requirements: [PDSH-07, NIT-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Overdue milestones, High open RAID, and Technology council tiles are clickable; click opens the matching drilldowns list; only one panel at a time; clicking the active tile collapses it (PDSH-07, D-07, UI-SPEC zero-one-many/loading/error portfolio-drilldown-panel)."
    - "Active drill-down with zero rows shows No items in this drill-down and The selected KPI has zero matching rows for the current filters. (UI-SPEC empty portfolio-drilldown-panel)."
    - "Drill-down rows link via href or id fields to project/milestone/RAID screens; missing ids omit the link (D-07, UI-SPEC populated/partial portfolio-drilldown-panel)."
    - "By stage shows L0–L5 CSS bars; By RAG shows green/amber/red bars; missing stage keys count as 0; zero counts still show labels with zero-width bars (UI-SPEC empty/populated/partial portfolio-charts)."
    - "Project list columns Name (link /projects/{id}), Code, Program, Stage, Status, RAG Badge, PM, Progress %; empty body uses No projects match these filters spanning columns (PDSH-07, UI-SPEC empty/populated portfolio-project-list)."
    - "Table wrappers overflow-x-auto; name cells truncate max-w-[200px] with title (UI-SPEC overflow/long-text list and drill-down)."
    - "Export Excel and Export PDF POST /api/dashboards/portfolio/export with format xlsx|pdf, download via downloadBlob using PORTFOLIO_EXPORT_FILENAME, toast Export downloaded or Export failed — try again. (D-08)."
    - "Export buttons disable while the POST is in-flight and stay enabled when the list is empty (UI-SPEC loading/empty/populated portfolio-export-actions)."
    - "data-testid spec-kpi-row still omits NIT-04 fiscal patterns after tile extract (NIT-04)."
  artifacts:
    - path: "modules/dashboards/ui/portfolio/PortfolioKpiTiles.tsx"
      provides: "Six tiles; three drill-down keys (D-05, D-07)"
    - path: "modules/dashboards/ui/portfolio/PortfolioCharts.tsx"
      provides: "CSS stage and RAG bars (21-UI-SPEC Charts)"
    - path: "modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx"
      provides: "Filtered project table (D-05, D-16)"
    - path: "modules/dashboards/ui/portfolio/PortfolioDrilldownTable.tsx"
      provides: "Drill-down tables + deep links (D-07)"
    - path: "modules/dashboards/ui/shared/downloadBlob.ts"
      provides: "createObjectURL click + revokeObjectURL (D-08)"
    - path: "modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts"
      provides: "exportDashboard POST blob download (D-08)"
  key_links:
    - from: "PortfolioKpiTiles.tsx"
      to: "drilldowns.overdue_milestones|high_raid|technology_council"
      via: "tile click sets activeKey"
      pattern: "overdue_milestones"
    - from: "usePortfolioSpecDashboard.ts"
      to: "/api/dashboards/portfolio/export"
      via: "POST { format }"
      pattern: "/api/dashboards/portfolio/export"
    - from: "downloadBlob.ts"
      to: "URL.revokeObjectURL"
      via: "after anchor click"
      pattern: "revokeObjectURL"
  prohibitions:
    - statement: "Must not generate xlsx/pdf in the browser (D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add a chart npm package; CSS bars per 21-UI-SPEC Charts"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not invent drill-down URLs when no id/href is present (D-07)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Spec dashboard shows stage/RAG charts, filtered project list, tile drill-downs with deep links, and server xlsx/pdf export (PDSH-07, D-05, D-07, D-08, NIT-04).

Purpose: Complete PDSH-07 beyond KPI tiles and filters.

Output: KPI extract, CSS charts, tables, downloadBlob, export hook methods, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-CONTEXT.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-RESEARCH.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-PATTERNS.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-UI-SPEC.md
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
@modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
@lib/export/dashboard-portfolio.ts
@app/api/dashboards/portfolio/export/route.ts
@components/ui/table.tsx
@app/projects/[id]/page.tsx

<interfaces>
PORTFOLIO_EXPORT_FILENAME = { xlsx: 'portfolio-dashboard.xlsx', pdf: 'portfolio-dashboard.pdf' } from @/lib/export/dashboard-portfolio
POST /api/dashboards/portfolio/export body { format: 'xlsx' | 'pdf' } returns blob; Content-Disposition filename from server.

Drill-down keys: overdue_milestones, high_raid, technology_council.
Overdue rows: project_id, id or milestone_id, name, project_name. Link when project_id present to /projects/{project_id}/milestones.
high_raid: project_id, id, entity_type, code. Link to /projects/{project_id}/raid when project_id present.
technology_council: project_id, id. Link to /projects/{project_id}/raid when project_id present.
Do not invent href for PM queues here (PM is 21-04). D-07 allows id-based links on portfolio drill-down.

Charts: CSS only. Bar height 8px, gap 8px. Stage order L0 L1 L2 L3 L4 L5. RAG colors bg-green-100 / bg-amber-100 / bg-red-100 (21-UI-SPEC). Do not import recharts in modules/dashboards/ui (UI-SPEC Charts; Claude discretion allowed CSS).

Table: Table, TableHeader, TableBody, TableHead h-8 px-2 text-xs, TableCell p-2 text-sm. Name Link text-blue-600 hover:underline truncate max-w-[200px] title={name}.
RAG Badge: bg-green-100 text-green-700 / amber / red per UI-SPEC Color.

Copywriting: Export Excel, Export PDF, Export downloaded, Export failed — try again., Exporting… optional on buttons.
Empty list heading No projects match these filters body Adjust or clear filters to see projects in the portfolio.

downloadBlob(blob, filename): createObjectURL, anchor download, click, revokeObjectURL (app/projects/[id]/page.tsx analog).
</interfaces>
</context>

<feature>
  <name>Charts, drill-downs, list, and export</name>
  <files>modules/dashboards/ui/portfolio/PortfolioKpiTiles.tsx, modules/dashboards/ui/portfolio/PortfolioCharts.tsx, modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx, modules/dashboards/ui/portfolio/PortfolioDrilldownTable.tsx, modules/dashboards/ui/shared/downloadBlob.ts, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts</files>
  <behavior>
    Click Overdue milestones opens drill-down table from drilldowns.overdue_milestones (D-07).
    CSS By stage has L0–L5 labels.
    Export Excel POSTs format xlsx and calls downloadBlob (D-08).
  </behavior>
  <implementation>
    Extract tiles; CSS charts; shadcn tables; hook exportDashboard using existing export route.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="21-03-01">
  <name>Clickable KPI tiles and drill-down panel</name>
  <files>modules/dashboards/ui/portfolio/PortfolioKpiTiles.tsx, modules/dashboards/ui/portfolio/PortfolioKpiTiles.component.test.tsx, modules/dashboards/ui/portfolio/PortfolioDrilldownTable.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - 21-UI-SPEC KPI tiles + drill-down tables + Copywriting empty drill-down
  - lib/services/spec-dashboards.service.ts drilldowns assembly
  </read_first>
  <behavior>
  - Click Overdue milestones sets the panel title and renders fixture overdue rows; ring-2 ring-blue-600 on that tile (D-07).
  - Click the same tile again hides the panel (UI-SPEC zero-one-many drill-down).
  - Zero-row overdue fixture shows No items in this drill-down (UI-SPEC empty drill-down).
  - Overdue row with project_id 10 has a link to /projects/10/milestones; row without project_id has no that link (D-07).
  - spec-kpi-row still matches the NIT-04 omission regex (NIT-04).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: PortfolioKpiTiles.component.test.tsx and page tests for click, toggle, empty copy, and id-based Link. Include a 120-char name fixture asserting truncate + title on the drill-down description cell (UI-SPEC long-text drill-down backstop). Commit test(21-03): red kpi drill-down panel.
  GREEN: extract PortfolioKpiTiles from the page. Props: kpis, activeKey, onSelect. First three tiles not clickable. Last three cursor-pointer hover:bg-slate-50; selected ring-2 ring-blue-600. Active projects / On track / Watch / act have optional subtitle project vs projects when count !== 1. PortfolioDrilldownTable: hidden when activeKey is null. Use drilldowns[activeKey] from the GET payload — no extra fetch (UI-SPEC loading/error drill-down). shadcn Table overflow from Table wrapper. Link only when project_id (or href) is present. Wire on the page. Drill-down errors stay on the page-shell error from 21-02. Commit feat(21-03): kpi drill-down panel.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioKpiTiles.component.test.tsx modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Three drill-down tiles open the matching GET lists with id-based deep links (PDSH-07, D-07).</done>
  <acceptance_criteria>
  - PortfolioKpiTiles and PortfolioDrilldownTable exist
  - Only one activeKey at a time
  - npx vitest run --project jsdom on those tests exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-03-02">
  <name>CSS stage and RAG charts from GET charts</name>
  <files>modules/dashboards/ui/portfolio/PortfolioCharts.tsx, modules/dashboards/ui/portfolio/PortfolioCharts.component.test.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx</files>
  <read_first>
  - 21-UI-SPEC Charts and Color semantic RAG
  - lib/dashboards/kpi.ts STAGE_BUCKETS L0–L5 and by_rag
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  </read_first>
  <behavior>
  - By stage lists L0 through L5 even when by_stage is {} (counts 0) (UI-SPEC empty/partial charts).
  - By RAG lists Green, Amber, Red.
  - A fixture with by_stage.L2 = 4 renders a bar whose width is proportional to that count (UI-SPEC populated charts).
  </behavior>
  <action>
  TDD. Merge RED and GREEN.
  RED: PortfolioCharts.component.test.tsx asserts six stage labels, three RAG labels, and a style/width that is 0 when count is 0 and non-zero when L2 is 4. Commit test(21-03): red css stage and rag charts.
  GREEN: two Card size=sm side by side (stack on small screens). CSS horizontal bars height 8px gap 8px; label text-xs left, count right. Width percent = count / max(counts, 1). RAG bar colors per UI-SPEC semantic tokens. Do not import a chart library in this file (21-UI-SPEC Charts; D-17 no new packages). Wire below the KPI row. Commit feat(21-03): css stage and rag charts.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioCharts.component.test.tsx</automated>
  </verify>
  <done>Stage and RAG charts render from charts.by_stage / charts.by_rag with CSS bars (PDSH-07, D-05).</done>
  <acceptance_criteria>
  - PortfolioCharts.tsx has no chart-library import
  - L0–L5 and three RAG labels always render
  - npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioCharts.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="21-03-03">
  <name>Project table and server export blob download</name>
  <precondition>app/api/dashboards/portfolio/export/route.ts exists and exports POST (Phase 16)</precondition>
  <files>modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx, modules/dashboards/ui/shared/downloadBlob.ts, modules/dashboards/ui/shared/downloadBlob.test.ts, modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - lib/export/dashboard-portfolio.ts PORTFOLIO_EXPORT_FILENAME
  - app/api/dashboards/portfolio/export/route.ts
  - app/projects/[id]/page.tsx exportExcel blob block
  - components/ui/table.tsx
  - 21-UI-SPEC Tables, Export, Copywriting
  </read_first>
  <behavior>
  - Empty list: No projects match these filters in a cell spanning all columns (UI-SPEC empty project list).
  - Populated row name links to /projects/{id} with truncate max-w-[200px] and title of the full name (UI-SPEC long-text backstop).
  - Export Excel POSTs { format: 'xlsx' } to /api/dashboards/portfolio/export; downloadBlob is called with portfolio-dashboard.xlsx (D-08).
  - Failed export calls toast.error Export failed — try again. and re-enables buttons (UI-SPEC error export).
  - Export buttons remain enabled when list is [] (UI-SPEC empty export).
  - downloadBlob always calls revokeObjectURL after click.
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Task file set is the production files plus tests; keep the page test in PortfolioDashboardPage.component.test.tsx.
  RED: downloadBlob.test.ts stubs document.createElement and URL.createObjectURL/revokeObjectURL. Page tests: empty copy; 200-char project name has truncate and title; click Export Excel with mocked blob 200; click Export PDF; click Export Excel with 500 and toast.error. Buttons disabled while a hanging POST is in-flight. Commit test(21-03): red project table and export download.
  GREEN: PortfolioProjectTable with columns Name, Code, Program, Stage, Status, RAG (Badge semantic tokens), PM, Progress %. Header row Export Excel / Export PDF in the page header, right-aligned, disabled when exporting. exportDashboard(format) POST JSON { format }, if !ok toast.error Copywriting export error; else blob = res.blob(), downloadBlob(blob, PORTFOLIO_EXPORT_FILENAME[format]), toast.success Export downloaded. Optional inline Exporting… on the in-flight button. Do not import exceljs or jspdf in modules/dashboards/ui (D-08). Do not add packages. Commit feat(21-03): project table and export download.
  </action>
  <verify>
    <automated>npx vitest run modules/dashboards/ui/shared/downloadBlob.test.ts modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</automated>
  </verify>
  <done>Filtered project list and server xlsx/pdf export work from the Spec dashboard (PDSH-07, D-08).</done>
  <acceptance_criteria>
  - downloadBlob calls revokeObjectURL
  - exportDashboard POSTs /api/dashboards/portfolio/export
  - Both automated vitest commands exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/dashboards/portfolio/export | CPMO session; binary blob returns |
| Drill-down links → project routes | Destination routes enforce project access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Elevation of Privilege | Drill-down Link href | high | mitigate | Links only from server-filtered ids; omit when id missing; destination withProjectAccess (D-07, ASVS V4) |
| T-21-08 | Tampering | Export buttons | medium | mitigate | POST existing export route; no client workbook/pdf library (D-08) |
| T-21-09 | Information Disclosure | KPI tiles after extract | medium | mitigate | NIT-04 regex still scoped to spec-kpi-row (D-14) |
| T-21-SC | Tampering | npm installs | high | accept | No package install; CSS charts not a new dependency |

ASVS L1: export and drill-down stay on existing server gates.
</threat_model>

<verification>
- 21-03-01: drill-down + NIT-04 still on KPI row
- 21-03-02: CSS charts
- 21-03-03: table + export + revokeObjectURL
- Wave: npx vitest run modules/dashboards/ui/portfolio modules/dashboards/ui/shared/downloadBlob.test.ts
</verification>

<success_criteria>
CPMO sees spec charts, project list, drill-downs, and can download xlsx/pdf from the existing export API (PDSH-07, D-07, D-08, NIT-04).
</success_criteria>

<output>
Create `.planning/phases/21-portfolio-pm-dashboard-pages/21-03-SUMMARY.md` when done
</output>
