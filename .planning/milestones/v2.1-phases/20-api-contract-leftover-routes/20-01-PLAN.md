---
phase: 20-api-contract-leftover-routes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - proxy.ts
  - lib/http/proxy.auth.test.ts
autonomous: true
requirements: [PROXY-01]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Unauthenticated request whose pathname starts with /api/ (not Accept header) receives JSON { error: 'Unauthorized' } status 401, same string as withAuth (PROXY-01, D-01, D-02)."
    - "Unauthenticated non-API page request still redirects to /login with from= pathname (PROXY-01)."
    - "PUBLIC paths (/login, /landing, /api/auth/, /api/health, /api/demo-requests) still pass through without JSON 401; PUBLIC check stays before the unauthenticated branch (PROXY-01)."
    - "A request with a non-empty pm_session cookie is not JSON-401ed by the proxy (PROXY-01)."
  artifacts:
    - path: "proxy.ts"
      provides: "Edge API vs page unauthenticated contract (D-01, D-02)"
      exports: ["proxy", "config"]
    - path: "lib/http/proxy.auth.test.ts"
      provides: "Vitest proof of JSON 401, page redirect, PUBLIC, session pass-through"
  key_links:
    - from: "proxy.ts"
      to: "lib/http/with-auth.ts"
      via: "same { error: 'Unauthorized' } JSON body"
      pattern: "error: 'Unauthorized'"
    - from: "proxy.ts PUBLIC check"
      to: "unauthenticated branch"
      via: "PUBLIC.some(pathname.startsWith) runs first"
      pattern: "PUBLIC.some"
  prohibitions:
    - statement: "Must not detect API vs page via Accept header (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not move PUBLIC check after the unauthenticated JSON 401 branch"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not change matcher config (static-asset bypass stays)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Unauthenticated callers of `/api/*` get JSON `{ error: 'Unauthorized' }` at 401 from `proxy.ts`, while unauthenticated page requests still redirect to login (PROXY-01). D-01: detect API by pathname prefix `/api/` (already computed as `isApi`). D-02: reuse the same error string as `withAuth`.

Purpose: Prove the edge auth contract before Jira hygiene, the ESLint gate, and leftover-route thinning.

Output: `proxy.ts` JSON 401 branch + `lib/http/proxy.auth.test.ts`. Matcher unchanged.
</objective>

## Artifacts this plan produces

- `proxy(req)` JSON 401 for unauthenticated `/api/*` after PUBLIC miss
- Page redirect to `/login?from=` for unauthenticated non-API paths (except `/` → `/landing`)
- Vitest `lib/http/proxy.auth.test.ts` (node project include already covers `lib/**/*.test.ts`)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-CONTEXT.md
@.planning/phases/20-api-contract-leftover-routes/20-RESEARCH.md
@.planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md
@.planning/phases/20-api-contract-leftover-routes/20-VALIDATION.md
@proxy.ts
@lib/http/with-auth.ts
@proxy.matcher.test.mjs

Planner-assigned locked IDs: D-01 pathname `/api/` prefix not Accept; D-02 `{ error: 'Unauthorized' }` matching withAuth; D-23 ops/companies break-glass is later plans; D-03/D-04/D-05/D-06 are later plans.

Do not implement Jira search, ESLint rule, or ops/admin/config thinning in this plan.

<interfaces>
From proxy.ts today:
export function proxy(req: NextRequest): NextResponse
const PUBLIC = ['/login', '/landing', '/api/auth/', '/api/health', '/api/demo-requests']
const isApi = pathname.startsWith('/api/')
cookie name: pm_session
unauthenticated `/` → redirect /landing
unauthenticated other pages → redirect /login with searchParams from=pathname
export const config.matcher — do not edit

From lib/http/with-auth.ts:
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

Vitest node project include: `{lib,app}/**/*.test.ts` — put the new test under lib/http so vitest.config.ts is not modified (20-03 owns that file).
</interfaces>
</context>

<feature>
  <name>Proxy JSON 401 for unauthenticated /api/*</name>
  <files>proxy.ts, lib/http/proxy.auth.test.ts</files>
  <behavior>
    No cookie, GET /api/projects → status 401, body { error: 'Unauthorized' } (D-01, D-02).
    No cookie, GET /projects → status 302 or 307, Location includes /login (PROXY-01).
    No cookie, GET /api/health → not 401 (PUBLIC).
    Cookie pm_session=x, GET /api/projects → not 401.
  </behavior>
  <implementation>
    Inside existing `if (!session?.value)` after the `/` landing redirect, if isApi return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); else keep the login redirect. Do not use Accept. Keep PUBLIC before this block.
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="20-01-01">
  <name>End-to-end unauthenticated /api/projects JSON 401 and page login redirect</name>
  <reversibility rating="costly">JSON 401 becomes the published unauthenticated API contract; later clients and tests will depend on it (D-01, D-02, PROXY-01).</reversibility>
  <files>lib/http/proxy.auth.test.ts, proxy.ts</files>
  <read_first>
  - .planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md (proxy.ts Pattern + withAuth 401)
  - proxy.ts (PUBLIC order, isApi, unauthenticated block, matcher)
  - lib/http/with-auth.ts (Unauthorized JSON)
  - proxy.matcher.test.mjs (do not duplicate matcher cases here)
  </read_first>
  <behavior>
  - proxy(NextRequest http://localhost/api/projects) with no cookie returns status 401 and JSON { error: 'Unauthorized' } (D-01, D-02).
  - proxy(NextRequest http://localhost/projects) with no cookie returns 302 or 307 and Location containing /login (PROXY-01).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Import proxy from ../../proxy and NextRequest from next/server.
  RED: add lib/http/proxy.auth.test.ts with the two cases above. They must fail because today unauthenticated /api/projects redirects to login. Commit test(20-01): red proxy json 401 and page redirect.
  GREEN: in proxy.ts, inside if (!session?.value), after the pathname === '/' landing redirect, if isApi return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }). Do not branch on Accept (D-01). Do not edit config.matcher. Commit feat(20-01): proxy json 401 for api paths.
  REFACTOR only if needed; tests stay green.
  </action>
  <verify>
    <automated>npx vitest run lib/http/proxy.auth.test.ts</automated>
  </verify>
  <done>Unauthenticated /api/projects is JSON 401 Unauthorized; unauthenticated /projects still redirects to login (PROXY-01, D-01, D-02).</done>
  <acceptance_criteria>
  - lib/http/proxy.auth.test.ts exists and covers API 401 JSON and page login redirect
  - proxy.ts unauthenticated branch returns JSON 401 when isApi is true
  - npx vitest run lib/http/proxy.auth.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="20-01-02">
  <name>PUBLIC API pass-through and session cookie skip JSON 401</name>
  <files>lib/http/proxy.auth.test.ts, proxy.ts</files>
  <read_first>
  - lib/http/proxy.auth.test.ts from 20-01-01
  - proxy.ts PUBLIC array and withId()
  </read_first>
  <behavior>
  - No cookie, GET /api/health is not 401 (PUBLIC, PROXY-01).
  - No cookie, GET /api/auth/login is not 401 (PUBLIC prefix /api/auth/).
  - Request with cookie pm_session=any, GET /api/projects is not 401 (PROXY-01).
  - No cookie, GET / still redirects to /landing (existing).
  </behavior>
  <action>
  TDD. RED: add the four cases. If 20-01-01 already made health fail because JSON 401 ran before PUBLIC, that is the bug this task exists to prevent — keep PUBLIC.some before the unauthenticated block (20-RESEARCH Pitfall 1). Commit test(20-01): red proxy public and session.
  GREEN: do not reorder PUBLIC after the 401 branch. Cookie check remains session?.value. Commit feat(20-01): keep public before api 401 only if a code change is required; if already correct, no extra feat commit.
  Do not add Accept-header detection (D-01).
  </action>
  <verify>
    <automated>npx vitest run lib/http/proxy.auth.test.ts</automated>
  </verify>
  <done>PUBLIC API paths and session-bearing API requests are not JSON-401ed; `/` still goes to landing (PROXY-01).</done>
  <acceptance_criteria>
  - proxy.auth.test.ts covers /api/health, /api/auth/login, cookie pass-through, and / landing
  - PUBLIC.some remains above the unauthenticated branch in proxy.ts
  - npx vitest run lib/http/proxy.auth.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-01-03">
  <name>Matcher regression still bypasses static assets</name>
  <files>proxy.ts, proxy.matcher.test.mjs</files>
  <read_first>
  - proxy.matcher.test.mjs
  - proxy.ts export const config
  </read_first>
  <action>
  Do not edit matcher. Run node proxy.matcher.test.mjs and confirm it still passes after 20-01-01/02. If it fails, you changed matcher — revert matcher to the pre-plan string.
  </action>
  <verify>
    <automated>node proxy.matcher.test.mjs</automated>
  </verify>
  <done>Static-asset matcher bypass is unchanged (PROXY-01 regression guard).</done>
  <acceptance_criteria>
  - node proxy.matcher.test.mjs prints ok
  - proxy.ts config.matcher is unchanged from the pre-plan pattern
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated client → proxy.ts | Cookie absence crosses here; API vs page response shape is decided at the edge |
| proxy.ts → route handlers | PUBLIC paths skip the unauthenticated branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | proxy.ts unauthenticated /api/* | high | mitigate | Pathname `/api/` JSON 401 `{ error: 'Unauthorized' }` instead of HTML login (D-01, D-02, ASVS V2) |
| T-20-02 | Spoofing | Accept-header API detection | medium | mitigate | Locked D-01: only pathname.startsWith('/api/'); tests do not set Accept |
| T-20-03 | Elevation of Privilege | PUBLIC list | high | mitigate | PUBLIC check before 401; tests for /api/health and /api/auth/login (ASVS V2) |
| T-20-04 | Information Disclosure | login from= query | low | accept | Existing page redirect already put pathname in from=; this plan does not change pages |
| T-20-SC | Tampering | npm installs | high | accept | No package install in this plan |

ASVS L1: high threats on the client→proxy boundary are mitigated (T-20-01, T-20-03).
</threat_model>

<verification>
- 20-01-01: API JSON 401 + page redirect green
- 20-01-02: PUBLIC + session + landing green
- 20-01-03: node proxy.matcher.test.mjs ok
- Wave: npx vitest run lib/http/proxy.auth.test.ts && node proxy.matcher.test.mjs
</verification>

<success_criteria>
Unauthenticated `/api/*` is JSON `{ error: 'Unauthorized' }` 401; unauthenticated pages still redirect to login; PUBLIC API paths still work (PROXY-01, D-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-01-SUMMARY.md` when done
</output>
