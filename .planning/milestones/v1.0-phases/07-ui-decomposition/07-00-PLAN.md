---
phase: 07-ui-decomposition
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
autonomous: true
requirements: [UI-10, HYG-03]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Vitest jsdom project discovers and runs `*.component.test.tsx` files under app/ and components/"
    - "Existing `components/ui/badge.test.tsx` still runs unchanged"
  artifacts:
    - path: vitest.config.ts
      provides: "jsdom include glob for *.component.test.tsx"
  key_links:
    - from: vitest.config.ts
      to: test/setup-jsdom.ts
      via: "jsdom project setupFiles unchanged"
      pattern: "setupFiles"
  prohibitions:
    - "No new npm packages"
    - "Do not remove existing `{components,app}/**/*.test.tsx` include"
---

<objective>
Extend the Vitest jsdom project so Phase 7 locked `*.component.test.tsx` naming is picked up by `npm test` and per-page quick runs.

Purpose: UI-10 component tests cannot run until Wave 0 fixes the include glob mismatch (Phase 1 used `badge.test.tsx`; Phase 7 locks `*.component.test.tsx`).
Output: Updated `vitest.config.ts` verified by include-glob inspection in config file.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-decomposition/07-RESEARCH.md
@.planning/phases/07-ui-decomposition/07-PATTERNS.md
@.planning/phases/07-ui-decomposition/07-VALIDATION.md
@vitest.config.ts
@test/setup-jsdom.ts
@components/ui/badge.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend jsdom include for component tests</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts
    - .planning/phases/07-ui-decomposition/07-PATTERNS.md (Wave 0 vitest pattern)
    - .planning/phases/07-ui-decomposition/07-VALIDATION.md (reporter caveat)
  </read_first>
  <action>
    In vitest.config.ts jsdom project test.include, keep the existing `{components,app}/**/*.test.tsx` entry and add `{components,app}/**/*.component.test.tsx` as a second array element. Do not change node project includes, alias, or setupFiles. No new packages.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('vitest.config.ts','utf8'); if(!/\{components,app\}\/\*\*\/\*\.component\.test\.tsx/.test(s)||!/\{components,app\}\/\*\*\/\*\.test\.tsx/.test(s)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts include array contains both `*.test.tsx` and `*.component.test.tsx` globs under components and app
    - vitest.config.ts jsdom include still lists `{components,app}/**/*.test.tsx` (badge.test.tsx discoverable once files exist)
    - Commit message is a pure config change (HYG-01)
  </acceptance_criteria>
  <done>Jsdom project is ready to run page-level component tests once they are added in plan 07-01.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-verify jsdom harness still green</name>
  <files>vitest.config.ts</files>
  <read_first>
    - components/ui/badge.test.tsx
    - test/setup-jsdom.ts
    - .planning/phases/07-ui-decomposition/07-VALIDATION.md
  </read_first>
  <action>
    Run the existing badge component test via the jsdom project to confirm the include change did not break Phase 1 harness. Use JSON reporter per VALIDATION.md if default reporter is mangled by RTK.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --project jsdom components/ui/badge.test.tsx --reporter=json --outputFile=vt.json &amp;&amp; node -e "const r=require('./vt.json'); if(r.numFailedTests!==0) process.exit(1)" &amp;&amp; del vt.json</automated>
  </verify>
  <acceptance_criteria>
    - badge.test.tsx reports 0 failed tests
    - test/setup-jsdom.ts unchanged
    - HYG-03: existing component test still passes before any page tests land
  </acceptance_criteria>
  <done>Wave 0 complete; downstream plans may add `*.component.test.tsx` files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → test runner | vitest.config.ts only affects local/CI test discovery, not production bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-00-01 | Tampering | vitest.config.ts | low | accept | Config-only; no runtime auth surface |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs this plan; package-legitimacy N/A |
</threat_model>

<verification>
- vitest.config.ts jsdom include contains both `*.test.tsx` and `*.component.test.tsx` globs
- badge.test.tsx passes
</verification>

<success_criteria>
Vitest jsdom project discovers `*.component.test.tsx` without breaking existing `*.test.tsx` coverage.
</success_criteria>

## Artifacts this phase produces

| Path | Kind |
|------|------|
| vitest.config.ts | modified config |

<output>
Create `.planning/phases/07-ui-decomposition/07-00-SUMMARY.md` when done
</output>
