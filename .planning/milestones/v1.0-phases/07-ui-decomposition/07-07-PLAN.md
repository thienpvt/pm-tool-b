---
phase: 07-ui-decomposition
plan: 07
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - components/timeline/ImportMappingDialog.tsx
  - components/timeline/useImportMapping.ts
  - components/timeline/_components/
  - components/timeline/ImportMappingDialog.component.test.tsx
autonomous: true
requirements: [UI-01, UI-07, UI-09, UI-10, UI-11, HYG-01, HYG-02, HYG-03]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ImportMappingDialog stays at components/timeline/ImportMappingDialog.tsx as thin container"
    - "useImportMapping owns fetch-on-open for /api/import-mapping and activities/import"
    - "Step/mapping/upload UI state stays in dialog container or _components/, not hook"
    - "All files under components/timeline/ import tree are under 400 lines"
    - "Timeline page still imports from @/components/timeline/ImportMappingDialog"
  artifacts:
    - path: components/timeline/useImportMapping.ts
      provides: "dialog data hook"
    - path: components/timeline/_components/
      provides: "MappingStep, CsvParser, ValueNormalizers, ImportPreview"
    - path: components/timeline/ImportMappingDialog.component.test.tsx
      provides: "UI-10/11"
  key_links:
    - from: app/projects/[id]/timeline/page.tsx
      to: components/timeline/ImportMappingDialog.tsx
      via: "unchanged default import path"
      pattern: "@/components/timeline/ImportMappingDialog"
  prohibitions:
    - "Do not move ImportMappingDialog to app/projects route _components (locked: stays shared under components/timeline/)"
    - "No client server-layer imports (UI-09)"
    - "No MSW; mock fetch only"
---

<objective>
Decompose components/timeline/ImportMappingDialog.tsx (1179 lines) in place under components/timeline/ with hook + _components/ + test.

Purpose: Delivers UI-07; depends on 07-03 only for caller stability (timeline page import path unchanged). Dialog is shared-domain, not a route page.
Output: Thin ImportMappingDialog with mapping-step component test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-decomposition/07-CONTEXT.md
@.planning/phases/07-ui-decomposition/07-RESEARCH.md
@components/timeline/ImportMappingDialog.tsx
@components/resources/ResourceImportDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Field normalizers, CSV parser, types, hook</name>
  <files>components/timeline/useImportMapping.ts, components/timeline/_components/ActivityFields.ts, components/timeline/_components/ValueNormalizers.ts, components/timeline/_components/CsvParser.ts, components/timeline/types.ts, components/timeline/ImportMappingDialog.tsx</files>
  <read_first>
    - components/timeline/ImportMappingDialog.tsx (banners: ActivityFields, Types, ValueNormalizers, CsvParser; fetch ~292-300)
    - components/resources/ResourceImportDialog.tsx (step wizard analog)
  </read_first>
  <action>
    Extract banners into components/timeline/_components/. Create useImportMapping.ts accepting { open, projectId } — move fetch-on-open for /api/import-mapping and /api/projects/{projectId}/activities/import verbatim. Step, column mapping, and upload state remain in ImportMappingDialog container or step modules (UI-01).

    Keep default export at components/timeline/ImportMappingDialog.tsx path unchanged for timeline page caller.
  </action>
  <verify>
    <automated>rg "useImportMapping" components/timeline/ImportMappingDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useImportMapping.ts exists
    - CsvParser and ValueNormalizers modules extracted
    - Dialog default export path unchanged
    - Fetch URLs unchanged
  </acceptance_criteria>
  <done>Hook and parser/normalizer modules extracted.</done>
  <reversibility rating="costly">Timeline page and tests depend on dialog export path; path change would ripple to 07-03.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Mapping step UI + thin dialog container</name>
  <files>components/timeline/ImportMappingDialog.tsx, components/timeline/_components/MappingStep.tsx, components/timeline/_components/ImportPreview.tsx</files>
  <read_first>
    - components/timeline/ImportMappingDialog.tsx (MainComponent banner)
    - .planning/phases/07-ui-decomposition/07-RESEARCH.md (Pitfall 1: MainComponent ~900 lines)
  </read_first>
  <action>
    Extract // ─── MainComponent into MappingStep.tsx, ImportPreview.tsx, and additional sub-modules at inner step boundaries if MainComponent block exceeds 400 lines after first pass. Thin ImportMappingDialog.tsx composes hook + step modules; preserve Dialog shell and reset-on-close behavior from ResourceImportDialog analog.

    POST /api/parse-file-headers and import POST handlers move verbatim; no payload changes.
  </action>
  <verify>
    <automated>powershell -Command "Get-ChildItem components/timeline -Recurse -Include *.ts,*.tsx | ForEach-Object { if((Get-Content $_.FullName | Measure-Object -Line).Lines -ge 400){ exit 1 } }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - ImportMappingDialog.tsx under 400 lines
    - All components/timeline/** files under 400 lines
    - Step wizard flow unchanged (same step count and labels)
  </acceptance_criteria>
  <done>Dialog fully modularized in shared location.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dialog component test + UI-09</name>
  <files>components/timeline/ImportMappingDialog.component.test.tsx</files>
  <read_first>
    - .planning/phases/07-ui-decomposition/07-PATTERNS.md (ImportMappingDialog mock URLs)
  </read_first>
  <behavior>
    - Test: render dialog open with mocked import-mapping and activities/import fetch
    - Test: advance mapping step (click next/upload control) without error
  </behavior>
  <action>
    Add ImportMappingDialog.component.test.tsx rendering dialog with open=true, projectId='1', stub fetch for /api/import-mapping and /api/projects/1/activities/import. Assert mapping step advance interaction per UI-11. UI-09 grep on components/timeline/**.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --project jsdom components/timeline/ImportMappingDialog.component.test.tsx --reporter=json --outputFile=vt.json &amp;&amp; node -e "const r=require('./vt.json'); if(r.numFailedTests!==0) process.exit(1)" &amp;&amp; del vt.json</automated>
  </verify>
  <acceptance_criteria>
    - 2+ passing tests
    - UI-09 grep clean on components/timeline/**
    - HYG-03 satisfied for UI-07
  </acceptance_criteria>
  <done>UI-07 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/import-mapping, /api/parse-file-headers | File upload and mapping CRUD via API |
| shared dialog | Imported by timeline page; must not expose server layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | ImportMappingDialog client | high | mitigate | UI-09 grep; no repo/service imports |
| T-07-05 | Tampering | CSV upload client | medium | accept | Server validates at /api/parse-file-headers (Phase 5); unchanged |
| T-07-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- ImportMappingDialog component test green
- Dialog stays at components/timeline/ImportMappingDialog.tsx
- All files under 400 lines
</verification>

<success_criteria>
ImportMappingDialog decomposed in shared timeline folder with test.
</success_criteria>

## Artifacts this phase produces

| Path | Kind |
|------|------|
| components/timeline/ImportMappingDialog.tsx | thin container |
| components/timeline/useImportMapping.ts | hook |
| components/timeline/types.ts | types |
| components/timeline/_components/ActivityFields.ts | fields |
| components/timeline/_components/ValueNormalizers.ts | normalizers |
| components/timeline/_components/CsvParser.ts | parser |
| components/timeline/_components/MappingStep.tsx | UI step |
| components/timeline/_components/ImportPreview.tsx | UI preview |
| components/timeline/ImportMappingDialog.component.test.tsx | test |

<output>
Create `.planning/phases/07-ui-decomposition/07-07-SUMMARY.md` when done
</output>
