---
phase: 07-ui-decomposition
plan: 01
type: execute
wave: 1
depends_on: [07-00]
files_modified:
  - app/page.tsx
  - app/usePortfolioDashboard.ts
  - app/_components/HealthScoreArc.tsx
  - app/_components/MiniSparkline.tsx
  - app/page.component.test.tsx
autonomous: true
requirements: [UI-01, UI-08, UI-09, UI-10, UI-11, HYG-01, HYG-02, HYG-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "app/page.tsx is a thin container under 400 lines composing Sidebar, usePortfolioDashboard, and _components/ modules"
    - "usePortfolioDashboard owns fetch for /api/portfolio and /api/auth/me; filter and viewMode state stay in container or _components/"
    - "app/page.component.test.tsx renders after mocked load and asserts one view-toggle interaction (cards ↔ list)"
    - "No file under app/page.tsx decomposition tree imports @/lib/db, repositories, services, integrations, or pg"
  artifacts:
    - path: app/usePortfolioDashboard.ts
      provides: "colocated data hook (UI-01)"
    - path: app/_components/
      provides: "presentational modules split on section banners"
    - path: app/page.component.test.tsx
      provides: "UI-10 render + UI-11 interaction proof"
  key_links:
    - from: app/page.tsx
      to: app/usePortfolioDashboard.ts
      via: "container calls hook; no inline useEffect fetch in page"
      pattern: "usePortfolioDashboard"
    - from: app/usePortfolioDashboard.ts
      to: "/api/portfolio"
      via: "fetch unchanged from pre-refactor page"
      pattern: "fetch\\('/api/portfolio'\\)"
  prohibitions:
    - "No client import of @/lib/db, lib/repositories, lib/services, lib/integrations, or pg (UI-09)"
    - "No visual redesign; Vietnamese copy byte-identical (UI-11)"
    - "No SWR, React Query, or project-root hooks/ directory"
---

<objective>
End-to-end tracer decomposition of app/page.tsx (1020 lines — smallest god page, 2 fetch endpoints) proving the Phase 7 pattern: colocated hook + `_components/` + thin container + `*.component.test.tsx`.

Purpose: Validates extraction seams, test harness, and UI-09 boundary before parallel work on 2655-line portfolio report and four other route pages. Tracer choice: simplest hook surface and fewest cross-page deps while still exercising Sidebar shell, loading gate, and cards/list toggle for UI-11.
Output: Decomposed home dashboard with passing component test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-decomposition/07-CONTEXT.md
@.planning/phases/07-ui-decomposition/07-RESEARCH.md
@.planning/phases/07-ui-decomposition/07-PATTERNS.md
@app/page.tsx
@components/ui/badge.test.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end home dashboard — hook + first modules + load test</name>
  <files>app/page.tsx, app/usePortfolioDashboard.ts, app/_components/HealthScoreArc.tsx, app/_components/MiniSparkline.tsx, app/page.component.test.tsx</files>
  <read_first>
    - app/page.tsx (banners at lines 18, 43, 103, 133; fetch block ~300-311; loading gate ~380-392)
    - .planning/phases/07-ui-decomposition/07-PATTERNS.md (Archetype A/B/E)
    - components/ui/badge.test.tsx
  </read_first>
  <action>
    Per locked decisions (D-module-layout, D-hooks): create app/usePortfolioDashboard.ts exporting usePortfolioDashboard — move the useCallback/useEffect fetch blocks for /api/portfolio and /api/auth/me verbatim (no URL or handler changes). Hook returns data, loading, companyName, meUser, refetch only; do not move selectedProgramId, viewMode, or showOnboarding into the hook (UI-01).

    Extract // ─── Health Score Arc and // ─── Mini sparkline banner sections into app/_components/HealthScoreArc.tsx and app/_components/MiniSparkline.tsx as props-down presentational components (no fetch inside).

    Refactor app/page.tsx to thin container: Sidebar, hook wiring, compose extracted modules plus remaining inline sections temporarily if needed for render. Preserve loading gate JSX verbatim.

    Add app/page.component.test.tsx: vi.mock Sidebar as data-testid sidebar stub; vi.stubGlobal fetch for /api/portfolio and /api/auth/me with JSON fixtures per RESEARCH Pattern 3; one it() asserts render after load (Loading portfolio text clears). HYG-01: commit hook+modules+container as pure move before test commit if splitting commits.

    Run UI-09 grep scoped to app/page.tsx, app/usePortfolioDashboard.ts, and app/_components/*.tsx.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --project jsdom app/page.component.test.tsx --reporter=json --outputFile=vt.json &amp;&amp; node -e "const r=require('./vt.json'); if(r.numFailedTests!==0) process.exit(1)" &amp;&amp; del vt.json</automated>
  </verify>
  <acceptance_criteria>
    - usePortfolioDashboard.ts exists next to page.tsx with named export usePortfolioDashboard
    - app/page.tsx imports usePortfolioDashboard; no useEffect calling fetch('/api/portfolio') remains in page.tsx
    - HealthScoreArc.tsx and MiniSparkline.tsx exist under app/_components/
    - page.component.test.tsx passes with at least one render-after-load test
    - rg "from '@/lib/db'|from '@/lib/repositories|from '@/lib/services|from '@/lib/integrations|from 'pg'" app/page.tsx app/usePortfolioDashboard.ts app/_components --glob "*.tsx" returns 0 matches
  </acceptance_criteria>
  <done>Single happy path (load portfolio dashboard) works end-to-end with hook, extracted modules, and passing load test.</done>
  <reversibility rating="reversible">File moves only; no API or schema changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Extract remaining banner modules + optional types.ts</name>
  <files>app/page.tsx, app/_components/ListRow.tsx, app/_components/ProjectCard.tsx, app/_components/ProgramSection.tsx, app/_components/helpers.ts, app/types.ts</files>
  <read_first>
    - app/page.tsx (banners: Helpers 43, List row 150, Project card 204, Program section 247, Page 290)
    - .planning/phases/07-ui-decomposition/07-CONTEXT.md (types colocate; types.ts only if 2+ modules share)
  </read_first>
  <action>
    Extract remaining // ─── section banners into app/_components/: helpers.ts (PHASE_COLOR and helper functions from Helpers banner), ListRow.tsx, ProjectCard.tsx, ProgramSection.tsx. Move shared type aliases (ProjectRow, ProgramGroup, PortfolioData, MeUser) to app/types.ts only if used by 2+ modules; otherwise keep types in owning module per CONVENTIONS.

    Finish thinning app/page.tsx: compose all _components/ modules; container keeps selectedProgramId, viewMode, showOnboarding state and passes props down. Target page.tsx well under 400 lines. No file in tree may exceed 400 lines — sub-split Helpers if needed at natural function boundaries, not arbitrary line counts.

    HYG-01: pure-move commit for extractions; name any behavior fix HYG-02 in commit message.
  </action>
  <verify>
    <automated>powershell -Command "(Get-Content app/page.tsx | Measure-Object -Line).Lines -lt 400"</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx line count under 400
    - Every extracted file under app/_components/ and app/types.ts (if created) is under 400 lines
    - ListRow, ProjectCard, ProgramSection modules exist and are imported from page.tsx
    - Filter state (selectedProgramId, viewMode) remains in page.tsx or _components/, not in usePortfolioDashboard
  </acceptance_criteria>
  <done>Home page fully decomposed; no god-file remnants in app/page.tsx tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UI-11 interaction test + line-count gate</name>
  <files>app/page.component.test.tsx</files>
  <read_first>
    - app/page.component.test.tsx
    - app/page.tsx (viewMode toggle button labels — cards/list)
    - .planning/phases/07-ui-decomposition/07-VALIDATION.md
  </read_first>
  <behavior>
    - Test: after load, clicking the list view toggle changes visible layout (cards ↔ list) without error
    - Test: fetch mock rejects unexpected URLs
  </behavior>
  <action>
    Extend app/page.component.test.tsx with a second it() that fires one filter/view interaction: fireEvent.click on the existing list/cards toggle control using stable accessible name from current UI. Assert DOM change consistent with viewMode switch (e.g. list row visible or cards hidden). Do not change production copy to satisfy the test.

    Run full line-count scan on all files touched by this plan. Re-run UI-09 grep on app/page.tsx tree.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --project jsdom app/page.component.test.tsx --reporter=json --outputFile=vt.json &amp;&amp; node -e "const r=require('./vt.json'); if(r.numFailedTests!==0) process.exit(1)" &amp;&amp; del vt.json &amp;&amp; powershell -Command "$files=@('app/page.tsx','app/usePortfolioDashboard.ts','app/page.component.test.tsx'); if(Test-Path 'app/types.ts'){$files+='app/types.ts'}; Get-ChildItem app/_components -Include *.ts,*.tsx -File | ForEach-Object {$files+=$_.FullName}; foreach($f in $files){ if((Get-Content $f | Measure-Object -Line).Lines -ge 400){ exit 1 } }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - page.component.test.tsx has at least 2 tests: render-after-load and one interaction
    - All files in app/page.tsx decomposition are under 400 lines
    - UI-09 grep on app/page.tsx tree returns 0 forbidden imports
    - HYG-03 satisfied for UI-08 slice
  </acceptance_criteria>
  <done>UI-08, UI-01, UI-10, UI-11 proven for home dashboard; pattern ready for wave 2 pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → /api/* | Untrusted rendering layer; must not import server layers (UI-09) |
| test mocks → fetch | Tests stub fetch; no real session or DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | app/page.tsx tree | high | mitigate | UI-09 grep after each task; no @/lib/db/repos/services/integrations/pg imports |
| T-07-01b | Spoofing | fetch('/api/portfolio') | medium | accept | Session cookie auth unchanged; API wrappers from Phases 5–6 enforce server-side |
| T-07-SC | Tampering | npm installs | high | mitigate | No new packages; RESEARCH Package Legitimacy Audit N/A |
</threat_model>

<verification>
- npx vitest run --project jsdom app/page.component.test.tsx passes
- app/page.tsx and all app/_components/* under 400 lines
- UI-09 grep clean on home tree
</verification>

<success_criteria>
app/page.tsx decomposed with colocated hook, _components/, component test (load + view toggle), zero forbidden client imports.
</success_criteria>

## Artifacts this phase produces

| Path | Kind |
|------|------|
| app/page.tsx | thin container |
| app/usePortfolioDashboard.ts | data hook |
| app/types.ts | optional shared types |
| app/_components/HealthScoreArc.tsx | presentational module |
| app/_components/MiniSparkline.tsx | presentational module |
| app/_components/helpers.ts | pure helpers |
| app/_components/ListRow.tsx | presentational module |
| app/_components/ProjectCard.tsx | presentational module |
| app/_components/ProgramSection.tsx | presentational module |
| app/page.component.test.tsx | component test |

<output>
Create `.planning/phases/07-ui-decomposition/07-01-SUMMARY.md` when done
</output>
