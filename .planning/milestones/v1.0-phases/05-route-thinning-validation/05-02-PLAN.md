---
phase: 05-route-thinning-validation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/api/projects/[id]/route.ts
  - app/api/projects/[id]/route.access.test.ts
  - app/api/projects/[id]/activities/route.ts
  - app/api/projects/[id]/activities/route.test.ts
  - app/api/projects/[id]/activities/import/route.ts
  - app/api/projects/[id]/activities/import/route.test.ts
  - app/api/projects/[id]/issues/route.ts
  - app/api/projects/[id]/issues/route.test.ts
  - app/api/projects/[id]/meetings/route.ts
  - app/api/projects/[id]/meetings/route.test.ts
  - app/api/projects/[id]/escalations/route.ts
  - app/api/projects/[id]/escalations/route.test.ts
  - app/api/projects/[id]/team/route.ts
  - app/api/projects/[id]/team/route.test.ts
  - app/api/projects/[id]/documents/route.ts
  - app/api/projects/[id]/documents/route.test.ts
  - app/api/projects/[id]/bugs/route.ts
  - app/api/projects/[id]/bugs/route.test.ts
  - app/api/projects/[id]/holidays/route.ts
  - app/api/projects/[id]/holidays/route.test.ts
  - app/api/projects/[id]/milestones/route.ts
  - app/api/projects/[id]/milestones/route.test.ts
  - app/api/projects/[id]/milestones/[milestoneId]/route.ts
  - app/api/projects/[id]/milestones/[milestoneId]/route.test.ts
  - app/api/projects/[id]/milestones/[milestoneId]/epics/route.ts
  - app/api/projects/[id]/milestones/[milestoneId]/epics/route.test.ts
  - app/api/projects/[id]/budget/route.ts
  - app/api/projects/[id]/budget/route.test.ts
  - app/api/projects/[id]/budget/[itemId]/route.ts
  - app/api/projects/[id]/budget/[itemId]/route.test.ts
  - app/api/projects/[id]/budget/[itemId]/route.access.test.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/route.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/route.test.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/[expId]/route.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/[expId]/route.test.ts
autonomous: true
requirements: [ROUTE-05, ROUTE-12]
estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every route.ts under app/api/projects/[id]/** (17 remaining files: the parent route plus activities, activities/import, issues, meetings, escalations, team, documents, bugs, holidays, milestones, milestones/[milestoneId], milestones/[milestoneId]/epics, budget, budget/[itemId], budget/[itemId]/expenses, budget/[itemId]/expenses/[expId]) is wrapped by withProjectAccess (or withAuth alone for the milestoneId sub-routes that need actor+params but no re-assert beyond the parent's) — none contain a local getSessionFromRequest call, actorOf definition, or try/catch."
    - "The 401/403/404 wire behavior for every converted route is byte-identical to before conversion, including the T-04-25 UnknownColumnError->400 branch on projects/[id] PATCH and budget/[itemId] PUT."
    - "A grep across app/api/** finds no dynamic SQL column assignment built from request body keys (ROUTE-12 — already true post-Phase 2, this conversion adds none)."
    - "Full suite: 0 failed, skipped exactly 113 (baseline held)."
  artifacts:
    - path: "app/api/projects/[id]/route.ts"
      provides: "GET/PATCH/DELETE via withProjectAccess, ctx.project available (no re-fetch needed for GET)"
    - path: "app/api/projects/[id]/budget/[itemId]/expenses/[expId]/route.ts"
      provides: "DELETE via withProjectAccess with the item/expense scoping guard preserved (404 on cross-item expense)"
  key_links:
    - from: "app/api/projects/[id]/**/route.ts (17 files)"
      to: "lib/http/with-project-access.ts"
      via: "withProjectAccess(handler)"
      pattern: "one-line handler bodies: NextResponse.json(await <service fn>(ctx.params.id, ctx.actor, ...body/query...))"
---

<objective>
Convert the remaining 17 files under `app/api/projects/[id]/**` onto the `withProjectAccess`/`withAuth` wrappers proven in 05-01's `risks/route.ts` tracer. This is mechanical expansion, not new design — every route in this tree already calls a service that itself calls `assertProjectAccess` (defense-in-depth preserved), so the conversion only deletes boilerplate and rewires the prologue/catch tail.

Purpose: complete ROUTE-05 (route handler = parse/authorize/call/respond, nothing else) across the full locked-scope tree named in ROADMAP/CONTEXT.
Output: 17 converted route.ts files + their route.test.ts files updated in place (same mocked boundaries, same expected wire behavior).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-route-thinning-validation/05-CONTEXT.md
@.planning/phases/05-route-thinning-validation/05-RESEARCH.md
@.planning/phases/05-route-thinning-validation/05-PATTERNS.md
@.planning/phases/05-route-thinning-validation/05-VALIDATION.md
@.planning/phases/05-route-thinning-validation/05-01-SUMMARY.md

@lib/http/with-auth.ts
@lib/http/with-project-access.ts
@app/api/projects/[id]/risks/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the parent route + single-nesting resources (route, activities, activities/import, issues, meetings, escalations, team, documents, bugs, holidays)</name>
  <files>app/api/projects/[id]/route.ts, app/api/projects/[id]/route.access.test.ts, app/api/projects/[id]/activities/route.ts, app/api/projects/[id]/activities/route.test.ts, app/api/projects/[id]/activities/import/route.ts, app/api/projects/[id]/activities/import/route.test.ts, app/api/projects/[id]/issues/route.ts, app/api/projects/[id]/issues/route.test.ts, app/api/projects/[id]/meetings/route.ts, app/api/projects/[id]/meetings/route.test.ts, app/api/projects/[id]/escalations/route.ts, app/api/projects/[id]/escalations/route.test.ts, app/api/projects/[id]/team/route.ts, app/api/projects/[id]/team/route.test.ts, app/api/projects/[id]/documents/route.ts, app/api/projects/[id]/documents/route.test.ts, app/api/projects/[id]/bugs/route.ts, app/api/projects/[id]/bugs/route.test.ts, app/api/projects/[id]/holidays/route.ts, app/api/projects/[id]/holidays/route.test.ts</files>
  <read_first>
    app/api/projects/[id]/risks/route.ts (the converted reference — copy this shape exactly);
    app/api/projects/[id]/route.ts, activities/route.ts, activities/import/route.ts, issues/route.ts, meetings/route.ts, escalations/route.ts, team/route.ts, documents/route.ts, bugs/route.ts, holidays/route.ts (current pre-conversion source for each — each has the identical actorOf+getSessionFromRequest+try/catch shape, verified in 05-RESEARCH.md);
    app/api/projects/[id]/route.access.test.ts (existing — admin-bypass test at line 102 asserts `projectAccessRow).not.toHaveBeenCalled()` for admin; this is UNRELATED to the 05-01 access.unit.test.ts flip — route-level `projectAccessRow` mocking is the repo layer the SERVICE calls, and `withProjectAccess`'s own `assertProjectAccess` call is what queries it now for admin too, so this specific assertion must also flip to expect the call);
    lib/services/projects.service.ts, activities.service.ts, issues.service.ts, meetings.service.ts, escalations.service.ts, team.service.ts, documents.service.ts, bugs.service.ts, holidays.service.ts (the service functions each route calls — signatures unchanged)
  </read_first>
  <action>
    Convert each of the 10 listed routes to the withProjectAccess after-shape from 05-PATTERNS.md Pattern 3, in the same style as risks/route.ts from 05-01: delete the local `actorOf`/`mapError`/manual-prologue, wrap each exported HTTP method in `withProjectAccess(async (req, ctx) => ...)`, replace `id` with `ctx.params.id`, `actorOf(user)` with `ctx.actor`, and `await req.json()` with `ctx.body` where the route already parsed a body (no schema passed yet — Plan 05-03 wires Zod; this plan is body-shape-preserving pass-through only). Preserve exactly:
    - `projects/[id]/route.ts` GET/PATCH/DELETE: GET can use `ctx.project` from the wrapper directly if it matches what `getProject` returns, but the existing service `getProject(projectId, actor)` re-fetches with its own shape — call the service as before (`ctx.params.id`, `ctx.actor`) rather than trusting the wrapper's row, since `getProject` returns the FULL project row while `ctx.project` is only the tenancy columns (`ProjectAccessRow`). Do not conflate the two.
    - `activities/import/route.ts`: no `UnknownColumnError` branch existed here originally (uses plain `serviceErrorResponse`) — the wrapper's unified catch tail still handles this correctly since it falls through when the thrown error isn't `UnknownColumnError`.
    - `documents/route.ts` POST: preserve the `201`-vs-`200` conditional (`result.created ? 201 : 200`) — this is handler-side status logic the wrapper does not touch.
    - `bugs/route.ts` GET: preserve the `list_dates=1` query-param branch inside the handler (query params are NOT something the wrapper parses).
    - `holidays/route.ts` POST: preserve destructuring `{ date, name }` from `ctx.body`.
    - DELETE handlers (activities, issues, meetings, team, documents, bugs, holidays): query-param extraction (`rowId`, `docId`, `date`, `hid`) stays handler-side via `new URL(req.url).searchParams`, per the locked constraint — the wrapper never parses DELETE bodies since no schema + non-POST/PUT/PATCH means `ctx.body` is `undefined`.

    Update each corresponding `route.test.ts`/`route.access.test.ts` only where the wrapper's uniform 401 body or the admin-bypass row-flip changes an assertion (e.g., `route.access.test.ts` line 102's `projectAccessRow).not.toHaveBeenCalled()` for the admin case must become `.toHaveBeenCalled()`, mirroring the 05-01 access.unit.test.ts flip, since `withProjectAccess` now queries the row for every actor including admin). All other assertions (401/403/404 bodies) stay unchanged — verify by running the suite before declaring done.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run "app/api/projects/[id]/route" "app/api/projects/[id]/activities" "app/api/projects/[id]/issues" "app/api/projects/[id]/meetings" "app/api/projects/[id]/escalations" "app/api/projects/[id]/team" "app/api/projects/[id]/documents" "app/api/projects/[id]/bugs" "app/api/projects/[id]/holidays" --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>All 10 routes converted to withProjectAccess with zero local getSessionFromRequest/actorOf/try-catch remaining; existing 401/403/404 test matrices pass with only the admin-bypass assertion flipped.</done>
  <acceptance_criteria>
    - `grep -v '^//' <each of the 10 route.ts files> | grep -cE "getSessionFromRequest|function actorOf|try \{"` returns 0 for each
    - `route.access.test.ts`'s admin-bypass assertion now expects `projectAccessRow` to have been called (flipped, matching 05-01's access.unit.test.ts change)
    - All other 401/403/404/200/201 body assertions in the 10 test files pass unchanged
    - `node node_modules/vitest/vitest.mjs run "app/api/projects/[id]/route" "app/api/projects/[id]/activities" "app/api/projects/[id]/issues" "app/api/projects/[id]/meetings" "app/api/projects/[id]/escalations" "app/api/projects/[id]/team" "app/api/projects/[id]/documents" "app/api/projects/[id]/bugs" "app/api/projects/[id]/holidays" --reporter=json --outputFile=vt.json` reports 0 failed; parse and `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert the milestones and budget sub-trees (milestones, milestones/[milestoneId], milestones/[milestoneId]/epics, budget, budget/[itemId], budget/[itemId]/expenses, budget/[itemId]/expenses/[expId])</name>
  <files>app/api/projects/[id]/milestones/route.ts, app/api/projects/[id]/milestones/route.test.ts, app/api/projects/[id]/milestones/[milestoneId]/route.ts, app/api/projects/[id]/milestones/[milestoneId]/route.test.ts, app/api/projects/[id]/milestones/[milestoneId]/epics/route.ts, app/api/projects/[id]/milestones/[milestoneId]/epics/route.test.ts, app/api/projects/[id]/budget/route.ts, app/api/projects/[id]/budget/route.test.ts, app/api/projects/[id]/budget/[itemId]/route.ts, app/api/projects/[id]/budget/[itemId]/route.test.ts, app/api/projects/[id]/budget/[itemId]/route.access.test.ts, app/api/projects/[id]/budget/[itemId]/expenses/route.ts, app/api/projects/[id]/budget/[itemId]/expenses/route.test.ts, app/api/projects/[id]/budget/[itemId]/expenses/[expId]/route.ts, app/api/projects/[id]/budget/[itemId]/expenses/[expId]/route.test.ts</files>
  <read_first>
    app/api/projects/[id]/risks/route.ts (the converted reference);
    app/api/projects/[id]/milestones/route.ts, milestones/[milestoneId]/route.ts, milestones/[milestoneId]/epics/route.ts, budget/route.ts, budget/[itemId]/route.ts, budget/[itemId]/expenses/route.ts, budget/[itemId]/expenses/[expId]/route.ts (current pre-conversion source — all 7 share the identical shape, verified in 05-RESEARCH.md);
    app/api/projects/[id]/budget/[itemId]/route.test.ts (existing — the admin session at line 28 `getSessionFromRequest.mockResolvedValue({ is_admin: 1 })` combined with `projectAccessRow: vi.fn()` mocked to return undefined by default; after conversion `withProjectAccess` will call `assertProjectAccess` which for an admin now fetches `projectAccessRow` — confirm this mock still resolves correctly, i.e. `projectAccessRow` returns `undefined` -> NotFoundError -> 404, matching the existing "returns 404 when the scoped update matches no child row" test);
    app/api/projects/[id]/budget/[itemId]/route.access.test.ts (existing cross-company 403 matrix to preserve verbatim);
    lib/services/milestones.service.ts, budget.service.ts, budget-items.service.ts (the service functions each route calls — signatures unchanged, all take `(projectId, actor, ...)` or `(projectId, itemId, actor, ...)`)
  </read_first>
  <action>
    Convert each of the 7 listed routes to the withProjectAccess after-shape, mirroring risks/route.ts. Key per-file notes:
    - `milestones/route.ts`: GET/POST, straightforward — same shape as activities/route.ts minus PUT/DELETE.
    - `milestones/[milestoneId]/route.ts`: PUT/DELETE take BOTH `id` and `milestoneId` from params — `ctx.params` carries `{ id, milestoneId }` (the wrapper's params generic already supports arbitrary extra keys via `TParams extends { id: string } & Record<string, string>`), so `ctx.params.milestoneId` replaces the destructured `milestoneId`.
    - `milestones/[milestoneId]/epics/route.ts`: same two-param shape; POST's `body.activity_id` becomes `(ctx.body as { activity_id: ... }).activity_id`; DELETE's `activity_id` stays a query param via `new URL(req.url).searchParams`.
    - `budget/route.ts`: GET/POST, straightforward, same shape as team/route.ts.
    - `budget/[itemId]/route.ts`: PUT/DELETE take `id` + `itemId` — same two-param pattern as milestones/[milestoneId].
    - `budget/[itemId]/expenses/route.ts`: GET/POST take `id` + `itemId`.
    - `budget/[itemId]/expenses/[expId]/route.ts`: DELETE takes `id` + `itemId` + `expId` — three params, all flow through `ctx.params`.

    For every route with a 3-segment param set (`{ id, itemId, expId }`), the wrapper's `TParams` generic must be instantiated explicitly at the call site (e.g. `withProjectAccess<{ id: string; itemId: string; expId: string }>(async (req, ctx) => ...)`) so `ctx.params.expId` type-checks — verify `npx tsc --noEmit` catches any generic-instantiation gap before considering the task done.

    Update the corresponding test files only where the admin-bypass or wrapper-uniform-401 assertions shift (same rule as Task 1) — the cross-company 403 matrix in `budget/[itemId]/route.access.test.ts` must stay verbatim since it tests the OWNER/FOREIGN distinction, not the admin bypass.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run "app/api/projects/[id]/milestones" "app/api/projects/[id]/budget" --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>All 7 milestones/budget-tree routes converted to withProjectAccess, including the multi-param (id+itemId+expId) routes; cross-company 403 matrix preserved verbatim.</done>
  <acceptance_criteria>
    - `grep -v '^//' <each of the 7 route.ts files> | grep -cE "getSessionFromRequest|function actorOf|try \{"` returns 0 for each
    - Three-param routes (`budget/[itemId]/expenses/[expId]`) type-check with `ctx.params.expId` accessible
    - `budget/[itemId]/route.access.test.ts`'s cross-company 403 assertions pass unchanged
    - `node node_modules/vitest/vitest.mjs run "app/api/projects/[id]/milestones" "app/api/projects/[id]/budget" --reporter=json --outputFile=vt.json` reports 0 failed; parse and `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase-tree boundary sweep — full suite, ROUTE-12 grep, tsc, eslint</name>
  <files>(no new files — verification only)</files>
  <read_first>
    .planning/phases/05-route-thinning-validation/05-VALIDATION.md (baseline: 573/460/0/113; ROUTE-12 grep gate)
  </read_first>
  <action>
    Run the full suite and confirm the baseline holds exactly (0 failed, numPendingTests exactly 113). Grep the ENTIRE `app/api/projects/[id]/**` tree (all 18 files including risks from 05-01) confirming zero `getSessionFromRequest`, zero `function actorOf`, zero bare `try {` remain — this is the ROUTE-05 completeness gate for the whole locked-scope tree. Run the ROUTE-12 gate: `grep -rE "Object\.keys\(body\)" app/api/` returns 0 (already true post-Phase-2; this conversion must not reintroduce it). Run `npx tsc --noEmit` and `npx eslint` on every file in this plan's `files_modified` list.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>Full suite green at baseline; ROUTE-12 grep gate clean; the entire 18-file projects/[id]/** tree confirmed free of getSessionFromRequest/actorOf.</done>
  <acceptance_criteria>
    - Full suite: 0 failed, numPendingTests exactly 113 (parse vt.json; `rm vt.json`)
    - `grep -crE "getSessionFromRequest|function actorOf" app/api/projects/[id]/route.ts app/api/projects/[id]/*/route.ts app/api/projects/[id]/*/*/route.ts app/api/projects/[id]/*/*/*/route.ts app/api/projects/[id]/*/*/*/*/route.ts 2>/dev/null | grep -v ':0'` returns empty (every file in the tree is 0)
    - `grep -rE "Object\.keys\(body\)" app/api/` returns 0 matches
    - `npx tsc --noEmit` exits 0; `npx eslint` clean on changed files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> each of the 17 converted routes | Untrusted project_id path param + request body; now uniformly gated by withProjectAccess |
| budget/[itemId]/expenses/[expId] -> item/expense scoping | A caller who owns the project but names an expense/item belonging to a DIFFERENT item must still 404 — this scoping guard lives in the SERVICE (budget-items.service.ts), not the wrapper, and must survive the conversion untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Elevation of Privilege | Cross-company access on any of the 17 converted routes | high | mitigate | `withProjectAccess` asserts ownership before every handler; the pre-existing per-service `assertProjectAccess` re-assert is defense-in-depth. Every route's existing 403/404 test coverage is preserved verbatim (no test deleted, only admin-bypass assertions flip per the 05-01 contract change). |
| T-05-06 | Tampering | budget-items scoping guard (item/expense cross-reference) | medium | mitigate | `getBudgetItemInProject`/`getExpenseInItem` service-level checks are untouched by this conversion — the route only changes its prologue/catch, never the service call signature or order. Existing scoping-guard tests (404 on cross-item expense) re-verified in Task 2. |
| T-05-07 | Tampering | Dynamic SQL column assignment reintroduced during conversion | high | mitigate | ROUTE-12 grep gate in Task 3 confirms zero `Object.keys(body)` patterns anywhere in `app/api/**` after the sweep. |

</threat_model>

<verification>
- Per-task: `node node_modules/vitest/vitest.mjs run <scoped path> --reporter=json --outputFile=vt.json`
- Task 3 (plan exit): full suite + ROUTE-12 grep gate + tsc + eslint
</verification>

<success_criteria>
- All 17 remaining files under `app/api/projects/[id]/**` converted to withProjectAccess
- Combined with 05-01's risks/route.ts, the full 18-file locked-scope tree is converted
- Full suite: 0 failed, 113 skipped (baseline held)
- ROUTE-12 grep gate clean
</success_criteria>

<output>
Create `.planning/phases/05-route-thinning-validation/05-02-SUMMARY.md` when done
</output>
