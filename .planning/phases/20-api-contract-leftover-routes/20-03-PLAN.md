---
phase: 20-api-contract-leftover-routes
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint/plugin.mjs
  - eslint/rules/require-auth-wrapper.mjs
  - eslint/rules/require-auth-wrapper.test.ts
  - eslint/route-wrapper-allowlist.json
  - eslint.config.mjs
  - vitest.config.ts
  - .github/workflows/test.yml
autonomous: true
requirements: [ENF-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ESLint reports an error when a project-scoped route.ts exports GET/POST/PUT/PATCH/DELETE whose initializer is not a direct CallExpression to withAuth, withProjectAccess, withProgramAccess, withCpmo, or withRole (ENF-01, D-04)."
    - "D-23 exemptions and public health live in eslint/route-wrapper-allowlist.json as posix paths, not comment conventions (ENF-01, D-04, D-23)."
    - "npm run lint is invoked in .github/workflows/test.yml after npm ci; the lint script targets app/api/**/route.ts so CI enforces ENF-01 without a whole-repo eslint cleanup (D-04)."
    - "Type arguments on wrappers (withProjectAccess&lt;Params&gt;(...)) count as wrapped (ENF-01)."
  artifacts:
    - path: "eslint/rules/require-auth-wrapper.mjs"
      provides: "pm-tool/require-auth-wrapper rule (D-04)"
    - path: "eslint/route-wrapper-allowlist.json"
      provides: "Explicit D-23 + health posix path list (D-23)"
    - path: ".github/workflows/test.yml"
      provides: "npm run lint after npm ci (D-04)"
  key_links:
    - from: "eslint.config.mjs"
      to: "eslint/plugin.mjs"
      via: "plugins.pm-tool + rule error on app/api/**/route.ts"
      pattern: "require-auth-wrapper"
    - from: ".github/workflows/test.yml"
      to: "package.json scripts.lint"
      via: "run: npm run lint"
      pattern: "npm run lint"
  prohibitions:
    - statement: "Must not use comment-based exemptions (D-04)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not expand the rule to fail portfolio/company raw handlers (Pitfall 3)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add withCpmo to operations or companies via this plan (D-23)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CI fails when a project-scoped `route.ts` exports an HTTP handler not wrapped by a sanctioned helper. D-23 and health are an explicit allowlist file. Wire `npm run lint` into the test workflow (ENF-01, D-04).

Purpose: Prevent unwrapped project handlers from merging.

Output: local ESLint plugin, allowlist JSON, lint script + CI step, Rule/Linter tests.
</objective>

## Artifacts this plan produces

- `eslint/plugin.mjs` + `eslint/rules/require-auth-wrapper.mjs`
- `eslint/route-wrapper-allowlist.json` (posix paths)
- `eslint.config.mjs` plugin block
- `package.json` `lint` script scoped to `app/api/**/route.ts`
- `.github/workflows/test.yml` `npm run lint` after `npm ci`
- `@typescript-eslint/utils@8.68.0` exact pin

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-CONTEXT.md
@.planning/phases/20-api-contract-leftover-routes/20-RESEARCH.md
@.planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md
@eslint.config.mjs
@.github/workflows/test.yml
@package.json
@vitest.config.ts
@app/api/projects/[id]/benefits/route.ts
@app/api/health/route.ts

Planner-assigned locked IDs: D-04 ESLint rule + allowlist file + npm run lint in CI; D-23 allowlist includes operations/** and app/api/admin/companies/route.ts.

Discretion: custom plugin via @typescript-eslint/utils (research). Do not use comment exemptions.

Whole-repo `eslint` currently has thousands of findings outside this phase. scripts.lint MUST run eslint on app/api/**/route.ts only so CI can be green while still failing unwrapped project-scoped handlers. That is the ENF-01 gate, not a repo-wide style cleanup.

Minimatch pitfall: files glob `projects/[id]/**` treats [id] as a character class. The rule MUST decide project-scoped by context.filename (posix, includes `/projects/[id]/`, `/programs/[id]/`, `/export/` plus `/[id]/`, or `/import/resource-plan/[id]/`) rather than relying on a character-class glob.

<interfaces>
Sanctioned wrapper callee names: withAuth, withProjectAccess, withProgramAccess, withCpmo, withRole.

HTTP method identifiers: GET POST PUT PATCH DELETE.

Wrapped: ExportNamedDeclaration VariableDeclaration whose Identifier is a method and init is CallExpression whose callee is Identifier in the set, OR CallExpression whose callee is TSInstantiationExpression of that Identifier (withProjectAccess&lt;Params&gt;).

Unwrapped (must report): export async function GET, export const GET = async () =&gt; {}, export const GET = someOtherHelper().

Not in scope (rule returns without report): files whose path is not project-scoped, even if they use export async function GET (e.g. app/api/projects/route.ts collection, app/api/portfolio/**).

Allowlist JSON: array of posix strings. If context.filename (relative posix) is in the list, skip. Minimum entries:
- app/api/health/route.ts
- all eight operations route.ts files under app/api/operations/systems/
- app/api/admin/companies/route.ts

eslint.config.mjs today: defineConfig([ ...nextVitals, ...nextTs, globalIgnores ]). Add a block with files: ['app/api/**/route.ts'], plugins: { 'pm-tool': pmToolPlugin }, rules: { 'pm-tool/require-auth-wrapper': 'error' }.

package.json today: "lint": "eslint". Change to eslint --no-error-on-unmatched-pattern with quoted app/api/**/route.ts so Ubuntu CI and Windows both match files.

test.yml: after npm ci, before npm run migrate, add run: npm run lint.

vitest.config.ts node include: add eslint/**/*.test.ts so the rule test runs.

Install: npm install -D @typescript-eslint/utils@8.68.0 exact (T-20-SC). RESEARCH [SUS] too-new signal; official typescript-eslint org — pin 8.68.0; skip blocking human checkpoint (research + unattended). Confirm package name @typescript-eslint/utils on npmjs in human-check.

Rule tests: valid wrapped export const GET = withProjectAccess(...); valid with type args; invalid export async function GET. Filename in tests must look like app/api/projects/[id]/benefits/route.ts so the path gate fires.
</interfaces>
</context>

<feature>
  <name>ENF-01 require-auth-wrapper ESLint rule</name>
  <files>eslint/rules/require-auth-wrapper.mjs, eslint/rules/require-auth-wrapper.test.ts, eslint/plugin.mjs, eslint/route-wrapper-allowlist.json, eslint.config.mjs</files>
  <behavior>
    Code export const GET = withProjectAccess(async () =&gt; {}) in a project-scoped filename → no error.
    export async function GET() {} in that filename → error unwrapped.
    Same unwrapped code in app/api/portfolio/report/route.ts → no error (not project-scoped).
    Allowlisted path → no error even if unwrapped.
  </behavior>
  <implementation>
    ESLintUtils.RuleCreator from @typescript-eslint/utils. Register in eslint/plugin.mjs. Allowlist JSON imported as ignores-equivalent inside the rule (read file or imported list) — CONTEXT requires an explicit file, not comments.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="20-03-01">
  <name>Failing tests for wrapped vs unwrapped project-scoped handlers</name>
  <files>eslint/rules/require-auth-wrapper.test.ts, vitest.config.ts</files>
  <read_first>
  - 20-RESEARCH.md Pattern 2 skeleton
  - vitest.config.ts include globs
  - app/api/projects/[id]/benefits/route.ts (valid analog)
  </read_first>
  <behavior>
  - Project-scoped filename + export const GET = withProjectAccess(fn) → zero messages (ENF-01).
  - Project-scoped filename + export async function GET → one messageId unwrapped (ENF-01).
  - Non-project path app/api/portfolio/report/route.ts + export async function GET → zero messages (Pitfall 3).
  </behavior>
  <action>
  TDD RED. Expand vitest.config.ts node include to {lib,app,eslint}/**/*.test.ts.
  Write eslint/rules/require-auth-wrapper.test.ts that imports the rule module (it may not exist yet — RED). Use ESLint Linter or RuleTester with typescript parser so type-argument CallExpressions parse. Filename option must include the literal projects/[id] segment so the path gate can match.
  Commit test(20-03): red require-auth-wrapper rule cases.
  Do not install packages in this task (20-03-02).
  </action>
  <verify>
    <automated>npx vitest run eslint/rules/require-auth-wrapper.test.ts</automated>
  </verify>
  <done>Rule tests exist and fail until the rule is implemented (ENF-01, D-04).</done>
  <acceptance_criteria>
  - vitest.config.ts node include covers eslint/**/*.test.ts
  - require-auth-wrapper.test.ts covers wrapped, unwrapped, and non-project path
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="20-03-02">
  <name>Rule, plugin, allowlist, eslint.config, utils pin</name>
  <reversibility rating="costly">CI lint contract and allowlist become the merge gate for project-scoped routes (D-04, D-23).</reversibility>
  <files>package.json, package-lock.json, eslint/rules/require-auth-wrapper.mjs, eslint/plugin.mjs, eslint/route-wrapper-allowlist.json</files>
  <read_first>
  - eslint.config.mjs
  - 20-RESEARCH.md Pattern 2 and Package Legitimacy
  - app/api/operations/systems/route.ts (D-23 path to list)
  - app/api/health/route.ts
  </read_first>
  <behavior>
  - Rule reports unwrapped project-scoped method exports and accepts sanctioned CallExpressions including TSInstantiationExpression (ENF-01, D-04).
  - Allowlist JSON lists health, eight operations route.ts files, and app/api/admin/companies/route.ts (D-23).
  </behavior>
  <action>
  npm install -D @typescript-eslint/utils@8.68.0 (exact, T-20-SC). Confirm the package is the typescript-eslint monorepo package before committing the lockfile.
  Implement eslint/rules/require-auth-wrapper.mjs with ESLintUtils.RuleCreator. Path gate: posix-normalized context.filename must include /projects/[id]/ or /programs/[id]/ or (/export/ and /[id]/) or /import/resource-plan/[id]/ OR skip. Then if allowlist includes the relative posix path, skip. Then ExportNamedDeclaration as in RESEARCH skeleton; also treat export async function METHOD as unwrapped. Callee names: withAuth, withProjectAccess, withProgramAccess, withCpmo, withRole.
  eslint/plugin.mjs default export { rules: { 'require-auth-wrapper': rule } }.
  Allowlist file: posix JSON array, D-23 operations + companies + health (D-04, D-23). No comment exemptions.
  Change package.json scripts.lint to eslint --no-error-on-unmatched-pattern targeting app/api/**/route.ts (whole-repo eslint is out of phase; ENF-01 is the route gate). Registering the plugin in eslint.config.mjs is 20-03-03.
  Commit feat(20-03): require-auth-wrapper rule allowlist and lint script.
  Do not wrap operations or companies with withCpmo (D-23). Do not fail portfolio raw handlers.
  </action>
  <verify>
    <automated>npx vitest run eslint/rules/require-auth-wrapper.test.ts</automated>
  </verify>
  <done>Rule + allowlist + lint script exist; tests green (ENF-01, D-04, D-23).</done>
  <acceptance_criteria>
  - package.json devDependencies @typescript-eslint/utils is 8.68.0
  - eslint/route-wrapper-allowlist.json includes app/api/admin/companies/route.ts and app/api/health/route.ts
  - npx vitest run eslint/rules/require-auth-wrapper.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-03-03">
  <name>Wire npm run lint into CI and prove current wrapped routes pass</name>
  <files>.github/workflows/test.yml, package.json, eslint.config.mjs</files>
  <read_first>
  - .github/workflows/test.yml
  - package.json scripts.lint after 20-03-02
  - eslint.config.mjs
  - eslint/plugin.mjs
  </read_first>
  <action>
  Register the local plugin in eslint.config.mjs after the nextVitals/nextTs spreads: files app/api/**/route.ts, plugins pm-tool from ./eslint/plugin.mjs, rules pm-tool/require-auth-wrapper error (D-04).
  In .github/workflows/test.yml add a step run: npm run lint immediately after npm ci and before npm run migrate (D-04). Keep migrate and npm test. Do not remove TEST_DATABASE_URL.
  Run npm run lint locally; it must exit 0 on current wrapped project-scoped routes. If it fails on a true unwrapped project-scoped handler, that is an ENF-01 finding — wrap that handler with the existing sanctioned helper (do not invent a new wrapper). If it fails on a non-project file, the path gate is wrong — fix the rule, do not wrap portfolio routes.
  Commit feat(20-03): ci lint step for route wrappers.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <human-check>On npmjs.com/package/@typescript-eslint/utils confirm 8.68.0 is typescript-eslint/typescript-eslint (T-20-SC).</human-check>
  </verify>
  <done>CI runs npm run lint after npm ci; current project-scoped routes pass (ENF-01, D-04).</done>
  <acceptance_criteria>
  - test.yml contains a step npm run lint after npm ci
  - npm run lint exits 0 on this branch
  - package.json scripts.lint still invokes eslint
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge → CI | Unwrapped project handlers must fail the build |
| Allowlist file → rule | D-23 paths skipped only if listed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Elevation of Privilege | project-scoped route.ts | high | mitigate | ESLint error on unwrapped method exports; CI npm run lint (ENF-01, D-04, ASVS V4) |
| T-20-09 | Elevation of Privilege | comment exemptions | medium | mitigate | Allowlist JSON file only (D-04) |
| T-20-10 | Denial of Service | D-23 over-gating via lint forcing withCpmo | medium | mitigate | Allowlist operations + companies; rule path-gated to project-scoped (D-23) |
| T-20-SC | Tampering | npm install @typescript-eslint/utils | high | mitigate | Pin 8.68.0; official typescript-eslint org; RESEARCH approved [SUS] provenance (ASVS V1) |

ASVS L1: high T-20-08 and T-20-SC mitigated.
</threat_model>

<verification>
- Rule unit tests green
- npm run lint exits 0
- test.yml has npm run lint after npm ci
</verification>

<success_criteria>
An unwrapped project-scoped handler fails lint; allowlisted D-23 paths do not; CI runs `npm run lint` (ENF-01, D-04, D-23).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-03-SUMMARY.md` when done
</output>
