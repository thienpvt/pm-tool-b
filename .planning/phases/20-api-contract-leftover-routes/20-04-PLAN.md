---
phase: 20-api-contract-leftover-routes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/operations.service.ts
  - lib/services/operations.service.unit.test.ts
  - app/api/operations/systems/route.ts
  - app/api/operations/systems/route.test.ts
  - app/api/operations/systems/[id]/route.ts
  - app/api/operations/systems/[id]/route.test.ts
autonomous: true
requirements: [THIN-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GET/POST /api/operations/systems call operations.service with SessionUser; routes keep getSessionFromRequest 401 and do not import operations.repo (THIN-01, D-23)."
    - "GET/PUT/DELETE /api/operations/systems/[id] call the service; 404 Not found when the system is missing; no withCpmo/withRole (THIN-01, D-23)."
    - "operations.service.ts exports collection, detail, and nested helpers so 20-05 can rewire nested routes without editing this module (THIN-01)."
  artifacts:
    - path: "lib/services/operations.service.ts"
      provides: "SessionUser pass-through to operations.repo (D-23 auth stays in route)"
    - path: "app/api/operations/systems/route.ts"
      provides: "Thin GET/POST"
    - path: "app/api/operations/systems/[id]/route.ts"
      provides: "Thin GET/PUT/DELETE"
  key_links:
    - from: "app/api/operations/systems/route.ts"
      to: "lib/services/operations.service.ts"
      via: "listOperationsSystems(user), createOperationsSystem(user, input)"
      pattern: "listOperationsSystems"
    - from: "lib/services/operations.service.ts"
      to: "lib/repositories/operations.repo.ts"
      via: "repo functions with company_id and Boolean(is_admin)"
      pattern: "listOperationsSystems as listOperationsSystemsRepo"
  prohibitions:
    - statement: "Must not add withCpmo, withRole, or assertCompanyWrite to operations routes (D-23)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not move getSessionFromRequest into the service"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Leftover operations collection and `[id]` routes go through `operations.service.ts` while D-23 session+tenant (not product CPMO) stays in the route (THIN-01, D-23).

Purpose: Prove route → service → repo for ops before nested budget/expense/incident routes (20-05).

Output: operations.service with all repo wrappers + thinned systems and `[id]` routes + tests.
</objective>

## Artifacts this plan produces

- `lib/services/operations.service.ts` — collection, detail, nested wrappers (complete surface for 20-05)
- Route tests mocking the service, not the repo

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-CONTEXT.md
@.planning/phases/20-api-contract-leftover-routes/20-RESEARCH.md
@.planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md
@app/api/operations/systems/route.ts
@app/api/operations/systems/[id]/route.ts
@lib/repositories/operations.repo.ts
@lib/services/holidays.service.ts
@app/api/admin/users/route.test.ts
@lib/auth.ts

Planner-assigned locked IDs: D-23 keep getSessionFromRequest; no withCpmo/withRole. D-05 split services is 20-06/20-07. Glue execute (not TDD plan) per orchestrator.

<interfaces>
SessionUser from lib/auth.ts: company_id: number | null; is_admin: number; ...

Repo (keep signatures; rename import as *Repo):
listOperationsSystems(companyId, isAdmin)
findOperationsSystem(id, companyId, isAdmin)
getOperationsSystem(id, companyId, isAdmin)
createOperationsSystem(companyId, body)
updateOperationsSystem(id, body)
deleteOperationsSystem(id, companyId, isAdmin)
listOperationsBudgetItems / createOperationsBudgetItem / updateOperationsBudgetItem / deleteOperationsBudgetItem
listOperationsExpenses / createOperationsExpense / deleteOperationsExpense
listOperationsIncidents / createOperationsIncident / updateOperationsIncident / deleteOperationsIncident

Service exports (exact names for 20-05):
listOperationsSystems(user: SessionUser)
createOperationsSystem(user: SessionUser, input: { project_id?, name, description?, go_live_date?, status? })
getOperationsSystemDetail(user, id) → { system, budgetItems, expenses, incidents } | null
findOperationsSystemForUser(user, id) → system | null
updateOperationsSystemForUser(user, id, patch) → updated | null (404 if find misses)
deleteOperationsSystemForUser(user, id)
findOperationsSystemForUser is the shared tenant predicate nested routes will use.

Nested wrappers (implement now, unused until 20-05):
listBudgetItemsForSystem(user, id) → items | null
createBudgetItemForSystem(user, id, input) → created | null
updateBudgetItemForSystem(user, id, itemId, patch) → updated | null
deleteBudgetItemForSystem(user, id, itemId) → boolean | null (null = no system)
listExpensesForSystem / createExpenseForSystem / deleteExpenseForSystem
listIncidentsForSystem / createIncidentForSystem / updateIncidentForSystem / deleteIncidentForSystem

Route session gate (verbatim pattern, do not wrap with withCpmo):
const user = await getSessionFromRequest(req);
if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });

Zod stays in the route (createOperationsSystemSchema, updateOperationsSystemSchema). PUT [id] keeps parsed.success ? parsed.data : raw passthrough.

createOperationsSystem on POST returns 201. GET [id] 404 { error: 'Not found' } when detail is null.

Tests: analog users/route.test.ts vi.hoisted mock @/lib/services/operations.service. Mock getSessionFromRequest. Do not mock operations.repo from the route test.
</interfaces>
</context>

<tasks>

<task type="auto" id="20-04-01">
  <name>operations.service pass-through including nested helpers</name>
  <files>lib/services/operations.service.ts, lib/services/operations.service.unit.test.ts</files>
  <read_first>
  - lib/repositories/operations.repo.ts export list
  - lib/services/holidays.service.ts import-rename pattern
  - lib/services/errors.ts (do not add HTTP)
  </read_first>
  <action>
  Create lib/services/operations.service.ts. Import repo functions with Repo suffix. Each exported function takes SessionUser (and ids/input) and calls the repo with user.company_id and Boolean(user.is_admin) where the repo already took those args. Nested list/create/update/delete first call findOperationsSystemForUser; if missing return null (route maps 404). Do not call withCpmo, withRole, or assertCompanyWrite (D-23). Do not import NextResponse.
  Unit test: mock the repo module; listOperationsSystems(user) calls listOperationsSystemsRepo(user.company_id, Boolean(user.is_admin)); find miss returns null and does not call nested list repos.
  Commit feat(20-04): operations service pass-through.
  </action>
  <verify>
    <automated>npx vitest run lib/services/operations.service.unit.test.ts</automated>
  </verify>
  <done>Service owns repo calls; D-23 auth stays out of the service (THIN-01, D-23).</done>
  <acceptance_criteria>
  - operations.service.ts exports listOperationsSystems, createOperationsSystem, getOperationsSystemDetail, findOperationsSystemForUser, and the nested wrappers listed in interfaces
  - npx vitest run lib/services/operations.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-04-02">
  <name>Thin GET/POST /api/operations/systems</name>
  <files>app/api/operations/systems/route.ts, app/api/operations/systems/route.test.ts</files>
  <read_first>
  - app/api/operations/systems/route.ts
  - app/api/operations/systems/schema.ts
  - app/api/admin/users/route.test.ts mock-service pattern
  </read_first>
  <action>
  Replace operations.repo imports with listOperationsSystems and createOperationsSystem from @/lib/services/operations.service. Keep getSessionFromRequest 401, createOperationsSystemSchema.safeParse, 201 on POST. Pass user into the service. Do not add withCpmo (D-23).
  Add route.test.ts: no session → 401 service not called; session → GET calls listOperationsSystems with the session user; POST valid body 201.
  Commit feat(20-04): operations systems collection uses service.
  </action>
  <verify>
    <automated>npx vitest run app/api/operations/systems/route.test.ts</automated>
  </verify>
  <done>Collection route is thin and D-23 session-gated (THIN-01, D-23).</done>
  <acceptance_criteria>
  - systems/route.ts imports from operations.service
  - systems/route.ts still uses getSessionFromRequest
  - npx vitest run app/api/operations/systems/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-04-03">
  <name>Thin GET/PUT/DELETE /api/operations/systems/[id]</name>
  <files>app/api/operations/systems/[id]/route.ts, app/api/operations/systems/[id]/route.test.ts</files>
  <read_first>
  - app/api/operations/systems/[id]/route.ts
  - lib/services/operations.service.ts from 20-04-01
  </read_first>
  <action>
  Route calls getOperationsSystemDetail, updateOperationsSystemForUser, deleteOperationsSystemForUser. Keep session 401. GET/PUT 404 when service returns null. PUT keeps schema passthrough. Do not add withCpmo (D-23).
  Tests mock the service: 401, GET 200 bundle, GET 404.
  Commit feat(20-04): operations system by id uses service.
  </action>
  <verify>
    <automated>npx vitest run app/api/operations/systems/[id]/route.test.ts</automated>
  </verify>
  <done>[id] route is thin; tenant 404 unchanged (THIN-01, D-23).</done>
  <acceptance_criteria>
  - [id]/route.ts imports from operations.service
  - npx vitest run app/api/operations/systems/[id]/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session cookie → operations route | D-23 session gate stays in the route |
| Service → operations.repo | company_id + is_admin predicates must still be passed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Elevation of Privilege | operations routes | high | mitigate | Keep getSessionFromRequest 401; pass user.company_id into repo via service; tests 401 (D-23, ASVS V4) |
| T-20-12 | Elevation of Privilege | withCpmo on ops | high | mitigate | Prohibition + tests still call raw GET/POST functions; no withCpmo import (D-23) |
| T-20-13 | Information Disclosure | cross-company system id | medium | mitigate | find/get still use company_id + is_admin from session (existing repo predicates) |
| T-20-SC | Tampering | npm installs | high | accept | No package install |

ASVS L1: high T-20-11 and T-20-12 mitigated.
</threat_model>

<verification>
- Service unit tests
- Collection and [id] route tests
- Wave: npx vitest run lib/services/operations.service.unit.test.ts app/api/operations/systems
</verification>

<success_criteria>
Operations systems collection and `[id]` go through the service; D-23 session+tenant is unchanged (THIN-01, D-23).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-04-SUMMARY.md` when done
</output>
