---
phase: 20-api-contract-leftover-routes
plan: 05
type: execute
wave: 2
depends_on: [20-04]
files_modified:
  - app/api/operations/systems/[id]/budget-items/route.ts
  - app/api/operations/systems/[id]/budget-items/route.test.ts
  - app/api/operations/systems/[id]/budget-items/[itemId]/route.ts
  - app/api/operations/systems/[id]/expenses/route.ts
  - app/api/operations/systems/[id]/expenses/route.test.ts
  - app/api/operations/systems/[id]/expenses/[expId]/route.ts
  - app/api/operations/systems/[id]/incidents/route.ts
  - app/api/operations/systems/[id]/incidents/route.test.ts
  - app/api/operations/systems/[id]/incidents/[incId]/route.ts
autonomous: true
requirements: [THIN-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Budget-item, expense, and incident operations routes call operations.service nested helpers and keep getSessionFromRequest 401 (THIN-01, D-23)."
    - "Nested routes return 404 { error: 'Not found' } when findOperationsSystemForUser would miss; they do not add withCpmo (THIN-01, D-23)."
  artifacts:
    - path: "app/api/operations/systems/[id]/budget-items/route.ts"
      provides: "Thin GET/POST"
    - path: "app/api/operations/systems/[id]/expenses/route.ts"
      provides: "Thin GET/POST"
    - path: "app/api/operations/systems/[id]/incidents/route.ts"
      provides: "Thin GET/POST"
  key_links:
    - from: "nested operations routes"
      to: "lib/services/operations.service.ts"
      via: "20-04 nested wrappers"
      pattern: "ForSystem"
  prohibitions:
    - statement: "Must not add withCpmo to nested operations routes (D-23)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit operations.service.ts (owned by 20-04)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Nested operations budget/expense/incident routes call the 20-04 service helpers; D-23 session gate stays (THIN-01, D-23).

Purpose: Finish THIN-01 for all eight operations route.ts files.

Output: six nested routes thinned; three route tests.
</objective>

## Artifacts this plan produces

- Six nested `route.ts` files calling `*ForSystem` helpers
- Tests for collection nested routes (401 + 404 + happy)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-04-PLAN.md
@lib/services/operations.service.ts
@app/api/operations/systems/[id]/budget-items/route.ts
@app/api/operations/systems/[id]/budget-items/[itemId]/route.ts
@app/api/operations/systems/[id]/expenses/route.ts
@app/api/operations/systems/[id]/expenses/[expId]/route.ts
@app/api/operations/systems/[id]/incidents/route.ts
@app/api/operations/systems/[id]/incidents/[incId]/route.ts
@app/api/admin/users/route.test.ts

Precondition: 20-04 exported nested helpers. Do not modify operations.service.ts.

<interfaces>
From 20-04 operations.service.ts:
listBudgetItemsForSystem, createBudgetItemForSystem, updateBudgetItemForSystem, deleteBudgetItemForSystem
listExpensesForSystem, createExpenseForSystem, deleteExpenseForSystem
listIncidentsForSystem, createIncidentForSystem, updateIncidentForSystem, deleteIncidentForSystem

Null return = system not visible → route 404 { error: 'Not found' }.
updateBudgetItemForSystem / updateIncidentForSystem returning null after system exists → 404 item missing (match today's if (!updated) 404).

Session gate unchanged. Zod: createOpsBudgetItemSchema, createOpsIncidentSchema stay in routes. Expense POST stays unschema'd body destructure as today.

Item routes: app/api/operations/systems/[id]/budget-items/[itemId]/route.ts PUT+DELETE; expenses/[expId] DELETE only; incidents/[incId] PUT+DELETE.
</interfaces>
</context>

<tasks>

<task type="auto" id="20-05-01">
  <name>Thin operations budget-item routes</name>
  <files>app/api/operations/systems/[id]/budget-items/route.ts, app/api/operations/systems/[id]/budget-items/[itemId]/route.ts, app/api/operations/systems/[id]/budget-items/route.test.ts</files>
  <read_first>
  - lib/services/operations.service.ts nested budget helpers
  - app/api/operations/systems/[id]/budget-items/route.ts
  - app/api/operations/systems/[id]/budget-items/[itemId]/route.ts
  - app/api/operations/systems/[id]/budget-items/schema.ts
  </read_first>
  <action>
  Collection GET/POST and [itemId] PUT/DELETE call the budget *ForSystem helpers. Keep session 401, name-required 400, 201 on POST. Do not add withCpmo (D-23). Do not edit the service file.
  Test file on the collection route: 401, 404 when service returns null, GET 200.
  Commit feat(20-05): operations budget items use service.
  </action>
  <!-- planner-discipline-allow: operations.repo -->
  <verify>
    <automated>npx vitest run app/api/operations/systems/[id]/budget-items/route.test.ts && node -e "const fs=require('fs'); const files=['app/api/operations/systems/[id]/budget-items/route.ts','app/api/operations/systems/[id]/budget-items/[itemId]/route.ts']; for (const f of files) { const s=fs.readFileSync(f,'utf8'); if (!s.includes('operations.service')) { console.error('missing service import', f); process.exit(1);} if (s.includes('operations.repo')) { console.error('repo import leftover', f); process.exit(1);} }"</automated>
  </verify>
  <done>Budget-item routes are thin (THIN-01, D-23).</done>
  <acceptance_criteria>
  - budget-items route.ts files import from operations.service
  - npx vitest run app/api/operations/systems/[id]/budget-items/route.test.ts exits 0
  - node import assert for both budget-items route.ts files exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-05-02">
  <name>Thin operations expense routes</name>
  <files>app/api/operations/systems/[id]/expenses/route.ts, app/api/operations/systems/[id]/expenses/[expId]/route.ts, app/api/operations/systems/[id]/expenses/route.test.ts</files>
  <read_first>
  - app/api/operations/systems/[id]/expenses/route.ts
  - app/api/operations/systems/[id]/expenses/[expId]/route.ts
  </read_first>
  <action>
  Same thinning for expenses GET/POST and [expId] DELETE. Keep 201 on POST. D-23 session gate. Tests: 401 and 404.
  Commit feat(20-05): operations expenses use service.
  </action>
  <!-- planner-discipline-allow: operations.repo -->
  <verify>
    <automated>npx vitest run app/api/operations/systems/[id]/expenses/route.test.ts && node -e "const fs=require('fs'); const files=['app/api/operations/systems/[id]/expenses/route.ts','app/api/operations/systems/[id]/expenses/[expId]/route.ts']; for (const f of files) { const s=fs.readFileSync(f,'utf8'); if (!s.includes('operations.service')) { console.error('missing service import', f); process.exit(1);} if (s.includes('operations.repo')) { console.error('repo import leftover', f); process.exit(1);} }"</automated>
  </verify>
  <done>Expense routes are thin (THIN-01, D-23).</done>
  <acceptance_criteria>
  - expenses route.ts files import from operations.service
  - npx vitest run app/api/operations/systems/[id]/expenses/route.test.ts exits 0
  - node import assert for both expenses route.ts files exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-05-03">
  <name>Thin operations incident routes</name>
  <files>app/api/operations/systems/[id]/incidents/route.ts, app/api/operations/systems/[id]/incidents/[incId]/route.ts, app/api/operations/systems/[id]/incidents/route.test.ts</files>
  <read_first>
  - app/api/operations/systems/[id]/incidents/route.ts
  - app/api/operations/systems/[id]/incidents/[incId]/route.ts
  - app/api/operations/systems/[id]/incidents/schema.ts
  </read_first>
  <action>
  Same thinning for incidents GET/POST and [incId] PUT/DELETE. Keep title-required 400. D-23. Tests: 401 and 404.
  Commit feat(20-05): operations incidents use service.
  </action>
  <!-- planner-discipline-allow: operations.repo -->
  <verify>
    <automated>npx vitest run app/api/operations/systems/[id]/incidents/route.test.ts && node -e "const fs=require('fs'); const files=['app/api/operations/systems/[id]/budget-items/route.ts','app/api/operations/systems/[id]/budget-items/[itemId]/route.ts','app/api/operations/systems/[id]/expenses/route.ts','app/api/operations/systems/[id]/expenses/[expId]/route.ts','app/api/operations/systems/[id]/incidents/route.ts','app/api/operations/systems/[id]/incidents/[incId]/route.ts']; for (const f of files) { const s=fs.readFileSync(f,'utf8'); if (!s.includes('operations.service')) { console.error('missing service import', f); process.exit(1);} if (s.includes('operations.repo')) { console.error('repo import leftover', f); process.exit(1);} }"</automated>
  </verify>
  <done>Incident routes are thin (THIN-01, D-23).</done>
  <acceptance_criteria>
  - incidents route.ts files import from operations.service
  - npx vitest run app/api/operations/systems/[id]/incidents/route.test.ts exits 0
  - node import assert for all six nested operations route.ts files exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nested ops route → service | Tenant visibility via findOperationsSystemForUser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-14 | Elevation of Privilege | nested operations routes | high | mitigate | Session 401 + null→404; no withCpmo (D-23, ASVS V4) |
| T-20-SC | Tampering | npm installs | high | accept | No package install |

ASVS L1: high T-20-14 mitigated.
</threat_model>

<verification>
- Three nested route test files
- All six nested route.ts files import operations.service and do not import operations.repo
- Wave: npx vitest run app/api/operations/systems
</verification>

<success_criteria>
All eight operations `route.ts` files call the service; D-23 is unchanged (THIN-01, D-23).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-05-SUMMARY.md` when done
</output>
