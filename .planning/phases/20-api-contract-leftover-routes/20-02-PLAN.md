---
phase: 20-api-contract-leftover-routes
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/api/jira/search/route.ts
  - app/api/jira/search/route.test.ts
  - app/api/jira/search/schema.ts
autonomous: true
requirements: [JIRA-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Malformed POST body to /api/jira/search returns 400 JSON { error: 'Invalid JSON' } via withAuth JSON catch (JIRA-01, D-03)."
    - "Jira search success path does not print issue field payloads (JIRA-01)."
    - "Authenticated user with null company_id still gets 401 with the existing Vietnamese Jira-not-configured message; missing creds still 503 with that message (JIRA-01)."
    - "resolveJiraCredentials and searchIssues call sites stay; INTG-08 credential resolution is not edited (JIRA-01)."
  artifacts:
    - path: "app/api/jira/search/route.ts"
      provides: "POST = withAuth(..., { schema: jiraSearchSchema }) (D-03)"
    - path: "app/api/jira/search/schema.ts"
      provides: "jiraSearchSchema (jql required)"
    - path: "app/api/jira/search/route.test.ts"
      provides: "400 Invalid JSON, no field dump, company/creds freeze"
  key_links:
    - from: "app/api/jira/search/route.ts"
      to: "lib/http/with-auth.ts"
      via: "schema JSON catch { error: 'Invalid JSON' }"
      pattern: "withAuth"
    - from: "POST handler"
      to: "lib/integrations/jira/client.ts"
      via: "searchIssues(creds, { jql, nextPageToken, maxResults, extraFields })"
      pattern: "searchIssues"
  prohibitions:
    - statement: "Must not edit lib/integrations/credentials.ts or Jira client (INTG-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not invent a new 400 envelope (D-03)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Jira search stops dumping issue field payloads and returns 400 `{ error: 'Invalid JSON' }` for a malformed body by migrating POST to `withAuth` + schema (JIRA-01, D-03).

Purpose: Close the debug leak and align malformed-body handling with every other wrapped POST.

Output: `app/api/jira/search/{route.ts,schema.ts,route.test.ts}`.
</objective>

## Artifacts this plan produces

- `export const POST = withAuth(handler, { schema: jiraSearchSchema })`
- `jiraSearchSchema` in `app/api/jira/search/schema.ts`
- Route tests: malformed JSON, missing jql, null company 401, missing creds 503, success without field dump

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-CONTEXT.md
@.planning/phases/20-api-contract-leftover-routes/20-RESEARCH.md
@.planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md
@app/api/jira/search/route.ts
@app/api/jira/jql-presets/route.ts
@app/api/jira/jql-presets/route.test.ts
@lib/http/with-auth.ts

Planner-assigned locked IDs: D-03 Invalid JSON via withAuth catch; D-01/D-02 are 20-01; do not touch D-23 routes.

<interfaces>
withAuth JSON catch (lib/http/with-auth.ts): catch { return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 }) }.

Existing search POST today: getSessionFromRequest; if !user?.company_id → 401 Vietnamese Jira-not-configured; resolveJiraCredentials; if !creds → 503 same message; unguarded req.json(); if !jql → 400 { error: 'jql là bắt buộc' }; searchIssues; then a debug dump of first-issue fields; integrationErrorResponse on throw.

Target: analog app/api/jira/jql-presets/route.ts (withAuth + schema). Preserve Vietnamese 401/503 inside the handler after session exists. withAuth no-session becomes { error: 'Unauthorized' } (intentional vs today's Vietnamese message for missing session).

jiraSearchSchema: z.object with jql string min 1 (message 'jql là bắt buộc'), nextPageToken optional string, maxResults optional number, extraFields optional string array.

Handler still calls resolveJiraCredentials(user.company_id) and searchIssues from existing modules. Do not open lib/integrations/credentials.ts or lib/integrations/jira/client.ts.

Mock pattern from jql-presets/route.test.ts: vi.hoisted + vi.mock getSessionFromRequest, resolveJiraCredentials, searchIssues.
</interfaces>
</context>

<feature>
  <name>Jira search withAuth JSON 400 and no field dump</name>
  <files>app/api/jira/search/route.ts, app/api/jira/search/schema.ts, app/api/jira/search/route.test.ts</files>
  <behavior>
    Authenticated POST with body `{not json` → 400 { error: 'Invalid JSON' } (D-03).
    Authenticated POST { jql: 'x' } with searchIssues returning one issue with fields does not print field payloads (JIRA-01).
    Session company_id null → 401 Vietnamese Jira-not-configured; creds null → 503 same message.
  </behavior>
  <implementation>
    Add schema.ts; export const POST = withAuth(..., { schema: jiraSearchSchema }); delete the first-issue debug dump after searchIssues; keep creds + integrationErrorResponse.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="20-02-01">
  <name>Jira search withAuth Invalid JSON 400 and no field-payload dump</name>
  <reversibility rating="reversible">Search HTTP wrapper only; credential resolver unchanged (D-03).</reversibility>
  <files>app/api/jira/search/route.test.ts, app/api/jira/search/route.ts, app/api/jira/search/schema.ts</files>
  <read_first>
  - app/api/jira/jql-presets/route.test.ts
  - app/api/jira/jql-presets/route.ts
  - 20-PATTERNS.md Jira search target shape
  - app/api/jira/search/route.ts
  - lib/http/with-auth.ts JSON catch
  </read_first>
  <behavior>
  - Session present, POST body the string {not json with Content-Type application/json → 400 { error: 'Invalid JSON' } (D-03).
  - Session with company_id 5, creds present, searchIssues resolves { issues: [{ fields: { summary: 'x' } }], total: 1 } → 200; spy on console must not have been called with issue field payloads (JIRA-01).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Follow jql-presets mock layout. Mock @/lib/auth getSessionFromRequest, @/lib/integrations/credentials resolveJiraCredentials, @/lib/integrations/jira/client searchIssues. Import POST from ./route.
  RED: create app/api/jira/search/route.test.ts with the Invalid JSON and success/no-dump cases. For malformed body, do not JSON.stringify — pass a raw illegal JSON string as Request body. After a successful search, a vi.spyOn(console, 'log') must not have been called. Today the route dumps first-issue fields, so RED must fail. Commit test(20-02): red jira search json 400 and no dump.
  GREEN: add app/api/jira/search/schema.ts exporting jiraSearchSchema (jql required with message jql là bắt buộc; optional nextPageToken, maxResults, extraFields). Replace export async function POST with export const POST = withAuth(async (_req, { user, body }) =&gt; { ... }, { schema: jiraSearchSchema }). Inside handler: if !user.company_id return the existing Vietnamese 401 JSON; creds via resolveJiraCredentials; if !creds Vietnamese 503; if !body.jql return 400 { error: 'jql là bắt buộc' }; try searchIssues; return issues/total/token; catch integrationErrorResponse. Delete the first-issue debug dump (the block that iterates issue fields after searchIssues). Do not print issue field payloads. Do not edit credentials or jira client modules. Do not add a new error envelope (D-03). Commit feat(20-02): jira search withAuth and drop field dump.
  REFACTOR only if needed; tests stay green.
  </action>
  <verify>
    <automated>npx vitest run app/api/jira/search/route.test.ts</automated>
  </verify>
  <done>Malformed JSON is 400 Invalid JSON; success does not dump fields (JIRA-01, D-03).</done>
  <acceptance_criteria>
  - app/api/jira/search/route.test.ts exists with Invalid JSON and success/no-dump cases
  - route.ts exports POST via withAuth
  - schema.ts exports jiraSearchSchema
  - npx vitest run app/api/jira/search/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="20-02-03">
  <name>Preserve jql 400, null-company 401, missing-creds 503</name>
  <files>app/api/jira/search/route.test.ts, app/api/jira/search/route.ts</files>
  <read_first>
  - app/api/jira/search/route.ts after 20-02-01
  - app/api/jira/search/route.test.ts
  </read_first>
  <behavior>
  - Session company_id null → 401 body.error includes Jira chưa được cấu hình; searchIssues not called (JIRA-01 freeze).
  - Session company_id 5, resolveJiraCredentials null → 503 same message substring; searchIssues not called.
  - Valid session+creds, body {} or { jql: '' } → 400 { error: 'jql là bắt buộc' }.
  </behavior>
  <action>
  TDD. RED if missing: add the three cases. GREEN: keep the existing Vietnamese strings and the inner jql check. Do not change resolveJiraCredentials. Commit test(20-02): jira search company creds jql freeze and feat only if route needs a tweak.
  </action>
  <verify>
    <automated>npx vitest run app/api/jira/search/route.test.ts</automated>
  </verify>
  <done>Null company, missing creds, and missing jql stay the pre-plan HTTP shapes (JIRA-01).</done>
  <acceptance_criteria>
  - Tests cover null company 401, creds 503, jql required 400
  - lib/integrations/credentials.ts is not in this plan's diff
  - npx vitest run app/api/jira/search/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JSON body → withAuth | Malformed bytes must not 500 |
| Jira Cloud issue payload → logs | Field values must not be printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Information Disclosure | jira/search success path | high | mitigate | Remove first-issue field dump; test spy on console (JIRA-01, ASVS V5) |
| T-20-06 | Tampering | malformed POST body | high | mitigate | withAuth JSON catch 400 { error: 'Invalid JSON' } (D-03, ASVS V5) |
| T-20-07 | Information Disclosure | Jira-not-configured message | low | accept | Existing Vietnamese copy unchanged; no-session now Unauthorized via withAuth (stricter) |
| T-20-SC | Tampering | npm installs | high | accept | No package install in this plan |

ASVS L1: high threats T-20-05 and T-20-06 are mitigated.
</threat_model>

<verification>
- Invalid JSON 400, no dump, company/creds/jql freeze
- Wave: npx vitest run app/api/jira/search/route.test.ts
</verification>

<success_criteria>
Jira search does not dump issue fields and malformed JSON is `{ error: 'Invalid JSON' }` 400 (JIRA-01, D-03).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-02-SUMMARY.md` when done
</output>
