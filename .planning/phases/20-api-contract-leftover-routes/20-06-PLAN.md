---
phase: 20-api-contract-leftover-routes
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/admin-platform.service.ts
  - lib/services/admin-platform.service.unit.test.ts
  - app/api/admin/companies/route.ts
  - app/api/admin/companies/route.test.ts
  - app/api/admin/demo-requests/route.ts
  - app/api/admin/demo-requests/route.test.ts
  - app/api/admin/resource-audit/route.ts
  - app/api/admin/resource-audit/route.access.test.ts
autonomous: true
requirements: [THIN-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "/api/admin/companies keeps requireAdmin is_admin break-glass (unauthorized/forbidden) and calls admin-platform.service; no withCpmo (THIN-01, D-23, D-05)."
    - "demo-requests GET/PUT/DELETE keep requireAdmin and call the same service module (THIN-01, D-05)."
    - "resource-audit GET stays session+company; POST keeps assertCompanyWrite(actor) in the route then service (not D-23 leftover) (THIN-01)."
  artifacts:
    - path: "lib/services/admin-platform.service.ts"
      provides: "companies, demo-requests, resource-audit wrappers (D-05)"
    - path: "app/api/admin/companies/route.ts"
      provides: "Thin break-glass CRUD (D-23)"
    - path: "app/api/admin/resource-audit/route.ts"
      provides: "Thin GET + POST with assertCompanyWrite in route"
  key_links:
    - from: "companies/route.ts"
      to: "admin-platform.service.ts"
      via: "listCompaniesPlatform / createCompanyPlatform"
      pattern: "listCompaniesPlatform"
    - from: "resource-audit POST"
      to: "lib/services/access.ts"
      via: "toAccessActor + assertCompanyWrite before service"
      pattern: "assertCompanyWrite"
  prohibitions:
    - statement: "Must not add withCpmo to /api/admin/companies (D-23)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not move assertCompanyWrite into the service for resource-audit POST"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Admin companies, demo-requests, and resource-audit call `admin-platform.service.ts` (split like users.service, D-05). Companies stay D-23 break-glass `requireAdmin` / `is_admin` (THIN-01, D-23).

Purpose: Platform admin leftover routes match route → service → repo.

Output: admin-platform.service + three thinned routes + tests.
</objective>

## Artifacts this plan produces

- `listCompaniesPlatform`, `createCompanyPlatform`, `updateCompanyPlatform`, `deleteCompanyPlatform`
- `listDemoRequestsPlatform`, `updateDemoRequestPlatform`, `deleteDemoRequestPlatform`
- `getResourceAudit`, `addMissingTeamMembersToPortfolioForCompany`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-api-contract-leftover-routes/20-CONTEXT.md
@.planning/phases/20-api-contract-leftover-routes/20-RESEARCH.md
@.planning/phases/20-api-contract-leftover-routes/20-PATTERNS.md
@app/api/admin/companies/route.ts
@app/api/admin/demo-requests/route.ts
@app/api/admin/resource-audit/route.ts
@app/api/admin/resource-audit/route.access.test.ts
@app/api/admin/users/route.ts
@lib/services/users.service.ts
@lib/repositories/admin.repo.ts
@lib/api-errors.ts
@lib/auth.ts

D-05: separate admin-platform.service.ts (do not dump jira/rag/settings into this file — 20-07).
D-23: companies remain requireAdmin + unauthorized()/forbidden() from lib/auth.ts. Do not add withCpmo.

resource-audit POST is product write (Phase 10 D-24), not D-23. Keep assertCompanyWrite in the route.

<interfaces>
requireAdmin(req): getSessionFromRequest; if !user return unauthorized(); if !user.is_admin return forbidden(); return null.

Companies GET: listCompaniesWithUserCounts(null, true) — preserve null + true (all companies for break-glass). Service listCompaniesPlatform() wraps that exact call.

createCompanyPlatform(name): try createCompany; on throw ConflictError('Company name already exists'). Route POST uses createCompanySchema; on service throw return serviceErrorResponse(e) so 409 body { error: 'Company name already exists' }.

updateCompanyPlatform(id, name), deleteCompanyPlatform(id).

Demo: listDemoRequests(), updateDemoRequest(id, status, notes), deleteDemoRequest(id) wrapped with Platform suffix or same names imported from service.

resource-audit GET: getResourceAudit(user.company_id) returns repo resourceAudit result; route still shapes summary JSON.
POST: toAccessActor(user); assertCompanyWrite(actor); addMissingTeamMembersToPortfolioForCompany(actor.company_id).

route.access.test.ts today mocks admin.repo. Retarget vi.mock to admin-platform.service and assert the service function is not called for viewer.
</interfaces>
</context>

<tasks>

<task type="auto" id="20-06-01">
  <name>admin-platform.service and thin companies route</name>
  <files>lib/services/admin-platform.service.ts, lib/services/admin-platform.service.unit.test.ts, app/api/admin/companies/route.ts, app/api/admin/companies/route.test.ts</files>
  <read_first>
  - app/api/admin/companies/route.ts
  - lib/services/users.service.ts (module granularity analog, D-05)
  - lib/repositories/admin.repo.ts company/demo/audit exports
  - lib/services/errors.ts ConflictError
  - lib/api-errors.ts serviceErrorResponse
  </read_first>
  <action>
  Create admin-platform.service.ts with companies + demo + resource-audit wrappers (demo/audit can be unused until later tasks). listCompaniesPlatform calls listCompaniesWithUserCounts(null, true). createCompanyPlatform maps thrown errors to ConflictError('Company name already exists').
  Companies route: keep requireAdmin using unauthorized/forbidden; switch data calls to the service; POST catch uses serviceErrorResponse. Do not add withCpmo (D-23).
  companies/route.test.ts: no session → 401; is_admin 0 → 403; is_admin 1 GET calls listCompaniesPlatform.
  Unit test: listCompaniesPlatform hits repo with (null, true).
  Commit feat(20-06): admin-platform service and companies route.
  </action>
  <verify>
    <automated>npx vitest run lib/services/admin-platform.service.unit.test.ts app/api/admin/companies/route.test.ts</automated>
  </verify>
  <done>Companies are service-backed break-glass (THIN-01, D-23, D-05).</done>
  <acceptance_criteria>
  - companies/route.ts imports admin-platform.service and still uses requireAdmin
  - companies/route.ts does not import withCpmo
  - npx vitest run lib/services/admin-platform.service.unit.test.ts app/api/admin/companies/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-06-02">
  <name>Thin admin demo-requests route</name>
  <files>app/api/admin/demo-requests/route.ts, app/api/admin/demo-requests/route.test.ts</files>
  <read_first>
  - app/api/admin/demo-requests/route.ts
  - lib/services/admin-platform.service.ts
  </read_first>
  <action>
  Switch list/update/delete to admin-platform.service. Keep requireAdmin. Do not add withCpmo. Tests: 401 and 403; GET calls list helper.
  Commit feat(20-06): demo-requests uses admin-platform service.
  </action>
  <verify>
    <automated>npx vitest run app/api/admin/demo-requests/route.test.ts</automated>
  </verify>
  <done>Demo-requests are service-backed platform admin (THIN-01, D-05).</done>
  <acceptance_criteria>
  - demo-requests/route.ts imports admin-platform.service
  - npx vitest run app/api/admin/demo-requests/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="20-06-03">
  <name>Thin resource-audit; keep assertCompanyWrite on POST</name>
  <files>app/api/admin/resource-audit/route.ts, app/api/admin/resource-audit/route.access.test.ts</files>
  <read_first>
  - app/api/admin/resource-audit/route.ts
  - app/api/admin/resource-audit/route.access.test.ts
  - lib/services/access.ts assertCompanyWrite
  </read_first>
  <action>
  GET: session 401 then getResourceAudit(user.company_id); keep the summary JSON shape. POST: toAccessActor, assertCompanyWrite(actor) still in the route, then addMissingTeamMembersToPortfolioForCompany(actor.company_id); catch serviceErrorResponse. Do not treat this route as D-23 (keep product write gate).
  Retarget route.access.test.ts mocks from admin.repo to admin-platform.service so viewer 403 still proves addMissing is not called.
  Commit feat(20-06): resource-audit uses admin-platform service.
  </action>
  <verify>
    <automated>npx vitest run app/api/admin/resource-audit/route.access.test.ts</automated>
  </verify>
  <done>Resource-audit is service-backed; POST still CPMO-write gated (THIN-01).</done>
  <acceptance_criteria>
  - resource-audit/route.ts imports admin-platform.service
  - POST still calls assertCompanyWrite
  - npx vitest run app/api/admin/resource-audit/route.access.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| is_admin break-glass → companies | Platform ops, not CPMO product role |
| actor.roles → resource-audit POST | Product write via assertCompanyWrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-15 | Elevation of Privilege | /api/admin/companies | high | mitigate | requireAdmin is_admin; tests 401/403; no withCpmo (D-23, ASVS V4) |
| T-20-16 | Elevation of Privilege | resource-audit POST | high | mitigate | assertCompanyWrite remains in route; access test viewer 403 (ASVS V4) |
| T-20-17 | Tampering | duplicate company name | medium | mitigate | ConflictError → 409 via serviceErrorResponse |
| T-20-SC | Tampering | npm installs | high | accept | No package install |

ASVS L1: high T-20-15 and T-20-16 mitigated.
</threat_model>

<verification>
- companies, demo-requests, resource-audit tests
- Wave: npx vitest run lib/services/admin-platform.service.unit.test.ts app/api/admin/companies app/api/admin/demo-requests app/api/admin/resource-audit
</verification>

<success_criteria>
Companies stay break-glass; demo-requests and resource-audit call admin-platform.service (THIN-01, D-23, D-05).
</success_criteria>

<output>
Create `.planning/phases/20-api-contract-leftover-routes/20-06-SUMMARY.md` when done
</output>
