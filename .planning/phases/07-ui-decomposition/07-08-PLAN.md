---
phase: 07-ui-decomposition
plan: 08
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07]
files_modified: []
autonomous: true
requirements: [UI-01, UI-09, UI-10, UI-11, HYG-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Zero client imports of @/lib/db, repositories, services, integrations, or pg across app/ and components/"
    - "All 7 god targets and their decomposition trees have no file ≥400 lines"
    - "All 7 *.component.test.tsx files pass in jsdom project"
    - "Each decomposed page has a colocated use* hook with fetch extracted from rendering"
    - "npm test and npx tsc --noEmit both succeed"
  artifacts:
    - path: app/page.component.test.tsx
      provides: "UI-08 test gate"
    - path: app/portfolio/report/page.component.test.tsx
      provides: "UI-02 test gate"
    - path: app/projects/[id]/timeline/page.component.test.tsx
      provides: "UI-03 test gate"
    - path: app/projects/[id]/report/page.component.test.tsx
      provides: "UI-04 test gate"
    - path: app/projects/[id]/milestones/page.component.test.tsx
      provides: "UI-05 test gate"
    - path: app/portfolio/roadmap/page.component.test.tsx
      provides: "UI-06 test gate"
    - path: components/timeline/ImportMappingDialog.component.test.tsx
      provides: "UI-07 test gate"
  key_links:
    - from: phase gate
      to: all use*.ts hooks
      via: "rg confirms hook colocated per page/dialog"
      pattern: "use[A-Z][a-zA-Z]+"
  prohibitions:
    - "Phase gate must not introduce product features or visual changes"
    - "Do not add Playwright or new packages during gate"
---

<objective>
Phase-level verification gate: UI-09 grep, UI-01 hook presence, line-count limits, and full test suite green before /gsd-verify-work.

Purpose: Confirms all eleven UI requirements and HYG-03 across the 7 decompositions; no code changes unless a gate fails.
Output: Documented gate results in 07-08-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-decomposition/07-VALIDATION.md
@.planning/phases/07-ui-decomposition/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static gates — UI-09, line counts, hook presence</name>
  <files></files>
  <read_first>
    - .planning/phases/07-ui-decomposition/07-RESEARCH.md (UI-09 grep command)
    - .planning/phases/07-ui-decomposition/07-CONTEXT.md (7 god file list)
  </read_first>
  <action>
    Run UI-09 grep across app/ and components/ for forbidden imports: from '@/lib/db', from '@/lib/repositories, from '@/lib/services', from '@/lib/integrations', from 'pg'. Expect 0 matches; @/lib/status-weights is allowed.

    Verify each god target root file is under 400 lines: app/page.tsx, app/portfolio/report/page.tsx, app/portfolio/roadmap/page.tsx, app/projects/[id]/timeline/page.tsx, app/projects/[id]/report/page.tsx, app/projects/[id]/milestones/page.tsx, components/timeline/ImportMappingDialog.tsx.

    Scan decomposition trees for any file ≥400 lines under: app/, app/portfolio/report/, app/portfolio/roadmap/, app/projects/[id]/timeline/, app/projects/[id]/report/, app/projects/[id]/milestones/, components/timeline/.

    Confirm colocated hooks exist: usePortfolioDashboard, usePortfolioReport, useTimelinePage, useProjectReport, useMilestonesPage, useRoadmapPage, useImportMapping (UI-01). Confirm no project-root hooks/ or lib/hooks/ directory was created.

    If any gate fails, fix in the owning plan's files (do not scope-reduce); HYG-02 any behavior fix.
  </action>
  <verify>
    <automated>rg "from '@/lib/db'|from '@/lib/repositories|from '@/lib/services|from '@/lib/integrations|from 'pg'|from \"pg\"" app components --glob "*.tsx"; if ($LASTEXITCODE -eq 0) { exit 1 }; powershell -Command "$roots=@('app/page.tsx','app/portfolio/report/page.tsx','app/portfolio/roadmap/page.tsx','app/projects/[id]/timeline/page.tsx','app/projects/[id]/report/page.tsx','app/projects/[id]/milestones/page.tsx','components/timeline/ImportMappingDialog.tsx'); foreach($r in $roots){ if(-not(Test-Path $r)){ exit 1 }; if((Get-Content $r | Measure-Object -Line).Lines -ge 400){ exit 1 } }; foreach($t in @('app/_components','app/portfolio/report','app/portfolio/roadmap','app/projects/[id]/timeline','app/projects/[id]/report','app/projects/[id]/milestones','components/timeline')){ Get-ChildItem $t -Recurse -Include *.ts,*.tsx -ErrorAction SilentlyContinue | ForEach-Object { if((Get-Content $_.FullName | Measure-Object -Line).Lines -ge 400){ exit 1 } } }; exit 0"; $hooks=@('app/usePortfolioDashboard.ts','app/portfolio/report/usePortfolioReport.ts','app/projects/[id]/timeline/useTimelinePage.ts','app/projects/[id]/report/useProjectReport.ts','app/projects/[id]/milestones/useMilestonesPage.ts','app/portfolio/roadmap/useRoadmapPage.ts','components/timeline/useImportMapping.ts'); foreach($h in $hooks){ if(-not(Test-Path $h)){ exit 1 } }; if((Test-Path hooks) -or (Test-Path lib/hooks)){ exit 1 }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - UI-09 grep returns 0 matches in app/ and components/ client tsx files
    - All 7 god root containers are under 400 lines
    - No file in decomposition trees is 400 lines or longer
    - Seven colocated use* hooks exist at paths listed in RESEARCH recommended structure
    - No hooks/ directory at project root and no lib/hooks/
  </acceptance_criteria>
  <done>Static gates pass for UI-01 and UI-09.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite + TypeScript gate</name>
  <files></files>
  <read_first>
    - .planning/phases/07-ui-decomposition/07-VALIDATION.md (full suite, tsc, reporter caveat)
  </read_first>
  <action>
    Run all seven *.component.test.tsx files via jsdom project in one vitest invocation. Run npm test (full suite including node project). Run npx tsc --noEmit. Use JSON reporter if RTK mangled default vitest output.

    Record pass/fail counts in SUMMARY. Manual UAT checklist for visual recognizability remains for /gsd-verify-work (UI-11 visual identity — not automated here).
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 (0 failed tests)
    - npx tsc --noEmit exits 0
    - All 7 component test files exist and were executed
    - HYG-03 satisfied for phase exit
  </acceptance_criteria>
  <done>Phase 7 ready for /gsd-verify-work UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase gate → codebase | Read-only verification; fixes only on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | client bundle | high | mitigate | UI-09 grep gate blocks ship if any forbidden import found |
| T-07-06 | Repudiation | missing tests | medium | mitigate | Require all 7 component tests in npm test |
| T-07-SC | Tampering | npm installs | high | mitigate | Gate plan installs no packages |
</threat_model>

<verification>
- UI-09 grep: 0 matches
- npm test: green
- tsc: 0 errors
- All hooks present
- All files under 400 lines
</verification>

<success_criteria>
All Phase 7 automated gates pass; manual UAT checklist deferred to /gsd-verify-work for visual identity.
</success_criteria>

## Artifacts this phase produces

| Path | Kind |
|------|------|
| .planning/phases/07-ui-decomposition/07-08-SUMMARY.md | gate report |

<output>
Create `.planning/phases/07-ui-decomposition/07-08-SUMMARY.md` when done
</output>
