---
phase: 13-weekly-periods-pm-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-weekly-reports.ts
  - lib/db-weekly-reports.ddl.unit.test.ts
  - lib/db.ts
  - lib/iso-week.ts
  - lib/iso-week.unit.test.ts
  - lib/repositories/weekly-periods.repo.ts
  - lib/repositories/weekly-periods.repo.test.ts
  - lib/repositories/weekly-reports.repo.ts
  - lib/repositories/weekly-reports.repo.test.ts
  - lib/services/weekly-reports.service.ts
  - lib/services/weekly-reports.service.unit.test.ts
  - app/api/weekly-periods/route.ts
  - app/api/weekly-periods/schema.ts
  - app/api/weekly-periods/route.test.ts
  - app/api/weekly-periods/config/route.ts
  - app/api/weekly-periods/config/schema.ts
  - app/api/weekly-periods/config/route.test.ts
autonomous: true
requirements: [PERD-01, PERD-02, PERD-03, WKRP-01]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO POST /api/weekly-periods creates a weekly_periods row with frozen display_name YYYY-Wnn | start – end, materialized due_at, and config_snapshot, then inserts at most one weekly_reports shell per obligated project in the same transaction (D-02, D-03, D-04, PERD-01, WKRP-01)."
    - "ISO week bounds and due_at use UTC Thursday-rule Monday–Sunday dates; default due is Friday 18:00 UTC when no company_weekly_config row exists (D-02, D-03)."
    - "PUT /api/weekly-periods/config upserts company_weekly_config only; existing period due_at, display_name, config_snapshot, and shells stay unchanged (D-03, PERD-02)."
    - "Obligation at create: same company_id, weekly_report_enabled true, weekly_report_start_period lexicographically less-or-equal iso_week, stage not L5, status not in Completed/Paused/Cancelled/Other; no backfill onto already-created periods; UNIQUE(period_id, project_id) (D-04, WKRP-01)."
    - "Overdue is computed now greater than due_at AND status in not_submitted or draft; it is never a stored status; late submit remains allowed (D-05, PERD-03)."
    - "Period mutate uses withCpmo plus assertCompanyWrite; list is company-scoped to actor.company_id (D-13)."
    - "migrateWeeklyReports runs from getDb after migrateRaidMasters; settings-flag DDL; company_weekly_config is treated as a non-serial-id table on INSERT (D-14)."
  artifacts:
    - path: "lib/db-weekly-reports.ts"
      provides: "DDL for company_weekly_config, weekly_periods, weekly_reports, weekly_report_versions plus unique indexes and migrateWeeklyReports (D-02, D-04, D-08, D-14)"
      exports: ["migrateWeeklyReports", "WEEKLY_REPORTS_DDL", "WEEKLY_REPORTS_DDL_FLAG", "WEEKLY_REPORTS_INDEX_DDL"]
    - path: "lib/db.ts"
      provides: "getDb awaits migrateWeeklyReports after migrateRaidMasters; company_weekly_config in noIdTables (D-14)"
    - path: "lib/iso-week.ts"
      provides: "UTC ISO week bounds, due_at materialization, display_name formatter (D-02)"
      exports: ["ISO_WEEK_PATTERN", "isoWeekBoundsUtc", "materializeDueAtUtc", "formatPeriodDisplayName"]
    - path: "lib/repositories/weekly-periods.repo.ts"
      provides: "config upsert, period insert, obligation query, transactional shells (D-03, D-04)"
    - path: "lib/services/weekly-reports.service.ts"
      provides: "createWeeklyPeriod, listWeeklyPeriods, getCompanyWeeklyConfig, upsertCompanyWeeklyConfig, isWeeklyReportOverdue (D-13, PERD-03)"
    - path: "app/api/weekly-periods/route.ts"
      provides: "GET list and POST create with withCpmo (D-13, D-15)"
    - path: "app/api/weekly-periods/config/route.ts"
      provides: "GET/PUT company_weekly_config with withCpmo (D-03, D-15)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-weekly-reports.ts"
      via: "getDb after migrateRaidMasters calls migrateWeeklyReports(pool) (D-14)"
      pattern: "migrateWeeklyReports"
    - from: "app/api/weekly-periods/route.ts"
      to: "lib/services/weekly-reports.service.ts"
      via: "POST handler calls createWeeklyPeriod (D-15)"
      pattern: "createWeeklyPeriod"
    - from: "lib/services/weekly-reports.service.ts"
      to: "lib/repositories/weekly-periods.repo.ts"
      via: "createPeriodWithShells transaction then auditLog (D-04, D-14)"
      pattern: "createPeriodWithShells"
  prohibitions:
    - Prisma or an external migrate CLI (D-14)
    - storing Phase 13 obligations in documents or extending the activity-weighted report helper (D-01)
    - modifying /api/projects/[id]/report, project reports pages, or /api/export/weekly-report/[id] (D-01, D-15)
    - physical row-removal SQL on weekly tables (D-17)
    - backfilling shells onto existing periods when a project becomes obligated later (D-04)
    - storing overdue as a status enum (D-05)
    - a second write-gate wrapper family or leftover ops/admin gating (D-13)
    - adding npm packages
    - CPMO tracking grid, consolidated export pack, or dashboard pages (D-16, D-18)
    - redesigning portfolio or activity-weighted project report pages (D-16)
---

<objective>
Prove the Phase 13 spine (D-01, D-02, D-03, D-04, D-05, D-13, D-14, D-15, PERD-01, PERD-02, PERD-03, WKRP-01): settings-flag weekly DDL in getDb after RAID masters; CPMO company config; one HTTP POST that creates a period, freezes display_name, snapshots config, and materializes obligated shells.

Purpose: Catch an architectural dead-end on the first commit — parallel weekly tables (not diary documents), getDb helper location, UTC ISO week contract, and obligation-at-create-only — before draft/submit/RAID expansion.

Output: Production-quality tracer. Later plans fill draft columns, immutable versions, RAID/milestone snapshots, and export list helpers without moving migrateWeeklyReports or mixing stores.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md
@.planning/phases/13-weekly-periods-pm-submit/13-RESEARCH.md
@.planning/phases/13-weekly-periods-pm-submit/13-PATTERNS.md
@.planning/phases/13-weekly-periods-pm-submit/13-VALIDATION.md

Discretion locked by planner: default due Friday 18:00 UTC (due_weekday 5, due_time_utc 18:00:00); draft columns live on weekly_reports (created here, written in 13-02); immutable weekly_report_versions rows on submit (table created here, rows in 13-02); ISO week UTC Thursday rule; display_name is `YYYY-Wnn | start – end` with ISO dates and an en dash; POST submit/correct paths are named in 13-02.

Do not implement deferred items: CPMO tracking grid / consolidated pack (Phase 14), dashboards (Phase 16), budget/ROI (Phase 15), document templates (Phase 17), full audit coverage (Phase 18). Incremental auditLog on period create is in scope (D-14).

Prior-phase artifact: lib/db-raid-masters.ts exports migrateRaidMasters and lib/db.ts already awaits it after migrateProjectMaster.

<interfaces>
migrateWeeklyReports(pool: Pool): Promise&lt;void&gt; — analog of migrateRaidMasters in lib/db-raid-masters.ts. Settings flags: weekly_reports_ddl_v1 (four CREATE TABLE statements), weekly_reports_index_v1 (unique indexes). Wrap the orchestrator in try/catch like db-raid-masters so a missing settings table does not crash first boot.

getDb in lib/db.ts (~616): after migrateRaidMasters(pool), dynamic-import migrateWeeklyReports and await it, then keep backfillWeightedCompletion.

PostgresClient.needsReturningId noIdTables MUST include company_weekly_config (PK is company_id, same failure mode as company_rag_config).

company_weekly_config: company_id INTEGER PK FK companies; due_weekday SMALLINT NOT NULL DEFAULT 5; due_time_utc TIME NOT NULL DEFAULT '18:00:00'; updated_at TIMESTAMPTZ; updated_by INTEGER FK users.

weekly_periods: id SERIAL PK; company_id INTEGER NOT NULL FK companies; iso_week TEXT NOT NULL; start_date DATE NOT NULL; end_date DATE NOT NULL; due_at TIMESTAMPTZ NOT NULL; display_name TEXT NOT NULL; config_snapshot JSONB NOT NULL; closed_at TIMESTAMPTZ NULL; created_by INTEGER; created_at TIMESTAMPTZ; UNIQUE (company_id, iso_week) (D-02, D-17).

weekly_reports shell: id SERIAL PK; period_id INTEGER FK weekly_periods; project_id INTEGER FK projects; status TEXT NOT NULL DEFAULT 'not_submitted' (not_submitted | draft | submitted); first_submitted_at TIMESTAMPTZ NULL; first_lateness TEXT NULL (on_time | late); latest_version INTEGER NOT NULL DEFAULT 0; correction_open BOOLEAN NOT NULL DEFAULT FALSE; draft columns for D-06: highlights TEXT, completed_work TEXT, next_week_goals TEXT, nearest_milestone TEXT, nearest_milestone_id INTEGER NULL, raid_dependency TEXT, leadership_support TEXT, this_week_rag TEXT, prev_week_rag TEXT; draft_raid_json JSONB NULL. UNIQUE (period_id, project_id) also as named index weekly_reports_period_project_unique (D-04, D-05, D-06).

weekly_report_versions: id SERIAL PK; report_id INTEGER FK weekly_reports; version INTEGER NOT NULL; snapshot JSONB NOT NULL; submitted_at TIMESTAMPTZ; submitted_by INTEGER FK users; rag TEXT; progress_pct INTEGER; UNIQUE (report_id, version) named weekly_report_versions_report_version_unique (D-08). No UPDATE/DELETE helpers this phase.

ISO_WEEK_PATTERN = /^\d{4}-W\d{2}$/ (same shape as project-governance WEEKLY_PERIOD_PATTERN; implement in lib/iso-week.ts, do not add date libraries).

isoWeekBoundsUtc(isoWeek): Monday 00:00 UTC through Sunday of that ISO week using the Thursday rule. formatPeriodDisplayName(isoWeek, startDate, endDate) returns `YYYY-Wnn | YYYY-MM-DD – YYYY-MM-DD` with Unicode en dash. materializeDueAtUtc(startDate, due_weekday, due_time_utc) returns timestamptz for that weekday/time in the week (0=Sun … 6=Sat).

config_snapshot JSON locked: { due_weekday, due_time_utc, obligation_rule_version: 1 }.

createWeeklyPeriod(actor, body): assertCompanyWrite(actor); validate iso_week; ConflictError on duplicate (company_id, iso_week) including Postgres 23505; load config or defaults; insert period+shells in one transaction; auditLog entity_type weekly_period, action create, actor_id actor.user_id, company_id actor.company_id (D-14).

Obligation SQL (D-04): company_id = $1 AND weekly_report_enabled IS TRUE AND weekly_report_start_period &lt;= $2 AND COALESCE(stage,'') &lt;&gt; 'L5' AND COALESCE(status,'') NOT IN ('Completed','Paused','Cancelled','Other').

Transaction: copy withPgTransaction from lib/repositories/pm-assignments.repo.ts but run every INSERT/SELECT on the same client that issued BEGIN (a second Pool connection is not in the transaction). INSERT shells ON CONFLICT (period_id, project_id) DO NOTHING.

isWeeklyReportOverdue(status, dueAt, now): true iff now &gt; dueAt AND status is not_submitted or draft (D-05). Never persist overdue.

GET /api/weekly-periods: withCpmo, listWeeklyPeriods(actor) WHERE company_id = actor.company_id ORDER BY iso_week DESC. Null company_id → ForbiddenError.

POST /api/weekly-periods: withCpmo + Zod createPeriodSchema { iso_week }; 201 with period including display_name, due_at, config_snapshot, and shells array.

GET/PUT /api/weekly-periods/config: withCpmo; PUT calls assertCompanyWrite and upserts company_weekly_config only (PERD-02).

Route tests analog app/api/admin/users/route.test.ts: 401 without session; 403 for pm/viewer; 201/200 for cpmo. No hasActivePmAssignment mock on CPMO routes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="13-01-01">
  <name>End-to-end CPMO create period with obligated shells</name>
  <precondition>lib/db-raid-masters.ts exports migrateRaidMasters and getDb already awaits it after migrateProjectMaster</precondition>
  <reversibility rating="costly">Additive weekly tables and unique (company_id, iso_week) are a published contract per D-02; undo is another migrate plus a data split, not a rename.</reversibility>
  <files>lib/db-weekly-reports.ts, lib/db-weekly-reports.ddl.unit.test.ts, lib/db.ts, lib/iso-week.ts, lib/iso-week.unit.test.ts, lib/repositories/weekly-periods.repo.ts, lib/repositories/weekly-periods.repo.test.ts, lib/repositories/weekly-reports.repo.ts, lib/repositories/weekly-reports.repo.test.ts, lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, app/api/weekly-periods/route.ts, app/api/weekly-periods/schema.ts, app/api/weekly-periods/route.test.ts, app/api/weekly-periods/config/route.ts, app/api/weekly-periods/config/schema.ts, app/api/weekly-periods/config/route.test.ts</files>
  <read_first>
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md (D-01 through D-05, D-13, D-14, D-15)
  - .planning/phases/13-weekly-periods-pm-submit/13-PATTERNS.md (db-raid-masters analog, withCpmo, obligation SQL)
  - lib/db-raid-masters.ts (settings flags, try/catch orchestrator)
  - lib/db.ts getDb migrate loop after migrateRaidMasters and PostgresClient.needsReturningId
  - lib/http/with-role.ts withCpmo
  - lib/services/access.ts assertCompanyWrite
  - lib/services/audit.service.ts auditLog
  - lib/repositories/pm-assignments.repo.ts withPgTransaction
  - lib/repositories/rag-config.repo.ts company one-row upsert
  - app/api/admin/users/route.ts withCpmo + Zod
  - test/db.ts TEST_DATABASE_URL _test suffix
  </read_first>
  <behavior>
  - migrateWeeklyReports is exported; WEEKLY_REPORTS_DDL_FLAG is weekly_reports_ddl_v1; joined DDL creates company_weekly_config, weekly_periods, weekly_reports, weekly_report_versions; UNIQUE (company_id, iso_week); named unique indexes weekly_reports_period_project_unique and weekly_report_versions_report_version_unique (D-02, D-04, D-08, D-14).
  - getDb source awaits migrateWeeklyReports after migrateRaidMasters (D-14).
  - isoWeekBoundsUtc('2026-W01') is Monday 2025-12-29 through Sunday 2026-01-04 UTC; display_name is `2026-W01 | 2025-12-29 – 2026-01-04`; default due_at is Friday 2026-01-02 18:00:00 UTC (D-02, D-03).
  - createWeeklyPeriod with a CPMO actor inserts one period, one shell per obligated project, calls auditLog action create, and throws ForbiddenError without writing when assertCompanyWrite fails (D-04, D-13, D-14, PERD-01, WKRP-01).
  - Duplicate iso_week for the same company throws ConflictError (D-02).
  - HTTP POST /api/weekly-periods as CPMO returns 201 with display_name and shells; PM/viewer return 403; missing session returns 401 (D-13, D-15).
  </behavior>
  <action>
  TDD first (D-16): add failing lib/db-weekly-reports.ddl.unit.test.ts, lib/iso-week.unit.test.ts, lib/services/weekly-reports.service.unit.test.ts, and app/api/weekly-periods/route.test.ts covering the behaviors above. Tests must fail before production modules exist. Then implement until green. Isolation is none — keep this plan sequential-safe on the main tree.

  Create lib/iso-week.ts with ISO_WEEK_PATTERN, isoWeekBoundsUtc, materializeDueAtUtc, formatPeriodDisplayName per interfaces. Unit-test 2026-W01, a W53 year, and an invalid iso_week string. No new packages (D-02).

  Create lib/db-weekly-reports.ts exporting migrateWeeklyReports(pool). Copy settingsFlagExists / writeSettingsFlag from lib/db-raid-masters.ts. WEEKLY_REPORTS_DDL creates the four tables in interfaces with draft columns present so 13-02 does not ALTER. WEEKLY_REPORTS_INDEX_DDL adds the two named unique indexes. Do not add an ORM migrate CLI (D-14). Do not add snapshot persistence onto diary document rows (D-01).

  Wire getDb() in lib/db.ts: after migrateRaidMasters(pool), await migrateWeeklyReports(pool) via dynamic import. Keep backfillWeightedCompletion after it. Add company_weekly_config to PostgresClient.needsReturningId noIdTables (PK is company_id).

  Create weekly-periods.repo.ts: get/upsert company_weekly_config (defaults Friday 18:00 UTC when missing), listObligatedProjectIds, createPeriodWithShells in one client transaction as specified. Create weekly-reports.repo.ts with insertShell used by that transaction (ON CONFLICT DO NOTHING) and getShellsForPeriod. Repo tests use describe.skipIf(!hasTestDb) from test/db.ts; TEST_DATABASE_URL must end in _test.

  Create weekly-reports.service.ts: createWeeklyPeriod and listWeeklyPeriods as specified; getCompanyWeeklyConfig returns stored row or defaults; upsertCompanyWeeklyConfig calls assertCompanyWrite then repo upsert only (does not UPDATE weekly_periods). Mock auditLog and access asserts in the unit test. Export isWeeklyReportOverdue even if list consumers land in the next task.

  Routes: app/api/weekly-periods/route.ts GET/POST withCpmo; app/api/weekly-periods/config/route.ts GET/PUT withCpmo; Zod schemas beside the routes. Error mapping stays in withAuth via serviceErrorResponse. Do not wrap with a new policy engine. Do not touch leftover ops/admin routes (D-13). Do not add pages (D-16). Do not import the activity-weighted report service or diary document repo from these modules (D-01).
  </action>
  <verify>
    <automated>npx vitest run lib/db-weekly-reports.ddl.unit.test.ts lib/iso-week.unit.test.ts lib/services/weekly-reports.service.unit.test.ts app/api/weekly-periods/route.test.ts app/api/weekly-periods/config/route.test.ts</automated>
  </verify>
  <done>CPMO can create a period with frozen display_name, config_snapshot, due_at, and obligated shells over HTTP; migrateWeeklyReports runs from getDb after migrateRaidMasters; Viewer/PM cannot POST periods (D-01, D-02, D-03, D-04, D-13, D-14, D-15, PERD-01, WKRP-01).</done>
  <acceptance_criteria>
  - lib/db-weekly-reports.ts exports migrateWeeklyReports
  - getDb awaits migrateWeeklyReports after migrateRaidMasters
  - company_weekly_config is in noIdTables
  - POST handler returns 201 with display_name matching YYYY-Wnn | start – end
  - createWeeklyPeriod calls assertCompanyWrite before any insert
  - npx vitest run lib/db-weekly-reports.ddl.unit.test.ts lib/iso-week.unit.test.ts lib/services/weekly-reports.service.unit.test.ts app/api/weekly-periods/route.test.ts app/api/weekly-periods/config/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="13-01-02">
  <name>Config snapshot isolation, obligation edges, computed overdue</name>
  <files>lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, lib/repositories/weekly-periods.repo.ts, lib/repositories/weekly-periods.repo.test.ts, lib/repositories/weekly-reports.repo.ts, lib/repositories/weekly-reports.repo.test.ts</files>
  <read_first>
  - lib/services/weekly-reports.service.ts from 13-01-01
  - lib/repositories/weekly-periods.repo.ts from 13-01-01
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md D-03 D-04 D-05 D-17
  - lib/services/project-governance.ts TERMINAL_STATUSES literals
  </read_first>
  <behavior>
  - After createWeeklyPeriod, upsertCompanyWeeklyConfig changing due_weekday does not change the existing period due_at or config_snapshot (D-03, PERD-02).
  - A project with weekly_report_enabled false, stage L5, status Completed, or weekly_report_start_period greater than the iso_week gets no shell; enabling weekly after the period exists does not insert a shell for that period (D-04, WKRP-01).
  - Two obligated projects yield two shells; UNIQUE (period_id, project_id) keeps a second insert from duplicating (D-04, WKRP-01).
  - isWeeklyReportOverdue is true for not_submitted or draft when now is after due_at, false for submitted even when now is after due_at, false when now is before due_at (D-05, PERD-03).
  - listWeeklyPeriods returns only actor.company_id rows, newest iso_week first (D-13).
  </behavior>
  <action>
  TDD: failing service and skipIf(!hasTestDb) repo tests for config isolation, each obligation exclusion, no-backfill, unique shells, overdue computation, and company-scoped list order. Then implement until green.

  Keep obligation predicates identical to TERMINAL_STATUSES in project-governance.ts. Do not add a job that writes an overdue status (D-05). Do not add a close-period HTTP route; closed_at may exist as a column and, if set in tests, must not delete shells (D-17). Do not backfill when flags flip (D-04).
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-reports.service.unit.test.ts lib/repositories/weekly-periods.repo.test.ts lib/repositories/weekly-reports.repo.test.ts</automated>
  </verify>
  <done>Later config edits leave created periods alone; shells exist only for projects obligated at create time; overdue is derived (D-03, D-04, D-05, PERD-02, PERD-03, WKRP-01).</done>
  <acceptance_criteria>
  - upsertCompanyWeeklyConfig does not UPDATE weekly_periods
  - L5, terminal status, disabled flag, and future start_period produce zero shells
  - isWeeklyReportOverdue never treats submitted as overdue
  - npx vitest run lib/services/weekly-reports.service.unit.test.ts lib/repositories/weekly-periods.repo.test.ts lib/repositories/weekly-reports.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET/POST /api/weekly-periods and GET/PUT /api/weekly-periods/config | Untrusted actor; must not create or list another company's periods |
| Service → weekly_periods / weekly_reports INSERT | company_id must come from actor, never from an unchecked body field |
| getDb → DDL helper | Boot-time schema; must not attach Phase 13 rows to diary documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | createWeeklyPeriod / listWeeklyPeriods | high | mitigate | withCpmo plus assertCompanyWrite on writes; all period SQL filters company_id = actor.company_id (D-13, ASVS V4) |
| T-13-09 | Tampering | config_snapshot / due_at | high | mitigate | Period create copies config into config_snapshot and due_at; upsertCompanyWeeklyConfig touches company_weekly_config only (D-03, PERD-02, ASVS V5) |
| T-13-10 | Tampering | obligation shell insert | medium | mitigate | Shells only inside the period-create transaction; UNIQUE(period_id, project_id); no backfill path (D-04) |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-13-11 | Denial of Service | duplicate iso_week POST | low | accept | Unique constraint plus ConflictError 409 is sufficient at ASVS L1 |
</threat_model>

<verification>
- 13-01-01: ddl unit + iso-week unit + createWeeklyPeriod service + CPMO route tests green
- 13-01-02: config isolation, obligation edges, overdue helper green
- Wave: npx vitest run lib/db-weekly-reports.ddl.unit.test.ts lib/iso-week.unit.test.ts lib/services/weekly-reports.service.unit.test.ts lib/repositories/weekly-periods.repo.test.ts lib/repositories/weekly-reports.repo.test.ts app/api/weekly-periods/route.test.ts app/api/weekly-periods/config/route.test.ts
</verification>

<success_criteria>
CPMO configures weekly periods with frozen display names, due_at, config snapshots, and at most one shell per obligated project; later config edits do not rewrite those periods; overdue is computed (PERD-01, PERD-02, PERD-03, WKRP-01, D-02, D-03, D-04, D-05, D-13, D-14).
</success_criteria>

<output>
Create `.planning/phases/13-weekly-periods-pm-submit/13-01-SUMMARY.md` when done
</output>
