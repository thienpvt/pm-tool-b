---
phase: 13-weekly-periods-pm-submit
plan: 02
type: tdd
wave: 2
depends_on: [13-01]
files_modified:
  - lib/repositories/weekly-reports.repo.ts
  - lib/repositories/weekly-reports.repo.test.ts
  - lib/services/weekly-reports.service.ts
  - lib/services/weekly-reports.service.unit.test.ts
  - app/api/projects/[id]/weekly-reports/route.ts
  - app/api/projects/[id]/weekly-reports/route.test.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/route.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/schema.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/correct/route.test.ts
autonomous: true
requirements: [PERD-03, WKRP-02, WKRP-03, WKRP-04, WKRP-05, WKRP-06]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Assigned PM PATCH of draft fields (highlights, completed_work, next_week_goals, nearest_milestone, nearest_milestone_id, raid_dependency, leadership_support, this_week_rag, draft_raid_json) moves not_submitted to draft on first successful save; RAID masters are not written on PATCH (D-06, D-11, WKRP-02)."
    - "prev_week_rag is copied at draft-open from the previous period latest submitted version this_week_rag, else projects.rag, stored on the shell, returned read-only, and stripped from PATCH (D-07, WKRP-03)."
    - "POST submit inserts an immutable weekly_report_versions row starting at version 1, sets status submitted, increments latest_version, and writes first_submitted_at plus first_lateness on_time or late once; a submit after due_at is allowed and sets late (D-05, D-08, PERD-03, WKRP-04, WKRP-05)."
    - "PATCH of a submitted shell when correction_open is false throws ConflictError (HTTP 409). POST .../correct sets correction_open true and copies the latest snapshot into draft columns; a later submit becomes version N+1 without changing first_submitted_at or first_lateness (D-08, WKRP-04, WKRP-05)."
    - "GET history is one row per period for the project, newest iso_week first, with display_name, status, computed overdue, latest RAG, submit time, submitter, on-time/late (D-09, WKRP-06)."
    - "Reads use assertProjectAccess; PATCH/submit/correct use assertProjectWriteAccess (D-09, D-13)."
  artifacts:
    - path: "lib/services/weekly-reports.service.ts"
      provides: "saveWeeklyReportDraft, getWeeklyReportShell, submitWeeklyReport, openWeeklyReportCorrection, listProjectWeeklyHistory (D-06, D-07, D-08, D-09)"
      exports: ["saveWeeklyReportDraft", "submitWeeklyReport", "openWeeklyReportCorrection", "listProjectWeeklyHistory", "getWeeklyReportShell"]
    - path: "lib/repositories/weekly-reports.repo.ts"
      provides: "allowlisted draft UPDATE, append-only version INSERT, history join (D-08, D-09)"
    - path: "app/api/projects/[id]/weekly-reports/route.ts"
      provides: "GET history via withProjectAccess (D-15)"
    - path: "app/api/projects/[id]/weekly-reports/[reportId]/route.ts"
      provides: "GET shell and PATCH draft (D-15)"
    - path: "app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts"
      provides: "POST submit (D-08, D-15)"
    - path: "app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts"
      provides: "POST correction overlay (D-08, D-15)"
  key_links:
    - from: "app/api/projects/[id]/weekly-reports/[reportId]/route.ts"
      to: "lib/services/weekly-reports.service.ts"
      via: "PATCH calls saveWeeklyReportDraft which 409s when submitted and correction_open is false (D-08)"
      pattern: "saveWeeklyReportDraft"
    - from: "lib/services/weekly-reports.service.ts"
      to: "lib/repositories/weekly-reports.repo.ts"
      via: "submitWeeklyReport inserts weekly_report_versions then updates shell metadata only if first_* is null (D-08)"
      pattern: "insertWeeklyReportVersion"
    - from: "app/api/projects/[id]/weekly-reports/route.ts"
      to: "lib/services/weekly-reports.service.ts"
      via: "GET calls listProjectWeeklyHistory ordered by iso_week descending (D-09)"
      pattern: "listProjectWeeklyHistory"
  prohibitions:
    - UPDATE of weekly_report_versions.snapshot (D-08, WKRP-05)
    - writing RAID masters on draft PATCH (D-11)
    - changing first_submitted_at or first_lateness on correction submit (D-08, WKRP-04)
    - storing overdue as a status (D-05)
    - storing Phase 13 rows in documents or calling the activity-weighted report helper (D-01)
    - modifying /api/projects/[id]/report or /api/export/weekly-report/[id] (D-15)
    - physical row-removal SQL on weekly tables (D-17)
    - a second write-gate wrapper family (D-13)
    - adding npm packages
    - CPMO tracking grid or dashboards (D-18)
---

<objective>
Deliver PM draft/submit versioning (D-06, D-07, D-08, D-09, WKRP-02 through WKRP-06, PERD-03): structured draft on the shell, read-only previous-week RAG, immutable version rows, frozen first lateness, 409 on PATCH of a submitted snapshot, correction via POST /correct, history one row per period newest first.

Purpose: TDD the state machine (not_submitted → draft → submitted → correction overlay → version N+1) before RAID master writes and progress/RAG sync land in 13-03.

Output: Working, tested draft/submit/correct/history API. 13-03 extends the same submitWeeklyReport export with RAID/milestone snapshot assembly and project-field side effects.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md
@.planning/phases/13-weekly-periods-pm-submit/13-RESEARCH.md
@.planning/phases/13-weekly-periods-pm-submit/13-PATTERNS.md
@.planning/phases/13-weekly-periods-pm-submit/13-VALIDATION.md

13-01 created tables, migrateWeeklyReports, createWeeklyPeriod, and shell rows. This plan owns draft columns, version inserts, and project weekly-report routes.

Discretion locked by planner: draft columns on the shell; immutable version rows on submit; POST /api/projects/[id]/weekly-reports/[reportId]/submit and POST .../correct; correction keeps status submitted, sets correction_open true, copies latest snapshot into draft columns; PATCH is allowed when status is not_submitted or draft, or when correction_open is true; PATCH of submitted with correction_open false is ConflictError.

Do not implement RAID master writes, milestone snapshot copies, progress_pct copy, or this_week_rag → projects.rag sync here (13-03 on the same submitWeeklyReport). Do not build tracking grids or dashboards (D-18).

<interfaces>
saveWeeklyReportDraft(projectId, reportId, actor, fields): assertProjectWriteAccess; load shell scoped by project_id AND id; NotFoundError if missing; if status === 'submitted' AND correction_open is not true, throw ConflictError('Submitted report cannot be edited'); strip prev_week_rag (and any other non-allowlist keys) before UPDATE; allowlist is highlights, completed_work, next_week_goals, nearest_milestone, nearest_milestone_id, raid_dependency, leadership_support, this_week_rag, draft_raid_json; this_week_rag if present must be Green | Amber | Red | Not applicable else ValidationError; first successful save of any allowlisted field sets status draft when it was not_submitted (D-06, D-07).

getWeeklyReportShell(projectId, actor, reportId): assertProjectAccess; if prev_week_rag is null, compute and persist once: prior period for the same company with max iso_week strictly less than current; latest submitted version.rag (fallback snapshot.this_week_rag); else projects.rag via getProject in projects.repo. Return shell including prev_week_rag read-only (D-07).

submitWeeklyReport(projectId, reportId, actor): assertProjectWriteAccess; load shell+period; allow when status is draft or not_submitted OR correction_open is true; require this_week_rag in the allowlisted set; INSERT weekly_report_versions with version = latest_version + 1 (first is 1), snapshot JSON locked this plan: { highlights, completed_work, next_week_goals, nearest_milestone: { text, milestone_id }, raid_dependency, leadership_support, this_week_rag, prev_week_rag, draft_raid_json }; denormalize rag = this_week_rag; progress_pct may be null this plan; submitted_at now, submitted_by actor.user_id. UPDATE shell status submitted, latest_version, correction_open false. If first_submitted_at is null, set it to now and first_lateness to on_time if now &lt;= period.due_at else late. Never overwrite first_* on later submits. auditLog entity_type weekly_report, action weekly_submit on first submit and weekly_correct otherwise (D-08, D-14, PERD-03). Do not call createRisk/updateRisk/createIssue/updateIssue in this plan.

openWeeklyReportCorrection(projectId, reportId, actor, body): assertProjectWriteAccess; require status submitted; set correction_open true; copy latest version snapshot fields into draft columns; overlay body allowlisted keys when provided; do not insert a version row; do not change first_* (D-08).

listProjectWeeklyHistory(projectId, actor): assertProjectAccess; one row per shell/period for that project; ORDER BY weekly_periods.iso_week DESC; columns: display_name, iso_week, status, overdue (isWeeklyReportOverdue), rag (latest version.rag), submitted_at, submitted_by, first_lateness, latest_version, report_id, period_id (D-09, WKRP-06).

Repo: updateWeeklyReportDraft scoped WHERE id AND project_id AND (status IN ('not_submitted','draft') OR correction_open IS TRUE) RETURNING *; insertWeeklyReportVersion INSERT only — no UPDATE of snapshot; listProjectWeeklyHistoryRepo join weekly_reports → weekly_periods → weekly_report_versions ON (report_id AND version = latest_version). IDOR: every shell read/write includes project_id.

Routes (D-15): GET /api/projects/[id]/weekly-reports withProjectAccess → listProjectWeeklyHistory. GET/PATCH /api/projects/[id]/weekly-reports/[reportId] withProjectAccess; PATCH Zod weeklyReportDraftSchema. POST .../submit and POST .../correct withProjectAccess. Analog app/api/projects/[id]/pm-assignments/route.ts and risks/route.ts. withAuth maps ConflictError to 409.

Route tests analog app/api/projects/[id]/risks/route.test.ts: vi.mock hasActivePmAssignment (not getProjectPmIdentity); ownerSession pm, viewerSession, cpmoSession. This plan proves PM PATCH draft 200, submitted PATCH 409, submit 201, history newest-first. Full Viewer 403 matrix is 13-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="13-02-01">
  <name>Draft save, prev-week RAG, not_submitted to draft</name>
  <files>lib/repositories/weekly-reports.repo.ts, lib/repositories/weekly-reports.repo.test.ts, lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/schema.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts</files>
  <read_first>
  - lib/services/weekly-reports.service.ts from 13-01
  - lib/repositories/weekly-reports.repo.ts from 13-01
  - lib/services/risks.service.ts assertProjectWriteAccess before repo
  - lib/services/access.ts assertProjectWriteAccess / assertProjectAccess
  - app/api/projects/[id]/risks/route.ts GET/PATCH shape
  - app/api/projects/[id]/risks/route.test.ts hasActivePmAssignment mock
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md D-06 D-07 D-11
  </read_first>
  <behavior>
  - saveWeeklyReportDraft does not call the repo when assertProjectWriteAccess rejects (D-13, WKRP-02).
  - First PATCH of highlights on a not_submitted shell persists the field and sets status draft (D-06, WKRP-02).
  - PATCH including prev_week_rag leaves the stored prev_week_rag unchanged (D-07, WKRP-03).
  - getWeeklyReportShell with no prior submitted version sets prev_week_rag from projects.rag; with a prior submitted version uses that version rag (D-07, WKRP-03).
  - PATCH with draft_raid_json does not call createRisk, updateRisk, createIssue, or updateIssue (D-11, RAID-02).
  - HTTP PATCH as assigned PM returns the updated shell (D-15).
  </behavior>
  <action>
  RED: write failing service unit tests (and route test) for write gate, status transition, prev_week_rag strip/prefill, and no RAID service calls on PATCH. Commit test(13-02): add failing tests for draft save and prev RAG. Tests must fail.

  GREEN: implement getWeeklyReportShell, saveWeeklyReportDraft, repo allowlisted UPDATE, GET/PATCH route + Zod schema per interfaces. Mock weekly-reports.repo, projects.repo getProject, risks.service, and issues.service. Mock hasActivePmAssignment in the route test. Do not persist draft_raid_json into risks or issues tables (D-11). Do not add pages (D-16).

  REFACTOR if needed once green; tests must still pass. Commit feat(13-02): implement draft save and prev RAG.
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts</automated>
  </verify>
  <done>PM can draft structured fields; previous-week RAG is prefilled and not client-writable; draft RAID JSON does not write masters (D-06, D-07, D-11, WKRP-02, WKRP-03).</done>
  <acceptance_criteria>
  - saveWeeklyReportDraft uses assertProjectWriteAccess
  - status moves not_submitted to draft on first allowlisted save
  - prev_week_rag is not taken from PATCH body
  - draft_raid_json PATCH does not invoke risks.service or issues.service
  - npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="13-02-02">
  <name>Submit versions, first lateness, correct, history</name>
  <files>lib/repositories/weekly-reports.repo.ts, lib/repositories/weekly-reports.repo.test.ts, lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, app/api/projects/[id]/weekly-reports/route.ts, app/api/projects/[id]/weekly-reports/route.test.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts, app/api/projects/[id]/weekly-reports/[reportId]/submit/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts, app/api/projects/[id]/weekly-reports/[reportId]/correct/route.ts, app/api/projects/[id]/weekly-reports/[reportId]/correct/route.test.ts</files>
  <read_first>
  - lib/services/weekly-reports.service.ts from 13-02-01
  - lib/repositories/audit.repo.ts append-only insert analog
  - lib/api-errors.ts ConflictError → 409
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md D-05 D-08 D-09
  </read_first>
  <behavior>
  - submitWeeklyReport inserts version 1, sets status submitted, latest_version 1, first_submitted_at, and first_lateness on_time when now is at or before due_at (D-08, WKRP-04, WKRP-05).
  - submitWeeklyReport after due_at still succeeds and sets first_lateness late (D-05, PERD-03).
  - saveWeeklyReportDraft on a submitted shell with correction_open false throws ConflictError and does not UPDATE draft columns (D-08, WKRP-05).
  - openWeeklyReportCorrection then submitWeeklyReport inserts version 2; first_submitted_at and first_lateness stay as on the first submit; auditLog action is weekly_correct (D-08, WKRP-04, WKRP-05).
  - insertWeeklyReportVersion has no snapshot UPDATE path (D-08, D-17).
  - listProjectWeeklyHistory returns one row per period, newest iso_week first, including computed overdue for draft/not_submitted past due (D-09, WKRP-06, PERD-03).
  - HTTP POST submit returns 201; PATCH submitted returns 409 (D-15).
  </behavior>
  <action>
  RED: failing tests for version insert, lateness freeze, ConflictError on submitted PATCH, correction N+1, history order, and overdue flag on history rows. Commit test(13-02) only if 13-02-01 already committed its RED; otherwise extend the failing suite then implement.

  GREEN: implement insertWeeklyReportVersion (INSERT only), submitWeeklyReport, openWeeklyReportCorrection, listProjectWeeklyHistory, and the submit/correct/history routes per interfaces. Snapshot this plan includes structured fields plus draft_raid_json as stored on the shell — do not call RAID or milestone services (13-03 extends submitWeeklyReport). Never UPDATE version snapshot rows (D-08). Never set first_* on correction (WKRP-04). Late submit must succeed (PERD-03).

  Route tests mock hasActivePmAssignment. Do not add a tracking grid (D-18). Do not touch the activity-weighted report route (D-15).
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/correct/route.test.ts</automated>
  </verify>
  <done>Submitted reports are immutable snapshots; corrections add versions; first lateness is frozen; history is one newest-first row per period; late submit is allowed (D-05, D-08, D-09, PERD-03, WKRP-04, WKRP-05, WKRP-06).</done>
  <acceptance_criteria>
  - weekly_report_versions rows are inserted, not updated
  - PATCH submitted with correction_open false throws ConflictError
  - correction submit increments latest_version and leaves first_lateness unchanged
  - listProjectWeeklyHistory is one row per period ordered by iso_week descending
  - npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/correct/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PATCH/POST project weekly-report routes | Untrusted actor; Viewer must not mutate; PM must be in assignment window |
| Service → weekly_reports UPDATE / weekly_report_versions INSERT | Scoped WHERE project_id AND id is the IDOR control; versions are append-only |
| Client → PATCH body prev_week_rag | Untrusted field; must not overwrite server-copied previous RAG |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Elevation of Privilege | saveWeeklyReportDraft / submitWeeklyReport / openWeeklyReportCorrection | high | mitigate | assertProjectWriteAccess before repo; Viewer ForbiddenError (D-13, ASVS V4) |
| T-13-06 | Elevation of Privilege | weekly-reports.repo UPDATE/SELECT | high | mitigate | WHERE id = ? AND project_id = ?; zero rows → NotFoundError (ASVS V4) |
| T-13-03 | Tampering | weekly_report_versions.snapshot | high | mitigate | INSERT-only version helper; PATCH submitted + correction_open false → ConflictError 409 (D-08, WKRP-05) |
| T-13-12 | Tampering | prev_week_rag | medium | mitigate | Strip from PATCH allowlist; compute at draft-open only (D-07, ASVS V5) |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-13-13 | Tampering | draft_raid_json on PATCH | high | mitigate | Persist JSON on the shell only; do not call risks/issues services until 13-03 submit (D-11) |
</threat_model>

<verification>
- 13-02-01: draft save + prev RAG service and PATCH route tests green
- 13-02-02: submit/correct/history + 409 tests green
- Wave: npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/correct/route.test.ts
- TDD gates: git log contains test(13-02) before feat(13-02)
</verification>

<success_criteria>
PM drafts and submits versioned weekly reports; previous-week RAG is read-only; first lateness is immutable; submitted snapshots are not patched in place; history is one newest-first row per period (WKRP-02, WKRP-03, WKRP-04, WKRP-05, WKRP-06, PERD-03, D-06, D-07, D-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/13-weekly-periods-pm-submit/13-02-SUMMARY.md` when done
</output>
