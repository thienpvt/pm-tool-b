---
phase: 13-weekly-periods-pm-submit
plan: 03
type: tdd
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - lib/services/errors.ts
  - lib/services/errors.unit.test.ts
  - lib/api-errors.ts
  - lib/api-errors.test.ts
  - lib/services/weekly-reports.service.ts
  - lib/services/weekly-reports.service.unit.test.ts
  - lib/repositories/weekly-reports.repo.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts
  - app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts
  - app/api/projects/[id]/weekly-reports/route.access.test.ts
  - app/api/weekly-periods/route.test.ts
autonomous: true
requirements: [MS-04, RAID-02, RAID-03, WKRP-03]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Submit validates draft_raid_json then writes masters only through createRisk/updateRisk/createIssue/updateIssue; on success the version snapshot.raid holds locked copies of those master rows; later master edits do not UPDATE old snapshots (D-11, RAID-02, RAID-03)."
    - "Submit validation failure throws SubmitValidationError and maps to HTTP 400 { error, fields: [...] } with no version row and no master writes (D-11, RAID-03)."
    - "Submit stores snapshot.milestones from nearest text plus the selected milestone_id row (plan_end, adjusted_end, status); later milestone cancel/edit does not change that snapshot (D-12, MS-04)."
    - "Submit copies live projects.progress_pct into the version row and snapshot and never writes that column back; if this_week_rag differs from projects.rag, only rag is updated (D-10, WKRP-03)."
    - "listPeriodShells is company-scoped for Phase 14; listProjectWeeklyHistory remains the Phase 16 helper; Viewer PATCH/submit/correct is 403; CPMO period routes stay withCpmo (D-13, D-18)."
  artifacts:
    - path: "lib/services/errors.ts"
      provides: "SubmitValidationError with fields: string[] (D-11, RAID-03)"
      exports: ["SubmitValidationError"]
    - path: "lib/api-errors.ts"
      provides: "serviceErrorResponse maps SubmitValidationError to 400 { error, fields } (D-11)"
    - path: "lib/services/weekly-reports.service.ts"
      provides: "submitWeeklyReport RAID/milestone/progress/RAG side effects; listPeriodShells (D-10, D-11, D-12, D-18)"
      exports: ["listPeriodShells", "listProjectWeeklyHistory", "submitWeeklyReport"]
    - path: "app/api/projects/[id]/weekly-reports/route.access.test.ts"
      provides: "Viewer 403 mutate; PM without assignment 403; CPMO GET history 200 (D-13)"
  key_links:
    - from: "lib/services/weekly-reports.service.ts"
      to: "lib/services/risks.service.ts"
      via: "submit applies id new via createRisk and numeric id via updateRisk with the same actor (D-11)"
      pattern: "createRisk"
    - from: "lib/api-errors.ts"
      to: "lib/services/errors.ts"
      via: "instanceof SubmitValidationError returns 400 fields array (D-11)"
      pattern: "SubmitValidationError"
    - from: "lib/services/weekly-reports.service.ts"
      to: "lib/repositories/projects.repo.ts"
      via: "READ progress_pct into snapshot; UPDATE rag only when this_week_rag differs (D-10)"
      pattern: "progress_pct"
  prohibitions:
    - UPDATE projects.progress_pct from weekly submit (D-10)
    - physical row-removal on milestones, risks, issues, periods, shells, or versions (D-12, D-17)
    - calling deactivateRisk/deactivateIssue from weekly submit unless a draft field explicitly requires it (default: reject deactivated ids in validation) (D-11)
    - UPDATE of weekly_report_versions.snapshot after insert (D-08)
    - storing Phase 13 rows in documents or calling the activity-weighted report helper (D-01)
    - modifying /api/projects/[id]/report or /api/export/weekly-report/[id] (D-15)
    - a second write-gate wrapper family or leftover ops/admin gating (D-13)
    - adding npm packages
    - CPMO tracking grid, tick-select, or consolidated pack UI (D-18)
---

<objective>
Finish submit as RAID/milestone snapshot assembly plus progress copy and RAG sync (D-10, D-11, D-12, D-18, MS-04, RAID-02, RAID-03, WKRP-03), export listPeriodShells / listProjectWeeklyHistory, and lock access tests.

Purpose: TDD the submit transaction that validates multi-field RAID, writes masters through existing services, freezes snapshots, and copies progress without write-back — the Phase 14/16 contract.

Output: Working, tested submit side effects, SubmitValidationError HTTP mapping, and company-scoped list helpers. No tracking grid and no dashboards.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md
@.planning/phases/13-weekly-periods-pm-submit/13-RESEARCH.md
@.planning/phases/13-weekly-periods-pm-submit/13-PATTERNS.md
@.planning/phases/13-weekly-periods-pm-submit/13-VALIDATION.md

13-02 already inserts version rows from draft columns. This plan extends submitWeeklyReport in place and adds SubmitValidationError plus listPeriodShells.

Discretion locked by planner: snapshot JSON { this_week_rag, prev_week_rag, highlights, completed_work, next_week_goals, nearest_milestone: { text, milestone_id }, raid_dependency, leadership_support, raid: { risks: [...master rows], issues: [...master rows] }, milestones: [...copied rows with id, name, plan_end, adjusted_end, status, end_date], progress_pct }; draft_raid_json entries { id: number | 'new', fields: {...} }; SubmitValidationError in lib/services/errors.ts mapped globally in serviceErrorResponse; listPeriodShells(companyId, periodId, actor) uses assertCompanyWrite and requires actor.company_id === companyId.

Do not implement Phase 14 grid/export pack or Phase 16 dashboard pages. Incremental auditLog on submit/correct already exists from 13-02.

<interfaces>
SubmitValidationError extends Error { name = 'SubmitValidationError'; fields: string[] }. Do not subclass ValidationError (that mapper emits a single field key). serviceErrorResponse: if e instanceof SubmitValidationError return 400 JSON { error: e.message, fields: e.fields } before the generic 500. Keep existing ValidationError { error, field? } unchanged. Add lib/api-errors.test.ts and errors.unit.test.ts cases.

submitWeeklyReport order in one logical transaction (D-11, D-10, D-12):
1. assertProjectWriteAccess (already).
2. Validate structured this_week_rag plus draft_raid_json. New risk/issue rows require non-empty description. Numeric ids must exist on the project and must not be deactivated (deactivated_at set or status deactivated) — collect every problem into fields paths like raid.risks[0].description, raid.issues[1].id, nearest_milestone_id. If nearest_milestone_id is set, call getMilestone(projectId, id) in this same pre-write pass; missing → add fields nearest_milestone_id. On any problem throw SubmitValidationError and do not INSERT a version and do not call create/update RAID services.
3. For each risks entry: id 'new' → createRisk(projectId, actor, fields); numeric id → updateRisk(projectId, actor, id, fields). Same for issues via createIssue/updateIssue. Pass the same actor. Do not call deactivate helpers from this path.
4. getProject(projectId) from projects.repo. Copy progress_pct (?? 0) into version.progress_pct and snapshot.progress_pct. Do not pass progress_pct to updateProject.
5. If this_week_rag !== project.rag, updateProject(projectId, { rag: this_week_rag }) via projects.repo only (skip projects.service so L5 governance does not rewrite).
6. Milestone snapshot: always include nearest_milestone text. Use the getMilestone result already loaded in step 2 (do not re-validate after RAID writes). Copy id, name, plan_end, adjusted_end, status, end_date into snapshot.milestones. Do not UPDATE or remove milestone rows.
7. After RAID writes, SELECT getRisk/getIssue for referenced ids (including newly created) and store locked copies in snapshot.raid.
8. INSERT version with the full snapshot; UPDATE shell as 13-02 already does; auditLog unchanged.

listPeriodShells(companyId, periodId, actor): assertCompanyWrite(actor); if actor.company_id !== companyId throw ForbiddenError; load period by id AND company_id else NotFoundError; return shells with project_id, status, overdue (isWeeklyReportOverdue), rag (latest version), first_lateness, first_submitted_at, latest_version (D-18). Do not add HTTP for the tracking grid.

Access tests (D-13): new app/api/projects/[id]/weekly-reports/route.access.test.ts driving real withProjectAccess + services with mocked repos. Viewer PATCH/submit/correct → 403. PM with hasActivePmAssignment false → 403. CPMO GET history → 200 without assignment. Keep CPMO-only 403 for PM on POST /api/weekly-periods. Mock hasActivePmAssignment, never getProjectPmIdentity.

Extend submit route test: 400 body has fields array (not only field) when SubmitValidationError is thrown.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="13-03-01">
  <name>Submit RAID writes, milestone snapshot, progress copy, RAG sync</name>
  <files>lib/services/errors.ts, lib/services/errors.unit.test.ts, lib/api-errors.ts, lib/api-errors.test.ts, lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, lib/repositories/weekly-reports.repo.ts, app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts</files>
  <read_first>
  - lib/services/weekly-reports.service.ts submitWeeklyReport from 13-02
  - lib/services/errors.ts ValidationError / ConflictError
  - lib/api-errors.ts serviceErrorResponse
  - lib/api-errors.test.ts serviceErrorResponse cases
  - lib/services/risks.service.ts createRisk updateRisk signatures
  - lib/services/issues.service.ts createIssue updateIssue signatures
  - lib/repositories/milestones.repo.ts getMilestone
  - lib/repositories/projects.repo.ts getProject updateProject PROJECT_COLUMNS rag
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md D-10 D-11 D-12
  </read_first>
  <behavior>
  - serviceErrorResponse(new SubmitValidationError('fix fields', ['raid.risks[0].description'])) returns 400 JSON { error: 'fix fields', fields: ['raid.risks[0].description'] } and does not use a singular field key (D-11, RAID-03).
  - Existing ValidationError mapping still returns optional field (not fields) (D-11).
  - submitWeeklyReport with invalid new RAID description throws SubmitValidationError, does not call createRisk, and does not insert a version (D-11, RAID-03).
  - submitWeeklyReport with id new and a valid description calls createRisk then stores the returned row inside snapshot.raid.risks (D-11, RAID-02).
  - submitWeeklyReport with a numeric risk id calls updateRisk and snapshot.raid holds the post-update row; a later mocked master change is not written back onto that snapshot object (D-11, RAID-02).
  - snapshot.milestones includes plan_end, adjusted_end, and status from getMilestone when nearest_milestone_id is set (D-12, MS-04).
  - version.progress_pct equals the project's live progress_pct; updateProject is not called with progress_pct; updateProject is called with { rag } only when this_week_rag differs (D-10, WKRP-03).
  </behavior>
  <action>
  RED: failing errors.unit.test.ts, api-errors.test.ts, and weekly-reports.service.unit.test.ts for SubmitValidationError mapping, RAID validate-then-write, milestone snapshot fields, progress copy, and rag-only project update. Commit test(13-03): add failing tests for submit snapshots and multi-field validation. Tests must fail.

  GREEN: add SubmitValidationError; extend serviceErrorResponse; extend submitWeeklyReport per interfaces. Mock createRisk/updateRisk/createIssue/updateIssue, getMilestone, getProject, updateProject. After successful RAID calls, put the returned master rows into snapshot.raid (locked copy). Copy progress_pct from getProject onto the version insert. Call updateProject only with rag when it differs. Do not call milestone row-removal or RAID deactivate helpers. Do not pass progress_pct into updateProject (D-10). Do not UPDATE version snapshot rows after insert (D-08).

  REFACTOR if needed once green. Commit feat(13-03): implement submit snapshots and SubmitValidationError.
  </action>
  <verify>
    <automated>npx vitest run lib/services/errors.unit.test.ts lib/api-errors.test.ts lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts</automated>
  </verify>
  <done>Submit validates RAID with a fields array, writes masters through existing services, locks RAID and milestone copies in the version snapshot, copies progress_pct, and syncs rag only (D-10, D-11, D-12, MS-04, RAID-02, RAID-03, WKRP-03).</done>
  <acceptance_criteria>
  - SubmitValidationError is exported from lib/services/errors.ts
  - serviceErrorResponse maps it to 400 { error, fields }
  - failed RAID validation does not insert weekly_report_versions
  - successful submit snapshot contains raid and milestones copies
  - updateProject is not invoked with a progress_pct key from submitWeeklyReport
  - npx vitest run lib/services/errors.unit.test.ts lib/api-errors.test.ts lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="13-03-02">
  <name>listPeriodShells and access matrix</name>
  <files>lib/services/weekly-reports.service.ts, lib/services/weekly-reports.service.unit.test.ts, lib/repositories/weekly-reports.repo.ts, app/api/projects/[id]/weekly-reports/route.access.test.ts, app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts, app/api/weekly-periods/route.test.ts</files>
  <read_first>
  - lib/services/weekly-reports.service.ts listProjectWeeklyHistory from 13-02
  - lib/services/raid-masters.service.ts thin company-scoped list analog
  - lib/services/access.ts assertCompanyWrite assertProjectWriteAccess
  - app/api/projects/[id]/risks/route.test.ts session fixtures and hasActivePmAssignment
  - app/api/weekly-periods/route.test.ts from 13-01
  - .planning/phases/13-weekly-periods-pm-submit/13-CONTEXT.md D-13 D-18
  </read_first>
  <behavior>
  - listPeriodShells throws ForbiddenError when assertCompanyWrite fails or actor.company_id does not equal companyId (D-13, D-18, T-13-01).
  - listPeriodShells returns shells for a period owned by that company including computed overdue, status, rag, first_lateness (D-05, D-18).
  - listProjectWeeklyHistory remains exported for Phase 16 and still newest iso_week first (D-09, D-18, WKRP-06).
  - Viewer PATCH, submit, and correct return 403 (D-13).
  - PM session with hasActivePmAssignment false returns 403 on PATCH/submit (D-13).
  - CPMO GET /api/projects/[id]/weekly-reports returns 200 without an assignment mock true-forced beyond CPMO short-circuit (D-13).
  - PM POST /api/weekly-periods remains 403 (D-13).
  </behavior>
  <action>
  RED: failing service tests for listPeriodShells company mismatch and overdue flag; failing route.access.test.ts for Viewer mutate 403, assignment-denied PM 403, CPMO history GET 200. Commit with the 13-03 test prefix if not already committed.

  GREEN: implement listPeriodShells in service+repo per interfaces. Add route.access.test.ts using the risks route test fixtures. Mock hasActivePmAssignment, not getProjectPmIdentity. Do not add a CPMO tracking page or export pack (D-18). Do not invent a policy library (D-13). Do not gate leftover ops/admin routes (D-13). Do not add npm packages.

  Keep listProjectWeeklyHistory as the exported history helper 13-02 created; do not duplicate it under another name.
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.access.test.ts app/api/weekly-periods/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts</automated>
  </verify>
  <done>Phase 14/16 can import listPeriodShells and listProjectWeeklyHistory; Viewer cannot mutate weekly reports; period writes stay CPMO company-scoped (D-13, D-18).</done>
  <acceptance_criteria>
  - weekly-reports.service.ts exports listPeriodShells and listProjectWeeklyHistory
  - listPeriodShells requires assertCompanyWrite and matching company_id
  - Viewer mutate paths return 403
  - route tests mock hasActivePmAssignment
  - npx vitest run lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.access.test.ts app/api/weekly-periods/route.test.ts app/api/projects/[id]/weekly-reports/[reportId]/route.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST submit with draft_raid_json | Untrusted RAID proposals; must not write masters until validation succeeds |
| Service → risks/issues/milestones masters | Submit may update masters; snapshots must freeze copies |
| Service → projects.rag / progress_pct | Only rag may be written; progress_pct is copy-at-submit |
| Client → listPeriodShells(companyId) | Untrusted companyId must match actor.company_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | listPeriodShells | high | mitigate | assertCompanyWrite plus actor.company_id === companyId; period lookup scoped by company_id (D-13, D-18, ASVS V4) |
| T-13-02 | Elevation of Privilege | PATCH/submit/correct | high | mitigate | assertProjectWriteAccess; Viewer 403; assignment-denied PM 403; tests mock hasActivePmAssignment (D-13, ASVS V4) |
| T-13-04 | Tampering | draft_raid_json submit | high | mitigate | Validate all entries first; SubmitValidationError 400 fields; no version row and no master writes on failure (D-11, RAID-03, ASVS V5) |
| T-13-05 | Tampering | projects.progress_pct | high | mitigate | Read progress_pct into snapshot only; updateProject rag-only when this_week_rag differs (D-10) |
| T-13-14 | Tampering | snapshot.milestones / snapshot.raid | high | mitigate | Locked copies at submit; no UPDATE of weekly_report_versions.snapshot; no milestone/RAID row-removal (D-11, D-12, MS-04) |
| T-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-13-15 | Information Disclosure | SubmitValidationError fields | low | accept | Field paths name draft indexes the actor already submitted; ASVS L1 accepts this over a generic 400 |
</threat_model>

<verification>
- 13-03-01: SubmitValidationError + submit snapshot/progress/RAG tests green
- 13-03-02: listPeriodShells + access matrix green
- Wave: npx vitest run lib/services/errors.unit.test.ts lib/api-errors.test.ts lib/services/weekly-reports.service.unit.test.ts app/api/projects/[id]/weekly-reports/route.access.test.ts app/api/projects/[id]/weekly-reports/[reportId]/submit/route.test.ts app/api/weekly-periods/route.test.ts
- TDD gates: git log contains test(13-03) before feat(13-03)
</verification>

<success_criteria>
Submit writes RAID through existing services then locks RAID and milestone snapshots; progress is copied not written back; rag syncs when changed; validation failures return 400 fields; list helpers are exported; Viewer cannot mutate (MS-04, RAID-02, RAID-03, WKRP-03, D-10, D-11, D-12, D-13, D-18).
</success_criteria>

<output>
Create `.planning/phases/13-weekly-periods-pm-submit/13-03-SUMMARY.md` when done
</output>
