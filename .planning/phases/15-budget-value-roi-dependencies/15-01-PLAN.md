---
phase: 15-budget-value-roi-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-fiscal-budget.ts
  - lib/db-fiscal-budget.ddl.unit.test.ts
  - lib/db.ts
  - lib/fiscal/vnd.ts
  - lib/fiscal/vnd.unit.test.ts
  - lib/fiscal/budget-metrics.ts
  - lib/fiscal/budget-metrics.unit.test.ts
  - lib/repositories/fiscal-budget.repo.ts
  - lib/repositories/fiscal-budget.repo.test.ts
  - lib/repositories/budget-adjustments.repo.ts
  - lib/services/fiscal-budget.service.ts
  - lib/services/fiscal-budget.service.unit.test.ts
  - app/api/projects/[id]/fiscal-budget/route.ts
  - app/api/projects/[id]/fiscal-budget/route.test.ts
  - app/api/projects/[id]/fiscal-budget/[budgetId]/adjustments/route.ts
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A write-access user POSTs /api/projects/[id]/fiscal-budget with fiscal_year, cost_type CAPEX or OPEX, and non-negative integer approved_amount_vnd (optional actual_amount_vnd default 0) and gets 201 with the unique (project_id, fiscal_year, cost_type) row (D-01, D-02, D-05, BUDG-01)."
    - "GET /api/projects/[id]/fiscal-budget returns each row plus computed metrics: remaining_vnd = approved_net - actual, utilization = actual / approved_net when approved_net > 0 else null, status over_budget when remaining < 0, fully_used when remaining === 0 and approved_net > 0, insufficient when approved_net <= 0 — flags are not stored columns (D-03, BUDG-02)."
    - "PATCH /api/projects/[id]/fiscal-budget updates actual_amount_vnd only (spend reporting); a second POST for the same project/year/cost_type is 409 ConflictError; Viewer mutators are 403 (D-04, D-05, BUDG-01)."
    - "POST /api/projects/[id]/fiscal-budget/[budgetId]/adjustments INSERTs amount_vnd (signed integer, not 0), effective_date, and required reason; the approved baseline column is unchanged after create; auditLog fires on fiscal create and adjustment insert (D-04, BUDG-03)."
    - "migrateFiscalBudget runs from getDb after migrateWeeklyReports via settings flag fiscal_budget_ddl_v1; DDL creates project_fiscal_budgets, budget_adjustments, financial_benefits, nonfinancial_benefits, and project_dependencies with BIGINT VND amounts and no row-removal helpers (D-09, D-12)."
    - "computeFiscalBudgetMetrics is a named export from lib/fiscal/budget-metrics.ts for Phase 16 (D-16)."
  artifacts:
    - path: "lib/db-fiscal-budget.ts"
      provides: "FISCAL_BUDGET_DDL_FLAG fiscal_budget_ddl_v1, FISCAL_BUDGET_DDL, migrateFiscalBudget invoked from getDb after migrateWeeklyReports (D-12)"
      exports: ["migrateFiscalBudget", "FISCAL_BUDGET_DDL", "FISCAL_BUDGET_DDL_FLAG"]
    - path: "lib/fiscal/vnd.ts"
      provides: "parseNonNegativeVnd and parseSignedNonZeroVnd (D-02, BUDG-01)"
      exports: ["parseNonNegativeVnd", "parseSignedNonZeroVnd"]
    - path: "lib/fiscal/budget-metrics.ts"
      provides: "computeFiscalBudgetMetrics remaining/utilization/status (D-03, D-16, BUDG-02)"
      exports: ["computeFiscalBudgetMetrics"]
    - path: "lib/repositories/fiscal-budget.repo.ts"
      provides: "insert, list, scoped get, actual-only updater, find by unique key (D-02, D-04)"
    - path: "lib/repositories/budget-adjustments.repo.ts"
      provides: "insertBudgetAdjustment and listBudgetAdjustments plus sumAdjustmentsVnd (D-04, BUDG-03)"
    - path: "lib/services/fiscal-budget.service.ts"
      provides: "createFiscalBudget, getFiscalBudgetOverview, patchFiscalBudgetActual, addBudgetAdjustment with assertProjectWriteAccess on mutators (D-05)"
      exports: ["createFiscalBudget", "getFiscalBudgetOverview", "patchFiscalBudgetActual", "addBudgetAdjustment"]
    - path: "app/api/projects/[id]/fiscal-budget/route.ts"
      provides: "GET/POST/PATCH withProjectAccess fiscal-budget (D-13)"
    - path: "app/api/projects/[id]/fiscal-budget/[budgetId]/adjustments/route.ts"
      provides: "POST-only adjustment insert (D-04, D-13)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-fiscal-budget.ts"
      via: "getDb awaits migrateFiscalBudget(pool) immediately after migrateWeeklyReports(pool) (D-12)"
      pattern: "migrateFiscalBudget"
    - from: "app/api/projects/[id]/fiscal-budget/route.ts"
      to: "lib/services/fiscal-budget.service.ts"
      via: "GET getFiscalBudgetOverview; POST createFiscalBudget; PATCH patchFiscalBudgetActual (D-05, D-13)"
      pattern: "getFiscalBudgetOverview"
    - from: "lib/services/fiscal-budget.service.ts"
      to: "lib/fiscal/budget-metrics.ts"
      via: "GET maps computeFiscalBudgetMetrics(approved, sumAdjustmentsVnd, actual) (D-03)"
      pattern: "computeFiscalBudgetMetrics"
    - from: "app/api/projects/[id]/fiscal-budget/[budgetId]/adjustments/route.ts"
      to: "lib/services/fiscal-budget.service.ts"
      via: "POST addBudgetAdjustment(params.id, params.budgetId, actor, body) (D-04)"
      pattern: "addBudgetAdjustment"
  prohibitions:
    - storing spec fiscal rows in budget_items or budget_expenses (D-01)
    - importing lib/repositories/budget.repo.ts from fiscal services or fiscal repos (D-01)
    - changing /api/projects/[id]/budget or /api/portfolio/budget* handlers (D-13)
    - overwriting approved_amount_vnd after insert (D-04, BUDG-03)
    - stored over_budget, fully_used, utilization, or roi columns (D-03)
    - row-removal SQL helpers on fiscal, adjustment, benefit, or dependency tables (D-12)
    - Prisma, an external migrate CLI, or a new package install (D-12)
    - CASL / a second write-gate wrapper family / leftover ops/admin gating (D-15)
    - UI-SPEC, dashboard tiles, or treating a thin page as a gate (D-14, D-16)
    - benefits or ROI HTTP (15-02) and dependency HTTP (15-03)
---

<objective>
D-01: parallel fiscal tables/routes, not budget_items. D-02: project_fiscal_budgets unique year/cost_type integer VND. D-03: computed remaining/utilization/over_budget/fully_used. D-04: INSERT-only adjustments; PATCH actual only. D-05: write assertProjectWriteAccess, read assertProjectAccess. D-09: project_dependencies table in this migrate. D-12: lib/db-fiscal-budget.ts after migrateWeeklyReports. D-13: /fiscal-budget plus adjustments. D-14: server tests are the gate. D-15: no CASL, no leftover ops/admin re-gate. D-16: export computeFiscalBudgetMetrics; no dashboard tiles.

Prove the Phase 15 fiscal ledger spine (BUDG-01, BUDG-02, BUDG-03): settings-flag DDL after weekly migrate; integer VND parsers; computed remaining/utilization; POST create + GET metrics + PATCH actual; append-only adjustments with auditLog.

Purpose: Catch an architectural dead-end on the first commit — parallel fiscal tables (not line-item budget_items), computed flags (not stored status), INSERT-only approval changes — before benefits/ROI and dependencies expand.

Output: Production-quality tracer. 15-02 adds benefits and ROI; 15-03 adds cross-project dependencies against tables already created here. Server tests are the gate (D-14).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md
@.planning/phases/15-budget-value-roi-dependencies/15-RESEARCH.md
@.planning/phases/15-budget-value-roi-dependencies/15-PATTERNS.md
@.planning/phases/15-budget-value-roi-dependencies/15-VALIDATION.md

Discretion locked by planner: cost_type allowlist CAPEX | OPEX; first POST creates the unique (project_id, fiscal_year, cost_type) row with initial approved_amount_vnd; later approved changes are INSERT-only budget_adjustments; PATCH actual_amount_vnd is spend reporting. File layout follows RESEARCH (lib/fiscal/vnd.ts, lib/fiscal/budget-metrics.ts) not PATTERNS.md lib/fiscal-budget/compute.ts. Zod for fiscal-budget lives at the top of each route.ts (no extra schema.ts — files_modified cap). Adjustment HTTP is nested [budgetId]/adjustments POST-only analog app/api/projects/[id]/budget/[itemId]/expenses/route.ts with withProjectAccess&lt;{ id: string; budgetId: string }&gt;. All five tables are created in this plan's migrate so 15-02 and 15-03 do not touch lib/db-fiscal-budget.ts.

Do not implement deferred items: dashboards (Phase 16 may import computeFiscalBudgetMetrics), document templates (Phase 17), full audit coverage (Phase 18). Do not build benefits/ROI/dependency routes here.

Prior-phase artifact: migrateWeeklyReports already awaited in getDb; assertProjectWriteAccess / assertProjectAccess; withProjectAccess; auditLog; ConflictError / ValidationError / ForbiddenError / NotFoundError; isUniqueViolation analog in stakeholders.service (pg code 23505); skipIf(!hasTestDb) repo tests.

<interfaces>
FISCAL_BUDGET_DDL_FLAG = 'fiscal_budget_ddl_v1'. Copy settingsFlagExists / writeSettingsFlag / try-catch retry from lib/db-weekly-reports.ts migrateWeeklyReports. migrateFiscalBudget(pool) runs DDL then optional indexes, swallows missing-settings errors.

FISCAL_BUDGET_DDL creates five tables (this plan wires fiscal + adjustments; 15-02/15-03 consume the rest):

project_fiscal_budgets: id SERIAL PK; project_id INTEGER NOT NULL REFERENCES projects(id); fiscal_year INTEGER NOT NULL; cost_type TEXT NOT NULL; approved_amount_vnd BIGINT NOT NULL; actual_amount_vnd BIGINT NOT NULL DEFAULT 0; created_at TIMESTAMPTZ DEFAULT now(); UNIQUE (project_id, fiscal_year, cost_type). No unit column. CHECK cost_type IN ('CAPEX','OPEX'); CHECK approved_amount_vnd &gt;= 0; CHECK actual_amount_vnd &gt;= 0.

budget_adjustments: id SERIAL PK; fiscal_budget_id INTEGER NOT NULL REFERENCES project_fiscal_budgets(id); amount_vnd BIGINT NOT NULL; effective_date DATE NOT NULL; reason TEXT NOT NULL; created_by INTEGER REFERENCES users(id); created_at TIMESTAMPTZ DEFAULT now(). CHECK amount_vnd &lt;&gt; 0. No UPDATE/DELETE helpers.

financial_benefits: id SERIAL PK; project_id INTEGER NOT NULL REFERENCES projects(id); fiscal_year INTEGER NOT NULL; benefit_type TEXT NOT NULL; expected_vnd BIGINT NOT NULL; actual_vnd BIGINT; UNIQUE (project_id, fiscal_year, benefit_type); CHECK benefit_type IN ('COST_SAVING','REVENUE','PRODUCTIVITY'); CHECK expected_vnd &gt;= 0.

nonfinancial_benefits: id SERIAL PK; project_id INTEGER NOT NULL REFERENCES projects(id); group_name TEXT NOT NULL; measure TEXT NOT NULL; target TEXT NOT NULL; actual_text TEXT; created_at TIMESTAMPTZ DEFAULT now().

project_dependencies: id SERIAL PK; from_project_id INTEGER NOT NULL REFERENCES projects(id); to_project_id INTEGER NOT NULL REFERENCES projects(id); dependency_type TEXT NOT NULL; need_by DATE NOT NULL; effective_from DATE NOT NULL; effective_to DATE; notes TEXT; created_by INTEGER REFERENCES users(id); created_at TIMESTAMPTZ DEFAULT now(); CHECK dependency_type IN ('FINISH_TO_START','START_TO_START','FINISH_TO_FINISH','START_TO_FINISH','BLOCKS').

getDb (lib/db.ts ~631-633): after await migrateWeeklyReports(pool), dynamically import migrateFiscalBudget and await it before backfillWeightedCompletion.

parseNonNegativeVnd(value, field): Number.isSafeInteger and &gt;= 0 else ValidationError(field). parseSignedNonZeroVnd: Number.isSafeInteger and !== 0 else ValidationError(field). parseFiscalYear: Number.isInteger in 1900..2100 else ValidationError('fiscal_year'). parseCostType: only 'CAPEX' | 'OPEX' else ValidationError('cost_type').

FiscalBudgetMetrics: { approved_net_vnd, actual_amount_vnd, remaining_vnd, utilization: number | null, status: 'ok' | 'over_budget' | 'fully_used' | 'insufficient' }. computeFiscalBudgetMetrics(approvedBaseline, adjustmentSum, actual): approved_net = baseline + adjustmentSum; remaining = approved_net - actual; if approved_net &lt;= 0 then utilization null and status insufficient; else utilization = actual / approved_net; remaining &lt; 0 → over_budget; remaining === 0 → fully_used; else ok. Coerce pg BIGINT strings with Number() before calling.

Repo (fiscal-budget.repo.ts): listFiscalBudgets(projectId) ORDER BY fiscal_year DESC, cost_type; getFiscalBudgetInProject(projectId, budgetId) WHERE id AND project_id; findFiscalBudgetByKey(projectId, fiscalYear, costType); insertFiscalBudget; updateFiscalBudgetActual(projectId, budgetId, actual) — SQL SET actual_amount_vnd only.

Repo (budget-adjustments.repo.ts): insertBudgetAdjustment INSERT RETURNING *; listBudgetAdjustments(fiscalBudgetId); sumAdjustmentsVnd(fiscalBudgetId) COALESCE(SUM(amount_vnd),0). Export only those three functions.

Service: assertProjectAccess on GET; assertProjectWriteAccess first on create/patch/adjust. Duplicate key → ConflictError (pre-check findFiscalBudgetByKey and map pg 23505). addBudgetAdjustment: getFiscalBudgetInProject else NotFoundError('Not found','fiscal_budget'); parseSignedNonZeroVnd; reason trim required; insert; auditLog entity_type 'budget_adjustment' action 'create'. createFiscalBudget auditLog entity_type 'fiscal_budget' action 'create'. patchFiscalBudgetActual body.id required; auditLog action 'update' with before/after snapshots of actual_amount_vnd.

Routes analog app/api/projects/[id]/stakeholders/route.ts. GET JSON array of { ...row, adjustments, metrics }. POST 201. PATCH body { id, actual_amount_vnd }. Adjustments POST 201. Error mapping stays in withAuth via serviceErrorResponse. Route tests analog stakeholders/route.test.ts (ownerSession, viewerSession, cpmoSession): 401 no session; 403 viewer POST/PATCH; 200/201 pm. Import nested POST from [budgetId]/adjustments/route.ts inside fiscal-budget/route.test.ts (do not add a 16th test file).

Isolation none. TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/pm_tool_test for skipIf(!hasTestDb) repo tests; name must end in _test.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="15-01-01">
  <name>End-to-end fiscal create, GET metrics, PATCH actual</name>
  <precondition>lib/db.ts already awaits migrateWeeklyReports(pool) before backfillWeightedCompletion</precondition>
  <reversibility rating="costly">Additive fiscal/benefit/dependency tables and /fiscal-budget become a published contract; undo is another migrate plus caller churn (D-01, D-12, D-13).</reversibility>
  <files>lib/db-fiscal-budget.ts, lib/db-fiscal-budget.ddl.unit.test.ts, lib/db.ts, lib/fiscal/vnd.ts, lib/fiscal/vnd.unit.test.ts, lib/fiscal/budget-metrics.ts, lib/fiscal/budget-metrics.unit.test.ts, lib/repositories/fiscal-budget.repo.ts, lib/repositories/fiscal-budget.repo.test.ts, lib/services/fiscal-budget.service.ts, lib/services/fiscal-budget.service.unit.test.ts, app/api/projects/[id]/fiscal-budget/route.ts, app/api/projects/[id]/fiscal-budget/route.test.ts</files>
  <read_first>
  - .planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md (D-01 through D-05, D-12, D-13)
  - .planning/phases/15-budget-value-roi-dependencies/15-VALIDATION.md (BUDG-01, BUDG-02 commands)
  - lib/db-weekly-reports.ts (settings flags, try/catch orchestrator)
  - lib/db-weekly-reports.ddl.unit.test.ts
  - lib/db.ts getDb migrateWeeklyReports wire (~631-633)
  - lib/http/with-project-access.ts
  - lib/services/access.ts assertProjectWriteAccess
  - lib/services/errors.ts ValidationError ConflictError
  - lib/services/stakeholders.service.ts isUniqueViolation / auditLog
  - app/api/projects/[id]/stakeholders/route.ts and route.test.ts
  - lib/repositories/weekly-periods.repo.test.ts skipIf(!hasTestDb)
  - test/db.ts TEST_DATABASE_URL _test suffix
  </read_first>
  <behavior>
  - migrateFiscalBudget is exported; FISCAL_BUDGET_DDL_FLAG is fiscal_budget_ddl_v1; joined DDL creates all five tables with UNIQUE (project_id, fiscal_year, cost_type) and BIGINT VND columns; getDb invokes migrateFiscalBudget after migrateWeeklyReports (D-02, D-09, D-12).
  - parseNonNegativeVnd throws ValidationError for 1.5, -1, '', and non-safe integers; accepts 0 and 1000000 (D-02, BUDG-01).
  - computeFiscalBudgetMetrics(100, 0, 40) remaining 60 utilization 0.4 status ok; (100, 0, 100) fully_used; (100, 0, 150) over_budget; (0, 0, 0) insufficient utilization null (D-03, BUDG-02).
  - createFiscalBudget with write actor inserts CAPEX/OPEX row; duplicate year+type throws ConflictError; Viewer assertProjectWriteAccess failure is not caught as success (D-05, BUDG-01).
  - GET overview attaches metrics; PATCH actual_amount_vnd changes spend only (D-04, BUDG-01).
  - HTTP GET 200 / POST 201 for PM; viewer POST 403; missing session 401 (D-05).
  - fiscal-budget.service.ts source does not import the line-item budget repository module (D-01).
  </behavior>
  <action>
  TDD first (D-14). Isolation is none — keep this plan sequential-safe on the main tree.
  RED: add failing lib/db-fiscal-budget.ddl.unit.test.ts cases for the flag, five CREATE TABLE fragments, UNIQUE (project_id, fiscal_year, cost_type), and a read of lib/db.ts asserting migrateFiscalBudget is awaited after migrateWeeklyReports; failing lib/fiscal/vnd.unit.test.ts; failing lib/fiscal/budget-metrics.unit.test.ts matrix; failing skipIf(!hasTestDb) fiscal-budget.repo.test.ts (insert + unique + actual updater); failing fiscal-budget.service.unit.test.ts (create/GET metrics/PATCH/ConflictError/viewer write gate/import guard); failing fiscal-budget/route.test.ts. Tests must fail before production modules exist. Commit `test(15-01): red fiscal budget create GET PATCH`.
  GREEN: implement until green. Commit `feat(15-01): fiscal budget API and computed metrics`.

  Implement lib/db-fiscal-budget.ts and wire getDb per interfaces. Do not add an ORM migrate CLI (D-12). Do not add row-removal helpers for the new tables (D-12).

  Implement parsers and computeFiscalBudgetMetrics per interfaces. Export computeFiscalBudgetMetrics for Phase 16 (D-16). Do not persist status flags (D-03).

  Repos use getDb() and project_id scoping on every read/write. updateFiscalBudgetActual SET list is only actual_amount_vnd. skipIf(!hasTestDb) repo tests call setupRepoTables then migrateFiscalBudget(testPool()) then seedProject. TEST_DATABASE_URL must end in _test.

  Service mocks access + repos + auditLog analog stakeholders.service.unit.test.ts. Map pg 23505 to ConflictError. Do not import the line-item budget repository. Do not wrap with a policy engine. Do not touch leftover ops/admin routes (D-15). Do not add pages or UI-SPEC (D-14). Do not change existing /api/projects/[id]/budget handlers (D-13).
  </action>
  <verify>
    <automated>npx vitest run lib/db-fiscal-budget.ddl.unit.test.ts lib/fiscal/vnd.unit.test.ts lib/fiscal/budget-metrics.unit.test.ts lib/repositories/fiscal-budget.repo.test.ts lib/services/fiscal-budget.service.unit.test.ts "app/api/projects/[id]/fiscal-budget/route.test.ts"</automated>
  </verify>
  <done>Write user can POST a fiscal year/cost-type row in integer VND, GET computed remaining/utilization/over_budget/fully_used, and PATCH actual spend; duplicate key is 409; Viewer cannot mutate; migrateFiscalBudget runs after weekly migrate (D-01, D-02, D-03, D-04, D-05, D-12, D-13, BUDG-01, BUDG-02).</done>
  <acceptance_criteria>
  - lib/db-fiscal-budget.ts exports migrateFiscalBudget and FISCAL_BUDGET_DDL_FLAG fiscal_budget_ddl_v1
  - getDb awaits migrateFiscalBudget after migrateWeeklyReports
  - computeFiscalBudgetMetrics is exported from lib/fiscal/budget-metrics.ts
  - GET handler returns metrics.status over_budget or fully_used from compute, not a DB column
  - POST handler returns 201 for PM and 403 for Viewer
  - npx vitest run lib/db-fiscal-budget.ddl.unit.test.ts lib/fiscal/vnd.unit.test.ts lib/fiscal/budget-metrics.unit.test.ts lib/repositories/fiscal-budget.repo.test.ts lib/services/fiscal-budget.service.unit.test.ts "app/api/projects/[id]/fiscal-budget/route.test.ts" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="15-01-02">
  <name>Append-only adjustments and auditLog</name>
  <files>lib/repositories/budget-adjustments.repo.ts, lib/repositories/fiscal-budget.repo.test.ts, lib/services/fiscal-budget.service.ts, lib/services/fiscal-budget.service.unit.test.ts, app/api/projects/[id]/fiscal-budget/[budgetId]/adjustments/route.ts, app/api/projects/[id]/fiscal-budget/route.test.ts</files>
  <read_first>
  - lib/services/fiscal-budget.service.ts from 15-01-01
  - .planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md D-04
  - app/api/projects/[id]/budget/[itemId]/expenses/route.ts (nested withProjectAccess Params analog)
  - lib/services/audit.service.ts
  </read_first>
  <behavior>
  - insertBudgetAdjustment persists signed non-zero amount_vnd, effective_date, reason, created_by; module exports no updater and no row-removal function (D-04, BUDG-03).
  - addBudgetAdjustment calls assertProjectWriteAccess, rejects amount 0 and empty reason with ValidationError, 404s unknown budgetId in the project, INSERTs, then auditLog entity_type budget_adjustment action create (D-04, D-05, BUDG-03).
  - After an adjustment, GET metrics approved_net includes the signed sum; createFiscalBudget still calls auditLog entity_type fiscal_budget action create (BUDG-03).
  - HTTP POST adjustments as PM returns 201; Viewer 403; GET fiscal-budget lists the new adjustment on that row (D-05, D-13).
  </behavior>
  <action>
  TDD. RED: failing repo cases for insert + sumAdjustmentsVnd (in fiscal-budget.repo.test.ts so files stay at 15); failing service cases for signed insert, amount 0, missing reason, auditLog on create and adjustment; failing route.test.ts cases that import POST from [budgetId]/adjustments/route.ts. Commit `test(15-01): red budget adjustments`.
  GREEN: implement budget-adjustments.repo.ts and addBudgetAdjustment plus nested POST route per interfaces. Commit `feat(15-01): append-only budget adjustments`.

  Changing the approved baseline is a new adjustment row, not an updater on the fiscal row (D-04). GET must recompute metrics after the insert. Do not add PATCH/DELETE on the adjustments route. Do not add benefits or ROI routes (15-02). Do not add dependency routes (15-03).
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/fiscal-budget.repo.test.ts lib/services/fiscal-budget.service.unit.test.ts "app/api/projects/[id]/fiscal-budget/route.test.ts"</automated>
  </verify>
  <done>Write user can append signed non-zero adjustments with date and reason; approved baseline is unchanged; GET remaining uses approved plus net adjustments minus actual; create and adjustment are audit-logged (D-04, BUDG-03).</done>
  <acceptance_criteria>
  - budget-adjustments.repo.ts exports insertBudgetAdjustment, listBudgetAdjustments, and sumAdjustmentsVnd only
  - addBudgetAdjustment INSERTs then auditLog
  - Nested adjustments route exports POST and does not export PATCH or DELETE
  - GET metrics.approved_net_vnd equals baseline plus adjustment sum
  - npx vitest run lib/repositories/fiscal-budget.repo.test.ts lib/services/fiscal-budget.service.unit.test.ts "app/api/projects/[id]/fiscal-budget/route.test.ts" exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/projects/[id]/fiscal-budget | Untrusted actor and body; must not mutate without write access or read another tenant's project |
| Service → fiscal-budget.repo | budgetId alone is insufficient; every SQL predicate includes project_id |
| getDb → migrateFiscalBudget | Boot-time schema; new tables must not replace line-item budget_items |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | createFiscalBudget / patch / addBudgetAdjustment | high | mitigate | assertProjectWriteAccess before any write; withProjectAccess then Viewer 403 via assertCanMutate (D-05, ASVS V4) |
| T-15-02 | Information Disclosure | getFiscalBudgetInProject / updateFiscalBudgetActual | high | mitigate | WHERE id AND project_id on every scoped get/update (ASVS V4) |
| T-15-03 | Tampering | parseNonNegativeVnd / parseSignedNonZeroVnd | medium | mitigate | Number.isSafeInteger; reject floats and negatives; BIGINT columns (D-02, ASVS V5) |
| T-15-04 | Tampering | updateFiscalBudgetActual | high | mitigate | SET actual_amount_vnd only; approval changes go through insertBudgetAdjustment (D-04, BUDG-03) |
| T-15-05 | Repudiation | createFiscalBudget / addBudgetAdjustment | medium | mitigate | auditLog append-only INSERT with before/after snapshots (BUDG-03) |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-15-06 | Denial of Service | unbounded fiscal-year list | low | accept | Per-project year/type cardinality is small; ASVS L1 list size is acceptable |
</threat_model>

<verification>
- 15-01-01: ddl unit + vnd + metrics + repo skipIf + service + fiscal-budget route tests green
- 15-01-02: adjustment insert/sum + auditLog + nested POST green
- Wave: npx vitest run lib/db-fiscal-budget.ddl.unit.test.ts lib/fiscal/vnd.unit.test.ts lib/fiscal/budget-metrics.unit.test.ts lib/repositories/fiscal-budget.repo.test.ts lib/services/fiscal-budget.service.unit.test.ts "app/api/projects/[id]/fiscal-budget/route.test.ts"
</verification>

<success_criteria>
Write users record approved integer-VND budget and actual spend by fiscal year and CAPEX/OPEX; remaining/utilization/over_budget/fully_used are computed; approval changes are append-only adjustments with audit; line-item budget_items and /budget routes stay untouched (BUDG-01, BUDG-02, BUDG-03, D-01, D-03, D-04, D-12, D-13).
</success_criteria>

<output>
Create `.planning/phases/15-budget-value-roi-dependencies/15-01-SUMMARY.md` when done
</output>
