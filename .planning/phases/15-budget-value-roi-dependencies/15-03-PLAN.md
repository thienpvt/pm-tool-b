---
phase: 15-budget-value-roi-dependencies
plan: 03
type: tdd
wave: 2
depends_on: [15-01]
files_modified:
  - lib/repositories/project-dependencies.repo.ts
  - lib/repositories/project-dependencies.repo.test.ts
  - lib/services/project-dependencies.service.ts
  - lib/services/project-dependencies.service.unit.test.ts
  - app/api/projects/[id]/dependencies/route.ts
  - app/api/projects/[id]/dependencies/schema.ts
  - app/api/projects/[id]/dependencies/route.test.ts
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03]
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Write user POSTs /api/projects/[id]/dependencies with to_project_id, dependency_type FINISH_TO_START|START_TO_START|FINISH_TO_FINISH|START_TO_FINISH|BLOCKS, need_by, effective_from, optional effective_to and notes; from_project_id is the path id; create requires assertProjectWriteAccess on from and assertProjectAccess on to (D-09, D-10, DEP-01)."
    - "Service rejects self-links, empty need_by, effective_to before effective_from when set, and overlapping equivalent active relations (same from, to, type with overlapping [effective_from, effective_to) windows) via ValidationError or ConflictError (D-10, DEP-02)."
    - "GET /api/projects/[id]/dependencies returns edges where from OR to equals id, each with direction outgoing|incoming and peer_project_id; a PM with access on either project can GET that project's list (D-11, DEP-03)."
    - "PATCH ends by setting effective_to (optional body.effective_to else CURRENT_DATE) on an open row whose from_project_id is the path id; no DELETE export; auditLog on create and end (D-09, D-11, DEP-03)."
    - "listOpenProjectDependencies is exported for Phase 16 (active window: effective_from &lt;= CURRENT_DATE AND (effective_to IS NULL OR effective_to &gt; CURRENT_DATE)) (D-16)."
  artifacts:
    - path: "lib/repositories/project-dependencies.repo.ts"
      provides: "insert, bidirectional list, overlap query, softEndDependency, listOpenProjectDependencies (D-09, D-11, D-16)"
      exports: ["insertProjectDependency", "listProjectDependencies", "hasOverlappingEquivalentDependency", "softEndDependency", "listOpenProjectDependencies", "getDependencyInFromProject"]
    - path: "lib/services/project-dependencies.service.ts"
      provides: "createProjectDependency, listProjectDependenciesForProject, endProjectDependency (D-05, D-10, D-11)"
      exports: ["createProjectDependency", "listProjectDependenciesForProject", "endProjectDependency"]
    - path: "app/api/projects/[id]/dependencies/route.ts"
      provides: "GET/POST/PATCH withProjectAccess; no DELETE export (D-09, D-13)"
  key_links:
    - from: "app/api/projects/[id]/dependencies/route.ts"
      to: "lib/services/project-dependencies.service.ts"
      via: "GET list; POST create with path id as from; PATCH end by body.id (D-13)"
      pattern: "createProjectDependency"
    - from: "lib/services/project-dependencies.service.ts"
      to: "lib/services/access.ts"
      via: "write on from then assertProjectAccess on to; GET assertProjectAccess on path project (D-10)"
      pattern: "assertProjectWriteAccess"
    - from: "lib/services/project-dependencies.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog entity_type project_dependency action create|end (D-11)"
      pattern: "auditLog"
  prohibitions:
    - physical DELETE of dependency rows (D-09)
    - creating an edge when to_project is out of actor company/access (D-10)
    - importing lib/repositories/budget.repo.ts (D-01)
    - changing /api/projects/[id]/budget or /api/portfolio/budget* (D-13)
    - CASL / leftover ops/admin gating (D-15)
    - UI-SPEC or dashboard tiles (D-14, D-16)
    - adding npm packages
---

<objective>
D-05: write assertProjectWriteAccess on create/end. D-09: project_dependencies soft-end via effective_to. D-10: reject self-link, overlap duplicate, invalid window; write on from plus access on to. D-11: list from OR to with direction; auditLog create/end. D-13: /dependencies route. D-14: server tests are the gate. D-15: no CASL. D-16: export listOpenProjectDependencies; no dashboard tiles.

Deliver bidirectional cross-project dependencies: create with required fields and date window, reject self/dup/invalid ranges, list both directions, soft-end, auditLog, cross-project auth (DEP-01, DEP-02, DEP-03).

Purpose: TDD validation and dual-project access before Phase 16 lists open edges.

Output: Working, tested /dependencies GET/POST/PATCH. Tables already exist from 15-01 migrateFiscalBudget.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md
@.planning/phases/15-budget-value-roi-dependencies/15-RESEARCH.md
@.planning/phases/15-budget-value-roi-dependencies/15-PATTERNS.md
@.planning/phases/15-budget-value-roi-dependencies/15-VALIDATION.md
@.planning/phases/15-budget-value-roi-dependencies/15-01-PLAN.md

15-01 created project_dependencies via migrateFiscalBudget. Do not modify lib/db-fiscal-budget.ts or fiscal routes.

Discretion locked by planner: dependency_type allowlist FINISH_TO_START | START_TO_START | FINISH_TO_FINISH | START_TO_FINISH | BLOCKS. from_project_id is always params.id (ignore a body from_project_id). Overlap duplicate = same from, to, type whose date windows overlap (NULL effective_to is open-ended), not merely CURRENT_DATE-active. End only from the from-project URL (getDependencyInFromProject). GET on either project returns the edge with direction. Analog ACTIVE_WINDOW and softEndPmAssignment in lib/repositories/pm-assignments.repo.ts; route analog stakeholders GET/POST/PATCH.

Do not implement deferred items: dashboards (Phase 16 imports listOpenProjectDependencies), document templates, full audit coverage.

<interfaces>
ACTIVE_DEP window SQL (for listOpenProjectDependencies only): effective_from &lt;= CURRENT_DATE AND (effective_to IS NULL OR effective_to &gt; CURRENT_DATE) — copy pm-assignments.repo.ts.

hasOverlappingEquivalentDependency(fromId, toId, type, effectiveFrom, effectiveTo): true when an existing row matches from/to/type and existing.effective_from &lt; COALESCE(newTo, far-future) AND newFrom &lt; COALESCE(existing.effective_to, far-future).

insertProjectDependency({ fromProjectId, toProjectId, dependencyType, needBy, effectiveFrom, effectiveTo, notes, createdBy }).

listProjectDependencies(projectId): SELECT *, CASE WHEN from_project_id = ? THEN 'outgoing' ELSE 'incoming' END AS direction WHERE from_project_id = ? OR to_project_id = ? ORDER BY need_by, id.

listOpenProjectDependencies(projectId): same list restricted by ACTIVE_DEP. Export for Phase 16 (D-16).

softEndDependency(fromProjectId, dependencyId, effectiveTo?: string): UPDATE effective_to WHERE id AND from_project_id AND effective_to IS NULL RETURNING *, analog softEndPmAssignment.

parseDependencyType: the five allowlist values else ValidationError('dependency_type').
parseIsoDate(value, field): YYYY-MM-DD string else ValidationError(field).

createProjectDependency(fromProjectId, actor, body): assertProjectWriteAccess(from); toId = Number(body.to_project_id); if from === to ValidationError('to_project_id'); assertProjectAccess(toId, actor) — covers foreign-company and PM-without-access; need_by required; effective_from required; if effective_to set and effective_to &lt; effective_from ValidationError('effective_to'); if hasOverlappingEquivalentDependency then ConflictError; insert; auditLog action create.

listProjectDependenciesForProject(projectId, actor): assertProjectAccess(projectId); map rows with direction and peer_project_id.

endProjectDependency(projectId, actor, dependencyId, body): assertProjectWriteAccess(projectId); load getDependencyInFromProject else NotFoundError('Not found','project_dependency'); soft-end; auditLog action end with before/after.

GET withProjectAccess list. POST 201 schema. PATCH like stakeholders: body.id required else 400 JSON; optional effective_to. Module must not export DELETE.

Route tests analog stakeholders/route.test.ts plus: PM assigned only to the to-project GETs incoming 200; PM without access on to cannot POST; Viewer POST 403; assert DELETE is undefined on the route module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="15-03-01">
  <name>Create dependency and list both directions</name>
  <files>lib/repositories/project-dependencies.repo.ts, lib/repositories/project-dependencies.repo.test.ts, lib/services/project-dependencies.service.ts, lib/services/project-dependencies.service.unit.test.ts, app/api/projects/[id]/dependencies/route.ts, app/api/projects/[id]/dependencies/schema.ts, app/api/projects/[id]/dependencies/route.test.ts</files>
  <read_first>
  - .planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md D-09 D-10 D-11
  - .planning/phases/15-budget-value-roi-dependencies/15-VALIDATION.md DEP-01 DEP-03
  - lib/repositories/pm-assignments.repo.ts ACTIVE_WINDOW softEndPmAssignment
  - lib/services/pm-assignments.service.ts auditLog create
  - app/api/projects/[id]/stakeholders/route.ts
  - lib/db-fiscal-budget.ts project_dependencies DDL (already migrated in 15-01)
  </read_first>
  <behavior>
  - createProjectDependency with write on from and access on to inserts required fields and auditLogs create (D-09, D-10, D-11, DEP-01).
  - listProjectDependenciesForProject returns the same edge as outgoing on from and incoming on to with peer_project_id (D-11, DEP-03).
  - HTTP POST 201 on from; GET 200 on from and on to for a PM with access to that path project (D-05, DEP-03).
  </behavior>
  <action>
  TDD. RED: failing skipIf(!hasTestDb) repo list/insert after migrateFiscalBudget; failing service create/list direction tests; failing route.test.ts GET/POST 401/201/200. Commit `test(15-03): red dependency create list`.
  GREEN: implement repo, service, schema, GET and POST. Commit `feat(15-03): project dependency create and list`. PATCH end lands in 15-03-03.

  from_project_id is the route params.id. Call assertProjectAccess on to after write on from (D-10). Do not add a DELETE handler (D-09). Do not import the line-item budget repository. Do not touch leftover ops/admin routes (D-15). Do not add dashboard tiles (D-16).
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts"</automated>
  </verify>
  <done>Write user can create a dependency between two different projects; both projects list it with the correct direction (DEP-01, DEP-03, D-11).</done>
  <acceptance_criteria>
  - POST uses path id as from_project_id
  - GET on to_project includes direction incoming
  - createProjectDependency calls assertProjectWriteAccess then assertProjectAccess(to)
  - npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="15-03-02">
  <name>Reject self-link, bad window, duplicate, cross-company</name>
  <files>lib/repositories/project-dependencies.repo.ts, lib/repositories/project-dependencies.repo.test.ts, lib/services/project-dependencies.service.ts, lib/services/project-dependencies.service.unit.test.ts, app/api/projects/[id]/dependencies/route.test.ts</files>
  <read_first>
  - lib/services/project-dependencies.service.ts from 15-03-01
  - .planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md D-10
  - .planning/phases/15-budget-value-roi-dependencies/15-VALIDATION.md DEP-02
  </read_first>
  <behavior>
  - from === to throws ValidationError field to_project_id (DEP-02).
  - empty need_by throws ValidationError field need_by (DEP-02).
  - effective_to &lt; effective_from throws ValidationError field effective_to (DEP-02).
  - overlapping same from/to/type throws ConflictError (DEP-02, D-10).
  - assertProjectAccess failure on to (foreign company or PM without access) propagates ForbiddenError and does not insert (D-10).
  </behavior>
  <action>
  TDD. RED: failing service unit matrix for self-link, empty need_by, inverted dates, overlap ConflictError, to-project ForbiddenError; route tests for 400/409/403. Commit `test(15-03): red dependency validation`.
  GREEN: implement guards and hasOverlappingEquivalentDependency. Commit `feat(15-03): dependency validation`.

  Overlap uses date-window intersection, not only CURRENT_DATE. Do not insert when to-project access fails. Do not add CASL.
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts"</automated>
  </verify>
  <done>System rejects self-links, duplicate equivalent overlapping relations, invalid date ranges, and edges to projects the actor cannot access (D-10, DEP-02).</done>
  <acceptance_criteria>
  - Self-link is ValidationError on to_project_id
  - Overlapping equivalent relation is ConflictError
  - Foreign to-project does not insert
  - npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="15-03-03">
  <name>Soft-end, auditLog, peer GET, open-list export</name>
  <files>lib/repositories/project-dependencies.repo.ts, lib/repositories/project-dependencies.repo.test.ts, lib/services/project-dependencies.service.ts, lib/services/project-dependencies.service.unit.test.ts, app/api/projects/[id]/dependencies/route.ts, app/api/projects/[id]/dependencies/schema.ts, app/api/projects/[id]/dependencies/route.test.ts</files>
  <read_first>
  - lib/repositories/pm-assignments.repo.ts softEndPmAssignment
  - lib/services/pm-assignments.service.ts endPmAssignment auditLog action end
  - .planning/phases/15-budget-value-roi-dependencies/15-CONTEXT.md D-09 D-11 D-16
  </read_first>
  <behavior>
  - endProjectDependency sets effective_to and auditLogs action end; already-ended row is NotFoundError (D-09, D-11, DEP-03).
  - Route module does not export DELETE; PATCH viewer 403 (D-05, D-09).
  - PM with access only on the to-project still GET-lists incoming (DEP-03).
  - listOpenProjectDependencies omits ended rows and is a named export (D-16).
  </behavior>
  <action>
  TDD. RED: failing repo soft-end and listOpen; failing service auditLog end; failing route PATCH plus DELETE undefined plus peer GET. Commit `test(15-03): red dependency end`.
  GREEN: implement PATCH end path and listOpenProjectDependencies. Commit `feat(15-03): dependency soft-end and open list`.

  End only when path id is from_project_id. Do not add row-removal SQL. Export listOpenProjectDependencies for Phase 16; do not build dashboard tiles (D-16).
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts"</automated>
  </verify>
  <done>A saved dependency can be soft-ended, remains visible with direction, is GET-able by PMs of either project, and is audit-logged (D-09, D-11, DEP-03).</done>
  <acceptance_criteria>
  - PATCH sets effective_to and does not export DELETE
  - auditLog is called on create and on end
  - listOpenProjectDependencies is exported
  - Peer-project PM GET returns incoming
  - npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts" exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /dependencies | Untrusted to_project_id; must not link a project the actor cannot access |
| List GET on either end | Actor must have assertProjectAccess on the path project only |
| Soft-end PATCH | Must not end another project's outgoing edge via a spoofed id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-13 | Elevation of Privilege | createProjectDependency | high | mitigate | assertProjectWriteAccess(from) then assertProjectAccess(to); foreign company 403 (D-10, ASVS V4) |
| T-15-14 | Elevation of Privilege | POST/PATCH | high | mitigate | Viewer 403 via assertProjectWriteAccess (D-05) |
| T-15-15 | Tampering | endProjectDependency | medium | mitigate | getDependencyInFromProject WHERE from_project_id = path id AND id = body.id (ASVS V4) |
| T-15-16 | Tampering | create validation | medium | mitigate | Reject self-link, inverted window, overlapping equivalent type (D-10, DEP-02, ASVS V5) |
| T-15-17 | Repudiation | create/end | medium | mitigate | auditLog action create and end (D-11, DEP-03) |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
| T-15-18 | Information Disclosure | GET list peer_project_id | low | accept | DEP-03 requires both ends to see the edge; actor already passed assertProjectAccess on the path project |
</threat_model>

<verification>
- 15-03-01: create + bidirectional list
- 15-03-02: self/dup/dates/cross-company
- 15-03-03: soft-end, audit, peer GET, listOpen export
- Wave: npx vitest run lib/repositories/project-dependencies.repo.test.ts lib/services/project-dependencies.service.unit.test.ts "app/api/projects/[id]/dependencies/route.test.ts"
</verification>

<success_criteria>
Users create valid bidirectional dependencies, see them on both projects with direction, cannot self-link or duplicate overlapping equivalents, and soft-end with audit — PMs of either project can GET (DEP-01, DEP-02, DEP-03, D-09, D-10, D-11).
</success_criteria>

<output>
Create `.planning/phases/15-budget-value-roi-dependencies/15-03-SUMMARY.md` when done
</output>
