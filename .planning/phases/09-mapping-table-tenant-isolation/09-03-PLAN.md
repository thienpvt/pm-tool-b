---
phase: 09-mapping-table-tenant-isolation
plan: 03
type: tdd
wave: 3
depends_on: ["09-02"]
files_modified:
  - lib/db-mapping-tenant.ts
  - lib/db.mapping-tenant-migration.integration.test.ts
  - lib/repositories/jira-config.repo.ts
  - lib/repositories/jira-config.repo.unit.test.ts
  - lib/services/jira-mapping.service.ts
  - lib/services/jira-mapping.service.unit.test.ts
  - app/api/jira/jql-presets/route.ts
  - app/api/jira/jql-presets/[id]/route.ts
  - app/api/jira/jql-presets/route.test.ts
  - app/api/jira/sync-mappings/route.ts
  - app/api/jira/sync-mappings/route.test.ts
  - test/repo-db.ts
autonomous: true
requirements: [TENANT-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A user in company A cannot read or mutate another company's Jira JQL presets (cross-company 403)."
    - "A user in company A cannot read another company's Jira sync mappings; POST save/eviction cannot destroy another company's rows."
    - "Every jira_jql_presets and jira_sync_mappings row belongs to a company; listings are scoped to that company."
    - "Existing Jira preset/sync data remains usable after backfill — no orphaned NULL company_id rows and no collapse of all tenants into one company."
    - "DELETE /api/jira/jql-presets/[id] for a foreign existing row returns 403 { error: 'Forbidden' } (D-03, D-05)."
    - "GET /api/jira/sync-mappings returns only the session company's recent rows; empty list is 200 (D-03, D-05). There is no by-id sync route."
    - "JQL preset names collide only within the same company and context; UNIQUE(company_id, name, context) (D-01 equivalent)."
  artifacts:
    - path: "lib/services/jira-mapping.service.ts"
      provides: "JQL preset list/create/delete + sync mapping list/save with tenant assert (D-04)"
    - path: "lib/repositories/jira-config.repo.ts"
      provides: "companyId on JQL and sync SQL; company-scoped sync eviction of last 5"
    - path: "app/api/jira/jql-presets/route.ts"
      provides: "Thin withAuth GET/POST calling jira-mapping.service"
    - path: "app/api/jira/sync-mappings/route.ts"
      provides: "Thin withAuth GET/POST calling jira-mapping.service"
  key_links:
    - from: "app/api/jira/jql-presets/[id]/route.ts"
      to: "lib/services/jira-mapping.service.ts"
      via: "deleteJqlPreset(params.id, actor)"
      pattern: "from '@/lib/services/jira-mapping.service'"
    - from: "lib/repositories/jira-config.repo.ts"
      to: "jira_sync_mappings"
      via: "DELETE ... WHERE company_id = ? AND id NOT IN (SELECT id FROM ... WHERE company_id = ? ORDER BY created_at DESC LIMIT 5)"
      pattern: "company_id"
  prohibitions:
    - collapsing every legacy JQL/sync row onto one company when more than one company exists
    - answering a foreign existing JQL preset id with NotFound
    - unique-indexing JQL preset names without company_id (and without context)
    - writing a tenant id taken from the JSON body
    - running sync-mapping eviction DELETE without a company_id predicate
    - wrapping JQL/sync routes in a project-scoped access helper
    - changing companyJiraConfig / setCompanyJiraConfig signatures or SQL
    - adding a by-id route for jira_sync_mappings
    - changing JiraSyncDialog UI
---

<objective>
Finish TENANT-01 for `jira_jql_presets` and `jira_sync_mappings`: D-01/D-02 migrate via the 09-01 helper (JQL unique is (company_id, name, context); sync has no name unique), D-04 service + repo companyId, D-03 403 on foreign JQL DELETE, session-company lists, session-stamped create/save, company-scoped JQL cap (10) and sync eviction (5), Vitest 4 on both tables (D-05). Sync has no [id] route — D-05 coverage is list isolation + POST scoping (research A4).

Purpose: Close the last global mapping tables, including the high-risk unscoped saveJiraSyncMapping DELETE.

Output: Jira presets and sync maps isolated per company; credential config repo functions unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-mapping-table-tenant-isolation/09-CONTEXT.md
@.planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md
@.planning/phases/09-mapping-table-tenant-isolation/09-PATTERNS.md
@.planning/phases/09-mapping-table-tenant-isolation/09-01-PLAN.md
@.planning/phases/09-mapping-table-tenant-isolation/09-02-PLAN.md

Flagged assumption (unclassified TENANT-01 probe): missing JQL id → 404; foreign existing id → 403. jira_sync_mappings has GET/POST only — no by-id 403 test; prove GET does not return other companies' rows and POST eviction cannot delete them (D-05 / A4).

Do not thin ops/admin/config. Do not change resolveJiraCredentials. Keep GET jql-presets `context` query param and MAX_PRESETS = 10. Keep sync POST { ok: true } and 400 { error: 'Missing mappings_json' }.

<interfaces>
companyJiraConfig(companyId) / setCompanyJiraConfig — already company-scoped; do not change.
listJqlPresets(context), createJqlPreset(name, jql, context, maxPresets=10) with global eviction DELETE by id, deleteJqlPreset(presetId) — must gain companyId (D-04).
listRecentJiraSyncMappings(), saveJiraSyncMapping(json) with global DELETE NOT IN (ORDER BY created_at DESC LIMIT 5) — must gain companyId; subquery must also filter company_id. PostgreSQL often needs the LIMIT subquery wrapped as a derived table.
createJqlPresetSchema: name, jql, optional context. syncMappingSchema: mappings_json union. Do not mark strict.
09-01 requireCompanyId / assertCompanyRow pattern — duplicate in jira-mapping.service.ts (do not import import-mapping.service).
withAuth already on all three Jira mapping routes (D-04).
</interfaces>
</context>

<feature>
  <name>Company-scoped Jira JQL presets and sync mappings</name>
  <files>lib/services/jira-mapping.service.ts, lib/repositories/jira-config.repo.ts, app/api/jira/jql-presets/route.ts, app/api/jira/jql-presets/[id]/route.ts, app/api/jira/sync-mappings/route.ts</files>
  <behavior>
  Cases:
  - Foreign DELETE existing JQL preset → ForbiddenError / 403 { error: 'Forbidden' } (D-03, D-05)
  - Missing JQL id → NotFoundError (A3)
  - listJqlPresets(actor, context) only that company + context; empty array 200 (D-03)
  - Same preset name allowed in two companies; same name+context in one company → ConflictError (D-01)
  - Same display name two contexts in one company allowed (unique includes context)
  - createJqlPreset eviction when 10 exist for that company+context deletes that slice's oldest only (D-04)
  - listRecentJiraSyncMappings(actor) only that company, limit 5 (D-03, D-05)
  - saveJiraSyncMapping eviction DELETE includes company_id on outer and inner query (D-04, T-09-04)
  - Null company_id actor → ForbiddenError on all JQL/sync service methods (D-03)
  </behavior>
  <implementation>
  Register both tables in migrateMappingTableTenancy. New jira-mapping.service.ts. Thin three routes onto the service. Keep companyJiraConfig untouched.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="false" id="09-03-01">
  <name>Migrate JQL presets and sync mappings</name>
  <reversibility rating="one-way">NOT NULL company_id on jira_jql_presets and jira_sync_mappings plus UNIQUE(company_id, name, context) on presets is a data-format door. CONTEXT already accepted D-01/D-02.</reversibility>
  <precondition>lib/db-mapping-tenant.ts from 09-01/09-02 already migrates timeline and bug tables; getDb already calls migrateMappingTableTenancy.</precondition>
  <files>lib/db-mapping-tenant.ts, lib/db.mapping-tenant-migration.integration.test.ts, test/repo-db.ts</files>
  <read_first>
  - lib/db-mapping-tenant.ts
  - lib/db.ts init CREATE TABLE jira_jql_presets / jira_sync_mappings and context column
  - .planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md (Recommended Composite Unique Keys)
  </read_first>
  <action>
  Register jira_jql_presets with D-01/D-02: copy name, jql, context; unique index (company_id, name, context) — research equivalent of UNIQUE(company_id, name) for this table (D-01). Register jira_sync_mappings: copy mappings_json; no name unique; index on company_id only (D-01). Same single- vs multi-company backfill as timeline/bug (D-02). Do not edit company_jira_config. Do not edit lib/db.ts.

  Extend test/repo-db.ts DDL for both tables with company_id (and context on presets). Extend the integration spec with two-company fixtures for both tables (D-02, D-05). Keep skipIf(!hasTestDb).
  </action>
  <verify>
    <automated>npx vitest run lib/db.mapping-tenant-migration.integration.test.ts -x</automated>
  </verify>
  <done>Both Jira mapping tables are non-null company-scoped after migrate; two-company backfill does not collapse tenants; JQL unique includes context (D-01, D-02).</done>
  <acceptance_criteria>
  - migrateMappingTableTenancy includes jira_jql_presets and jira_sync_mappings
  - Unique index for presets is (company_id, name, context)
  - Sync table has no name unique index
  - Integration spec covers both tables' two-company backfill
  - npx vitest run lib/db.mapping-tenant-migration.integration.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-03-02">
  <name>JQL preset service, repo, routes, and 403</name>
  <files>lib/repositories/jira-config.repo.ts, lib/repositories/jira-config.repo.unit.test.ts, lib/services/jira-mapping.service.ts, lib/services/jira-mapping.service.unit.test.ts, app/api/jira/jql-presets/route.ts, app/api/jira/jql-presets/[id]/route.ts, app/api/jira/jql-presets/route.test.ts</files>
  <read_first>
  - lib/repositories/jira-config.repo.ts (listJqlPresets, createJqlPreset, deleteJqlPreset — do not change companyJiraConfig)
  - app/api/jira/jql-presets/route.ts (context query param, MAX_PRESETS)
  - app/api/jira/jql-presets/[id]/route.ts
  - app/api/jira/jql-presets/route.test.ts
  - lib/services/import-mapping.service.ts (assert pattern to copy, not import)
  - lib/repositories/tenant-scope.repo.unit.test.ts (normalizedSql)
  </read_first>
  <behavior>
  - deleteJqlPreset(id, foreignActor) throws ForbiddenError; repo delete not called (D-03).
  - listJqlPresets(actor, context) binds companyId and context (D-04).
  - DELETE route 403 { error: 'Forbidden' } for foreign session when service throws ForbiddenError (D-05).
  - GET/POST/DELETE 401 tests remain (D-04).
  </behavior>
  <action>
  RED then GREEN (D-05). Add getJqlPresetById(id) PK fetch. listJqlPresets(companyId, context), createJqlPreset(companyId, name, jql, context, maxPresets), deleteJqlPreset(companyId, id). Eviction SELECT/DELETE must include company_id and context (D-04).

  New jira-mapping.service.ts: duplicate requireCompanyId/assertCompanyRow (D-04). list/create/delete JQL passing actor; stamp company from actor (D-03); ConflictError on unique violation for (company, name, context).

  Thin GET/POST/DELETE JQL routes onto the service; keep withAuth and context query param; keep MAX_PRESETS = 10 as an argument to create. Switch route tests to mock jira-mapping.service; add 403 DELETE case; preserve 401.

  Add jira-config.repo.unit.test.ts asserting list/create/delete SQL contains company_id binds. Do not alter companyJiraConfig tests or callers.
  </action>
  <verify>
    <automated>npx vitest run lib/services/jira-mapping.service.unit.test.ts lib/repositories/jira-config.repo.unit.test.ts app/api/jira/jql-presets/route.test.ts -x</automated>
  </verify>
  <done>JQL presets are company+context scoped; foreign DELETE is 403; 401 tests pass; companyJiraConfig unchanged (D-01, D-03, D-04, D-05).</done>
  <acceptance_criteria>
  - jql-presets routes import jira-mapping.service, not jira-config.repo
  - companyJiraConfig / setCompanyJiraConfig signatures unchanged
  - Route test 403 body { error: 'Forbidden' } on foreign DELETE
  - Repo unit: listJqlPresets SQL contains WHERE company_id = ?
  - npx vitest run lib/services/jira-mapping.service.unit.test.ts app/api/jira/jql-presets/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="09-03-03">
  <name>Sync mappings list isolation and scoped eviction</name>
  <files>lib/repositories/jira-config.repo.ts, lib/repositories/jira-config.repo.unit.test.ts, lib/services/jira-mapping.service.ts, lib/services/jira-mapping.service.unit.test.ts, app/api/jira/sync-mappings/route.ts, app/api/jira/sync-mappings/route.test.ts</files>
  <read_first>
  - lib/repositories/jira-config.repo.ts saveJiraSyncMapping / listRecentJiraSyncMappings (current global DELETE)
  - app/api/jira/sync-mappings/route.ts ({ ok: true } POST)
  - app/api/jira/sync-mappings/route.test.ts
  - .planning/phases/09-mapping-table-tenant-isolation/09-RESEARCH.md Pitfall 3
  - 09-PATTERNS.md saveJiraSyncMapping company-scoped DELETE example
  </read_first>
  <behavior>
  - listRecentJiraSyncMappings(actor) calls repo with actor.company_id; null company throws ForbiddenError (D-03).
  - saveJiraSyncMapping(actor, json) inserts with company_id; eviction DELETE predicates company_id on both the outer delete and the keep-5 subquery (D-04, T-09-04).
  - GET route returns only the service list for the session company (D-05 / A4).
  - POST still { ok: true } for owner and 400 { error: 'Missing mappings_json' } on schema failure.
  - 401 tests remain (D-04).
  </behavior>
  <action>
  RED then GREEN (D-05). Change listRecentJiraSyncMappings(companyId) and saveJiraSyncMapping(companyId, mappingsJson). Eviction SQL must not delete rows of other companies. Use a derived-table subquery if PostgreSQL rejects LIMIT in NOT IN.

  Service list/save with requireCompanyId; stamp company from actor (D-03). No by-id API (do not add [id]/route.ts).

  Thin GET/POST onto jira-mapping.service; keep withAuth. Route tests mock the service; add a company-scoped GET case (service called / list not mixed with another company). Repo unit test: saveJiraSyncMapping SQL string (normalized) includes company_id in the DELETE.

  Do not change JiraSyncDialog. Do not change syncMappingSchema except to avoid strict mode.
  </action>
  <verify>
    <automated>npx vitest run lib/services/jira-mapping.service.unit.test.ts lib/repositories/jira-config.repo.unit.test.ts app/api/jira/sync-mappings/route.test.ts app/api/jira/jql-presets/route.test.ts -x</automated>
  </verify>
  <done>Sync mappings list is session-company only; eviction cannot delete another company's rows; POST shape unchanged; no by-id route added (D-03, D-04, D-05).</done>
  <acceptance_criteria>
  - sync-mappings/route.ts imports jira-mapping.service, not jira-config.repo
  - saveJiraSyncMapping DELETE includes company_id in the keep-last-5 subquery
  - Route tests still assert 401 without session and { ok: true } on owner POST
  - No new app/api/jira/sync-mappings/[id]/route.ts file
  - npx vitest run app/api/jira/sync-mappings/route.test.ts lib/repositories/jira-config.repo.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser session → /api/jira/jql-presets and /api/jira/sync-mappings | company_id from AccessActor only |
| saveJiraSyncMapping → PostgreSQL DELETE | Global keep-last-5 currently crosses tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | DELETE /api/jira/jql-presets/[id] | high | mitigate | assertCompanyRow → 403 (D-03, D-04) |
| T-09-02 | Information Disclosure | GET JQL list and GET sync list | high | mitigate | WHERE company_id = ? (D-03, D-04) |
| T-09-03 | Tampering | POST body tenant field | high | mitigate | Stamp from actor (D-03) |
| T-09-04 | Destruction | saveJiraSyncMapping eviction; createJqlPreset cap | high | mitigate | Scope DELETE/SELECT by company_id (and context for JQL) (D-04) |
| T-09-05 | Tampering | Jira table backfill | high | mitigate | D-02 helper; unique (company_id, name, context) for presets (D-01) |
| T-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
- 09-03-01: integration migrate spec includes JQL + sync two-company backfill
- 09-03-02: JQL 403 DELETE + repo company_id SQL + 401 preserved
- 09-03-03: sync list/eviction company-scoped; POST { ok: true }
- Regression: npx vitest run app/api/import-mapping/route.test.ts app/api/bug-import-mapping/route.test.ts app/api/jira/jql-presets/route.test.ts app/api/jira/sync-mappings/route.test.ts -x
</verification>

<success_criteria>
All four TENANT-01 tables are company-scoped: JQL foreign DELETE is 403, sync lists and eviction are per company, unique JQL names are per company+context, backfill does not collapse tenants, and withAuth remains the HTTP gate (D-01..D-05).
</success_criteria>

<output>
Create `.planning/phases/09-mapping-table-tenant-isolation/09-03-SUMMARY.md` when done
</output>
