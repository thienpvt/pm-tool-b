---
phase: 12-milestone-raid-master-registers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-raid-masters.ts
  - lib/db-raid-masters.ddl.unit.test.ts
  - lib/db.ts
  - test/repo-db.ts
  - lib/repositories/milestones.repo.ts
  - lib/repositories/milestones.repo.test.ts
  - lib/services/milestones.service.ts
  - lib/services/milestones.service.unit.test.ts
  - lib/services/raid-masters.service.ts
  - lib/services/raid-masters.service.unit.test.ts
  - app/api/projects/[id]/milestones/[milestoneId]/route.ts
  - app/api/projects/[id]/milestones/[milestoneId]/route.test.ts
  - app/api/projects/[id]/milestones/route.test.ts
  - app/api/projects/[id]/milestones/[milestoneId]/epics/route.test.ts
autonomous: true
requirements: [MS-01, MS-02, MS-03, MS-05]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Assigned PM and CPMO cancel a milestone via HTTP DELETE; the row stays with status cancelled; Viewer gets ForbiddenError (D-01, D-02, D-13, MS-01, MS-05)."
    - "Cancel writes cancelled_at and cancelled_by and calls auditLog action cancel (D-01, D-10)."
    - "listUpcomingMilestones returns company-scoped rows whose status is not completed or cancelled and whose COALESCE(adjusted_end, plan_end) is today through today+7 days inclusive, UTC date-only (D-03, MS-02)."
    - "listOverdueMilestones returns company-scoped rows whose status is not completed or cancelled and whose effective end is strictly before today UTC (D-03, MS-03)."
    - "create/update dual-write plan_end and end_date when either is set (D-14)."
    - "Schema is migrateRaidMasters in getDb after migrateProjectMaster; no Prisma; the only new table is raid_due_date_history (D-10, D-12)."
    - "linkEpic and unlinkEpic stay write-gated with no Jira sync change (D-04)."
  artifacts:
    - path: "lib/db-raid-masters.ts"
      provides: "DDL, backfill, unique indexes, migrateRaidMasters (D-01, D-05, D-06, D-10, D-12)"
      exports: ["migrateRaidMasters", "RAID_MASTERS_DDL", "RAID_MASTERS_DDL_FLAG"]
    - path: "lib/db.ts"
      provides: "getDb awaits migrateRaidMasters after migrateProjectMaster (D-10)"
    - path: "lib/repositories/milestones.repo.ts"
      provides: "cancelMilestone UPDATE; listUpcomingMilestones; listOverdueMilestones; dual-write plan_end/end_date (D-01, D-03, D-14)"
    - path: "lib/services/milestones.service.ts"
      provides: "cancelMilestone uses assertProjectWriteAccess and auditLog (D-02, D-10, MS-01)"
    - path: "lib/services/raid-masters.service.ts"
      provides: "Company-scoped listUpcomingMilestones and listOverdueMilestones (D-03, D-15)"
    - path: "app/api/projects/[id]/milestones/[milestoneId]/route.ts"
      provides: "DELETE calls cancelMilestone and returns { ok: true } (D-13)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-raid-masters.ts"
      via: "getDb after migrateProjectMaster calls migrateRaidMasters(pool) (D-10)"
      pattern: "migrateRaidMasters"
    - from: "app/api/projects/[id]/milestones/[milestoneId]/route.ts"
      to: "lib/services/milestones.service.ts"
      via: "DELETE handler calls cancelMilestone (D-13)"
      pattern: "cancelMilestone"
    - from: "lib/services/raid-masters.service.ts"
      to: "lib/repositories/milestones.repo.ts"
      via: "listUpcomingMilestones / listOverdueMilestones pass companyId plus UTC today window (D-03, D-15)"
      pattern: "listUpcomingMilestones"
  prohibitions:
    - Prisma or an external migrate CLI (D-10)
    - Phase 13 weekly snapshot persistence tables (D-12)
    - a second write-gate wrapper family (D-02)
    - dashboard pages or a UI-SPEC (D-11)
    - adding npm packages
    - changing linkEpic / unlinkEpic Jira semantics (D-04)
    - putting company-scoped helpers inside portfolio.service (D-15)
---

<objective>
Prove the Phase 12 spine (D-01, D-02, D-03, D-10, D-13, D-14, MS-01, MS-02, MS-03, MS-05): migrateRaidMasters in getDb adds milestone lifecycle columns and the due-date history table; HTTP DELETE on a milestone cancels in place; company-scoped upcoming/overdue list helpers use the 7-day UTC date-only window.

Purpose: Catch an architectural dead-end on the first commit — cancel instead of row removal, getDb helper location, and Phase 16 list exports — before RAID codes and history append.

Output: Production-quality tracer. Later plans fill RAID deactivate, unique codes, due-date append, and High/tech-council lists without moving migrateRaidMasters.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-milestone-raid-master-registers/12-CONTEXT.md
@.planning/phases/12-milestone-raid-master-registers/12-RESEARCH.md
@.planning/phases/12-milestone-raid-master-registers/12-PATTERNS.md
@.planning/phases/12-milestone-raid-master-registers/12-VALIDATION.md

Discretion locked by planner: RAID retire status is deactivated; auto codes are zero-padded 3-digit R-nnn / I-nnn; overdue comparisons are UTC date-only strings.

Do not implement deferred items: MS-04 / RAID-02 / RAID-03 weekly snapshots (Phase 13), dashboard pages (Phase 16), full audit (Phase 18).

<interfaces>
migrateRaidMasters(pool: Pool): Promise&lt;void&gt; — analog of migrateProjectMaster in lib/db-project-master.ts. Settings flags: raid_masters_ddl_v1 (columns + raid_due_date_history), raid_masters_backfill_v1 (plan_end from end_date; code from TRIM(risk_id)/TRIM(issue_id) with duplicate LOWER(code) suffixes before indexes), raid_masters_index_v1 (partial unique indexes). Wrap the orchestrator in try/catch like db-project-master so a missing settings table does not crash first boot.

getDb in lib/db.ts (~596): after migrateProjectMaster(pool), dynamic-import migrateRaidMasters and await it, then keep backfillWeightedCompletion.

Milestone columns (ADD COLUMN IF NOT EXISTS): status TEXT NOT NULL DEFAULT 'planned'; plan_end TEXT; adjusted_end TEXT; cancelled_at TIMESTAMPTZ; cancelled_by INTEGER REFERENCES users(id). Status values: planned | completed | cancelled (D-01).

RAID columns this plan must still emit (owned by 12-02/12-03 writers, not used here): risks.code TEXT; risks.deactivated_at TIMESTAMPTZ; issues.code TEXT; issues.technology_council BOOLEAN DEFAULT FALSE; issues.deactivated_at TIMESTAMPTZ.

raid_due_date_history (D-06): id BIGSERIAL PK; entity_type TEXT NOT NULL CHECK (entity_type IN ('risk','issue')); entity_id TEXT NOT NULL; old_due TEXT; new_due TEXT; changed_at TIMESTAMPTZ DEFAULT now(); changed_by INTEGER REFERENCES users(id). No UPDATE or DELETE helpers this phase.

Partial unique indexes (create only after backfill): risks_project_code_lower_unique ON risks (project_id, LOWER(code)) WHERE code IS NOT NULL AND TRIM(code) &lt;&gt; ''; issues_project_code_lower_unique same on issues (D-05).

cancelMilestone(projectId, milestoneId, cancelledBy) in milestones.repo: scoped UPDATE setting status cancelled, cancelled_at now(), cancelled_by; WHERE id AND project_id AND status is not already cancelled; RETURNING *. Remove the previous row-removal export.

cancelMilestone(projectId, actor, milestoneId) in milestones.service: assertProjectWriteAccess first (D-02); load prior via getMilestone; call repo; NotFoundError if missing; auditLog entity_type milestone, action cancel, actor_id actor.user_id, company_id actor.company_id (D-10). Stop exporting deleteMilestone from the service.

HTTP DELETE in app/api/projects/[id]/milestones/[milestoneId]/route.ts calls cancelMilestone and returns { ok: true } (D-13). Do not return 405.

listUpcomingMilestones(companyId, today, windowEnd) and listOverdueMilestones(companyId, today) in milestones.repo: JOIN projects, LEFT JOIN customers, same tenant filter as listPortfolioMilestones (portfolio.repo.ts ~218-231). Effective end = COALESCE(adjusted_end, plan_end). Null effective end is neither upcoming nor overdue. Upcoming: status NOT IN completed/cancelled AND effective_end &gt;= today AND effective_end &lt;= windowEnd. Overdue: status NOT IN completed/cancelled AND effective_end &lt; today (D-03).

raid-masters.service.ts (D-15): export listUpcomingMilestones(companyId) and listOverdueMilestones(companyId). Compute today = new Date().toISOString().slice(0, 10) and windowEnd = today plus 7 UTC calendar days. Do not put these helpers in portfolio.service.

createMilestone / updateMilestone: write plan_end from body.plan_end ?? body.end_date; write end_date from body.end_date ?? body.plan_end; write adjusted_end from body.adjusted_end; default status planned (D-14).

Extend every vi.mock of milestones.repo used by route tests with cancelMilestone: vi.fn() so the service import is defined. Replace deleteMilestone service/repo tests with cancelMilestone tests. Extend the owner fixture in milestones.service.unit.test.ts to a full AccessActor including user_id (match risks.service.unit.test.ts).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="12-01-01">
  <name>End-to-end cancel milestone via HTTP DELETE</name>
  <reversibility rating="costly">Additive getDb DDL and cancel-in-place contract; undo is another migrate plus restoring a row-removal path, not a child-FK rewrite.</reversibility>
  <files>lib/db-raid-masters.ts, lib/db-raid-masters.ddl.unit.test.ts, lib/db.ts, test/repo-db.ts, lib/repositories/milestones.repo.ts, lib/repositories/milestones.repo.test.ts, lib/services/milestones.service.ts, lib/services/milestones.service.unit.test.ts, app/api/projects/[id]/milestones/[milestoneId]/route.ts, app/api/projects/[id]/milestones/[milestoneId]/route.test.ts, app/api/projects/[id]/milestones/route.test.ts, app/api/projects/[id]/milestones/[milestoneId]/epics/route.test.ts</files>
  <read_first>
  - .planning/phases/12-milestone-raid-master-registers/12-CONTEXT.md (D-01, D-02, D-10, D-12, D-13)
  - .planning/phases/12-milestone-raid-master-registers/12-PATTERNS.md (db-project-master analog, cancel UPDATE, auditLog)
  - lib/db-project-master.ts (settings flags, try/catch orchestrator)
  - lib/db.ts getDb migrate loop after migrateProjectMaster
  - lib/repositories/milestones.repo.ts create/update/row-removal
  - lib/services/milestones.service.ts deleteMilestone
  - lib/services/audit.service.ts auditLog
  - app/api/projects/[id]/milestones/[milestoneId]/route.ts DELETE
  - test/repo-db.ts milestones / risks / issues CREATE TABLE
  </read_first>
  <behavior>
  - Viewer or write-denied actor calling cancelMilestone throws ForbiddenError and does not call the repo (D-02, MS-01).
  - Assigned writer cancelMilestone sets status cancelled, writes cancelled_at / cancelled_by, calls auditLog action cancel, and does not call a row-removal repo function (D-01, D-10, MS-05).
  - Missing or cross-project id throws NotFoundError (scoped WHERE).
  - HTTP DELETE on /api/projects/[id]/milestones/[milestoneId] calls cancelMilestone and returns 200 { ok: true } (D-13).
  - migrateRaidMasters is exported from lib/db-raid-masters.ts and getDb awaits it after migrateProjectMaster (D-10).
  - DDL unit test asserts milestone status/plan_end/adjusted_end/cancelled_at/cancelled_by, raid_due_date_history, risks/issues code columns, unique index names, and exactly one CREATE TABLE (D-01, D-05, D-06, D-12).
  </behavior>
  <action>
  TDD first (D-11): add failing cancelMilestone service tests (write gate, auditLog, NotFound, no row-removal repo call) and a failing lib/db-raid-masters.ddl.unit.test.ts that migrateRaidMasters exists, RAID_MASTERS_DDL_FLAG is raid_masters_ddl_v1, unique index identifiers are risks_project_code_lower_unique and issues_project_code_lower_unique, and the joined DDL contains exactly one CREATE TABLE named raid_due_date_history. Tests must fail before production code exists. Then implement until green.

  Create lib/db-raid-masters.ts exporting migrateRaidMasters(pool). Copy settingsFlagExists / writeSettingsFlag from lib/db-project-master.ts. Run DDL, then backfill, then unique indexes, each behind its own flag, in that order so backfill cannot collide with the unique index (Pitfall 4). Dual-write is not this task; columns must still exist. Do not use Prisma (D-10). Do not add Phase 13 snapshot persistence tables (D-12). Do not add npm packages.

  Wire getDb() in lib/db.ts: after migrateProjectMaster(pool), await migrateRaidMasters(pool) via dynamic import. Keep backfillWeightedCompletion after it.

  Mirror new milestone, risks, and issues columns plus raid_due_date_history in test/repo-db.ts (Pitfall 5).

  Replace the milestones.repo row-removal function with cancelMilestone UPDATE RETURNING as specified in interfaces. Add getMilestone(projectId, milestoneId) scoped SELECT for audit. Keep linkEpic / unlinkEpic unchanged (D-04).

  Replace deleteMilestone in milestones.service with cancelMilestone as specified. Mock auditLog. Extend the owner/foreign fixtures to full AccessActor with user_id.

  Point the [milestoneId] DELETE handler at cancelMilestone; keep { ok: true }. Add a route test that owner DELETE returns 200 { ok: true } and that the repo cancel is invoked. Add cancelMilestone: vi.fn() to every existing vi.mock of milestones.repo in this plan's route tests so imports resolve.

  Rewrite the repo test that previously asserted changes===0 on a foreign id to assert cancelMilestone returns empty/undefined and the foreign row status is unchanged.
  </action>
  <verify>
    <automated>npx vitest run lib/db-raid-masters.ddl.unit.test.ts lib/services/milestones.service.unit.test.ts app/api/projects/[id]/milestones/[milestoneId]/route.test.ts -x</automated>
  </verify>
  <done>HTTP DELETE cancels a milestone in place with auditLog; migrateRaidMasters runs from getDb; Viewer cannot cancel; the service no longer exports deleteMilestone (D-01, D-02, D-10, D-13, MS-01, MS-05).</done>
  <acceptance_criteria>
  - lib/db-raid-masters.ts exports migrateRaidMasters
  - getDb awaits migrateRaidMasters after migrateProjectMaster
  - Unique index names are risks_project_code_lower_unique and issues_project_code_lower_unique
  - milestones.service exports cancelMilestone and does not export deleteMilestone
  - DELETE handler returns { ok: true }
  - npx vitest run lib/db-raid-masters.ddl.unit.test.ts lib/services/milestones.service.unit.test.ts app/api/projects/[id]/milestones/[milestoneId]/route.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="12-01-02">
  <name>Upcoming and overdue company-scoped milestone lists</name>
  <files>lib/repositories/milestones.repo.ts, lib/repositories/milestones.repo.test.ts, lib/services/milestones.service.ts, lib/services/raid-masters.service.ts, lib/services/raid-masters.service.unit.test.ts</files>
  <read_first>
  - lib/repositories/milestones.repo.ts from 12-01-01
  - lib/repositories/portfolio.repo.ts listPortfolioMilestones tenant SQL
  - lib/services/project-report.service.ts today ISO slice (lines 191-192)
  - .planning/phases/12-milestone-raid-master-registers/12-CONTEXT.md D-03 D-14 D-15
  </read_first>
  <behavior>
  - listUpcomingMilestones(companyId) includes a planned row whose effective end is today or today+7 and excludes completed, cancelled, and today+8 (D-03, MS-02).
  - listOverdueMilestones(companyId) includes a planned row whose effective end is yesterday and excludes completed/cancelled and today (D-03, MS-03).
  - COALESCE(adjusted_end, plan_end) is the effective end; null effective end is in neither list (D-03).
  - createMilestone with only end_date persists plan_end equal to that value; create/update with plan_end also persists end_date (D-14).
  - Helpers live in raid-masters.service.ts, not portfolio.service (D-15).
  </behavior>
  <action>
  TDD: failing raid-masters.service.unit.test.ts that mocks listUpcomingMilestones / listOverdueMilestones from milestones.repo and asserts the service passes actor/company today plus today+7 UTC date strings. Failing milestones.repo.test.ts cases (skipIf !hasTestDb) for the window, overdue, adjusted_end precedence, and completed/cancelled exclusion. Failing create/update tests that plan_end and end_date are dual-written (D-14). Then implement until green.

  Add repo listUpcomingMilestones(companyId, today, windowEnd) and listOverdueMilestones(companyId, today) with the tenant SQL and predicates in interfaces. Create lib/services/raid-masters.service.ts exporting those two helpers per D-15. Dual-write plan_end and end_date on createMilestone and updateMilestone. Default status planned.

  Do not build dashboard pages (D-11). Do not call these helpers from portfolio.service (D-15). Do not change linkEpic (D-04).
  </action>
  <verify>
    <automated>npx vitest run lib/services/raid-masters.service.unit.test.ts lib/services/milestones.service.unit.test.ts lib/repositories/milestones.repo.test.ts -x</automated>
  </verify>
  <done>Phase 16 can import listUpcomingMilestones and listOverdueMilestones from raid-masters.service; dual-write keeps report readers of end_date in sync (D-03, D-14, D-15, MS-02, MS-03).</done>
  <acceptance_criteria>
  - lib/services/raid-masters.service.ts exports listUpcomingMilestones and listOverdueMilestones
  - Effective end uses COALESCE(adjusted_end, plan_end)
  - Window is inclusive 7 UTC days
  - npx vitest run lib/services/raid-masters.service.unit.test.ts lib/services/milestones.service.unit.test.ts lib/repositories/milestones.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → DELETE/PUT /api/projects/[id]/milestones/[milestoneId] | Untrusted actor; must not cancel another tenant's row |
| Service → milestones UPDATE | Scoped WHERE id AND project_id is the IDOR control |
| getDb → DDL helper | Boot-time schema; must not introduce Phase 13 snapshot tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | cancelMilestone | high | mitigate | assertProjectWriteAccess before repo; Viewer ForbiddenError (D-02) |
| T-12-02 | Elevation of Privilege | cancelMilestone repo UPDATE | high | mitigate | WHERE id = ? AND project_id = ?; zero rows → NotFoundError |
| T-12-07 | Tampering | milestones.repo retire path | high | mitigate | Cancel UPDATE only; stop exporting the row-removal function (D-01, MS-05) |
| T-12-05 | Information Disclosure | listUpcomingMilestones / listOverdueMilestones | high | mitigate | Same company_id OR customer.company_id tenant filter as listPortfolioMilestones (D-03) |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-12-08 | Tampering | HTTP DELETE still accepted | low | accept | D-13 requires mapping DELETE to cancel with 200 { ok: true } so existing clients keep working |
</threat_model>

<verification>
- 12-01-01: ddl unit + cancelMilestone service + DELETE route tests green
- 12-01-02: raid-masters upcoming/overdue tests green; dual-write tests green
- Wave: npx vitest run lib/db-raid-masters.ddl.unit.test.ts lib/services/milestones.service.unit.test.ts lib/services/raid-masters.service.unit.test.ts app/api/projects/[id]/milestones/[milestoneId]/route.test.ts -x
</verification>

<success_criteria>
Assigned PM/CPMO cancel milestones in place; Viewer cannot; upcoming/overdue helpers use the 7-day UTC window; schema lives in getDb via lib/db-raid-masters.ts (MS-01, MS-02, MS-03, MS-05, D-01, D-03, D-10, D-13).
</success_criteria>

<output>
Create `.planning/phases/12-milestone-raid-master-registers/12-01-SUMMARY.md` when done
</output>
