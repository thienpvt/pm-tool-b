---
phase: 19-data-layer-cutover
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - migrations/0001-baseline-schema.sql
  - migrations/README.md
  - lib/migrate/baseline-content.test.ts
  - lib/db-roles.ts
  - lib/db-roles.ddl.unit.test.ts
  - lib/db-mapping-tenant.ts
autonomous: true
requirements: [DATA-02]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "migrations/0001-baseline-schema.sql exists, parses as version 1 via parseMigrationFile, and contains weekly_periods, audit_logs, user_roles, project_fiscal_budgets, document_catalog, dashboard_filter_state, raid_due_date_history, project_pm_assignments (DATA-02, D-03)."
    - "0001 has three labelled parts: Part 1 init CREATE TABLE IF NOT EXISTS from lib/db.ts initPostgresSchema; Part 2 legacy ALTER/CREATE from migratePostgresSchema without the four boot data-fix UPDATE fingerprints; Part 3 v2.0 DDL from lib/db-*.ts exports (DATA-02, D-03)."
    - "Part 3 RAID order is RAID_MASTERS_DDL, then backfill/dedupe DML, then RAID_MASTERS_INDEX_DDL (D-09)."
    - "0001 contains no DROP TABLE; brownfield stamp is idempotent IF NOT EXISTS / ADD COLUMN IF NOT EXISTS (D-04)."
    - "migrations/README.md documents npm run migrate, --check, ledger contract, brownfield stamp steps, and tells operators not to run two migrate processes in parallel against the same database (DATA-02 concurrency)."
    - "Origin branch baseline SQL body is not the source of 0001 (D-02)."
  artifacts:
    - path: "migrations/0001-baseline-schema.sql"
      provides: "Regenerated v2.0 baseline (DATA-02, D-03, D-04, D-09)"
    - path: "migrations/README.md"
      provides: "Operator runbook including single-writer guidance"
    - path: "lib/migrate/baseline-content.test.ts"
      provides: "v2.0 table names, no origin-only baseline, RAID order, fingerprint bans"
    - path: "lib/db-roles.ts"
      provides: "ROLES_AUDIT_DDL export for Part 3 (D-08)"
      exports: ["ROLES_AUDIT_DDL", "migrateUsersRolesAndAudit", "backfillUserRoles"]
    - path: "lib/db-mapping-tenant.ts"
      provides: "MAPPING_TENANT_DDL export for Part 3 (D-08)"
      exports: ["MAPPING_TENANT_DDL", "migrateMappingTableTenancy"]
  key_links:
    - from: "migrations/0001-baseline-schema.sql"
      to: "lib/db-weekly-reports.ts"
      via: "WEEKLY_REPORTS_DDL concatenated into Part 3"
      pattern: "weekly_periods"
    - from: "lib/migrate/baseline-content.test.ts"
      to: "lib/migrate/plan.ts"
      via: "parseMigrationFile on 0001"
      pattern: "parseMigrationFile"
  prohibitions:
    - statement: "Must not merge origin v1.0 0001 SQL (would drop/omit v2.0 tables)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not leave dual writers (boot DDL AND external migrate)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not run data-fix UPDATEs on every process start"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Regenerate migrations/0001-baseline-schema.sql from current v2.0 schema (lib/db.ts plus lib/db-*.ts), not from origin v1.0 SQL (D-02, D-03, DATA-02). D-04: no DROP TABLE so brownfield stamp is additive. D-09: RAID unique indexes after backfill DML inside Part 3.

Purpose: Fresh and brownfield databases get the same idempotent baseline the boot path used to build.

Output: 0001 SQL, README, baseline-content.test.ts, ROLES_AUDIT_DDL and MAPPING_TENANT_DDL exports.
</objective>

## Artifacts this phase produces

- `migrations/0001-baseline-schema.sql` (version 1 baseline)
- `migrations/README.md`
- `ROLES_AUDIT_DDL` in lib/db-roles.ts
- `MAPPING_TENANT_DDL` in lib/db-mapping-tenant.ts
- `lib/migrate/baseline-content.test.ts`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-data-layer-cutover/19-CONTEXT.md
@.planning/phases/19-data-layer-cutover/19-RESEARCH.md Pattern 3
@.planning/phases/19-data-layer-cutover/19-PATTERNS.md 0001 generation
@.planning/phases/19-data-layer-cutover/19-01-PLAN.md

D-02: git show origin/...:migrations/0001-baseline-schema.sql is a negative example (lacks weekly_periods / user_roles). Do not copy that file body.

Four boot DML fingerprints belong in 19-03 scripts, not Part 2: users onboarding_completed WHERE created_at; portfolio_members member_type external; projects SET company_id FROM customers; activities parent_id from EPIC.

Existing *_DDL unit tests remain source of truth for Part 3 strings (D-08). Isolation none.

19-03 may run in parallel on Wave 2: it does not consume ROLES_AUDIT_DDL / MAPPING_TENANT_DDL; it uses pre-existing backfill helpers.

<interfaces>
initPostgresSchema: lib/db.ts ~69-420 — 35 CREATE TABLE IF NOT EXISTS blocks; copy SQL text into Part 1.
migratePostgresSchema: lib/db.ts ~425-505 — include ALTER/CREATE/DO; omit the four UPDATE strings above.
lib/db-roles.ts migrateRolesDdl SQL → export ROLES_AUDIT_DDL: string[] then loop it inside migrateRolesDdl; keep migrateUsersRolesAndAudit (D-08).
lib/db-mapping-tenant.ts: export MAPPING_TENANT_DDL with ADD COLUMN IF NOT EXISTS company_id, CREATE UNIQUE INDEX IF NOT EXISTS idx_${table}_company_unique, CREATE INDEX IF NOT EXISTS idx_${table}_company_id for timeline_import_mappings, bug_import_mappings, jira_jql_presets (unique company_id, name, context), jira_sync_mappings (company_id index only). Do not put CROSS JOIN backfill in 0001 (19-03).
PROJECT_MASTER_DDL, PROJECT_MASTER_CONSTRAINTS_DDL from lib/db-project-master.ts
RAID_MASTERS_DDL then backfill then RAID_MASTERS_INDEX_DDL (D-09)
WEEKLY_REPORTS_DDL, WEEKLY_REPORTS_INDEX_DDL, WEEKLY_EXPORT_LOGS_DDL
FISCAL_BUDGET_DDL
DASHBOARDS_DDL
DOCUMENTS_DDL
parseMigrationFile from @/lib/migrate/plan
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="19-02-01">
  <name>Export Part 3 source arrays for 0001</name>
  <files>lib/db-roles.ts, lib/db-roles.ddl.unit.test.ts, lib/db-mapping-tenant.ts</files>
  <read_first>
  - lib/db-roles.ts migrateRolesDdl (~22-73)
  - lib/db-mapping-tenant.ts migrateOneTable unique index names (~131-150)
  - lib/db-weekly-reports.ddl.unit.test.ts (string-join assertion style)
  </read_first>
  <behavior>
  - ROLES_AUDIT_DDL joined text matches CREATE TABLE IF NOT EXISTS user_roles and CREATE TABLE IF NOT EXISTS audit_logs (D-03, D-08).
  - MAPPING_TENANT_DDL joined text includes timeline_import_mappings company_id and idx_jira_jql_presets_company_unique (D-03).
  </behavior>
  <action>
  TDD. RED then GREEN lib/db-roles.ddl.unit.test.ts analog lib/db-weekly-reports.ddl.unit.test.ts asserting ROLES_AUDIT_DDL join matches user_roles and audit_logs.
  Export const ROLES_AUDIT_DDL as the SQL strings currently inlined in migrateRolesDdl; migrateRolesDdl loops ROLES_AUDIT_DDL then writes users_roles_audit_ddl_v1. Keep backfillUserRoles and migrateUsersRolesAndAudit (D-08).
  Export const MAPPING_TENANT_DDL as the additive column/index SQL fragments (not the CROSS JOIN / DELETE backfill). Keep migrateMappingTableTenancy for repo integration tests (D-08).
  Do not write migrations/0001-baseline-schema.sql here (19-02-02). Do not copy origin 0001 body (D-02). Commit feat(19-02): export roles and mapping DDL arrays.
  </action>
  <verify>
    <automated>npx vitest run lib/db-roles.ddl.unit.test.ts lib/db-weekly-reports.ddl.unit.test.ts</automated>
  </verify>
  <done>Part 3 sources are exported arrays used by 0001 generation (D-03, D-08, DATA-02).</done>
  <acceptance_criteria>
  - lib/db-roles.ts exports ROLES_AUDIT_DDL
  - lib/db-mapping-tenant.ts exports MAPPING_TENANT_DDL
  - npx vitest run lib/db-roles.ddl.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="19-02-02">
  <name>Write three-part 0001 from live schema including RAID order</name>
  <files>migrations/0001-baseline-schema.sql, lib/migrate/baseline-content.test.ts</files>
  <read_first>
  - lib/db.ts initPostgresSchema (~69-420) and migratePostgresSchema (~425-505)
  - lib/db-raid-masters.ts RAID_MASTERS_DDL, backfillRaidMasters, dedupeRaidCodes, RAID_MASTERS_INDEX_DDL
  - ROLES_AUDIT_DDL and MAPPING_TENANT_DDL from 19-02-01
  - PROJECT_MASTER_DDL, PROJECT_MASTER_CONSTRAINTS_DDL, WEEKLY_REPORTS_DDL, WEEKLY_REPORTS_INDEX_DDL, WEEKLY_EXPORT_LOGS_DDL, FISCAL_BUDGET_DDL, DASHBOARDS_DDL, DOCUMENTS_DDL
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:migrations/0001-baseline-schema.sql (negative: confirm weekly_periods absent)
  </read_first>
  <behavior>
  - 0001 sql matches /weekly_periods/, /audit_logs/, /user_roles/, /project_fiscal_budgets/, /document_catalog/, /dashboard_filter_state/, /raid_due_date_history/, /project_pm_assignments/ (DATA-02, D-03).
  - 0001 does not match /DROP TABLE/i (D-04).
  - 0001 does not contain the four boot fingerprints: onboarding_completed = 1 WHERE created_at; member_type = 'external'; UPDATE projects SET company_id; SET parent_id = e.id (DATA-03 split; those scripts are 19-03).
  - Index of RAID_MASTERS_DDL marker (risks ADD COLUMN IF NOT EXISTS code) is less than index of plan_end backfill DML, which is less than index of risks_project_code_lower_unique (D-09).
  </behavior>
  <action>
  Create migrations/0001-baseline-schema.sql with comments Part 1, Part 2, Part 3.
  Part 1: every CREATE TABLE IF NOT EXISTS from initPostgresSchema, same column lists.
  Part 2: every migratePostgresSchema statement except the four boot DML fingerprints. Keep ADD COLUMN IF NOT EXISTS and CREATE TABLE IF NOT EXISTS. For allocated_headcount TYPE NUMERIC(6,1), wrap in a DO block that ignores already-converted columns so brownfield stamp does not abort (D-04).
  Part 3 concatenate in getDb helper order: MAPPING_TENANT_DDL; ROLES_AUDIT_DDL; PROJECT_MASTER_DDL; PROJECT_MASTER_CONSTRAINTS_DDL; RAID_MASTERS_DDL; then RAID backfill DML from backfillRaidMasters (plan_end from end_date; risks.code from risk_id; issues.code from issue_id) plus a DO block that suffixes duplicate (project_id, lower(code)) rows the same way dedupeRaidCodes does; then RAID_MASTERS_INDEX_DDL; then weekly, fiscal, dashboards, documents arrays (D-09, D-03).
  TDD: write lib/migrate/baseline-content.test.ts first (readFileSync 0001, parseMigrationFile version 1, table name matches, fingerprint bans, RAID order, no DROP TABLE). Implement 0001 until green. Commit test(19-02): red 0001 content then feat(19-02): v2 baseline 0001 SQL.
  Do not copy origin 0001 body (D-02). Do not add Prisma (D-06). RAID DML in Part 3 is required (D-09); other boot UPDATEs stay out.
  </action>
  <verify>
    <automated>npx vitest run lib/migrate/baseline-content.test.ts lib/migrate/plan.test.ts</automated>
  </verify>
  <done>0001 is a regenerated v2.0 baseline with RAID backfill before unique indexes and without table-removal statements (DATA-02, D-02, D-03, D-04, D-09).</done>
  <acceptance_criteria>
  - migrations/0001-baseline-schema.sql exists and parseMigrationFile version is 1
  - file text includes weekly_periods, user_roles, project_fiscal_budgets, document_catalog
  - baseline-content.test.ts exits 0 via npx vitest run lib/migrate/baseline-content.test.ts
  </acceptance_criteria>
</task>

<task type="auto" id="19-02-03">
  <name>Migrations README: ledger, stamp, single-writer</name>
  <files>migrations/README.md</files>
  <read_first>
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:migrations/README.md
  - migrations/0001-baseline-schema.sql header comments from 19-02-02
  </read_first>
  <action>
  Write migrations/README.md from origin structure, updated for three parts (Part 3 = v2.0 DDL from lib/db-*.ts). Document: npm run migrate and npm run migrate -- --check; schema_migrations columns version name checksum applied_at; append-only (add a new NNNN file rather than edit applied SQL); brownfield stamp rehearsal on pg_dump scratch then live DATABASE_URL; 0001 is IF NOT EXISTS so existing v2.0 tables are kept (D-04). State that operators must not run two migrate processes in parallel against the same database (DATA-02 concurrency) even though the runner takes pg_advisory_lock(1347246335). List data-fix paths as operator one-off (full scripts land in 19-03). Note Docker COPY of migrations/ is 19-04. Do not tell anyone to merge the origin branch (D-02).
  Commit docs(19-02): migrations operator README.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('migrations/README.md','utf8'); if(!t.includes('npm run migrate')) process.exit(1); if(!t.includes('schema_migrations')) process.exit(1); if(!t.toLowerCase().includes('parallel')) process.exit(1);"</automated>
  </verify>
  <done>Operators have a runbook for stamp, checksum ledger, and single-writer migrate (DATA-02, D-04).</done>
  <acceptance_criteria>
  - migrations/README.md contains npm run migrate and schema_migrations
  - README mentions not running two migrate processes in parallel
  - README describes three parts including v2.0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 0001 SQL → production schema | Wrong baseline omits v2.0 tables or removes brownfield data |
| Operator → first migrate | Stamp must be additive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | 0001 file vs origin v1.0 | high | mitigate | Generate from lib/db.ts + exports; baseline-content.test.ts requires weekly_periods and user_roles (D-02, D-03) |
| T-19-06 | Tampering / Destruction | 0001 DROP TABLE | high | mitigate | baseline-content.test.ts forbids DROP TABLE; IF NOT EXISTS only (D-04, T-19 brownfield) |
| T-19-07 | Denial of Service | RAID unique indexes before dedupe | high | mitigate | Part 3 order DDL → backfill DML → RAID_MASTERS_INDEX_DDL (D-09) |
| T-19-03 | Denial of Service | parallel migrate | medium | mitigate | README single-writer instruction plus runner lock from 19-01 |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan |

</threat_model>

<verification>
- 19-02-01: ROLES_AUDIT_DDL and MAPPING_TENANT_DDL exported
- 19-02-02: baseline-content.test.ts green on 0001
- 19-02-03: README has migrate + parallel warning
- Existing lib/db-*.ddl.unit.test.ts still pass (D-08)
</verification>

<success_criteria>
A regenerated 0001 includes v2.0 weekly, fiscal, roles, RAID, dashboard, checklist, and audit objects; brownfield stamp cannot drop those tables; RAID indexes follow backfill DML (DATA-02, D-03, D-04, D-09).
</success_criteria>

<output>
Create `.planning/phases/19-data-layer-cutover/19-02-SUMMARY.md` when done
</output>
