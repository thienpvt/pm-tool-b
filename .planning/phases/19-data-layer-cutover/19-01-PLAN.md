---
phase: 19-data-layer-cutover
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/migrate/plan.ts
  - lib/migrate/plan.test.ts
  - lib/migrate/runner.ts
  - lib/migrate/runner.test.ts
  - lib/migrate/assertMigrated.ts
  - lib/migrate/assertMigrated.test.ts
  - scripts/migrate.ts
  - lib/db.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [DATA-01, DATA-02]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "parseMigrationFile accepts NNNN-name.sql (1-4 digit version, hyphens in the tail) and rejects other names; planPendingMigrations sorts by version, throws on duplicate versions, returns empty toApply when every version is applied, and lists drifted filenames when an applied checksum no longer matches (DATA-02, D-02, D-10)."
    - "runMigrations takes a session advisory lock on key 1347246335, creates schema_migrations if needed, applies each pending file as one query inside BEGIN/COMMIT, inserts version+name+checksum, and a second run reports alreadyApplied with no new ledger row (DATA-02)."
    - "Checksum drift throws an Error whose message includes checksum drift; a failing file rolls back and rethrows with that filename (DATA-02, T-19-01)."
    - "assertMigrated resolves when schema_migrations has at least one row; an empty ledger throws a message containing npm run migrate; a missing ledger with a companies row present resolves (legacy brownfield probe); a missing ledger and missing companies throws the same runbook message (DATA-01, T-19-02)."
    - "Migrate is a single-writer: the runner always acquires pg_advisory_lock(1347246335) before planning or applying; a second run against an already-applied set is a no-op; an interrupted file can be re-run because a failed file rolls back without a ledger row (DATA-02 concurrency)."
    - "package.json scripts.migrate is npx tsx scripts/migrate.ts and devDependencies.tsx is exactly 4.23.12 (D-10)."
    - "lib/db.ts exports resolveSsl(databaseUrl) with the existing sslmode / railway.internal / localhost behavior; getDb still uses that helper for Pool ssl (D-06)."
    - statement: "getDb() does not yet drop boot-time schema helpers in this plan; that cutover is 19-04 (DATA-01 flagged assumption — fail closed after 19-04)."
      verification: backstop
  artifacts:
    - path: "lib/migrate/plan.ts"
      provides: "Filename parse, sha256, pending plan (D-02)"
      exports: ["MigrationFile", "AppliedMigration", "parseMigrationFile", "planPendingMigrations", "sha256"]
    - path: "lib/migrate/runner.ts"
      provides: "Advisory lock, ledger, apply loop (DATA-02)"
      exports: ["QueryableClient", "RunResult", "DEFAULT_LEDGER_TABLE", "MIGRATION_LOCK_KEY", "runMigrations", "computePendingMigrations"]
    - path: "lib/migrate/assertMigrated.ts"
      provides: "Boot guard with no fs (DATA-01, D-05)"
      exports: ["assertMigrated"]
    - path: "scripts/migrate.ts"
      provides: "CLI: apply or --check; reads migrations/*.sql via fs (D-10)"
    - path: "package.json"
      provides: "migrate script + tsx@4.23.12 (D-10, T-19-SC)"
  key_links:
    - from: "scripts/migrate.ts"
      to: "lib/migrate/plan.ts"
      via: "parseMigrationFile on each *.sql"
      pattern: "parseMigrationFile"
    - from: "scripts/migrate.ts"
      to: "lib/migrate/runner.ts"
      via: "runMigrations / computePendingMigrations"
      pattern: "runMigrations"
    - from: "lib/migrate/runner.ts"
      to: "lib/migrate/plan.ts"
      via: "planPendingMigrations"
      pattern: "planPendingMigrations"
  prohibitions:
    - statement: "Must not merge origin v1.0 0001 SQL (would drop/omit v2.0 tables)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not leave dual writers (boot DDL AND external migrate)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not run data-fix UPDATEs on every process start"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Ship the production migrate engine end-to-end on one SQL file: parse → checksum plan → advisory-locked apply → schema_migrations ledger → assertMigrated fail-closed (DATA-01 guard, DATA-02 runner). D-01: DATA-01, DATA-02, and DATA-03 stay in this phase (this plan starts DATA-01/DATA-02; 19-03 and 19-04 complete the set). D-02: port origin gsd/quick-260826-ded-data-layer-migrations lib/migrate/* and scripts/migrate.ts as pattern only — do not copy that branch's baseline SQL. D-10: tsx@4.23.12. D-06: single pg.Pool; no Prisma/Drizzle/Kysely.

Purpose: Prove the ledger architecture before regenerating v2.0 0001 (19-02) and slimming getDb (19-04).

Output: lib/migrate/{plan,runner,assertMigrated}.ts with origin tests, scripts/migrate.ts, exported resolveSsl, npm run migrate.
</objective>

## Artifacts this phase produces

- `parseMigrationFile`, `planPendingMigrations`, `sha256`, `MigrationFile`, `AppliedMigration` (lib/migrate/plan.ts)
- `runMigrations`, `computePendingMigrations`, `DEFAULT_LEDGER_TABLE`, `MIGRATION_LOCK_KEY`, `QueryableClient`, `RunResult` (lib/migrate/runner.ts)
- `assertMigrated` (lib/migrate/assertMigrated.ts)
- CLI `scripts/migrate.ts` flags: default apply, `--check`
- `package.json` script `migrate`; pin `tsx` 4.23.12
- `export function resolveSsl(databaseUrl: string)` from lib/db.ts

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-data-layer-cutover/19-CONTEXT.md
@.planning/phases/19-data-layer-cutover/19-RESEARCH.md
@.planning/phases/19-data-layer-cutover/19-PATTERNS.md
@.planning/phases/19-data-layer-cutover/19-VALIDATION.md

Planner-assigned locked IDs (CONTEXT.md bullets + orchestrator defaults): D-01 phase contains DATA-01..03; D-02 origin pattern only; D-03 0001 from current schema (19-02); D-04 brownfield stamp (19-02); D-05 slim getDb (19-04); D-06 single Pool; D-07 Docker/CI (19-04); D-08 keep *_DDL exports (19-04); D-09 RAID order (19-02); D-10 tsx runner.

Do not implement deferred items: Kysely, module split, v2 UI, ops/admin thinning.

Do not copy origin migrations/0001-baseline-schema.sql body into this repo in this plan. Inspect via git show only.

Canon OWASP/injection: breadcrumb only — ledger INSERT uses filename+checksum from parseMigrationFile (strict FILENAME_RE); do not mint a new injection prohibition.

<interfaces>
From origin (git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/plan.ts) — port signatures:
parseMigrationFile(filename: string, sql: string): MigrationFile
planPendingMigrations(files: MigrationFile[], applied: AppliedMigration[]): { toApply: MigrationFile[]; drifted: string[] }
sha256(content: string): string  // createHash('sha256').update(content, 'utf8').digest('hex')
FILENAME_RE = /^(\d{1,4})-[A-Za-z0-9_-]+\.sql$/

From origin runner.ts:
DEFAULT_LEDGER_TABLE = 'schema_migrations'
MIGRATION_LOCK_KEY = 1347246335
QueryableClient.query(text: string): Promise<{ rows: unknown[] }>
runMigrations(client, files, opts?: { ledgerTable?: string }): Promise<RunResult>
computePendingMigrations(client, files, opts?: { ledgerTable?: string }): Promise<{ toApply; alreadyApplied; drifted }>
Ledger CREATE: version INTEGER PRIMARY KEY, name TEXT NOT NULL, checksum TEXT NOT NULL, applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
Lock SQL: SELECT pg_advisory_lock(1347246335) / pg_advisory_unlock
Per file: BEGIN; client.query(file.sql); INSERT INTO ${ledgerTable} (version, name, checksum) VALUES (...); COMMIT. On error ROLLBACK then throw Migration ${file.filename} failed: ...

From origin assertMigrated.ts:
assertMigrated(query: (sql: string) => Promise<{ rows: unknown[] }>, ledgerTable = 'schema_migrations'): Promise<void>
Runbook substring: npm run migrate
Ledger probe: SELECT 1 FROM ${ledgerTable} LIMIT 1
Legacy probe: SELECT 1 FROM companies LIMIT 1

From lib/db.ts resolveSsl (currently unexported at ~582): (databaseUrl: string) => false | { rejectUnauthorized: boolean }
sslmode=disable → false; other sslmode → { rejectUnauthorized: false }; else host railway.internal / localhost / 127.0.0.1 / RFC1918 → false; else { rejectUnauthorized: false }

scripts/migrate.ts: MIGRATIONS_DIR = path.resolve(import.meta.dirname, '../migrations'); DATABASE_URL required; --check uses computePendingMigrations (exit 1 if pending or drifted); apply uses runMigrations then getDb() for seed-if-empty.

test/db.ts: hasTestDb, testPool(), closeTestPool(); database name must end in _test.

Vitest: npx vitest run lib/migrate  (no -x; Vitest 4 ignores it). Isolation none.
</interfaces>
</context>

<feature>
  <name>Checksum ledger migrate engine plus boot assert</name>
  <files>lib/migrate/plan.ts, lib/migrate/runner.ts, lib/migrate/assertMigrated.ts, scripts/migrate.ts, lib/migrate/plan.test.ts, lib/migrate/runner.test.ts, lib/migrate/assertMigrated.test.ts</files>
  <behavior>
    parseMigrationFile('0001-baseline-schema.sql', sql).version === 1 and checksum === sha256(sql).
    planPendingMigrations([f1,f2], [{version:1, checksum:f1.checksum}]).toApply versions === [2].
    Tampered applied file → drifted includes that filename; toApply empty.
    runMigrations fake client: one INSERT into schema_migrations framed by BEGIN/COMMIT; second call applied [] and alreadyApplied includes the filename.
    Drift throws /checksum drift/i.
    Failed SQL → ROLLBACK and error includes filename.
    assertMigrated empty rows → rejects /npm run migrate/; one row → resolves; ledger missing + companies row → resolves; both missing → rejects.
  </behavior>
  <implementation>
    Port origin lib/migrate/plan.ts, runner.ts, assertMigrated.ts and their tests via git show (D-02). Do not port origin 0001 SQL. Real-Postgres runner example in this plan uses inline CREATE TABLE IF NOT EXISTS probe SQL and an alternate ledger table name schema_migrations_probe, not a shipped baseline file (19-02 adds 0001 apply coverage).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="19-01-01">
  <name>End-to-end one SQL file through plan, runner, ledger, assertMigrated</name>
  <reversibility rating="costly">schema_migrations plus checksum-per-file becomes the schema history contract; later phases add NNNN-*.sql on this ledger (D-02, DATA-02).</reversibility>
  <files>lib/migrate/plan.ts, lib/migrate/plan.test.ts, lib/migrate/runner.ts, lib/migrate/runner.test.ts, lib/migrate/assertMigrated.ts, lib/migrate/assertMigrated.test.ts</files>
  <read_first>
  - .planning/phases/19-data-layer-cutover/19-PATTERNS.md (Pattern 1 and 2; origin analog)
  - .planning/phases/19-data-layer-cutover/19-RESEARCH.md (Package Legitimacy tsx, anti-patterns dual path / fs-in-getDb)
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/plan.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/runner.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/assertMigrated.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/plan.test.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/runner.test.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:lib/migrate/assertMigrated.test.ts
  - test/db.ts
  </read_first>
  <behavior>
  - parseMigrationFile('0001-a.sql', 'CREATE TABLE a (id SERIAL PRIMARY KEY);') returns version 1 and checksum sha256 of that sql (DATA-02, D-02).
  - runMigrations on FakeClient records one ledger INSERT with version and checksum, contains BEGIN and COMMIT, and a second run returns applied [] and alreadyApplied ['0001-baseline-schema.sql'] (DATA-02).
  - assertMigrated with rows [] rejects with /npm run migrate/; with one row resolves; ledger query throw plus companies row resolves; both throw rejects (DATA-01, T-19-02).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Do not check out origin/gsd/quick-260826-ded-data-layer-migrations; use git show only (D-02).
  RED: add lib/migrate/plan.test.ts, runner.test.ts (FakeClient unit cases from origin), and assertMigrated.test.ts from origin so they fail before production modules exist. For the real-Postgres describe.skipIf(!hasTestDb) in runner.test.ts, do not read migrations/0001-baseline-schema.sql (that file is 19-02). Use parseMigrationFile('0001-probe.sql', 'CREATE TABLE IF NOT EXISTS migrate_tracer_probe (id SERIAL PRIMARY KEY);') and runMigrations(..., { ledgerTable: 'schema_migrations_probe' }); afterAll DROP TABLE IF EXISTS schema_migrations_probe and migrate_tracer_probe. Commit test(19-01): red migrate plan runner assert.
  GREEN: port origin plan.ts, runner.ts, assertMigrated.ts verbatim in structure: FILENAME_RE, sha256 utf8, MIGRATION_LOCK_KEY 1347246335, DEFAULT_LEDGER_TABLE schema_migrations, fail loud on drift (no empty catch around apply). Do not import fs in these three modules. Do not add Prisma, Drizzle, or Kysely (D-06). Do not write migrations/0001-baseline-schema.sql. Commit feat(19-01): ledger migrate engine and boot assert.
  REFACTOR if needed with tests still green. Commit refactor(19-01): migrate engine cleanup only if you change code.
  </action>
  <verify>
    <automated>npx vitest run lib/migrate/plan.test.ts lib/migrate/runner.test.ts lib/migrate/assertMigrated.test.ts</automated>
  </verify>
  <done>One SQL file can be planned, applied with a checksum ledger row, skipped on second run, and assertMigrated fail-closes on an empty ledger (DATA-01, DATA-02, D-02).</done>
  <acceptance_criteria>
  - lib/migrate/plan.ts exports parseMigrationFile, planPendingMigrations, sha256
  - lib/migrate/runner.ts exports runMigrations, computePendingMigrations, MIGRATION_LOCK_KEY equal to 1347246335
  - lib/migrate/assertMigrated.ts exports assertMigrated
  - npx vitest run lib/migrate/plan.test.ts lib/migrate/runner.test.ts lib/migrate/assertMigrated.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="19-01-02">
  <name>Drift, duplicates, rollback, and --check pending preview</name>
  <files>lib/migrate/plan.ts, lib/migrate/plan.test.ts, lib/migrate/runner.ts, lib/migrate/runner.test.ts</files>
  <read_first>
  - lib/migrate/plan.ts from 19-01-01
  - lib/migrate/runner.ts from 19-01-01
  - origin plan.test.ts remaining cases (hyphens, 9999, duplicate version, applied-without-file)
  - origin runner.test.ts failOn / checksum drift cases
  </read_first>
  <behavior>
  - parseMigrationFile rejects README.md, 0001.sql, foo.sql, 12345-oversize.sql, -no-version.sql with /Invalid migration filename/ (DATA-02).
  - planPendingMigrations duplicate versions throws /Duplicate migration version/ (DATA-02).
  - Applied version with no file is not listed in drifted (DATA-02).
  - runMigrations tampered checksum throws /checksum drift/i; FakeClient failOn SQL triggers ROLLBACK and error matching the broken filename (DATA-02, T-19-01).
  - computePendingMigrations with missing ledger treats applied as empty (DATA-02 --check).
  </behavior>
  <action>
  TDD. RED: if 19-01-01 already ported the full origin plan.test.ts and runner.test.ts suites, this task only adds any missing cases listed in behavior (duplicate version, oversize filename, drift throw, ROLLBACK, computePendingMigrations missing-ledger). They must fail until runner/plan implement them. Commit test(19-01): red drift duplicate rollback check.
  GREEN: keep origin runner INSERT using file.version, file.filename, file.checksum; drifted.length &gt; 0 throws before applying. computePendingMigrations uses the same lock key and does not CREATE the ledger (missing relation → applied []). Commit feat(19-01): migrate drift lock and check preview.
  Do not swallow apply errors. Do not split SQL on semicolons — one client.query(file.sql) per file.
  </action>
  <verify>
    <automated>npx vitest run lib/migrate/plan.test.ts lib/migrate/runner.test.ts</automated>
  </verify>
  <done>Invalid names, duplicate versions, checksum drift, and mid-file failure are loud; --check can preview pending without creating the ledger (DATA-02, T-19-01).</done>
  <acceptance_criteria>
  - plan.test.ts covers invalid filenames, duplicate versions, and drift
  - runner.test.ts covers idempotent second run, drift throw, and ROLLBACK on failure
  - computePendingMigrations is exported from lib/migrate/runner.ts
  - npx vitest run lib/migrate/plan.test.ts lib/migrate/runner.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="19-01-03">
  <name>tsx pin, npm run migrate CLI, export resolveSsl</name>
  <files>package.json, package-lock.json, scripts/migrate.ts, lib/db.ts</files>
  <read_first>
  - package.json scripts and devDependencies
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:scripts/migrate.ts
  - lib/db.ts resolveSsl (~582) and getDb Pool ssl (~615)
  - lib/migrate/plan.ts and runner.ts from this plan
  </read_first>
  <action>
  Pin tsx at 4.23.12 with npm install --save-dev tsx@4.23.12 (D-10, T-19-SC). Do not use a floating caret. User accepted this pin in discuss defaults; origin already used 4.23.12. Confirm the installed package name is tsx (npmjs.com/package/tsx, repo privatenumber/tsx) before committing the lockfile.
  Add scripts.migrate exactly: npx tsx scripts/migrate.ts
  Create scripts/migrate.ts from origin structure: readdirSync migrations/*.sql sorted, parseMigrationFile, Pool with resolveSsl(databaseUrl), --check branch, else runMigrations then await getDb(). DATABASE_URL missing throws. Import getDb and resolveSsl from @/lib/db. Import parseMigrationFile from @/lib/migrate/plan and runMigrations, computePendingMigrations from @/lib/migrate/runner.
  Change lib/db.ts function resolveSsl to export function resolveSsl. Do not change getDb's boot chain in this task (19-04). Do not import fs in lib/db.ts. Do not add Prisma (D-06).
  mkdir migrations is allowed as an empty dir with a .gitkeep if needed so readdirSync does not throw; do not add 0001-baseline-schema.sql (D-03 is 19-02).
  Commit feat(19-01): tsx migrate CLI and export resolveSsl.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.migrate!=='npx tsx scripts/migrate.ts') process.exit(1); if(p.devDependencies.tsx!=='4.23.12') process.exit(1);"</automated>
    <human-check>On npmjs.com/package/tsx confirm 4.23.12 is privatenumber/tsx (T-19-SC). Pin already accepted in phase discuss defaults.</human-check>
  </verify>
  <done>Operators can run npm run migrate via tsx; CLI and data-fix scripts can import resolveSsl (D-10, D-06, DATA-02).</done>
  <acceptance_criteria>
  - package.json scripts.migrate equals npx tsx scripts/migrate.ts
  - package.json devDependencies.tsx equals 4.23.12
  - scripts/migrate.ts exists and references parseMigrationFile and runMigrations
  - lib/db.ts contains export function resolveSsl
  - node -e package.json checks above exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI → PostgreSQL | npm run migrate applies operator-controlled SQL files; app replicas must not be a second writer yet (cutover in 19-04) |
| App boot → schema_migrations | assertMigrated is the fail-closed gate once getDb calls it (19-04); this plan ships the function |
| Migration files → ledger checksum | Editing an applied file must not apply silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | lib/migrate/runner.ts checksum | high | mitigate | planPendingMigrations drifted[]; runMigrations throws Migration checksum drift detected; unit tests in runner.test.ts / plan.test.ts (ASVS V6) |
| T-19-02 | Tampering | assertMigrated empty/missing ledger | high | mitigate | Empty ledger throws runbook; fresh DB (no companies) throws; unit tests in assertMigrated.test.ts (ASVS V5 fail closed) |
| T-19-03 | Denial of Service | concurrent migrate | medium | mitigate | pg_advisory_lock(1347246335) around plan and apply (DATA-02 concurrency) |
| T-19-04 | Elevation of Privilege | scripts/migrate.ts | medium | mitigate | CLI uses DATABASE_URL only; no HTTP; filename regex rejects path characters (ASVS V5) |
| T-19-05 | Information Disclosure | seed via getDb after migrate | low | accept | Existing seedAuthData only when users empty; unchanged this plan |
| T-19-SC | Tampering | npm install tsx | high | mitigate | Pin tsx@4.23.12 exact; origin-validated; user-accepted default; verify npmjs package tsx (privatenumber/tsx) in 19-01-03 |

</threat_model>

<verification>
- 19-01-01: plan + FakeClient runner + assertMigrated green
- 19-01-02: drift, duplicates, rollback, computePendingMigrations green
- 19-01-03: migrate script and tsx 4.23.12 present; resolveSsl exported
- Wave: npx vitest run lib/migrate
</verification>

<success_criteria>
A single SQL file can be applied exactly once with a checksum ledger row; drift and empty ledger fail loudly; npm run migrate exists with tsx 4.23.12 (DATA-01 guard library, DATA-02, D-02, D-10).
</success_criteria>

<output>
Create `.planning/phases/19-data-layer-cutover/19-01-SUMMARY.md` when done
</output>
