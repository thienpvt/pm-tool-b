---
phase: 19-data-layer-cutover
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - scripts/data-fixes/run-sql-fix.ts
  - scripts/data-fixes/01-users-onboarding-completed.ts
  - scripts/data-fixes/02-portfolio-members-member-type.ts
  - scripts/data-fixes/03-projects-company-id-sync.ts
  - scripts/data-fixes/04-activities-jira-parent-repair.ts
  - scripts/data-fixes/backfill-weighted-completion.ts
  - scripts/data-fixes/backfill-user-roles.ts
  - scripts/data-fixes/backfill-pm-assignments.ts
  - scripts/data-fixes/backfill-raid-masters.ts
  - scripts/data-fixes/backfill-mapping-tenant.ts
  - lib/migrate/data-fixes.test.ts
  - scripts/data-fixes/README.md
autonomous: true
requirements: [DATA-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "scripts/data-fixes/ contains operator scripts for the four former boot UPDATEs plus weighted completion and v2.0 backfills; each requires DATABASE_URL and is not imported from lib/db.ts (DATA-03, D-02)."
    - "runFix prints name and rowCount and uses resolveSsl from @/lib/db with a dedicated Pool that is ended in finally (DATA-03)."
    - "lib/migrate/data-fixes.test.ts asserts 01-04 UPDATE scripts carry SQL templates, and v2.0 backfill scripts either carry SQL or call the existing helper/flag (DATA-03)."
    - statement: "getDb will stop invoking these DML paths in 19-04; this plan only relocates the SQL (DATA-01/DATA-03 flagged assumption)."
      verification: backstop
  artifacts:
    - path: "scripts/data-fixes/run-sql-fix.ts"
      provides: "Shared Pool runner (D-02 origin pattern)"
      exports: ["runFix", "Fix"]
    - path: "scripts/data-fixes/01-users-onboarding-completed.ts"
      provides: "Former lib/db.ts ~440 UPDATE"
    - path: "scripts/data-fixes/backfill-user-roles.ts"
      provides: "backfillUserRoles SQL + roles_backfill_v1 flag"
    - path: "lib/migrate/data-fixes.test.ts"
      provides: "Script SQL presence checks (DATA-03)"
  key_links:
    - from: "scripts/data-fixes/run-sql-fix.ts"
      to: "lib/db.ts"
      via: "import { resolveSsl } from '@/lib/db'"
      pattern: "resolveSsl"
    - from: "scripts/data-fixes/*.ts"
      to: "scripts/data-fixes/run-sql-fix.ts"
      via: "runFix({ name, sql })"
      pattern: "runFix"
  prohibitions:
    - statement: "Must not merge origin v1.0 0001 SQL (would drop/omit v2.0 tables)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not leave dual writers (boot DDL AND external migrate)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not run data-fix UPDATEs on every process start"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Move boot-time data-fix UPDATEs and v2.0 backfills to scripts/data-fixes/ so they are operator one-offs (DATA-03, D-02). They must not be imported by getDb (enforced in 19-04).

Purpose: DML no longer rides every replica start.

Output: run-sql-fix.ts, origin 01-04 plus weighted completion, four v2.0 backfill scripts, data-fixes.test.ts (scripts only — do not scan migrations/*.sql for UPDATE, because 19-02 Part 3 RAID DML is allowed per D-09).
</objective>

## Planner notes (revision 1)

Scope_sanity: files_modified count and estimate tokens (48k, confidence low, 0 samples) accepted — leave as-is; do not split; do not treat token figures as calibrated.

Dependency: Wave 2 parallel with 19-02 is safe. This plan only imports helpers that already exist before 19-02's export refactor (`backfillUserRoles`, `backfillPmAssignments`, `backfillRaidMasters`, `migrateMappingTableTenancy`). It must not require `ROLES_AUDIT_DDL` or `MAPPING_TENANT_DDL`.

## Artifacts this phase produces

- `runFix`, `Fix` (scripts/data-fixes/run-sql-fix.ts)
- `scripts/data-fixes/01-users-onboarding-completed.ts`
- `scripts/data-fixes/02-portfolio-members-member-type.ts`
- `scripts/data-fixes/03-projects-company-id-sync.ts`
- `scripts/data-fixes/04-activities-jira-parent-repair.ts`
- `scripts/data-fixes/backfill-weighted-completion.ts`
- `scripts/data-fixes/backfill-user-roles.ts`
- `scripts/data-fixes/backfill-pm-assignments.ts`
- `scripts/data-fixes/backfill-raid-masters.ts`
- `scripts/data-fixes/backfill-mapping-tenant.ts`
- `lib/migrate/data-fixes.test.ts`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-data-layer-cutover/19-PATTERNS.md data-fix analog
@.planning/phases/19-data-layer-cutover/19-01-PLAN.md (resolveSsl export)
lib/db.ts ~440, ~448, ~475, ~489-504, backfillWeightedCompletion ~516-552
lib/db-roles.ts backfillUserRoles
lib/db-project-master.ts backfillPmAssignments
lib/db-raid-masters.ts backfillRaidMasters / dedupeRaidCodes
lib/db-mapping-tenant.ts migrateOneTable backfill (CROSS JOIN)

19-02 may land in parallel and put RAID DML in 0001. data-fixes.test.ts must not require migrations/*.sql to contain zero UPDATE lines. Do not import 19-02-only DDL array exports; call the pre-existing helper functions listed in interfaces.

<interfaces>
runFix({ name, sql }: Fix): Promise<void>
Fix: { name: string; sql: string }
DATABASE_URL required; Pool({ connectionString, ssl: resolveSsl(databaseUrl) }); pool.query(sql); console.log(`${name}: ${result.rowCount ?? 0} rows affected`); pool.end() in finally.
Entry scripts: runFix({...}).catch(err => { console.error(...); process.exitCode = 1 })
Weighted completion and v2.0 backfills may use a small async main that opens a Pool like runFix, checks settings flags (completion_pct_weighted_v1, roles_backfill_v1, pm_assignment_backfill_v1, raid_masters_backfill_v1, mapping_tenant_*_v1) then runs the same SQL as the current helpers. Prefer importing backfillUserRoles, backfillPmAssignments from lib/db-*.ts when that avoids duplicating parameterized loops; mapping and RAID procedural loops can import those functions. Scripts still must not be imported from lib/db.ts.
npx tsx scripts/data-fixes/&lt;file&gt;.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="19-03-01">
  <name>Shared runFix helper and four boot UPDATE scripts</name>
  <files>scripts/data-fixes/run-sql-fix.ts, scripts/data-fixes/01-users-onboarding-completed.ts, scripts/data-fixes/02-portfolio-members-member-type.ts, scripts/data-fixes/03-projects-company-id-sync.ts, scripts/data-fixes/04-activities-jira-parent-repair.ts, lib/migrate/data-fixes.test.ts</files>
  <read_first>
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:scripts/data-fixes/run-sql-fix.ts
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:scripts/data-fixes/01-users-onboarding-completed.ts
  - lib/db.ts lines 440, 448, 475, 489-504
  - lib/db.ts export resolveSsl
  </read_first>
  <behavior>
  - Each of 01-04 files contains a sql template literal whose trimmed text starts with UPDATE (DATA-03).
  - run-sql-fix.ts exports runFix and throws when DATABASE_URL is missing (DATA-03).
  </behavior>
  <action>
  TDD. RED: lib/migrate/data-fixes.test.ts reads scripts/data-fixes, skips run-sql-fix.ts, asserts each other .ts has sql `...` matching /^(UPDATE|INSERT|SELECT)/i. Commit test(19-03): red data-fix scripts exist.
  GREEN: port origin run-sql-fix.ts and 01-04; SQL bodies must be the current lib/db.ts strings (not origin-only if they differ). Import resolveSsl from @/lib/db. Do not import run-sql-fix from lib/db.ts. Commit feat(19-03): boot UPDATE data-fix scripts.
  Do not add a migrations/*.sql UPDATE ban in this test (D-09 RAID DML lives in 0001).
  </action>
  <verify>
    <automated>npx vitest run lib/migrate/data-fixes.test.ts</automated>
  </verify>
  <done>The four former boot UPDATEs live under scripts/data-fixes/ and are operator-run via tsx (DATA-03, D-02).</done>
  <acceptance_criteria>
  - scripts/data-fixes/01-users-onboarding-completed.ts through 04-activities-jira-parent-repair.ts exist
  - scripts/data-fixes/run-sql-fix.ts exports runFix
  - npx vitest run lib/migrate/data-fixes.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="19-03-02">
  <name>Weighted completion and v2.0 backfill scripts</name>
  <files>scripts/data-fixes/backfill-weighted-completion.ts, scripts/data-fixes/backfill-user-roles.ts, scripts/data-fixes/backfill-pm-assignments.ts, scripts/data-fixes/backfill-raid-masters.ts, scripts/data-fixes/backfill-mapping-tenant.ts, lib/migrate/data-fixes.test.ts</files>
  <read_first>
  - lib/db.ts backfillWeightedCompletion (~516-552) and STATUS_WEIGHTS usage
  - git show origin/gsd/quick-260826-ded-data-layer-migrations:scripts/data-fixes/backfill-weighted-completion.ts
  - lib/db-roles.ts backfillUserRoles
  - lib/db-project-master.ts backfillPmAssignments
  - lib/db-raid-masters.ts backfillRaidMasters
  - lib/db-mapping-tenant.ts migrateOneTable null-company branch
  </read_first>
  <behavior>
  - data-fixes.test.ts lists the five backfill-*.ts files and still extracts a SQL template or documents that scripts which only call exported backfill functions contain the helper name backfillUserRoles / backfillPmAssignments / backfillRaidMasters / migrateMappingTableTenancy / completion_pct_weighted_v1 (DATA-03).
  </behavior>
  <action>
  TDD. Extend data-fixes.test.ts so scripts that cannot expose a single sql: `...` (procedural backfills) are allowed if the file text includes the corresponding helper export name or FLAG constant from read_first. RED then GREEN.
  Implement backfill-weighted-completion.ts from current backfillWeightedCompletion (flag completion_pct_weighted_v1, leaf CASE from STATUS_WEIGHTS, EPIC average). Implement v2.0 scripts: preferred pattern is Pool + resolveSsl + call backfillUserRoles(pool) / backfillPmAssignments(pool) / backfillRaidMasters logic / migrateMappingTableTenancy(pool) so settings flags stay the source of idempotency. Those helpers already exist on main; do not import ROLES_AUDIT_DDL or MAPPING_TENANT_DDL (19-02 export refactor is not required). RAID script remaining useful after 19-02 0001 for operator re-run when flags are unset.
  Catch missing DATABASE_URL. Do not import these scripts from lib/db.ts. Commit feat(19-03): v2 backfill operator scripts.
  </action>
  <verify>
    <automated>npx vitest run lib/migrate/data-fixes.test.ts</automated>
  </verify>
  <done>Weighted completion, roles, PM assignments, RAID, and mapping-tenant backfills are operator scripts (DATA-03).</done>
  <acceptance_criteria>
  - five backfill-*.ts files exist under scripts/data-fixes/
  - npx vitest run lib/migrate/data-fixes.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="19-03-03">
  <name>README list of data-fix commands</name>
  <files>scripts/data-fixes/README.md</files>
  <read_first>
  - migrations/README.md (created in 19-02 or write a data-fixes section if 19-02 has not merged yet — if the file is missing, create only a ## One-off data fixes section file that 19-02 can absorb; if 19-02 already wrote README, append the command list)
  - scripts/data-fixes filenames from 19-03-01 and 19-03-02
  </read_first>
  <action>
  File overlap: 19-02 also writes migrations/README.md on wave 2. If 19-02 is not merged, skip this task's README edit and put the command list in scripts/data-fixes/README.md instead so wave-2 parallel does not clobber 19-02. Prefer scripts/data-fixes/README.md always for the command list (npx tsx scripts/data-fixes/01-users-onboarding-completed.ts through backfill-mapping-tenant.ts). State DATABASE_URL required and that these are not on the app boot path (DATA-03).
  Commit docs(19-03): data-fix operator commands.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='scripts/data-fixes/README.md'; if(!fs.existsSync(p)) process.exit(1); const t=fs.readFileSync(p,'utf8'); if(!t.includes('01-users-onboarding-completed')) process.exit(1); if(!t.includes('backfill-mapping-tenant')) process.exit(1);"</automated>
  </verify>
  <done>Operators have a command list for one-off DML (DATA-03).</done>
  <acceptance_criteria>
  - scripts/data-fixes/README.md lists 01-users-onboarding-completed and backfill-mapping-tenant
  - README states DATABASE_URL is required
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator laptop → production DML | Data-fixes run with DATABASE_URL; must not be triggered by getDb |
| scripts/data-fixes → lib/db.ts | One-way import of resolveSsl only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Tampering | data-fixes on boot | high | mitigate | Scripts live under scripts/data-fixes/; 19-04 greps getDb does not import that directory (DATA-03) |
| T-19-04 | Elevation of Privilege | runFix Pool | medium | mitigate | Same DATABASE_URL as app; no extra role; missing env throws |
| T-19-SC | Tampering | npm installs | high | mitigate | No new packages; reuse tsx from 19-01 |

</threat_model>

<verification>
- 19-03-01: four UPDATE scripts + runFix + data-fixes.test.ts green
- 19-03-02: five backfill scripts + test green
- 19-03-03: scripts/data-fixes/README.md present
</verification>

<success_criteria>
Former boot UPDATEs and v2.0 backfills are tsx operator scripts under scripts/data-fixes/ and are not part of the versioned DDL ledger except RAID DML already in 0001 Part 3 (DATA-03, D-09).
</success_criteria>

<output>
Create `.planning/phases/19-data-layer-cutover/19-03-SUMMARY.md` when done
</output>
