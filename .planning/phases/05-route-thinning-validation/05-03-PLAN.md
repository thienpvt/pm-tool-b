---
phase: 05-route-thinning-validation
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/api/projects/[id]/risks/schema.ts
  - app/api/projects/[id]/activities/schema.ts
  - app/api/projects/[id]/issues/schema.ts
  - app/api/projects/[id]/meetings/schema.ts
  - app/api/projects/[id]/escalations/schema.ts
  - app/api/projects/[id]/team/schema.ts
  - app/api/projects/[id]/documents/schema.ts
  - app/api/projects/[id]/bugs/schema.ts
  - app/api/projects/[id]/holidays/schema.ts
  - app/api/projects/[id]/milestones/schema.ts
  - app/api/projects/[id]/milestones/[milestoneId]/epics/schema.ts
  - app/api/projects/[id]/budget/schema.ts
  - app/api/projects/[id]/budget/[itemId]/schema.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/schema.ts
  - app/api/projects/[id]/route.ts
  - app/api/projects/[id]/risks/route.ts
  - app/api/projects/[id]/activities/route.ts
  - app/api/projects/[id]/issues/route.ts
  - app/api/projects/[id]/meetings/route.ts
  - app/api/projects/[id]/escalations/route.ts
  - app/api/projects/[id]/team/route.ts
  - app/api/projects/[id]/documents/route.ts
  - app/api/projects/[id]/bugs/route.ts
  - app/api/projects/[id]/holidays/route.ts
  - app/api/projects/[id]/milestones/route.ts
  - app/api/projects/[id]/milestones/[milestoneId]/epics/route.ts
  - app/api/projects/[id]/budget/route.ts
  - app/api/projects/[id]/budget/[itemId]/route.ts
  - app/api/projects/[id]/budget/[itemId]/expenses/route.ts
  - app/api/admin/companies/schema.ts
  - app/api/admin/companies/route.ts
  - app/api/admin/users/schema.ts
  - app/api/admin/users/route.ts
  - app/api/admin/demo-requests/schema.ts
  - app/api/admin/demo-requests/route.ts
  - app/api/demo-requests/schema.ts
  - app/api/demo-requests/route.ts
  - app/api/import-mapping/schema.ts
  - app/api/import-mapping/route.ts
  - app/api/import-mapping/[id]/route.ts
  - app/api/bug-import-mapping/schema.ts
  - app/api/bug-import-mapping/route.ts
  - app/api/jira/sync-mappings/schema.ts
  - app/api/jira/sync-mappings/route.ts
  - app/api/jira/jql-presets/schema.ts
  - app/api/jira/jql-presets/route.ts
  - app/api/operations/systems/schema.ts
  - app/api/operations/systems/route.ts
  - app/api/operations/systems/[id]/schema.ts
  - app/api/operations/systems/[id]/route.ts
  - app/api/operations/systems/[id]/budget-items/schema.ts
  - app/api/operations/systems/[id]/budget-items/route.ts
  - app/api/operations/systems/[id]/incidents/schema.ts
  - app/api/operations/systems/[id]/incidents/route.ts
  - app/api/portfolio/program-allocations/schema.ts
  - app/api/portfolio/program-allocations/route.ts
  - app/api/config/schema.ts
  - app/api/config/route.ts
  - app/api/admin/rag-config/[companyId]/schema.ts
  - app/api/admin/rag-config/[companyId]/route.ts
  - app/api/admin/jira-config/[companyId]/schema.ts
  - app/api/admin/jira-config/[companyId]/route.ts
autonomous: true
requirements: [ROUTE-06]
estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every request body-accepting route in the tree-A (projects/[id]/**, 12 schema.ts files) and tree-B (~14 routes: admin, demo-requests, import-mapping, bug-import-mapping, jira sync-mappings/jql-presets, operations, program-allocations, config, rag-config, jira-config) scope validates its body against an explicit Zod schema via safeParse before reaching a service or repository call."
    - "A Zod safeParse failure returns the ROUTE's pre-existing 400 body shape verbatim — 'Name required', 'All fields are required', 'Missing fields', 'id and name required', 'Username and password required', frozen strings are never replaced with a generic Zod error message."
    - "Tree-A schemas are shape guards only (object + optional field type-checks) — they never reject a body that reaches the service today; no CAPEX/OPEX enum duplicate is added (services own that business rule per budget.service.ts:56)."
    - "rag-config and jira-config POST schemas use z.coerce.number()/permissive typing so today's Number()-coercion and ??''-default behavior is preserved for non-numeric or missing inputs (orchestrator resolution: A3-A5 permissive schemas)."
    - "auth/login, auth/change-password, auth/complete-onboarding payload schemas are NOT created (deferred to owning phases per CONTEXT)."
    - "Full suite: 0 failed, skipped exactly 113 (baseline held)."
  artifacts:
    - path: "app/api/projects/[id]/risks/schema.ts"
      provides: "riskInputSchema, riskUpdateSchema — passthrough shape guards wired into risks/route.ts via withProjectAccess's opts.schema"
    - path: "app/api/demo-requests/schema.ts"
      provides: "demoRequestSchema — strict full_name/phone/email/company_name .trim().min(1, 'All fields are required')"
    - path: "app/api/admin/rag-config/[companyId]/schema.ts"
      provides: "ragConfigSchema — permissive z.coerce.number().optional() per field, preserving today's Number(x ?? default) fallback"
  key_links:
    - from: "app/api/projects/[id]/risks/route.ts"
      to: "app/api/projects/[id]/risks/schema.ts"
      via: "withProjectAccess(handler, { schema: riskInputSchema })"
      pattern: "opts.schema.safeParse in lib/http/with-auth.ts, 400 on failure with the route's frozen body"
    - from: "app/api/demo-requests/route.ts"
      to: "app/api/demo-requests/schema.ts"
      via: "demoRequestSchema.safeParse(await req.json())"
      pattern: "in-place safeParse swap, same 400 body, tree-B routes are NOT wrapper-converted"
---

<objective>
Wire Zod request-body validation into (a) the 12 tree-A resources under `app/api/projects/[id]/**` via the `withProjectAccess`/`withAuth` wrapper's `opts.schema`, and (b) the ~14 tree-B routes that have inline validation today (`if (!x)` chains) but are NOT wrapper-converted this phase — replacing their manual checks with `safeParse` against an adjacent `schema.ts`, preserving the exact same 400 body on every route.

Sequencing note: this plan runs in wave 3, after 05-02 has already converted every tree-A `route.ts` onto `withProjectAccess` — Task 1 here only ADDS an `opts.schema` argument to those already-wrapped calls (file overlap with 05-02 is real and intentional; it is why this plan is sequential, not parallel, with 05-02).

Purpose: complete ROUTE-06 (every request body validated against an explicit Zod schema at the route boundary before reaching a service), the validation half of Phase 5's two mechanisms.
Output: 14 new `schema.ts` files (12 tree-A + one shared per some tree-B groupings) and the corresponding route.ts files wired to use them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-route-thinning-validation/05-CONTEXT.md
@.planning/phases/05-route-thinning-validation/05-RESEARCH.md
@.planning/phases/05-route-thinning-validation/05-PATTERNS.md
@.planning/phases/05-route-thinning-validation/05-VALIDATION.md
@.planning/phases/05-route-thinning-validation/05-01-SUMMARY.md
@.planning/phases/05-route-thinning-validation/05-02-SUMMARY.md

@lib/integrations/jira/schemas.ts
@lib/http/with-auth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tree-A schemas — 12 schema.ts files wired into projects/[id]/** via opts.schema</name>
  <files>app/api/projects/[id]/risks/schema.ts, app/api/projects/[id]/activities/schema.ts, app/api/projects/[id]/issues/schema.ts, app/api/projects/[id]/meetings/schema.ts, app/api/projects/[id]/escalations/schema.ts, app/api/projects/[id]/team/schema.ts, app/api/projects/[id]/documents/schema.ts, app/api/projects/[id]/bugs/schema.ts, app/api/projects/[id]/holidays/schema.ts, app/api/projects/[id]/milestones/schema.ts, app/api/projects/[id]/milestones/[milestoneId]/epics/schema.ts, app/api/projects/[id]/budget/schema.ts, app/api/projects/[id]/budget/[itemId]/schema.ts, app/api/projects/[id]/budget/[itemId]/expenses/schema.ts, plus the route.ts in each of those 12 directories (13 route files: route.ts appears once per directory except budget/[itemId]/expenses/[expId] which has no body-accepting method and is excluded), app/api/projects/[id]/route.ts</files>
  <read_first>
    lib/integrations/jira/schemas.ts (the z.object + .passthrough() + nullable/optional idiom to copy);
    lib/http/with-auth.ts (opts.schema / opts.badRequest contract from 05-01 — the wrapper calls `opts.schema.safeParse(raw)` and on failure returns `opts.badRequest(parsed.error)` or the first issue message);
    app/api/projects/[id]/risks/route.ts, activities/route.ts, issues/route.ts, meetings/route.ts, escalations/route.ts, team/route.ts, documents/route.ts, bugs/route.ts, holidays/route.ts, milestones/route.ts, milestones/[milestoneId]/epics/route.ts, budget/route.ts, budget/[itemId]/route.ts, budget/[itemId]/expenses/route.ts, projects/[id]/route.ts (the current, ALREADY-CONVERTED-in-05-02 shape — this task only adds schema.ts + wires opts.schema into the existing withProjectAccess call, it does not re-touch the handler bodies);
    lib/services/budget.service.ts:56 (the CAPEX/OPEX ValidationError this task must NOT duplicate in a Zod enum)
  </read_first>
  <behavior>
    - Each schema is a `z.object({...}).passthrough()` shape guard: fields the route/service reads today with an inline check get a matching Zod type (`.optional()` unless the field is truly required with today's inline validation); fields never checked today stay untyped via passthrough.
    - `risks/schema.ts`, `activities/schema.ts`, `issues/schema.ts`, `meetings/schema.ts`: no inline validation exists today (services accept any object shape) — schema is `z.object({}).passthrough()` for both input and update, matching Pitfall 3's guidance (a strict schema here would reject bodies that pass today).
    - `escalations/schema.ts`: PUT only, `z.object({}).passthrough()` (escalations has no create, only update — the fields are dynamic per allowlist).
    - `team/schema.ts`, `documents/schema.ts`: input/update shape guards, `.passthrough()`; `documents/schema.ts` does NOT require `content` (service handles `body.content ?? {}` gracefully).
    - `bugs/schema.ts`: POST body `{ bugs: unknown[], snapshot_date?: string }` — `bugs: z.array(z.record(z.string(), z.unknown()))` (matches the service's own `Array.isArray` check, ValidationError already exists for non-array — do not duplicate, just shape-guard so a non-object body 400s at the boundary instead of reaching the service).
    - `holidays/schema.ts`: POST body `{ date?: string, name?: string }` — both optional (the service throws ValidationError('date required') when missing; Zod must not pre-empt that with a stricter 400, so keep both `.optional()`).
    - `milestones/schema.ts`, `milestones/[milestoneId]/epics/schema.ts`: `z.object({}).passthrough()` for milestone create/update; epics POST is `{ activity_id: z.union([z.string(), z.number()]) }` (matches `body.activity_id` usage).
    - `budget/schema.ts`, `budget/[itemId]/schema.ts`: budget item create/update body has `type`, `name`, `group_name`, `planned_amount`, `approved_amount`, `actual_amount`, `unit`, `notes` — all `.optional()` EXCEPT do not add `.min(1)` on `name` or `.enum(['CAPEX','OPEX'])` on `type` (both already throw `ValidationError` in `budget.service.ts`/`budget-items.service.ts` — a Zod-level rejection would return a DIFFERENT 400 body/message than the service's `'Name is required'`/`'Invalid type'`, which is a freeze break). Type each optional field loosely (`z.string()`, `z.union([z.string(), z.number()])` for amounts) so the shape guard only rejects genuinely malformed JSON (non-object body), never a today-valid-but-empty-name body.
    - `budget/[itemId]/expenses/schema.ts`: POST body `{ expense_date?, description?, amount?, reference? }`, all optional (service throws ValidationError on missing `description`).
    - `projects/[id]/route.ts` PATCH: `z.object({}).passthrough()` (the PROJECT_COLUMNS allowlist in the repo already rejects unknown keys via UnknownColumnError — Zod here is a shape guard only, not a column allowlist duplicate).
  </behavior>
  <action>
    Write each `schema.ts` file per the behavior spec above, then wire it into the corresponding route's already-converted `withProjectAccess(handler, opts)` call by adding `{ schema: <name>InputSchema }` (POST) or `{ schema: <name>UpdateSchema }` (PUT/PATCH) as the second argument — GET/DELETE calls stay `withProjectAccess(handler)` with no `opts` since they don't parse a body. Do not touch handler bodies further — this task only adds the `opts.schema` argument to existing wrapped calls and creates the schema files. Where a route's `ctx.body` was previously `unknown` and used via `body.field` casts, `ctx.body` is now the validated+typed shape from `parsed.data` — remove any now-redundant `as Record<string, unknown>` casts if straightforward, but do not refactor further than necessary.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run "app/api/projects/[id]" --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>12 tree-A schema.ts files created and wired via opts.schema into their withProjectAccess calls; no schema rejects a body that reaches the service today (CAPEX/OPEX, name-required, date-required cases all still resolve via the service's own ValidationError).</done>
  <acceptance_criteria>
    - All 12 schema.ts files exist under their respective `app/api/projects/[id]/**` directories, each exporting `.passthrough()` shape guards (no `.min()`/`.enum()` duplicating a service-level ValidationError)
    - `grep -c "CAPEX\|OPEX" app/api/projects/[id]/budget/schema.ts app/api/projects/[id]/budget/[itemId]/schema.ts` returns 0 (no enum duplicate)
    - A budget item POST with `{ name: '', type: 'CAPEX' }` still reaches the service and gets `'Name is required'` (NOT a Zod-shaped 400) — existing budget/route.test.ts "POST returns 400 for empty name" test still passes unchanged
    - A holidays POST with no `date` still reaches the service and gets `'date required'` — not pre-empted by Zod
    - `node node_modules/vitest/vitest.mjs run "app/api/projects/[id]" --reporter=json --outputFile=vt.json` reports 0 failed; parse and `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tree-B schemas — ~14 non-converted routes, in-place safeParse swap preserving frozen 400 bodies</name>
  <files>app/api/admin/companies/schema.ts, app/api/admin/companies/route.ts, app/api/admin/users/schema.ts, app/api/admin/users/route.ts, app/api/admin/demo-requests/schema.ts, app/api/admin/demo-requests/route.ts, app/api/demo-requests/schema.ts, app/api/demo-requests/route.ts, app/api/import-mapping/schema.ts, app/api/import-mapping/route.ts, app/api/import-mapping/[id]/route.ts, app/api/bug-import-mapping/schema.ts, app/api/bug-import-mapping/route.ts, app/api/jira/sync-mappings/schema.ts, app/api/jira/sync-mappings/route.ts, app/api/jira/jql-presets/schema.ts, app/api/jira/jql-presets/route.ts, app/api/operations/systems/schema.ts, app/api/operations/systems/route.ts, app/api/operations/systems/[id]/schema.ts, app/api/operations/systems/[id]/route.ts, app/api/operations/systems/[id]/budget-items/schema.ts, app/api/operations/systems/[id]/budget-items/route.ts, app/api/operations/systems/[id]/incidents/schema.ts, app/api/operations/systems/[id]/incidents/route.ts, app/api/portfolio/program-allocations/schema.ts, app/api/portfolio/program-allocations/route.ts, app/api/config/schema.ts, app/api/config/route.ts, app/api/admin/rag-config/[companyId]/schema.ts, app/api/admin/rag-config/[companyId]/route.ts, app/api/admin/jira-config/[companyId]/schema.ts, app/api/admin/jira-config/[companyId]/route.ts</files>
  <read_first>
    lib/integrations/resend/client.ts:59-63 (the safeParse-then-branch idiom to copy for tree-B, no wrapper involved here);
    app/api/admin/companies/route.ts, admin/users/route.ts, admin/demo-requests/route.ts, demo-requests/route.ts, import-mapping/route.ts, import-mapping/[id]/route.ts, bug-import-mapping/route.ts, jira/sync-mappings/route.ts, jira/jql-presets/route.ts, operations/systems/route.ts, operations/systems/[id]/route.ts, operations/systems/[id]/budget-items/route.ts, operations/systems/[id]/incidents/route.ts, portfolio/program-allocations/route.ts, config/route.ts, admin/rag-config/[companyId]/route.ts, admin/jira-config/[companyId]/route.ts (current source for each — every inline `if (!x)` 400 check this task replaces);
    lib/rag.ts (DEFAULT_RAG_CONFIG shape for the rag-config schema);
    lib/services/portfolio.service.ts (createProgramAllocation:385-395 — the ValidationError('program_id required') this task must not duplicate at the Zod layer since program-allocations/route.ts calls the SERVICE, not inline validation — verify before assuming it needs a schema swap)
  </read_first>
  <behavior>
    - `admin/companies/schema.ts`: `{ name: z.string().trim().min(1, 'Name required') }` for POST; `{ id: z.union([z.number(), z.string()]), name: z.string().trim().min(1, 'id and name required') }` for PUT — on safeParse failure return the route's EXACT existing 400 body (`{ error: 'Name required' }` / `{ error: 'id and name required' }`), using `opts.badRequest`-style manual branching since these routes are NOT wrapper-converted (call `schema.safeParse` inline, branch on `parsed.success`, return `NextResponse.json({ error: '<frozen string>' }, { status: 400 })` using the FIRST issue's custom message or a literal string — do not rely on Zod's default message).
    - `admin/users/schema.ts`: POST `{ username: z.string().trim().min(1), password: z.string().min(1), display_name?, company_id?, is_admin? }` — failure returns `{ error: 'Username and password required' }` verbatim, not per-field Zod messages.
    - `admin/demo-requests/schema.ts`: PUT `{ id: required }`, DELETE has no body (query-param `id` — no schema needed, leave untouched). Failure body: `{ error: 'id required' }`.
    - `demo-requests/schema.ts`: POST `{ full_name, phone, email, company_name }` all `.trim().min(1, 'All fields are required')` — the SAME message on every field means the schema's `parsed.error.issues[0].message` is `'All fields are required'` regardless of which field(s) failed, matching today's single combined check exactly.
    - `import-mapping/schema.ts`: POST `{ name: required, mappings_json: required }` (note: `mappings_json` may be a string OR object today per `typeof mappings_json === 'string' ? mappings_json : JSON.stringify(mappings_json)` — schema must be `z.union([z.string(), z.record(z.string(), z.unknown())])`, never `z.string()` alone, or it breaks the object-body case per the Coercion-match rule); PUT (`import-mapping/[id]/route.ts`) has no inline validation today (name/mappings_json can be undefined) — schema.ts is optional for PUT, or PUT stays untouched since there's no frozen 400 to preserve (verify: original PUT has zero validation — do not ADD a new 400 here, that would be scope creation beyond "replace existing checks").
    - `bug-import-mapping/schema.ts`: same POST shape as import-mapping (`{ name, mappings_json }`), same union type; `[id]/route.ts` DELETE has no body, leave untouched.
    - `jira/sync-mappings/schema.ts`: POST `{ mappings_json: required }` (same union type), failure body `{ error: 'Missing mappings_json' }`.
    - `jira/jql-presets/schema.ts`: POST `{ name: required, jql: required, context?: string }`, failure body `{ error: 'Missing fields' }`; `[id]/route.ts` DELETE has no body, leave untouched.
    - `operations/systems/schema.ts`: POST `{ name: required, description?, project_id?, go_live_date?, status? }`, failure body `{ error: 'name required' }`.
    - `operations/systems/[id]/schema.ts`: PUT has NO inline validation today (`name`/`description`/etc all pass through unchecked) — schema is a passthrough shape guard only, no frozen 400 to preserve since none exists; do not add a new required-field check (scope creation).
    - `operations/systems/[id]/budget-items/schema.ts`: POST `{ category?, name: required, planned_amount?, actual_amount?, unit?, period_label?, notes? }`, failure body `{ error: 'name required' }`.
    - `operations/systems/[id]/incidents/schema.ts`: POST `{ title: required, severity?, description?, reported_at?, resolved_at?, cost_impact?, status? }`, failure body `{ error: 'title required' }`.
    - `portfolio/program-allocations/schema.ts`: the route calls `createProgramAllocation(actorOf(user), body)` which is a SERVICE that already throws `ValidationError('program_id required')` — confirm during read_first whether a Zod schema adds value here (shape guard only, since the service owns the required-field throw) or whether this route should be left as a passthrough-only schema. Do NOT duplicate the `program_id required` message at the Zod layer if the service already produces it — a passthrough shape guard (`z.object({ program_id: z.unknown().optional(), allocated_headcount: z.unknown().optional() }).passthrough()`) that only rejects non-object bodies is correct; the 400 with the frozen message continues to come from the service via `serviceErrorResponse`.
    - `config/schema.ts`: POST accepts an arbitrary `Record<string, unknown>` today (looped via `Object.entries(body)`) — schema is `z.record(z.string(), z.unknown())`, a passthrough shape guard only (this route sets arbitrary settings keys, there is no per-field frozen validation to preserve).
    - `admin/rag-config/[companyId]/schema.ts`: **per orchestrator resolution** — permissive `z.object({ spi_red_threshold: z.coerce.number().optional(), spi_amber_threshold: z.coerce.number().optional(), deadline_red_days: z.coerce.number().optional(), deadline_amber_days: z.coerce.number().optional(), risks_red: z.coerce.number().optional(), risks_amber: z.coerce.number().optional(), issues_amber: z.coerce.number().optional(), low_progress_amber: z.coerce.number().optional() }).passthrough()`. `z.coerce.number()` on a non-numeric string throws a Zod issue where today's `Number("abc")` silently produces `NaN` and gets stored — to preserve EXACT behavior (accept anything, coerce, store NaN if garbage), do NOT let a coercion failure 400; instead validate shape only (`z.record(z.string(), z.unknown()).passthrough()`) and leave the `Number(body.x ?? default)` coercion in the route exactly as it is today. Confirm during implementation which reading holds; prefer the passthrough-shape-only schema since it strictly preserves current accept-anything behavor (safer per the Coercion-match rule in 05-RESEARCH.md).
    - `admin/jira-config/[companyId]/schema.ts`: POST `{ base_url_var?: string, email_var?: string, token_var?: string }` all optional/nullable-ish since the route does `?? ''` defaulting — passthrough shape guard, no rejection of a today-valid empty-string body.
  </behavior>
  <action>
    For each of the ~14 tree-B routes, create the adjacent `schema.ts` per the behavior spec, then swap the route's inline `if (!x)` check for `const parsed = schema.safeParse(await req.json()); if (!parsed.success) return NextResponse.json({ error: '<frozen string>' }, { status: 400 }); const { ...fields } = parsed.data;` — using the frozen string as a literal in the route (not relying on Zod's auto-generated message), matching the exact pre-existing 400 body byte-for-byte. Where a route has NO inline validation today (operations/systems/[id] PUT, import-mapping/[id] PUT, config POST, program-allocations shape only, rag-config, jira-config), the schema is a passthrough type-shape guard ONLY — do not invent a new required-field 400 that didn't exist before (that would be undeferred scope, a freeze violation in the other direction). Every `requireAdmin`/session-check prologue in these routes is UNTOUCHED — this task only touches body validation, never the auth prologue (these routes are tree-B, not wrapper-converted).
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run app/api/admin app/api/demo-requests app/api/import-mapping app/api/bug-import-mapping app/api/jira/sync-mappings app/api/jira/jql-presets app/api/operations app/api/portfolio/program-allocations app/api/config --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>~14 tree-B routes swapped from inline if(!x) checks to safeParse, every frozen 400 body preserved byte-identical; no new validation added to routes that had none today.</done>
  <acceptance_criteria>
    - Every frozen 400 string ('Name required', 'id and name required', 'Username and password required', 'Cannot delete yourself' remains untouched since it's not body-shape validation, 'All fields are required', 'Missing fields', 'Missing mappings_json', 'name required', 'title required') is byte-identical after the swap — verify via existing route tests where they exist (`admin/rag-config/[companyId]/route.test.ts`, `portfolio/program-allocations/route.test.ts`) plus manual verification for routes with no existing test
    - No new 400 validation was added to a route that had none today (operations/systems/[id] PUT, import-mapping/[id] PUT, config POST, rag-config, jira-config) — these get passthrough-only schemas
    - `admin/rag-config/[companyId]/route.test.ts`'s two existing tests ("returns the stable eight-field shape", "returns the same shape when falling back to defaults") still pass unchanged
    - `portfolio/program-allocations/route.test.ts`'s four existing tests (401, 400 program_id missing, 200 success, 500 generic) still pass unchanged
    - `node node_modules/vitest/vitest.mjs run app/api/admin app/api/demo-requests app/api/import-mapping app/api/bug-import-mapping app/api/jira/sync-mappings app/api/jira/jql-presets app/api/operations app/api/portfolio/program-allocations app/api/config --reporter=json --outputFile=vt.json` reports 0 failed; parse and `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase-wide boundary sweep — full suite, ROUTE-06 completeness grep, tsc, eslint, REQUIREMENTS.md update</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/phases/05-route-thinning-validation/05-VALIDATION.md (baseline: 573/460/0/113, frozen-string checklist);
    .planning/REQUIREMENTS.md (ROUTE-01/02/05/06/07/12 rows to mark complete)
  </read_first>
  <action>
    Run the full suite and confirm 0 failed, numPendingTests exactly 113. Grep-confirm no `.parse(` calls exist anywhere in the new schema.ts files (only `.safeParse` — matching the zero-`.parse()` codebase idiom). Grep-confirm auth/login, auth/change-password, auth/complete-onboarding have NO new schema.ts (deferred per CONTEXT — `ls app/api/auth/login/schema.ts` etc. must not exist). Update `.planning/REQUIREMENTS.md`: mark ROUTE-01, ROUTE-02, ROUTE-05, ROUTE-06, ROUTE-07, ROUTE-12 complete (all six Phase 5 requirements). Run `npx tsc --noEmit` and `npx eslint` across all changed files from Tasks 1-2.
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>Full suite green at baseline; zero .parse() calls in any schema.ts; auth payload schemas confirmed absent (deferred); all 6 Phase 5 requirements marked complete in REQUIREMENTS.md.</done>
  <acceptance_criteria>
    - Full suite: 0 failed, numPendingTests exactly 113 (parse vt.json; `rm vt.json`)
    - `grep -rn "\.parse(" app/api/*/schema.ts app/api/*/*/schema.ts app/api/*/*/*/schema.ts 2>/dev/null | grep -v safeParse` returns empty
    - `ls app/api/auth/login/schema.ts app/api/auth/change-password/schema.ts app/api/auth/complete-onboarding/schema.ts 2>&1` all report "No such file" (deferred, not created)
    - REQUIREMENTS.md shows all 6 Phase 5 requirement rows marked complete
    - `npx tsc --noEmit` exits 0; `npx eslint` clean on changed files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> tree-A body (12 resources) | Untrusted JSON now shape-guarded by Zod before the wrapper's try block reaches the service |
| client -> tree-B body (~14 routes) | Untrusted JSON now shape-guarded in-place; auth prologue on these routes is untouched (still session/admin-gated the same way) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | Tree-A schemas silently tightening acceptance vs. today | high | mitigate | Every tree-A schema is a `.passthrough()` shape guard with no `.min()`/`.enum()` duplicating a service ValidationError (Pitfall 3) — verified per-file in Task 1's acceptance criteria (budget empty-name test, holidays missing-date test) |
| T-05-09 | Tampering | rag-config/jira-config Number()-coercion behavior change | medium | mitigate | Per orchestrator resolution, both schemas are passthrough-shape-only (not `z.coerce.number()` that could reject non-numeric input) — today's `Number(x ?? default)` coercion in the route is left completely untouched |
| T-05-10 | Information Disclosure | Zod's default error message leaking internal schema shape | low | mitigate | Every tree-B swap uses a literal frozen string in the route's 400 body, never Zod's auto-generated `parsed.error.issues[0].message` unless that message IS the frozen string (demo-requests: all fields share one message by design) |
| T-05-11 | Denial of Service (n/a) | — | low | accept | No new external input surface; schemas only shape-guard existing body-accepting endpoints |

</threat_model>

<verification>
- Per-task: `node node_modules/vitest/vitest.mjs run <scoped path> --reporter=json --outputFile=vt.json`
- Task 3 (plan exit): full suite + `.parse(` absence grep + auth-deferred-schema absence check + tsc + eslint
</verification>

<success_criteria>
- 12 tree-A schema.ts files created and wired into withProjectAccess's opts.schema
- ~14 tree-B routes swapped from inline `if(!x)` to safeParse, same frozen 400 bodies
- No CAPEX/OPEX enum duplicate; no auth payload schemas created
- Full suite: 0 failed, 113 skipped (baseline held)
- All 6 Phase 5 requirements (ROUTE-01/02/05/06/07/12) marked complete in REQUIREMENTS.md
</success_criteria>

<output>
Create `.planning/phases/05-route-thinning-validation/05-03-SUMMARY.md` when done
</output>
