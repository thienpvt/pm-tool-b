---
phase: 05-route-thinning-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/http/with-auth.ts
  - lib/http/with-auth.test.ts
  - lib/http/with-project-access.ts
  - lib/http/with-project-access.test.ts
  - lib/http/with-program-access.ts
  - lib/http/with-program-access.test.ts
  - lib/services/access.ts
  - lib/services/access.unit.test.ts
  - app/api/projects/[id]/risks/route.ts
  - app/api/projects/[id]/risks/route.test.ts
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-07, ROUTE-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "lib/http/with-auth.ts resolves the session, returns 401 { error: 'Unauthorized' } on missing session, and passes { user, actor, params, body } into the wrapped handler."
    - "lib/http/with-project-access.ts asserts project ownership via assertProjectAccess before the handler runs and hands the authorized project row to the handler in ctx.project — a denied caller never reaches the handler."
    - "assertProjectAccess returns the ProjectAccessRow instead of void; wire behavior (401/403/404 bodies) is byte-identical to before the flip."
    - "app/api/projects/[id]/risks/route.ts contains zero getSessionFromRequest calls, zero actorOf definitions, and zero try/catch blocks — each handler is one service call wrapped by withProjectAccess."
    - "The UnknownColumnError -> 400-with-columns branch (T-04-25) survives the wrap: a rejected column on risks PUT still 400s naming the column, never 500/403."
    - "Full suite: 0 failed, skipped exactly 113 (baseline held)."
  artifacts:
    - path: "lib/http/with-auth.ts"
      provides: "withAuth(handler, opts?) HOF — session resolve, actor derivation, params await, optional body parse, unified catch tail"
    - path: "lib/http/with-project-access.ts"
      provides: "withProjectAccess(handler, opts?) — composes withAuth + assertProjectAccess, hands ctx.project"
    - path: "lib/http/with-program-access.ts"
      provides: "withProgramAccess(handler, opts?) — composes withAuth + assertProgramAccess, hands ctx.program (zero Phase 5 consumers, built per locked decision for Phase 6)"
    - path: "lib/services/access.ts"
      provides: "assertProjectAccess now returns Promise<ProjectAccessRow> instead of Promise<void>"
  key_links:
    - from: "app/api/projects/[id]/risks/route.ts"
      to: "lib/http/with-project-access.ts"
      via: "withProjectAccess(handler)"
      pattern: "GET/POST/PUT/DELETE each one line: NextResponse.json(await <service fn>(ctx.params.id, ctx.actor, ...))"
    - from: "lib/http/with-project-access.ts"
      to: "lib/services/access.ts"
      via: "assertProjectAccess(params.id, actor) -> ProjectAccessRow"
      pattern: "wrapper awaits the assert inside withAuth's try, so Forbidden/NotFound map to 403/404 through the existing catch tail"
---

<objective>
Build the `lib/http/` wrapper substrate that the rest of Phase 5 converts routes onto, and prove it end-to-end on ONE route (`risks`, the fullest of the 31 identical-shape routes — it carries the `UnknownColumnError` dual-mapper). This is the tracer: every subsequent plan in this phase is a mechanical repeat of the pattern proven here.

Purpose: delete the `actorOf + getSessionFromRequest + try/catch + mapError` boilerplate duplicated in 31 route files down to one wrapper, with the `assertProjectAccess` return-row flip as the enabler for handing the authorized project to handlers without a second query.
Output: `lib/http/with-auth.ts`, `lib/http/with-project-access.ts`, `lib/http/with-program-access.ts` (all with unit tests), `assertProjectAccess` flipped, `risks/route.ts` converted and its test updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-route-thinning-validation/05-CONTEXT.md
@.planning/phases/05-route-thinning-validation/05-RESEARCH.md
@.planning/phases/05-route-thinning-validation/05-PATTERNS.md
@.planning/phases/05-route-thinning-validation/05-VALIDATION.md

@lib/services/access.ts
@lib/api-errors.ts
@lib/auth.ts
@lib/repositories/projects.repo.ts
@lib/services/programs.service.ts
@app/api/projects/[id]/risks/route.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: withAuth + withProjectAccess + assertProjectAccess row-flip, proven on risks/route.ts</name>
  <files>lib/http/with-auth.ts, lib/http/with-auth.test.ts, lib/http/with-project-access.ts, lib/http/with-project-access.test.ts, lib/services/access.ts, lib/services/access.unit.test.ts, app/api/projects/[id]/risks/route.ts, app/api/projects/[id]/risks/route.test.ts</files>
  <read_first>
    lib/services/access.ts (assertProjectAccess:22-41, AccessActor:5-8 — the flip target);
    lib/repositories/projects.repo.ts (ProjectAccessRow:30-33, projectAccessRow:36-44);
    lib/services/programs.service.ts (assertProgramAccess:21-40 — the return-row idiom to mirror, note the admin branch queries too);
    lib/api-errors.ts (serviceErrorResponse:41-59, repoErrorResponse:21-27 — call these, never reimplement);
    lib/auth.ts (getSessionFromRequest:47-51, SessionUser:23-31);
    app/api/projects/[id]/risks/route.ts (current full source — the actorOf+mapError+try/catch shape to delete);
    app/api/projects/[id]/risks/route.test.ts (existing — vi.hoisted mock idiom to preserve);
    lib/services/access.unit.test.ts (the 2 assertions on lines 22-23 that MUST flip: `resolves.toBeUndefined()` and `projectAccessRow).not.toHaveBeenCalled()`)
  </read_first>
  <behavior>
    - with-auth.test.ts, Test 1: no session -> withAuth-wrapped handler returns 401 with body `{ error: 'Unauthorized' }`; the inner handler function is never called.
    - with-auth.test.ts, Test 2: valid session, no `opts.schema` -> inner handler receives `ctx.user` (the full SessionUser), `ctx.actor` (`{ company_id, is_admin }` derived, matching the `actorOf` shape byte-for-byte), `ctx.params` (awaited), and on POST/PUT/PATCH `ctx.body` (parsed JSON).
    - with-auth.test.ts, Test 3: POST/PUT/PATCH with malformed JSON body -> 400 `{ error: 'Invalid JSON' }` (the one sanctioned WR-05/HYG-02 behavior change — call it out in the commit message).
    - with-auth.test.ts, Test 4: inner handler throws `UnknownColumnError(['company_id'])` -> wrapper's catch returns `repoErrorResponse`'s 400 body `{ error: ..., columns: ['company_id'] }`, never 500 or 403 (T-04-25).
    - with-auth.test.ts, Test 5: inner handler throws `ForbiddenError`/`NotFoundError`/`ValidationError`/`ConflictError`/generic `Error` -> maps through `serviceErrorResponse` to 403/404/400/409/500 respectively; the generic-Error case returns `{ error: 'Internal server error' }`, never `String(e)`.
    - with-project-access.test.ts, Test 1: `assertProjectAccess` (mocked) resolves a `ProjectAccessRow` -> the inner handler is called with `ctx.project` equal to that row, plus `ctx.actor`/`ctx.params` from withAuth.
    - with-project-access.test.ts, Test 2: `assertProjectAccess` (mocked) rejects `ForbiddenError` -> 403 `{ error: 'Forbidden' }`, inner handler never called.
    - with-project-access.test.ts, Test 3: `assertProjectAccess` (mocked) rejects `NotFoundError` -> 404 `{ error: 'Not found' }`, inner handler never called.
    - access.unit.test.ts, flipped Test: `assertProjectAccess(1, admin)` resolves the fetched row (NOT `toBeUndefined()`), and `projectAccessRow` IS called (NOT `.not.toHaveBeenCalled()`) — the admin branch now fetches to have a row to return, matching `assertProgramAccess`'s shape. All other 6 existing assertions in the file (NotFoundError, owner via company_id, owner via customer_company_id, ForbiddenError cross-company, null-company allow, null-company deny) stay exactly as written — only the admin-bypass test's two assertions change.
    - risks/route.test.ts: the existing 401/403/404/200/201 matrix (mocked at `projectAccessRow` + `risks.repo` boundary) still passes unchanged in expectations — only the route's internal wiring changes, not its wire behavior. Add one assertion: the converted GET handler never calls `getSessionFromRequest` directly with old-style local wiring (it goes through the wrapper) — verify via the existing 401 test still returning `{ error: 'Unauthorized' }`.
  </behavior>
  <action>
    Write the RED tests first (with-auth.test.ts, with-project-access.test.ts do not exist yet — create them failing against not-yet-written wrappers; flip access.unit.test.ts's two admin-branch assertions to fail against the current void-returning `assertProjectAccess`).

    Then implement GREEN:

    In `lib/services/access.ts`: change `assertProjectAccess`'s return type from `Promise<void>` to `Promise<import('@/lib/repositories/projects.repo').ProjectAccessRow>`. The admin branch (`if (actor.is_admin) return;`) becomes a fetch-and-return: query `projectAccessRow(projectId)`, throw `NotFoundError('Not found', 'project')` if missing, else return the row — mirroring `assertProgramAccess`'s admin branch exactly (it queries even for admin). The owner branch and null-company branch each already have the row in scope (`row`); change their `return;` to `return row;`. Preserve the exact order comment (admin bypass / missing project / owner / null-company) but update the "no ownership query" phrase since admin now queries too.

    Create `lib/http/with-auth.ts` per the Pattern 1 source in 05-PATTERNS.md: exports `AccessActor`, `HandlerContext<TParams, TBody>`, `RouteHandler`, `WrapperOptions<TBody>` (optional `schema: z.ZodType<TBody>` + optional `badRequest`), and `withAuth(handler, opts?)`. The returned function: resolves session via `getSessionFromRequest` (401 on null), awaits `rawCtx.params`, derives `actor = { company_id: user.company_id, is_admin: user.is_admin }`, parses body via `opts.schema.safeParse` when present (400 on failure using `opts.badRequest` or `{ error: parsed.error.issues[0]?.message ?? 'Invalid body' }`) or plain `req.json()` for POST/PUT/PATCH without a schema, catches malformed JSON as 400 `{ error: 'Invalid JSON' }`, then calls `handler(req, { user, actor, params, body })` inside a try/catch whose catch checks `instanceof UnknownColumnError` first (-> `repoErrorResponse`) then falls through to `serviceErrorResponse`. Import `UnknownColumnError` from `@/lib/repositories/_helpers`, `repoErrorResponse`/`serviceErrorResponse` from `@/lib/api-errors`, `getSessionFromRequest`/`SessionUser` from `@/lib/auth`.

    Create `lib/http/with-project-access.ts` per Pattern 2: `withProjectAccess(handler, opts?)` wraps `withAuth` — inside the wrapped function, call `assertProjectAccess(ctx.params.id, ctx.actor)` (now returning the row), then call the inner `handler(req, { ...ctx, project })`. Type the params generic as `TParams extends { id: string }`.

    Then convert `app/api/projects/[id]/risks/route.ts` to the after-shape from Pattern 3 in 05-PATTERNS.md: delete the local `actorOf`, `mapError`, and the manual session/try-catch prologue in each of GET/POST/PUT/DELETE. Each handler becomes `export const GET = withProjectAccess(async (req, ctx) => NextResponse.json(await listRisks(ctx.params.id, ctx.actor)))` (and analogous for POST 201, PUT destructuring `id`/`rowId` out of `ctx.body`, DELETE reading `rowId` from `new URL(req.url).searchParams` as today — the wrapper does not parse DELETE's body since no schema is passed and DELETE isn't POST/PUT/PATCH).

    Update `risks/route.test.ts` only if the existing mock boundaries (`@/lib/auth` `getSessionFromRequest`, `@/lib/repositories/projects.repo` `projectAccessRow`, `@/lib/repositories/risks.repo`) still satisfy the new wrapper's import graph — they should, since the wrapper imports the same modules. Run the suite; fix only assertion drift caused by the row-flip (there should be none at the route-test level, since `serviceErrorResponse`/`repoErrorResponse` bodies are unchanged).
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run lib/http app/api/projects/risks lib/services/access --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>withAuth and withProjectAccess exist and pass their unit tests; assertProjectAccess returns the row with access.unit.test.ts updated in the same commit; risks/route.ts is fully converted with zero getSessionFromRequest/actorOf/try-catch remaining; T-04-25 UnknownColumnError->400 preserved.</done>
  <acceptance_criteria>
    - `lib/http/with-auth.ts` and `lib/http/with-project-access.ts` exist and export the types/functions named above
    - `assertProjectAccess` signature returns `Promise<ProjectAccessRow>`; `grep -c "Promise<void>" lib/services/access.ts` returns 0
    - `access.unit.test.ts`'s admin-bypass test asserts `projectAccessRow` WAS called and the result is NOT `toBeUndefined()`; the other 6 tests in that file are untouched and still pass
    - `grep -v '^//' app/api/projects/[id]/risks/route.ts | grep -cE "getSessionFromRequest|function actorOf|try \{"` returns 0
    - A rejected `company_id` on risks PUT still 400s with `columns: ['company_id']` (existing T-04-25 test coverage, or a new one if none exists)
    - `node node_modules/vitest/vitest.mjs run lib/http app/api/projects/risks lib/services/access --reporter=json --outputFile=vt.json` reports 0 failed; parse and `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: withProgramAccess substrate (zero Phase 5 consumers, built for Phase 6)</name>
  <files>lib/http/with-program-access.ts, lib/http/with-program-access.test.ts</files>
  <read_first>
    lib/services/programs.service.ts (assertProgramAccess:21-40 — already returns the row, no flip needed);
    lib/http/with-project-access.ts (the sibling just built in Task 1 — mirror its shape)
  </read_first>
  <behavior>
    - Test 1: `assertProgramAccess` (mocked) resolves a program row -> inner handler called with `ctx.program` equal to that row.
    - Test 2: `assertProgramAccess` (mocked) rejects `ForbiddenError` -> 403 `{ error: 'Forbidden' }`, inner handler never called.
    - Test 3: `assertProgramAccess` (mocked) rejects `NotFoundError` -> 404 `{ error: 'Not found' }`, inner handler never called.
    - Test 4: no session -> 401, `assertProgramAccess` never called (session check happens in the composed `withAuth` before the program assert runs).
  </behavior>
  <action>
    Create `lib/http/with-program-access.ts` as `withProjectAccess` with `assertProgramAccess` (from `@/lib/services/programs.service`) substituted for `assertProjectAccess`, and `ctx.program` substituted for `ctx.project`. No source changes needed to `programs.service.ts` — `assertProgramAccess` already returns the row (verified `programs.service.ts:21-40`). This wrapper has zero Phase 5 route consumers (Phase 6 converts `programs/[id]/**`); it still ships with full unit test coverage per the locked decision (CONTEXT: "Two wrappers, not a parameterized one").
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run lib/http/with-program-access --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>withProgramAccess exists, mirrors withProjectAccess's shape, and passes its unit tests (403/404/401 mapping, ctx.program handoff).</done>
  <acceptance_criteria>
    - `lib/http/with-program-access.ts` exports `withProgramAccess` with the same shape as `withProjectAccess`
    - `node node_modules/vitest/vitest.mjs run lib/http/with-program-access --reporter=json --outputFile=vt.json` reports 0 failed; `rm vt.json`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Boundary sweep — full suite, tsc, eslint, baseline confirm</name>
  <files>(no new files — verification only)</files>
  <read_first>
    .planning/phases/05-route-thinning-validation/05-VALIDATION.md (baseline: 573/460/0/113)
  </read_first>
  <action>
    Run the full test suite and confirm the baseline holds exactly (0 failed, numPendingTests exactly 113 — a rising or falling skip count hides a stopped suite, per 05-VALIDATION.md). Run `npx tsc --noEmit` and `npx eslint` on the changed files list from Tasks 1-2. Grep-confirm `app/api/projects/[id]/risks/route.ts` has zero `getSessionFromRequest`, zero `actorOf`, zero `try {` — the tracer's thinned-handler proof for ROUTE-05. Confirm no other route.ts file was touched (this plan's scope is the wrapper substrate + risks only; the remaining 17-file tree conversion is Plan 05-02/05-03).
  </action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run --reporter=json --outputFile=vt.json</automated>
  </verify>
  <done>Full suite green at the 573/460/0/113 baseline; tsc and eslint clean; only the 10 planned files touched.</done>
  <acceptance_criteria>
    - Full suite: 0 failed, numPendingTests exactly 113 (parse vt.json for `numFailedTests`/`numPendingTests`; `rm vt.json`)
    - `npx tsc --noEmit` exits 0
    - `npx eslint lib/http app/api/projects/[id]/risks lib/services/access.ts` clean
    - `git diff --stat` shows only the 10 files listed in this plan's frontmatter `files_modified`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> withAuth | Untrusted session cookie / request body crosses here; the wrapper is the single chokepoint for every route that adopts it |
| withProjectAccess -> assertProjectAccess | The tenant-ownership decision; a bug here reopens IDOR across every future consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `assertProjectAccess` return-row flip | high | mitigate | Admin branch now fetches the row before returning (mirrors `assertProgramAccess`); ownership-decision ORDER (admin bypass / NotFound / company_id-or-customer_company_id / null-company CR-01) is unchanged — only the return value gains a payload. Both `access.unit.test.ts` (8 assertions) and `risks` route tests re-verify the 401/403/404 wire contract is byte-identical. |
| T-05-02 | Tampering | `lib/http/with-auth.ts` catch tail | high | mitigate | The `UnknownColumnError` branch is checked BEFORE the generic `serviceErrorResponse` fallthrough, preserving T-04-25 (a rejected column stays 400-with-columns, never a 500 or 403 that would mask a mass-assignment attempt). Test 4 in Task 1 asserts this ordering directly. |
| T-05-03 | Information Disclosure | `withAuth`'s generic-error path | medium | mitigate | Unknown errors map through `serviceErrorResponse`'s existing `console.error` + generic `{ error: 'Internal server error' }` body — never `String(e)`, never a stack trace to the client. |
| T-05-04 | Denial of Service (n/a) | — | low | accept | No new external input surface introduced; the wrapper only re-houses existing session/body-parse logic. |

</threat_model>

<verification>
- `node node_modules/vitest/vitest.mjs run lib/http app/api/projects/risks lib/services/access --reporter=json --outputFile=vt.json` after Tasks 1-2, then the full suite after Task 3
- `npx tsc --noEmit` exits 0 after every task
- Manual read-through of `risks/route.ts` confirms the after-shape from 05-PATTERNS.md Pattern 3
</verification>

<success_criteria>
- `lib/http/with-auth.ts`, `lib/http/with-project-access.ts`, `lib/http/with-program-access.ts` exist, tested, and pass
- `assertProjectAccess` returns the row; `access.unit.test.ts` updated in the same commit (HYG-01)
- `risks/route.ts` is the reference conversion: one line per handler, no session/SQL/try-catch inline
- Full suite: 0 failed, 113 skipped (baseline held)
</success_criteria>

<output>
Create `.planning/phases/05-route-thinning-validation/05-01-SUMMARY.md` when done
</output>
