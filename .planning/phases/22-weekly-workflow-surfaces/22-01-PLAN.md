---
phase: 22-weekly-workflow-surfaces
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - modules/weekly/ui/shared/VirtualRows.tsx
  - modules/weekly/ui/shared/VirtualRows.component.test.tsx
  - modules/weekly/ui/shared/types.ts
  - modules/weekly/ui/shared/weekly.fixture.ts
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - modules/weekly/ui/periods/WeeklyPeriodList.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx
  - app/weekly/periods/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/Sidebar.weekly-nav.component.test.tsx
autonomous: true
requirements: [PERD-04, PERF-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO opens /weekly/periods via a thin app re-export of WeeklyPeriodsPage and sees title Weekly periods (PERD-04, D-01)."
    - "One GET /api/weekly-periods loads the period list; the page never invents a weekly endpoint (D-03, D-04)."
    - "Initial GET shows a centered spinner and Loading weekly periods… (UI-SPEC loading periods-page-shell)."
    - "401 shows Session expired — refresh the page and sign in again.; 403 shows You don't have access to this page.; 5xx shows Couldn't load this page. Try again. in a centered main panel with AlertTriangle (UI-SPEC error periods-page-shell, D-04)."
    - "Zero periods renders Copywriting empty heading No weekly periods yet and body Create the first period for your company using an ISO week above. inside the table body spanning all columns (UI-SPEC empty periods-list)."
    - "A typical list (<20 periods) renders a compact table of Week, ISO week, Due at, and Track submissions without row virtualization (UI-SPEC populated periods-list, D-04)."
    - "Subtitle uses period vs periods from the loaded count (UI-SPEC zero-one-many periods-list)."
    - "Period list wrapper uses overflow-x-auto (UI-SPEC overflow periods-list)."
    - "Track submissions links to /weekly/tracking?periodId={id} (D-10)."
    - "Sidebar shows Weekly periods (/weekly/periods) and Weekly tracking (/weekly/tracking) when roles includes cpmo; pm and viewer never receive those two links (D-02, UI-SPEC populated/error sidebar-nav-weekly-links)."
    - "NAV still renders from cached /api/auth/me while a weekly page body loads (UI-SPEC loading sidebar-nav-weekly-links)."
    - "VirtualRows windows a 150-item fixture at ROW_HEIGHT 40 so mounted row nodes stay at or below 30 (PERF-01, D-08, D-09, UI-SPEC populated virtual-rows-window)."
    - "No new npm dependency is added; VirtualRows is in-repo only (D-08)."
  artifacts:
    - path: "modules/weekly/ui/shared/VirtualRows.tsx"
      provides: "Fixed-height in-repo window (D-08, PERF-01)"
    - path: "modules/weekly/ui/shared/VirtualRows.component.test.tsx"
      provides: "150-row DOM bound proof (D-09)"
    - path: "modules/weekly/ui/shared/types.ts"
      provides: "Client-safe weekly payload types for periods, tracking, and editor (D-03)"
    - path: "modules/weekly/ui/shared/weekly.fixture.ts"
      provides: "Shared mocked payloads including 150 tracking rows"
    - path: "app/weekly/periods/page.tsx"
      provides: "Thin use-client re-export (D-01)"
    - path: "modules/weekly/ui/periods/WeeklyPeriodsPage.tsx"
      provides: "Periods shell, loading/error, list (PERD-04, D-04)"
    - path: "components/layout/Sidebar.tsx"
      provides: "CPMO Weekly periods and Weekly tracking links (D-02)"
  key_links:
    - from: "app/weekly/periods/page.tsx"
      to: "modules/weekly/ui/periods/WeeklyPeriodsPage.tsx"
      via: "default re-export"
      pattern: "modules/weekly/ui/periods/WeeklyPeriodsPage"
    - from: "useWeeklyPeriods.ts"
      to: "/api/weekly-periods"
      via: "fetch GET"
      pattern: "/api/weekly-periods"
    - from: "components/layout/Sidebar.tsx"
      to: "/weekly/periods"
      via: "Weekly periods Link when roles includes cpmo"
      pattern: "/weekly/periods"
    - from: "VirtualRows.component.test.tsx"
      to: "VirtualRows.tsx"
      via: "150-row window assertion"
      pattern: "virtual-row"
  prohibitions:
    - statement: "Must not add npm packages or change package.json (D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not replace the existing activity weekly-report App Router page or the project-report page (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add weekly API routes (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not put a PM report item on Sidebar; PMs use dashboard deep links (D-02)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Prove the weekly module + thin App Router re-export + in-repo VirtualRows + CPMO Sidebar links end-to-end, and ship the CPMO periods list/shell (PERD-04 list, PERF-01 primitive, D-01, D-02, D-03, D-04, D-08, D-09).

Purpose: Lock the Phase 21 module pattern and the PERF-01 window before create/config, tracking, export, and the PM editor expand from it.

Output: VirtualRows with D-09 test, shared types and fixtures, periods GET page + re-export, CPMO weekly NAV.
</objective>

## Artifacts this plan produces

- In-repo VirtualRows + 150-row window test (PERF-01, D-08, D-09)
- Client-safe weekly types and fixtures consumed by 22-02 through 22-05 without editing these two files
- Periods list/shell at /weekly/periods (D-01, D-04)
- Sidebar Weekly periods and Weekly tracking for cpmo (D-02)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-weekly-workflow-surfaces/22-CONTEXT.md
@.planning/phases/22-weekly-workflow-surfaces/22-RESEARCH.md
@.planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md
@.planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md
@.planning/phases/21-portfolio-pm-dashboard-pages/21-01-PLAN.md
@vitest.config.ts
@components/layout/Sidebar.tsx
@components/layout/Sidebar.dashboard-nav.component.test.tsx
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
@modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
@app/dashboards/portfolio/page.tsx
@app/api/weekly-periods/route.ts
@lib/repositories/weekly-periods.repo.ts

Planner-assigned locked IDs for this plan: D-01 module pages + thin re-export at /weekly/periods; D-02 both CPMO weekly NAV links; D-03 consume existing APIs only; D-04 periods list + 403 in-page; D-08 in-repo VirtualRows; D-09 150-row DOM proof.

Do not implement period create, config PUT, tracking grid, export, or the PM editor (22-02 through 22-05). Isolation none: this is the only plan that may edit Sidebar.tsx or create VirtualRows.tsx.

<interfaces>
From lib/repositories/weekly-periods.repo.ts WeeklyPeriodRow (GET /api/weekly-periods returns this array):
id, company_id, iso_week, start_date, end_date, due_at, display_name, config_snapshot, created_by, created_at
Ordered iso_week DESC.

From app/api/weekly-periods/config GET (used by 22-02; declare the type now):
{ due_weekday: number, due_time_utc: string } default { due_weekday: 5, due_time_utc: '18:00:00' }
due_weekday is 0-6 (Sun-Sat), not 1-7.

From lib/services/weekly-tracking.service.ts (declare types now; 22-03 consumes):
PeriodTrackingFilters: status?, lateness?, pm_user_id?, stage?, rag?, technology_council?
PeriodTrackingRow: project_id, report_id, name, project_code, stage, status, overdue, rag, first_lateness, latest_version, pm_user_id, pm_display_name, has_technology_council_issues
PeriodTrackingCounts: obligated, not_submitted, draft, submitted, overdue, late
GET tracking JSON: { period, counts, rows }

From weekly report GET shell (declare now; 22-05 consumes):
id, period_id, project_id, status, correction_open, highlights, completed_work, next_week_goals, nearest_milestone, nearest_milestone_id, raid_dependency, leadership_support, this_week_rag, prev_week_rag, iso_week, due_at, display_name
RAG Title Case: Green | Amber | Red | Not applicable

Copywriting (22-UI-SPEC, English): page title Weekly periods; loading Loading weekly periods…; 401 Session expired — refresh the page and sign in again.; 403 You don't have access to this page.; 5xx Couldn't load this page. Try again.; empty heading No weekly periods yet; empty body Create the first period for your company using an ISO week above.; row action Track submissions.

Page shell: flex flex-col lg:flex-row min-h-screen bg-slate-50, Sidebar, main flex-1 p-4 lg:p-6 lg:p-8 overflow-auto. Spinner w-8 h-8 border-2 border-blue-500 border-t-transparent rounded-full animate-spin. Headings text-base font-semibold. Labels/chips text-xs font-semibold. Body text-sm. Do not add extra font-weight utilities beyond font-semibold.

VirtualRows: ROW_HEIGHT 40, OVERSCAN 5, height 400 in the proof test. Export ROW_HEIGHT from the module for tests.

vitest.config.ts already includes modules/** (Phase 21). Do not edit it. Do not edit package.json.
</interfaces>
</context>

<feature>
  <name>Weekly periods list tracer with VirtualRows primitive and CPMO NAV</name>
  <files>modules/weekly/ui/shared/VirtualRows.tsx, modules/weekly/ui/shared/VirtualRows.component.test.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, app/weekly/periods/page.tsx, components/layout/Sidebar.tsx</files>
  <behavior>
    150 mocked rows in VirtualRows mount at most 30 row nodes (PERF-01, D-09).
    Mocked GET /api/weekly-periods 200 shows Weekly periods and list rows (PERD-04, D-04).
    cpmo Sidebar includes Weekly periods and Weekly tracking (D-02).
  </behavior>
  <implementation>
    In-repo fixed-height window; periods GET hook + page; thin app re-export; Sidebar links after My dashboard and before NAV_SECONDARY (D-01, D-02, D-08).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="22-01-01">
  <name>End-to-end weekly periods GET path plus VirtualRows window</name>
  <reversibility rating="costly">Module root modules/weekly/ui plus /weekly/periods and in-repo VirtualRows become the v2.1 weekly landing pattern (D-01, D-08).</reversibility>
  <files>modules/weekly/ui/shared/VirtualRows.tsx, modules/weekly/ui/shared/VirtualRows.component.test.tsx, modules/weekly/ui/shared/types.ts, modules/weekly/ui/shared/weekly.fixture.ts, modules/weekly/ui/periods/useWeeklyPeriods.ts, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx, app/weekly/periods/page.tsx</files>
  <read_first>
  - .planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md (Pattern 1 re-export, Pattern 2 hook, Pattern 3 VirtualRows)
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (periods zones, Copywriting, spacing, typography)
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
  - app/dashboards/portfolio/page.tsx
  - app/api/weekly-periods/route.ts
  - lib/repositories/weekly-periods.repo.ts
  </read_first>
  <behavior>
  - VirtualRows with 150 fixture items, container height 400, ROW_HEIGHT 40: elements with data-testid virtual-row number at most 30, not 150 (PERF-01, D-08, D-09, UI-SPEC virtual-rows-window).
  - Deferred fetch on WeeklyPeriodsPage: Loading weekly periods… is in the document before JSON arrives (UI-SPEC loading periods-page-shell).
  - GET 200: heading Weekly periods, table shows fixture display_name and iso_week, Track submissions href includes periodId of the row (PERD-04, D-04, D-10).
  - GET 403: You don't have access to this page. (UI-SPEC error periods-page-shell, D-04).
  </behavior>
  <action>
  TDD (workflow.tdd_mode). Isolation none. Merge RED and GREEN in this task. Do not create a RED-only task. Tests before implementation. Two commits: test(22-01) then feat(22-01).
  RED: Add modules/weekly/ui/shared/weekly.fixture.ts with: periodsFixture (two WeeklyPeriod-shaped rows, iso_week 2026-W36 then 2026-W35, ids 2 and 1), empty periods array helper, configFixture due_weekday 5 due_time_utc 18:00:00, trackingPayload { period, counts with zeros allowed, three mixed-status rows }, trackingRows150 (150 rows with distinct project_id 1..150, report_id, name, status submitted), reportShellFixture matching GET weekly report JSON including prev_week_rag Green and this_week_rag null. Add modules/weekly/ui/shared/types.ts as client-only types (do not import lib/services or lib/repositories — those pull pg). Export WeeklyPeriodListItem, WeeklyConfig, PeriodTrackingFilters, PeriodTrackingRow, PeriodTrackingCounts, PeriodTrackingPayload, WeeklyReportEditorShell, WeeklyRag (Title Case union). Add VirtualRows.component.test.tsx that renders VirtualRows with trackingRows150, height 400, rowHeight 40, renderRow writing a node data-testid virtual-row; assert getAllByTestId virtual-row length is less than or equal to 30 and trackingRows150.length is 150. Add WeeklyPeriodsPage.component.test.tsx: mock next/navigation usePathname to /weekly/periods; mock Sidebar as nav data-testid sidebar; stub fetch GET /api/weekly-periods with deferred Promise for loading, 200 JSON periodsFixture, and 403. Assert sidebar testid, title Weekly periods, loading copy, 403 copy, and Track submissions. Tests must fail because production modules are missing. Commit test(22-01): red virtual rows and periods list tracer.
  GREEN: Implement VirtualRows as a generic window: props items, height, rowHeight (default ROW_HEIGHT 40), overscan default 5, rowKey, renderRow. Track scrollTop on the overflow container; start is max(0, floor(scrollTop / rowHeight) - overscan); visible count is ceil(height / rowHeight) + overscan * 2; slice items; inner spacer height items.length * rowHeight; translateY start * rowHeight. Export ROW_HEIGHT. Create useWeeklyPeriods: useState data/loading/error; load GET /api/weekly-periods only (D-03); 401 unauthorized, 403 forbidden, other !ok load_failed, 200 sets the JSON array; do not assign window.location. Create WeeklyPeriodsPage: Phase 21 shell; loading spinner + Loading weekly periods…; error panel AlertTriangle + UI-SPEC strings (403 is this page not this dashboard); after load, h1 Weekly periods, subtitle N period or periods, compact Table via inline markup or a local list (extract in task 3). Track submissions is a Link to /weekly/tracking?periodId={id} (D-10). Create app/weekly/periods/page.tsx with use client and export { default } from the module page (D-01). Do not POST or PUT. Do not add packages. Do not edit the activity weekly-report page or the project-report page (D-01). Commit feat(22-01): virtual rows and periods list tracer.
  REFACTOR only if tests stay green.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/shared/VirtualRows.component.test.tsx modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</automated>
    <human-check>
      <test>Sign in as CPMO and open /weekly/periods</test>
      <expected>Sidebar present; title Weekly periods; loading then a period table or the empty copy; no full-page crash</expected>
      <why_human>Shell spacing and NAV density are visual</why_human>
    </human-check>
  </verify>
  <done>VirtualRows windows 150 rows to at most 30 DOM nodes; CPMO periods GET path renders through the module re-export (PERF-01, PERD-04, D-01, D-03, D-04, D-08, D-09).</done>
  <acceptance_criteria>
  - app/weekly/periods/page.tsx re-exports WeeklyPeriodsPage
  - useWeeklyPeriods fetch URL is /api/weekly-periods
  - VirtualRows.component.test.tsx asserts virtual-row count at most 30 for 150 items
  - npx vitest run --project jsdom modules/weekly/ui/shared/VirtualRows.component.test.tsx modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-01-02">
  <name>Sidebar Weekly periods and Weekly tracking links</name>
  <reversibility rating="costly">NAV hrefs /weekly/periods and /weekly/tracking are the published CPMO entry points (D-02).</reversibility>
  <files>components/layout/Sidebar.tsx, components/layout/Sidebar.weekly-nav.component.test.tsx</files>
  <read_first>
  - components/layout/Sidebar.tsx (My dashboard block then NAV_SECONDARY)
  - components/layout/Sidebar.dashboard-nav.component.test.tsx
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Routes and NAV table)
  - .planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md (Pattern 6)
  </read_first>
  <behavior>
  - me.roles includes cpmo: links Weekly periods to /weekly/periods and Weekly tracking to /weekly/tracking are in the document (D-02).
  - me.roles includes pm only: neither weekly CPMO link is present (UI-SPEC error sidebar-nav-weekly-links).
  - me.roles is viewer or missing: neither weekly CPMO link is present.
  - pathname /weekly/tracking applies existing active classes bg-blue-600 text-white on Weekly tracking (UI-SPEC populated sidebar-nav-weekly-links).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add components/layout/Sidebar.weekly-nav.component.test.tsx copying stubFetch from Sidebar.dashboard-nav.component.test.tsx. Cover cpmo sees both weekly links with those hrefs; pm-only and viewer see neither; pathname /weekly/tracking applies bg-blue-600 text-white on Weekly tracking. Tests fail until links exist. Commit test(22-01): red sidebar weekly periods and tracking nav.
  GREEN: Import CalendarDays and ListChecks from lucide-react. After the My dashboard block and before NAV_SECONDARY.map, when me?.roles?.includes('cpmo'), render Weekly periods (CalendarDays, href /weekly/periods) then Weekly tracking (ListChecks, href /weekly/tracking) using the same Link className and onNavClick as Spec dashboard (D-02). Do not add a PM report Sidebar item (D-02). Do not change Spec dashboard or My dashboard hrefs. Do not hide Admin Panel. English labels only. Commit feat(22-01): sidebar weekly periods and tracking links.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom components/layout/Sidebar.weekly-nav.component.test.tsx components/layout/Sidebar.dashboard-nav.component.test.tsx</automated>
    <human-check>
      <test>As cpmo, confirm weekly links sit after My dashboard; as pm, confirm they are absent</test>
      <expected>cpmo sees Weekly periods and Weekly tracking; pm still sees My dashboard without those two links</expected>
      <why_human>Role-gated NAV order is visual</why_human>
    </human-check>
  </verify>
  <done>CPMO sees Weekly periods and Weekly tracking; pm and viewer do not; existing dashboard NAV tests stay green (D-02).</done>
  <acceptance_criteria>
  - Sidebar.tsx contains href /weekly/periods and /weekly/tracking
  - Both weekly links are gated on roles includes cpmo
  - npx vitest run --project jsdom components/layout/Sidebar.weekly-nav.component.test.tsx components/layout/Sidebar.dashboard-nav.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-01-03">
  <name>Period list empty, overflow, and Track submissions density</name>
  <files>modules/weekly/ui/periods/WeeklyPeriodList.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx from 22-01-01
  - modules/dashboards/ui/portfolio/PortfolioProjectTable.tsx (TableHead h-8 px-2 text-xs, TableCell p-2 text-sm)
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (period list table, empty copy, overflow)
  </read_first>
  <behavior>
  - Zero periods: table headers remain; one body cell colSpan all columns; heading No weekly periods yet and the ISO-week empty body (UI-SPEC empty periods-list).
  - Two fixture periods: columns Week (display_name), ISO week, Due at (UTC formatted), Actions Track submissions (UI-SPEC populated periods-list).
  - Wrapper overflow-x-auto; long display_name truncates with title (UI-SPEC overflow/long-text periods-list).
  - Subtitle 1 period vs 2 periods (UI-SPEC zero-one-many periods-list).
  - GET 401 uses Session expired — refresh the page and sign in again. (UI-SPEC error periods-page-shell).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend WeeklyPeriodsPage.component.test.tsx for empty array, 401 copy, subtitle pluralization, overflow-x-auto on the list wrapper, and Track submissions href /weekly/tracking?periodId= for each row (D-10). Commit test(22-01): red period list empty overflow and 401.
  GREEN: Extract WeeklyPeriodList with compact shadcn Table matching Phase 21 density. Columns Week, ISO week, Due at, Actions. Empty state Copywriting strings in a spanning cell. Link Track submissions. Wire it from WeeklyPeriodsPage. Keep GET-only hook (D-03, D-04). Do not add create or config forms. Commit feat(22-01): period list empty overflow and 401.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</automated>
  </verify>
  <done>Period list covers empty, populated, overflow, and 401 copy; Track submissions uses periodId query (PERD-04, D-04, D-10).</done>
  <acceptance_criteria>
  - WeeklyPeriodList.tsx exists and is rendered by WeeklyPeriodsPage
  - Empty copy matches 22-UI-SPEC periods-list empty strings
  - npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser module UI → GET /api/weekly-periods | Session cookie; CPMO-only period list crosses here |
| Sidebar NAV → page URL | Hiding a link is UX only; API remains the authz truth (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | WeeklyPeriodsPage / Sidebar | high | mitigate | NAV hide is not policy; 401/403 handled in-page; rely on withCpmo (D-04, ASVS V4) |
| T-22-02 | Information Disclosure | WeeklyPeriodList | medium | mitigate | Consume GET /api/weekly-periods only; do not fetch other-company URLs (D-03) |
| T-22-03 | Tampering | VirtualRows | low | accept | Window math is client UX only; it does not change server row sets |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-08) |

ASVS L1: high client→API authz stays on withCpmo; this plan does not add a second policy engine.
</threat_model>

<verification>
- 22-01-01: VirtualRows window + periods GET tracer green
- 22-01-02: Sidebar weekly links green; dashboard NAV still green
- 22-01-03: empty/401/overflow list green
- Wave: npx vitest run --project jsdom modules/weekly/ui/shared/VirtualRows.component.test.tsx modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx components/layout/Sidebar.weekly-nav.component.test.tsx
- package.json and package-lock.json unchanged
</verification>

<success_criteria>
A CPMO can open /weekly/periods (module UI + thin re-export) and see the period list from GET /api/weekly-periods; Sidebar exposes Weekly periods and Weekly tracking by cpmo role; VirtualRows proves 150 mocked items do not mount 150 row nodes (PERD-04, PERF-01, D-01, D-02, D-03, D-04, D-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/22-weekly-workflow-surfaces/22-01-SUMMARY.md` when done
</output>
