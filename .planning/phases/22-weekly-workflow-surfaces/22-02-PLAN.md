---
phase: 22-weekly-workflow-surfaces
plan: 02
type: tdd
wave: 2
depends_on: [22-01]
files_modified:
  - modules/weekly/ui/periods/WeeklyConfigForm.tsx
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx
autonomous: true
requirements: [PERD-04]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Config card GET /api/weekly-periods/config pre-fills due weekday Select 0-6 Sun-Sat and due time UTC; Save schedule PUT that body (PERD-04, D-03, D-04, UI-SPEC populated periods-config-form)."
    - "Save schedule is disabled while PUT is in-flight (UI-SPEC loading periods-config-form)."
    - "Successful PUT toasts Schedule saved; failed PUT toasts Couldn't save schedule — try again. (UI-SPEC error periods-config-form)."
    - "Create period card POSTs { iso_week } to /api/weekly-periods; Create period is disabled while POST is in-flight (PERD-04, D-04, UI-SPEC loading periods-create-form)."
    - "201 toasts Period created and the new row appears in the list; 409 toasts Period already exists for this week (UI-SPEC error periods-create-form)."
    - "Viewer still sees the 403 page panel from 22-01; mutations are not a second client authz layer (D-04)."
    - "Primary buttons Create period and Save schedule use bg-blue-600 text-white hover:bg-blue-700; labels text-xs font-semibold (22-UI-SPEC color/typography)."
  artifacts:
    - path: "modules/weekly/ui/periods/WeeklyConfigForm.tsx"
      provides: "Due weekday and due time UTC form (D-04)"
    - path: "modules/weekly/ui/periods/useWeeklyPeriods.ts"
      provides: "GET config, PUT config, POST period (D-03, D-04)"
    - path: "modules/weekly/ui/periods/WeeklyPeriodsPage.tsx"
      provides: "Config card then create card then list (D-04)"
  key_links:
    - from: "useWeeklyPeriods.ts"
      to: "/api/weekly-periods/config"
      via: "GET load and PUT save"
      pattern: "/api/weekly-periods/config"
    - from: "useWeeklyPeriods.ts"
      to: "/api/weekly-periods"
      via: "POST { iso_week }"
      pattern: "iso_week"
  prohibitions:
    - statement: "Must not add weekly API routes (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not compute ISO week on the client beyond posting the typed YYYY-Wnn string (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages (D-08)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO can create a weekly period and save company weekly due schedule from /weekly/periods using the existing POST and config routes (PERD-04, D-03, D-04).

Purpose: Complete PERD-04 after the 22-01 list tracer.

Output: WeeklyConfigForm, create-period card, hook mutations, tests, Copywriting toasts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-weekly-workflow-surfaces/22-CONTEXT.md
@.planning/phases/22-weekly-workflow-surfaces/22-RESEARCH.md
@.planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md
@.planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md
@.planning/phases/22-weekly-workflow-surfaces/22-01-PLAN.md
@modules/weekly/ui/periods/useWeeklyPeriods.ts
@modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
@modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
@app/api/weekly-periods/schema.ts
@app/api/weekly-periods/config/schema.ts
@app/api/weekly-periods/config/route.ts

Planner-assigned locked IDs: D-03 consume existing APIs only; D-04 list already shipped — this plan is create + config + viewer 403 already in-page.

Isolation none: only this Wave 2 plan may edit periods hook/page files. Do not touch Sidebar.tsx, VirtualRows.tsx, tracking, or report modules.

<interfaces>
POST /api/weekly-periods body: { iso_week: string } matching /^\d{4}-W\d{2}$/. 201 period row; 409 duplicate week.
PUT /api/weekly-periods/config body: { due_weekday: 0-6, due_time_utc: HH:MM or HH:MM:SS }. 200 { ok: true }.
GET /api/weekly-periods/config: { due_weekday, due_time_utc }.
Copywriting: Create period, Save schedule, Period created, Period already exists for this week, Schedule saved, Couldn't save schedule — try again.
Layout: config Card then create Card then list. Config fields flex flex-wrap gap-4 items-end. Select labels Sunday=0 through Saturday=6.
Primary Button size sm className bg-blue-600 text-white hover:bg-blue-700.
</interfaces>
</context>

<feature>
  <name>Period create and company weekly config</name>
  <files>modules/weekly/ui/periods/WeeklyConfigForm.tsx, modules/weekly/ui/periods/useWeeklyPeriods.ts, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <behavior>
    PUT config persists weekday and time; POST iso_week appends a period; 409 shows conflict toast (PERD-04, D-04).
  </behavior>
  <implementation>
    Extend useWeeklyPeriods with parallel GET config on mount, saveConfig PUT, createPeriod POST; Card forms on WeeklyPeriodsPage (D-03, D-04).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="22-02-01">
  <name>Load and save company weekly config</name>
  <files>modules/weekly/ui/periods/WeeklyConfigForm.tsx, modules/weekly/ui/periods/useWeeklyPeriods.ts, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioFiltersBar.tsx
  - app/api/weekly-periods/config/schema.ts
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Company weekly config)
  </read_first>
  <behavior>
  - After mount, GET /api/weekly-periods and GET /api/weekly-periods/config both run; weekday Select and time Input show configFixture values (UI-SPEC populated periods-config-form).
  - Save schedule PUT { due_weekday, due_time_utc } then toast Schedule saved (D-04).
  - PUT !ok toasts Couldn't save schedule — try again. (UI-SPEC error periods-config-form).
  - Save schedule disabled while the PUT is in flight (UI-SPEC loading periods-config-form).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Extend WeeklyPeriodsPage.component.test.tsx stubFetch to answer GET /api/weekly-periods/config with configFixture. Assert data-testid weekly-config-form shows the loaded weekday and time. Click Save schedule and assert PUT URL /api/weekly-periods/config and JSON body due_weekday number 0-6 plus due_time_utc. Assert success toast Schedule saved and failure toast Couldn't save schedule — try again. Tests fail until the form and PUT exist. Commit test(22-02): red weekly config form save.
  GREEN: On hook mount, fetch GET config in parallel with GET periods (Promise.all or sequential; 401/403 on either sets the same in-page error keys as 22-01). Expose config, saveConfig, savingConfig. WeeklyConfigForm: Card size sm data-testid weekly-config-form, FieldRow labels text-xs font-semibold, Select Sunday through Saturday mapped to 0-6, time Input, Save schedule primary bg-blue-600. Place the config card above the list (D-04). Do not POST periods in this task. Do not add routes (D-03). Commit feat(22-02): weekly config form save.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</automated>
  </verify>
  <done>CPMO can load and PUT company weekly due schedule from the periods page (PERD-04, D-03, D-04).</done>
  <acceptance_criteria>
  - WeeklyConfigForm.tsx exists with data-testid weekly-config-form
  - Hook PUTs /api/weekly-periods/config
  - npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-02-02">
  <name>Create period POST and duplicate-week toast</name>
  <files>modules/weekly/ui/periods/useWeeklyPeriods.ts, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/periods/useWeeklyPeriods.ts
  - app/api/weekly-periods/schema.ts
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Create period)
  </read_first>
  <behavior>
  - Create period POSTs { iso_week } using the input value; 201 toasts Period created and the list includes the new display_name (PERD-04, D-04).
  - 409 toasts Period already exists for this week (UI-SPEC error periods-create-form).
  - Create period disabled while POST is in flight (UI-SPEC loading periods-create-form).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Add tests: type 2026-W40, click Create period, assert POST /api/weekly-periods body iso_week 2026-W40, 201 toast Period created, list refresh or optimistic row; 409 toast Period already exists for this week. Commit test(22-02): red create period post and conflict toast.
  GREEN: Expose createPeriod(isoWeek) that POST JSON { iso_week } (D-03). Client hint placeholder 2026-W05; server still validates YYYY-Wnn. 201: toast Period created and reload GET periods. 409: toast Period already exists for this week without treating it as page-shell error. Other errors: Couldn't load this page. Try again. or a mutation toast that does not redirect. Create period button primary bg-blue-600, disabled while in-flight. Card sits under config, above the list (D-04). Do not add ISO-week arithmetic beyond sending the typed string. Commit feat(22-02): create period post and conflict toast.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</automated>
  </verify>
  <done>CPMO can POST a period and sees duplicate-week 409 as the Copywriting toast (PERD-04, D-04).</done>
  <acceptance_criteria>
  - createPeriod POSTs /api/weekly-periods with iso_week
  - 409 maps to Period already exists for this week
  - npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-02-03">
  <name>Periods page zone order and in-flight buttons</name>
  <files>modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Periods page zones)
  </read_first>
  <behavior>
  - Visible order is header, config card, create card, period list (22-UI-SPEC periods zones).
  - Both primary CTAs stay disabled for the duration of their in-flight fetch (UI-SPEC loading periods-config-form and periods-create-form).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Assert DOM order via test ids weekly-config-form then the create card (data-testid weekly-create-form) then weekly-period-list. Hold the PUT/POST promises unresolved and assert both primary buttons disabled. Commit test(22-02): red periods zone order and in-flight disable.
  GREEN: Add data-testid weekly-create-form on the create Card. Keep header title Weekly periods. Do not virtualize this list (typically under 100 rows). Do not touch tracking or Sidebar. Commit feat(22-02): periods zone order and in-flight disable.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</automated>
    <human-check>
      <test>As CPMO on /weekly/periods, save schedule and create a unique ISO week</test>
      <expected>Toasts match Copywriting; new row appears; buttons disable while requests run</expected>
      <why_human>Toast timing and button disable are interaction checks</why_human>
    </human-check>
  </verify>
  <done>Periods page matches UI-SPEC zone order with in-flight primary buttons (PERD-04, D-04).</done>
  <acceptance_criteria>
  - weekly-config-form, weekly-create-form, and weekly-period-list appear in that order
  - npx vitest run --project jsdom modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/weekly-periods | CPMO session; iso_week string crosses here |
| Browser → PUT /api/weekly-periods/config | CPMO session; weekday/time cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Elevation of Privilege | createPeriod / saveConfig | high | mitigate | Mutations go to withCpmo routes only; 403 stays in-page (D-03, D-04, ASVS V4) |
| T-22-05 | Tampering | iso_week Input | medium | mitigate | Send allowlisted iso_week string; server regex is source of truth (D-03) |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install in this plan (D-08) |

ASVS L1: authz remains on withCpmo.
</threat_model>

<verification>
- Config GET/PUT and create POST tests green
- package.json unchanged
</verification>

<success_criteria>
CPMO can create weekly periods and save the company due schedule from /weekly/periods using existing APIs (PERD-04, D-03, D-04).
</success_criteria>

<output>
Create `.planning/phases/22-weekly-workflow-surfaces/22-02-SUMMARY.md` when done
</output>
