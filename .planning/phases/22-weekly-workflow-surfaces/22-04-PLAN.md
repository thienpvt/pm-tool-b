---
phase: 22-weekly-workflow-surfaces
plan: 04
type: tdd
wave: 3
depends_on: [22-03]
files_modified:
  - modules/weekly/ui/tracking/ExportToolbar.tsx
  - modules/weekly/ui/tracking/usePeriodTracking.ts
  - modules/weekly/ui/tracking/WeeklyTrackingPage.tsx
  - modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx
autonomous: true
requirements: [CPMO-05]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Export pack POSTs /api/weekly-periods/{periodId}/export with body { project_ids, format } where project_ids is checkbox selection order (CPMO-05, D-07, D-12)."
    - "Format Select is xlsx default, docx, pptx; there is no preview surface and no call to the preview route (D-11)."
    - "200 blob is passed to downloadBlob from modules/dashboards/ui/shared/downloadBlob.ts using Content-Disposition filename or weekly-export.xlsx (D-07)."
    - "Success toast Export downloaded; failure toast Export failed — try again.; buttons re-enable after failure (UI-SPEC populated/error tracking-export-actions)."
    - "Export pack disabled when selection is empty or POST in-flight; empty hint Select at least one project to export. (UI-SPEC empty/zero-one-many/loading tracking-export-actions)."
    - "Export pack primary uses bg-blue-600 text-white hover:bg-blue-700; in-flight label Exporting… (22-UI-SPEC)."
  artifacts:
    - path: "modules/weekly/ui/tracking/ExportToolbar.tsx"
      provides: "Format Select + Export pack (D-07, D-11)"
    - path: "modules/weekly/ui/tracking/usePeriodTracking.ts"
      provides: "exportPack POST + downloadBlob (D-07)"
  key_links:
    - from: "usePeriodTracking.ts"
      to: "/api/weekly-periods/"
      via: "POST export JSON project_ids format"
      pattern: "/export"
    - from: "usePeriodTracking.ts"
      to: "modules/dashboards/ui/shared/downloadBlob.ts"
      via: "import downloadBlob"
      pattern: "downloadBlob"
  prohibitions:
    - statement: "Must not build an export preview UI or fetch the preview route (D-11)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not generate xlsx/docx/pptx in the browser (D-07)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages (D-08)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not copy downloadBlob into weekly/shared unless the dashboards import is impossible (D-07)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
CPMO exports a consolidated weekly pack from the tracking toolbar using existing POST export and downloadBlob, with project_ids in checkbox order and no preview UI (CPMO-05, D-07, D-11, D-12).

Purpose: Complete CPMO-05 after the 22-03 grid.

Output: ExportToolbar, exportPack on the tracking hook, tests with mocked blob and downloadBlob.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-weekly-workflow-surfaces/22-CONTEXT.md
@.planning/phases/22-weekly-workflow-surfaces/22-RESEARCH.md
@.planning/phases/22-weekly-workflow-surfaces/22-PATTERNS.md
@.planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md
@modules/weekly/ui/tracking/usePeriodTracking.ts
@modules/weekly/ui/tracking/WeeklyTrackingPage.tsx
@modules/weekly/ui/tracking/TrackingGrid.tsx
@modules/dashboards/ui/shared/downloadBlob.ts
@app/api/weekly-periods/[periodId]/export/schema.ts
@app/api/weekly-periods/[periodId]/export/route.ts

Planner-assigned locked IDs: D-07 POST export + downloadBlob; D-11 toolbar only, no preview; D-12 project_ids = checkbox order.

Isolation none: this is the only Wave 3 plan; it owns tracking hook/page test files after 22-03. Do not edit Sidebar, VirtualRows, periods, or report modules.

<interfaces>
POST /api/weekly-periods/{periodId}/export JSON { project_ids: number[] unique min 1, format: xlsx|docx|pptx }
200 binary; Content-Disposition attachment; filename="..."
400 may include { error, fields: string[] } for ineligible project ids — toast Export failed — try again. and keep buttons enabled after.
downloadBlob(blob, filename) from @/modules/dashboards/ui/shared/downloadBlob
Copywriting: Export pack, Exporting…, Export downloaded, Export failed — try again., Select at least one project to export.
</interfaces>
</context>

<feature>
  <name>Tracking export pack toolbar</name>
  <files>modules/weekly/ui/tracking/ExportToolbar.tsx, modules/weekly/ui/tracking/usePeriodTracking.ts, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</files>
  <behavior>
    Export pack POSTs selected project_ids in check order and calls downloadBlob on 200 (CPMO-05, D-07, D-12).
  </behavior>
  <implementation>
    exportPack on the hook; toolbar format Select + primary button; mock downloadBlob in tests (D-07, D-11).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="22-04-01">
  <name>Export pack POST, blob download, and empty selection</name>
  <precondition>TrackingGrid selection state from plan 22-03 is available on WeeklyTrackingPage</precondition>
  <files>modules/weekly/ui/tracking/ExportToolbar.tsx, modules/weekly/ui/tracking/usePeriodTracking.ts, modules/weekly/ui/tracking/WeeklyTrackingPage.tsx, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</files>
  <read_first>
  - modules/weekly/ui/tracking/usePeriodTracking.ts
  - modules/weekly/ui/tracking/WeeklyTrackingPage.tsx
  - modules/dashboards/ui/portfolio/usePortfolioSpecDashboard.ts (export blob + Content-Disposition)
  - modules/dashboards/ui/shared/downloadBlob.ts
  - app/api/weekly-periods/[periodId]/export/schema.ts
  - .planning/phases/22-weekly-workflow-surfaces/22-UI-SPEC.md (Export pack)
  </read_first>
  <behavior>
  - Zero selected: Export pack disabled and Select at least one project to export. is visible (UI-SPEC empty tracking-export-actions).
  - After selecting two submitted rows in order A then B, Export pack POST body project_ids equals [A, B] and format xlsx by default (D-12, D-07).
  - 200: downloadBlob called with the response blob and filename from Content-Disposition; toast Export downloaded (D-07, UI-SPEC populated tracking-export-actions).
  - !ok: toast Export failed — try again.; downloadBlob not called; button re-enabled (UI-SPEC error tracking-export-actions).
  - While POST in flight: Export pack disabled and label Exporting… (UI-SPEC loading tracking-export-actions).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Mock downloadBlob from @/modules/dashboards/ui/shared/downloadBlob. Extend WeeklyTrackingPage.component.test.tsx: empty selection disables Export pack; select two submitted rows then click Export pack; assert fetch POST /api/weekly-periods/{id}/export JSON project_ids in click order and format xlsx; 200 with Content-Disposition filename weekly-pack.xlsx calls downloadBlob and success toast; 400 JSON error calls failure toast and not downloadBlob. Commit test(22-04): red export pack post and blob download.
  GREEN: Add exportPack(projectIds, format) on usePeriodTracking: POST Content-Type application/json, body { project_ids: projectIds, format }, exporting flag (D-07). Parse Content-Disposition filename= quoted; fallback weekly-export.xlsx. Import downloadBlob from dashboards shared (D-07). ExportToolbar: format Select xlsx/docx/pptx, primary Export pack bg-blue-600, disabled when selectedIds.length === 0 or exporting. Place toolbar in the header right as UI-SPEC tracking zone 1 and keep the same control in the export bar zone 5 if that would duplicate — one ExportToolbar instance bound to header actions is enough as long as format Select and Export pack are visible together (D-11). Do not fetch a preview URL (D-11). Do not import exceljs/docx/pptxgenjs in the client. Commit feat(22-04): export pack post and blob download.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</automated>
  </verify>
  <done>Export pack downloads via downloadBlob using checkbox-ordered project_ids (CPMO-05, D-07, D-11, D-12).</done>
  <acceptance_criteria>
  - usePeriodTracking imports downloadBlob from modules/dashboards/ui/shared/downloadBlob
  - POST body includes project_ids and format
  - npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="22-04-02">
  <name>Format select and unique project_ids</name>
  <files>modules/weekly/ui/tracking/ExportToolbar.tsx, modules/weekly/ui/tracking/usePeriodTracking.ts, modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</files>
  <read_first>
  - app/api/weekly-periods/[periodId]/export/schema.ts (unique project_ids, format enum)
  - modules/weekly/ui/tracking/ExportToolbar.tsx from 22-04-01
  </read_first>
  <behavior>
  - Choosing docx then Export pack POSTs format docx; pptx likewise (D-07).
  - project_ids sent to POST are unique and match current selection order (export schema unique refine, D-12).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Tests before implementation. Two commits.
  RED: Change format Select to docx and assert POST format docx. Assert project_ids length equals Set size. Commit test(22-04): red export format select and unique ids.
  GREEN: Wire format into exportPack. Deduplicate project_ids by first-seen order if a header-checkbox toggle could double-push. Keep xlsx as the default Select value. Still no preview UI (D-11). Commit feat(22-04): export format select and unique ids.
  </action>
  <verify>
    <automated>npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx</automated>
    <human-check>
      <test>As CPMO, select one submitted project and export xlsx</test>
      <expected>Browser download starts; success toast; no preview panel</expected>
      <why_human>Blob download behavior is browser-specific</why_human>
    </human-check>
  </verify>
  <done>xlsx/docx/pptx format Select posts unique project_ids without a preview UI (CPMO-05, D-07, D-11, D-12).</done>
  <acceptance_criteria>
  - Format Select includes xlsx, docx, pptx
  - npx vitest run --project jsdom modules/weekly/ui/tracking/WeeklyTrackingPage.component.test.tsx exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST export | CPMO session; project_ids and format cross here; binary returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-08 | Information Disclosure | exportPack | high | mitigate | POST existing withCpmo export only; do not fetch other period ids the Select did not load (D-03, D-07, ASVS V4) |
| T-22-09 | Tampering | project_ids body | medium | mitigate | Send unique ids from current filtered selection; server assertExportEligible remains source of truth |
| T-22-10 | Denial of Service | Export pack | low | accept | exporting flag disables repeat clicks; server still enforces pack generation |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install (D-08) |

ASVS L1: export stays on withCpmo; UI does not generate Office files.
</threat_model>

<verification>
- Export POST + downloadBlob tests green
- No preview route string in tracking UI source
- package.json unchanged
</verification>

<success_criteria>
CPMO can export a consolidated pack from /weekly/tracking with checkbox-ordered project_ids and downloadBlob, without a preview UI (CPMO-05, D-07, D-11, D-12).
</success_criteria>

<output>
Create `.planning/phases/22-weekly-workflow-surfaces/22-04-SUMMARY.md` when done
</output>
