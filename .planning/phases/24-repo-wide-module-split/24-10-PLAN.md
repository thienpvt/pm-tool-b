---
phase: 24-repo-wide-module-split
plan: 10
type: tdd
wave: 10
depends_on: [24-09]
files_modified:
  - modules/operations/backend/operations-module-split.test.ts
  - modules/operations/ui/OperationsListPage.tsx
  - modules/operations/ui/OperationsDetailPage.tsx
  - app/operations/page.tsx
  - app/operations/[id]/page.tsx
  - modules/operations/backend/services/operations.service.ts
  - modules/operations/backend/repositories/operations.repo.ts
  - modules/operations/backend/routes/operations/systems/route.ts
  - app/api/operations/systems/route.ts
  - app/api/operations/systems/[id]/route.ts
  - app/api/operations/systems/[id]/budget-items/route.ts
  - app/api/operations/systems/[id]/expenses/route.ts
  - app/api/operations/systems/[id]/incidents/route.ts
autonomous: true
requirements: [MOD-01, MOD-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "modules/operations/{backend,ui}/ exist; /operations and /operations/[id] still resolve via P1 shells (MOD-01, MOD-02, D-01, D-02, D-06, D-10)."
    - "Every moved operations route handler keeps getSessionFromRequest and does not import @/lib/http/with-role (D-07)."
    - "eslint/route-wrapper-allowlist.json still lists the same app/api/operations/** paths (D-08)."
    - "All ten feature modules now have backend/ and ui/ (MOD-01)."
    - "No new npm dependency is added (D-08)."
  artifacts:
    - path: "modules/operations/ui/OperationsListPage.tsx"
      provides: "Moved operations list (P5)"
    - path: "app/operations/page.tsx"
      provides: "P1 shell (D-02)"
    - path: "modules/operations/backend/routes/operations/systems/route.ts"
      provides: "D-23 session+tenant handlers (D-07, P4)"
    - path: "app/api/operations/systems/route.ts"
      provides: "P4 named re-export; allowlist path unchanged"
  key_links:
    - from: "app/operations/page.tsx"
      to: "modules/operations/ui/OperationsListPage"
      via: "default re-export"
      pattern: "modules/operations/ui/OperationsListPage"
    - from: "app/api/operations/systems/route.ts"
      to: "modules/operations/backend/routes/operations/systems/route.ts"
      via: "P4 named re-export"
      pattern: "modules/operations/backend/routes/operations/systems/route"
  prohibitions:
    - statement: "Must not wrap operations routes with the CPMO role helper (D-07)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit eslint/route-wrapper-allowlist.json"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit Sidebar.tsx or vitest.config.ts"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not introduce a second DB pool or ORM"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Move operations UI and /api/operations/** last, with extra D-07 verification that no with-role import lands on ops handlers (MOD-01, MOD-02, D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-10).

Purpose: Last wave so ops auth cannot be "cleaned up" to CPMO wrappers while copying other features.

Output: operations ui/ plus backend/; P1 shells; P4 re-exports; allowlist paths unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-repo-wide-module-split/24-CONTEXT.md
@.planning/phases/24-repo-wide-module-split/24-RESEARCH.md
@.planning/phases/24-repo-wide-module-split/24-PATTERNS.md
@app/operations/page.tsx
@app/operations/[id]/page.tsx
@app/api/operations/systems/route.ts
@eslint/route-wrapper-allowlist.json

Isolation none. Do not edit Sidebar.tsx, vitest.config.ts, or the allowlist JSON. Execute RESEARCH Wave 10 map. Extra verify: no operations module route file imports @/lib/http/with-role (D-07). Phase 20 D-23 semantics stay.

<interfaces>
P1: OperationsListPage and OperationsDetailPage shells.
P4: git mv each operations route including getSessionFromRequest; app/api shell named-exports GET/POST/etc. Do not add with-role imports.
</interfaces>
</context>

<feature>
  <name>Operations list P1 plus systems P4 (D-07)</name>
  <files>modules/operations/ui/OperationsListPage.tsx, app/operations/page.tsx, modules/operations/backend/routes/operations/systems/route.ts, app/api/operations/systems/route.ts</files>
  <behavior>
    /operations re-exports OperationsListPage (D-02).
    Systems module route uses getSessionFromRequest and does not import with-role (D-07).
  </behavior>
  <implementation>
    git mv pages and systems routes; write P1 and P4 shells.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="24-10-01">
  <name>Move operations pages and systems collection route (D-07)</name>
  <files>modules/operations/backend/operations-module-split.test.ts, modules/operations/ui/OperationsListPage.tsx, modules/operations/ui/OperationsDetailPage.tsx, app/operations/page.tsx, app/operations/[id]/page.tsx, modules/operations/backend/services/operations.service.ts, modules/operations/backend/services/operations.service.unit.test.ts, modules/operations/backend/repositories/operations.repo.ts, modules/operations/backend/repositories/operations.repo.test.ts, modules/operations/backend/routes/operations/systems/route.ts, app/api/operations/systems/route.ts</files>
  <read_first>
  - app/operations/page.tsx
  - app/operations/[id]/page.tsx
  - app/api/operations/systems/route.ts
  - .planning/phases/24-repo-wide-module-split/24-PATTERNS.md (P1, P4)
  - eslint/route-wrapper-allowlist.json
  </read_first>
  <behavior>
  - app/operations/page.tsx re-exports OperationsListPage; app/operations/[id]/page.tsx re-exports OperationsDetailPage (D-02, D-06).
  - modules/operations/backend/routes/operations/systems/route.ts contains getSessionFromRequest and does not contain @/lib/http/with-role (D-07).
  - app/api/operations/systems/route.ts is a P4 named re-export (D-02).
  - allowlist JSON still lists app/api/operations/systems/route.ts.
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(24-10) then feat(24-10). Do not edit the allowlist file, Sidebar.tsx, vitest.config.ts, or package.json (D-08).
  RED: Add operations-module-split.test.ts for both P1 shells, systems module D-07 assertions, P4 re-export, allowlist path present, and that modules/dashboards, audit, weekly, documents, portfolio, projects, reports, jira, and admin each still have a backend directory (MOD-01 closeout). Commit test(24-10): red operations tracer D-07.
  GREEN: git mv both operations pages to the RESEARCH names. Write P1 shells. git mv operations.service* and operations.repo*. git mv systems/route.ts plus schema/tests. Write P4 shell. Copy session auth verbatim (D-03, D-07). Do not restyle (D-10). Keep Sidebar import from components if the list page uses it (D-05). Commit feat(24-10): operations P1 and systems P4.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/operations/backend/operations-module-split.test.ts</automated>
    <human-check>
      <test>Open /operations and /operations/:id</test>
      <expected>Same pages at the same URLs (D-10)</expected>
      <why_human>Ops chrome is visual</why_human>
    </human-check>
  </verify>
  <done>/operations URLs are P1 shells; systems collection keeps session+tenant auth (D-07, MOD-02).</done>
  <acceptance_criteria>
  - app/operations/page.tsx re-exports OperationsListPage
  - systems module route does not import @/lib/http/with-role
  - operations-module-split.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="24-10-02">
  <name>Move remaining operations nested routes with D-07 sweep</name>
  <files>modules/operations/backend/routes/operations/systems/[id]/route.ts, app/api/operations/systems/[id]/route.ts, app/api/operations/systems/[id]/budget-items/route.ts, app/api/operations/systems/[id]/budget-items/[itemId]/route.ts, app/api/operations/systems/[id]/expenses/route.ts, app/api/operations/systems/[id]/expenses/[expId]/route.ts, app/api/operations/systems/[id]/incidents/route.ts, app/api/operations/systems/[id]/incidents/[incId]/route.ts, modules/operations/backend/operations-module-split.test.ts</files>
  <read_first>
  - .planning/phases/24-repo-wide-module-split/24-RESEARCH.md (Wave 10 routes)
  - eslint/route-wrapper-allowlist.json
  </read_first>
  <behavior>
  - Every remaining app/api/operations/**/route.ts is a P4 named re-export of a module file that contains getSessionFromRequest and does not import @/lib/http/with-role (D-07, D-02).
  - Allowlist JSON still lists every operations path it listed before this phase.
  - app/api/admin/companies module file from Wave 9 still has no with-role import (D-07).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits.
  RED: Extend operations-module-split.test.ts: for each remaining RESEARCH operations route, P4 re-export plus D-07 source assertions on the module file; re-read modules/admin/backend/routes/admin/companies/route.ts for the same with-role absence; assert eslint/route-wrapper-allowlist.json parsed JSON equals the current committed file (health, companies, and the eight operations nested paths). Do not assert an allowlist count of 9. Commit test(24-10): red remaining operations P4 D-07.
  GREEN: git mv remaining nested operations routes with tests and schemas. Write P4 shells. Do not add with-role imports (D-07). Fix @/ imports only (D-03). Confirm git diff on eslint/route-wrapper-allowlist.json is empty. Commit feat(24-10): remaining operations P4 routes.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/operations/backend/operations-module-split.test.ts modules/admin/backend/admin-module-split.test.ts</automated>
  </verify>
  <done>All operations APIs are P4 shells over session+tenant handlers; D-07 holds for ops and admin companies (D-07, MOD-01, MOD-02).</done>
  <acceptance_criteria>
  - Every modules/operations/backend/routes/**/route.ts file omits @/lib/http/with-role
  - allowlist JSON is unchanged
  - npx vitest run --project node modules/operations/backend/operations-module-split.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/operations/systems/** | Session+tenant; allowlisted; must not become CPMO-wrapped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-14 | Elevation of Privilege | app/api/operations/**/route.ts | critical | mitigate | P4 move of getSessionFromRequest blocks; contract tests forbid with-role import (D-07). Extra verify vs admin companies. ASVS L1 V4. |
| T-24-SC | Tampering | npm installs | high | accept | No package-manager install (D-08). |
</threat_model>

<verification>
- 24-10-01 operations tracer D-07 green
- 24-10-02 nested P4 plus admin companies regression green
- Wave: npx vitest run modules/operations
- git diff -- eslint/route-wrapper-allowlist.json empty
</verification>

<success_criteria>
Operations module has backend and UI; ops and admin companies keep session+tenant / requireAdmin; all ten feature areas have backend/ and ui/ (MOD-01, MOD-02, D-07).
</success_criteria>

<output>
Create `.planning/phases/24-repo-wide-module-split/24-10-SUMMARY.md` when done
</output>
