---
phase: 24-repo-wide-module-split
plan: 08
type: tdd
wave: 8
depends_on: [24-07]
files_modified:
  - modules/jira/backend/jira-module-split.test.ts
  - modules/jira/ui/JiraSyncDialog.tsx
  - modules/jira/ui/BugImportDialog.tsx
  - modules/jira/ui/timeline-import
  - modules/jira/backend/services/import-mapping.service.ts
  - modules/jira/backend/services/jira-mapping.service.ts
  - modules/jira/backend/repositories/import-mapping.repo.ts
  - modules/jira/backend/routes/jira/search/route.ts
  - modules/jira/backend/routes/import/resource-plan/[id]/handlers.ts
  - app/api/jira/search/route.ts
  - app/api/import/resource-plan/[id]/route.ts
  - app/api/parse-file-headers/route.ts
  - modules/projects/ui/timeline
  - modules/projects/ui/bugs/ProjectBugsPage.tsx
autonomous: true
requirements: [MOD-01, MOD-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "modules/jira/{backend,ui}/ exist (MOD-01, D-01, D-06)."
    - "/api/jira/*, /api/import-mapping*, /api/bug-import-mapping*, /api/parse-file-headers still resolve via P2 re-exports (MOD-02, D-02)."
    - "app/api/import/resource-plan/[id]/route.ts still calls withProjectAccess locally (P3, ENF-01)."
    - "No dedicated /jira page is invented; import dialogs move and existing project/admin screens keep working (D-06, D-10)."
    - "No new npm dependency is added (D-08)."
  artifacts:
    - path: "modules/jira/ui/JiraSyncDialog.tsx"
      provides: "Moved Jira sync dialog (P5)"
    - path: "modules/jira/backend/services/import-mapping.service.ts"
      provides: "Moved import-mapping service"
    - path: "app/api/import/resource-plan/[id]/route.ts"
      provides: "P3 wrapper-stays shell"
  key_links:
    - from: "app/api/jira/search/route.ts"
      to: "modules/jira/backend/routes/jira/search/route.ts"
      via: "P2 named re-export"
      pattern: "modules/jira/backend/routes/jira/search/route"
    - from: "app/api/import/resource-plan/[id]/route.ts"
      to: "modules/jira/backend/routes/import/resource-plan/[id]/handlers.ts"
      via: "withProjectAccess(handler)"
      pattern: "withProjectAccess"
  prohibitions:
    - statement: "Must not add a new /jira page route"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not edit Sidebar.tsx, vitest.config.ts, or eslint allowlist"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages (D-08)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Move Jira/import dialogs and APIs into modules/jira. P2 for non-scoped import APIs; P3 for import/resource-plan/[id] (MOD-01, MOD-02, D-01, D-02, D-03, D-05, D-06, D-08, D-09, D-10, ENF-01).

Purpose: Import surfaces are a feature module even without a /jira page.

Output: jira ui/ plus backend/; updated imports on projects timeline/bugs (already in modules/projects/ui).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-repo-wide-module-split/24-RESEARCH.md
@.planning/phases/24-repo-wide-module-split/24-PATTERNS.md
@components/jira/JiraSyncDialog.tsx
@components/bugs/BugImportDialog.tsx
@components/timeline/ImportMappingDialog.tsx
@app/api/jira/search/route.ts
@app/api/import/resource-plan/[id]/route.ts
@app/api/parse-file-headers/route.ts

Isolation none. Do not edit Sidebar.tsx. Also git mv import-mapping.service.unit.test.ts and jira-mapping.service.unit.test.ts (D-09). After UI moves, update imports in modules/projects/ui/timeline, modules/projects/ui/bugs, modules/projects/ui/milestones, and modules/admin only if admin page already moved (it has not — Wave 9). Until Wave 9, update app/admin/page.tsx import paths for JiraSyncDialog.

<interfaces>
P2: jira/*, import-mapping*, bug-import-mapping*, parse-file-headers.
P3: import/resource-plan/[id].
No new app/jira/page.tsx.
</interfaces>
</context>

<feature>
  <name>Jira search API P2 plus import dialogs in modules/jira/ui</name>
  <files>modules/jira/ui/JiraSyncDialog.tsx, modules/jira/backend/routes/jira/search/route.ts, app/api/jira/search/route.ts</files>
  <behavior>
    JiraSyncDialog imports from @/modules/jira/ui/JiraSyncDialog (D-06).
    app/api/jira/search/route.ts is a P2 re-export (D-02).
  </behavior>
  <implementation>
    git mv dialogs and jira search route; write P2 shell; retarget imports (D-03).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="24-08-01">
  <name>Move Jira dialogs and GET /api/jira/search</name>
  <files>modules/jira/backend/jira-module-split.test.ts, modules/jira/ui/JiraSyncDialog.tsx, modules/jira/ui/BugImportDialog.tsx, app/api/jira/search/route.ts, modules/jira/backend/routes/jira/search/route.ts, modules/jira/backend/routes/jira/search/route.test.ts, modules/jira/backend/routes/jira/search/schema.ts</files>
  <read_first>
  - components/jira/JiraSyncDialog.tsx
  - app/api/jira/search/route.ts
  - .planning/phases/24-repo-wide-module-split/24-RESEARCH.md (Wave 8)
  </read_first>
  <behavior>
  - JiraSyncDialog and BugImportDialog exist under modules/jira/ui (D-01, D-06).
  - app/api/jira/search/route.ts re-exports GET (and POST if present) from the module route (P2, D-02).
  - app/api/import/resource-plan/[id]/route.ts still contains withProjectAccess( (P3, ENF-01).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(24-08) then feat(24-08). Do not add an app/jira page. Do not edit Sidebar.tsx or package.json (D-08).
  RED: Add jira-module-split.test.ts for dialog file existence via import, P2 jira/search re-export, and P3 resource-plan wrapper identifier. Commit test(24-08): red jira search tracer.
  GREEN: git mv JiraSyncDialog and BugImportDialog. git mv jira/search route plus test plus schema. Write P2 shell. Update importer files that referenced @/components/jira or @/components/bugs/BugImportDialog. Do not restyle dialogs (D-10). Commit feat(24-08): jira dialogs and search P2.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/jira/backend/jira-module-split.test.ts modules/jira/backend/routes/jira/search/route.test.ts</automated>
  </verify>
  <done>Jira dialogs live under modules/jira/ui; /api/jira/search is a P2 shell (MOD-01, MOD-02, D-06).</done>
  <acceptance_criteria>
  - modules/jira/ui/JiraSyncDialog.tsx exists
  - app/api/jira/search/route.ts re-exports from modules/jira/backend
  - jira-module-split.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="24-08-02">
  <name>Move timeline import UI tree</name>
  <files>modules/jira/ui/timeline-import, modules/projects/ui/timeline, modules/jira/backend/jira-module-split.test.ts</files>
  <read_first>
  - components/timeline/ImportMappingDialog.tsx
  - modules/projects/ui/timeline
  </read_first>
  <behavior>
  - Timeline import files live under modules/jira/ui/timeline-import including ImportMappingDialog.component.test.tsx (D-06, D-09).
  - Project timeline page still compiles and imports the dialog from the jira module (D-03, D-10).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits.
  RED: Extend jira-module-split.test.ts: ImportMappingDialog is importable from @/modules/jira/ui/timeline-import/ImportMappingDialog. Commit test(24-08): red timeline import UI move.
  GREEN: git mv components/timeline/** to modules/jira/ui/timeline-import/**. Update imports in modules/projects/ui/timeline and any remaining app/ references. Keep Sidebar unused here. Do not restyle (D-10). Commit feat(24-08): timeline import UI in jira module.
  </action>
  <verify>
    <automated>npx vitest run modules/jira/ui modules/projects/ui/timeline</automated>
  </verify>
  <done>Timeline import UI is under modules/jira/ui; project timeline still loads it (D-06, D-09, D-10).</done>
  <acceptance_criteria>
  - ImportMappingDialog lives under modules/jira/ui/timeline-import
  - vitest on that tree and projects timeline exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="24-08-03">
  <name>Move remaining jira/import backend including P3 resource-plan</name>
  <files>modules/jira/backend/services/import-mapping.service.ts, modules/jira/backend/services/jira-mapping.service.ts, modules/jira/backend/repositories/import-mapping.repo.ts, app/api/jira/fields/route.ts, app/api/jira/jql-presets/route.ts, app/api/jira/sync-mappings/route.ts, app/api/jira/test/route.ts, app/api/import-mapping/route.ts, app/api/bug-import-mapping/route.ts, app/api/parse-file-headers/route.ts, app/api/import/resource-plan/[id]/route.ts</files>
  <read_first>
  - .planning/phases/24-repo-wide-module-split/24-RESEARCH.md (Wave 8 backend)
  - app/api/import/resource-plan/[id]/route.ts
  </read_first>
  <behavior>
  - Remaining jira and import-mapping app/api files are P2 re-exports (D-02).
  - import/resource-plan/[id] contains withProjectAccess( and imports a jira-module handler (P3, ENF-01).
  </behavior>
  <action>
  TDD. Merge RED and GREEN. Two commits.
  RED: Extend jira-module-split.test.ts for each remaining RESEARCH P2 route plus P3 resource-plan. Commit test(24-08): red remaining jira APIs.
  GREEN: git mv services (including unit tests), repos, and routes per RESEARCH. P2 shells; P3 resource-plan handlers with wrapper in app/api. parse-file-headers URL stays app/api/parse-file-headers (A3). Run eslint on the resource-plan route file. Commit feat(24-08): remaining jira backend.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/jira/backend && npx eslint "app/api/import/resource-plan/[id]/route.ts"</automated>
  </verify>
  <done>Jira/import APIs live under modules/jira/backend with correct P2/P3 shells (MOD-02, D-06, ENF-01).</done>
  <acceptance_criteria>
  - app/api/import/resource-plan/[id]/route.ts contains withProjectAccess(
  - app/api/parse-file-headers/route.ts re-exports from modules/jira/backend
  - eslint on the resource-plan route file exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/jira/search | Jira proxy; existing validation stays |
| Browser → /api/import/resource-plan/[id] | Project tenancy; ENF-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-11 | Elevation of Privilege | import/resource-plan/[id] | critical | mitigate | P3 wrapper-stays (ENF-01). ASVS L1 V4. |
| T-24-12 | Information Disclosure | jira/search | medium | mitigate | Move handler unchanged (D-03); JIRA-01 logging rules stay. |
| T-24-SC | Tampering | npm installs | high | accept | No package-manager install (D-08). |
</threat_model>

<verification>
- 24-08-01 search tracer green
- 24-08-02 timeline import UI green
- 24-08-03 remaining APIs plus eslint green
- Wave: npx vitest run modules/jira
</verification>

<success_criteria>
Jira module has backend and UI; import URLs unchanged; resource-plan stays wrapped in app/api (MOD-01, MOD-02, D-06, ENF-01).
</success_criteria>

<output>
Create `.planning/phases/24-repo-wide-module-split/24-08-SUMMARY.md` when done
</output>
