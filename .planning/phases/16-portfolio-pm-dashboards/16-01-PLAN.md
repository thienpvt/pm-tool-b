---
phase: 16-portfolio-pm-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db-dashboards.ts
  - lib/db-dashboards.ddl.unit.test.ts
  - lib/db.ts
  - lib/dashboards/rag.ts
  - lib/dashboards/kpi.ts
  - lib/dashboards/kpi.unit.test.ts
  - lib/dashboards/filters.ts
  - lib/dashboards/filters.unit.test.ts
  - lib/repositories/dashboard-filter-state.repo.ts
  - lib/repositories/dashboard-filter-state.repo.test.ts
  - lib/services/spec-dashboards.service.ts
  - lib/services/spec-dashboards.service.unit.test.ts
  - app/api/dashboards/portfolio/route.ts
  - app/api/dashboards/portfolio/route.test.ts
autonomous: true
requirements: [PDSH-01, PDSH-02, PDSH-03, PDSH-04, PDSH-05]
estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO GET /api/dashboards/portfolio returns JSON with filters, kpis, charts, list, and drilldowns computed from live project master status/stage/rag after AND filters (D-01, D-16, PDSH-01, PDSH-02, PDSH-05)."
    - "kpis.active_count is status Active and stage L0–L4; on_track_count is that set with normalized Green; watch_act_count is Amber or Red; missing or invalid rag on Active L0–L4 counts as Amber so charts.by_rag green + amber + red equals active_count (D-02, D-03, D-04, PDSH-01, PDSH-02)."
    - "Stage chart counts the filtered set including L5; RAG chart buckets only the Active L0–L4 subset of that filtered set (D-04, PDSH-02)."
    - "kpis.overdue_milestone_project_count is distinct project_id from listOverdueMilestones intersect filtered list; drilldowns.overdue_milestones is one row per overdue milestone; kpis.high_open_raid_count is filtered listHighOpenRaid.records length (record count); kpis.technology_council_count is filtered listTechnologyCouncilIssues length; each drill-down list is those same filtered rows (D-05, D-14, PDSH-03, PDSH-04)."
    - "AND filters portfolio_year, program (customer_id), pm_user_id (active primary), stage, status, rag, type (classification), weekly_report_enabled apply to tiles, charts, list, and drill-downs together; unit is accepted and does not narrow; unknown keys throw ValidationError (D-06, PDSH-05)."
    - "GET reads dashboard_filter_state for (actor.user_id, surface portfolio) and applies that blob; missing row means empty filters; Viewer and PM are 403; seed admin with null company_id is 403 via assertCompanyWrite (D-07, D-12)."
    - "spec-dashboards.service does not import portfolio.service; migrateDashboards runs from getDb after migrateFiscalBudget (D-01, D-07)."
  artifacts:
    - path: "lib/db-dashboards.ts"
      provides: "DASHBOARDS_DDL_FLAG dashboards_ddl_v1, DASHBOARDS_DDL CREATE TABLE dashboard_filter_state, migrateDashboards invoked from getDb after migrateFiscalBudget (D-07)"
      exports: ["migrateDashboards", "DASHBOARDS_DDL", "DASHBOARDS_DDL_FLAG"]
    - path: "lib/dashboards/rag.ts"
      provides: "normalizeRag and isActiveProject (D-02, D-03)"
      exports: ["normalizeRag", "isActiveProject", "ACTIVE_STAGES"]
    - path: "lib/dashboards/kpi.ts"
      provides: "computePortfolioKpis and computePortfolioCharts (D-02, D-03, D-04, PDSH-01, PDSH-02)"
      exports: ["computePortfolioKpis", "computePortfolioCharts"]
    - path: "lib/dashboards/filters.ts"
      provides: "parseDashboardFilters and applyDashboardFilters (D-06, PDSH-05)"
      exports: ["parseDashboardFilters", "applyDashboardFilters", "DASHBOARD_FILTER_KEYS"]
    - path: "lib/repositories/dashboard-filter-state.repo.ts"
      provides: "getDashboardFilters(userId, surface) returning {} when no row (D-07)"
      exports: ["getDashboardFilters"]
    - path: "lib/services/spec-dashboards.service.ts"
      provides: "getPortfolioDashboard(actor) (D-01, PDSH-01..05)"
      exports: ["getPortfolioDashboard"]
    - path: "app/api/dashboards/portfolio/route.ts"
      provides: "GET withCpmo portfolio dashboard (D-12)"
  key_links:
    - from: "lib/db.ts"
      to: "lib/db-dashboards.ts"
      via: "getDb awaits migrateDashboards(pool) immediately after migrateFiscalBudget(pool) and before backfillWeightedCompletion (D-07)"
      pattern: "migrateDashboards"
    - from: "app/api/dashboards/portfolio/route.ts"
      to: "lib/services/spec-dashboards.service.ts"
      via: "GET calls getPortfolioDashboard(actor) (D-01, D-12)"
      pattern: "getPortfolioDashboard"
    - from: "lib/services/spec-dashboards.service.ts"
      to: "lib/repositories/projects.repo.ts"
      via: "listProjects(actor.company_id) then applyDashboardFilters; live projects.rag (D-14, D-16)"
      pattern: "listProjects"
    - from: "lib/services/spec-dashboards.service.ts"
      to: "lib/services/raid-masters.service.ts"
      via: "listOverdueMilestones, listHighOpenRaid, listTechnologyCouncilIssues then intersect filtered project ids (D-05, D-14)"
      pattern: "listOverdueMilestones"
  prohibitions:
    - editing getPortfolioSummary, listPortfolioProjects enrichment, or GET /api/portfolio (D-01)
    - importing lib/services/portfolio.service from spec-dashboards.service (D-01)
    - calling getPeriodTracking (D-10)
    - KPI rag from weekly_report_versions snapshot columns (D-16)
    - reimplementing overdue/high-RAID/tech-council SQL (D-14)
    - High RAID tile as distinct project count (D-05)
    - adding npm packages
    - CASL or leftover ops/admin re-gating (D-12)
    - UI-SPEC or treating a thin page as a gate (D-13)
    - budget remaining or open-dependency tiles (D-14)
    - filter persist PUT/clear/export routes (16-02)
    - PM dashboard routes (16-03)
    - physical row-removal SQL helpers on dashboard_filter_state or governed tables (D-15)
---

<objective>
Prove the Phase 16 portfolio spine. D-01: parallel /api/dashboards surface and spec-dashboards.service, not GET /api/portfolio. D-02: active = status Active and stage L0–L4. D-03: on-track Green, watch/act Amber/Red, missing rag counts Amber so G+A+R equals active. D-04: stage chart is the filtered set including L5; RAG identity is the active subset. D-05: overdue tile is distinct projects, drill-down per milestone, RAID and tech-council are record counts. D-06: AND filters with type=classification and unit no-op. D-07: dashboard_filter_state DDL after migrateFiscalBudget and GET applies stored blob. D-12: withCpmo plus assertCompanyWrite. D-13: server tests are the gate. D-14: consume Phase 12 list helpers. D-16: live project master rag/stage/status.

Purpose: Catch an architectural dead-end on the first commit — parallel /api/dashboards surface (not GET /api/portfolio), live projects.rag (not snapshot rag), Phase 12 list helpers (not new SQL) — before filter persist and export.

Output: Production-quality tracer plus expansions for charts/drill-downs and AND filters. Persist/clear/export is 16-02. PM actions are 16-03. Server tests are the gate (D-13).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md
@.planning/phases/16-portfolio-pm-dashboards/16-RESEARCH.md
@.planning/phases/16-portfolio-pm-dashboards/16-PATTERNS.md
@.planning/phases/16-portfolio-pm-dashboards/16-VALIDATION.md

Discretion locked by planner: one GET returns { filters, kpis, charts, list, drilldowns }. type filter maps to classification. unit is a known key and a no-op. Overdue tile is distinct project_id; drilldowns.overdue_milestones is one row per overdue milestone; also expose overdue_milestone_project_count on kpis. High RAID tile is record count. pm_user_id filter matches getActivePrimaryAssignment.user_id (role primary, active window), not the collaborator EXISTS used by listProjects pmUserId. GET reads stored filters; missing row is {}. PUT/export/PM routes are later plans.

Do not implement deferred items: document catalog (Phase 17), full audit coverage (Phase 18), v1 portfolio home redesign, budget/dependency tiles.

Prior-phase artifact: listProjects(companyId), listOverdueMilestones, listHighOpenRaid, listTechnologyCouncilIssues, getActivePrimaryAssignment, withCpmo, assertCompanyWrite, migrateFiscalBudget, ValidationError.

<interfaces>
DASHBOARDS_DDL_FLAG = dashboards_ddl_v1. DASHBOARDS_DDL creates dashboard_filter_state: user_id INTEGER NOT NULL REFERENCES users(id); surface TEXT NOT NULL CHECK (surface IN ('portfolio', 'pm')); filters_json JSONB NOT NULL DEFAULT '{}'; updated_at TIMESTAMPTZ NOT NULL DEFAULT now(); PRIMARY KEY (user_id, surface). Copy settingsFlagExists / writeSettingsFlag / try-catch retry from lib/db-fiscal-budget.ts migrateFiscalBudget. Do not add UPDATE-by-id or row-removal helpers.

getDb: after await migrateFiscalBudget(pool) dynamically import migrateDashboards and await migrateDashboards(pool) before backfillWeightedCompletion.

normalizeRag(raw): trim lower-case; green / red / amber stay; anything else including null empty Not applicable maps to amber (D-03).

isActiveProject(p): p.status === 'Active' AND p.stage is one of L0 L1 L2 L3 L4 (exclude L5 and null stage).

computePortfolioKpis(filteredProjects, overdueRows, highRaidRecords, techCouncilRows): active_count / on_track_count / watch_act_count from isActiveProject + normalizeRag; overdue_milestone_project_count = number of distinct project_id in overdueRows whose project_id is in the filtered id set; high_open_raid_count = highRaidRecords in the filtered id set (length, not distinct projects); technology_council_count = techCouncilRows in the filtered id set.

computePortfolioCharts(filteredProjects): by_stage counts every filtered row whose stage is L0–L5 (missing stage omitted from buckets); by_rag counts only isActiveProject rows into green/amber/red via normalizeRag. Invariant: by_rag.green + by_rag.amber + by_rag.red === active_count.

DASHBOARD_FILTER_KEYS = portfolio_year, program, unit, pm_user_id, stage, status, rag, type, weekly_report_enabled. parseDashboardFilters(input): object of those keys only; unknown key throws ValidationError(message, key); omitted/null/empty-string key means absent. applyDashboardFilters(rows, filters): AND present keys; program compares customer_id; type compares classification; rag compares normalizeRag(row.rag) to normalizeRag(filter); unit is ignored (no column); pm_user_id compares row.pm_user_id (caller must attach active primary).

getDashboardFilters(userId, surface): SELECT filters_json, updated_at WHERE user_id AND surface; no row returns { filters: {}, updated_at: null }. Parse JSONB object. Do not upsert in this plan.

getPortfolioDashboard(actor): assertCompanyWrite(actor) first; stored = getDashboardFilters(actor.user_id, 'portfolio'); filters = parseDashboardFilters(stored.filters); projects = listProjects(actor.company_id) from lib/repositories/projects.repo (not projects.service); attach pm_user_id via getActivePrimaryAssignment from lib/repositories/pm-assignments.repo.ts (repo export; do not import from projects.service) per project (null if none); filtered = applyDashboardFilters(projects, filters); load listOverdueMilestones / listHighOpenRaid / listTechnologyCouncilIssues for actor.company_id; intersect by project_id in filtered ids; return { filters, kpis: computePortfolioKpis(...), charts: computePortfolioCharts(filtered), list: filtered dashboard rows, drilldowns: { overdue_milestones, high_raid: highRaid.records filtered, technology_council } }. Dashboard list row fields: id, name, project_code, portfolio_year, customer_id, program_name, stage, status, rag, classification, weekly_report_enabled, progress_pct, pm_user_id, pm_name. Do not read weekly snapshot rag.

GET /api/dashboards/portfolio = withCpmo; return NextResponse.json(await getPortfolioDashboard(actor)). Analog app/api/weekly-periods/[periodId]/tracking/route.ts. Route tests analog that file: 401 no session; 403 pm; 403 viewer; 403 session { is_admin: 1, company_id: null, roles: [] }; 200 cpmo with company_id: 5. Mock getPortfolioDashboard. No CASL.

Isolation none. TEST_DATABASE_URL for skipIf(!hasTestDb) must end in _test (postgres://postgres:postgres@localhost:5432/pm_tool_test).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="16-01-01">
  <name>End-to-end CPMO GET portfolio dashboard tiles</name>
  <precondition>lib/db-fiscal-budget.ts exports migrateFiscalBudget; getDb already awaits migrateFiscalBudget; lib/repositories/projects.repo.ts exports listProjects; lib/services/raid-masters.service.ts exports listOverdueMilestones, listHighOpenRaid, listTechnologyCouncilIssues; lib/services/access.ts exports assertCompanyWrite</precondition>
  <reversibility rating="costly">Additive dashboard_filter_state and GET /api/dashboards/portfolio become a published parallel surface; mixing this into GET /api/portfolio later would force a split (D-01, D-07).</reversibility>
  <files>lib/db-dashboards.ts, lib/db-dashboards.ddl.unit.test.ts, lib/db.ts, lib/dashboards/rag.ts, lib/dashboards/kpi.ts, lib/dashboards/kpi.unit.test.ts, lib/dashboards/filters.ts, lib/repositories/dashboard-filter-state.repo.ts, lib/repositories/dashboard-filter-state.repo.test.ts, lib/services/spec-dashboards.service.ts, lib/services/spec-dashboards.service.unit.test.ts, app/api/dashboards/portfolio/route.ts, app/api/dashboards/portfolio/route.test.ts</files>
  <read_first>
  - .planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md (D-01 through D-07, D-12, D-16)
  - .planning/phases/16-portfolio-pm-dashboards/16-PATTERNS.md (settings-flag DDL, withCpmo, listProjects)
  - lib/db-fiscal-budget.ts (settingsFlagExists, writeSettingsFlag, try/catch)
  - lib/db.ts getDb migrateFiscalBudget then backfillWeightedCompletion
  - lib/repositories/projects.repo.ts listProjects and PROJECT_COLUMNS
  - lib/repositories/pm-assignments.repo.ts getActivePrimaryAssignment
  - lib/http/with-role.ts withCpmo
  - lib/services/access.ts assertCompanyWrite
  - app/api/weekly-periods/[periodId]/tracking/route.ts and route.test.ts
  - test/db.ts TEST_DATABASE_URL _test suffix
  </read_first>
  <behavior>
  - migrateDashboards is exported; DASHBOARDS_DDL_FLAG is dashboards_ddl_v1; joined DDL creates dashboard_filter_state with PRIMARY KEY (user_id, surface), filters_json JSONB, surface CHECK portfolio or pm; getDb awaits migrateDashboards after migrateFiscalBudget and before backfillWeightedCompletion (D-07).
  - getPortfolioDashboard with a CPMO actor returns kpis.active_count / on_track_count / watch_act_count from live status, stage, rag; list is the filtered project rows; filters is {} when no stored row (D-01, D-02, D-03, D-16, PDSH-01).
  - getPortfolioDashboard throws ForbiddenError without listing projects when assertCompanyWrite fails (D-12).
  - HTTP GET /api/dashboards/portfolio as CPMO returns 200 with filters, kpis, charts, list, drilldowns; PM/viewer return 403; missing session returns 401; null-company admin session returns 403 (D-12).
  - spec-dashboards.service source does not import lib/services/portfolio.service (D-01).
  </behavior>
  <action>
  TDD first (D-13). Isolation is none — keep this plan sequential-safe on the main tree.
  RED: add failing lib/db-dashboards.ddl.unit.test.ts cases for the flag, CREATE TABLE, PRIMARY KEY (user_id, surface), and getDb wire order after migrateFiscalBudget; failing skipIf(!hasTestDb) getDashboardFilters empty-row case; failing kpi.unit.test.ts for active/on-track/watch from live master; failing spec-dashboards.service.unit.test.ts for getPortfolioDashboard tiles plus assertCompanyWrite-before-list; failing portfolio route.test.ts auth matrix. Tests must fail before production modules exist. Commit `test(16-01): red CPMO portfolio dashboard GET`.
  GREEN: implement until green. Commit `feat(16-01): CPMO portfolio dashboard API`.

  Extend lib/db.ts per interfaces. Do not add an ORM migrate CLI. Do not add row-removal helpers for dashboard_filter_state (D-15).

  Create rag.ts / kpi.ts / filters.ts per interfaces. Tracer applyDashboardFilters may treat missing keys as pass-through; task 16-01-03 completes the AND matrix. Tracer GET still calls parseDashboardFilters + applyDashboardFilters so the filter seam exists (D-06).

  getPortfolioDashboard only — do not add save/clear/export or getPmDashboard here. Mock assertCompanyWrite, listProjects from projects.repo, getDashboardFilters, getActivePrimaryAssignment, listOverdueMilestones, listHighOpenRaid, listTechnologyCouncilIssues. Import list helpers from raid-masters.service; do not copy SQL.

  Route: GET withCpmo as specified. Error mapping stays in withAuth via serviceErrorResponse. Do not wrap with a policy engine. Do not touch leftover ops/admin routes (D-12). Do not add pages or UI-SPEC (D-13). Do not edit app/api/portfolio/route.ts or lib/services/portfolio.service.ts (D-01).
  </action>
  <verify>
    <automated>npx vitest run lib/db-dashboards.ddl.unit.test.ts lib/dashboards/kpi.unit.test.ts lib/repositories/dashboard-filter-state.repo.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/route.test.ts</automated>
  </verify>
  <done>CPMO can GET /api/dashboards/portfolio and receive live-master active/on-track/watch tiles plus a project list; migrateDashboards runs after migrateFiscalBudget; Viewer/PM cannot GET the portfolio dashboard (D-01, D-02, D-03, D-07, D-12, PDSH-01).</done>
  <acceptance_criteria>
  - lib/db-dashboards.ts exports migrateDashboards and DASHBOARDS_DDL_FLAG dashboards_ddl_v1
  - getDb awaits migrateDashboards after migrateFiscalBudget and before backfillWeightedCompletion
  - getPortfolioDashboard calls assertCompanyWrite before listProjects
  - GET handler returns 200 JSON with filters, kpis, charts, list, drilldowns for CPMO
  - spec-dashboards.service.unit.test.ts asserts the service module source does not import portfolio.service (D-01)
  - npx vitest run lib/db-dashboards.ddl.unit.test.ts lib/dashboards/kpi.unit.test.ts lib/repositories/dashboard-filter-state.repo.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="16-01-02">
  <name>Charts, RAG identity, matching drill-downs</name>
  <files>lib/dashboards/kpi.ts, lib/dashboards/kpi.unit.test.ts, lib/services/spec-dashboards.service.ts, lib/services/spec-dashboards.service.unit.test.ts</files>
  <read_first>
  - lib/dashboards/kpi.ts from 16-01-01
  - lib/services/spec-dashboards.service.ts from 16-01-01
  - .planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md D-03 D-04 D-05
  - lib/services/raid-masters.service.ts listOverdueMilestones listHighOpenRaid listTechnologyCouncilIssues
  </read_first>
  <behavior>
  - computePortfolioCharts by_stage counts L5 rows that pass filters; by_rag ignores L5; green + amber + red equals active_count including null rag as amber (D-03, D-04, PDSH-02).
  - getPortfolioDashboard kpis.overdue_milestone_project_count is distinct project_id; drilldowns.overdue_milestones has one element per overdue milestone so two overdue milestones on one filtered project yield count 1 and rows length 2 (D-05, PDSH-03).
  - high_open_raid_count equals filtered highRaid.records.length, not distinct project_id; drilldowns.high_raid ids equal those records (D-05, PDSH-03).
  - technology_council_count equals drilldowns.technology_council length for the filtered set (D-05, PDSH-04).
  </behavior>
  <action>
  TDD. RED: failing kpi.unit.test.ts for stage chart including L5, RAG chart identity, and null rag to amber; failing service unit tests for overdue distinct-vs-rows, RAID record count (two records same project stay count 2), and tech-council equality. Commit `test(16-01): red portfolio charts and drilldowns`.
  GREEN: implement computePortfolioCharts and drilldown intersection in getPortfolioDashboard per interfaces. Commit `feat(16-01): portfolio charts and matching drilldowns`.

  Do not count High RAID as distinct projects (D-05). Do not feed L5 into active/on-track/watch tiles (D-02, D-04). Do not reimplement Phase 12 SQL (D-14). Do not add persist or export routes.
  </action>
  <verify>
    <automated>npx vitest run lib/dashboards/kpi.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts</automated>
  </verify>
  <done>Charts cover L0–L5 and RAG with Green+Amber+Red equal to active count; overdue tile is distinct projects with per-milestone drill-down rows; High RAID and tech-council tiles match their drill-down lists (D-03, D-04, D-05, PDSH-02, PDSH-03, PDSH-04).</done>
  <acceptance_criteria>
  - charts.by_rag.green + amber + red === kpis.active_count
  - overdue_milestone_project_count uses distinct project_id; overdue_milestones is per milestone
  - high_open_raid_count is filtered record length
  - technology_council drill-down length equals the tile count
  - npx vitest run lib/dashboards/kpi.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="16-01-03">
  <name>AND filters on tiles, list, and drill-downs</name>
  <files>lib/dashboards/filters.ts, lib/dashboards/filters.unit.test.ts, lib/services/spec-dashboards.service.ts, lib/services/spec-dashboards.service.unit.test.ts</files>
  <read_first>
  - lib/dashboards/filters.ts from 16-01-01
  - .planning/phases/16-portfolio-pm-dashboards/16-CONTEXT.md D-06 D-07
  - .planning/phases/16-portfolio-pm-dashboards/16-RESEARCH.md Filter Column Map
  - lib/repositories/projects.repo.ts PROJECT_COLUMNS (no unit column; classification is type)
  </read_first>
  <behavior>
  - applyDashboardFilters AND-combines portfolio_year, program (customer_id), pm_user_id, stage, status, rag, type (classification), weekly_report_enabled; a row failing any present key is dropped (D-06, PDSH-05).
  - unit present does not throw and does not change membership (D-06).
  - parseDashboardFilters throws ValidationError on an unknown key (D-06).
  - getPortfolioDashboard applies stored filters once; kpis, charts, list, and drilldowns all shrink together (D-07, PDSH-05).
  </behavior>
  <action>
  TDD. RED: failing filters.unit.test.ts for each mapped dimension, combined AND, unit no-op, type matching classification, unknown key ValidationError; failing service unit test that a stored stage filter reduces tiles, list, and overdue drill-down together. Commit `test(16-01): red dashboard AND filters`.
  GREEN: implement parseDashboardFilters and applyDashboardFilters per interfaces; getPortfolioDashboard already threads stored filters — keep a single parse/apply so drill-downs inherit (D-07). Commit `feat(16-01): portfolio AND dashboard filters`.

  Do not SQL-reference a unit column (there is none). Do not persist PUT/clear in this plan (16-02). Do not treat collaborator assignments as the pm_user_id filter (active primary only via getActivePrimaryAssignment).
  </action>
  <verify>
    <automated>npx vitest run lib/dashboards/filters.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts</automated>
  </verify>
  <done>AND filters apply to the whole portfolio dashboard; unit is a no-op; unknown keys 400 via ValidationError; drill-downs inherit the same filtered project set (D-06, D-07, PDSH-05).</done>
  <acceptance_criteria>
  - type filter compares classification
  - unit filter does not narrow the set
  - unknown keys throw ValidationError
  - one stored filter blob drives kpis, charts, list, and drilldowns
  - npx vitest run lib/dashboards/filters.unit.test.ts lib/services/spec-dashboards.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /api/dashboards/portfolio | Untrusted actor; must not leak another company's live master KPIs |
| Service → listProjects / Phase 12 lists | Company_id from assertCompanyWrite; filters must not bypass tenancy |
| getDb → migrateDashboards | Boot-time schema; must not drop v1 portfolio tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege | GET /api/dashboards/portfolio | high | mitigate | withCpmo plus assertCompanyWrite; PM/Viewer 403; null-company admin 403 (D-12, ASVS V4) |
| T-16-02 | Information Disclosure | getPortfolioDashboard listProjects | high | mitigate | listProjects(actor.company_id) company-or-customer tenancy; never list another company (D-02, ASVS V4) |
| T-16-03 | Tampering | parseDashboardFilters | medium | mitigate | Allowlisted keys only; unknown key ValidationError 400 (D-06, ASVS V5) |
| T-16-04 | Information Disclosure | KPI rag source | medium | mitigate | Live projects.rag via listProjects rows; do not join weekly_report_versions for tiles (D-16) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
| T-16-05 | Denial of Service | unbounded company project list | low | accept | Same listProjects set as existing project index; ASVS L1 list size is acceptable |
</threat_model>

<verification>
- 16-01-01: ddl unit + kpi tiles + getPortfolioDashboard + CPMO route tests green
- 16-01-02: chart identity and drill-down matching green
- 16-01-03: AND filter matrix green
- Wave: npx vitest run lib/db-dashboards.ddl.unit.test.ts lib/dashboards/kpi.unit.test.ts lib/dashboards/filters.unit.test.ts lib/repositories/dashboard-filter-state.repo.test.ts lib/services/spec-dashboards.service.unit.test.ts app/api/dashboards/portfolio/route.test.ts
</verification>

<success_criteria>
CPMO reads spec portfolio KPIs from live master with AND filters and drill-downs that match tiles; Green+Amber+Red equals active count; v1 GET /api/portfolio is untouched (PDSH-01, PDSH-02, PDSH-03, PDSH-04, PDSH-05, D-01, D-02, D-03, D-04, D-05, D-06, D-12, D-16).
</success_criteria>

<output>
Create `.planning/phases/16-portfolio-pm-dashboards/16-01-SUMMARY.md` when done
</output>
