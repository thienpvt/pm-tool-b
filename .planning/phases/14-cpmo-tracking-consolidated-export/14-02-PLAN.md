---
phase: 14-cpmo-tracking-consolidated-export
plan: 02
type: tdd
wave: 2
depends_on: [14-01]
files_modified:
  - lib/services/weekly-tracking.service.ts
  - lib/services/weekly-tracking.service.unit.test.ts
  - app/api/weekly-periods/[periodId]/export/preview/route.ts
  - app/api/weekly-periods/[periodId]/export/preview/schema.ts
  - app/api/weekly-periods/[periodId]/export/preview/route.test.ts
autonomous: true
requirements: [CPMO-03]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO POST /api/weekly-periods/[periodId]/export/preview with { project_ids } returns ordered section summaries; output order matches the request array (D-06, CPMO-03)."
    - "Only shells in that period with status submitted and latest_version at least 1 are eligible; any other id throws SubmitValidationError whose fields array names the ineligible ids (HTTP 400 { error, fields }) (D-06, D-14, CPMO-03)."
    - "Empty project_ids is rejected at the Zod boundary (400); unknown/foreign period is 404 via getWeeklyPeriodByCompany; ids not in the period shells are ineligible (D-14)."
    - "Each preview section is assembled from getLatestVersionSnapshot(report_id, latest_version) plus tracking identity/PM/stage already on the shell; missing snapshot keys render blank; live RAID masters are not read (D-01, D-08, CPMO-04 fields as summaries)."
    - "Technology-issue counts on preview come from snapshot.raid.issues where technology_council is true (or empty when the flag is absent); live listTechnologyCouncilIssues is not used for preview content (D-02)."
    - "Preview is withCpmo plus assertCompanyWrite; PM/Viewer 403 (D-11)."
  artifacts:
    - path: "lib/services/weekly-tracking.service.ts"
      provides: "previewConsolidatedExport plus assertExportEligible and assembleSnapshotSections for 14-03 reuse (D-06, D-08)"
      exports: ["previewConsolidatedExport", "assertExportEligible", "assembleSnapshotSections"]
    - path: "app/api/weekly-periods/[periodId]/export/preview/route.ts"
      provides: "POST withCpmo preview (D-06, D-11)"
    - path: "app/api/weekly-periods/[periodId]/export/preview/schema.ts"
      provides: "periodExportPreviewSchema { project_ids: z.array(z.number().int().positive()).min(1) } (D-14)"
  key_links:
    - from: "app/api/weekly-periods/[periodId]/export/preview/route.ts"
      to: "lib/services/weekly-tracking.service.ts"
      via: "POST calls previewConsolidatedExport(actor.company_id, periodId, actor, body.project_ids) (D-06)"
      pattern: "previewConsolidatedExport"
    - from: "lib/services/weekly-tracking.service.ts"
      to: "lib/repositories/weekly-reports.repo.ts"
      via: "eligible shells then getLatestVersionSnapshot per report; never live risk/issue list helpers for section RAID (D-01, D-08)"
      pattern: "getLatestVersionSnapshot"
  prohibitions:
    - persisted project-order / rank table (D-06)
    - live RAID or live issues as preview section content (D-01, D-02)
    - calling the activity-weighted report helper or the v1 weekly Excel route (D-01)
    - generating xlsx/docx/pptx buffers (14-03)
    - inserting weekly_export_logs or auditLog weekly_export (14-03)
    - a second write-gate wrapper family (D-11)
    - adding npm packages
    - physical row-removal SQL helpers (D-09)
    - UI-SPEC or portfolio redesign (D-12)
---

<objective>
Deliver preview eligibility, caller-order sections, and snapshot assembly (D-01, D-02, D-06, D-08, D-11, D-14, CPMO-03): POST preview returns ordered summaries from latest submitted snapshots; ineligible ids are 400 fields.

Purpose: TDD the snapshot contract and eligibility gate before binary generators and export-log writes land in 14-03.

Output: Working, tested previewConsolidatedExport and assembleSnapshotSections. 14-03 reuses those exports for the pack.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-RESEARCH.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-PATTERNS.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-VALIDATION.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-01-PLAN.md

14-01 created getPeriodTracking, company-scoped listPeriodShellsRepo, and weekly_export_logs DDL. This plan owns preview HTTP and snapshot section mapping. Do not insert export logs.

Discretion locked by planner: POST (not GET) preview; no rank table; export assembleSnapshotSections so 14-03 does not rebuild mapping; preview JSON { period, sections }; each section includes identity (code, name), pm_display_name, stage, prev_week_rag, this_week_rag, progress_pct, highlights, next_week_goals, nearest_milestone, raid_counts { risks, issues }, tech_issue_counts, project_id, report_id, latest_version; SubmitValidationError('Projects not eligible for export', ineligibleIdsAsStrings) already maps to 400 { error, fields } via serviceErrorResponse.

Do not implement xlsx/docx/pptx files or weekly_export_logs INSERT here.

<interfaces>
assertExportEligible(shellsByProjectId, projectIds): for each id, shell must exist in the period map and status === 'submitted' and latest_version &gt;= 1; collect ineligible ids in request order; if any, throw SubmitValidationError with fields = those ids as strings (D-06, D-14).

assembleSnapshotSections(projectIds, shellMap, snapshotByReportId): walk projectIds in caller order; load snapshot for shell.report_id + shell.latest_version; map D-08 fields from snapshot keys highlights, next_week_goals, this_week_rag, prev_week_rag, progress_pct, nearest_milestone, raid.risks, raid.issues, milestones; identity/PM/stage from the shell row (name, project_code, stage, pm_display_name); tech issues = issues where technology_council === true; if the flag is missing on every issue, tech list is empty (do not substitute live rows) (D-02, D-08). Blank missing keys. Return full section objects; preview may pick summary fields plus counts from the same objects.

previewConsolidatedExport(companyId, periodId, actor, projectIds): same company gate as getPeriodTracking (assertCompanyWrite, actor.company_id, getWeeklyPeriodByCompany); listPeriodShellsRepo(companyId, periodId); assertExportEligible; getLatestVersionSnapshot per eligible shell; return { period, sections } ordered by projectIds.

POST /api/weekly-periods/[periodId]/export/preview = withCpmo + { schema: periodExportPreviewSchema }; periodExportPreviewSchema = z.object({ project_ids: z.array(z.number().int().positive()).min(1) }); handler returns NextResponse.json(await previewConsolidatedExport(...)). Analog app/api/weekly-periods/route.ts POST.

Route tests analog tracking route.test.ts: 401/403; CPMO 200 with mocked preview; Zod empty array 400 without calling the service. Mock weekly-tracking.service, not assignment helpers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="14-02-01">
  <name>Preview eligibility and 400 ineligible fields</name>
  <files>lib/services/weekly-tracking.service.ts, lib/services/weekly-tracking.service.unit.test.ts, app/api/weekly-periods/[periodId]/export/preview/route.ts, app/api/weekly-periods/[periodId]/export/preview/schema.ts, app/api/weekly-periods/[periodId]/export/preview/route.test.ts</files>
  <read_first>
  - lib/services/weekly-tracking.service.ts from 14-01
  - lib/services/errors.ts SubmitValidationError
  - lib/api-errors.ts SubmitValidationError 400 fields mapping
  - app/api/weekly-periods/route.ts withCpmo POST + schema
  - .planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md D-06 D-11 D-14
  </read_first>
  <behavior>
  - previewConsolidatedExport does not read snapshots when assertCompanyWrite rejects (D-11).
  - Unknown period throws NotFoundError (D-14).
  - A draft or not_submitted id, or latest_version 0, throws SubmitValidationError whose fields include that id (D-06, D-14).
  - An id absent from the period shells is ineligible the same way (D-14).
  - HTTP POST as PM/viewer is 403; empty project_ids is 400 from Zod (D-11, D-14).
  </behavior>
  <action>
  TDD. Isolation none.
  RED: failing service tests for write-gate, 404 period, and ineligible id lists (draft, version 0, foreign project_id); failing preview route tests for 403 and empty-array 400. Commit `test(14-02): red export preview eligibility`.
  GREEN: implement assertExportEligible, previewConsolidatedExport (snapshots may return empty section bodies until 14-02-02), POST route + Zod schema per interfaces. Commit `feat(14-02): preview eligibility gate`.

  Reuse SubmitValidationError (do not invent a parallel 400 type). Do not persist order. Do not generate files. Do not insert export logs.
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-tracking.service.unit.test.ts "app/api/weekly-periods/[periodId]/export/preview/route.test.ts"</automated>
  </verify>
  <done>Only submitted versioned shells in the owned period can be previewed; ineligible ids surface as 400 fields; PM/Viewer cannot POST preview (D-06, D-11, D-14, CPMO-03).</done>
  <acceptance_criteria>
  - assertExportEligible throws SubmitValidationError with fields naming ineligible ids
  - previewConsolidatedExport uses withCpmo-equivalent assertCompanyWrite plus getWeeklyPeriodByCompany
  - POST schema min(1) on project_ids
  - npx vitest run lib/services/weekly-tracking.service.unit.test.ts "app/api/weekly-periods/[periodId]/export/preview/route.test.ts" exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="14-02-02">
  <name>Snapshot section assembly in caller order</name>
  <files>lib/services/weekly-tracking.service.ts, lib/services/weekly-tracking.service.unit.test.ts, app/api/weekly-periods/[periodId]/export/preview/route.test.ts</files>
  <read_first>
  - lib/services/weekly-tracking.service.ts from 14-02-01
  - lib/repositories/weekly-reports.repo.ts getLatestVersionSnapshot
  - .planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md D-01 D-02 D-08
  - .planning/phases/14-cpmo-tracking-consolidated-export/14-RESEARCH.md Pattern 4 snapshot keys
  </read_first>
  <behavior>
  - previewConsolidatedExport([B, A]) returns sections in B then A order (D-06, CPMO-03).
  - Section this_week_rag, prev_week_rag, progress_pct, highlights, next_week_goals, nearest_milestone, raid_counts, and tech_issue_counts match the mocked snapshot, not live masters (D-01, D-08).
  - When snapshot.raid.issues lack technology_council, tech_issue_counts is 0 and no live issue helper is called (D-02).
  - Missing snapshot keys become blank or zero counts; getLatestVersionSnapshot is the only snapshot source (D-08).
  - assembleSnapshotSections preserves snapshot.raid.risks, snapshot.raid.issues, and the filtered technology-issue rows on each section object so 14-03 can render full RAID tables (not counts only) (D-08).
  </behavior>
  <action>
  TDD. RED: failing tests that mock getLatestVersionSnapshot (and not risk/issue list helpers) for reorder, mapped fields, blank missing keys, tech-count from snapshot flags only, and preserved raid.risks / raid.issues arrays on assembled sections. Commit `test(14-02): red snapshot section assembly`.
  GREEN: implement assembleSnapshotSections and wire it from previewConsolidatedExport. Export assembleSnapshotSections for 14-03. Commit `feat(14-02): snapshot preview sections`.

  Do not import live RAID list helpers into weekly-tracking.service.ts for this mapping (D-01). Do not backfill blanks from projects or masters (D-08). Do not add binary generators (14-03).
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-tracking.service.unit.test.ts "app/api/weekly-periods/[periodId]/export/preview/route.test.ts"</automated>
  </verify>
  <done>Preview sections follow project_ids order and are snapshot-only, including blank missing fields and snapshot tech-issue counts (D-01, D-02, D-06, D-08, CPMO-03).</done>
  <acceptance_criteria>
  - assembleSnapshotSections is exported and used by previewConsolidatedExport
  - section order equals the request project_ids array
  - snapshot mapping covers D-08 summary fields, RAID/tech counts, and full raid.risks / raid.issues / tech-issue arrays for 14-03
  - tests mock getLatestVersionSnapshot and do not require live RAID repos for preview content
  - npx vitest run lib/services/weekly-tracking.service.unit.test.ts "app/api/weekly-periods/[periodId]/export/preview/route.test.ts" exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST export/preview body.project_ids | Untrusted ids; must not leak another company's snapshots |
| Service → getLatestVersionSnapshot | Snapshot JSON is trusted as submit-time immutable data, not a live register |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | previewConsolidatedExport | high | mitigate | Same company gate as tracking; ids must belong to that period's shells (D-11, D-14, ASVS V4) |
| T-14-07 | Information Disclosure | preview of draft/unsubmitted shells | high | mitigate | Eligibility requires submitted and latest_version at least 1; SubmitValidationError 400 (D-06, ASVS V4) |
| T-14-08 | Tampering | preview RAID content | high | mitigate | Read getLatestVersionSnapshot only; do not substitute live RAID (D-01, D-08) |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
| T-14-09 | Information Disclosure | empty project_ids | low | mitigate | Zod min(1) at the route boundary (D-14, ASVS V5) |
</threat_model>

<verification>
- 14-02-01: eligibility + 403/400 route tests green
- 14-02-02: order + snapshot mapping green
- Wave: npx vitest run lib/services/weekly-tracking.service.unit.test.ts "app/api/weekly-periods/[periodId]/export/preview/route.test.ts"
</verification>

<success_criteria>
CPMO can preview a caller-ordered consolidation of submitted snapshots; ineligible projects are named in 400 fields; section bodies do not use live RAID (CPMO-03, D-01, D-06, D-08, D-14).
</success_criteria>

<output>
Create `.planning/phases/14-cpmo-tracking-consolidated-export/14-02-SUMMARY.md` when done
</output>
