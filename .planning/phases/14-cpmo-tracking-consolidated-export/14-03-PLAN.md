---
phase: 14-cpmo-tracking-consolidated-export
plan: 03
type: tdd
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - lib/export/consolidated-weekly.ts
  - lib/export/consolidated-weekly.unit.test.ts
  - lib/repositories/weekly-export.repo.ts
  - lib/repositories/weekly-export.repo.test.ts
  - lib/services/weekly-tracking.service.ts
  - lib/services/weekly-tracking.service.unit.test.ts
  - app/api/weekly-periods/[periodId]/export/route.ts
  - app/api/weekly-periods/[periodId]/export/schema.ts
  - app/api/weekly-periods/[periodId]/export/route.test.ts
autonomous: true
requirements: [CPMO-03, CPMO-04]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO POST /api/weekly-periods/[periodId]/export with { project_ids, format: xlsx|docx|pptx } returns a binary Buffer; xlsx is one workbook with sheet Portfolio Summary plus one sheet per project in caller order (D-07, CPMO-03)."
    - "docx and pptx are generated from the same ConsolidatedWeeklyPayload as xlsx; generators accept that payload only and do not load projects by id or read live RAID (D-01, D-07, D-08)."
    - "Each project section includes identity (code, name), active primary PM display name, stage, prev_week_rag, this_week_rag, progress_pct, highlights, next_week_goals, nearest milestone, snapshot RAID, and snapshot technology issues; missing keys render blank (D-08, CPMO-04)."
    - "A successful export inserts weekly_export_logs (period_id, company_id, exported_by, format, data_version = max latest_version among included reports, project_ids JSON, period_display_name) and auditLog action weekly_export; concurrent exports append additional rows (D-09, D-14, CPMO-04)."
    - "Export reuses 14-02 eligibility and assembleSnapshotSections; ineligible ids remain 400 fields; PM/Viewer 403; withCpmo plus assertCompanyWrite (D-06, D-11, D-14)."
  artifacts:
    - path: "lib/export/consolidated-weekly.ts"
      provides: "generateConsolidatedXlsx / generateConsolidatedDocx / generateConsolidatedPptx(payload) returning Buffer (D-07, D-08)"
      exports: ["generateConsolidatedWeekly", "generateConsolidatedXlsx", "generateConsolidatedDocx", "generateConsolidatedPptx"]
    - path: "lib/repositories/weekly-export.repo.ts"
      provides: "insertWeeklyExportLog append-only (D-09)"
      exports: ["insertWeeklyExportLog"]
    - path: "lib/services/weekly-tracking.service.ts"
      provides: "exportConsolidatedWeekly orchestration (D-07, D-09)"
      exports: ["exportConsolidatedWeekly"]
    - path: "app/api/weekly-periods/[periodId]/export/route.ts"
      provides: "POST withCpmo binary pack (D-07, D-11)"
  key_links:
    - from: "lib/services/weekly-tracking.service.ts"
      to: "lib/export/consolidated-weekly.ts"
      via: "exportConsolidatedWeekly builds payload via assembleSnapshotSections then generateConsolidatedWeekly(payload, format) (D-07, D-08)"
      pattern: "generateConsolidatedWeekly"
    - from: "lib/services/weekly-tracking.service.ts"
      to: "lib/repositories/weekly-export.repo.ts"
      via: "after Buffer success, insertWeeklyExportLog then auditLog action weekly_export (D-09)"
      pattern: "insertWeeklyExportLog"
    - from: "app/api/weekly-periods/[periodId]/export/route.ts"
      to: "lib/services/weekly-tracking.service.ts"
      via: "POST returns NextResponse(buffer) with format Content-Type and Content-Disposition (D-07)"
      pattern: "exportConsolidatedWeekly"
  prohibitions:
    - calling the v1 weekly Excel route or the activity-weighted report helper (D-01, D-07)
    - live RAID / live issue list helpers inside lib/export/consolidated-weekly.ts (D-01, D-08)
    - generateProjectPlan / generateWordDoc / generateKickoffPPT as the pack entry point (D-07)
    - UPDATE or row-removal helpers on weekly_export_logs (D-09)
    - writing projects.progress_pct (D-10 analog from Phase 13)
    - a second write-gate wrapper family (D-11)
    - adding npm packages
    - UI-SPEC or portfolio redesign (D-12)
    - persisted export rank table (D-06)
---

<objective>
Deliver the editable consolidated pack and export record (D-01, D-07, D-08, D-09, D-11, D-14, CPMO-03, CPMO-04): xlsx primary plus docx and pptx from the 14-02 snapshot payload; append-only weekly_export_logs plus auditLog weekly_export.

Purpose: TDD payload-driven generators (existing exceljs/docx/pptxgenjs, new functions) so the pack cannot regress into live project-id export.

Output: Working, tested POST export returning a Buffer and an export log row.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-RESEARCH.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-PATTERNS.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-VALIDATION.md
@.planning/phases/14-cpmo-tracking-consolidated-export/14-02-PLAN.md

14-02 exported assertExportEligible and assembleSnapshotSections. weekly_export_logs already exists from 14-01 DDL. This plan owns generators, insertWeeklyExportLog, and POST export.

Discretion locked by planner: generateConsolidatedWeekly(payload, format) dispatches the three format functions; xlsx sheet Portfolio Summary then one sheet per project named from sanitized project_code (Excel 31-char safe unique names); Word uses docx Document + Packer.toBuffer with Heading 1 = period.display_name and Heading 2 per project covering every D-08 field including full snapshot RAID tables; PPT uses pptxgenjs with a cover slide (period, project count, data_version) then per project enough slides to include all D-08 fields and all snapshot RAID/tech rows (do not drop rows); filename `{sanitized display_name} consolidated.{ext}`; Content-Type map xlsx spreadsheetml.sheet, docx wordprocessingml.document, pptx presentationml.presentation; auditLog entity_type weekly_period, entity_id String(periodId), action weekly_export, before null, after { format, data_version, project_ids }; styling helpers may be copied from lib/export/excel.ts / word.ts / ppt.ts but those project-id entry points must not be called.

Do not redesign v1 export routes. Incremental auditLog on export is in scope (D-09); full audit coverage is Phase 18.

<interfaces>
ConsolidatedWeeklyPayload: { period: { id, display_name, iso_week, due_at }, data_version: number, sections: return type of assembleSnapshotSections }.

generateConsolidatedWeekly(payload, format): Promise&lt;Buffer&gt; for xlsx | docx | pptx. Pure payload in; no getDb, no repos, no access asserts.

insertWeeklyExportLog({ period_id, company_id, exported_by, format, data_version, project_ids, period_display_name }): INSERT RETURNING id; analog lib/repositories/audit.repo.ts (jsonb bind via JSON.stringify). No update/remove exports.

exportConsolidatedWeekly(companyId, periodId, actor, body): same gate as preview; assertExportEligible; assembleSnapshotSections; data_version = Math.max of included latest_version; buffer = generateConsolidatedWeekly; insertWeeklyExportLog; auditLog weekly_export; return { buffer, filename, contentType }. Generate the Buffer before the log insert so a generator failure does not write a log row.

periodExportSchema = z.object({ project_ids: z.array(z.number().int().positive()).min(1), format: z.enum(['xlsx','docx','pptx']) }).

POST /api/weekly-periods/[periodId]/export = withCpmo + schema; analog app/api/export/excel/[id]/route.ts binary NextResponse. Isolation none. Repo tests skipIf(!hasTestDb); TEST_DATABASE_URL must end in _test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="14-03-01">
  <name>Snapshot payload xlsx, docx, and pptx generators</name>
  <files>lib/export/consolidated-weekly.ts, lib/export/consolidated-weekly.unit.test.ts</files>
  <read_first>
  - .planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md D-07 D-08
  - lib/export/excel.ts palette / header helpers (copy style only)
  - lib/export/word.ts Packer.toBuffer pattern
  - lib/export/ppt.ts pptx.write arraybuffer pattern
  - lib/export/excel.unit.test.ts Buffer assertion analog (do not call generateProjectPlan)
  </read_first>
  <behavior>
  - generateConsolidatedXlsx(payload) returns a Buffer; workbook has a sheet named Portfolio Summary and one sheet per section in payload order (D-07).
  - generateConsolidatedDocx and generateConsolidatedPptx return Buffers; D-08 field labels (identity, PM, stage, prior/current RAG, progress, highlights, next-week goals, nearest milestone, RAID, technology issues) appear in the unpacked or stringified output for a fixture snapshot (D-08, CPMO-04).
  - A partial snapshot (missing highlights and raid) still returns a Buffer without calling any repository (D-08).
  - generateConsolidatedWeekly dispatches the three formats (D-07).
  </behavior>
  <action>
  TDD. Isolation none.
  RED: failing lib/export/consolidated-weekly.unit.test.ts covering Buffer return, xlsx sheet names via ExcelJS read, and D-08 headings for all three formats using an in-memory fixture payload (no repo mocks required). Commit `test(14-03): red consolidated pack generators`.
  GREEN: implement generateConsolidatedWeekly and the three format functions per interfaces. Commit `feat(14-03): snapshot xlsx docx pptx packs`.

  Do not import repositories or weekly-reports.service into this module (D-01). Do not call existing project-id generators as the implementation (D-07). Copy colors/header helpers if useful. Include every snapshot RAID row and tech-issue row in Word/PPT (D-08). Sanitize Excel sheet names to 31 characters and uniquify collisions.
  </action>
  <verify>
    <automated>npx vitest run lib/export/consolidated-weekly.unit.test.ts</automated>
  </verify>
  <done>xlsx, docx, and pptx packs are produced from a snapshot payload with D-08 sections and no live RAID reads (D-01, D-07, D-08, CPMO-03, CPMO-04).</done>
  <acceptance_criteria>
  - generateConsolidatedWeekly is exported and returns a Buffer for xlsx, docx, and pptx
  - xlsx includes Portfolio Summary plus per-project sheets
  - generator module has no repository imports
  - npx vitest run lib/export/consolidated-weekly.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="14-03-02">
  <name>Export log, audit, and POST export route</name>
  <files>lib/repositories/weekly-export.repo.ts, lib/repositories/weekly-export.repo.test.ts, lib/services/weekly-tracking.service.ts, lib/services/weekly-tracking.service.unit.test.ts, app/api/weekly-periods/[periodId]/export/route.ts, app/api/weekly-periods/[periodId]/export/schema.ts, app/api/weekly-periods/[periodId]/export/route.test.ts</files>
  <read_first>
  - lib/services/weekly-tracking.service.ts from 14-02
  - lib/repositories/audit.repo.ts insert analog
  - lib/services/audit.service.ts auditLog
  - app/api/export/excel/[id]/route.ts binary response
  - .planning/phases/14-cpmo-tracking-consolidated-export/14-CONTEXT.md D-07 D-09 D-11 D-14
  </read_first>
  <behavior>
  - exportConsolidatedWeekly calls generateConsolidatedWeekly then insertWeeklyExportLog then auditLog with action weekly_export; data_version is the max latest_version among included shells (D-09, CPMO-04).
  - Two sequential successful exports result in two insertWeeklyExportLog calls (append-only, D-14).
  - Ineligible project_ids still throw SubmitValidationError and do not insert a log row (D-06, D-14).
  - HTTP POST as CPMO returns 200 with the xlsx Content-Type and attachment filename; PM/viewer 403 (D-07, D-11).
  </behavior>
  <action>
  TDD. RED: failing service tests for log+audit ordering, data_version max, skipped log on ineligible, and route tests for 403 plus 200 Content-Type. Commit `test(14-03): red export log and POST`.
  GREEN: implement insertWeeklyExportLog, exportConsolidatedWeekly, and the POST route per interfaces. Mock generateConsolidatedWeekly, insertWeeklyExportLog, and auditLog in the service unit test. skipIf(!hasTestDb) repo test inserts a row and reads project_ids JSON back. Commit `feat(14-03): weekly export logs and POST pack`.

  Reuse assertExportEligible and assembleSnapshotSections from 14-02. Do not add row-removal or update helpers on the log table (D-09). Do not write projects.progress_pct. Do not call the v1 weekly Excel route (D-07). Concurrent exports are allowed (D-14).
  </action>
  <verify>
    <automated>npx vitest run lib/services/weekly-tracking.service.unit.test.ts lib/repositories/weekly-export.repo.test.ts "app/api/weekly-periods/[periodId]/export/route.test.ts"</automated>
  </verify>
  <done>Successful CPMO export returns an editable pack, records period/data_version/exporter, and appends auditLog weekly_export without mutating snapshots (D-07, D-09, D-14, CPMO-03, CPMO-04).</done>
  <acceptance_criteria>
  - insertWeeklyExportLog is append-only INSERT
  - exportConsolidatedWeekly calls auditLog with action weekly_export after a successful Buffer
  - POST sets Content-Disposition attachment and the matching format Content-Type
  - ineligible ids do not insert a log row
  - npx vitest run lib/services/weekly-tracking.service.unit.test.ts lib/repositories/weekly-export.repo.test.ts "app/api/weekly-periods/[periodId]/export/route.test.ts" exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST export body (project_ids, format) | Untrusted ids and format enum; must not export another company's snapshots |
| Generator → Buffer | In-memory only; must not fetch live masters |
| Service → weekly_export_logs INSERT | Append-only audit record; must not rewrite history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-10 | Elevation of Privilege | exportConsolidatedWeekly | high | mitigate | withCpmo plus assertCompanyWrite plus 14-02 eligibility (D-11, D-14, ASVS V4) |
| T-14-11 | Tampering | consolidated-weekly generators | high | mitigate | Payload-only module; no live RAID imports; D-08 fields from assembleSnapshotSections (D-01, D-08) |
| T-14-12 | Repudiation | weekly_export_logs / auditLog | medium | mitigate | Dual append-only record with exported_by, data_version, project_ids (D-09, ASVS V4 at L1) |
| T-14-13 | Information Disclosure | export of draft shells | high | mitigate | Same submitted + version gate as preview (D-06) |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages; use existing exceljs, docx, pptxgenjs |
| T-14-14 | Denial of Service | concurrent export | low | accept | Append-only log; no unique (period, user) constraint by design (D-14) |
</threat_model>

<verification>
- 14-03-01: consolidated-weekly unit tests green for three formats
- 14-03-02: export service + repo skipIf + POST route green
- Wave: npx vitest run lib/export/consolidated-weekly.unit.test.ts lib/services/weekly-tracking.service.unit.test.ts lib/repositories/weekly-export.repo.test.ts "app/api/weekly-periods/[periodId]/export/route.test.ts" "app/api/weekly-periods/[periodId]/export/preview/route.test.ts"
</verification>

<success_criteria>
CPMO exports an editable xlsx/docx/pptx pack built from submitted snapshots, with a weekly_export_logs row and auditLog weekly_export recording period, data version, and exporter (CPMO-03, CPMO-04, D-07, D-08, D-09).
</success_criteria>

<output>
Create `.planning/phases/14-cpmo-tracking-consolidated-export/14-03-SUMMARY.md` when done
</output>
