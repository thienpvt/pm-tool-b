---
phase: 18-append-only-audit-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/repositories/audit.repo.ts
  - lib/repositories/audit.repo.unit.test.ts
  - lib/repositories/audit.repo.test.ts
  - lib/services/audit.service.ts
  - lib/services/audit.service.unit.test.ts
  - app/api/audit/route.ts
  - app/api/audit/route.test.ts
autonomous: true
requirements: [AUDIT-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO GET /api/audit returns only rows whose company_id equals the actor company; Viewer and PM receive 403; null-company CPMO receives 403 via assertCompanyWrite (D-05, D-06, AUDIT-01)."
    - "listAuditLogs SQL always anchors company_id = the actor company; foreign-company rows and NULL-company rows are absent from the result (D-05)."
    - "audit.repo exports insertAuditLog and listAuditLogs only; the module source has no mutating SQL against the audit table (D-01, D-04)."
    - "The audit HTTP module exports GET only (D-04)."
    - "After two inserts for the same entity_id, list returns both rows and the first row actor_id, created_at, before, and after are unchanged (D-07)."
    - "GET accepts optional entity_type, entity_id, from, to; limit defaults to 50 and is capped at 200 (D-06)."
    - "insertAuditLog signature and INSERT path are unchanged; no second audit table and no settings-flag migrate (D-01, D-10)."
  artifacts:
    - path: "lib/repositories/audit.repo.ts"
      provides: "listAuditLogs SELECT plus existing insertAuditLog (D-01, D-04, D-05)"
      exports: ["insertAuditLog", "listAuditLogs", "AuditLogInput", "AuditListFilters", "AuditLogRow"]
    - path: "lib/services/audit.service.ts"
      provides: "auditLog INSERT wrapper plus listAuditLogs with assertCompanyWrite and limit clamp (D-05, D-06)"
      exports: ["auditLog", "listAuditLogs"]
    - path: "app/api/audit/route.ts"
      provides: "GET withCpmo only (D-06)"
    - path: "lib/repositories/audit.repo.unit.test.ts"
      provides: "Source-scan immutability plus company-scoped SELECT (D-04, D-05)"
    - path: "lib/repositories/audit.repo.test.ts"
      provides: "D-07 two-row persistence against TEST_DATABASE_URL"
    - path: "app/api/audit/route.test.ts"
      provides: "Auth matrix, pagination, GET-only module shape (D-04, D-06)"
  key_links:
    - from: "app/api/audit/route.ts"
      to: "lib/services/audit.service.ts"
      via: "GET calls listAuditLogs(actor, filters) (D-06)"
      pattern: "listAuditLogs"
    - from: "lib/services/audit.service.ts"
      to: "lib/services/access.ts"
      via: "assertCompanyWrite before repo list (D-05)"
      pattern: "assertCompanyWrite"
    - from: "lib/services/audit.service.ts"
      to: "lib/repositories/audit.repo.ts"
      via: "listAuditLogsRepo(actor.company_id, filters) (D-05)"
      pattern: "listAuditLogs"
  prohibitions:
    - inventing a second audit table or changing insertAuditLog columns (D-01)
    - adding mutating SQL helpers on the audit table (D-04)
    - exporting non-GET handlers from app/api/audit/route.ts (D-04)
    - adding lib/db-audit.ts or wiring a settings-flag migrate (D-10)
    - adding npm packages (D-09)
    - CASL or leftover ops/admin re-gating (D-09)
    - UI-SPEC or treating a thin page as a gate (D-08)
    - filling D-02 service gaps (18-02, 18-03)
---

<objective>
Prove append-only, company-scoped audit read end-to-end. D-01: keep audit_logs and the existing INSERT path. D-04: repo INSERT plus SELECT only. D-05/D-06: GET /api/audit withCpmo plus assertCompanyWrite, company_id filter, limit 50 max 200. D-07: two-row persistence. AUDIT-01 read and immutability.

Purpose: Catch a tenant-leak or mutating-SQL dead-end on the first commit before gap-filling mutators.

Output: Production-quality tracer (list SELECT + GET + immutability scan) plus expansions for filters, pagination, D-07, and the auth matrix. Gap fills are 18-02 and 18-03.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-append-only-audit-log/18-CONTEXT.md
@.planning/phases/18-append-only-audit-log/18-RESEARCH.md
@.planning/phases/18-append-only-audit-log/18-PATTERNS.md
@.planning/phases/18-append-only-audit-log/18-VALIDATION.md

Discretion locked by planner: keep table name audit_logs; do not backfill NULL company_id; skip new index; service export name listAuditLogs (not a second wrapper name). Isolation none.

Do not implement deferred items: UI-SPEC, ops/admin leftover audit, unified raid entity_type.

Prior-phase artifact: insertAuditLog and auditLog already exist; withCpmo in lib/http/with-role.ts; assertCompanyWrite in lib/services/access.ts; audit_logs DDL in lib/db-roles.ts and test/repo-db.ts.

<interfaces>
insertAuditLog(input: AuditLogInput): Promise&lt;void&gt; — do not change columns, placeholders, or JSON stringify of before/after.

AuditListFilters: optional entity_type, entity_id, from (YYYY-MM-DD), to (YYYY-MM-DD), limit (number).

AuditLogRow: id, company_id, actor_id, entity_type, entity_id, action, before, after, created_at.

listAuditLogs(companyId: number, filters?: AuditListFilters): Promise&lt;AuditLogRow[]&gt; in audit.repo.ts. Always conditions = ['company_id = ?'] with params starting [companyId]. Optional AND entity_type = ?, entity_id = ?, created_at &gt;= ?::date, created_at &lt; (?::date + INTERVAL '1 day') for inclusive to-date. ORDER BY created_at DESC, id DESC. LIMIT ? using the already-clamped limit. NULL company_id rows excluded by equality. Analog lib/repositories/users.repo.ts listUsers and lib/repositories/import-mapping.repo.unit.test.ts company_id predicate.

Service listAuditLogs(actor, filters = {}): call assertCompanyWrite(actor) then listAuditLogsRepo(actor.company_id!, { ...filters, limit: clampLimit(filters.limit) }). Clamp: if limit is missing, NaN, or &lt; 1, use 50; then Math.min(floored, 200). Invalid from/to (present but not YYYY-MM-DD) throw ValidationError using parseIsoDate from lib/fiscal/iso-date.ts with field names from and to. Empty or omitted from/to stay undefined.

GET /api/audit = withCpmo analog app/api/dashboards/portfolio/route.ts plus query parse analog app/api/admin/users/route.ts. Parse entity_type, entity_id, from, to, limit from searchParams. Delegate to listAuditLogs. Return NextResponse.json(rows). Export GET only.

assertCompanyWrite: if !isCpmo throw ForbiddenError; if actor.company_id === null throw ForbiddenError.

Route tests analog app/api/dashboards/portfolio/route.test.ts: mock @/lib/auth getSessionFromRequest and @/lib/services/audit.service listAuditLogs. Sessions: cpmo company_id 5; pm; viewer; { is_admin: 1, company_id: null, roles: [] }; plus cpmo with company_id null and roles ['cpmo'] for the assertCompanyWrite path. jsonReq url http://localhost/api/audit.

Isolation none. TEST_DATABASE_URL for skipIf(!hasTestDb) must end in _test. Prefer mocked getDb in repo unit tests.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true" id="18-01-01">
  <name>End-to-end CPMO GET /api/audit plus append-only list</name>
  <precondition>lib/repositories/audit.repo.ts exports insertAuditLog; lib/http/with-role.ts exports withCpmo; lib/services/access.ts exports assertCompanyWrite; audit_logs exists in lib/db-roles.ts</precondition>
  <reversibility rating="costly">GET /api/audit plus listAuditLogs become the published company-scoped read contract; replacing the table later would force a dual-write migration (D-01).</reversibility>
  <files>lib/repositories/audit.repo.ts, lib/repositories/audit.repo.unit.test.ts, lib/services/audit.service.ts, lib/services/audit.service.unit.test.ts, app/api/audit/route.ts, app/api/audit/route.test.ts</files>
  <read_first>
  - .planning/phases/18-append-only-audit-log/18-CONTEXT.md (D-01, D-04, D-05, D-06)
  - .planning/phases/18-append-only-audit-log/18-PATTERNS.md (listUsers analog, withCpmo GET, source-scan)
  - lib/repositories/audit.repo.ts
  - lib/services/audit.service.ts
  - lib/services/audit.service.unit.test.ts
  - lib/repositories/users.repo.ts listUsers
  - lib/http/with-role.ts withCpmo
  - lib/services/access.ts assertCompanyWrite
  - app/api/dashboards/portfolio/route.ts
  - app/api/dashboards/portfolio/route.test.ts
  - app/api/admin/users/route.ts GET
  - lib/repositories/import-mapping.repo.unit.test.ts
  </read_first>
  <behavior>
  - listAuditLogs(5) issues SQL containing company_id = ? with first bound param 5; it does not return a row whose company_id is 9 (D-05, AUDIT-01).
  - listAuditLogs service calls assertCompanyWrite then the repo with actor.company_id; actor company_id null throws ForbiddenError and does not call the repo (D-05, D-06).
  - HTTP GET /api/audit as CPMO company_id 5 returns 200 and the mocked list payload; missing session returns 401; PM and Viewer return 403 without calling listAuditLogs (D-06).
  - audit.repo.unit.test.ts source-scan: module text has no mutating SQL against the audit table; exported names include insertAuditLog and listAuditLogs and do not include updateAuditLog or deleteAuditLog (D-04).
  - Existing auditLog still delegates only to insertAuditLog with the same AuditLogInput (D-01).
  </behavior>
  <action>
  TDD first (D-08). Isolation is none — keep this plan sequential-safe on the main tree.
  RED: add failing lib/repositories/audit.repo.unit.test.ts for company-scoped SELECT (mock getDb like import-mapping.repo.unit.test.ts) and for the source-scan immutability contract (readFileSync of audit.repo.ts, analog lib/export/consolidated-weekly.unit.test.ts). Add failing listAuditLogs cases to lib/services/audit.service.unit.test.ts (mock listAuditLogs from the repo; use real assertCompanyWrite). Add failing app/api/audit/route.test.ts CPMO 200, 401, PM 403, Viewer 403. Tests must fail before listAuditLogs and the route exist. Commit `test(18-01): red GET /api/audit and append-only list`.
  GREEN: implement listAuditLogs in the repo and service and GET in app/api/audit/route.ts per interfaces. Do not change insertAuditLog SQL. Do not add a settings-flag migrate (D-10). Do not add npm packages. Mock getDb in repo unit tests; mock listAuditLogs in route tests like portfolio/route.test.ts mocks getPortfolioDashboard.
  Commit `feat(18-01): company-scoped GET /api/audit`.
  Tracer filters and D-07 may remain unimplemented (unfiltered list plus default limit is enough for the happy path); task 18-01-02 completes those. Tracer GET still calls listAuditLogs so the read seam exists (D-06).
  Do not wrap the route with a policy engine (D-09). Do not add pages or UI-SPEC (D-08). Do not edit leftover ops/admin routes (D-09). Do not gap-fill project/RAID/milestone/checklist mutators.
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/audit.repo.unit.test.ts lib/services/audit.service.unit.test.ts app/api/audit/route.test.ts</automated>
  </verify>
  <done>CPMO can GET /api/audit for their company; PM and Viewer cannot; the repo list is company-scoped; insert path is unchanged; the repo has no mutating SQL helpers (D-01, D-04, D-05, D-06, AUDIT-01).</done>
  <acceptance_criteria>
  - GET handler returns 200 for CPMO company_id 5 and 403 for PM and Viewer
  - listAuditLogs repo SQL includes company_id = ? bound to the actor company
  - audit.repo.unit.test.ts source-scan passes (D-04)
  - auditLog still calls insertAuditLog with the same input
  - npx vitest run lib/repositories/audit.repo.unit.test.ts lib/services/audit.service.unit.test.ts app/api/audit/route.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="18-01-02">
  <name>Filters, limit cap, and two-row persistence</name>
  <files>lib/repositories/audit.repo.ts, lib/repositories/audit.repo.unit.test.ts, lib/repositories/audit.repo.test.ts, lib/services/audit.service.ts, lib/services/audit.service.unit.test.ts</files>
  <read_first>
  - lib/repositories/audit.repo.ts from 18-01-01
  - lib/services/audit.service.ts from 18-01-01
  - lib/fiscal/iso-date.ts parseIsoDate
  - test/repo-db.ts setupRepoTables seedCompany audit_logs DDL
  - lib/repositories/weekly-reports.repo.test.ts skipIf(!hasTestDb)
  - .planning/phases/18-append-only-audit-log/18-CONTEXT.md D-06 D-07
  </read_first>
  <behavior>
  - listAuditLogs with entity_type and entity_id adds those predicates; with from/to uses inclusive calendar-day bounds on created_at (D-06).
  - Service clamp: omitted or invalid limit becomes 50; 201 becomes 200; 1 stays 1 (D-06).
  - Invalid from or to (not YYYY-MM-DD) throws ValidationError and does not call the repo (D-06).
  - Two insertAuditLog calls for the same entity_id then listAuditLogs: both rows present; the earlier row actor_id, created_at, before, and after match the first insert (D-07, AUDIT-01).
  </behavior>
  <action>
  TDD (D-08). RED: failing repo unit tests for optional filters and LIMIT bound to the clamped value; failing service tests for clamp 50/200 and ValidationError on bad from/to; failing lib/repositories/audit.repo.test.ts describe.skipIf(!hasTestDb) that inserts two rows via insertAuditLog (seed a company plus a users row for the actor_id FK) then listAuditLogs and asserts the first payload is unchanged after the second insert (D-07). Commit `test(18-01): red audit filters and two-row persistence`.
  GREEN: implement filter predicates and LIMIT in the repo; clamp and parseIsoDate in the service. Keep insertAuditLog returning void. Hermetic D-07: mock getDb with an in-memory row array so insert then list proves the first row is not replaced. Integration D-07 uses hasTestDb. Commit `feat(18-01): audit list filters and append-only persistence`.
  Do not add a migrate module. Do not change INSERT columns.
  </action>
  <verify>
    <automated>npx vitest run lib/repositories/audit.repo.unit.test.ts lib/repositories/audit.repo.test.ts lib/services/audit.service.unit.test.ts</automated>
  </verify>
  <done>Filters and limit cap work; two inserts leave the first audit row readable unchanged (D-06, D-07, AUDIT-01).</done>
  <acceptance_criteria>
  - default limit 50 and max 200 are unit-tested
  - entity_type, entity_id, from, to predicates are unit-tested
  - D-07 two-row assertion exists in audit.repo.unit.test.ts and in skipIf(!hasTestDb) audit.repo.test.ts
  - npx vitest run lib/repositories/audit.repo.unit.test.ts lib/repositories/audit.repo.test.ts lib/services/audit.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="18-01-03">
  <name>Auth matrix, GET-only module, cross-company isolation</name>
  <files>app/api/audit/route.ts, app/api/audit/route.test.ts, lib/services/audit.service.unit.test.ts</files>
  <read_first>
  - app/api/audit/route.ts from 18-01-01
  - app/api/dashboards/portfolio/route.test.ts session fixtures
  - lib/services/audit.service.ts listAuditLogs
  </read_first>
  <behavior>
  - GET as null-company admin (roles empty, is_admin 1) returns 403; GET as CPMO with company_id null returns 403; listAuditLogs is not called for the empty-roles admin (D-05, D-06).
  - GET as CPMO company_id 5 with mocked rows never includes a payload whose company_id is 9 (D-05, AUDIT-01).
  - The route module has a GET export and does not export POST, PUT, PATCH, or DELETE (D-04).
  - GET passes entity_type, entity_id, from, to, and numeric limit through to listAuditLogs (D-06).
  </behavior>
  <action>
  TDD. RED: failing route.test.ts cases for null-company admin 403, null-company CPMO 403, cross-company payload absence, query-string filter forwarding, and module shape (import the route module and assert it has GET only). Commit `test(18-01): red audit GET auth matrix`.
  GREEN: keep GET-only; service already throws ForbiddenError for null company_id. Do not add other HTTP verb exports. Commit `feat(18-01): audit GET tenant isolation`.
  Do not add CASL. Do not edit app/api/admin/companies or operations routes (D-09).
  </action>
  <verify>
    <automated>npx vitest run app/api/audit/route.test.ts lib/services/audit.service.unit.test.ts</automated>
  </verify>
  <done>PM, Viewer, and null-company actors cannot read audit rows; GET is the only HTTP export; foreign-company payloads do not leak (D-04, D-05, D-06, D-09, AUDIT-01).</done>
  <acceptance_criteria>
  - route.test.ts covers 401, PM 403, Viewer 403, null-company 403, CPMO 200
  - route module exports GET only
  - mocked foreign-company row never appears in the CPMO JSON body
  - npx vitest run app/api/audit/route.test.ts lib/services/audit.service.unit.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /api/audit | Untrusted actor; must not read another company's audit rows or mutate audit history |
| Service → audit.repo listAuditLogs | Tenant predicate must be applied in SQL, not only in JS after fetch |
| App code → audit_logs | Must not expose in-place edit or row removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | GET /api/audit | high | mitigate | withCpmo plus assertCompanyWrite; SQL WHERE company_id = actor.company_id; route tests for PM/Viewer/null-company 403 and foreign payload absence (D-05, D-06, ASVS V4) |
| T-18-02 | Tampering | audit.repo.ts | high | mitigate | INSERT and SELECT only; source-scan unit test; no other HTTP verbs on the audit route (D-04, ASVS V4) |
| T-18-03 | Elevation of Privilege | GET /api/audit | high | mitigate | withCpmo rejects PM/Viewer; assertCompanyWrite rejects null company_id even when role is cpmo (D-06, ASVS V4) |
| T-18-04 | Denial of Service | listAuditLogs LIMIT | medium | mitigate | Default 50, cap 200 in the service before SQL LIMIT (D-06, ASVS V5) |
| T-18-05 | Information Disclosure | before/after JSON | low | accept | Reuse existing snapshot minimization on mutators; this plan only lists stored JSON (ASVS L1) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 18-01-01: company-scoped list + CPMO GET 200 + immutability source-scan green
- 18-01-02: filters, limit cap, D-07 two-row green
- 18-01-03: auth matrix and GET-only module green
- Wave: npx vitest run lib/repositories/audit.repo.unit.test.ts lib/repositories/audit.repo.test.ts lib/services/audit.service.unit.test.ts app/api/audit/route.test.ts
</verification>

<success_criteria>
CPMO lists only their company's audit rows; other roles and null-company actors are denied; audit rows cannot be updated or deleted through repo or HTTP helpers; a second insert leaves the first row unchanged (AUDIT-01, D-01, D-04, D-05, D-06, D-07).
</success_criteria>

<output>
Create `.planning/phases/18-append-only-audit-log/18-01-SUMMARY.md` when done
</output>
