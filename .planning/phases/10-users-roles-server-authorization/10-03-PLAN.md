---
phase: 10-users-roles-server-authorization
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - lib/services/access.ts
  - lib/services/access.unit.test.ts
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
  - lib/repositories/projects.repo.ts
  - lib/repositories/projects.repo.unit.test.ts
  - app/api/projects/[id]/route.access.test.ts
  - app/api/projects/route.ts
  - lib/repositories/tenant-scope.repo.unit.test.ts
autonomous: true
requirements: [AUTH-04, AUTH-05, USER-03]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "CPMO can view the full company portfolio and cannot read another company's projects (D-13, AUTH-04)."
    - "PM can view and update only assigned projects; unassigned project mutate is 403 (D-14, AUTH-04)."
    - "PM-only nested GET inherits D-14 assignment after tenant match in assertProjectAccess (and therefore withProjectAccess); Viewer-only and CPMO stay company-scoped; pm+viewer union stays company-read (D-14, D-24, AUTH-04)."
    - "Every API enforces authorization on the server for list/create/get/update/delete project — hiding a UI control is not treated as access control (D-15, D-16, AUTH-05)."
    - "Cross-company 403 must not regress (D-16, D-19)."
  artifacts:
    - path: "lib/services/access.ts"
      provides: "isCpmo, assertPmWriteAccess, assertProjectWriteAccess; company-scoped CPMO in assertProjectAccess; PM-only D-14 after tenant on read (D-13, D-14, D-17, D-24)"
      exports: ["isCpmo", "assertPmWriteAccess", "assertProjectWriteAccess", "hasRole"]
    - path: "lib/services/projects.service.ts"
      provides: "listProjects company-scoped for CPMO/Viewer; PM assigned-only; createProject CPMO-only (D-13, D-14)"
    - path: "lib/repositories/projects.repo.ts"
      provides: "listProjects drops global all-rows branch; optional PM assignment filter (D-13, D-14)"
  key_links:
    - from: "lib/services/projects.service.ts"
      to: "lib/services/access.ts"
      via: "list/create use isCpmo/hasRole; update/delete use assertProjectWriteAccess (D-16)"
      pattern: "assertProjectWriteAccess"
    - from: "assertPmWriteAccess"
      to: "projects.pm_email then pm_name"
      via: "interim D-14 match; Phase 11 replaces lookup only"
      pattern: "assertPmWriteAccess"
  prohibitions:
    - restoring a global all-companies list for CPMO or the break-glass flag (D-13)
    - replacing tenant assert with a role-only check (D-16)
    - implementing Phase 11 assignment windows
    - adding npm packages
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Replace v1.0 global admin project access with company-scoped CPMO and interim PM assignment (D-13, D-14, D-16, D-24, AUTH-04): assertProjectAccess no longer treats the break-glass flag as a cross-tenant superuser; after tenant match, PM-only actors run the D-14 matcher so nested list/get inherit assignment; listProjects never returns another company's rows for CPMO; PM lists/writes only D-14 matches; createProject is CPMO-only in the session company.

Purpose: This is the privilege-escalation fix for leftover global bypass and the AUTH-04 portfolio/assignment split. Nested mutators after this plan (10-04, 10-06, 10-07, 10-08) call assertProjectWriteAccess; company writes are 10-09 (D-23 carve-out stays off leftover ops/admin).

Output: access helpers + projects list/create/update/delete behavior + inverted access.unit.test.ts + cross-company regression still green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-RESEARCH.md
@.planning/phases/10-users-roles-server-authorization/10-PATTERNS.md
@.planning/phases/10-users-roles-server-authorization/10-01-PLAN.md

10-01 added hasRole, assertCanMutate, toAccessActor, roles on fixtures. This plan inverts the access.unit.test.ts case that currently allows the admin fixture to read a foreign company row.

Flagged assumption A2 locked as D-14: email first when pm_email is non-empty; otherwise name; do not OR email and name when email is set.

<interfaces>
isCpmo(actor) = hasRole(actor, 'cpmo').

assertProjectAccess: remove the early-return that used the break-glass flag to skip company matching. If isCpmo and actor.company_id is not null, allow when row.company_id or row.customer_company_id equals actor.company_id; else ForbiddenError. Missing row still NotFoundError first. Non-CPMO tenant logic (owner company, null-company unassigned) stays. A CPMO with null company_id is ForbiddenError on assigned projects (same as requireCompanyId spirit). After that tenant match succeeds, if the actor is PM-only (hasRole pm, not hasRole cpmo, not hasRole viewer), run the D-14 matcher (same seam as assertPmWriteAccess) before returning the row (D-14, D-24). Viewer-only and CPMO stay company-scoped after tenant. pm+viewer union stays company-read (has viewer, so skip the matcher on GET). withProjectAccess does not need a second matcher — it already calls assertProjectAccess. Do not add nested-route files for this.

assertPmWriteAccess(projectId, actor): if isCpmo return (tenant already checked). Load project pm_name/pm_email (add a narrow repo helper getProjectPmIdentity(projectId) returning { pm_name, pm_email } or reuse getProject if cheaper). Match D-14. Else ForbiddenError. Viewer-only never reaches this if assertCanMutate ran first.

assertProjectWriteAccess(projectId, actor): await assertProjectAccess; await assertCanMutate(actor); await assertPmWriteAccess(projectId, actor).

listProjects(actor): never pass true into a global all-rows repo branch. CPMO or viewer-only: listProjectsRepo(companyId) company filter (existing non-admin SQL). PM without cpmo: same company filter plus assignment predicate (pm_email/pm_name vs actor). Actor with null company_id: keep today's unassigned-only list, not all tenants.

createProject(actor, body): if not isCpmo throw ForbiddenError. Stamp company from actor.company_id only (ignore body company_id). Null company_id actor ForbiddenError.

updateProject/deleteProject: assertProjectWriteAccess instead of assertProjectAccess only.

getProject: assertProjectAccess only (read). Nested list/get through withProjectAccess inherit the same PM-only D-14 matcher because they call assertProjectAccess. Do not add nested-route files. CPMO and viewer-only GET stay company-scoped tenant only. pm+viewer GET stays company-read.

Export getProjectPmIdentity from projects.repo.ts. Do not add CASL.

projects.repo listProjects(companyId, isAdmin) today. Change signature to drop the boolean global branch: listProjects(companyId: number | null, opts?: { pmEmail?: string; pmName?: string; username?: string }). When opts pm fields provided, AND the D-14 predicate. Update all callers (projects.service only if grep agrees).

Existing tenant-scope.repo.unit.test.ts and projects.service.unit.test.ts must be updated so they no longer expect a global list when the second arg is true.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-03-01">
  <name>Company-scope CPMO in assertProjectAccess</name>
  <files>lib/services/access.ts, lib/services/access.unit.test.ts</files>
  <read_first>
  - lib/services/access.ts
  - lib/services/access.unit.test.ts (admin fixture currently allowed on company 9)
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-13, D-16)
  </read_first>
  <behavior>
  - assertProjectAccess with roles ['cpmo'], company_id 5, against a row company_id 9 throws ForbiddenError not NotFoundError (D-13).
  - assertProjectAccess with roles ['cpmo'], company_id 5, against a row company_id 5 returns the row (D-13).
  - assertProjectAccess with roles ['cpmo'], company_id 5, against customer_company_id 5 returns the row (D-13).
  - Owner fixture roles ['pm'] cross-company still ForbiddenError (D-16, D-19).
  - Missing project still NotFoundError (D-16).
  </behavior>
  <action>
  TDD (D-19): rewrite the access.unit.test.ts case that currently expects the admin fixture to receive a foreign row so it expects ForbiddenError for CPMO company 5 vs company 9. Add the CPMO same-company cases. Then delete the break-glass early-return in assertProjectAccess and implement isCpmo company match (D-13). Keep throw-on-deny. Do not add a second wrapper family (D-17).
  </action>
  <verify>
    <automated>npx vitest run lib/services/access.unit.test.ts -x</automated>
  </verify>
  <done>CPMO is company-scoped; a CPMO in company A cannot open company B's project via assertProjectAccess (D-13, D-16, AUTH-04).</done>
  <acceptance_criteria>
  - access.unit.test.ts includes CPMO foreign ForbiddenError
  - assertProjectAccess has no global superuser early-return
  - npx vitest run lib/services/access.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-03-02">
  <name>Interim PM assignment seam and project list/create</name>
  <files>lib/services/access.ts, lib/services/access.unit.test.ts, lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts, lib/repositories/projects.repo.ts, lib/repositories/projects.repo.unit.test.ts, lib/repositories/tenant-scope.repo.unit.test.ts</files>
  <read_first>
  - lib/services/projects.service.ts (listProjects, createProject, updateProject)
  - lib/repositories/projects.repo.ts (listProjects isAdmin branch, PROJECT_COLUMNS pm_name pm_email)
  - lib/services/access.ts after 10-03-01
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-14)
  </read_first>
  <behavior>
  - assertPmWriteAccess: CPMO returns without matching pm_* (D-13, D-14).
  - assertPmWriteAccess: PM whose email equals pm_email (case-insensitive) returns; PM whose email differs and name does not match throws ForbiddenError (D-14).
  - When pm_email is non-empty, a matching display_name alone does not grant write (D-14 email-first).
  - When pm_email is empty, trim+lower pm_name matching display_name or username grants write (D-14).
  - listProjects for CPMO uses company filter and never the old all-rows branch (D-13).
  - listProjects for PM without cpmo returns only D-14-matching rows in that company (D-14).
  - createProject for PM or viewer-only throws ForbiddenError; createProject for CPMO stamps actor.company_id (D-13, D-15).
  - updateProject/deleteProject use assertProjectWriteAccess (AUTH-04).
  - PM-only assertProjectAccess on an unassigned in-company project throws ForbiddenError after tenant match (nested GET inheritance via withProjectAccess; D-14, D-24).
  - Viewer-only and CPMO assertProjectAccess stay company-scoped after tenant; pm+viewer does not run the D-14 matcher on GET (D-14, D-24).
  </behavior>
  <action>
  TDD (D-19): add assertPmWriteAccess and list/create unit tests first, plus a PM-only assertProjectAccess unassigned ForbiddenError case (the nested GET inheritance proof — do not add a nested-route file). Implement getProjectPmIdentity. After tenant match in assertProjectAccess, if the actor is PM-only (has pm, not cpmo, not viewer), run the D-14 matcher before returning the row (D-14, D-24). Viewer-only, CPMO, and pm+viewer skip that matcher on GET. Change listProjects repo to drop the global branch (D-13). Service listProjects passes company id and, for PM-only, actor.email / display_name / username. createProject ignores body company_id. Export assertProjectWriteAccess and use it on update/delete. Document in a one-line comment on assertPmWriteAccess that Phase 11 replaces the lookup, not the function name (D-14). Mock getProjectPmIdentity in access.unit.test.ts.

  Update projects.service.unit.test.ts mocks: export the new asserts; stop expecting listProjectsRepo(company, true). Update lib/repositories/tenant-scope.repo.unit.test.ts so listProjects calls match the new signature (no boolean global-all-rows second argument).
  </action>
  <verify>
    <automated>npx vitest run lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts lib/repositories/projects.repo.unit.test.ts lib/repositories/tenant-scope.repo.unit.test.ts -x</automated>
  </verify>
  <done>PM write/list/GET is interim D-14 assignment (nested GET inherits via assertProjectAccess); CPMO lists the company portfolio only; createProject is CPMO-stamped to the session company (D-13, D-14, D-24, AUTH-04).</done>
  <acceptance_criteria>
  - assertPmWriteAccess is exported from access.ts
  - listProjects repo has no all-rows admin branch
  - createProject requires isCpmo
  - Email-first D-14 test exists
  - PM-only assertProjectAccess on unassigned in-company project expects ForbiddenError (D-14, D-24 nested GET)
  - npx vitest run lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts lib/repositories/projects.repo.unit.test.ts lib/repositories/tenant-scope.repo.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-03-03">
  <name>Keep cross-company 403 on project GET/PATCH</name>
  <files>app/api/projects/[id]/route.access.test.ts, app/api/projects/route.ts</files>
  <read_first>
  - app/api/projects/[id]/route.access.test.ts
  - app/api/projects/route.ts
  - lib/services/projects.service.ts after 10-03-02
  </read_first>
  <behavior>
  - GET /api/projects/:id with a foreign-company session still returns 403 { error: 'Forbidden' } (D-16, D-19).
  - PATCH /api/projects/:id with a viewer-only in-company session returns 403 (D-15).
  - PATCH /api/projects/:id with a PM in-company session that fails D-14 returns 403 (D-14).
  - Existing 401 no-session tests still pass.
  </behavior>
  <action>
  TDD (D-19): extend route.access.test.ts session fixtures with roles, status, email, display_name, username (toAccessActor fields). Add Viewer PATCH 403 and PM-unassigned PATCH 403. Keep foreign GET 403. If GET/PATCH handlers need no route edits because services now throw, only tests change. If route still constructs a two-field actor, switch to toAccessActor / withProjectAccess ctx.actor from 10-01.
  </action>
  <verify>
    <automated>npx vitest run app/api/projects/[id]/route.access.test.ts lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts -x</automated>
  </verify>
  <done>Cross-company 403 still holds; Viewer and unassigned PM cannot PATCH a project (D-14, D-15, D-16, D-19, AUTH-05).</done>
  <acceptance_criteria>
  - Foreign GET still expects 403 Forbidden
  - Viewer PATCH 403 case exists
  - PM unassigned PATCH 403 case exists
  - npx vitest run app/api/projects/[id]/route.access.test.ts lib/services/access.unit.test.ts lib/services/projects.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccessActor.roles → assertProjectAccess | CPMO must not become a global reader |
| PM actor → project write | Text pm_* match is interim; still after tenant check |
| POST /api/projects body.company_id | Client must not pick another tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Elevation of Privilege | assertProjectAccess former bypass | high | mitigate | Company match for CPMO; unit test foreign ForbiddenError (D-13) |
| T-10-10 | Elevation of Privilege | listProjects former all-rows branch | high | mitigate | Remove global list; CPMO uses company WHERE (D-13) |
| T-10-11 | Elevation of Privilege | PM unassigned write or nested GET | high | mitigate | assertPmWriteAccess D-14 after tenant; PM-only assertProjectAccess on GET (D-16, D-24) |
| T-10-12 | Tampering | createProject body company_id | high | mitigate | Stamp actor.company_id only (D-13) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-03-01: CPMO foreign deny
- 10-03-02: D-14 + list/create
- 10-03-03: route.access matrix slice
- Also run existing app/api/projects/[id]/route.access.test.ts full file
</verification>

<success_criteria>
CPMO sees only their company portfolio; PM sees/writes assigned projects via D-14 including nested GET; Viewer cannot PATCH; cross-company 403 remains (AUTH-04, AUTH-05, USER-03 union via hasRole).
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-03-SUMMARY.md` when done
</output>
