---
phase: 10-users-roles-server-authorization
plan: 09
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - lib/services/access.ts
  - lib/services/access.unit.test.ts
  - lib/services/programs.service.ts
  - lib/services/programs.service.unit.test.ts
  - lib/repositories/programs.repo.ts
  - lib/repositories/programs.repo.test.ts
  - app/api/programs/[id]/project-allocations/route.ts
  - app/api/programs/[id]/project-allocations/route.access.test.ts
  - lib/services/import-mapping.service.ts
  - lib/services/import-mapping.service.unit.test.ts
  - lib/services/portfolio.service.ts
  - lib/services/portfolio.service.unit.test.ts
  - lib/repositories/portfolio.repo.ts
  - lib/repositories/portfolio.repo.test.ts
  - lib/services/roadmap.service.ts
  - lib/services/roadmap.service.unit.test.ts
  - lib/services/jira-mapping.service.ts
  - lib/services/jira-mapping.service.unit.test.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Company-scoped writes (programs, portfolio, import-mapping, jira-mapping, program project-allocations POST) are CPMO-only; Viewer and PM receive ForbiddenError (D-13, D-15, AUTH-05)."
    - "CPMO company 5 cannot read company 9 programs or portfolio/roadmap rows even when the leftover break-glass flag is set (D-03, D-13, AUTH-04)."
    - "Role checks compose with tenant checks — program/company assert still runs first; assertCompanyWrite does not re-check resource company_id so tenant assert must already be company-scoped (D-16)."
  artifacts:
    - path: "lib/services/access.ts"
      provides: "assertCompanyWrite for company-scoped mutators (D-13, D-17)"
      exports: ["assertCompanyWrite"]
    - path: "lib/services/programs.service.ts"
      provides: "assertProgramAccess company match; create/update/delete then assertCompanyWrite; createProgram stamps actor.company_id (D-13, D-16)"
    - path: "lib/services/portfolio.service.ts"
      provides: "getPortfolioSummary company-scoped lists; writes call tenant then assertCompanyWrite (D-13, D-16)"
    - path: "lib/services/jira-mapping.service.ts"
      provides: "Writes call tenant then assertCompanyWrite (D-16)"
    - path: "app/api/programs/[id]/project-allocations/route.ts"
      provides: "POST: withProgramAccess then assertProjectAccess then assertCompanyWrite (D-15, AUTH-05)"
  key_links:
    - from: "lib/services/programs.service.ts mutators"
      to: "lib/services/access.ts"
      via: "tenant/program assert then assertCompanyWrite (D-16)"
      pattern: "assertCompanyWrite"
    - from: "assertProgramAccess"
      to: "customers.company_id"
      via: "same company match as 10-03 assertProjectAccess; leftover break-glass flag is not an early-return (D-13)"
      pattern: "assertProgramAccess"
    - from: "POST /api/programs/[id]/project-allocations"
      to: "lib/services/access.ts"
      via: "program tenant + project tenant then assertCompanyWrite (D-15, D-16, AUTH-05)"
      pattern: "assertCompanyWrite"
    - from: "lib/services/portfolio.service.ts mutators"
      to: "lib/services/access.ts"
      via: "tenant then assertCompanyWrite (D-16)"
      pattern: "assertCompanyWrite"
  prohibitions:
    - skipping tenant assert because a role check exists (D-16)
    - using the break-glass flag for these product writes (D-03)
    - reintroducing global CPMO bypass (D-13)
    - adding npm packages
    - adding CPMO/PM/Viewer write asserts on app/api/operations/** or platform /api/admin/companies (D-23 AUTH-05 carve-out)
---

<objective>
Gate company-scoped product reads and writes (D-03, D-13, D-15, D-16, AUTH-04, AUTH-05): export assertCompanyWrite; replace assertProgramAccess leftover-flag early-return with company match; stop using the leftover flag on portfolio/program/roadmap lists; stamp createProgram from actor.company_id; POST program project-allocations after tenant asserts then assertCompanyWrite.

Purpose: After 10-01 backfill, CPMOs still have the leftover flag set. 10-03 removed the project-list bypass; programs/portfolio/roadmap still treated that flag as a cross-tenant superuser. assertCompanyWrite after a bypassed tenant assert cannot recover the resource company.

Output: assertCompanyWrite + company-scoped program/portfolio/roadmap lists + four write services + allocations POST Viewer 403. D-23 leftover ops/admin stay session+tenant.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-users-roles-server-authorization/10-CONTEXT.md
@.planning/phases/10-users-roles-server-authorization/10-03-PLAN.md

10-03 exported isCpmo and assertProjectWriteAccess and removed the project-level leftover-flag bypass. This plan is the only Wave-3 plan that edits access.ts. 10-08 owns portfolio-report.service.ts (stop passing the leftover flag there in 10-08; this plan deletes all-rows branches in programs.repo.ts and portfolio.repo.ts).

AUTH-05 inventory: this plan owns programs (including assertProgramAccess + POST /api/programs/[id]/project-allocations), portfolio.service, roadmap.service, import-mapping, jira-mapping. Carve-out D-23: app/api/operations/** and platform /api/admin/companies stay session+tenant (D-21 break-glass). Program project-allocations POST is a product write (not leftover ops/admin).

<interfaces>
assertCompanyWrite(actor): ForbiddenError unless isCpmo(actor) and actor.company_id is not null. Call tenant/program assert first, then assertCompanyWrite on create/update/delete. assertCompanyWrite checks the actor only — it does not re-read the resource company_id (D-16).

assertProgramAccess(programId, actor): same company match as 10-03 assertProjectAccess (D-13). Load the row; missing → NotFoundError first. If actor.company_id is not null, allow when row.company_id equals actor.company_id, else ForbiddenError. Null-company actor: only rows with company_id null. A CPMO with null company_id is ForbiddenError on a company-assigned program. Do not early-return on the leftover break-glass flag (D-03). Programs have company_id only (no customer_company_id OR).

listPrograms / listCompanyPrograms / projectCountsByProgram / programProjectAllocations: delete the all-rows leftover-flag SQL branch. Callers this plan owns pass actor.company_id only (D-13). Keep an unused optional second boolean only if Wave-3 parallel callers still typecheck; never branch SQL on it.

createProgram(actor, body): await assertCompanyWrite after name validation; stamp company from actor.company_id only (ignore body.company_id), mirroring createProject in 10-03 (D-13).

POST /api/programs/[id]/project-allocations: withProgramAccess (assertProgramAccess), then assertProjectAccess(body.project_id, actor), then assertCompanyWrite(actor), then upsert (D-15, AUTH-05). Viewer → ForbiddenError / 403. GET allocations stays tenant-read (Viewer may read) but programProjectAllocations SQL is company-scoped.

getPortfolioSummary / getRoadmap / listPortfolioMilestones: pass actor.company_id into listPortfolioProjects, listPrograms, listCompanyPrograms, listPortfolioMilestones; do not pass actor leftover flag as all-rows (D-13).

portfolio.repo.ts: delete every leftover-flag all-rows / empty-scope branch (listPortfolioProjects, listPortfolioMilestones, internalPortfolioMembers, reportCompanyScope, and the other if (isAdmin) all-rows sites in that file). Company and null-company SQL stay.

Keep Phase 9 company_id stamping on import-mapping. Viewer may read company-scoped lists/gets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="10-09-01">
  <name>Export assertCompanyWrite</name>
  <files>lib/services/access.ts, lib/services/access.unit.test.ts</files>
  <read_first>
  - lib/services/access.ts after 10-03
  - lib/services/access.unit.test.ts
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-13, D-15)
  </read_first>
  <behavior>
  - assertCompanyWrite throws ForbiddenError for viewer-only (D-15).
  - assertCompanyWrite throws ForbiddenError for pm without cpmo (D-13).
  - assertCompanyWrite returns for cpmo with non-null company_id (D-13).
  - assertCompanyWrite throws ForbiddenError for cpmo with null company_id (D-13).
  </behavior>
  <action>
  TDD (D-19): add the four cases above in access.unit.test.ts first. Then export assertCompanyWrite using isCpmo and actor.company_id. Keep assertProjectWriteAccess unchanged.
  </action>
  <verify>
    <automated>npx vitest run lib/services/access.unit.test.ts -x</automated>
  </verify>
  <done>assertCompanyWrite is the CPMO-only company-write seam (D-13, D-15, AUTH-05).</done>
  <acceptance_criteria>
  - assertCompanyWrite is exported from access.ts
  - Viewer and PM-without-cpmo cases throw ForbiddenError
  - npx vitest run lib/services/access.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-09-02">
  <name>Company-scope programs and gate allocations POST</name>
  <files>lib/services/programs.service.ts, lib/services/programs.service.unit.test.ts, lib/repositories/programs.repo.ts, lib/repositories/programs.repo.test.ts, app/api/programs/[id]/project-allocations/route.ts, app/api/programs/[id]/project-allocations/route.access.test.ts, lib/services/import-mapping.service.ts, lib/services/import-mapping.service.unit.test.ts</files>
  <read_first>
  - lib/services/programs.service.ts (assertProgramAccess, listProgramsWithCounts, createProgram)
  - lib/repositories/programs.repo.ts (listPrograms, listCompanyPrograms, projectCountsByProgram, programProjectAllocations)
  - app/api/programs/[id]/project-allocations/route.ts
  - lib/services/import-mapping.service.ts
  - .planning/phases/10-users-roles-server-authorization/10-CONTEXT.md (D-03, D-13, D-15)
  </read_first>
  <behavior>
  - assertProgramAccess: roles ['cpmo'], company_id 5, leftover flag set, program company_id 9 throws ForbiddenError (D-13, AUTH-04).
  - assertProgramAccess: roles ['cpmo'], company_id 5, program company_id 5 returns the row (D-13).
  - listProgramsWithCounts for that CPMO actor calls listPrograms/projectCountsByProgram with actor.company_id and does not pass a leftover-flag all-rows true (D-13).
  - createProgram stamps actor.company_id and ignores body.company_id; PM/Viewer throw ForbiddenError via assertCompanyWrite (D-13, D-15).
  - programs create/update/delete call assertProgramAccess first, then assertCompanyWrite (D-16, AUTH-05).
  - POST /api/programs/:id/project-allocations with a viewer-only in-company session returns 403 after program+project tenant asserts (D-15, AUTH-05).
  - import-mapping create/update/delete call company tenant first, then assertCompanyWrite (D-16).
  </behavior>
  <action>
  TDD (D-19): invert programs.service.unit.test.ts "admin bypasses company check" so a CPMO leftover-flag actor company 5 vs program company 9 is ForbiddenError (D-13). Add createProgram body.company_id ignored. Add route.access.test.ts Viewer POST 403 on project-allocations (mock getSessionFromRequest with roles: ['viewer']; mock repo upsert so it is not called). Then: replace assertProgramAccess leftover-flag early-return with company_id equality (D-13). listProgramsWithCounts and createProgram stop using actor leftover flag. Delete all-rows leftover-flag SQL in listPrograms, listCompanyPrograms, projectCountsByProgram, and programProjectAllocations (drop OR ? = 1). GET allocations passes company_id only. POST: after withProgramAccess and assertProjectAccess(project_id), await assertCompanyWrite(actor), then upsert. Invert programs.repo.test.ts leftover-flag all-companies case to company isolation. Keep Phase 9 company_id stamping on import-mapping. Leave app/api/operations/** and /api/admin/companies unchanged (D-23).
  </action>
  <verify>
    <automated>npx vitest run lib/services/programs.service.unit.test.ts lib/services/import-mapping.service.unit.test.ts app/api/programs/[id]/project-allocations/route.access.test.ts lib/repositories/programs.repo.test.ts -x</automated>
  </verify>
  <done>Program tenant assert is company-scoped; allocations POST is CPMO-only after tenant compose; createProgram stamps the session company (D-13, D-15, D-16, AUTH-04, AUTH-05).</done>
  <acceptance_criteria>
  - programs.service.unit.test.ts includes CPMO company 5 vs program company 9 ForbiddenError
  - POST project-allocations Viewer 403 case exists
  - createProgram requires assertCompanyWrite and stamps actor.company_id
  - npx vitest run lib/services/programs.service.unit.test.ts lib/services/import-mapping.service.unit.test.ts app/api/programs/[id]/project-allocations/route.access.test.ts lib/repositories/programs.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="10-09-03">
  <name>Company-scope portfolio and roadmap lists; gate writes</name>
  <files>lib/services/portfolio.service.ts, lib/services/portfolio.service.unit.test.ts, lib/repositories/portfolio.repo.ts, lib/repositories/portfolio.repo.test.ts, lib/services/roadmap.service.ts, lib/services/roadmap.service.unit.test.ts, lib/services/jira-mapping.service.ts, lib/services/jira-mapping.service.unit.test.ts</files>
  <read_first>
  - lib/services/portfolio.service.ts (getPortfolioSummary, listPortfolioMilestones)
  - lib/services/roadmap.service.ts (getRoadmap)
  - lib/repositories/portfolio.repo.ts (listPortfolioProjects, listPortfolioMilestones, reportCompanyScope, internalPortfolioMembers)
  - lib/services/jira-mapping.service.ts
  - matching unit tests
  </read_first>
  <behavior>
  - getPortfolioSummary for roles ['cpmo'], company_id 5, leftover flag set, calls listPortfolioProjects/listPrograms with company 5 and does not request all-rows; company 9 projects are absent (GET /api/portfolio, D-13, AUTH-04).
  - getRoadmap for the same actor calls listPortfolioProjects/listCompanyPrograms with company 5 only (D-13).
  - portfolio.service writes (quota, budgets, members, allocations, and any other create/update/delete) call tenant then assertCompanyWrite (D-13, D-16, AUTH-05).
  - jira-mapping.service writes call tenant then assertCompanyWrite (D-16, AUTH-05).
  - Viewer may still GET company-scoped portfolio/roadmap data (AUTH-04 read).
  </behavior>
  <action>
  TDD (D-19): add getPortfolioSummary / getRoadmap cases first so a leftover-flag CPMO company 5 never receives company 9 rows. Invert portfolio.repo.test.ts leftover-flag all-companies list. Then delete leftover-flag all-rows / empty-scope SQL in listPortfolioProjects, listPortfolioMilestones, internalPortfolioMembers, reportCompanyScope, and every other leftover-flag all-rows branch in portfolio.repo.ts (D-13). getPortfolioSummary, getRoadmap, and listPortfolioMilestones pass actor.company_id only. Wire assertCompanyWrite on portfolio and jira-mapping mutators after tenant. 10-08 stops passing the leftover flag from portfolio-report.service.ts; this task owns the repo branches those helpers share. Do not add CPMO/PM/Viewer asserts on app/api/operations/** (D-23).
  </action>
  <verify>
    <automated>npx vitest run lib/services/portfolio.service.unit.test.ts lib/services/roadmap.service.unit.test.ts lib/services/jira-mapping.service.unit.test.ts lib/repositories/portfolio.repo.test.ts -x</automated>
  </verify>
  <done>GET /api/portfolio and getRoadmap are company-scoped for leftover-flag CPMO; portfolio and jira-mapping writes are CPMO-only after tenant compose (D-13, D-16, AUTH-04, AUTH-05).</done>
  <acceptance_criteria>
  - portfolio.service.unit.test.ts includes CPMO company 5 vs company 9 isolation on getPortfolioSummary
  - roadmap.service.unit.test.ts includes leftover-flag CPMO company-scoped getRoadmap
  - portfolio.service mutators call assertCompanyWrite
  - jira-mapping.service mutators call assertCompanyWrite
  - npx vitest run lib/services/portfolio.service.unit.test.ts lib/services/roadmap.service.unit.test.ts lib/services/jira-mapping.service.unit.test.ts lib/repositories/portfolio.repo.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Viewer or PM → programs/portfolio/mapping/allocations writes | Must 403; CPMO is company-scoped |
| Leftover break-glass flag → program/portfolio/roadmap lists | Must not bypass company_id (D-03, D-13) |
| Break-glass flag → product writes | Must not bypass role asserts (D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | Company-scoped mutators | high | mitigate | assertCompanyWrite CPMO-only after tenant (D-13, AUTH-05) |
| T-10-25 | Elevation of Privilege | assertProgramAccess / listPrograms / listPortfolioProjects leftover-flag bypass | high | mitigate | Company match + delete all-rows SQL; CPMO 5 vs 9 ForbiddenError / filtered list (D-13) |
| T-10-26 | Elevation of Privilege | POST /api/programs/[id]/project-allocations | high | mitigate | assertCompanyWrite after program+project tenant; Viewer 403 (D-15, AUTH-05) |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs |
</threat_model>

<verification>
- 10-09-01: access.unit.test.ts assertCompanyWrite
- 10-09-02: programs + import-mapping + allocations POST Viewer 403; assertProgramAccess CPMO 5 vs 9
- 10-09-03: getPortfolioSummary / getRoadmap company isolation + portfolio.service.unit.test.ts + jira-mapping.service.unit.test.ts
</verification>

<success_criteria>
Viewer and non-CPMO cannot mutate programs, portfolio, import-mapping, jira-mapping, or program project-allocations; leftover-flag CPMO cannot read another company's portfolio/programs (AUTH-04, AUTH-05). D-23 leftover ops/admin are not gated here.
</success_criteria>

<output>
Create `.planning/phases/10-users-roles-server-authorization/10-09-SUMMARY.md` when done
</output>
