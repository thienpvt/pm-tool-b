---
phase: 11-project-master-pm-assignment-stakeholders
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - lib/repositories/stakeholders.repo.ts
  - lib/services/stakeholders.service.ts
  - lib/services/stakeholders.service.unit.test.ts
  - app/api/projects/[id]/stakeholders/route.ts
  - app/api/projects/[id]/stakeholders/schema.ts
  - app/api/projects/[id]/stakeholders/route.test.ts
autonomous: true
requirements: [STKH-01, STKH-02, STKH-03]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Write access can record sponsor, PSC chair, PSC members, project director, and key stakeholders as a company user or as an external name/email (D-16, D-17, STKH-01)."
    - "Ending a role sets effective_to; history rows are not physically deleted (D-16, STKH-02)."
    - "At most one active sponsor, PSC chair, and project director; multiple PSC members and key stakeholders are allowed (D-18)."
    - "listProjectStakeholders is the exported source for later dashboards and reports; no duplicate stakeholder columns on projects (D-17, STKH-03)."
    - "Viewer cannot mutate stakeholders (Phase 10 AUTH-05 via assertProjectWriteAccess)."
  artifacts:
    - path: "lib/repositories/stakeholders.repo.ts"
      provides: "list/insert/soft-end SQL for project_stakeholders (D-16)"
    - path: "lib/services/stakeholders.service.ts"
      provides: "listProjectStakeholders plus create/end with D-18 and auditLog (D-17, D-18, D-19, STKH-03)"
      exports: ["listProjectStakeholders"]
    - path: "app/api/projects/[id]/stakeholders/route.ts"
      provides: "Nested GET/POST/PATCH with withProjectAccess (D-17)"
  key_links:
    - from: "lib/services/stakeholders.service.ts"
      to: "lib/services/access.ts"
      via: "mutations call assertProjectWriteAccess; list calls assertProjectAccess (D-17)"
      pattern: "assertProjectWriteAccess"
    - from: "app/api/projects/[id]/stakeholders/route.ts"
      to: "lib/services/stakeholders.service.ts"
      via: "thin withProjectAccess handlers (D-17)"
      pattern: "listProjectStakeholders"
    - from: "listProjectStakeholders"
      to: "listStakeholdersRepo"
      via: "single list helper for info/dashboards/reports (STKH-03)"
      pattern: "listProjectStakeholders"
  prohibitions:
    - physical DELETE of stakeholder history (D-16)
    - duplicating sponsor/chair/director columns on projects (D-17, STKH-03)
    - CPMO-only gate that blocks assigned PM writes (D-17)
    - adding npm packages
    - a UI-SPEC (D-20)
    - Phase 13 snapshot tables (D-09)
---

<objective>
Deliver stakeholder history as one source of truth (D-16, D-17, D-18, STKH-01 through STKH-03): nested /api/projects/[id]/stakeholders, write access via assertProjectWriteAccess, soft-end effective_to, singleton active sponsor/chair/director, exported listProjectStakeholders.

Purpose: Phase 16 dashboards must call the same list helper later; this plan creates that helper now.

Output: stakeholders repo/service/routes with unit and route tests as the gate (D-20).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-RESEARCH.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-01-PLAN.md

11-01 created project_stakeholders. This plan does not edit projects.service.ts or access.ts so it can run in parallel with 11-02. Write access uses existing assertProjectWriteAccess (email lookup until 11-03; window lookup after 11-03 without further stakeholder changes). Do not implement PM assignment APIs.

<interfaces>
stakeholder_role enum: sponsor, psc_chair, psc_member, project_director, key_stakeholder (D-16).

Create body: stakeholder_role required. Either user_id (findUserById; user.company_id must match actor.company_id) OR both external_name and external_email (non-empty). Mixing user_id with externals: if user_id set, ignore externals. If neither user nor external identity, ValidationError.

D-18: roles sponsor, psc_chair, project_director are singletons while active (same date window predicate as assignments). Before insert, if an active row exists for that singleton role, ValidationError telling the client to end the previous window first (do not auto-end; unlike primary PM swap this is explicit). psc_member and key_stakeholder may have many active rows.

listProjectStakeholders(projectId, actor): assertProjectAccess then list all rows for the project including ended, ORDER BY effective_from DESC, id DESC (STKH-03). Do not add sponsor columns to PROJECT_COLUMNS.

createProjectStakeholder / endProjectStakeholder: assertProjectWriteAccess (D-17). Soft-end UPDATE effective_to (default CURRENT_DATE) WHERE id and project_id and effective_to IS NULL. No DELETE.

auditLog entity_type project_stakeholder, actions create and end, snapshots limited to id, project_id, stakeholder_role, user_id, external_name, external_email, effective_from, effective_to (D-19). Do not dump extra PII.

Routes: withProjectAccess; GET list; POST create 201; PATCH end. Schema passthrough like milestones.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="11-04-01">
  <name>Stakeholder service list helper and invariants</name>
  <files>lib/repositories/stakeholders.repo.ts, lib/services/stakeholders.service.ts, lib/services/stakeholders.service.unit.test.ts</files>
  <read_first>
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md (D-16, D-17, D-18)
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md stakeholders SQL excerpts
  - lib/repositories/milestones.repo.ts
  - lib/services/milestones.service.ts
  - app/api/projects/[id]/milestones/route.ts
  - lib/repositories/users.repo.ts findUserById
  - lib/services/audit.service.ts
  </read_first>
  <behavior>
  - listProjectStakeholders calls assertProjectAccess then returns repo rows including ended history (D-17, STKH-03).
  - create with user_id in-company succeeds for key_stakeholder (STKH-01).
  - create with external_name and external_email and no user_id succeeds (STKH-01).
  - create sponsor while another sponsor window is active throws ValidationError (D-18).
  - second active psc_member does not throw (D-18).
  - end sets effective_to and does not DELETE (D-16, STKH-02).
  - viewer-only assertProjectWriteAccess failure prevents create (D-17).
  </behavior>
  <action>
  TDD (D-20): failing stakeholders.service.unit.test.ts for the behavior list, mocking repo, access, findUserById, and auditLog. Then implement repo plus service.

  Analog is nested milestones (milestones.repo.ts list/soft-end, milestones.service.ts, milestones/route.ts withProjectAccess). Copy that shape. This plan is wave 2; do not treat an assignment repository from 11-03 as a read_first analog.

  Export listProjectStakeholders as the STKH-03 helper name. Do not add stakeholder fields to projects. Do not physically delete rows (D-16). Mutations use assertProjectWriteAccess not a CPMO-only gate (D-17).
  </action>
  <verify>
    <automated>npx vitest run lib/services/stakeholders.service.unit.test.ts -x</automated>
  </verify>
  <done>listProjectStakeholders is the single list source; create/end enforce D-18 and soft-end history (D-16, D-17, D-18, STKH-01, STKH-02, STKH-03).</done>
  <acceptance_criteria>
  - listProjectStakeholders is exported
  - singleton sponsor test expects ValidationError
  - end test does not call a delete repo function
  - npx vitest run lib/services/stakeholders.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="11-04-02">
  <name>Nested stakeholders HTTP routes</name>
  <files>app/api/projects/[id]/stakeholders/route.ts, app/api/projects/[id]/stakeholders/schema.ts, app/api/projects/[id]/stakeholders/route.test.ts</files>
  <read_first>
  - app/api/projects/[id]/milestones/route.ts
  - app/api/projects/[id]/milestones/schema.ts
  - app/api/projects/[id]/milestones/route.test.ts
  - lib/services/stakeholders.service.ts from 11-04-01
  </read_first>
  <behavior>
  - GET /api/projects/:id/stakeholders with in-company session returns 200 JSON array from listProjectStakeholders.
  - POST as assigned-write actor (CPMO or PM that passes write access) returns 201.
  - POST as viewer-only returns 403 (AUTH-05, D-17, D-20).
  - PATCH ends a role with 200 and does not expose a DELETE handler.
  </behavior>
  <action>
  TDD: failing route.test.ts following milestones/users route test patterns (mock session, run real wrapper if practical, or mock service and assert wrapper wiring). Then add route.ts and schema.ts with withProjectAccess. GET list; POST create status 201; PATCH end. Do not export DELETE.

  Keep the route thin: no SQL in the route file.
  </action>
  <verify>
    <automated>npx vitest run app/api/projects/[id]/stakeholders/route.test.ts lib/services/stakeholders.service.unit.test.ts -x</automated>
  </verify>
  <done>Nested stakeholders API is tenancy-gated; writes use project write access; history is patch-ended (D-17, STKH-01, STKH-02).</done>
  <acceptance_criteria>
  - Viewer POST test expects status 403
  - Route file has no DELETE export
  - npx vitest run app/api/projects/[id]/stakeholders/route.test.ts lib/services/stakeholders.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/projects/[id]/stakeholders | Write access required; user_id must stay in-company |
| Audit log ← stakeholder row | Limit snapshot fields to reduce PII spread |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information Disclosure | auditLog stakeholder snapshots | medium | mitigate | Snapshot only role, ids, name, email, dates (D-19) |
| T-11-08 | Tampering | endProjectStakeholder | medium | mitigate | UPDATE effective_to only; no DELETE (D-16) |
| T-11-09 | Elevation of Privilege | createProjectStakeholder user_id | high | mitigate | findUserById company_id must match actor.company_id (D-17) |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 11-04-01: stakeholders.service.unit.test.ts green
- 11-04-02: stakeholders route.test.ts green
- Wave: npx vitest run lib/services/stakeholders.service.unit.test.ts app/api/projects/[id]/stakeholders/route.test.ts -x
</verification>

<success_criteria>
Write access records user or external stakeholders with ranges; end keeps history; listProjectStakeholders is the only list source (STKH-01, STKH-02, STKH-03).
</success_criteria>

<output>
Create `.planning/phases/11-project-master-pm-assignment-stakeholders/11-04-SUMMARY.md` when done
</output>
