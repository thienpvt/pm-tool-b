---
phase: 11-project-master-pm-assignment-stakeholders
plan: 05
type: execute
wave: 4
depends_on: [11-02, 11-03, 11-04]
files_modified:
  - app/projects/new/page.tsx
  - app/projects/[id]/page.tsx
autonomous: true
requirements: [PROJ-01, PROJ-03, PMAS-03, STKH-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CPMO can set project_code, portfolio_year, and program on the existing new-project screen so create satisfies PROJ-01 (D-20, D-04)."
    - "Project detail edit can send stage, status_reason, rag, progress_pct, weekly_report fields, and timeline ends enough for CPMO/PM to operate (D-20, PROJ-03)."
    - "Project detail lists assignment and stakeholder history from the nested APIs rather than duplicated columns (D-15, D-17, STKH-03, D-20)."
    - "Viewer remains read-only because Phase 10/11 server asserts still 403 mutators (D-20)."
  artifacts:
    - path: "app/projects/new/page.tsx"
      provides: "Create form fields project_code, portfolio_year, customer_id posted to POST /api/projects (D-20, PROJ-01)"
    - path: "app/projects/[id]/page.tsx"
      provides: "Edit fields for stage/code/year plus fetch lists from nested assignment and stakeholder routes (D-20)"
  key_links:
    - from: "app/projects/new/page.tsx"
      to: "POST /api/projects"
      via: "payload includes project_code, portfolio_year, customer_id (PROJ-01)"
      pattern: "project_code"
    - from: "app/projects/[id]/page.tsx"
      to: "/api/projects/${id}/pm-assignments"
      via: "GET list for CPMO operate (PMAS-03, D-20)"
      pattern: "pm-assignments"
    - from: "app/projects/[id]/page.tsx"
      to: "/api/projects/${id}/stakeholders"
      via: "GET listProjectStakeholders consumer (STKH-03, D-20)"
      pattern: "stakeholders"
  prohibitions:
    - a UI-SPEC or new design-system file (D-20)
    - treating hidden buttons as access control (AUTH-05)
    - adding npm packages
    - new weekly snapshot UI (D-09)
---

<objective>
Give CPMO (and write-access PM) enough fields on the existing create and detail screens to operate identity, governance, assignments, and stakeholders (D-20). Server tests remain the phase gate; this plan only wires fields and list fetches on current pages.

Purpose: workflow.ui_phase is false — no UI-SPEC — but D-20 still requires existing screens to expose the new fields.

Output: updates to app/projects/new/page.tsx and app/projects/[id]/page.tsx only.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-02-PLAN.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-03-PLAN.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-04-PLAN.md

APIs exist: POST /api/projects requires project_code, portfolio_year, customer_id. PATCH returns the project row plus warnings. Nested pm-assignments (CPMO POST/PATCH) and stakeholders (write-access POST/PATCH). Create response still has top-level id.

Do not add a UI-SPEC. Do not decompose into _components unless the page cannot accept a short field block. Prefer existing Input/Select/Card/toast.

<interfaces>
New project form state adds: project_code string, portfolio_year string (parse to number on submit), keep customer_id as program. handleSubmit already posts JSON to /api/projects and routes to /projects/${data.id} — keep that; include the new fields in payload. Client-side require name plus project_code plus portfolio_year plus customer_id before POST (server still validates).

Project type on detail page adds optional: project_code, portfolio_year, stage, status_reason, rag, progress_pct, weekly_report_enabled, weekly_report_start_period, plan_end, adjusted_end, actual_end, classification, governance. Edit dialog includes those fields. On PATCH 200, if warnings is a non-empty array, toast.warning each string (D-07, D-08). Disable project_code input when the session user is not CPMO if role is already on the client; still rely on server strip (D-03, AUTH-05).

Detail page: GET /api/projects/${id}/pm-assignments and GET /api/projects/${id}/stakeholders after loadProject. Render compact lists (role, person, effective_from, effective_to). CPMO: small form to POST a primary/collaborator window (user_id, role, effective_from). Write access: small form to POST a stakeholder (role, user_id or external fields) and PATCH to end (id). Do not build a dashboard (Phase 16).
</interfaces>
</context>

<tasks>

<task type="auto" id="11-05-01">
  <name>Add identity fields to new project form</name>
  <files>app/projects/new/page.tsx</files>
  <read_first>
  - app/projects/new/page.tsx form state and handleSubmit
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md (D-03, D-04, D-20)
  </read_first>
  <behavior>
  - Form state includes project_code and portfolio_year.
  - Submit JSON includes project_code, portfolio_year as number, and customer_id.
  - Existing program Select remains the program control (D-04).
  </behavior>
  <action>
  Extend the existing wizard form only. Add labeled inputs for project_code and portfolio_year on the first step next to name. Keep customer_id program Select. Include the new keys in the POST payload. Do not add a UI-SPEC. Do not install packages. Client required-field toasts may mention code and year; server ValidationError still shows via toast.error.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts -x</automated>
  </verify>
  <done>New-project POST body includes project_code, portfolio_year, and program customer_id (D-04, D-20, PROJ-01).</done>
  <acceptance_criteria>
  - app/projects/new/page.tsx form includes project_code
  - handleSubmit payload includes portfolio_year
  - npx vitest run lib/services/projects.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="11-05-02">
  <name>Detail edit plus assignment and stakeholder lists</name>
  <files>app/projects/[id]/page.tsx</files>
  <read_first>
  - app/projects/[id]/page.tsx Project type, openEdit, PATCH fetch
  - app/api/projects/[id]/pm-assignments/route.ts
  - app/api/projects/[id]/stakeholders/route.ts
  </read_first>
  <behavior>
  - Edit form can send stage and project_code among the identity/governance fields (PROJ-03, D-20).
  - PATCH response warnings array is toasted, not treated as a failed request (D-07).
  - Page fetches pm-assignments and stakeholders lists (STKH-03, PMAS-03).
  </behavior>
  <action>
  Extend the existing edit dialog with the fields listed in interfaces. Toast warnings on successful PATCH. Add two compact Card lists that GET the nested APIs and, where the existing layout allows, POST/PATCH without a new route tree. Do not hide mutate controls as the only access control. Do not add jsdom page tests unless a tiny assertion is free; D-20 says server tests are the gate.

  Automated check: the detail page source contains the nested path segments pm-assignments and stakeholders, and the edit form includes stage.
  </action>
  <verify>
    <automated>npx vitest run lib/services/pm-assignments.service.unit.test.ts lib/services/stakeholders.service.unit.test.ts -x &amp;&amp; node -e "const fs=require('fs'); const s=fs.readFileSync('app/projects/[id]/page.tsx','utf8'); for (const t of ['pm-assignments','stakeholders','stage']) { if (!s.includes(t)) { console.error('missing '+t); process.exit(1); } }"</automated>
    <human-check>Open /projects/new as CPMO: code, year, and program are required to create. Open a project: edit stage/code; lists show assignments and stakeholders from the APIs.</human-check>
  </verify>
  <done>Existing create/detail screens expose enough fields and lists for CPMO to operate; server asserts remain the access gate (D-20, PROJ-03, PMAS-03, STKH-03).</done>
  <acceptance_criteria>
  - app/projects/[id]/page.tsx fetches pm-assignments
  - app/projects/[id]/page.tsx fetches stakeholders
  - Edit payload can include stage
  - node source check: app/projects/[id]/page.tsx includes pm-assignments, stakeholders, and stage
  - npx vitest run lib/services/pm-assignments.service.unit.test.ts lib/services/stakeholders.service.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser forms → project APIs | UI is not authorization; server 11-01..11-04 asserts remain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Elevation of Privilege | project detail mutate controls | high | mitigate | Keep relying on assertProjectWriteAccess / CPMO assignment service; hiding inputs is not access control (D-20, AUTH-05) |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 11-05-01: create form includes identity fields; create service tests still green
- 11-05-02: detail page lists nested APIs; assignment and stakeholder service tests still green
- Wave: npx vitest run lib/services/projects.service.unit.test.ts lib/services/pm-assignments.service.unit.test.ts lib/services/stakeholders.service.unit.test.ts -x
</verification>

<success_criteria>
CPMO can create with code/year/program and manage assignments/stakeholders from existing screens; server tests remain the gate (D-20).
</success_criteria>

<output>
Create `.planning/phases/11-project-master-pm-assignment-stakeholders/11-05-SUMMARY.md` when done
</output>
