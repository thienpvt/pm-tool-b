---
phase: 11-project-master-pm-assignment-stakeholders
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - lib/services/project-governance.ts
  - lib/services/project-governance.unit.test.ts
  - lib/services/projects.service.ts
  - lib/services/projects.service.unit.test.ts
  - app/api/projects/[id]/route.ts
autonomous: true
requirements: [PROJ-02, PROJ-03, PROJ-04, PROJ-05, PROJ-06, PROJ-07, PROJ-08]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Write access can maintain classification, governance, stage L0 through L5, status, RAG, progress 0-100, and timeline dates (D-05, PROJ-03)."
    - "Only CPMO can change project_code; the change is an in-place UPDATE of the same projects.id (D-02, D-03, PROJ-02)."
    - "Status Other requires non-empty status_reason; weekly-report enabled requires weekly_report_start_period YYYY-Wnn (D-06, PROJ-04)."
    - "Stage L5 persists status Completed, RAG Not applicable, progress 100, and returns warnings in the JSON body rather than HTTP 400 when the client sent overrides (D-07, PROJ-05)."
    - "Stage L5 or status Completed, Paused, Cancelled, or Other defaults RAG to Not applicable with the same warning-not-block pattern (D-08, PROJ-06)."
    - "Live progress_pct updates do not write Phase 13 snapshot rows; turning weekly_report_enabled off does not delete history (D-09, D-10, PROJ-07, PROJ-08)."
    - "Code change is auditLog action code_change (D-19)."
  artifacts:
    - path: "lib/services/project-governance.ts"
      provides: "applyProjectGovernance defaults + warnings builder (D-07, D-08)"
      exports: ["applyProjectGovernance"]
    - path: "lib/services/projects.service.ts"
      provides: "updateProject strips project_code for non-CPMO; ValidationError for D-06; returns row plus warnings (D-02, D-03, D-06)"
    - path: "app/api/projects/[id]/route.ts"
      provides: "PATCH JSON includes warnings array on 200 (D-07, D-08)"
  key_links:
    - from: "lib/services/projects.service.ts"
      to: "lib/services/project-governance.ts"
      via: "updateProject and createProject call applyProjectGovernance before repo write (D-07, D-08)"
      pattern: "applyProjectGovernance"
    - from: "lib/services/projects.service.ts"
      to: "lib/services/audit.service.ts"
      via: "auditLog on project_code change (D-19)"
      pattern: "auditLog"
    - from: "updateProject"
      to: "updateProjectRepo"
      via: "in-place UPDATE by project id; never deleteProject for a code change (D-02)"
      pattern: "updateProjectRepo"
  prohibitions:
    - HTTP 400 for L5 or terminal RAG overrides (D-07, D-08)
    - delete plus insert to change project_code (D-02)
    - allowing PM PATCH to persist project_code (D-03)
    - Phase 13 snapshot persistence tables (D-09)
    - adding npm packages
    - a UI-SPEC (D-20)
---

<objective>
Deliver governance and identity maintenance (D-02, D-03, D-05 through D-10, PROJ-02 through PROJ-08): PATCH/create apply L0-L5 stage, hard ValidationError only for Other-without-reason and weekly-enabled-without-start-period, L5/terminal defaults with warnings on the 200 JSON body, CPMO-only in-place code UPDATE with auditLog, live progress_pct only.

Purpose: Spec-compliant master fields without blocking overrides that the spec wants warned, not rejected.

Output: project-governance helper wired into projects.service; PATCH still returns a top-level project id so existing UI keeps working, plus warnings.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-RESEARCH.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-PATTERNS.md
@.planning/phases/11-project-master-pm-assignment-stakeholders/11-01-PLAN.md

11-01 added columns, unique index, and CPMO create required fields. This plan owns PATCH governance and CPMO code change. Do not rewire PM assignment lookup (11-03).

<interfaces>
applyProjectGovernance(input: Record&lt;string, unknown&gt;, prior?: { progress_pct?: number | null; status?: string | null; rag?: string | null; stage?: string | null }): { fields: Record&lt;string, unknown&gt;; warnings: string[] }

Stage literals: L0 L1 L2 L3 L4 L5. Keep current_phase as a free-text field the client may still send; do not derive or delete it (D-05). New API writers use stage.

Terminal statuses for D-08: Completed, Paused, Cancelled, Other. RAG default string: Not applicable. L5 also sets status Completed and progress_pct 100 (D-07).

Warning-not-block: if the client sent status other than Completed under L5, still persist Completed and push a warning string. If client sent RAG other than Not applicable under L5 or terminal status, persist Not applicable and push a warning. If prior.progress_pct is below 100 when applying L5, push a warning and persist 100.

Hard ValidationError only (D-06): merged status Other with empty/whitespace status_reason (field status_reason). weekly_report_enabled true (boolean true or string Yes) with missing/invalid weekly_report_start_period. Period pattern: four digits, hyphen, W, two digits (ISO week). progress_pct if present must be an integer 0 through 100 inclusive (PROJ-03) — ValidationError field progress_pct.

updateProject: assertProjectWriteAccess first. Clone fields. If actor is not CPMO, delete project_code from the clone so a PM cannot persist a code change (D-03). If CPMO sends project_code different from current row, run findProjectByCompanyCode excluding this id; ConflictError on clash; then in-place updateProjectRepo (D-02). auditLog entity_type project, action code_change, before/after { project_code } only (D-19). Apply applyProjectGovernance using getProjectRepo as prior. Return { ...row, warnings } so id stays top-level (existing GET/PATCH clients read data.id). Empty warnings array when none.

createProject: after identity validation from 11-01, run applyProjectGovernance on body with no prior; persist warnings the same way on the returned row.

Do not map warnings to 400 in app/api/projects/[id]/route.ts. PATCH already JSON.stringifies the service return; keep that. Do not add a blocking schema for rag.

Phase 13 contract (comment on progress_pct in project-governance or projects.service): period submit copies progress_pct at submit time and must not write it back. This phase has no snapshot write path (D-09, PROJ-07). weekly_report_enabled false, or stage L5, or terminal status, is the skip signal Phase 13 will use; do not invent history rows to delete (D-10, PROJ-08).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" id="11-02-01">
  <name>L5 and terminal defaults return warnings</name>
  <files>lib/services/project-governance.ts, lib/services/project-governance.unit.test.ts</files>
  <read_first>
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10)
  - .planning/phases/11-project-master-pm-assignment-stakeholders/11-RESEARCH.md Pattern 3
  - lib/services/errors.ts ValidationError
  </read_first>
  <behavior>
  - Stage L5 yields fields.status Completed, fields.rag Not applicable, fields.progress_pct 100 (D-07, PROJ-05).
  - If input.status was Active under L5, warnings is non-empty and the function does not throw (D-07).
  - If prior.progress_pct is 40 under L5, warnings is non-empty and fields.progress_pct is 100 (D-07).
  - Status Other without status_reason throws ValidationError (D-06, PROJ-04).
  - weekly_report_enabled true without YYYY-Wnn start period throws ValidationError (D-06, PROJ-04).
  - Status Paused with client rag Green yields rag Not applicable and a warning, no throw (D-08, PROJ-06).
  - progress_pct 101 throws ValidationError (PROJ-03).
  </behavior>
  <action>
  TDD (D-20): write failing unit tests for applyProjectGovernance covering the behavior list. They must fail before the helper exists. Then implement applyProjectGovernance as a pure function (no DB). Do not throw for L5 or terminal RAG/status overrides — only D-06 hard rules and progress bounds throw ValidationError.

  Do not persist here. Do not create snapshot tables (D-09). Keep current_phase out of the defaulting logic (D-05).
  </action>
  <verify>
    <automated>npx vitest run lib/services/project-governance.unit.test.ts -x</automated>
  </verify>
  <done>Governance helper applies L5/terminal defaults and returns warnings without throwing on overrides (D-05, D-06, D-07, D-08, PROJ-04, PROJ-05, PROJ-06).</done>
  <acceptance_criteria>
  - applyProjectGovernance is exported
  - L5 override tests expect warnings length greater than 0 and not ValidationError
  - Other without reason expects ValidationError
  - npx vitest run lib/services/project-governance.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="11-02-02">
  <name>Wire PATCH create governance and CPMO code UPDATE</name>
  <files>lib/services/projects.service.ts, lib/services/projects.service.unit.test.ts, app/api/projects/[id]/route.ts</files>
  <read_first>
  - lib/services/projects.service.ts updateProject and createProject
  - lib/services/users.service.ts auditLog snapshot pattern
  - lib/services/audit.service.ts auditLog
  - app/api/projects/[id]/route.ts PATCH
  - lib/services/project-governance.ts from 11-02-01
  </read_first>
  <behavior>
  - PM updateProject with project_code in fields calls updateProjectRepo without project_code (D-03).
  - CPMO updateProject with a new unused project_code calls updateProjectRepo with that code and auditLog action code_change; deleteProject is not called (D-02, D-19, PROJ-02).
  - CPMO updateProject with a duplicate in-company code throws ConflictError (D-01).
  - updateProject with stage L5 returns an object that has id and warnings array; assertProjectWriteAccess still runs (D-07).
  - createProject after 11-01 validation also attaches warnings from applyProjectGovernance.
  </behavior>
  <action>
  TDD: failing tests for stripping project_code for non-CPMO, in-place CPMO code change plus auditLog, duplicate code ConflictError, and L5 update return shape including warnings. Mock auditLog and getProjectRepo. Then wire updateProject and createProject as specified in interfaces.

  findProjectByCompanyCode must ignore the current project id on update (pass exclude id or filter in service). Never call deleteProject on the code-change path (D-02).

  PATCH route: leave passthrough schema; do not convert warnings into a 400 (D-07, D-08). No extra files_modified on mass-assignment route.test.ts unless a test breaks — UnknownColumnError contract stays.

  Document the Phase 13 read-at-submit contract next to progress_pct handling (D-09, PROJ-07). Do not write any weekly snapshot rows.
  </action>
  <verify>
    <automated>npx vitest run lib/services/projects.service.unit.test.ts lib/services/project-governance.unit.test.ts -x</automated>
  </verify>
  <done>CPMO code change is in-place UPDATE with audit; PM cannot persist code; L5/terminal return warnings on the project JSON; D-06 still 400 (D-02, D-03, D-06, D-07, D-08, D-09, D-10, D-19, PROJ-02 through PROJ-08).</done>
  <acceptance_criteria>
  - Non-CPMO update test expects updateProjectRepo not to receive project_code
  - CPMO code change test expects auditLog action code_change and no deleteProject
  - L5 update test expects warnings to be an array on a 200-shaped object with id
  - npx vitest run lib/services/projects.service.unit.test.ts lib/services/project-governance.unit.test.ts -x exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PATCH /api/projects/[id] | Passthrough body; service must strip code for non-CPMO |
| Governance helper → persisted row | Overrides must warn, not reject, except D-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Tampering | updateProject project_code | high | mitigate | Strip project_code unless isCpmo; unique pre-check on change (D-03, D-01) |
| T-11-04 | Tampering | L5/terminal defaults | medium | mitigate | Server applies defaults; warnings on 200; ValidationError only for D-06 (D-07, D-08) |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs; do not add dependencies |
</threat_model>

<verification>
- 11-02-01: governance unit green
- 11-02-02: projects.service update/create tests green
- Wave: npx vitest run lib/services/project-governance.unit.test.ts lib/services/projects.service.unit.test.ts -x
</verification>

<success_criteria>
Write access maintains spec governance fields; CPMO-only in-place code change; L5/terminal warn rather than 400; live progress_pct does not write snapshot tables (PROJ-02 through PROJ-08).
</success_criteria>

<output>
Create `.planning/phases/11-project-master-pm-assignment-stakeholders/11-02-SUMMARY.md` when done
</output>
