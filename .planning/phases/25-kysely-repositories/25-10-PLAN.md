---
phase: 25-kysely-repositories
plan: 10
type: tdd
wave: 10
depends_on: [25-09]
files_modified:
  - modules/weekly/backend/repositories/weekly-reports.repo.ts
  - modules/weekly/backend/repositories/weekly-reports.repo.test.ts
  - modules/weekly/backend/repositories/weekly-periods.repo.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "weekly-reports.repo.ts reads, locks, drafts, versions, submit, and correction helpers use getKysely (ENF-02, D-05, W8)."
    - "insertShell uses getKysely so createPeriodWithShells inserts shells on the ALS connection (D-06)."
    - "Existing weekly-reports.repo.test.ts stays green (D-07)."
    - "weekly-reports.service.ts is not rewritten (D-06)."
  artifacts:
    - path: "modules/weekly/backend/repositories/weekly-reports.repo.ts"
      provides: "Kysely weekly report shells and versions"
  key_links:
    - from: "insertShell"
      to: "getKysely"
      via: "ALS inside createPeriodWithShells"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not rewrite weekly-reports.service.ts (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert weekly-reports.repo.ts to getKysely and point insertShell at the transactional factory (ENF-02, D-05, D-06, W8).

Purpose: 410-line report repo plus the shell insert used inside period creation.

Output: converted weekly-reports.repo.ts; insertShell caller in weekly-periods.repo.ts updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/weekly/backend/repositories/weekly-reports.repo.ts
@modules/weekly/backend/repositories/weekly-reports.repo.test.ts
@modules/weekly/backend/repositories/weekly-periods.repo.ts

Planner-assigned locked IDs: D-05 convert this repo; D-06 keep service runInTransaction; D-02 getKysely ALS; D-07 testKysely.

Conversion contract: getKysely; TDD two commits test(25-10) then feat(25-10). Isolation none.

<interfaces>
Slice A: insertShell, getShellsForPeriod, getWeeklyReportFullRow, lockWeeklyReportShell, getWeeklyReportWithPeriod, updatePrevWeekRag, getPriorPeriodSubmittedRag, listProjectWeeklyHistoryRepo, getWeeklyPeriodByCompany, listPeriodShellsRepo.

Slice B: updateWeeklyReportDraft, insertWeeklyReportVersion, finalizeWeeklyReportSubmit, openCorrectionOnShell, getLatestVersionSnapshot.

insertShell: drop the PoolClient first argument. Use insertInto weekly_reports onConflict (period_id, project_id) doNothing returning shell columns. Update the single caller in weekly-periods.repo.ts createPeriodWithShells to insertShell(period.id, projectId) inside withPgTransaction so ALS applies.

FULL_SHELL_SELECT columns stay the same. JSON draft_raid_json stringify if the current code stringifies.

Do not edit weekly-reports.service.ts (D-06).
</interfaces>
</context>

<feature>
  <name>weekly-reports.repo.ts on Kysely</name>
  <files>modules/weekly/backend/repositories/weekly-reports.repo.ts</files>
  <behavior>
    Existing weekly-reports.repo.test.ts cases pass via testKysely (D-05).
  </behavior>
  <implementation>
    Convert both slices; insertShell joins ALS (D-06).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-10-01">
  <name>Convert weekly-reports reads, lock, history, insertShell</name>
  <files>modules/weekly/backend/repositories/weekly-reports.repo.ts, modules/weekly/backend/repositories/weekly-reports.repo.test.ts, modules/weekly/backend/repositories/weekly-periods.repo.ts</files>
  <read_first>
  - modules/weekly/backend/repositories/weekly-reports.repo.ts
  - modules/weekly/backend/repositories/weekly-reports.repo.test.ts
  - modules/weekly/backend/repositories/weekly-periods.repo.ts (insertShell call)
  </read_first>
  <behavior>
  - Existing weekly-reports.repo.test.ts cases that hit slice A still pass (D-05).
  - insertShell no longer takes a PoolClient (D-06).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely on weekly-reports.repo.test.ts; add getKysely called assertion. Commit test(25-10): red weekly reports reads kysely.
  GREEN: Convert slice A. Change insertShell signature and the weekly-periods.repo.ts caller. Keep withPgTransaction in periods. Do not edit the service. Commit feat(25-10): weekly reports reads and insertShell kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/repositories/weekly-reports.repo.test.ts modules/weekly/backend/repositories/weekly-periods.repo.test.ts</automated>
  </verify>
  <done>Slice A of weekly-reports.repo.ts uses getKysely (W8, D-05).</done>
  <acceptance_criteria>
  - insertShell uses getKysely
  - npx vitest run --project node modules/weekly/backend/repositories/weekly-reports.repo.test.ts modules/weekly/backend/repositories/weekly-periods.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-10-02">
  <name>Convert weekly-reports draft, version, submit, correction</name>
  <files>modules/weekly/backend/repositories/weekly-reports.repo.ts, modules/weekly/backend/repositories/weekly-reports.repo.test.ts</files>
  <read_first>
  - modules/weekly/backend/repositories/weekly-reports.repo.ts (updateWeeklyReportDraft through getLatestVersionSnapshot)
  </read_first>
  <behavior>
  - Remaining weekly-reports.repo.test.ts cases pass via testKysely (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: If submit/draft cases are missing, add one draft update round-trip. Commit test(25-10): red weekly reports writes kysely.
  GREEN: Convert slice B. After this task every export uses getKysely. Honor D-07. Commit feat(25-10): weekly reports writes kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/weekly/backend/repositories</automated>
  </verify>
  <done>weekly-reports.repo.ts is fully on getKysely (W8, D-05).</done>
  <acceptance_criteria>
  - weekly-reports.repo.ts imports getKysely
  - npx vitest run --project node modules/weekly/backend/repositories/weekly-reports.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Submit/finalize → weekly_reports + versions | Split-brain if Kysely leaves the service transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-15 | Tampering | finalizeWeeklyReportSubmit | high | mitigate | getKysely honors ALS from service runInTransaction (D-06) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withProjectAccess on weekly report routes unchanged (D-06).
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/weekly/backend/repositories
</verification>

<success_criteria>
weekly-reports.repo.ts queries through getKysely; insertShell joins the period-creation transaction (ENF-02, D-05, D-06, W8).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-10-SUMMARY.md` when done
</output>
