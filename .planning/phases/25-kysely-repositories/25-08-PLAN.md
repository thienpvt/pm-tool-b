---
phase: 25-kysely-repositories
plan: 08
type: tdd
wave: 8
depends_on: [25-07]
files_modified:
  - modules/portfolio/backend/repositories/portfolio.repo.ts
  - modules/portfolio/backend/repositories/portfolio.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every export in portfolio.repo.ts uses getKysely (ENF-02, D-05, W7)."
    - "listPortfolioProjects still company-scopes (existing tests) (D-05, D-06)."
    - "Member, budget, and report helpers keep the same signatures so services need no rewrite (D-06)."
  artifacts:
    - path: "modules/portfolio/backend/repositories/portfolio.repo.ts"
      provides: "Kysely portfolio queries"
  key_links:
    - from: "portfolio.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not rewrite portfolio services or UI (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert portfolio.repo.ts (all exports) to getKysely in three slices (ENF-02, D-05, W7).

Purpose: 725-line file exceeds one task's context; slices are sequential on one file.

Output: fully converted portfolio.repo.ts; existing company-scope tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/portfolio/backend/repositories/portfolio.repo.ts
@modules/portfolio/backend/repositories/portfolio.repo.test.ts

Planner-assigned locked IDs: D-05 convert this repo; D-06 no service/UI rewrite; D-02 getKysely; D-07 testKysely.

Conversion contract: getKysely; keep signatures; mock testKysely; switch test imports to @/test/db and @/test/repo-db; TDD two commits test(25-08) then feat(25-08). Isolation none. Only this file among portfolio repos (25-07 already converted the others).

<interfaces>
Slice A (through listPortfolioMilestones): listPortfolioProjects, risk/issue/activity counts, roadmap helpers, members CRUD, companyNameAndQuota, setCompanyHeadcountQuota, bugCountsByAssignee, listPortfolioMilestones.

Slice B: list/create/find/update/delete portfolio budgets, categories, allocations, spendByCategory, programFteAllocations, upsert/update/delete portfolio program allocations.

Slice C: portfolioMilestoneSelection and all listPortfolioReport* / topPortfolio* / activity / bug / FTE / fill-rate / report milestone helpers through portfolioReportMilestones.

Joins: leftJoin as today. Raw CASE/ORDER fragments use kysely sql tagged template. Honor D-07.
</interfaces>
</context>

<feature>
  <name>portfolio.repo.ts on Kysely</name>
  <files>modules/portfolio/backend/repositories/portfolio.repo.ts</files>
  <behavior>
    listPortfolioProjects(companyA) includes projectA and excludes other-company projectB (D-05).
  </behavior>
  <implementation>
    Convert each slice to getKysely without changing exported names (D-06).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-08-01">
  <name>Convert portfolio reads, members, quota</name>
  <files>modules/portfolio/backend/repositories/portfolio.repo.ts, modules/portfolio/backend/repositories/portfolio.repo.test.ts</files>
  <read_first>
  - modules/portfolio/backend/repositories/portfolio.repo.ts (listPortfolioProjects through listPortfolioMilestones)
  - modules/portfolio/backend/repositories/portfolio.repo.test.ts
  </read_first>
  <behavior>
  - Existing company-scope tests for listPortfolioProjects pass via testKysely (D-05).
  - getKysely is called (D-02).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Mock getKysely with testKysely; retarget test imports to @/test/db; add getKysely called assertion after listPortfolioProjects. Commit test(25-08): red portfolio reads kysely.
  GREEN: Convert slice A functions only. Leave slice B/C SQL on DbClient until later tasks in this plan. Commit feat(25-08): portfolio reads members quota kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts</automated>
  </verify>
  <done>Slice A of portfolio.repo.ts uses getKysely (W7, D-05).</done>
  <acceptance_criteria>
  - listPortfolioProjects uses getKysely
  - npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-08-02">
  <name>Convert portfolio budget and program-allocation writes</name>
  <files>modules/portfolio/backend/repositories/portfolio.repo.ts, modules/portfolio/backend/repositories/portfolio.repo.test.ts</files>
  <read_first>
  - modules/portfolio/backend/repositories/portfolio.repo.ts (listPortfolioBudgets through deletePortfolioProgramAllocation)
  </read_first>
  <behavior>
  - createPortfolioBudget then listPortfolioBudgets returns the period_label for that company (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Extend portfolio.repo.test.ts with create+list budget for companyA. Commit test(25-08): red portfolio budgets kysely.
  GREEN: Convert slice B. Explicit column sets from body keys already named in SQL — do not introduce pickAllowed unless a function already spread Object.keys onto SET. Commit feat(25-08): portfolio budgets kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts</automated>
  </verify>
  <done>Slice B of portfolio.repo.ts uses getKysely (W7, D-05).</done>
  <acceptance_criteria>
  - budget and program-allocation functions use getKysely
  - npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-08-03">
  <name>Convert portfolio report helpers</name>
  <files>modules/portfolio/backend/repositories/portfolio.repo.ts, modules/portfolio/backend/repositories/portfolio.repo.test.ts</files>
  <read_first>
  - modules/portfolio/backend/repositories/portfolio.repo.ts (portfolioMilestoneSelection through end)
  </read_first>
  <behavior>
  - listPortfolioReportProjects(companyA) includes projectA (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Assert listPortfolioReportProjects company scope. Commit test(25-08): red portfolio report kysely.
  GREEN: Convert remaining exports. After this task every function in the file uses getKysely. Commit feat(25-08): portfolio report helpers kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts modules/portfolio/backend/repositories/programs.repo.test.ts</automated>
  </verify>
  <done>portfolio.repo.ts is fully on getKysely (W7, D-05).</done>
  <acceptance_criteria>
  - portfolio.repo.ts imports getKysely
  - npx vitest run --project node modules/portfolio/backend/repositories/portfolio.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portfolio list → company_id | Cross-tenant project leak if WHERE dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-13 | Information Disclosure | listPortfolioProjects | high | mitigate | Keep company_id filter; existing tests must stay green (D-05, D-06) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withAuth/withCpmo on portfolio routes unchanged (D-06).
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/portfolio/backend/repositories
</verification>

<success_criteria>
portfolio.repo.ts queries through getKysely; company scope tests still pass (ENF-02, D-05, W7).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-08-SUMMARY.md` when done
</output>
