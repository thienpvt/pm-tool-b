---
phase: 25-kysely-repositories
plan: 05
type: tdd
wave: 5
depends_on: [25-04]
files_modified:
  - modules/documents/backend/repositories/document-catalog.repo.ts
  - modules/documents/backend/repositories/document-catalog.repo.test.ts
  - modules/documents/backend/repositories/document-templates.repo.ts
  - modules/documents/backend/repositories/document-templates.repo.test.ts
  - modules/documents/backend/repositories/project-document-checklist.repo.ts
  - modules/documents/backend/repositories/project-document-checklist.repo.test.ts
autonomous: true
requirements: [ENF-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "document-catalog list/get/insert/update use getKysely and stay company-scoped (ENF-02, D-05, W4)."
    - "document-templates version/retire/listEffective use getKysely (D-05)."
    - "project-document-checklist insert-if-missing and status updates use getKysely (D-05)."
    - "New colocated repo tests cover one happy path each on testKysely (D-07)."
  artifacts:
    - path: "modules/documents/backend/repositories/document-catalog.repo.ts"
      provides: "Kysely catalog"
    - path: "modules/documents/backend/repositories/document-templates.repo.ts"
      provides: "Kysely templates"
    - path: "modules/documents/backend/repositories/project-document-checklist.repo.ts"
      provides: "Kysely checklist rows"
  key_links:
    - from: "modules/documents/backend/repositories/*.repo.ts"
      to: "lib/db/kysely.ts"
      via: "getKysely"
      pattern: "getKysely"
  prohibitions:
    - statement: "Must not restyle document UI or add document API routes (D-06)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Convert the three documents module repositories to getKysely (ENF-02, D-05, W4).

Purpose: Catalog, templates, and checklist SQL join the typed factory without changing Phase 23 UI or APIs.

Output: three converted repos plus new integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-kysely-repositories/25-CONTEXT.md
@.planning/phases/25-kysely-repositories/25-PATTERNS.md
@modules/documents/backend/repositories/document-catalog.repo.ts
@modules/documents/backend/repositories/document-templates.repo.ts
@modules/documents/backend/repositories/project-document-checklist.repo.ts
@test/repo-db.ts

Planner-assigned locked IDs: D-05 convert these three; D-06 no UI/route rewrite; D-02 getKysely; D-07 testKysely.

Conversion contract: getKysely; keep signatures; new tests skipIf(!hasTestDb) with setupRepoTables; TDD two commits test(25-05) then feat(25-05). Isolation none.

<interfaces>
document-catalog: listDocumentCatalog, getDocumentCatalog, insertDocumentCatalog, updateDocumentCatalog, listActiveCatalogForStage. Preserve active/stage filters.

document-templates: getDocumentTemplate, getMaxVersion, retireCurrentTemplate, insertDocumentTemplate, listEffectiveTemplates, retireTemplateById.

project-document-checklist: insertChecklistRowIfMissing, listChecklistCatalogIds, listChecklistByProject, getChecklistItem, updateChecklistItem.

If setupRepoTables lacks document_* DDL, add CREATE TABLE IF NOT EXISTS matching 0001 column names inside test/repo-db.ts DDL (same advisory lock). That is test harness only, not a schema migration.
</interfaces>
</context>

<feature>
  <name>Documents module repositories on Kysely</name>
  <files>modules/documents/backend/repositories/document-catalog.repo.ts, modules/documents/backend/repositories/document-templates.repo.ts, modules/documents/backend/repositories/project-document-checklist.repo.ts</files>
  <behavior>
    listDocumentCatalog returns inserted company rows (D-05).
    insertDocumentTemplate then listEffectiveTemplates sees the row (D-05).
    insertChecklistRowIfMissing is idempotent (D-05).
  </behavior>
  <implementation>
    Convert SQL to Kysely; do not edit document pages or routes (D-06).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true" id="25-05-01">
  <name>Convert document-catalog.repo.ts</name>
  <files>modules/documents/backend/repositories/document-catalog.repo.ts, modules/documents/backend/repositories/document-catalog.repo.test.ts, test/repo-db.ts</files>
  <read_first>
  - modules/documents/backend/repositories/document-catalog.repo.ts
  - test/repo-db.ts
  - migrations/0001-baseline-schema.sql (document_catalog)
  </read_first>
  <behavior>
  - insertDocumentCatalog then listDocumentCatalog returns the name for that companyId (D-05).
  - getKysely is called (D-02).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Add document-catalog.repo.test.ts skipIf(!hasTestDb), seedCompany, mock testKysely, cover insert+list. Commit test(25-05): red document catalog kysely.
  GREEN: Convert all catalog exports. Extend test/repo-db.ts DDL only if document_catalog is missing. Commit feat(25-05): document catalog kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/documents/backend/repositories/document-catalog.repo.test.ts</automated>
  </verify>
  <done>document-catalog.repo.ts uses getKysely (W4, D-05).</done>
  <acceptance_criteria>
  - document-catalog.repo.ts imports getKysely
  - npx vitest run --project node modules/documents/backend/repositories/document-catalog.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-05-02">
  <name>Convert document-templates.repo.ts</name>
  <files>modules/documents/backend/repositories/document-templates.repo.ts, modules/documents/backend/repositories/document-templates.repo.test.ts</files>
  <read_first>
  - modules/documents/backend/repositories/document-templates.repo.ts
  </read_first>
  <behavior>
  - insertDocumentTemplate then listEffectiveTemplates includes the template_url (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Add document-templates.repo.test.ts covering insert + listEffectiveTemplates. Commit test(25-05): red document templates kysely.
  GREEN: Convert all template exports. Commit feat(25-05): document templates kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/documents/backend/repositories/document-templates.repo.test.ts</automated>
  </verify>
  <done>document-templates.repo.ts uses getKysely (W4, D-05).</done>
  <acceptance_criteria>
  - document-templates.repo.ts imports getKysely
  - npx vitest run --project node modules/documents/backend/repositories/document-templates.repo.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="25-05-03">
  <name>Convert project-document-checklist.repo.ts</name>
  <files>modules/documents/backend/repositories/project-document-checklist.repo.ts, modules/documents/backend/repositories/project-document-checklist.repo.test.ts</files>
  <read_first>
  - modules/documents/backend/repositories/project-document-checklist.repo.ts
  </read_first>
  <behavior>
  - insertChecklistRowIfMissing twice does not duplicate catalog_id for the project (D-05).
  </behavior>
  <action>
  TDD. Two commits. Never RED-only.
  RED: Add project-document-checklist.repo.test.ts covering insert-if-missing + listChecklistByProject. Commit test(25-05): red checklist kysely.
  GREEN: Convert all checklist exports. Commit feat(25-05): document checklist kysely.
  </action>
  <verify>
    <automated>npx vitest run --project node modules/documents/backend/repositories/project-document-checklist.repo.test.ts</automated>
  </verify>
  <done>project-document-checklist.repo.ts uses getKysely (W4, D-05).</done>
  <acceptance_criteria>
  - project-document-checklist.repo.ts imports getKysely
  - npx vitest run --project node modules/documents/backend/repositories/project-document-checklist.repo.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checklist repo → project rows | project_id scoping must stay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-09 | Information Disclosure | document-catalog.repo.ts | medium | mitigate | list remains WHERE company_id (D-05, D-06) |
| T-25-SC | Tampering | npm installs | high | accept | No package-manager install in this plan |

ASVS L1: withAuth/withCpmo on document routes unchanged (D-06).
</threat_model>

<verification>
- Wave: npx vitest run --project node modules/documents/backend/repositories
</verification>

<success_criteria>
Catalog, templates, and checklist repos query through getKysely (ENF-02, D-05, W4).
</success_criteria>

<output>
Create `.planning/phases/25-kysely-repositories/25-05-SUMMARY.md` when done
</output>
