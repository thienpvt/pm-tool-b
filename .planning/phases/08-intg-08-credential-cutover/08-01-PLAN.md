---
phase: 08-intg-08-credential-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/jira/search/route.ts
  - app/api/jira/fields/route.ts
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [INTG-08]
user_setup:
  - service: postgresql
    why: "Read-only cutover script SELECTs company_jira_config and must reach the same DB the resolver uses"
    env_vars:
      - name: DATABASE_URL
        source: "Uncommitted repo-root .env.local. Load with npx tsx --env-file=.env.local. Never paste the value into docs, SUMMARY, or VERIFICATION."

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cutover script invoked as npx tsx --env-file=.env.local scripts/verify-credential-cutover.ts on this Windows host exits 0 (D-01)"
    - "Stdout is recorded verbatim in 08-01-SUMMARY.md using only OK/UNSET labels; no DATABASE_URL, tokens, or connection strings appear in planning docs (D-01, D-04)"
    - "Zero-row company_jira_config plus exit 0 plus an anthropic match: yes line is valid vacuous per-company evidence; missing DATABASE_URL or unreachable DB is a hard stop, not empty-success"
    - "Old vs new are compared as match yes/no only; any match: no row or non-zero exit HALTS deletion — no merge, no force-delete"
    - "Per-company rows are independent; stdout line order is not a correctness criterion; do not add sorting"
    - "getJiraCredentials is gone from app/api/jira/search/route.ts and oldInlineCredentialBlock is gone from app/api/jira/fields/route.ts, including their INTG-08 comments (D-02)"
    - "Search POST and fields GET still call resolveJiraCredentials; fields GET still uses the jira-config.repo helper for the two 503 strings Jira chưa cấu hình vs Thiếu env vars (D-02)"
    - "lib/integrations/credentials.ts is byte-unchanged (Jira DB-names-then-env, Anthropic env-then-DB with empty-string fallthrough, Resend env-only) (D-02, D-04)"
    - "scripts/verify-credential-cutover.ts still exists (D-03)"
    - "Dead-helper deletion landed in its own commit with message exactly: refactor(08): delete old inline credential paths after resolver cutover verified (INTG-08, HYG-01) (D-03)"
    - "npx tsc --noEmit and npm test both exit 0 after deletion; no new test files added unless an existing test failed (D-04, HYG-03)"
    - "INTG-08 is checked off in REQUIREMENTS.md only after evidence plus the HYG-01 deletion both exist (D-04)"
    - "WINDOWS.md entries 1, 2, and 3 are closed via gsd-tools windows fixed (D-03)"
  artifacts:
    - path: app/api/jira/search/route.ts
      provides: "Deleted symbol getJiraCredentials (and unused jira-config.repo import); live POST unchanged"
    - path: app/api/jira/fields/route.ts
      provides: "Deleted symbol oldInlineCredentialBlock; live GET and 503 split unchanged"
    - path: .planning/phases/08-intg-08-credential-cutover/COVERAGE.md
      provides: "No-new-external-API declaration for Phase 8"
    - path: .planning/phases/08-intg-08-credential-cutover/08-01-SUMMARY.md
      provides: "Sanitized cutover stdout + HYG-01 commit hash"
  key_links:
    - from: scripts/verify-credential-cutover.ts
      to: lib/integrations/credentials.ts
      via: "oldJiraPath/oldAnthropicPath compared to resolveJiraCredentials/resolveAnthropicCredentials"
      pattern: "match: yes"
    - from: app/api/jira/search/route.ts POST
      to: resolveJiraCredentials
      via: "live search still resolves via the shared resolver after helper deletion"
      pattern: "resolveJiraCredentials("
    - from: app/api/jira/fields/route.ts GET
      to: companyJiraConfig then resolveJiraCredentials
      via: "config-row presence check then resolver; two 503 strings preserved"
      pattern: "Jira chưa cấu hình"
  prohibitions:
    - statement: "Do not delete without script evidence"
      status: unresolved
    - statement: "Do not change lib/integrations/credentials.ts or resolver precedence"
      status: unresolved
    - statement: "Do not delete scripts/verify-credential-cutover.ts"
      status: unresolved
    - statement: "Do not remove fields-route live companyJiraConfig 503 split"
      status: unresolved
    - statement: "Do not commit .env.local or paste DATABASE_URL / tokens into docs"
      status: unresolved
---

<!-- planner-discipline-allow: getJiraCredentials -->
<!-- planner-discipline-allow: oldInlineCredentialBlock -->
<!-- planner-discipline-allow: companyJiraConfig -->
<!-- planner-discipline-allow: match: no -->

<objective>
Close INTG-08: prove every configured tenant's old inline Jira path matches the unified resolver, then delete the two marked-dead helpers in a dedicated HYG-01 commit.

Implements locked CONTEXT groups D-01 Cutover Evidence Gate, D-02 Deletion Scope, D-03 Commits & Leftovers, D-04 Verification. Discretion: use the RESEARCH-verified Windows invocation; grep decides Anthropic leftovers; single plan.

Purpose: INTG-08's defining clause is verified per configured company before the old paths are deleted.
Output: recorded script stdout (OK/UNSET only), HYG-01 deletion of getJiraCredentials and oldInlineCredentialBlock, INTG-08 checked off, WINDOWS 1–3 closed.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/08-intg-08-credential-cutover/08-CONTEXT.md
@.planning/phases/08-intg-08-credential-cutover/08-RESEARCH.md
@.planning/phases/08-intg-08-credential-cutover/08-PATTERNS.md
@.planning/phases/08-intg-08-credential-cutover/08-VALIDATION.md
@.planning/phases/08-intg-08-credential-cutover/COVERAGE.md
@.planning/phases/03-integration-clients/03-04-SUMMARY.md
@scripts/verify-credential-cutover.ts
@lib/integrations/credentials.ts
@app/api/jira/search/route.ts
@app/api/jira/fields/route.ts
@app/api/jira/test/route.ts

<interfaces>
From lib/integrations/credentials.ts — do not edit this module:
export async function resolveJiraCredentials(
  companyId: number | null,
  explicit?: { base_url_var: string; email_var: string; token_var: string },
): Promise<JiraCredentials | null>
Precedence frozen: Jira DB-names-then-env; Anthropic env-then-DB (empty string treated unset); Resend env-only.

From app/api/jira/search/route.ts live POST (KEEP): session null-company returns 401 with the existing Vietnamese config string; resolveJiraCredentials(user.company_id); missing creds 503 with the same string; jql required 400; searchIssues; custom-fields debug log; integrationErrorResponse catch.

From app/api/jira/fields/route.ts live GET (KEEP, including jira-config.repo import):
companyJiraConfig(user.company_id) then if !cfg?.base_url_var return 503 { error: 'Jira chưa cấu hình' };
resolveJiraCredentials(user.company_id) then if !creds return 503 { error: 'Thiếu env vars' }.

From app/api/jira/test/route.ts (DO NOT TOUCH unless leftover inline value-resolution grep hits): process.env[cfg.*_var] reads at lines 63–67 are name-level diagnostic presence checks, not credential resolution. Values flow through resolveJiraCredentials(null, cfg).

From scripts/verify-credential-cutover.ts (KEEP, RUN only): missing DATABASE_URL prints set DATABASE_URL and re-run and returns 1; 5s pool timeout; per-row old vs new; always prints anthropic | ... | match: yes|no; exit 1 on any mismatch.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>08-01-01 End-to-end cutover evidence — one script path</name>
  <precondition>Repo-root .env.local exists as a file (do not open it to copy secrets). If the file is absent, halt — do not delete helpers.</precondition>
  <read_first>
    - scripts/verify-credential-cutover.ts (read-only evidence tool; D-01, D-03)
    - .planning/phases/08-intg-08-credential-cutover/08-CONTEXT.md (D-01 Cutover Evidence Gate)
    - .planning/phases/08-intg-08-credential-cutover/08-RESEARCH.md (verified Windows invocation; vacuous zero-row evidence)
    - .planning/phases/08-intg-08-credential-cutover/08-VALIDATION.md (task 08-01-01)
  </read_first>
  <files>scripts/verify-credential-cutover.ts</files>
  <action>
    Wire one operator path through every layer this phase uses: PowerShell → npx tsx with --env-file=.env.local → scripts/verify-credential-cutover.ts → PostgreSQL SELECT on company_jira_config → oldJiraPath versus resolveJiraCredentials per row → oldAnthropicPath versus resolveAnthropicCredentials → stdout match labels. Do not edit the script, the resolver, or any route in this task. Per D-01: never bake dotenv into the script; never copy DATABASE_URL or token values into the terminal log beyond what the script already prints (OK/UNSET only).

    Run exactly: npx tsx --env-file=.env.local scripts/verify-credential-cutover.ts
    Then print $LASTEXITCODE. Discretion: this is the RESEARCH-verified invocation on this Windows host. If tsx ignores --env-file, retry with node --env-file=.env.local plus tsx for the TypeScript file; still do not add a dotenv import.

    Capture stdout verbatim for 08-01-SUMMARY.md / VERIFICATION (D-01). Zero company_jira_config rows plus exit 0 plus the anthropic match: yes line is valid vacuous evidence. Missing DATABASE_URL (script prints set DATABASE_URL and re-run) or unreachable DB is a hard stop, not empty-success.

    Halt this plan's remaining tasks on non-zero exit or any stdout line containing match: no. Do not delete helpers in this task.
  </action>
  <verify>
    <automated>$out = npx tsx --env-file=.env.local scripts/verify-credential-cutover.ts 2>&amp;1 | Out-String; $code = $LASTEXITCODE; Write-Output $out; if ($code -ne 0) { exit $code }; if ($out -match 'match: no') { exit 1 }; if ($out -notmatch 'anthropic \|') { exit 1 }; exit 0</automated>
    <human-check>Confirm captured stdout uses only OK/UNSET labels and that no connection string or API token was pasted into chat or planning files (D-01).</human-check>
  </verify>
  <acceptance_criteria>
    - Script exit code is 0 (D-01)
    - No stdout line contains match: no (adjacency: mismatch HALTS; no force-delete)
    - anthropic match line is present; zero Jira rows is allowed (empty probe: vacuous match)
    - scripts/verify-credential-cutover.ts is unmodified (D-03)
    - No route files changed in this task
  </acceptance_criteria>
  <done>Per-tenant (or vacuous) old-vs-new match is proven end-to-end and recorded; deletion remains gated.</done>
</task>

<task type="auto">
  <name>08-01-02 Delete dead helpers after evidence + leftover greps</name>
  <precondition>Task 08-01-01 recorded exit 0 and zero match: no lines. If that gate failed, skip this task entirely (D-01).</precondition>
  <read_first>
    - app/api/jira/search/route.ts (dead getJiraCredentials lines 8–26; unused companyJiraConfig import line 3; live POST lines 28–84)
    - app/api/jira/fields/route.ts (live GET lines 8–27 KEEP; dead oldInlineCredentialBlock lines 29–43 DELETE)
    - app/api/jira/test/route.ts (touch only if leftover value-resolution grep hits; diagnostic process.env reads stay)
    - .planning/phases/08-intg-08-credential-cutover/08-PATTERNS.md (D-02 deletion scope, two-503 split)
    - .planning/phases/08-intg-08-credential-cutover/08-VALIDATION.md (task 08-01-02)
  </read_first>
  <files>app/api/jira/search/route.ts, app/api/jira/fields/route.ts</files>
  <action>
    Immediately before deleting, re-run npx tsx --env-file=.env.local scripts/verify-credential-cutover.ts (A1 freshness). If exit is non-zero or any line is match: no, halt — do not delete (D-01). Do not merge mismatches.

    Per D-02, delete only the two marked-dead functions and the comments that mark them:
    1. app/api/jira/search/route.ts: remove lines 8–26 (INTG-08 comment plus getJiraCredentials). Remove the jira-config.repo import (line 3) because only the dead helper used it. Keep resolveJiraCredentials import and the entire POST handler unchanged.
    2. app/api/jira/fields/route.ts: remove lines 29–43 (INTG-08 comment plus oldInlineCredentialBlock). Keep the jira-config.repo import and the live GET two-503 split verbatim (Jira chưa cấu hình vs Thiếu env vars). File should end after the GET handler.

    Do not edit app/api/jira/test/route.ts unless a leftover inline env-value resolution grep (not the diagnostic name-presence checks) finds a second credential path. Do not edit lib/integrations/credentials.ts. Do not delete scripts/verify-credential-cutover.ts (D-03).

    Grep Anthropic routes for leftover inline API-key fallbacks (settings-repo anthropic key lookup inside app/api). If the grep is empty, do not invent deletions (D-02 discretion). If matches remain, delete those leftovers in this same HYG-01 commit only.

    Commit only the route deletion(s) with this exact message per D-03: refactor(08): delete old inline credential paths after resolver cutover verified (INTG-08, HYG-01)
  </action>
  <verify>
    <automated>rg "getJiraCredentials|oldInlineCredentialBlock" app/; if ($LASTEXITCODE -ne 1) { exit 1 }; rg "resolveJiraCredentials\(" app/api/jira/; if ($LASTEXITCODE -ne 0) { exit 1 }; rg "companyJiraConfig" app/api/jira/search/route.ts; if ($LASTEXITCODE -ne 1) { exit 1 }; rg "companyJiraConfig" app/api/jira/fields/route.ts; if ($LASTEXITCODE -ne 0) { exit 1 }; rg "Jira chưa cấu hình" app/api/jira/fields/route.ts; if ($LASTEXITCODE -ne 0) { exit 1 }; rg "Thiếu env vars" app/api/jira/fields/route.ts; if ($LASTEXITCODE -ne 0) { exit 1 }; git diff --exit-code -- lib/integrations/credentials.ts; if ($LASTEXITCODE -ne 0) { exit 1 }; if (-not (Test-Path scripts/verify-credential-cutover.ts)) { exit 1 }; npx vitest run app/api/jira/test lib/integrations/credentials.unit.test.ts; if ($LASTEXITCODE -ne 0) { exit 1 }; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getJiraCredentials and oldInlineCredentialBlock have zero matches under app/ (D-02)
    - resolveJiraCredentials( appears in search, fields, and test route files (D-02, D-04)
    - search/route.ts has no companyJiraConfig; fields/route.ts still has companyJiraConfig plus both 503 strings (D-02)
    - git diff of lib/integrations/credentials.ts is empty (D-02)
    - scripts/verify-credential-cutover.ts still exists (D-03)
    - Dedicated HYG-01 commit exists with the exact D-03 message
    - npx vitest run app/api/jira/test and npx tsc --noEmit exit 0 (D-04)
  </acceptance_criteria>
  <done>Dead helpers are gone in a bisectable HYG-01 commit; live Jira handlers and the fields 503 split remain.</done>
</task>

<task type="auto">
  <name>08-01-03 Full suite, INTG-08 checkoff, WINDOWS close</name>
  <precondition>The D-03 HYG-01 deletion commit from 08-01-02 exists on HEAD (or the working tree already lacks both dead helpers after that commit).</precondition>
  <read_first>
    - .planning/REQUIREMENTS.md (INTG-08 checkbox line and traceability row)
    - .planning/WINDOWS.md (open ids 1, 2, 3)
    - .planning/phases/08-intg-08-credential-cutover/08-PATTERNS.md (REQUIREMENTS checkbox after-state; windows fixed command)
    - .planning/phases/08-intg-08-credential-cutover/08-VALIDATION.md (task 08-01-03)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <action>
    Per D-04 and HYG-03: run npm test then npx tsc --noEmit. Do not add new tests unless an existing test failed because of the deletion; if so, fix the test or the deletion mistake without changing resolver precedence.

    Confirm leftover greps still clean (dead helper names absent under app/; three live Jira resolver call sites).

    Check off INTG-08 in .planning/REQUIREMENTS.md only after both 08-01-01 evidence and the 08-01-02 HYG-01 commit exist (D-04): mark the INTG-08 bullet [x], set the traceability row to Complete, set the footer checked-off count to 54/54. In the HTML comment, note script exit 0 plus the HYG-01 deletion commit. Do not paste DATABASE_URL or tokens.

    Close the three Phase 3 WINDOWS stubs via gsd-tools windows fixed 1 then 2 then 3 (D-03). Prefer gsd-tools on PATH; otherwise node $HOME/.cursor/gsd-core/bin/gsd-tools.cjs windows fixed followed by the numeric id. Do not hand-edit the ledger JSON out of sync. After the three calls, WINDOWS.md frontmatter open_count must be 0 and ids 1-3 status fixed.

    Paste the 08-01-01 stdout (OK/UNSET labels only) into 08-01-SUMMARY.md. Secret-scan that SUMMARY file only for connection-string URI schemes or raw token-shaped values and fail if any hit (D-01, D-04).
  </action>
  <verify>
    <automated>npm test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx tsc --noEmit; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg "getJiraCredentials|oldInlineCredentialBlock" app/; if ($LASTEXITCODE -ne 1) { exit 1 }; rg "^\- \[x\] \*\*INTG-08\*\*" .planning/REQUIREMENTS.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg "^open_count: 0$" .planning/WINDOWS.md; if ($LASTEXITCODE -ne 0) { exit 1 }; if (Test-Path .planning/phases/08-intg-08-credential-cutover/08-01-SUMMARY.md) { rg "postgres://|postgresql://" .planning/phases/08-intg-08-credential-cutover/08-01-SUMMARY.md; if ($LASTEXITCODE -ne 1) { exit 1 } }; exit 0</automated>
    <human-check>Spot-check 08-01-SUMMARY.md evidence: OK/UNSET labels only, INTG-08 checked off, WINDOWS open_count 0 (D-04).</human-check>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 with 0 failed (D-04, HYG-03)
    - npx tsc --noEmit exits 0
    - REQUIREMENTS.md INTG-08 is [x] and coverage footer is 54/54 (D-04)
    - WINDOWS.md open_count is 0 and entries 1, 2, 3 are fixed (D-03)
    - 08-01-SUMMARY.md records sanitized stdout and the HYG-01 commit hash
    - 08-01-SUMMARY.md contains no database connection URIs
  </acceptance_criteria>
  <done>INTG-08 is closed with evidence, deletion, green suite, and a clean WINDOWS ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → cutover script | .env.local values load into process.env; stdout must not echo secrets |
| Cutover script → PostgreSQL | Read-only SELECT of company_jira_config var **names** (not env values) |
| Script/docs → git | Planning artifacts and SUMMARY must never receive DATABASE_URL, tokens, or .env.local |
| Executor → Jira route files | Deletion must not remove live handlers or the fields 503 split |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | scripts/verify-credential-cutover.ts stdout, SUMMARY, PLAN docs | high | mitigate | Script already prints OK/UNSET only; executor records that stdout and secret-scans SUMMARY; never paste DATABASE_URL or tokens (D-01) |
| T-08-02 | Tampering | Dead-helper deletion without evidence | high | mitigate | Tracer + 08-01-02 precondition: non-zero exit or any match: no HALTS deletion; no force-delete (D-01) |
| T-08-03 | Tampering | app/api/jira/fields/route.ts live 503 split | high | mitigate | Delete only lines 29–43; keep companyJiraConfig and both Vietnamese 503 strings; grep-gated (D-02) |
| T-08-04 | Tampering | lib/integrations/credentials.ts precedence | high | mitigate | git diff --exit-code on credentials.ts; no resolver edits (D-02, D-04) |
| T-08-05 | Information Disclosure | git add of .env.local | high | mitigate | Do not stage .env.local; HYG-01 commit is route files only (D-03, D-04) |
| T-08-06 | Spoofing | Cutover against unintended DATABASE_URL | medium | accept | CONTEXT locks evidence to this host's .env.local; residual wrong-DB risk is operator env hygiene, not a code change |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs in this phase; executor must not add dependencies |
</threat_model>

<verification>
- 08-01-01: npx tsx --env-file=.env.local scripts/verify-credential-cutover.ts exits 0; no match: no; anthropic line present
- 08-01-02: dead helper names absent under app/; live resolver calls remain; fields 503 split remains; credentials.ts untouched; HYG-01 commit with exact D-03 message; vitest jira/test + tsc green
- 08-01-03: npm test green; INTG-08 [x]; WINDOWS open_count 0; SUMMARY has sanitized stdout only
</verification>

<success_criteria>
INTG-08 is checked off because every configured company (including the vacuous zero-row case) matched the resolver before the old paths were deleted, the HYG-01 commit removed only getJiraCredentials and oldInlineCredentialBlock, live Jira routes still use resolveJiraCredentials, and the existing test suite plus tsc pass.
</success_criteria>

## Artifacts this phase produces

This phase does not add runtime APIs. It deletes two unreachable symbols and records planning evidence.

| Path / symbol | Kind |
|---------------|------|
| getJiraCredentials in app/api/jira/search/route.ts | DELETED (plus unused jira-config.repo import) |
| oldInlineCredentialBlock in app/api/jira/fields/route.ts | DELETED |
| scripts/verify-credential-cutover.ts | KEPT (evidence tool) |
| lib/integrations/credentials.ts | UNCHANGED |
| .planning/phases/08-intg-08-credential-cutover/COVERAGE.md | Planning (no new external API) |
| .planning/phases/08-intg-08-credential-cutover/08-01-SUMMARY.md | Planning (sanitized stdout + commit hash) |
| .planning/REQUIREMENTS.md INTG-08 | Checkbox closed after evidence + deletion |
| .planning/WINDOWS.md ids 1–3 | Ledger closed via gsd-tools windows fixed |

<output>
Create `.planning/phases/08-intg-08-credential-cutover/08-01-SUMMARY.md` when done
</output>
