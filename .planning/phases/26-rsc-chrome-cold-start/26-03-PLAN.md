---
phase: 26-rsc-chrome-cold-start
plan: 03
type: tdd
wave: 3
depends_on: [26-02]
files_modified:
  - lib/db.cold-start.test.ts
  - .planning/phases/26-rsc-chrome-cold-start/COLD-START.md
autonomous: true
requirements: [PERF-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A vitest node suite times getDb() connect plus assertMigrated plus seedAuthData against TEST_DATABASE_URL after module reset; it does not run schema migrate (PERF-03, D-04)."
    - "The test fails when measured p95 is greater than 5000ms; COLD-START.md records samples, p95, and the 2000ms local target (D-04)."
    - "No second Pool is added to production code; getPool() from lib/db.ts is the only pool under test (D-05)."
    - "When TEST_DATABASE_URL is unset, the timing suite is skipped and COLD-START.md still exists with a SKIP verdict (D-04, RESEARCH Environment Availability)."
    - "This plan does not change PageChrome, layout.tsx, Sidebar, or route wrappers (D-01, D-02, D-03, D-06)."
  artifacts:
    - path: "lib/db.cold-start.test.ts"
      provides: "p95 cold-start integration (PERF-03, D-04)"
    - path: ".planning/phases/26-rsc-chrome-cold-start/COLD-START.md"
      provides: "Recorded budget artifact (D-04)"
  key_links:
    - from: "lib/db.cold-start.test.ts"
      to: "lib/db.ts"
      via: "dynamic import getDb after vi.resetModules"
      pattern: "getDb"
    - from: "lib/db.cold-start.test.ts"
      to: "test/db.ts"
      via: "hasTestDb and TEST_DATABASE_URL"
      pattern: "hasTestDb"
  prohibitions:
    - statement: "Must not create a second production Pool or re-inline migrate into getDb (D-04, D-05)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add APM npm packages (D-05)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Record PERF-03 cold-start budget for getDb() after Phase 19 removed migrate from boot (D-04, D-05). Chrome decisions D-01, D-02, D-03, D-06 stay as shipped in 26-01/26-02.

Purpose: Make connect+assert cost visible with a CI-safe p95 gate.

Output: lib/db.cold-start.test.ts and COLD-START.md.

Note: RESEARCH allows this work after 26-01 independently; isolation none still sequences it after 26-02 on the main tree so waves do not overlap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-rsc-chrome-cold-start/26-RESEARCH.md
@.planning/phases/26-rsc-chrome-cold-start/26-PATTERNS.md
@.planning/phases/26-rsc-chrome-cold-start/26-CONTEXT.md
@lib/db.ts
@lib/db.getDb.boot.unit.test.ts
@lib/db.test.ts
@test/db.ts
@vitest.config.ts

Isolation none. TDD: test(26-03) then feat(26-03) per task. Never RED-only. Do not edit getDb() except if a bug blocks measurement — prefer measuring the existing boot path. Do not install packages (D-05). Do not add withCpmo (D-05).

<interfaces>
test/db.ts: TEST_DATABASE_URL, hasTestDb, closeTestPool(). Database name must end in _test.

lib/db.ts getDb(): if _client return it; new Pool({ connectionString: process.env.DATABASE_URL }); assertMigrated; assign _pool/_client; seedAuthData. getPool() awaits getDb() then returns _pool.

Cold-start loop (26-RESEARCH Pattern 4): SAMPLE_COUNT 20, P95_FAIL_MS 5000, timeout 120_000. Per sample: vi.resetModules(); process.env.DATABASE_URL = TEST_DATABASE_URL; t0 = performance.now(); const { getDb, getPool } = await import('@/lib/db'); await getDb(); push duration; const pool = await getPool(); await pool.end().

p95: sort copy ascending; index = Math.ceil(0.95 * length) - 1; return sorted[Math.max(0, idx)].

Warm-cache detection (RESEARCH A2): if 18+ of 20 samples are below 50ms, fail the test — resetModules or pool.end did not clear the singleton.

describe.skipIf(!hasTestDb) for the timing it(). afterAll closeTestPool().

COLD-START.md template (D-04):
# Cold Start Budget (PERF-03)
Target: p95 ≤ 2000ms (local connect + assertMigrated + seedAuthData)
CI fail threshold: p95 > 5000ms
Measured: ISO date
Environment: TEST_DATABASE_URL, vitest node project
Samples table, p95, Verdict PASS or SKIP (no TEST_DATABASE_URL)

Prefer Vitest over a tsx script (D-04). Hand-roll p95 (D-05).
</interfaces>
</context>

<feature>
  <name>getDb cold-start p95 budget</name>
  <files>lib/db.cold-start.test.ts, .planning/phases/26-rsc-chrome-cold-start/COLD-START.md</files>
  <behavior>
    When hasTestDb, p95 of 20 reset-module getDb() samples is less than 5000ms (PERF-03, D-04).
    COLD-START.md exists in the phase directory and records target 2000ms and fail 5000ms (D-04).
  </behavior>
  <implementation>
    Vitest node integration using TEST_DATABASE_URL, vi.resetModules, pool.end, no second pool (D-04, D-05).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="26-03-01">
  <name>End-to-end getDb cold-start p95 suite</name>
  <reversibility rating="reversible">Timing test and markdown artifact; production getDb path unchanged (D-04).</reversibility>
  <files>lib/db.cold-start.test.ts</files>
  <read_first>
  - .planning/phases/26-rsc-chrome-cold-start/26-RESEARCH.md (Pattern 4, Pitfall 3)
  - lib/db.ts (getDb / getPool)
  - lib/db.getDb.boot.unit.test.ts
  - lib/db.test.ts
  - test/db.ts
  </read_first>
  <behavior>
  - lib/db.cold-start.test.ts uses describe.skipIf(!hasTestDb), SAMPLE_COUNT 20, expect(p95(samples)).toBeLessThan(5000), and 120_000 timeout (PERF-03, D-04).
  - Each sample calls vi.resetModules, sets process.env.DATABASE_URL to TEST_DATABASE_URL, dynamically imports @/lib/db, awaits getDb(), then awaits getPool().end() (D-04, D-05).
  - Test fails if most samples are below 50ms (warm singleton) (D-04).
  - File does not construct Pool except via getDb/getPool (D-05).
  </behavior>
  <action>
  TDD. Isolation none. Two commits test(26-03) then feat(26-03). Never RED-only.
  RED: Add lib/db.cold-start.test.ts with the behaviors above. If the file is missing the test runner reports it missing — that is red. If you write the full test first, run it: skipIf is green when TEST_DATABASE_URL is unset (allowed); when the URL is set, the test must fail until the loop, p95 helper, and teardown exist. Commit test(26-03): red getDb cold-start p95.
  GREEN: Implement the loop, p95 helper, skipIf, afterAll closeTestPool, warm-cache assertion, and timeout. Do not add a second Pool to lib/db.ts. Do not call migrate inside the test. Do not add npm packages. Re-run npx vitest run --project node lib/db.cold-start.test.ts lib/db.getDb.boot.unit.test.ts. Commit feat(26-03): getDb cold-start p95 suite.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/db.cold-start.test.ts lib/db.getDb.boot.unit.test.ts</automated>
  </verify>
  <done>Cold-start suite measures connect+assert via resetModules and fails p95 above 5000ms (PERF-03, D-04, D-05).</done>
  <acceptance_criteria>
  - lib/db.cold-start.test.ts exists under the vitest node glob
  - source contains resetModules, getDb, and 5000
  - npx vitest run --project node lib/db.cold-start.test.ts exits 0 (skip or pass)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="26-03-02">
  <name>Write COLD-START.md budget artifact</name>
  <files>lib/db.cold-start.test.ts, .planning/phases/26-rsc-chrome-cold-start/COLD-START.md</files>
  <read_first>
  - .planning/phases/26-rsc-chrome-cold-start/26-PATTERNS.md (COLD-START.md template)
  - lib/db.cold-start.test.ts
  </read_first>
  <behavior>
  - A node test that always runs (not skipIf) reads COLD-START.md from the phase directory and expects it to contain PERF-03, 2000, and 5000 (D-04).
  - After a timed run, the file includes a samples table and a p95 line; if hasTestDb is false, Verdict is SKIP (no TEST_DATABASE_URL) (D-04).
  </behavior>
  <action>
  TDD. Two commits test(26-03) then feat(26-03). Never RED-only.
  RED: Add an always-on describe in lib/db.cold-start.test.ts that readFileSync the COLD-START.md path and asserts the required substrings. It fails while the file is missing. Commit test(26-03): red COLD-START.md artifact.
  GREEN: Write COLD-START.md using the template. If TEST_DATABASE_URL is set, run the timing test and paste real sample milliseconds plus measured p95 (Verdict PASS if p95 ≤ 5000). If unset, write Verdict SKIP (no TEST_DATABASE_URL) with empty samples and still document the 2000ms target and 5000ms fail threshold. Optionally have the timing it() append samples via writeFileSync after measurement so CI refreshes numbers — keep the always-on existence test. Do not add tsx or APM packages (D-04, D-05). Commit feat(26-03): record cold-start budget.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/db.cold-start.test.ts</automated>
  </verify>
  <done>COLD-START.md records the PERF-03 budget (target 2000ms, CI fail 5000ms) (D-04).</done>
  <acceptance_criteria>
  - .planning/phases/26-rsc-chrome-cold-start/COLD-START.md contains 2000 and 5000
  - npx vitest run --project node lib/db.cold-start.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → PostgreSQL | TEST_DATABASE_URL must name a _test database |
| Test → lib/db.ts | Dynamic import after resetModules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Tampering | TEST_DATABASE_URL | high | mitigate | Reuse test/db.ts suffix _test guard; do not point timing at production (D-04) |
| T-26-09 | Denial of Service | 20× getDb loop | low | accept | skipIf without URL; 120s timeout; CI-only cost; no production request-path change |
| T-26-10 | Information Disclosure | COLD-START.md | low | accept | Artifact records timings only, no connection strings |
| T-26-SC | Tampering | npm installs | high | mitigate | No new packages; p95 is a local helper (D-05) |

ASVS L1: test-only; production getDb unchanged.
</threat_model>

<verification>
- 26-03-01: cold-start suite green or skipIf
- 26-03-02: COLD-START.md present with 2000/5000
- Wave: npx vitest run --project node lib/db.cold-start.test.ts lib/db.getDb.boot.unit.test.ts
- Confirm lib/db.ts still calls assertMigrated and not a migrate loop (existing boot unit test)
</verification>

<success_criteria>
Cold-start connect+assert is measured with a recorded p95 budget; CI fails above 5000ms; no second pool and no new npm (PERF-03, D-04, D-05).
</success_criteria>

<output>
Create `.planning/phases/26-rsc-chrome-cold-start/26-03-SUMMARY.md` when done
</output>
