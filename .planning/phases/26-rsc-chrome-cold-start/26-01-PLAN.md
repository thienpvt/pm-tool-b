---
phase: 26-rsc-chrome-cold-start
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - components/layout/PageChrome.tsx
  - components/layout/PageLoadingShell.tsx
  - components/layout/PageErrorShell.tsx
  - lib/rsc-chrome.gate.test.ts
  - app/dashboards/portfolio/page.tsx
  - app/dashboards/portfolio/loading.tsx
  - app/dashboards/pm/page.tsx
  - app/dashboards/pm/loading.tsx
  - app/weekly/periods/page.tsx
  - app/weekly/periods/loading.tsx
  - app/audit/page.tsx
  - app/audit/loading.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
  - modules/dashboards/ui/pm/PmDashboardPage.tsx
  - modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx
  - modules/audit/ui/AuditLogPage.tsx
  - modules/audit/ui/AuditLogPage.component.test.tsx
autonomous: true
requirements: [PERF-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Root layout stays a Server Component and is not marked client (D-01)."
    - "PageChrome is a Server Component that owns flex flex-col lg:flex-row min-h-screen bg-slate-50, renders the existing client Sidebar as a child, and wraps children in a main landmark (D-01, D-02, D-06)."
    - "Pilot routes app/dashboards/portfolio, app/dashboards/pm, app/weekly/periods, and app/audit are Server Component wrappers of PageChrome around the existing client module page (PERF-02, D-03)."
    - "Pilot module pages keep data hooks and KPI widgets client-side and no longer duplicate the outer chrome shell (D-03, D-06)."
    - "No new npm packages; Sidebar still fetches /api/auth/me on the client; ops/admin companies are not given withCpmo (D-02, D-05)."
    - "New chrome markup uses only font weights 400 and 600 (D-06)."
  artifacts:
    - path: "components/layout/PageChrome.tsx"
      provides: "Server page frame (D-01, D-02)"
      exports: ["PageChrome"]
    - path: "components/layout/PageLoadingShell.tsx"
      provides: "Server loading spinner markup (D-03)"
      exports: ["PageLoadingShell"]
    - path: "components/layout/PageErrorShell.tsx"
      provides: "Server error panel markup (D-03)"
      exports: ["PageErrorShell"]
    - path: "lib/rsc-chrome.gate.test.ts"
      provides: "PERF-02 source gate for converted routes"
    - path: "app/dashboards/portfolio/page.tsx"
      provides: "Pilot RSC wrapper (PERF-02)"
  key_links:
    - from: "components/layout/PageChrome.tsx"
      to: "components/layout/Sidebar.tsx"
      via: "server parent renders client Sidebar"
      pattern: "Sidebar"
    - from: "app/dashboards/portfolio/page.tsx"
      to: "components/layout/PageChrome.tsx"
      via: "PageChrome wraps PortfolioDashboardPage"
      pattern: "PageChrome"
    - from: "app/dashboards/portfolio/loading.tsx"
      to: "components/layout/PageLoadingShell.tsx"
      via: "route loading chrome"
      pattern: "PageLoadingShell"
  prohibitions:
    - statement: "Must not mark app/layout.tsx as a Client Component (D-01)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not move /api/auth/me into a Server Component (D-02)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not import PageChrome, PageLoadingShell, or PageErrorShell from a Client Component module page (D-03)"
      status: unresolved
      verification: flagged-unverified
    - statement: "Must not add npm packages or a second pool (D-05)"
      status: unresolved
      verification: flagged-unverified
---

<objective>
Prove PERF-02 on four pilot routes: server PageChrome owns the static shell, client Sidebar stays the nav leaf, and dashboard/weekly/audit module pages render content only (D-01, D-02, D-03, D-05, D-06). Cold-start budget is 26-03 (D-04).

Purpose: Lock the RSC boundary on a thin vertical slice before the 26-02 rollout.

Output: PageChrome plus loading/error shells, rsc-chrome.gate.test.ts, four pilot wrappers and loading.tsx files, stripped pilot module pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-rsc-chrome-cold-start/26-CONTEXT.md
@.planning/phases/26-rsc-chrome-cold-start/26-RESEARCH.md
@.planning/phases/26-rsc-chrome-cold-start/26-PATTERNS.md
@.planning/phases/26-rsc-chrome-cold-start/26-UI-SPEC.md
@app/layout.tsx
@components/layout/Sidebar.tsx
@app/dashboards/portfolio/page.tsx
@modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
@modules/dashboards/backend/dashboards-module-split.test.ts
@vitest.config.ts

Isolation none. Sequential on the main tree. TDD: two commits per task, test(26-01) then feat(26-01). Never a RED-only task. Do not convert remaining chrome routes (26-02). Do not add lib/db.cold-start.test.ts (26-03 / D-04). Do not install packages (D-05).

<interfaces>
PageChrome (new Server Component, no client directive) per 26-PATTERNS and 26-UI-SPEC:

export function PageChrome({ children, projectId, mainClassName = 'flex-1 overflow-auto' }: { children: React.ReactNode; projectId?: string; mainClassName?: string })

Outer div className exactly: flex flex-col lg:flex-row min-h-screen bg-slate-50
Child 1: default import Sidebar from @/components/layout/Sidebar with projectId forwarded when provided.
Child 2: main with className={mainClassName} wrapping children.

PageLoadingShell({ message: string }): spinner w-8 h-8 border-2 border-blue-500 border-t-transparent rounded-full animate-spin plus p.text-slate-400.text-sm with message. No client directive.

PageErrorShell({ message: string }): AlertTriangle h-8 w-8 text-muted-foreground plus p.text-sm.text-slate-600. No client directive. Import lucide-react AlertTriangle (already in the project).

Pilot success-path mainClassName (verbatim from current module main):
- Portfolio dashboard, PM dashboard, weekly periods, audit: flex-1 p-4 lg:p-6 lg:p-8 overflow-auto
Pilot loading.tsx mainClassName: flex-1 flex items-center justify-center
Pilot loading copy (verbatim): Loading dashboard… / Loading dashboard… / Loading weekly periods… / Loading audit log…

Gate file lives under lib/ so the vitest node project include {lib,app,eslint,modules}/**/*.test.ts collects it. Do not add components/layout/*.test.ts — that glob is not in the node project.

Windows: relative(root, file).replace(/\\/g, '/') like kysely-migration.gate.test.ts.

Sidebar stays 'use client' and keeps fetch('/api/auth/me') in useEffect (D-02). Do not edit Sidebar.tsx or app/layout.tsx except to confirm they stay as they are.

Vitest: npx vitest run --project node (no -x; Vitest 4 ignores it).
</interfaces>
</context>

<feature>
  <name>Server PageChrome on pilot v2 routes</name>
  <files>components/layout/PageChrome.tsx, lib/rsc-chrome.gate.test.ts, app/dashboards/portfolio/page.tsx</files>
  <behavior>
    Pilot app page files are Server Components that contain PageChrome and the existing module page import (PERF-02, D-01, D-03).
    PageChrome source has no client-component directive and imports Sidebar (D-01, D-02).
  </behavior>
  <implementation>
    Add server chrome components, convert four pilot app/page.tsx files, strip duplicated shells from matching module pages, add pilot loading.tsx (D-01, D-02, D-03, D-06).
  </implementation>
</feature>

<tasks>

<task type="tracer" tdd="true" id="26-01-01">
  <name>End-to-end portfolio dashboard through PageChrome</name>
  <reversibility rating="costly">PageChrome becomes the shared chrome contract every later v2 route must use (D-01, D-02).</reversibility>
  <files>components/layout/PageChrome.tsx, components/layout/PageLoadingShell.tsx, components/layout/PageErrorShell.tsx, lib/rsc-chrome.gate.test.ts, app/dashboards/portfolio/page.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx, modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx</files>
  <read_first>
  - .planning/phases/26-rsc-chrome-cold-start/26-PATTERNS.md (PageChrome, gate, strip)
  - .planning/phases/26-rsc-chrome-cold-start/26-UI-SPEC.md (preserve-existing shell)
  - .planning/phases/26-rsc-chrome-cold-start/26-RESEARCH.md (Pattern 1–2, Pitfall 1)
  - app/layout.tsx
  - components/layout/Sidebar.tsx
  - app/dashboards/portfolio/page.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.tsx
  - modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx
  - lib/repositories/kysely-migration.gate.test.ts
  </read_first>
  <behavior>
  - lib/rsc-chrome.gate.test.ts: PageChrome.tsx, PageLoadingShell.tsx, and PageErrorShell.tsx do not match /^['"]use client['"]/m; PageChrome source contains Sidebar and min-h-screen bg-slate-50; app/layout.tsx does not match that client-directive regex (D-01, D-02, D-03).
  - Gate CHROME_ROUTES is exactly app/dashboards/portfolio/page.tsx for this task; that file does not match the client-directive regex and contains PageChrome and modules/dashboards/ui/portfolio/PortfolioDashboardPage (PERF-02, D-03).
  - Gate: components/layout/Sidebar.tsx does match the client-directive regex; package.json has no datadog or newrelic dependency keys (D-02, D-05).
  - PortfolioDashboardPage.tsx code lines do not import @/components/layout/Sidebar; loading/error/success branches do not render the outer min-h-screen flex shell; hook-driven loading keeps the existing spinner classes and Loading dashboard… copy as inner markup (D-03, D-06).
  - PortfolioDashboardPage.component.test.tsx no longer mocks the layout Sidebar and no longer queries test id sidebar; existing KPI/filter assertions still pass (D-03).
  </behavior>
  <action>
  TDD. Isolation none. Merge RED and GREEN. Two commits: test(26-01) then feat(26-01). Never RED-only.
  RED: Create lib/rsc-chrome.gate.test.ts following the kysely-migration.gate.test.ts walk/readFileSync style. Assert the three new layout files exist as Server Components, layout.tsx has no client-component directive, Sidebar.tsx still has one, package.json has no APM packages, and CHROME_ROUTES currently lists only app/dashboards/portfolio/page.tsx as a Server PageChrome wrapper. Add a module-source assertion that PortfolioDashboardPage.tsx does not import the layout Sidebar. In the portfolio component test, change expectations so the page no longer exposes test id sidebar (remove that assertion and the Sidebar mock). Run npx vitest run --project node lib/rsc-chrome.gate.test.ts and the jsdom portfolio component test — they must fail because the chrome files and wrapper do not exist yet. Commit test(26-01): red PageChrome portfolio tracer.
  GREEN: Add PageChrome, PageLoadingShell, and PageErrorShell per interfaces (D-01, D-02, D-03, D-06). Do not add a third font weight on the new shells. Convert app/dashboards/portfolio/page.tsx into a Server Component that renders PageChrome with mainClassName flex-1 p-4 lg:p-6 lg:p-8 overflow-auto around PortfolioDashboardPage. Strip the duplicated outer shell and Sidebar usage from all branches of PortfolioDashboardPage; keep ERROR_COPY, KPI tiles, and hooks client-side; inner loading/error markup stays class-equivalent (D-03, D-06). Do not import the server shells from the client module. Do not edit app/layout.tsx or Sidebar.tsx. Do not wrap login, landing, or operations. Commit feat(26-01): PageChrome portfolio tracer.
  dashboards-module-split.test.ts already uses toContain on the module path — leave it unless it fails; if it fails, keep the module-path assertion and do not require a default-re-export.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/rsc-chrome.gate.test.ts && npx vitest run --project jsdom modules/dashboards/ui/portfolio/PortfolioDashboardPage.component.test.tsx && npx vitest run --project node modules/dashboards/backend/dashboards-module-split.test.ts</automated>
  </verify>
  <done>Portfolio dashboard HTML chrome is owned by server PageChrome; the client module renders body content only (PERF-02, D-01, D-02, D-03).</done>
  <acceptance_criteria>
  - PageChrome.tsx has no client-component directive and renders Sidebar inside a main landmark shell
  - app/dashboards/portfolio/page.tsx contains PageChrome and has no client-component directive
  - npx vitest run --project node lib/rsc-chrome.gate.test.ts exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="26-01-02">
  <name>Convert PM dashboard and weekly periods pilots</name>
  <files>lib/rsc-chrome.gate.test.ts, app/dashboards/pm/page.tsx, modules/dashboards/ui/pm/PmDashboardPage.tsx, modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx, app/weekly/periods/page.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.tsx, modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx</files>
  <read_first>
  - components/layout/PageChrome.tsx from 26-01-01
  - lib/rsc-chrome.gate.test.ts from 26-01-01
  - app/dashboards/pm/page.tsx
  - modules/dashboards/ui/pm/PmDashboardPage.tsx
  - app/weekly/periods/page.tsx
  - modules/weekly/ui/periods/WeeklyPeriodsPage.tsx
  </read_first>
  <behavior>
  - Gate CHROME_ROUTES also includes app/dashboards/pm/page.tsx and app/weekly/periods/page.tsx; both lack the client-component directive and contain PageChrome plus their current module import paths (PERF-02, D-03).
  - PmDashboardPage.tsx and WeeklyPeriodsPage.tsx do not import the layout Sidebar; inner loading copy stays Loading dashboard… and Loading weekly periods… (D-03, D-06).
  - Matching component tests drop the Sidebar mock and sidebar test id assertions and still pass content assertions (D-03).
  </behavior>
  <action>
  TDD. Two commits test(26-01) then feat(26-01). Never RED-only.
  RED: Expand CHROME_ROUTES and module Sidebar-import assertions for the two new pages. Update the two component tests the same way as portfolio. Tests fail until wrappers exist. Commit test(26-01): red pm and weekly-periods chrome.
  GREEN: Convert both app pages to Server PageChrome wrappers with mainClassName flex-1 p-4 lg:p-6 lg:p-8 overflow-auto. Strip duplicated shells from both module pages. Keep widgets and hooks client. Do not import server shells from those client files. Do not add withCpmo anywhere (D-05). Commit feat(26-01): pm and weekly-periods PageChrome.
  weekly-module-split and dashboards-module-split P1 tests use toContain — keep module path strings.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/rsc-chrome.gate.test.ts && npx vitest run --project jsdom modules/dashboards/ui/pm/PmDashboardPage.component.test.tsx modules/weekly/ui/periods/WeeklyPeriodsPage.component.test.tsx && npx vitest run --project node modules/dashboards/backend/dashboards-module-split.test.ts modules/weekly/backend/weekly-module-split.test.ts</automated>
  </verify>
  <done>PM dashboard and weekly periods pilots use PageChrome (PERF-02, D-03, D-06).</done>
  <acceptance_criteria>
  - app/dashboards/pm/page.tsx and app/weekly/periods/page.tsx contain PageChrome
  - matching module pages do not import the layout Sidebar
  - listed vitest commands exit 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true" id="26-01-03">
  <name>Audit pilot plus loading.tsx on all four pilots</name>
  <files>lib/rsc-chrome.gate.test.ts, app/audit/page.tsx, app/audit/loading.tsx, app/dashboards/portfolio/loading.tsx, app/dashboards/pm/loading.tsx, app/weekly/periods/loading.tsx, modules/audit/ui/AuditLogPage.tsx, modules/audit/ui/AuditLogPage.component.test.tsx</files>
  <read_first>
  - .planning/phases/26-rsc-chrome-cold-start/26-PATTERNS.md (loading.tsx Pattern 3)
  - app/audit/page.tsx
  - modules/audit/ui/AuditLogPage.tsx
  - modules/audit/ui/AuditLogPage.component.test.tsx
  </read_first>
  <behavior>
  - Gate CHROME_ROUTES includes app/audit/page.tsx as a Server PageChrome wrapper; AuditLogPage.tsx does not import the layout Sidebar (PERF-02, D-03).
  - Gate PILOT_LOADING lists the four loading.tsx files; each lacks the client-component directive and contains PageChrome and PageLoadingShell with the route-specific copy from 26-UI-SPEC (D-03, D-06).
  - AuditLogPage.component.test.tsx drops Sidebar mock and sidebar test id; remaining audit table assertions pass (D-03).
  </behavior>
  <action>
  TDD. Two commits test(26-01) then feat(26-01). Never RED-only.
  RED: Expand the gate for audit page plus four loading.tsx files. Update the audit component test. Tests fail until files exist. Commit test(26-01): red audit chrome and pilot loading.tsx.
  GREEN: Convert app/audit/page.tsx like the other pilots (success mainClassName flex-1 p-4 lg:p-6 lg:p-8 overflow-auto). Strip AuditLogPage shell. Add four loading.tsx files composing PageChrome (centered main) and PageLoadingShell with verbatim messages Loading dashboard…, Loading dashboard…, Loading weekly periods…, Loading audit log…. Do not add loading.tsx on non-pilot routes. Do not convert remaining Sidebar routes. Commit feat(26-01): audit PageChrome and pilot loading.tsx.
  audit-module-split.test.ts uses toContain — keep the module path.
  </action>
  <verify>
    <automated>npx vitest run --project node lib/rsc-chrome.gate.test.ts modules/audit/backend/audit-module-split.test.ts && npx vitest run --project jsdom modules/audit/ui/AuditLogPage.component.test.tsx</automated>
    <human-check>After sign-in, open /dashboards/portfolio, /dashboards/pm, /weekly/periods, and /audit. Each shows the existing Sidebar NAV and the same page titles/copy. Navigate between two pilots and confirm the loading spinner copy matches the table above. Confirm login and operations still have no PageChrome Sidebar frame (D-06).</human-check>
  </verify>
  <done>Four pilots have server chrome and route-level loading shells (PERF-02, D-03, D-06).</done>
  <acceptance_criteria>
  - four pilot app/page.tsx files contain PageChrome
  - four pilot loading.tsx files contain PageLoadingShell
  - npx vitest run --project node lib/rsc-chrome.gate.test.ts exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC page | Untrusted navigation; chrome markup is static |
| Client Sidebar → /api/auth/me | Session still fetched in the browser (D-02) |
| Server PageChrome props | Only children, optional projectId, mainClassName cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Information Disclosure | PageChrome props | medium | mitigate | Pass only children, optional public route projectId, and className strings — no session secrets (D-02) |
| T-26-02 | Elevation of Privilege | Sidebar auth | high | mitigate | Keep /api/auth/me on the client Sidebar; do not add server session chrome (D-02) |
| T-26-03 | Tampering | Client module imports | medium | mitigate | Gate forbids chrome-route pages from using a client-component directive; module pages must not import PageLoadingShell/PageErrorShell/PageChrome (D-03) |
| T-26-04 | Elevation of Privilege | operations/admin | high | mitigate | Do not wrap operations; do not add withCpmo to ops/admin companies (D-05) |
| T-26-SC | Tampering | npm installs | high | mitigate | No install tasks; gate asserts package.json has no APM packages (D-05) |

ASVS L1: authentication and access control stay on existing API wrappers; this plan only moves static markup.
</threat_model>

<verification>
- 26-01-01: portfolio PageChrome tracer green
- 26-01-02: pm + weekly periods green
- 26-01-03: audit + four loading.tsx green
- Wave: npx vitest run --project node lib/rsc-chrome.gate.test.ts
- Isolation none. TDD RED then GREEN per task.
</verification>

<success_criteria>
Pilot v2 chrome is server-rendered via PageChrome; Sidebar remains a client leaf; no new npm; layout.tsx unchanged (PERF-02, D-01, D-02, D-03, D-05, D-06).
</success_criteria>

<output>
Create `.planning/phases/26-rsc-chrome-cold-start/26-01-SUMMARY.md` when done
</output>
